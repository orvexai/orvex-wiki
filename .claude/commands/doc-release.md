---
description: Gather a release changelog (git log / merged PRs / closed Linear issues since the previous version) and file it as a versioned release-notes page in the living wiki. Routes through doc-amend on the DATED/APPEND branch — a new version is a NEW dated child, never an amend of a prior one.
argument-hint: <version — e.g. "v1.1"; omit to infer the next version from git tags>
---

You are filing a release changelog into the project's living Docmost wiki as a versioned release-notes page: **$ARGUMENTS**

`release-notes` is **dual-mode** (see `{project-root}/_bmad/doc/data/taxonomy.md` §release-notes): a living **landing** page (`Release Notes`, updated in place each ship) plus one **dated/append version child** per release (`Release Notes vX.Y`, slug `release-notes-vX-Y`). This command files a **new version child** — that is genuinely-new dated content (taxonomy §5 per-occurrence dedup), **not** an amend of a prior version, and **not** ephemeral scratch.

1. **Parse the version** from `$ARGUMENTS` (e.g. `v1.1` → title `Release Notes v1.1`, slug `release-notes-v1-1`; semver dots → hyphens). If omitted, infer the next version from `git tag --sort=-v:refname` / the landing's latest pointer; confirm only if ambiguous. This version is the page's legitimate append-only key (analogous to an ADR's `NNNN-` sequence) — **not** a banned `-v2`/`-final` suffix.
2. **Pre-flight:** `docmost-cli auth status --output json` (exit non-zero → ask the user to authenticate, then stop).
3. **Gather the changelog** for everything shipped since the previous version: `git log <prev-tag>..HEAD` (commits + merge commits), merged PRs (`gh pr list --state merged --base <release-branch>` over that range), and closed Linear issues for the milestone/cycle. Bucket each entry into **Added / Changed / Fixed / Deprecated / Removed / Security / Breaking changes**, and distil a 10-second **Highlights** lead (the 2–3 highest-impact, user-facing changes).
4. **Fill the per-version template** `{project-root}/_bmad/doc/data/doc-type-templates/release-notes.md`: Highlights → the seven change buckets → **Links** for traceability. Per `data/citations-and-crosslinks.md`, link driving PRDs/ADRs by **Docmost page mention** (not bare URLs), Linear issues by **ID/URL**, and the prior release `Release Notes vX.(Y-1)` by page mention. P4 no-obsolescence-narration is **EXEMPT** here — past-tense "Released YYYY-MM-DD" is correct on a dated version page.
5. **Route** via `skill:doc-amend` on the **DATED/APPEND branch**: doc-amend's find-before-create asks "does *this version* already exist?" — a prior version is **not** a duplicate, so its amend-in-place reflex is **suspended** and it CREATEs a NEW dated child titled `Release Notes vX.Y` (slug `release-notes-vX-Y`) under the living `Release Notes` section landing, at `--status draft` (NEVER `--status canonical` — AI never self-promotes; promotion is `/doc-ratify`, which ships it to canonical and updates the landing's latest pointer).
6. **Surface** a readable summary — the page title/url/slug, the bucket counts, and that it lands as DRAFT pending human ratification. **No raw JSON.** Ask **one** clarifying question at most (e.g. only if the version or the previous-version baseline is genuinely ambiguous). If `docmost-cli` is unavailable, WARN and name the unsynced local draft; do not silently keep it local-authoritative.
