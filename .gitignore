.env
.env.dev
.env.prod
/data
# compiled output
dist
node_modules/

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

.nx
.nx/installation
.nx/cache
.cache/linear/

# Local-only: symlink to the superproject cache. MUST NOT be committed —
# ArgoCD refuses to render a repo containing an out-of-bounds symlink.
.cache

# Claude Code agent worktrees (ephemeral build scratch, never committed)
.claude/worktrees/

# Playwright captured sessions (real auth cookies — never commit)
apps/client/e2e/.auth-state.json
apps/client/e2e/.sweep-auth-state.json
apps/client/e2e/.*-auth-state.json
