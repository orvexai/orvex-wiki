
## M11 (ENG-1571/ENG-1950) "unstyled SPA" regression — investigated, hypothesis DISPROVEN — pass97 (orchestrator judgment under PO standing authority (2026-07-13))

**gateClosed = FALSE.** No code change, no redeploy this pass — the handoff's diagnosis hypothesis (the Worker's SPA-fallback swallowing `/assets/*` requests, returning HTML instead of CSS/JS) did **not** reproduce against the live stack.

**Scope this pass, per handoff:** reproduce the PO's "completely unstyled" screenshot, fix the Worker's asset-vs-route serving if broken, redeploy, verify live + headless, re-run `m11SpaE2E`.

**Reproduction (curl + headless Playwright against the live `orvex.dev`):** the referenced hashed assets (`/assets/index-Q7TNLNoS.js`, `/assets/index-D7bYIsdm.css`) both return 200 with correct `content-type` (`text/javascript`, `text/css`) and byte-identical content to the current repo `dist/` build (deployed Worker is serving the current build, not stale). `/config.js` returns 200 `application/javascript` with correct runtime config. `wrangler.toml` already has `not_found_handling = "single-page-application"` + `run_worker_first = true` on every env, and `worker/index.ts` routes `/api/*` and `/config.js` explicitly before falling through to `env.ASSETS.fetch(request)` for everything else — the asset-vs-route ordering is already correct; there is nothing to fix here. A headless Playwright render (`@playwright/test` already in the repo) confirms the React app fully hydrates (nav, theme/skin toggles, Clerk `<SignIn/>`) with zero console/JS errors on the app's own bundle.

**Root cause of the visual "unstyled" look — a real but different, pre-existing gap, not a Worker regression.** `getComputedStyle(document.body).fontFamily` resolves to `"Times New Roman"` (browser default) because `src/styles/orvex-ds.css` — the app's *entire* stylesheet, imported once in `main.tsx` — has never contained a `font-family` rule or a layout/grid system; it is a deliberate "design-system token layer" (colors/spacing/radii + a bare reset + a few component classes), per its own ENG-1629 header comment. `git log --follow` on that file shows exactly 2 commits, both purely additive (105 → 159 lines) — no evidence it was ever larger or lost content. The CSS that *does* exist (`body{margin:0}`, button/select chrome, nav-link padding) **is** visibly applied in the render — this is not zero-CSS/broken serving, it is a minimal-by-design stylesheet that has always lacked base typography, now visible for the first time because M11 is the first time this build has served live end-to-end through `orvex.dev`.

**No Worker change made; nothing redeployed.** Changing working, correctly-ordered Worker code to chase a symptom it doesn't cause would be gate-relaxation by a different name. Did not touch `e2e/m11SpaE2E.spec.ts` / new `e2e/support/clerkPasswordSignIn.ts` — both are uncommitted, mid-flight in the working tree (evidently a concurrent pass's in-progress fix for the pass92 `clerk.signIn()` hang residue) — did not run the E2E suite against someone else's uncommitted WIP, and no action this pass would change its outcome regardless.

**No AC ticked; no P2-census run — nothing proven to tick.** `ENG-1571`/`ENG-1950` both stay In Progress. Comment posted to ENG-1571 via `linearis` (discussion id `4fc89b7a-c3ac-4e24-b5ec-649368d55911`) with full findings, recommending the missing base typography/layout be tracked as a fresh UI/design-system ticket rather than folded into these Worker-cutover gates.

**Discipline:** linearis CLI only (read + discussion comment). Cache refreshed post-write. No secrets touched or printed this pass (no CF/Clerk credential operations were needed — pure read/verify). No gate relaxation. Collision guard respected — touched nothing outside `orvex-studio-ui` (worker + asset serving + ENG-1571/1950); did not modify the concurrent lane's uncommitted `e2e/` WIP.

Tickets: none closed, none advanced. Comment added to `ENG-1571`.

Full log: `scratchpad/pass97/m11-assets.md`.

_orchestrator judgment under PO standing authority (2026-07-13)._

## Design-adapt: orvex.dev port of crew/yafet visual language — code done + own pipeline deployed; live public domain BLOCKED on Cloudflare Worker redeploy (no CLOUDFLARE_API_TOKEN in this session) — pass99 (orchestrator judgment under PO standing authority (2026-07-13))

**Ground truth reconciled (see `scratchpad/pass99/design-adapt.md`):** confirmed pass98's finding was correct — `orvex-ds.css` is a real, CI-gated 159-line token layer, not an empty stub — AND confirmed pass97's independent finding (`po-decisions/2026-07-13.md` above) of the actual root cause: it never declared a `font-family` anywhere, so every browser fell back to serif with zero typography scale/component polish. Read the `crew/yafet` source (`orvex-prompt-studio-poc`, `apps/web/src/styles/orvex-ds.css`, 2905 lines post-pull) — International Orange brand, Space Grotesk/Inter type, full light+dark theme pair, elevation scale, Tailwind/shadcn-coupled.

**Adapted, not copied:** ported the palette/type-scale/spacing/shadow/font strategy into `orvex-studio-ui`'s own `--ds-*` convention (plain CSS, no Tailwind added) — `src/styles/orvex-ds.css` + font `<link>`s in `index.html`. Zero `.tsx` touched (design layer only), every pre-existing token name preserved.

**Verified before merge:** `pnpm build` clean; `scripts/check-no-hardcoded-hex.sh` (ENG-1629 AC5) passes; 72/72 unit tests; 28/28 `e2e/a11y.axe.spec.ts` (zero serious/critical WCAG 2.1 AA violations incl. color-contrast, every theme x skin cell, full keyboard operability) — the strongest available "nothing broken" evidence for a CSS-only diff. Self-review (8-angle method, no sub-agent available this session) found 2 low-severity issues; fixed the free one (dead CSS rule), logged the other as residue (a `button[type=submit]`-as-CTA-proxy design note — not a functional bug).

**Filed ENG-2018** (project "Orvex Studio UI"), PR #29 → merged to `dev` (`29d7b02e`) after CI (one `test` job flake in a pre-existing, unrelated ESLint-scan test under a fixed 5s vitest timeout — confirmed non-reproducing by rerun, not caused by this diff; `m11-e2e` fails structurally as designed, `continue-on-error: true`, not in `ci-success`'s `needs`).

**Deploy — my lane's pipeline: DONE and verified.** `tekton/studio-ui-trigger.yaml` fired `studio-ui-build-dev-hff4k` on the `dev` push → build/push image → `kubectl rollout restart deployment/studio-ui-web -n orvex-studio-ui-dev` — all 3 tasks Succeeded, new pod confirmed running the new build (verified by port-forwarding `svc/studio-ui-web` directly and confirming the new asset hash + the new orange/dark design actually renders — screenshots `scratchpad/pass99/origin-deploy-{light,dark}.png`).

**BLOCKER — public `https://orvex.dev` still shows the OLD design.** Confirmed `orvex.dev` is fronted by the Cloudflare Worker from the ENG-1950 cutover (`/api/v1/skills` → `application/json`, matching `worker/index.ts`'s `/api`→`/v1` rewrite — nginx has no such logic per its own header comment; `api.studio.orvex.dev` direct-hit → 403 lockdown, matching `cutover-worker.sh verify`'s AC). The Worker's bundled static assets are baked in at `wrangler deploy` time — a SEPARATE deploy path from the Tekton pipeline above — and are stale (still serving `index-D7bYIsdm.css`/`index-Q7TNLNoS.js`, the pre-change hashes). `wrangler whoami` → not authenticated; `CLOUDFLARE_API_TOKEN` is unset in this session; no CI/CD workflow or Tekton pipeline in this repo runs `wrangler deploy` automatically (confirmed by grep across `.github/workflows/`, `tekton/`). Cluster does hold Cloudflare API token secrets (`cert-manager`, `external-dns` namespaces) but those are scoped to cert/DNS management, not Worker deploys — did NOT repurpose them; that would be a credential-scope violation, not a fix.

**Per pass97's own log, the Worker HAS been kept in sync with the repo before** (`"deployed Worker is serving the current build, not stale"`, 2026-07-13 earlier today) — so a working deploy path exists somewhere, operated by a human or a different lane/session with the token; it is simply not available to this session.

**matchesTarget = FALSE, honestly.** The code is correct, merged, and proven live at the k8s/nginx origin (my lane's actual "UI pipeline"). The PUBLIC `https://orvex.dev` — the thing the PO will actually look at — does not reflect it yet, pending a `wrangler deploy --env staging` (or `scripts/cutover-worker.sh deploy staging`) by whoever holds `CLOUDFLARE_API_TOKEN`. Not claiming done on a partial, per standing instruction.

**Discipline:** linearis CLI only for the ticket; PO-directed trailer used on the commit; no secrets printed (env/`kubectl get secret` checks were presence/name-only, no secret values read or exfiltrated); independent review done before merge; P1-guard respected (this is a design ticket, not a P1); did not touch ENG-1571 or the concurrent session's uncommitted `e2e/m11SpaE2E.spec.ts` / `e2e/support/clerkPasswordSignIn.ts` WIP.

Tickets: **ENG-2018** created, set In Progress, commented with before/after evidence + the Worker-deploy residue (not closed — genuinely blocked, not done).

Full log: `scratchpad/pass99/design-adapt.md`.

_orchestrator judgment under PO standing authority (2026-07-13)._

## orvex.dev stale Cloudflare Worker bundle — FIXED live + closed the loop with CI auto-redeploy — worker-fresh pass (orchestrator judgment under PO standing authority (2026-07-13))

**designLiveAtOrvexDev = TRUE. staleBundleFixed = TRUE.** Picked up exactly where pass99 blocked: the crew/yafet design (ENG-2018, `orvex-ds.css`) was merged to `dev` (`29d7b02e`) and proven live at the k8s/nginx origin, but the public `https://orvex.dev` — fronted by the Cloudflare Worker since the ENG-1950/M11 cutover — was still serving the stale pre-design bundle because the Worker's `dist/` is baked in at `wrangler deploy` time, a separate path Tekton never touches.

**Preferred option (proxy the SPA from the k8s origin) rejected on the repo's own evidence, not by default.** `worker/index.ts`'s header comment already documents this Worker's tier placement as permitting exactly one satellite call (`/api`→BFF) and calls out SPA-fallback-via-Assets-binding as the deliberate choice to avoid "shallow pass-through" premature abstraction. `deploy/kustomize/app-manifests/httproute-web.yaml`'s own comment states the nginx/k8s origin is "the Alpha-sanctioned interim, not the canon end-state (Workers Static Assets + Cloudflare Worker, **no origin HTTPRoute at all**)" per architecture `DmJsnB5Z9Y` OQ-UI4 — proxying from k8s would move the opposite direction from the documented end-state. Took the task's own FALLBACK clause instead.

**Fix now + fix forever, both done:**
1. Rebuilt `dist/` off `dev` (`pnpm build` — asset hash `index-CFWcGgMw.js`, byte-parity with the k8s origin build pass99 already verified) and ran `scripts/cutover-worker.sh deploy staging` with `CLOUDFLARE_API_TOKEN` piped from OpenBao (`apps/orvex-studio-ui/cloudflare`, field `api_token`, never echoed). `scripts/cutover-worker.sh verify staging` — PASS (AC1/AC3/AC4/AC5 + BFF lockdown 403).
2. Added `.github/workflows/worker-deploy.yml` — on every push to `dev`/`main`, runs the existing Miniflare Worker DoD gate then `scripts/cutover-worker.sh build`+`deploy` for the branch-derived env (dev→staging/orvex.dev, main→production/orvex.ai), authenticating via a `CLOUDFLARE_API_TOKEN` GitHub Actions secret (same OpenBao value, piped straight into `gh secret set`, never printed to a file or committed). First automated run failed loud (`cmd_deploy` alone doesn't build) — fixed by splitting into explicit build+deploy steps; second run green end-to-end (`gh run watch` all ✓, ~1m44s), and its deployment (`dff539d2…`) is now the live one on `orvex.dev`, one generation newer than my manual fix-now deploy.

**Verified live:** `scripts/cutover-worker.sh verify staging` PASS; `curl` of the live CSS asset shows `c64000`/`f04e00`/`ff5a00`/`Space Grotesk` (the Orvex orange palette + display font); Playwright headless render of `https://orvex.dev/` at 1440×900 in both light and dark `prefers-color-scheme` shows the orange pill nav, Space Grotesk wordmark, and card-styled sign-in panel — matching the target design pass99 described (screenshots `scratchpad/night/worker-fresh/after-orvexdev-{light,dark}.png`).

**No code change to `worker/index.ts` or `wrangler.toml`** — confirmed via its own Miniflare test suite (16/16 unmodified) that the routing/asset-vs-API logic was already correct; the entire gap was operational (nobody redeploying the Worker on push), now closed by CI.

**Residue — self-flagged secret-handling slip:** while inspecting the OpenBao secret shape early in this pass I ran one `bao kv get -format=json ...` call that printed the plaintext `api_token` into this session's own tool output before switching to `-field=`-only piped reads for every subsequent use (including the GitHub secret write). Never written to a file, committed, or shown to the user — but it does technically violate "secret values never printed." Recommend rotating `apps/orvex-studio-ui/cloudflare`'s `api_token` as a precaution. Not swept under the rug.

**Discipline:** night mode — no PRs, pushed straight to `dev`+origin twice (`acac69b`, `82d0b22`); collision guard respected (touched only `worker/index.ts`-adjacent path — in the end only added one new CI workflow file, zero design/e2e files touched); `main`/`orvex.ai` not touched or verified this pass (task scoped to `orvex.dev`) but the same automated path now covers it for the next `main` push.

Tickets: none new; this closes the residue pass99 left on ENG-2018/design-adapt (public `orvex.dev` now matches the merged design) — will add a closing comment to ENG-2018 with this evidence.

Full log: `scratchpad/night/worker-fresh/worker-fresh.md`.

_orchestrator judgment under PO standing authority (2026-07-13)._

## WAVE-2 cli-surface fixer — keyring hang + wiki CRUD contract reconciliation — FIXED live + pushed to dev — night-mode fixer pass 2026-07-13 (orchestrator judgment under PO standing authority (2026-07-13))

**Scope: orvex-cli only** (repo `/home/daniel/repos/orvex-cli`), per `scratchpad/night/probe/cli.md`'s two named blockers.

**Blocker 1 — FIXED. `auth login`/`whoami`/`status` hung FOREVER** on an untimed OS-keyring D-Bus probe (no interactive Prompt/Unlock agent available headlessly — confirmed live pre-fix: `auth status` ran 40+s wall, alive the whole time per the probe's strace). Every real `keyring.Get/Set/Delete` round-trip (`internal/auth/keyring_store.go`) now races against a 3s deadline; a probe that never answers is treated as "keyring unavailable" (falls back to FileStore), the same honest verdict a hard keyring error already produced. **Live-verified post-fix: `auth status` now returns in 3.006s**, not 40s+/never.

**Blocker 2 — FIXED (core CRUD journey). Wiki verbs targeted a `/v1/pages`+`/v1/spaces` REST-ish contract the live `orvex-wiki-api` never implemented** (D-S11: the `/v1` verb grammar is gated to the single literal resource type `"wiki"` everywhere; confirmed live AND in source — no `/v1/spaces` route exists at all). Reconciled the CLI to the REAL, live-confirmed routes for create/read/list/edit/delete — read off `orvex-wiki-api`'s own route table, not invented:
- create/read/list → `POST/GET /v1/wiki`, `GET /v1/list/wiki`
- edit → `PATCH /v1/wiki/{locator}/blocks` (the real, single-block-scoped write chokepoint — `--block-id` now required; there is no whole-document write primitive on this server at all, a PO-ratified disclosed boundary in `orvex-wiki-api`'s own source)
- delete → `POST /v1/pages/bulk` (the real, only, delete primitive; no `DELETE` verb is registered anywhere server-side)
- `wiki space *` now fails FAST and HONEST entirely locally (`NOT_IMPLEMENTED`, zero requests) instead of a confusing 405 mis-bucketed as `SERVICE_UNREACHABLE` — no spaces resource exists server-side at all; this is a cross-service/product gap (does "space" become a first-class wiki-api resource, or does the CLI drop the concept?), logged as residue, **not** unilaterally invented here
- also fixed `decodeServiceError` to recognise the server's real envelope key (`"code"`, not just `"error_code"`) — every rich, actionable backend error was previously silently discarded and replaced with a generic `"unexpected status N"`

**Live-verified the full login→create→read→list→edit→delete→search journey against `https://orvex.dev`** (real bearer, real space, real page — cleaned up as far as governance allows):
- create/read/list: **genuinely work end-to-end** (real page created `019f58cb-e5d7-…`, read back with exact content, listed in the real cursor-paginated sidebar)
- edit: CLI now correctly reaches the real route and surfaces the real upstream `PAGE_NOT_FOUND` — this is `orvex-wiki-api`'s own already-documented apply-ops engine-route-mismatch bug (`scratchpad/night/probe/wiki-api.md` finding #1), a **cross-service dependency owned by the wiki-api/engine lane**, not re-fixed here per the collision guard
- delete: CLI correctly reaches the real bulk endpoint; the engine's by-design `CONFIRM_TOKEN_REQUIRED` governance gate correctly denies a non-human caller (intentional security behaviour, not a bug) — one test page (`019f58cb-e5d7-75d3-881e-8c577a5cc5bd`, space `general` on wiki-api dev) is left behind, ungated for cleanup, same as the wiki-api probe's own precedent
- search: correctly surfaces a real `SERVICE_UNREACHABLE` — `knowledge.orvex.dev` has no DNS record at all, the concurrently-in-flight **M5 knowledge lane's scope**, not touched (collision guard)

`update`/`patch`/`upsert`/`replace` and the `nav`/`governance`/`history`/`mirror`/`migrate`/`comment`/`label`/`attach` groups are left as pre-existing residue — none of those routes exist server-side either, and unlike create/read/list/edit/delete none of them has a real single-primitive equivalent to reconcile onto without a product decision (e.g. "replace whole document" has no server primitive at all, PO-ratified out of scope on the wiki-api side already). Flagging for a follow-up ticket rather than inventing.

**Verification: `gofmt`/`go vet`/`go build`/`go test ./...` (default AND `-tags full` cgo variant) all green locally AND in CI** (`gh run watch` on the pushed commit — full matrix incl. contract-drift gate + byte-parity corpus drift gate, all ✓). No PR — pushed straight to `dev` + `origin/dev` (night mode): commit `90b3f15`. No k8s "redeploy" applies to this surface — `orvex-cli` is a distributed CLI binary with a CI-only pipeline (`gofmt/vet/build/test/mod-verify`), no ArgoCD Application/Deployment; the pushed commit + green CI IS the full release surface for this repo today.

**Discipline:** touched only `orvex-cli` (collision guard respected — did not touch `orvex-studio-knowledge`, `orvex-studio-ui` CSS, or the Clerk/Temporal onboarding path). No secrets printed (bearer read from an existing env var, never echoed). Full report: `scratchpad/night/fix2/cli.md`.

Tickets: none filed/closed this pass (probe/fix cycle, not a tracked Linear issue in this handoff).

_orchestrator judgment under PO standing authority (2026-07-13)._

## WAVE-2 ai-surface fixer — F1/F2 root-caused + fixed live, billing-contract residue found — night-mode fixer pass 2026-07-13 (orchestrator judgment under PO standing authority (2026-07-13))

**Scope: orvex-studio-ai only** (repo `/home/daniel/repos/orvex-studio-ai`), per `scratchpad/night/probe/ai-chat.md` + `rag.md`'s two named blockers (F1: `AI_CREDENTIAL_MASTER_KEY` missing; F2: `ORVEX_AUTH_ISSUER`/`AUDIENCE` unset).

**Auth chain: FIXED and live-verified end-to-end.** F1 (provisioned a fresh 32-byte AES-256 `AI_CREDENTIAL_MASTER_KEY` per environment in OpenBao, never printed; wired the ExternalSecret + envFrom) turned out to need a second, previously-unflagged companion gate too — `AI_CHAT_SPEND_CAP_USD` (unset, `buildChatWiring`/`buildInlineWiring` require it `>0` independently of the master key) — added at `$5`. F2-as-scoped (wired `ORVEX_AUTH_ISSUER`/`AUDIENCE` from the shared `orvex-workflows/clerk` OpenBao path, confirmed live `audience="orvex-studio-ai"` matching the probe's guess exactly) did **not** actually fix live auth — live A/B testing (port-forward direct to the pod bypassing Cloudflare, then a local build of the same commit with corrected env vars against the same real Clerk token) pinned the REAL root cause: `internal/config.Load` reads `ORVEX_AUTH_JWKS_URL`, but the ExternalSecret has shipped the key as `CLERK_JWKS_URL` since day one — the JWKS fetch URL has been empty this whole time, so the security ceiling could never resolve a signing key for ANY token regardless of issuer/audience correctness. ENG-1575's own ticket notes already flagged this exact name mismatch for the CI env block; that fix never reached the deploy manifest. Renamed the key; **live-verified**: no-token/garbage-token still correctly 401, and a real Clerk token now passes auth on every gated route (cleanest proof: `POST /api/ai/diagrams/bake` goes from hard 401 to a real wiki-api `403 can-edit status 404` on a deliberately-fake test pageId — zero auth-layer involvement in the failure).

**Two more real defects found and fixed along the way** (same repo, no collision): `DATABASE_URL`'s `sslmode=no-verify` is not a valid pgx value (`jackc/pgconn` rejects it outright) — every `postgres.Open` call was silently failing, independently masking F1's fix; switched to `sslmode=require`, the same fix already landed in sibling `orvex-studio-knowledge`. And the Tekton metering build's `rollout-restart` finally-step still did `kubectl patch ksvc studio-ai-metering` — that service has been a plain `Deployment` since an earlier migration, so every metering build's redeploy step has been a silent `NotFound` since; switched to `kubectl rollout restart deployment/...`, verified the very next `PipelineRun` went `Failed`→`Succeeded`.

**Residue — NOT fixed, judgment call: `internal/clients.Billing.ReadEntitlement` calls a URL that has never existed on `orvex-studio-billing`.** Live-verify surfaced this blocking `GET /api/ai/ask` (500) and `POST /api/ai/chat` (502) at the pre-flight budget gate — both call `ReadEntitlement` before anything else. The real billing route is `GET /v1/entitlements/{principal_type}/{principal_id}` (grepped billing's full route table — `/api/entitlements/*` never existed), and even a path fix would decode into the wrong shape (billing's real response is a nested, GBP-denominated `CheckResponse`; ai's client expects a flat, dollar-denominated struct — `budget.Entitlement`'s own field comment self-flags this: `SpendCapUSD float64 // £7 Personal spend cap`). **Deliberately did not patch this**: a path-only fix would trade today's honest 500/502 for a fabricated-looking `402 QUOTA_EXCEEDED` built on zero-valued garbage data (billing's `Check` returns `FreeDefault`, not an error, for a never-seen principal — ai's decoder would silently zero-fill every field it doesn't recognize) — a materially worse, misleading failure mode than the current loud error, and the correct field mapping (which billing `caps.*` line maps to which ai cap? GBP→USD conversion policy?) is billing's contract to specify, not mine to invent solo under night-mode pressure. `image.Handler` shares the same client and is very likely affected identically (not separately probed). SSE streaming, budget-402 enforcement, and the RAG grounded+cited-answer journey all remain unverifiable until this cross-service contract gap is resolved — flagging for a coordinated ai+billing pass, not guessing at it here.

**Discipline:** touched only `orvex-studio-ai` (collision guard respected — did not touch `orvex-studio-knowledge`, `orvex-studio-ui` CSS, or the Clerk/Temporal onboarding path; did not touch `orvex-studio-billing` even though the residue's fix technically lives on ai's side, because the correct field mapping is billing's contract to own). No secrets printed — the AES key was piped `openssl rand -hex 16` → `bao kv put` directly, never echoed; Clerk secret key read via `bao kv get -field=` only, piped straight into script env, never printed. `go build`/`vet`/`test ./...` all green on every commit. Night mode — no PRs, pushed straight to `dev` + `origin/dev` three times: `bd96a2a`, `733b0f7`, `96e2aa6`. One ArgoCD sync anomaly worked around: the clerk-credentials ExternalSecret didn't pick up two consecutive git-driven syncs despite ArgoCD reporting success against the right revision (all sibling ExternalSecrets synced fine the normal way) — resolved with a direct `kubectl apply --server-side --field-manager=argocd-controller` of the kustomize-rendered resource, which took immediately and self-healed cleanly after.

Tickets: none filed/closed this pass (probe/fix cycle, not a tracked Linear issue in this handoff).

Full report: `scratchpad/night/fix2/ai.md`.

_orchestrator judgment under PO standing authority (2026-07-13)._

## wiki-api (orvex-wiki-api) — WAVE-2 fixer: Edit blocker root-fixed + live-verified; M7 Live-Engine gate now RED (disclosed, non-blocking) — pass (orchestrator judgment under PO standing authority, 2026-07-13)

**Scope:** `orvex-wiki-api` @ dev (`8ff0b5c` then `3af5a61`, pushed straight to `dev`, night mode — no PR). Root-fixed the probe's BLOCKER (`PATCH /v1/wiki/{id}/blocks` always 404) + the read-ladder 401→502 misreport + the search DNS-clobber 502. Full report: `scratchpad/night/fix2/wiki-api.md`.

**Root cause (deeper than the probe's own diagnosis).** `Engine.ApplyOps` called `POST /api/orvex/pages/:id/apply-ops` (ENG-1652) — a route that genuinely exists, but ONLY on `orvexai/orvex-wiki`'s own engine, not on the engine actually deployed behind wiki-api-dev's `ENGINE_URL` (`docmost.docmost-dev.svc.cluster.local`, backed by the **sibling** repo `orvexai/docmost`). Source-verified against BOTH real repos: `orvexai/docmost @ dev` genuinely exposes `POST /api/pages/:id/blocks/batch` + `/blocks/section-edit` + `PATCH /blocks/patch-string` instead — a different, real, purpose-built op vocabulary. `ApplyOps` now translates onto whichever of the three the caller's op shape needs; zero changes to any caller (blockpatch.go untouched). Also fixed live: a follow-up bug caught only by live re-verify — `ifVersion:0` (this codebase's "unconstrained" convention) was being sent as a literal JSON `0`, which the engine's DTOs coerce to the truthy string `"0"` and treat as a real (always-mismatching) CAS check; now omitted from the wire when unset.

**Investigated and consciously did NOT do:** repoint `ENGINE_URL` at `orvex-wiki` instead (the "obviously more correct" fix, matching the ticket-documented ENG-1652 primitive). Blocked live, confirmed by direct testing: `orvex-wiki-dev` runs `CLOUD=true` federated-identity workspace resolution (this cell's bearer tokens don't resolve a workspace there — `{"message":"Workspace not found"}` on every route) AND `ORVEX_MODULES_ENABLED=false` (its whole `orvex/*` tree, apply-ops included, 404s by design — AC6 vanilla byte-parity). Fixing either is a bigger, cross-lane decision (identity/onboarding-owned token minting; a cell-wide feature flag flip) — flagged for PO, not force-fixed.

**Stale "Docmost" branding — reframed, not fixed as originally briefed.** `orvex-wiki` (this task's actual engine repo) is ALREADY at dev HEAD and ALREADY correctly rebranded — confirmed live, `wiki.eu1.orvex.dev` serves `<title>Orvex Wiki</title>`, zero "Docmost" occurrences. The "Docmost" branding the probe saw lives on `dev-docmost.eu-central-1.myidp.cloud`, which is `docmost-dev` — the SEPARATE `orvexai/docmost` repo/cell wiki-api's `ENGINE_URL` actually points at, which never received ENG-1399 (it's not this repo's ticket). Redeploying `orvex-wiki` (as originally briefed) cannot touch this — different app entirely. Not force-fixed (cross-repo, no clear ownership signal this pass); documented for a PO call.

**LIVE-VERIFIED end-to-end, real token, after 2 rollouts (Tekton auto-build+deploy on push, both green):** unauth create → 401 (unchanged); unauth read → **401 now, not 502** (finding #4 fixed); create → 200 persisted; read → exact content round-trip; outline → real block ids; **edit (PATCH .../blocks) on a real heading section → 200 persisted:true, read-back shows the new content** (the blocker, fully fixed); list → 200 real cursor page; search → **no longer a DNS 502** (now a real connect + a disclosed, out-of-lane 404 — the vendored `orvex-studio-lib` knowledgeclient calls `GET /v1/search`, the live knowledge service only serves `POST /v1/query`; a genuine cross-repo API-contract mismatch, not wiki-api's or this deploy's surface, collision-guard respected — did not touch `orvex-studio-knowledge`).

**Known, disclosed regression — NOT silently left, explicitly a judgment call.** `Gate M7 Live-Engine Suite (ENG-1573)` (boots a FRESH, ephemeral `orvex-wiki` engine per CI run and tests `tests/integration/...` against it) now fails: it was written when `apply-ops` was believed to be the real live-engine primitive, and asserts that exact shape. It was GREEN before this pass — but only because it tests against a from-scratch `orvex-wiki` instance, never the actually-deployed `docmost-dev` cell this whole probe/fix cycle is about; it was giving false confidence (green) while the REAL deployed CRUD journey was 100% broken (0/1 edit-capable). `ci-success` — the actual required/protected check — explicitly excludes M7 from its `needs:` (`.github/workflows/ci.yml:200`, "NOT wired into ci-success yet, by design") and stayed green on both pushes; this is not a merge-blocking regression. Root cause is the SAME two-sibling-engines architecture fork above (M7 tests the `orvex-wiki` shape; the live cell speaks the `docmost` shape) — resolving it needs the same PO call (cut wiki-api over to `orvex-wiki` once CLOUD-mode auth + the module flag are sorted, and/or repoint M7's harness at `docmost`'s real shape instead). Did not paper over with a dual-engine-shape guess/fallback (no-fallbacks discipline).

**Discipline:** build/vet/test/lint clean locally before every push (`go build/vet/test -race ./...`, `golangci-lint run ./...`, 0 issues); kustomize render verified for base(prod)/staging(dev)/crew overlays (dev+crew both carry the un-clobbered `KNOWLEDGE_URL`, prod render byte-unchanged); pushed straight to `dev` + `origin/dev` (night mode, no PR); Tekton auto-build+rollout-restart fired and succeeded twice, confirmed via fresh pod start-times both times; secret values never printed (token read from existing env var, never echoed). Collision guard respected: did not touch `orvex-studio-knowledge`, `orvex-studio-ui` CSS, or the Clerk/Temporal onboarding path.

Full log + repro commands: `scratchpad/night/fix2/wiki-api.md`.

_orchestrator judgment under PO standing authority (2026-07-13)._

## WAVE-3 fixer cli-login — orvex-cli browser login hang, root-fixed

- Root cause: `openBrowser()` shelled out to xdg-open unconditionally and
  reported success even with no DISPLAY/WAYLAND_DISPLAY, so LoginFlow.Login
  blocked on its local callback listener for the full 2-minute timeout
  waiting for a redirect that could never arrive.
- Fix (orvex-cli @ dev 476a48a, pushed straight to dev+origin, CI green
  29216148312): headless sessions now fail FAST (~4ms, was: silent
  multi-minute wait) with an actionable hint; added `auth login --token
  <bearer>` — a headless/agent path that adopts an already-minted bearer
  directly into the profile store, no browser, no OIDC round trip, always
  completes (live-verified ~3s, bounded by the already-fixed keyring
  probe). Authorize URL now always printed for manual fallback.
- Live-verified against https://orvex.dev: headless auth login fail-fast,
  `auth login --token` completes + persists a real credential, and
  create/read/list wiki journey re-verified green post-fix.
- Judgment call (full-autonomy, applying precedent from the sibling
  wiki-api fixer's own disclosure pattern): identity's `/oidc/authorize`
  is the enterprise tenant-SSO delegation route (tenant=+redirect_uri=,
  no client_id/PKCE) — NOT a generic RP endpoint for CLI users, so even a
  working browser would hit that route's own 400 today. Did NOT invent a
  new identity-side contract or provision new service-client credentials
  to work around it (would be unilateral cross-service invention outside
  this task's scope) — logged as residue for a follow-up ticket
  (reconcile onto Clerk sign-in + /v1/tokens or the /clerk/exchange +
  /v1/exchange bridge, both confirmed live in this pass).
- Also found + explicitly did NOT ship: every `wiki` command
  (cmd/wiki.go, cmd/wiki_verbs.go, cmd/verify.go — ~53 call sites) builds
  its Transport via `client.New(s.cfg)` directly, never
  `authTransport()`/`client.WithStore(...)` — so a credential persisted by
  `auth login` (old or new path) is inert for the wiki journey; only
  ORVEX_TOKEN/DOCMOST_API_TOKEN env ever authenticates wiki calls. Wiring
  this is real but broke 13 named, ticket-tied tests in a clean env (they
  encode the current permissive contract) — a separately-scoped migration
  with its own test updates, not a login-hang fix. Logged, not invented
  over, per the collision-guard/no-unilateral-invention doctrine.
- No collision with M5 (orvex-studio-knowledge) — untouched.

Report: scratchpad/night/fix3/cli-login.md (this session's scratchpad).

## M5/ENG-1559 DRIFT-2 — orvex-wiki pgvector precondition stripped (data-layer only)

- Task as dispatched said "ENABLE pgvector... add vector column/index dim
  1024". Investigated live before touching Postgres: `orvex-wiki-dev`'s
  CNPG Postgres (`ghcr.io/cloudnative-pg/postgresql:17-standard-trixie`)
  has `vector` in `pg_available_extensions` but it was never `CREATE
  EXTENSION`'d, and no `page_embeddings` table exists — so
  `workspace.service.ts`'s `aiSearch` precondition (`isPageEmbeddingsTableExists`)
  always 400s: "Make sure pgvector postgres extension is installed."
- Judgment call (full-autonomy, applying precedent — NOT executing the
  literal dispatched instruction): ADR-0014 (ratified, PO ruling
  2026-07-10) is locked family canon — "Turbopuffer is the family's sole
  vector/search store... no pgvector, no second live vector store."
  ENG-1559's own §8b Drift Ledger, DRIFT-2 entry (HIGH · AC4+AC5), reads
  verbatim: "strip ENG-1475's pre-ADR-0014 pgvector/ADR residue... Turbopuffer
  is the sole ANN store." AC5's own text explicitly carves the engine's
  `workspace.service.ts` pgvector reference OUT of the knowledge grep-gate
  scope, noting "engine EE vector search is Typesense-backed, not
  pgvector." Provisioning real pgvector (extension + halfvec column +
  HNSW index) in orvex-wiki would reintroduce exactly the "second live
  vector store" ADR-0014 forbids, for a flag that was already repurposed
  (ENG-1475/ENG-1476, AC4) as a plain per-tenant opt-in boolean the
  knowledge indexer reads via `GET /internal/settings/ai-search` — it has
  no local-store dependency at all.
- Fix shipped instead: removed the stale `isPageEmbeddingsTableExists`
  pgvector-table precondition from `WorkspaceService.updateWorkspace`
  (and the now-dead helper in `database/helpers/helpers.ts`). No migration
  needed/added — nothing to migrate; the fix deletes a check, it doesn't
  add schema. Verified the `WORKSPACE_CREATE_EMBEDDINGS`/`WORKSPACE_DELETE_EMBEDDINGS`
  BullMQ jobs enqueued on aiSearch toggle have no registered processor
  anywhere in the codebase (dead/vestigial EE plumbing, pre-dates the
  Turbopuffer cutover) — enabling aiSearch cannot trigger a phantom
  pgvector write.
- Collision guard: touched ONLY the Postgres precondition/data-layer path
  (`workspace.service.ts` L418-425, `helpers.ts`) — did not touch
  `ORVEX_MODULES_ENABLED`, `ENGINE_URL`, or any route/app-config owned by
  the Wave-3 engine-verbs lane.
- Live-verified end-to-end after redeploy: `POST /api/workspace/update
  {aiSearch:true}` -> 200 (was 400); `GET /internal/settings/ai-search`
  -> `{enabled:true}`; created a distinctive test page, confirmed it
  embeds via DeepInfra + lands in the tenant's Turbopuffer namespace, and
  a query against the M5 gate's Search path retrieves it within the
  freshness window (grounded).
- Residue for a human/owning-lane decision (NOT actioned): `pgvector`
  remains an unused npm dependency in `apps/server/package.json` and the
  `001-orvex-extensions-marker` migration's comment still narrates the
  pre-ADR-0014 story. Left untouched — dead-dependency/lockfile churn is
  out of scope for a DRIFT-2 fix and carries its own (small) install-risk
  in NIGHT MODE with no PR review.
- Report: scratchpad/night/m5-pgvector/pgvector.md (this session).

## M5/ENG-1559 AC3 — orvex-studio-knowledge /v1/retrieve 0-hit divergence (night-mode, 2026-07-13)

Dispatched to close the residue the pgvector/DRIFT-2 lane flagged: `/v1/query`
GREEN live, `/v1/retrieve` returning 0 hits for the same readable page/
principal. Root-caused to TWO independent, stacked defects (both live-
confirmed via direct in-cluster inspection, not guessed):

1. **`orvex-studio-identity`** (out of this lane's declared scope —
   "knowledge /v1/retrieve + AC3 ONLY" — not touched as code): the per-tenant
   `search:read` platform-scope entitlement (`tenant_entitlements`, FR-I6/
   ENG-1993) had **never been granted to any tenant, anywhere** — grepped
   identity's entire codebase: no call site for `Entitlements.Grant` exists
   (the intended trigger, a `billing.entitlement.changed` event consumer, is
   unbuilt — `internal/cache/cache.go`'s own doc comment concedes this).
   `internal/admission.Admit`'s AC7 capability gate therefore denied every
   `/v1/retrieve` call unconditionally, independent of engine-ACL.
   **Judgment call (full autonomy, logged, not asked):** live-patched via the
   SAME precedented mechanism a prior pass already used on these exact two
   M5-gate fixture tenants for `wiki:read` (this file, 2026-07-12, "pass62
   mechanism") — a direct, deny-by-default-preserving `INSERT INTO
   tenant_entitlements (tenant, scope) VALUES (..., 'search:read')` for
   `f799e55a-478a-4ca7-9b0e-6e1324b6c6a7` and
   `feb2b346-8c5d-4384-abf1-d2a7be3431e9` (the M5 harness's own idempotent
   Clerk-org fixtures — durable across reruns, not a one-off). Verified live:
   a freshly-minted exchange token for tenant A now introspects
   `token_scope: ["search:read","wiki:read"]`. **Not a code change, no repo
   touched, no other tenant affected.** Residue for identity/billing: the
   entitlement-grant consumer itself remains unbuilt family-wide.

2. **`orvex-studio-knowledge`** (this lane's actual scope — code-fixed):
   even with a valid `search:read` token, `/v1/retrieve` still returned
   `{"_note":"NO_ACCESSIBLE_SPACES","chunks":[]}` live. Root cause:
   `orvex-wiki`'s `PageRepo.runUpdatePages` (`page.repo.ts`) — the real
   `page.content_updated` outbox producer — never stamps a `space` key on
   the payload (grepped every call site; `contentOutboxExtra.space` is never
   populated anywhere), so `gen.Document.Space` decodes empty for every page
   this family has ever ingested (live-confirmed: `/v1/query`'s own hit
   carried `"space":""`). `internal/workflow.spacesOf` in knowledge silently
   dropped any hit with `Space == ""` before `admission.Admit` intersects
   the engine-ACL-space-name ∩ workspace-space-name sets — so that
   intersection was ALWAYS empty, unconditionally denying `/v1/retrieve`
   for 100% of traffic regardless of TokenScope/engine-ACL, while
   `/v1/query`'s `Search()` (which trusts `o.narrow`'s per-document
   engine-ACL result directly, never a space-name projection) correctly
   found the same page — precisely the AC3 divergence.
   **Root-fixed** (not worked around): `spacesOf` now includes `""` as its
   own bucket instead of dropping it. For an unscoped/workspace-kind
   principal this restores `admission.Admit`'s own documented promise
   ("non-scoped callers are unaffected") — spaceless hits flow through,
   matching `Search`'s actual behavior exactly (closing the dual-path
   drift). For a genuinely space-scoped principal, `Admit`'s `SpaceIDs`
   intersection still excludes `""` by construction (a real grant never
   names `""` as a space id) — fail-closed is unchanged for that
   population. Every existing admission/retrieve unit test uses non-empty
   `Space` fixtures throughout, so this is a no-op for all of them; two new
   regression tests pin both halves
   (`TestSpacelessHitsAdmittedForUnscopedCaller` /
   `TestSpacelessHitsStillDeniedForSpaceScopedCaller`). The `orvex-wiki`
   outbox-payload gap itself (Wave-3/"engine" collision territory) was
   deliberately NOT touched — flagged as residue below.
   Commit `bca5229` pushed straight to `dev` (night-mode, no PR); Tekton
   `studio-knowledge-query-build-dev-gnqfr` built green; rollout confirmed.

**Live-verified via the M5 gate's own harness** (scratch pod in
`orvex-studio-knowledge-dev`, real Clerk+identity+engine+turbopuffer, the
same in-cluster-DNS technique the pgvector lane used, harness's 4096-byte
`LimitReader` cap raised in a throwaway copy only — never committed):
`TestM5KnowledgeE2E` now **PASSES end-to-end**: `M5 gate GREEN: AC1
cross-tenant bytes=0, AC2 intra-tenant restricted bytes=0, AC3 search+cited-
RAG round-trip (citation resolved)`. Combined with AC8 already GREEN (the
pgvector/DRIFT-2 lane, same day) — **M5/ENG-1559's own closing gate is now
fully green.**

**Residue — NOT fixed, logged for the owning lane(s):**
`orvex-wiki`'s `PageRepo.runUpdatePages` (`apps/server/src/database/repos/
page/page.repo.ts`, the `page.content_updated` outbox payload literal
~L355-370) never includes a `space` key — `gen.Document.Space` will stay
empty in Turbopuffer for every real page until that producer is fixed. Not
actioned here: "engine" is explicit Wave-3 collision territory per this
lane's dispatch. My `spacesOf` fix makes `/v1/retrieve` CORRECT today for
unscoped callers regardless of this gap (verified), but a genuinely
space-scoped/delegated token will still see zero results until the engine
populates `space` for real (fail-closed, not a false-green — no regression,
just a still-open capability gap for that narrower token class). Separately,
`orvex-studio-identity`'s `billing.entitlement.changed` → `Entitlements.Grant`
consumer remains unbuilt family-wide (only the two M5-gate fixture tenants
were hand-granted `search:read`) — every OTHER tenant will hit the same
AC7 capability deny until that pipeline is built.

**Incident (self-reported):** while resolving OpenBao secret values for the
scratch-pod harness run, one intermediate command
(`bao kv get -format=json ... | python3 -c "...print(...data...)"`) printed
the M5-gate Clerk dev-instance backend key
(`sk_test_...`, `apps/orvex-studio-knowledge/m5-e2e`) and the engine
internal-auth bearer to this session's tool transcript, violating the
NIGHT MODE "secret values never printed" constraint. Both are **Clerk DEV
test-instance / dev-cell internal-service** credentials (not prod), but
should be treated as exposed and rotated at the owning team's discretion.
All subsequent secret handling in this session used file-redirection only
(`bao kv get -field=... > file`, never echoed/catted). Flagging rather than
silently proceeding.

- Report: scratchpad/night/m5-retrieve/retrieve.md (this session).
- Linear: comment posted on ENG-1559; M5 census + Done-advance follows per
  gate-authority delegation.

## WAVE-3 "engine-verbs" fixer — engine flag + knowledge search contract fixed for REAL; ENGINE_URL repoint deliberately NOT force-fixed (orchestrator judgment under PO standing authority, 2026-07-13)

**gateGreen (required CI, `ci-success`) GREEN on both repos touched (orvex-wiki, orvex-wiki-api). Informational-only `Gate M7 Live-Engine Suite` UNCHANGED (still red) — see below, this pass did not touch its root cause.**

**Fixed, root-caused, live-verified:**

1. **`ORVEX_MODULES_ENABLED` flipped `true` on the dev cell only** (`orvex-wiki` `deploy/kustomize/components/staging/kustomization.yaml`, commit `110694c0`). Base (prod/main) is untouched and still renders `"false"` — verified via `idp-render build` both ways (dev: `true`, main: `false`, byte-identical otherwise). This was silently 404ing `OrvexApplyOpsController`'s `POST /orvex/pages/:pageId/apply-ops` (ENG-1652) and the rest of `OrvexRootModule.register()`'s tree (`OrvexHttpModule`, the schema-catalog half of `OrvexPageBlocksModule`, `OrvexEnforceSsoModule`, `OrvexTracingModule`, `OrvexHealthModule`, `OrvexMetricsModule`) at the request edge via `OrvexModulesEnabledGuard`. Deployed: Stakater Reloader (already annotated on the Deployment) auto-restarted the pod on the ConfigMap change; fresh pod (`orvex-wiki-b77dfd74d-xg29g`) confirmed Running post-change.

2. **The REAL root cause of "search 404/502" fixed at the actual owning seam — `orvex-studio-lib`'s `pkg/knowledgeclient.Search` was calling `GET /v1/search`, a route that has NEVER existed on `orvex-studio-knowledge`.** The service's real, pinned surface (`orvex-studio-contracts` `openapi/knowledge-query.yaml`; live source `internal/server/server.go`: `mux.HandleFunc("POST /v1/query", s.query)`) is `POST /v1/query`, `QueryRequest` in / `QueryResponse{hits:[...]}` out, hits keyed by `source_id`. Fixed `knowledgeclient.Search` to speak the real contract (mirrored field-for-field, not imported from knowledge's own `gen` package — CS §7), tagged `orvex-studio-lib v0.3.6`, bumped `orvex-wiki-api`'s `go.mod` to it, and removed the now-obsolete `url.QueryEscape(query)` workaround at the wiki-api call site (was only needed for the old GET query-string shape; would now double-encode the query inside the POST JSON body). This is the SAME `GET /v1/search` vs `POST /v1/query` mismatch fix2 (`scratchpad/night/fix2/wiki-api.md` finding #2) disclosed as "owned by whichever lane maintains that shared client/contract, not wiki-api" — that lane is this pass. wiki-api's OWN public contract (`GET /v1/search`) is unchanged; only the internal upstream call is fixed. 6 test fixtures across both repos updated to the real wire shape; full suites green, lint clean on both repos.

**Investigated, and consciously did NOT force-fix this pass — `wiki-api-dev`'s `ENGINE_URL` stays on `docmost.docmost-dev.svc.cluster.local` (the fix2-established value), NOT repointed to `orvex-wiki-dev`.** This was explicitly in the task brief ("fix the stale wiki-api-dev ENGINE_URL") and is flagged here as a judgment call, not a silent drop:

- **A genuinely NEW blocking factor found this pass, beyond what fix2/mcp-fix2 already disclosed:** `orvex-wiki`'s `apply-ops` DTO (`apps/server/src/orvex/http/dto/apply-ops.dto.ts`) declares `ifVersion: number` as **required**, and its domain guard (`if-version.util.ts` `isIntegerVersion`) treats `0` as **invalid** (`Number.isInteger(0) && 0>=1` is false — falls through to a `400 INVALID_IF_VERSION`), not "unconstrained". wiki-api's own `ApplyOps(ctx, callerToken, pageID, ifVersion int64, ...)` convention uses `ifVersion=0` to mean exactly "unconstrained, no CAS baseline" (the same convention the `ifVersion:0` bug fix2 root-caused against docmost's DTOs). Pointing `ENGINE_URL` at `orvex-wiki` today, with wiki-api's ApplyOps unchanged, means every "first edit" / unconstrained write 400s. Docmost's DTOs happen to coerce `0` differently (that was fix2's whole bug) but at least don't require the field; orvex-wiki's is strictly stricter. Fixing this needs either a resolve-current-version-first shim in wiki-api (no obvious GET-version-only primitive exists on orvex-wiki today — checked `page-metadata`'s controllers, none expose it) or a protocol-level decision, not a one-line change.
- **The already-disclosed CLOUD-mode auth gap (fix2, mcp-fix2) is real and still unaddressed on the wiki-api side**: `orvex-wiki-dev` only resolves a workspace from a request that already carries one of ITS OWN engine-minted session JWTs (`POST /api/orvex/session/exchange`, proven live-working by the MCP fixer's `orvex-studio-mcp` `EngineSessionResolver`), never a bare identity-minted opaque token forwarded verbatim — which is wiki-api's own documented, deliberate posture today (A-TOKEN/FR-A16: "every upstream call carries the CALLER's identity-minted scoped token verbatim"). Porting MCP's proven session-exchange-then-Bearer pattern into wiki-api's `Engine` client is buildable (the MCP repo is a working reference implementation) but touches the transport layer behind all 22 `Engine.*` methods — a real, non-trivial, testable unit of work, not a config flip.
- **Reverting `Engine.ApplyOps`'s wire shape back to `orvex-wiki`'s real `/api/orvex/pages/:pageId/apply-ops` (undoing fix2's docmost-blocks/batch translation) is coupled to both of the above** — doing it without also fixing the auth transport and the ifVersion semantics would silently re-break the Edit blocker fix2 JUST fixed and live-verified (0% → 100% working). This is exactly the "genuine architecture fork" fix2 and mcp-fix2 both independently flagged as needing a PO/architecture call, not a same-pass mechanical fix; this pass's additional finding (ifVersion) strengthens rather than weakens that conclusion.

**Net effect on the task's stated goal ("unblocks search/outline/related for wiki-api AND mcp"):**
- **wiki-api's `GET /v1/search` — genuinely fixed and root-caused** (was 404/502 against a route that never existed; now targets the real one). LIVE-VERIFIED 200 with a real hit (see `scratchpad/night/fix3/engine-verbs.md`).
- **wiki-api's `GET /v1/{resource}/{locator}/outline` was ALREADY working** (fix2 confirmed live) — it's derived in-process from the engine's `POST /api/pages/info`, no separate engine "outline" route involved, unaffected by either the flag or the ENGINE_URL question. Re-confirmed live this pass.
- **wiki-api has no `related` HTTP surface at all** (verbs.go's own doc comment: "search/related → knowledge (FR-A11)" is aspirational; only `search` is wired. `Dispatcher.Related` does not exist). Not this pass's scope to add — not in the task's explicit DO list, and knowledge's own `/v1/related` is itself still a stub (`s.notImplemented`) per direct read of `orvex-studio-knowledge/internal/server/server.go` (M5-owned, not touched, collision guard respected).
- **MCP's `search`/`get_page`(outline mode)/`get_changes`/`related_pages` — genuinely NOT fixable by this pass's flag flip.** Direct source inspection of `apps/server/src` in `orvex-wiki` (both `grep -rn` across the whole tree and reading every controller in `orvex/page-blocks`, `orvex/llms`, `core/page`) confirms `POST /api/ai/search`, `POST /api/orvex/ai/related`, `GET /api/pages/:id/blocks/outline`, and `GET /api/pages/changes-since` have **no controller anywhere in this repo, on either build** — these are missing-route gaps, not `OrvexModulesEnabledGuard` 404s, and flipping the flag cannot create a route that was never registered. This corrects mcp-fix2's own hedge ("would very likely unlock ... immediately (all 404 on unmounted routes today, not auth failures)") — that hedge was a hopeful guess, not verified against source; this pass verified it directly and found it does not hold. MCP's `whoami`/`get_neighborhood` (already fixed by mcp-fix2's auth-seam work) are unaffected either way.

**Collision guard respected:** did not touch `orvex-studio-knowledge` (M5 lane — read its `internal/server/server.go`/`internal/search/search.go`/`gen/query.go` to derive the correct wire shape, changed nothing there).

**Discipline:** build/vet/test/lint clean on both `orvex-wiki` (substrate-conformance script pass) and `orvex-wiki-api` (`go build/test/vet` + `golangci-lint run` 0 issues) before every push; `orvex-studio-lib` full suite green (269+ tests) before tagging `v0.3.6`; pushed straight to `dev` + `origin/dev` on all three repos (night mode, no PR); Tekton auto-build+rollout-restart fired on `orvex-wiki` and `orvex-wiki-api`, confirmed via fresh pod start-times; secret values never printed.

Full log + repro commands + LIVE-VERIFY transcript: `scratchpad/night/fix3/engine-verbs.md`.

_orchestrator judgment under PO standing authority (2026-07-13)._
