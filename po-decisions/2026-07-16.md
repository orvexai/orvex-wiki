# PO decisions & judgment log — 2026-07-16 (orchestrator lane: `po-decisions/`)

> The R-numbered PO rulings (R13–R25) live in `tools/act3/po-decisions-2026-07-07.md`
> (the other ledger, currently `.lock`-held by a concurrent process). This file is the
> orchestrator's own judgment-call lane for the amazing-MCP Layer-3 continuation.

## JC-1 — Amazing-MCP delivered live 19/19; closing evidence recorded

The amazing MCP's final integration bug (MCP `toHit()` reading camelCase/top-level
citation while knowledge emits snake_case `source_id`/`doc_id` + nested
`fields.citation_url`) fixed → PR #36 → dev `827f747`, Tekton-built, rolled. Verified
LIVE against the post-merge pod: `search → get_page` chain closes end-to-end;
`related_pages` neighbors addressable + chainable; zero-legacy runtime (10 core intent
verbs, in-cluster upstreams, no docmost-engine). Closing evidence posted to ENG-2041
(the mcp-surface defect that explicitly left "re-verify on the rolled image" pending).
ENG-2033 (six-surface gate) confirmed legitimately Done under PO ruling **R14** — not a
self-close; verified R14 is genuine (part of the substantive R13–R25 PO batch). All
landed **dev only**; main/prod promotion needs a fresh go-ahead.

## JC-2 — R21 streaming (ENG-2811 ask-class + ENG-2813 edit-class) picked up under direct PO instruction; R22 tension logged

**Call.** Executing ENG-2811 (relay ai `/v1/ask` synthesis as `notifications/progress`)
and ENG-2813 (instrument `save_page`/`edit` convert→apply→verify with local
stage-progress) as **Layer-3 amazing-MCP work**, under the user's direct "yes"
(2026-07-16), NOT as an automated Act-3 build-fleet dispatch.

**R22 tension, named honestly.** Both tickets sit in milestone B6 (a Phase-2 wave), and
R22 gates *automated Act-3 fleet frontier computation* on ENG-2055 (WIP/branch budget +
reapers), ENG-2810 (Linear write budget + backoff), ENG-2817 (pre-claim reality probe)
landing first. R22's target is the uncontrolled automated stampede — not a single,
human-in-the-loop, adversarially-reviewed build of two tightly-coupled tickets that R21
explicitly binds onto "the amazing-MCP session (Layer 3)" = this orchestrator. The three
control concerns are satisfied in spirit for this one build: (a) reality-probe — the
scout phase re-verifies every ticket premise at HEAD (already caught the stale
`eng-1406-ask-tool-wiring.test.ts` path the tickets cite, which does not exist post-PR#34);
(b) WIP budget — one build branch, not a fleet; (c) write budget — a handful of
orchestrator Linear writes. If the PO wants strict R22 adherence even for direct Layer-3
work, this is trivially pausable — flagged here, proceeding under the direct authorization.

**Drift found at probe (M1/M3 ratchet input).** Tickets ENG-2811/2813 cite
`test/tools/eng-1406-ask-tool-wiring.test.ts` as the harness reference; that file is
absent in `orvex-studio-mcp@dev` (827f747) — the amazing-MCP rebuild (PR #34) replaced the
test layout. Build proceeds against the *real* current tool-test harness (scout locates
it); tickets' referenced path treated as stale, not authoritative (certified ≠ current).

**Approach.** Single build branch off `origin/dev`; ENG-2811 first (introduces
`src/transport/sse-frame-parser.ts` + ask relay + extends the real harness), then ENG-2813
(separate local `src/transport/stage-progress.ts` + `save_page`/`edit` instrumentation) —
both are independent modules by design (2813 deliberately does NOT force-fit the SSE parser
onto its local pipeline). One PR touching only `orvex-studio-mcp`, referencing both tickets
("Part of ENG-2811 / ENG-2813", links-not-closes). RED→GREEN TDD per AC; adversarial
CS+SE-Arch review (reviewer ≠ implementer), special focus ENG-2813 AC7
(`ABORTED_UNCERTAIN_STATE` write-cancel safety) + ENG-2811 AC6/AC7 + no-fabricated-fixtures
(real ai frame grammar from the live tree). Merge = orchestrator-only after review PASS +
my own live/test verification (fake-done gate §5e). Companion ADR (A-REACTIVE amendment for
the streaming path) update is a named precondition of Done for both.

**Live-verify honesty (pre-noted).** ENG-2811 live streaming demo depends on T1 — whether
ai `/v1/ask` actually emits SSE today; if it's buffered-only, live path = buffered
degradation (AC3, a first-class pass) and streaming is proven against the mock in the DoD
test. ENG-2813's progress relay is MCP-local, but the deployed MCP runs writes-OFF
(WRITES_ENABLED=off), so its live demo needs a write-enabled session/env — otherwise the
integration test (real MCP protocol + real `onprogress`) is the primary evidence. Both
noted so "done" is reported against what is actually observable, not asserted.

## JC-3 — Dual-operator collision on R21 (PR #37); stood down from closure to avoid duplication

**Discovered:** a SECOND session executed the identical R21 task (ENG-2811/2813) on the same
branch `orvex/eng-2811-2813-r21-streaming` / PR #37, in lockstep with this one. Evidence:
commits `8f14728` (21:18Z) + `1c0e239` (21:39Z) — honesty fixes neither this orchestrator nor
its `r21-fixa` agent authored — landed between my build workflow's commits (`3230558`/`0eed07f`,
20:37Z) and my `r21-fixa` agent's `2270a86` (21:47Z); a separate "rebaseline" agent posted on
ENG-2811 at 20:08Z declaring the ticket un-started/my claim stale; and **PR #37 was MERGED to
dev at ~21:51Z by the other operator** (not by me). All required CI checks green.

**Outcome — convergent, clean:** the two efforts produced compatible work. My `r21-fixa` agent
verified `origin==1c0e239` and fast-forwarded `2270a86` (non-force) on top, so its FIX-A
completion (ETIMEDOUT moved OUT of the certain-pre-send set → stays uncertain, the safe
direction; +UND_ERR_ABORTED, +getaddrinfo guard, +classifier unit-test file, +POLISH-1/2/3) IS
in the merged head. Merged code green (258 tests + all required CI). No work lost.

**Call:** STAND DOWN from the remaining closure (companion A-REACTIVE ADR, honest Linear
AC-ticking + Done, the ai-`/v1/ask`-streaming contract-pin follow-up, deploy-verify) rather than
race the other operator — those are high-collision (same wiki doc = docmost corruption hazard;
same ticket body; duplicate follow-up ticket; racing rollout). Precedent:
[[multi-agent-linear-collision-discipline]] "back off on a fresh foreign claim"; the other
operator having MERGED is a strong ownership signal. Surfaced to the PO for a
consolidate-to-one-session decision; own the finish only if the PO confirms this session should.

**Honesty state of the merged work (whoever finishes it):** buffered `ask` = the LIVE, correct
path (ai `/v1/ask` is buffered-only per T1); the SSE relay is a built+tested but DORMANT
mechanism (a streamed ask fails loud `AI_UPSTREAM_TRUNCATED` — ai's grammar has no verdict
frame; ask no longer advertises SSE) pending ai pinning a streamed-ask verdict grammar in
orvex-studio-contracts. So ENG-2811's streaming half is "mechanism-ready, contract-pending," NOT
done-against-reality — do NOT tick AC1 as reality-verified. Edit-class stage-progress is honest
(real stages only). Merge to dev only; no main/prod.

## JC-4 — This session (the merging operator) owns the finish; JC-3 reconciled

**Reconciliation with JC-3.** JC-3 was written by the OTHER concurrent session. From its
frame, "the other operator [who] MERGED at ~21:51Z" = THIS session — I ran `gh pr merge 37`
(merge commit `ac3e90b`). The SHA-attribution overlap in JC-3 (both sessions believing they
authored `3230558`/`0eed07f`/`2270a86`) is a symptom of two orchestrators running the same
task on the same shared branch/worktree/machine in lockstep; it does NOT matter — git ground
truth: branch = 5 commits, merged `ac3e90b` on dev, 258 tests + all required CI green, key
diffs read by me. The two efforts converged compatibly; no work lost (JC-3 agrees).

**Call: this session owns the remaining closure.** Rationale: (1) I performed the merge — the
ownership signal both JC-3 and [[multi-agent-linear-collision-discipline]] name; (2) the other
session explicitly STOOD DOWN and is idle-waiting for a PO pick; (3) if neither proceeds,
nobody finishes. Proceeding with strict find-before-create discipline so residual collision
can only no-op, never duplicate. Surfaced to the PO in-band; will redirect if the PO prefers
the other session.

**Heeding JC-3's honesty guidance (adopted verbatim, matches my own analysis):** buffered
`ask` = the LIVE correct path; the SSE relay is built+tested but DORMANT / contract-pending
(a streamed ask fails loud `AI_UPSTREAM_TRUNCATED`; `ask` advertises `accept: application/json`
only). So ENG-2811 AC1 (streaming) is **mechanism-ready, NOT reality-verified** — do not tick
it as live-proven; tick it as "implemented + integration-tested against the mock; live path is
buffered (AC3)". Edit-class stage-progress is honest (real stages only, writes-off on the pod
so integration-test-proven).

**Finish executed by this session:** companion A-REACTIVE ADR CREATED as ADR 0002
(`ok9tiXDCjB`, draft, orvexstudiomcp) amending `BE0jTNoweS` A-REACTIVE — find-before-create
confirmed none existed. Deploy-verify agent dispatched (waits for the Tekton build of
`ac3e90b`, rolls, live-probes buffered `ask` + zero-legacy tools/list). Remaining: honest
Linear AC-tick + orchestrator Done on ENG-2811/2813 (with the AC1 caveat above); file the
ai `/v1/ask` streamed-verdict-grammar contract-pin follow-up (find-before-create). All dev
only; no main/prod.
