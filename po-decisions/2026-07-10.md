## ENG-1551 honesty fix — 2026-07-10T11:13:36+02:00
- Audit found F1 (rebuild schemas not pinned in orvex-studio-contracts) and F2 (PRD rebuild-RTO id mismatch: NFR-K4 vs NFR-K7) ticked [X] without cross-repo work landing.
- Unticked T0 (F2) and T7 (F1) on ENG-1551 with dated notes; ENG-1551 stays Done on proven scope (split-and-close precedent).
- Companion ticket queued: team ENG, project Orvex Studio Contracts, Todo — pins F1 schemas + F2 NFR-K4 rename; cites ENG-1551 + engine audit.
- Linear writes RATE_LIMITED at execution time; staged to scratchpad/pass5/staged/ (ENG-1551_description.md, companion body, PENDING_COMMANDS.sh) for replay on quota reset.


## ENG-1465 AC5 reword — 2026-07-10T12:53:39Z
- AC5 literal wording ("any unregistered type -> 400 at dispatch, zero engine calls") incompatible with legitimate base ProseMirror writes (paragraph/heading pass-through required by ENG-1366 AC10).
- Ruling: AC5 scopes to the orvex-* type namespace — unregistered ORVEX-namespace types -> 400 at dispatch with zero engine calls; base ProseMirror vocabulary is registered-by-definition and passes through (ENG-1604 reword-to-real-invariant precedent). Invariant protected: no-unregistered-orvex-surface-reaches-the-engine, not a ban on base PM writes.
- ENG-1465 body amended (AC5 text + dated amendment note), boxes untouched; ticket returned to Todo; ruling comment posted on the ticket.

## ENG-1359 real-SSE capture — CRITICAL cross-repo wire-contract mismatch found (2026-07-10)

**Context.** ENG-1450 (SSE producer) merged to orvex-studio-ai@dev (6ea5e8bce099e4cc40d7411cdff39d38b4c83f3b, PR #18) — the prior blocker for a RECORDED (not hand-authored) SSE transcript on ENG-1359 (§5b/§5c + NFR-honesty) is gone.

**What was done.** Booted the real merged producer in an isolated worktree (`/tmp/worktrees/eng1450-capture`, detached at 6ea5e8b): a throwaway Go test harness (`internal/server/zz_capture_real_sse_test.go`, never committed) wired the REAL `internal/chat.Service` + REAL `internal/stream.SSEWriter`, mounted on the REAL `NewRouter`/auth-middleware path (RS256 JWKS-fixture auth — the same pattern the repo's own `chat_route_test.go` treats as real edge auth), and POSTed one real turn through `/api/ai/chat` against the REAL LiteLLM backend (`LITELLM_URL`/`LITELLM_API_KEY`, actual cluster secret). `gpt-4o-mini`/`gpt-5-nano`/`gpt-4.1-mini`/`o4-mini` were all live 429 rate-limited on the shared key at capture time (a real, reproducible finding); `gemini-3.1-flash-lite` answered. Captured 1034 bytes of genuine model-generated SSE (message-queue explanation + a mermaid diagram), replaced the hand-authored `sse-transcript.happy.txt` fixture on `eng-1359-work` with it, provenance-stamped (source commit, command, date, model, quota-429 notes).

**CRITICAL FINDING — blocks Done, do not merge PR #87 as-is.** The real captured transcript proves ENG-1359's client (`types/ai-chat.types.ts` `AiChatStreamEvent`, `services/ai-chat-service.ts`) was built against a WRONG assumed wire contract (`chat_created`/`content`/`stream_state`/`citation_preview`/`done{messageId,citations[]}`) that does not match what the real merged ENG-1450 producer actually emits (`chat_id`/`state`/`banner`/`token`/`citation`(single string)/`cap`/`error`/`keepalive`, completion = `{"type":"state","state":"done"}`, no `messageId`, no structured citation objects, no tool/citation registry ported at all). Per the client's own documented AC8 forward-compat behavior, every real frame type is unrecognized and silently no-op'd. Re-ran the DoD test (`ai-chat-stream-renders.spec.tsx`) against the real transcript: **RED** — `screen.findByText("The citation above documents this pattern.")` times out; the assistant turn never renders (only the user bubble + an empty placeholder div), confirmed via full DOM dump. This is a genuine cross-repo integration break, not a fixture-quality gap.

**Ruling (decided, executed).** Do NOT merge PR #87. Do NOT tick §5c/NFR-honesty boxes (they are now provably FALSE, not merely unproven). Committed + pushed the real fixture + provenance + failing-test evidence to `eng-1359-work` (honest record beats a synthetic green). Escalating via ticket comment on ENG-1359 for a PO/eng call on the follow-up: the client's SSE parser (`ai-chat-service.ts` + `ai-chat.types.ts`) needs a rewrite against ENG-1450's actual wire contract — out of scope to silently patch in-flight during a "swap the fixture" task. No Linear status/box writes attempted beyond the comment (avoids a false-positive box-tick under an unproven/now-disproven claim).

**Discipline:** worktree isolation (`eng1450-capture` detached at 6ea5e8b, `eng-1359-work` for the fixture swap), real infra only (real Postgres/Redis containers already up, real LiteLLM endpoint, real merged commit — no fabrication), capture harness never committed to orvex-studio-ai, linearis CLI only for the Linear comment, trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the wiki-repo commit. Full trace: `scratchpad/pass6/1359.md`.

## ENG-1405 thrown-chain diagnosis — 2026-07-10

**Context.** Workflow `wf_f7624910-697` journal shows a normal build→review chain: an agent claimed ENG-1405, built + landed a real first slice (branch `eng-1405-work`, commit `335b13e`, **PR #18 OPEN/MERGEABLE**), and an adversarial reviewer ran and returned verdict **ESCALATE** (0/8 ACs met end-to-end; two genuine PO/eng-level blockers — ENG-1442 not live, family Kafka-client-library unpinned). No journal `error` entries exist; this was a clean escalation, not a crash.

**The break.** The ESCALATE verdict was never carried forward — not logged to po-decisions, not posted as a ticket comment. The ticket sat In Progress with a real, clean, additive PR open and no visible next step. Classified as a **harness/chain gap** (an escalation output that had nowhere to land), not a content defect — the build and review work themselves are sound and match the ticket's own honesty doctrine (no boxes ticked, scope-limited PR body, no false claims).

**Action taken.** Posted the full diagnosis (PR link, escalation summary, the 2 hard blockers, the F1-F6 buildable-now follow-up list) as a ticket comment on ENG-1405 (comment id `98f13e25`). Refreshed the Linear cache. Left status **In Progress** — there is a salvageable claim (PR #18) and real unblocked follow-up work (F1-F6), so this is not a stranded/no-progress ticket and does not get reset to Todo. No DoD boxes touched (review's `verifiedAcs = NONE` stands).

**Discipline:** linearis CLI only (`issues discuss`, cache refresh via `_bmad/lnr/tools/linear-sync.sh`); no GitHub write needed (PR #18 already exists, untouched); comment body drafted to temp file first; trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Full trace: `scratchpad/pass7/1405.md`.

## ENG-1927 F1+F2 legs — merged; wiki-citation leg verified clean-of-defect (2026-07-10)

**Context.** ENG-1927 (split from ENG-1551) carries F1 (contracts) + F2 (3 boxes: knowledge rename, wiki citation, TBD re-verify). F1 was already fully built + reviewed 3x by earlier passes in this workflow (PR #33, gates cold-green, mutation-checked non-tautological) but left unmerged. This pass independently re-verified F1 from a fresh worktree (did not trust prior self-report): validate/schemas/events-contract/cell-discovery/agpl-guard/actionlint/drift-report/full pytest (90 passed) all green cold; mutation-check re-run (restored `origin/dev`'s pre-change `knowledge-internal.yaml`, 3/5 sub-tests failed red, restored); `context-check` fails identically on clean `origin/dev` (same CS-pin sha pair as the knowledge leg below — a family-wide, pre-existing drift, not introduced by this diff). Merged PR #33 (`6d09088`).

**F2 knowledge-repo rename (executed).** Grepped `orvex-studio-knowledge` for `NFR-K7`: found it in TWO categorically different places — (a) `internal/workflow/rebuild.go`'s `NFRK7RebuildRTOBudget` const + `rebuild_test.go`'s `TestRebuildMeetsRTOBudget`, genuinely mislabeling the rebuild-RTO budget (the ratified PRD pins rebuild-RTO as **NFR-K4**); (b) `internal/turbopuffer/turbopuffer.go`'s package header `(A3, NFR-K7)`, which the ticket text also names as a target — but per the ratified PRD (`NFR-K7 Vendor seam`) and architecture doc (`A3 — Store adapter seam (NFR-K7)`), **that citation is already correct** (a different NFR, not the rebuild-RTO one). Renamed only (a): `NFRK7RebuildRTOBudget` -> `NFRK4RebuildRTOBudget` + doc comments, left (b) untouched — renaming it would have introduced a NEW mismatch, not fixed one. Gates cold-green (`gofmt`/`go build`/`go vet`/full `go test ./...`/`golangci-lint`/`gosec`/`govulncheck`); `context-check` fails identically on clean `origin/dev` (pre-existing). Mutation-checked: temporarily set the renamed const to `1*time.Nanosecond`, `TestRebuildMeetsRTOBudget` failed red as expected, restored. Committed `f73ee39`, pushed, PR #17, CI green, merged `a7878bd`.

**F2 orvex-wiki citation (verified — no edit found necessary).** Searched every plausible wiki location for a wrong "rebuild-RTO = NFR-K7" citation: the ratified `orvexstudioknowledge` PRD (`dCbFzRQGDr`, last edited 2026-07-05 — before ENG-1551/ENG-1927 existed) already correctly separates `NFR-K4 Durability & DR` (rebuild-RTO, `FR-K24`'s sizing input) from `NFR-K7 Vendor seam` (`FR-K23`'s alias-swap conformance); the Architecture doc (`azRwTCZMqw`, "A3 — Store adapter seam (NFR-K7)"), PRD Addendum (`dcOUbpzE8L`), R-9 Search-Stack doc (`0mFImku9Gp`), and the Orchestrator Prompt's own retro entry (`gkkUDzn277`, "an `NFR-K4`-vs-`NFR-K7` id mismatch") all already cite the split correctly. The `sat-0119509a §5` id cited by ENG-1551/1514/1548 as the "M5-gap source" does not resolve to any docmost page (`PAGE_NOT_FOUND`) — it is an internal audit-report reference label, not a live wiki page. **Conclusion: the wiki was never wrong; only the code (fixed above) and the ENG-1551 ticket body's own citations (already corrected in the prior "ENG-1551 honesty fix" pass) carried the `NFR-K7` mislabel.** No wiki write made — a spurious edit against already-correct canon would be a regression, not a fix.

**F2 TBD re-verify (box 4) — FINDING, do NOT tick.** Ran the ticket's literal `grep -rE "NFR[- ].*TBD"` scoped to the M5 Knowledge Backbone milestone's adopted issues (ENG-1358/1437/1451/1475/1476/1477/1479/1480/1514/1539). Result is **NOT zero** — ~10 issues still carry `NFR ids TBD` / `orvexstudioknowledge PRD NFR — TBD` lines. Investigated: every hit is a **freshness/latency** NFR (search p95, provisioning latency, extraction SLA, etc.), explicitly deferred by each ticket's own text to a *separate, still-unfiled* "M5 knowledge-NFR-baseline Issue" — categorically different from the rebuild-RTO mismatch ENG-1927's F2 actually describes (ENG-1927's body only names rebuild-RTO/NFR-K4-vs-NFR-K7; it says nothing about latency/freshness). Ticking box 4 as satisfied would be false-done: the literal grep is non-zero. Left unticked; flagged as a separate, larger pre-existing gap (ENG-1551's T0 promised to resolve ALL "TBD" M5 citations, not just rebuild-RTO, and that broader promise was never landed) for a PO call on whether it needs its own follow-up ticket — out of scope to silently expand ENG-1927 to cover it.

**Discipline:** linearis CLI only; worktree isolation (`/tmp/worktrees/eng-1927-knowledge` orvex-studio-knowledge, `/tmp/worktrees/eng-1927-contracts-verify` orvex-studio-contracts, both removed after merge); fetch-rebase-retry (both branches pushed with zero drift, no rebase needed); temp-file commit/PR-body writes; trailer exactly `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`; tick only proven. Appended under `flock` on `po-decisions/2026-07-10.md.lock`. Full trace: `scratchpad/pass7/1927.md`.

## ENG-1415 AC11 scope ruling — reworded to its Assert; PII + real-metering split; finalized In Review (2026-07-10T15:27:09+02:00)

**Escalation resolved.** ENG-1415 AC11 ("same metering + PII pipeline as chat") was flagged across 3 adversarial review passes: (a) PII-redaction-before-LiteLLM is unbuilt with no seam repo-wide, and (b) the metering half is a prod nil no-op — both honestly disclosed, and the AC's own machine-checkable *Assert* omits PII entirely. Orchestrator ruling executed.

**(a) Metering half — assessed NOT in-repo buildable → SPLIT (ENG-1932).** Verified against up-to-date `origin/dev` (not the stale review branch): the concrete outbox-backed `event.Publisher` and `metering.Journal` that would make `ai.usage.recorded` a real delivery are stubbed repo-wide (`event.Relay.Publish`, `metering.Journal.Record`, `postgres.EnqueueOutbox/AppendUsage/DrainOutbox/MarkPublished` all return `ErrNotImplemented`); `internal/event`'s own doc says the real form *"wraps `orvex-studio-lib/pkg/events` once wired"* — an absent CROSS-REPO Kafka seam. No AI feature emits real usage yet (chat/ask/workflow all wire `pub`/`Meter` nil per `server.go` comment). A conformant real path (D-S13 outbox-only; the request path must not write the journal directly) needs the Kafka relay, so it is NOT in-repo buildable — building it for inline alone would invent cross-cutting infra ahead of its owning cutover ticket (❌#6) and still no-op end-to-end. ENG-1415 keeps the injected `inline.Metering` seam + wiring point (AC11's Assert exercises it via a seam double, green); the concrete Journal is ENG-1932. Per ruling (a): "needs an absent cross-repo seam → split."

**(b) PII-redaction → SPLIT (ENG-1931), find-before-create.** No pre-LLM prompt-redaction seam exists anywhere in `orvex-studio-ai` (chat/ask forward raw prompts too). FIND-BEFORE-CREATE: ENG-1453 (Done, merged #20 to origin/dev) landed `internal/pii.Redact` — but that is the EXPORT direction only (sole caller `internal/export`, pattern set narrowed to its AC5). No existing ticket owns a request-path redactor. Created ENG-1931 (relates-to ENG-1415 + relates-to ENG-1453 as the reusable primitive). Cross-cutting-seam-absent = the ENG-1477-schema pattern → relates-to, not blocker.

**(c) AC11 reworded to exactly its Assert + dated ruling note.** AC11's "Then" no longer claims PII redaction or a real prod journal; it now states spend-preflight before the LiteLLM call (over-cap → typed `SPEND_CAP`, zero upstream) + a usage record emitted through the injected `Metering` seam on success — both green. A dated SCOPE RULING note folded into the description top. AC1–AC11 ticked (AC1–AC10 verified by the 3-pass review; AC11 on its reworded Assert). Task/DoD/merge boxes left unticked (not proven Done — PR unmerged).

**Finalize → In Review (NOT Done).** PR #23 (`eng-1415-work`, base dev) is code-complete + review-passed but stale vs origin/dev — `git merge-tree` reports content conflicts in 6 files (`cmd/server/main.go`, `internal/clients/clients.go`, `internal/server/{server.go,server_test.go,server_contract_test.go,chat_route_test.go}`) after ENG-1452/1456/1453 merged. Needs a rebase-conflict-resolution pass + gate re-run before merge; marking Done now would be fake-done (9VUHxAcoXw H14/H15). Set status In Review; a follow-up rebase-merge pass takes it to Done.

**Discipline:** linearis CLI only (no rate-limit hit — nothing staged); temp-file body writes; refresh-on-write (cache re-synced ENG-1415/1931/1932); tick only proven; worktree isolation (merge-conflict probe via `git merge-tree` + `git worktree`, no branch mutated); no code committed (both halves split, no build); companions relates-to (not blockers). Appended under `flock` on `po-decisions/2026-07-10.md.lock`. Full trace: `scratchpad/pass7/1415.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1929 lib-leg unblock — K5Answer grown to 8 fields on orvex-studio-lib dev; PR #28 merged (2026-07-10)

**Ruling executed (per orchestrator directive):** growing/creating `contracts.K5Answer` (8-field envelope + typed `Citation`) IS in ENG-1929's scope — the missing-on-`dev` state was the work item, not a cross-repo blocker to escalate around. A prior pass had correctly identified the blocker (AC1's struct existed only on ENG-1497's unmerged `eng-1497-work` branch, still 6-field, entangled with ENG-1497's live in-flight work) but stopped short of building the lib-side restoration itself.

**Salvage, not a raw cherry-pick.** Archived `orvex-studio-lib`'s `eng-1497-work` tip first (`archive/eng-1497-work-1525475`, pushed — a safety ref, non-destructive, branch left untouched) before reading from it. That branch carried an unrelated ENG-1616 commit (`pkg/clients/WorkspaceExistenceClient`) alongside the ENG-1497 codegen work — cherry-picking the whole branch would have dragged in a sibling ticket's in-flight scope. Cherry-picked only the self-contained codegen-scaffolding commit (`359d078`, `feat(gen): emit ai.* DTO Go types...`) onto a fresh branch off `dev`, leaving `eng-1497-work` untouched (zero collision risk with ENG-1497's active work).

**Growth.** From that scaffolding baseline (6-field `K5Answer`, untyped `Citations []map[string]any`), grew to the ratified 8-field envelope: `{answer, citations, confidence, unanswered, gapNote, followups, nearMiss, didYouMean}` with typed `Citation{PageId, Anchor, Title, Url, Snippet, IsDraftCitation}`. This required extending the lib's hand-rolled mini-YAML codegen (`gen/yamlmini.go` + `gen/envelope.go`) with a scoped `$ref` resolver (`#/components/schemas/Name` only — cross-file refs deliberately rejected, mirroring the repo's existing closed-switch `refGoType` precedent, ❌#3 no speculative generality) so an array-of-object schema field resolves to a real named Go type instead of the generic `map[string]any` fallback. New generator unit tests (`gen/ai_test.go`: `TestYamlRefTypeName`, `TestGenerateAIDTOTypesCitationRef`, `TestGenerateAIDTOTypesUnsupportedRefRejected`).

**Regression gate, mutation-checked.** Added `TestK5AnswerEnvelopeShape` + `TestCitationTypedFields` (`pkg/contracts/ai_gen_test.go`) asserting exact field/type/json-tag shape by reflection. Verified these are load-bearing, not decorative: hand-reverted the generated `K5Answer` to the pre-fix 6-field/untyped shape — the pre-existing golden round-trip test (`TestAIDTOsGoldenRoundTrip`) stayed GREEN (an untyped map round-trips JSON fine), but the two new shape tests went RED as expected. Restored via `go generate ./gen/...`, re-verified GREEN.

**Disclosed boundary.** `gen/contracts/openapi/ai.yaml` is nominally a vendored, pinned snapshot of `orvex-studio-contracts` (refreshed only via `scripts/codegen.sh --refresh` against a checkout at `gen/contracts/PIN`'s exact SHA). This growth was authored directly in that vendored copy, AHEAD of upstream: verified both the actually-pinned SHA (`a910f821`, from the salvaged commit) and its later unmerged successor (`bca0c1f6`) still carry only the 6-field `K5Answer` on `orvex-studio-contracts`' own unmerged `eng-1497-work` branch — the true root cause is a 3-repo unmerged chain (contracts → lib → ai), and syncing the contracts-repo leg is explicitly out of this ticket's scope (lib-leg only). Documented inline in the yaml (a `NOTE (ENG-1929, disclosed lib-local growth)` comment) so a future `--refresh` doesn't silently clobber this growth without re-applying it or bumping PIN to a SHA that already carries it. `gen/contracts/PIN` itself was left untouched (still points at the real historical vendor commit for the other 5 unrelated schemas that genuinely came from it).

**Gates cold, merged.** `gofmt`/`go vet`/`go build`/`golangci-lint` (0 issues)/`gosec` (0 issues)/`bash gates/agpl-import/agpl-import-guard.sh` (OK)/`go test -race -count=1 ./gen/... ./pkg/contracts/...` all green pre-push. PR #28 (`orvexai/orvex-studio-lib`, `eng-1929-work` → `dev`) — full CI green (lint/security/test/sbom/rehearsal/M1-gate/actionlint/`ci-success`, `dev` branch protection satisfied) — squash-merged at `df5593b206badc948828cc3467bb3da6e2714c48`. Local `dev` checkout fast-forwarded; feature branch deleted (remote via `--delete-branch`, local via `git branch -D`).

**ENG-1929 ticket.** Posted a corrective dev-context comment recording the lib-leg landing (sha, what changed, provenance, disclosed boundary, main-leg unblock note) and returned status to Todo (was In Progress from the earlier blocked attempt) so the main leg (`orvex-studio-ai` — AC2/AC3 + the named DoD gate `TestAsk_ReturnsCitedEnvelopeWithAbstentionAndAnchors`, bumping the go.mod pin to consume the new lib version) can be picked up. Cache refreshed post-write (`status: Todo` confirmed).

**Discipline:** linearis CLI only for Linear (`issues discuss` + `issues update`, no rate-limit hit — nothing staged); temp-file body writes (comment + commit message + PR body all drafted to scratchpad temp files, never shell heredoc into `git commit -m`); worktree isolation (`scratchpad/pass7/1929/lib-worktree`, removed after merge); fetch-rebase-retry (rebased onto `origin/dev` before push — already up to date, no conflicts); refresh-on-write; tick only proven (no AC boxes ticked on ENG-1929 itself — that's the main leg's job once it lands); trailer exactly `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the lib commit. Appended under `flock` on `po-decisions/2026-07-10.md.lock`. Full trace: `scratchpad/pass7/1929.md`.

## ENG-1465 CI-fix review PASS + merged; finalize STAGED (rate_limited) — 2026-07-10

**Adversarial review (reviewer != implementer, T7) of the ENG-1465 merge-gate CI fix (PR #15, tip `d04e6b9`): PASS.** All four mandated axes verified:
1. **openapi snapshot = ONLY the AC8 route.** `route-table.snapshot.txt` diff vs origin/dev is exactly one added line `GET /v1/pages/blocks/schema`; re-derived the live route table with the gate's own extraction and diffed — EXACT MATCH, zero residual drift; route really registered at `server.go:233`.
2. **Coverage tests BITE (mutation-proven).** Deleted `attachmentHandler`'s second-field (`name`) check → `TestAttachmentHandler_SecondFieldValidatedIndependently` went RED, reverted → GREEN. `internal/pageblocks/handlers` 0.0%→49.0%; repo total 72.0% ≥ 70.0% (CI-identical computation).
3. **Full gates cold-green.** gofmt/`go build`/`go vet` clean; `make contract-gates` all 4 OK; `go test -race ./...` all green; `gh pr checks 15` all 8 required GREEN; `mergeStateStatus: CLEAN`. handler-thinness fix (`fetchIfVersionRoot` extraction) confirmed byte-identical error strings (`fmt.Errorf("...: %w").Error()` == prior concat).
4. **AC5 namespace reword intact.** Code matches the 2026-07-10 AC5 ruling: unregistered orvex-namespace → 400 zero engine calls; base PM passes through (`isBaseProseMirrorType` via real `dfm.RegisteredTypes()`). Tests `TestBatchOpsUnregisteredOrvexTypeRejectedNoEngineCall` + `TestBatchOpsBaseContentTypeUnaffectedByRegistry` green.

**Full-ticket close-out.** DoD binary gate `TestBlockHandlerRegistry_CoversAllSupportedTypes_LinearDroppedAndUnknownRejected` GREEN; AC1–AC9 + AC-NFR all GREEN (ran each on the merged tip); AC2 `grep -RE 'linear_(graph|issue|view|cycle|roadmap|mention|project|dashboard)' internal/pageblocks` == 0 + `orvex_dashboard` kept; AC-NFR `TestFacadeOverhead_P95WithinBudget` GREEN. Planned 5b test names differ from implemented names — each named behaviour is covered 1:1 by a real green test.

**Merged + cleaned.** PR #15 squash-merged to `dev` `3a3b1d8` (branch fully rebased, merge-base == origin/dev tip). Remote `eng-1465-work` deleted; worktree `/tmp/worktrees/A-d1-eng-1465` removed; local branch deleted. (`gh pr merge --delete-branch` errored only on the local worktree-checkout step because `dev` is checked out elsewhere — remote merge landed; did remote delete + worktree removal by hand.)

**Finalize → Done STAGED (rate_limited).** Refresh-on-write: read the live ENG-1465 description, produced the fully-ticked body (40/40 boxes `[x]`, 0 unticked). `linearis issues update ENG-1465 --status Done --description <ticked>` hit the Linear 2500/hr rate limit. Staged a self-contained replay: `scratchpad/pass8/staged/PENDING_COMMANDS.sh` + `staged/ENG-1465_description_all-ticked.md`. Ticket stays In Progress in cache until replay on quota reset. Advancing to Done is v11-compliant — refuse-Done boxes-clean guard satisfiable (all 40 proven), gate-authoring pre-check satisfied (named gate test exists + passes), §5e's 4 orchestrator-advance conditions (TestX green ∧ boxes ∧ review PASS ∧ PRs merged) all met.

**Discipline:** linearis CLI only; stage-on-rate_limited to scratchpad/pass8/staged/; temp-file body writes; refresh-on-write; tick only proven; worktree isolation; fetch before merge (origin/dev fresh); trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Full trace: `scratchpad/pass8/1465-review.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1488 finalize — auth-flow ADR filed, PR merged, Linear writes staged (rate_limited) (2026-07-10)

**Auth-flow ADR (§4i).** ADR-0018: wiki-api edge — caller-token pass-through (never re-mint, never a service credential), space `orvexstudioarch`, slug `hSJ5tgNZMm`, status **draft — pending PO doc-ratify** (never self-promoted). Confirmed already filed by an earlier pass and correctly captures the fail-closed `MultiIssuerVerifier`/`CallerVerifier` design + both disclosed deferrals, citing ENG-1488 directly. Corrected an earlier PROGRESS comment on the ticket that falsely claimed the ADR was "filed as a commit" — it was always a wiki draft, the correct location; only the wording of that prior claim was wrong, not the artifact.

**Known ADR-number collision (flagged, not resolved here — PO call).** ADR-0018 (`hSJ5tgNZMm`, this ticket) collides with a same-day, unrelated ADR-0018 filing (`MQaTt8e22e`, "Dedicated Kata-isolated DinD runner set for public-repo CI", orvex-wiki repo scope) — both self-assigned 0018 off a stale "next free number" read on the family registry (`32Huug8U4B`). Both still draft, neither ratified. Renumbering one is a PO/doc-ratify decision outside ENG-1488's scope; left as-is with both sides flagged in the ENG-1488 ticket comment (staged, see below).

**PR merge.** `orvexai/orvex-wiki-api#16` was `CONFLICTING` against `dev` at claim time (ENG-1367/1368/1468/1934/1465 had all landed on `dev` since the branch was cut, including two more mid-resolution). Resolved via two `git merge origin/dev` passes (not rebase — repo convention is squash-merge-on-GitHub, so one conflict resolution beats re-resolving per commit) in worktree `/tmp/worktrees/pass8-eng1488`. The real semantic conflict: `dev`'s already-landed ENG-1368 had independently rebuilt the `search` verb+client with a superset signature (resource-gate, `shape.Options`, SLI, `url.QueryEscape`) but WITHOUT this ticket's AC6 knowledge-error-mapping fix — a naive merge would have silently reintroduced the exact "knowledge 401/403/404 degrades to 502" bug ENG-1488's own review escalation F1 fixed. Resolution kept `dev`'s superset shape and folded in `clients.mapKnowledgeError`/`bareBearer`, plus added a missing `clients.ErrUnauthorized→401` case to `writeReadLadderError` that dev's read-ladder mapper never needed before this fix existed. Verified cold on the merge commit: gofmt/build/vet/golangci-lint(0)/gosec(0)/govulncheck(clean)/`go test -race ./...` all green, named DoD gate `TestTokenPassThroughScopeIntersection` green (5 subtests), mutation-proven non-tautological (bearer(r)→"Bearer MUTANT" in `v1PageBlocksBatch` → gate RED at the byte-equality assertion; reverted, green, clean). GitHub CI re-ran green on the merge commit before squash-merge to `dev` at `ad7e20dc6c5c2db99c4d7190c5ca9bcfb827018e`.

**Linear finalize — staged (rate_limited).** DoD checklist reconciled (description re-fetched live, all AC1–AC8/NFR-sec/agpl/honesty/operability/review/T1–T9/named-test-scenario/constraint boxes ticked against the merged+re-verified code; `NFR-AC-perf` left unticked — the ticket's own text scopes it to an absent cell perf harness, genuinely unmeasured, dated deferral, not a defect). `linearis issues update ENG-1488 --description <ticked>` hit the Linear 2500/hr rate limit (confirmed on retry). Staged a self-contained replay: `scratchpad/pass8/staged/eng1488-write-commands.txt` + `eng1488-description-updated.md` + `eng1488-final-comment.md` (the dated annotation comment covering both deferrals + the ADR-number collision + the merge conflict resolution). §5e's 4 orchestrator-advance conditions (named gate green ∧ DoD boxes ticked barring the disclosed perf deferral ∧ adversarial review PASS ∧ PR merged) are all met — the staged replay includes `--status "Done"` as the final step.

**Discipline:** linearis CLI only; stage-on-rate_limited to `scratchpad/pass8/staged/`; temp-file body writes; refresh-on-write (live description re-fetched, not the stale cache); tick only proven; worktree isolation (`/tmp/worktrees/pass8-eng1488`, removed post-merge); fetch-rebase-retry (two merge passes as `dev` moved twice under the PR); trailer exactly `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on both merge commits. Full trace: `scratchpad/pass8/1488.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## RULING — ENG-1405 dependency adjudication: ENG-1442 chain already wired + Kafka client PINNED to segmentio/kafka-go (2026-07-10)

Resolves the two blockers the ENG-1405 build+review chain escalated (journal `wf_fa0982af-af5`): (1) ENG-1442 identity dependency, (2) the "unpinned family Kafka client-library" that blocks AC5's live `token.revoked` consumer. Zero code changes this pass — a dependency ruling + records only.

### (1) ENG-1442 — FIND-BEFORE-CREATE: found, in-scope, Done, relation already wired
- **ENG-1442** = "Identity mints + scopes tokens server-side (deny-by-default)", project **Orvex Studio Identity**, state **Done** (live-confirmed via `linearis issues read ENG-1442`, `updatedAt 2026-07-08T18:48:45Z`). It is a real in-scope ticket, NOT missing — so nothing is filed (find-before-create: found).
- The **ENG-1405 blocked-by ENG-1442** relation is **already wired** — cache `ENG-1405.blockedBy = [ENG-1404, ENG-1442]`. Both blockers are **Done** (ENG-1404 R0 repoint Done; ENG-1442 Done). No relation added — the chain is already ordered correctly and both prerequisites are satisfied in the tracker. The engine builds 1442 "when ready" is moot: it is already Done.
- **Honesty note on the reviewers' "1442 live JWKS/introspect/revocation not shipped" claim.** The ENG-1442 *ticket* (server-side token mint + deny-by-default scoping) is Done; the sibling identity legs are Done too (ENG-1443/1444 mint/endpoints, ENG-1446 Clerk JIT + polymorphic tenancy, ENG-1458 tenant→cell registry, ENG-1611 offline-sealed break-glass verify). The reviewers' concern is a **runtime-availability / three-repo-E2E-environment** matter (a live identity instance the MCP edge can reach for JWKS/introspect at test time), NOT a Linear dependency-wiring gap. The wiring action is complete; the live-surface gate is an E2E/deploy-environment concern for the DoD test author, out of scope for this dependency ruling.

### (2) KAFKA CLIENT PIN — segmentio/kafka-go v0.4.51 (family-majority; CS driver-agnostic)
**Ruling: the event-tier / CloudEvents-spine Kafka client for the family is `github.com/segmentio/kafka-go v0.4.51`.** Builds on ENG-1405 (AC5 `token.revoked` consumer) and ENG-1932 (outbox relay + metering consumer) proceed on this pin. Do NOT introduce a second Kafka client for the event spine.

**Survey (go.mod across all satellites + lib + engine + workflows):**
- `orvex-studio-lib` — `github.com/segmentio/kafka-go v0.4.51` **DIRECT** dependency; it is the shared family event transport (`pkg/events/publisher.go`, `pkg/events/consumer.go` — real, tested CloudEvents-over-Kafka spine that stamps `orvexcell`/`orvextenant` and fails closed on cell mismatch; integration test `publisher_kafka_integration_test.go`).
- `orvex-studio-billing` — segmentio/kafka-go v0.4.51 (indirect, via lib).
- `orvex-studio-knowledge` — segmentio/kafka-go v0.4.51 (indirect, via lib).
- **No `franz-go`, no `sarama`, no `confluent-kafka-go` is wired anywhere** in the family (`grep -rE 'franz-go|sarama|confluent-kafka' --include=*.go` across all repos returns only a doc-comment TODO — see below). Majority is unanimous among actually-bound clients: segmentio.

**The one franz-go reference is a red herring / separate concern.** `orvex-studio-lib/pkg/kafkautil/doc.go` is a **stdlib-only stub** (returns `ErrNotImplemented`) for a DIFFERENT, narrow use case — raw-topic **offset-based streaming reads** (replay/tail/backfill), explicitly NOT the event bus ("most eventing rides the Knative Broker… but some reads need the raw topic"). Its `// TODO: implement with franz-go` is aspirational, has **no go.mod entry**, no implementation, and does not touch the CloudEvents spine that ENG-1405 AC5 / ENG-1932 bind to. It does not create a family majority and is not a competing pin for the event tier. (If/when that offset-replay stub is built, the driver choice for THAT package is a separate, later decision — but the single-transport spirit argues for reusing segmentio there too rather than adding a second client.)

**Verified against Coding Standards (`orvexstudioarch/6aMAzsYeQb`) event-tier language.** CS is **driver-agnostic on Kafka**: it fixes the *seam*, not the *client*. §3.2/§5/§6 classify "Kafka (studio-spine)" as **remote-but-owned** → Go `Publisher`/`Consumer` port + real adapter (prod) + in-memory/test-container adapter (tests), confined to `internal/event/`, outbox pattern, CloudEvents validated against orvex-studio-contracts. The concrete driver is a **repo `go.mod` pin** (§5 states this explicitly for the analogous store tier: "pinned pg driver (repo go.mod)"). CS therefore does NOT dictate a specific Kafka library — so the "family-majority wins unless CS says otherwise" rule governs, and CS does **not** say otherwise. Family majority ⇒ **segmentio/kafka-go v0.4.51**. The pin is fully CS-compliant provided each consumer keeps the driver confined behind the `internal/event/` port (CS ❌#2-style confinement; ENG-1932's review already confirmed "Kafka driver confined to internal/event").

**Recorded on:** dev-context comments posted to **ENG-1405** and **ENG-1932** (they share the Kafka seam).

**Discipline:** linearis CLI only (`issues read` ×1 live for ENG-1442, `issues discuss` ×2 for the two dev-context comments; cache refresh via the tracking adapter after each write); cache-first reads for everything else; temp-file body writes for both comments and this entry; refresh-on-write; zero code changes (survey `grep` over go.mod/*.go only, no edits); trailer exactly `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`; staged to `scratchpad/pass9/staged/` on any `rate_limited`. Appended under `flock` on `po-decisions/2026-07-10.md.lock`. Full trace: `scratchpad/pass9/1405.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ADR-0018 collision resolved — auth-flow ADR renumbered 0018→0021, not 0019 (2026-07-10)

**Ruling executed (Houston renumber lesson):** entrenched DinD runner-set ADR (`MQaTt8e22e`, referenced in this ledger's own round-8/enact/re-scope rulings, the orchestrator prompt §13 append, and the ratify ledger) keeps **ADR-0018**. The ENG-1488 auth-flow ADR (`hSJ5tgNZMm`, `orvexstudioarch`) was retitled + renumbered to **ADR-0021** (H1 heading, page-title metadata, and a Status-line renumber note all updated in place; a markdown round-trip corruption the renumber patch introduced on an unrelated bold+code span — `IDENTITY_VERIFY_ENABLED=true` in "Alternatives considered" — was caught via `--prosemirror` diffing and repaired byte-clean via `page update --content-json`, verified against the pre-edit body with a full-content diff showing only the two intended lines changed).

**Deviation from the directed target, disclosed:** the ruling as given named ADR-0019 as the auth-flow ADR's new number. Verified before writing (`page list --space orvexstudioarch`) that ADR-0019 is itself ALREADY double-claimed by two unrelated same-day filings — `pjacNq8SQY` (DeprovisionWorkflow ownership, ENG-1424) and `ODz3dEqZRJ` (Studio BFF cell-compliance 421, ENG-1428) — neither ratified. Assigning 0019 to the auth-flow ADR would have recreated a three-way collision, defeating the renumber's purpose, so **ADR-0021** was assigned instead as the first genuinely free number (registry high-water mark was ADR-0020, `9KcwAG5SRg`, singly claimed).

**Staged-file sweep:** `scratchpad/pass8/staged/eng1488-final-comment.md` (the ENG-1488 rate-limited Linear finalize comment, queued for replay) cited the ADR by number and URL — edited in place to ADR-0021, with the resolution and the newly-found 0019 collision both recorded so the replayed comment is accurate. `scratchpad/pass8/staged/eng1488-description-updated.md` does not cite a specific ADR number — no edit needed. ENG-1488's already-posted Linear comment (live cache, `.cache/linear/issues/ENG-1488.yaml:271`) still names the old number in a historical "COMMITS:" note — left as-is per instruction (avoid burning Linear write quota on already-posted history); the corrected staged final comment supersedes it on replay.

**New findings flagged, not resolved here (out of this task's scope):** ADR-0017 carries its own unresolved same-day double-claim (`UWJzZBVDng` vs `iBHqRbLIiF`); ADR-0019 now carries the double-claim described above. The family Decision-Records registry (`32Huug8U4B`, canonical) still tells filers "the next free number is 0017" — that stale line is the root cause of all three collisions and was not corrected here (registry page has a table; a safe fix needs `--content-json`, out of this task's explicit 5-step scope) — recommend a follow-up pass updates it to ADR-0022 and adjudicates 0017/0019. Verified post-fix: `MQaTt8e22e` is the only page in `orvexstudioarch` claiming ADR-0018.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ADR registry root-cause fixed — stale "next free number is 0017" line killed; all live ADRs 0001–0023 catalogued (2026-07-10)

**Root cause closed.** The family Decision-Records registry (`32Huug8U4B`, `orvexstudioarch`, canonical) still told filers "the highest number in use is **0016**; the next free number is **0017**" — the stale line flagged in this ledger (lines 145/149) as the root cause of all three same-day ADR-number collisions. Its Registry table listed only 0001–0016, while seven newer ADRs (0017–0023) lived only in free-text "ADR-00xx filed" notes appended below the table — and the ADR-0018 note was itself factually wrong post-renumber (it named `hSJ5tgNZMm` "wiki-api edge" as 0018, when `hSJ5tgNZMm` is now ADR-0021 and 0018 is the DinD-runner `MQaTt8e22e`).

**Enumerated every live ADR, verified each claim.** `page list --space orvexstudioarch --fields slug,title --limit 0 --include-superseded` returned exactly 23 ADR pages, numbers 0001–0023, each claimed once (no superseded ghost claims). Per-page `page get -o json` confirmed statuses: 0001–0016 + 0020 canonical, 0017/0018/0019/0021/0022/0023 draft. Note 0020 (`9KcwAG5SRg`, MCP cell-discovery routing, canonical) was absent from the registry entirely — neither a table row nor a free-text note.

**Registry table rebuilt faithfully via `--content-json` (lossless, no markdown round-trip).** Fetched `--prosemirror` before-state; built the new ProseMirror doc programmatically: appended seven table rows (0017–0023, cloning the existing row node shape — plain-text cells, no bold+code spans, so the known round-trip corruption class does not apply), each with a faithful summary drawn from the live ADR body. The 0001–0016 header + rows and every untouched top-level node were asserted byte-identical to the pre-edit PM (ids stripped) before writing. Wrote via `page update --content-json @after.json --operation replace --force-self-ratify --reason "…"` (canonical page; standing-lessons authorized for this specific registry edit). Post-write `--prosemirror` re-fetch diffed against the intended doc: 12 top-level nodes, 24 table rows, 0 text mismatches, structure+marks equal (ids stripped), zero literal `**` — no corruption.

**Stale line replaced with the correct value AND a rot-proof process guard.** The numbering bullet now reads "highest number in use is **0023**; the next free number is **0024**." A new bullet directly below instructs filers NOT to trust that number in isolation (it rots — it caused three collisions on 2026-07-10) and to, before filing, (a) scan the Registry table for the current high-water number AND (b) verify the intended number is unclaimed with a live `docmost-cli page list --space orvexstudioarch … | grep -i ADR-00` — the table plus a live page-list are the authoritative claim surface; the next-free line is a convenience hint only. The three stale free-text "ADR-00xx filed" notes were folded into the table and replaced by a single accurate "Registry reconciliation — 2026-07-10" note recording the three renumbers (Rerank `iBHqRbLIiF` 0017→0022, Deprovision `pjacNq8SQY` 0019→0023, wiki-api-edge `hSJ5tgNZMm` 0018→0021 with DinD `MQaTt8e22e` keeping 0018) under the entrenched-keeps rule.

**Post-fix duplicate sweep (whole space, incl. superseded):** every ADR number 0001–0023 is claimed by exactly one page; zero duplicates. The collision class the registry seeded is closed.

**Discipline:** docmost writes via the lossless `--content-json` route ONLY (verified with a `--prosemirror` before/after node-level diff, watching the known bold+code round-trip corruption); the canonical body was touched under standing-lessons authorization for this specific edit, restoring untouched nodes byte-for-byte and adding only the intended changes; temp-file body writes for both the PM doc and this ledger entry; appended under `flock` on `po-decisions/2026-07-10.md.lock`; committed on `dev` and pushed. Full trace: `scratchpad/adr-hygiene/registry.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1467 AC-NFR resolved — NFR-A1a draft filed to orvexwikiapi PRD, ratify ledger entry (2026-07-10)

**Gap.** Two independent adversarial reviews (`scratchpad/act3/ENG-1467-review2.md`, `...-review3.md`) verified ENG-1467's functional delivery green — the named DoD test + AC1–AC7 all independently pass, gates clean, slim-AGPL boundary and auth sound — but could not tick AC-NFR: no benchmark or asserted per-item facade-overhead bound existed anywhere in `internal/bulkops`/`internal/server` (`grep Benchmark.*[Bb]ulk` → zero hits), and the ticket's own text forbade inventing a number, citing only a cached "NFR-A1 ≈ p95 ≤ 20 ms" reference pending confirmation against the (then-unratified) orvexwikiapi PRD.

**Ruling executed.** Filed **NFR-A1a** as a DRAFT amendment to the `orvexwikiapi` PRD (page `8jAiCBifDW`, https://docs.eu-central-1.myidp.cloud/s/orvexwikiapi/p/prd-orvex-wiki-api-8jAiCBifDW), appended embed-safe (`page update --operation append`, verified lossless via `--prosemirror` re-fetch) — mirrors the page's own existing FR-A24 draft-amendment precedent (Status: DRAFT banner, not self-promoted, awaits human doc-ratify per doctrine 3.4). NFR-A1a extends the already-filed NFR-A1 (facade overhead p95 ≤ 20 ms in-cell on proxy paths — itself flagged in the PRD's own Assumptions index as "targets pending Phase-0 measurement") to the bulk fan-out's per-item composition overhead specifically, reusing the same 20 ms figure as a conservative provisional ceiling (the bulk per-item unit of work is strictly lighter than a full proxy round trip). No measured evidence existed to derive a tighter number from, so the bound is explicitly marked **PROVISIONAL, pending-ratify** — a follow-up ticket must add an `internal/bulkops` benchmark before it is treated as measured rather than assumed. The page's own status remains `draft` (unchanged) throughout.

**Resolution pattern.** Follows ENG-1933 (M5 knowledge NFR baseline ticket): when a ticket's AC-NFR is sanctioned-deferred on an unfiled PRD NFR id/threshold, resolve it by filing the real id/threshold (draft, human-ratify-gated) and re-pointing the AC's citation at it — never by blank-ticking the AC or inventing a number outright.

**ENG-1467 finalized per v11 rules, not advanced to Done.** Ticket body updated: AC1–AC7, the named DoD test, T1–T4, and all 5b/5c test/gate checklist lines ticked (independently proven by 2 adversarial reviews). AC-NFR annotated **sanctioned-deferred pending PRD-ratify**, citing NFR-A1a, and left unticked (the bound is provisional, no benchmark yet exists). The 3 DoD-checklist gate boxes (AC-NFR-inclusive completeness / clean review PASS / all-PRs-merged) and T5's benchmark clause and T6 (review re-confirmation) stay open, each annotated. A progress comment was posted to ENG-1467 recording the resolution and the remaining blocker. **Status intentionally left In Progress, not Done:** PR #19 (`orvex-wiki-api`) is OPEN with all required CI checks green but not merged — the ticket's own §9/5e gate ("Done ONLY the orchestrator advances — after ... all this Issue's PRs merged") withholds Done independent of the AC-NFR resolution; this is a separate, unrelated blocker (merge, not NFR) and out of this task's scope to force.

**Ratify ledger.** NFR-A1a (draft, `8jAiCBifDW`) is added to the ratify ledger — pending human doc-ratify, does not gate ENG-1467's functional delivery, per the standing PO rule that ratifications formalize but never block.

**Discipline:** linearis CLI only for the Linear writes (description update + discuss comment), no MCP; temp-file body writes for both the ticket description/comment and this ledger entry; docmost write via the embed-safe `page update --operation append` route (lowest blast radius, preserves the page's existing tables/embeds), verified lossless via `--prosemirror`; appended under `flock` on `po-decisions/2026-07-10.md.lock`; committed on `dev` and pushed.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ADR-0024..0028 filed — staging + workgraph new-service decision set, ratify-ledger entries (2026-07-10)

**Gap.** The ratified-scope PRDs (`MyS79tK0k7` staging, `HTExyRFHhs` workgraph) and their architecture spines name five ADR-gated decisions that had no filed ADR: the two supersessions (OPS Librarian/Card-Contract lineage; OPS agent-side memory + FR-AI11), the two new `studio.*` event subdomains (incl. the per-service producer generalization of ADR-0010 and workgraph's P9 source-adapter registration), and the ADR-0003 D5 vocabulary-surfaces enrollment. The Decision-Records registry became unblocked on 2026-07-10 (0001–0023 catalogued, commit 0d8271fa), removing staging PRD OQ7's blocker.

**Ruling executed (PO instruction 2026-07-10: "file the adrs").** Filed five draft ADRs at space root of `orvexstudioarch`, doc_type `adr`, status `draft`, per the registry's filing mechanics (live scan confirmed 0024 free; no collisions):

- **ADR-0024** `ulVURyc2pd` — Agent Staging Area supersedes the OPS Librarian feature specs; Card Contract v1 → Proposal verbatim superset; OPS Librarian PRD (`yHnxCDuZ6O`) whole-doc at ratification, Card Contract (`MEcmPZvEdV`, FROZEN canonical) carry-forward at cutover; FR-SA12–16 section-level amend.
- **ADR-0025** `AqXpAlgvIv` — Workgraph supersedes the OPS agent-side memory design (`m93DraUfSE`) + FR-AI11 agent-side concepts (section-level); user/agent memory split — `/v1/memory` FormSpec, `studio.memory.*`, `studio_memory_*` stay product-side; no data migration.
- **ADR-0026** `W6MoFZgXEW` — `studio.staging.*` subdomain; service name `orvex-studio-staging` confirmed; per-service producer generalization of ADR-0010; staging events are telemetry, engine `wiki.*` is the sole reindex trigger.
- **ADR-0027** `Iff6l2VvGt` — `studio.workgraph.*` subdomain; workgraph as P9 source-adapter, rich-event-inline authoritative; fail-closed acl_primitive + bytes=0/P8 probes; GDPR purge event.
- **ADR-0028** `8kTZK91vHd` — 402 QUOTA_EXCEEDED vocabulary surfaces enrollment for both services (ADR-0003 D5 lane); surfaces only, pricing dimensions stay open.

Registry `32Huug8U4B` updated in place via faithful ProseMirror `--content-json` write (the markdown patch path was refused by the EMBED_DEGRADATION guard — table node): five rows added (24→29), next-free advanced 0024→0029, `--if-version` CAS, `--ratify-acknowledged` with reason; verified by full-text equality (16421 chars) + row count post-write.

**Precedent.** Registry filing mechanics (`32Huug8U4B` §Numbering, post-collision scan-before-file guard); ADR-0023 masthead/section format; judgment-call mode (orchestrator prompt `gkkUDzn277` §7) — draft ADRs at the head of the ratify ledger, ratification formalizes but does not gate delivery.

**Ratify ledger.** ADR-0024 (`ulVURyc2pd`), ADR-0025 (`AqXpAlgvIv`), ADR-0026 (`W6MoFZgXEW`), ADR-0027 (`Iff6l2VvGt`), ADR-0028 (`8kTZK91vHd`) — all draft, pending human doc-ratify; ratification formalizes but does not gate the new-services delivery (spaces/repos/infra proceed). Supersession link execution on `yHnxCDuZ6O`/`m93DraUfSE` waits for ratification per ADR-0024/0025 §Decision; `MEcmPZvEdV` flips only at Curator cutover.

**Discipline.** All wiki writes via docmost-cli (create at draft, never self-promoted; registry via `--content-json` + `--if-version` + `--ratify-acknowledged`); numbers claimed against a live page-list scan; appended under `flock` on `po-decisions/2026-07-10.md.lock`; committed on `dev`.

## New-services delivery executed — staging + workgraph: spaces, canon re-home, repos, GitOps, Linear (2026-07-10)

**Instruction.** PO 2026-07-10: "file the adrs, then … create the 2 new microservices complete with their wiki space, prd and architecture inside their dedicated wiki space and repos complete with all infrastructure etc and then update studioarch documentation … also need to create linear projects." ADR filing logged in the prior entry; this entry covers the rest.

**Ruling executed.**
- **Wiki spaces + canon re-home:** created `orvexstudiostaging` + `orvexstudioworkgraph` (sibling-space convention: two mandatory root pages + addendum child). Cross-space `page move` is server-rejected, so canon was re-homed by faithful copy: `page get --prosemirror` → PM-node transform (PRD pseudo-frontmatter dropped, H1s normalized to `<Kind>: <repo-name>`) → `page update --content-json` with explicit `--if-version`. New canon (all draft): staging PRD `mOzNDhn322` / Addendum `P5R7eCXJA0` / Architecture `GOacy9kdJz`; workgraph PRD `Kgp6JT3IOR` / Addendum `2KrwsvY5zL` / Architecture `bnbRUdCm6R`. Fidelity proven by character-level PM full-text diff — only the intended transforms differ. (The markdown create path was REJECTED after it corrupted PM tables: pipes inside cells (`{user|org}`) split columns and silently dropped 60–90 chars/page; also CLI auto-CAS lag produced silent `outcome:conflict` non-writes — both recorded to agent memory.)
- **Repos scaffolded + verified:** `orvexai/orvex-studio-staging` (dev, `bc0b3c6` + `c92555a`) and `orvexai/orvex-studio-workgraph` (dev, `246ae45` + `fb1d94a`) — knowledge-shape six-tier compiling skeletons with billing's private-module Dockerfile graft, spine-bound structural seeds, RLS-fail-closed 0001 baselines (real-PG18 testcontainer test), CI (cell-lint), Tekton, kustomize. Adversarial verify from CLEAN clones: build/vet/lint/kustomize/kubeconform + cell-lint + CS-❌ + secret scans all PASS. The one LOW finding (staging DB name underscored vs the hyphenated family convention) was aligned to the live-sibling convention pre-provisioning (`c92555a`).
- **GitOps registration:** `my-idp-apps` commit `2c12612` (main) — 2 files per service (app.yaml + AppProject), dev-only `branchFilter "^(dev)$"` bootstrap per the exact sibling precedent (no prod Application until dev is Synced+Healthy and the first image exists); `my-idp` platform untouched (zero platform edits needed); `studio-events` destinations deliberately omitted until the deferred Kafka triggers are re-enabled.
- **Linear:** projects "Orvex Studio Staging" (`9dd16c33`) + "Orvex Studio Workgraph" (`e22b6e30`) created under initiative "Orvex Studio" (now 17 member projects), team ENG, canon pointers in descriptions.
- **studioarch updated:** canon root `CxjFpIVUZY` + Index `GmKOk6xz0J` roster tables gained both service rows (PM table surgery, `--ratify-acknowledged` on the canonical root); `orvex-studio-api` PRD `85qj2wwU2L` (after FR-SA16) and `orvex-studio-ai` PRD `pbKI3BpQmY` (after FR-AI11) gained PROVISIONAL delegation notes pending ADR-0024/0025 ratify.

**Escalation — human-gated remainder (server-enforced, not skippable):** flipping the six OLD studioarch pages to superseded requires a human CONFIRM_TOKEN or the workspace gate (`allowForcedSupersede` is OFF; `allowForcedDelete` likewise for the stray probe page `BK1rYkvbV8`). Queued for the PO: either enable the gates once via `POST /api/orvex/settings/confirm-gate` and the agent runs the six supersedes + `--redirect-from` registrations, or run them from a human session. Until then the old pages remain live drafts (quarantined from grounding by status anyway).

**Ratify ledger.** Added pending human doc-ratify (formalizes, never gates): the six re-homed service-space pages (slugs above, all draft) and — from the prior entry — ADR-0024..0028. Post-ratify follow-ups bound to them: execute OPS supersessions (`yHnxCDuZ6O`, `m93DraUfSE` whole-doc; `MEcmPZvEdV` carry-forward at Curator cutover), then contracts authoring per ADR-0026/0027/0028.

**Discipline.** All wiki writes via docmost-cli PM-JSON path with explicit `--if-version` + exit-code branching; canonical bodies only with `--ratify-acknowledged` + reason; every page authored/updated at draft, nothing self-ratified; repos verified from clean clones by a non-implementing reviewer agent; GitOps followed the exact sibling bootstrap; appended under `flock`; committed on `dev`.
