
## Pack authoring proceeds on all four waves despite entry Box 6 RED — orchestrator judgment under PO standing authority (2026-07-15)

**Call: PROCEED.** Box 6 (fake-done auto-close hazard) is red at entry, but the hazard's trigger is a **PR MERGE** — the auto-close fires off merge events. Phase-1 pack authoring merges nothing: it authors specs/packs only. A red gate whose failure mode cannot be reached by this phase's work is not a block on this phase's work.

**Precedent:** §1's standing "proceed on the unblocked waves only" rule. Applied directly — waves 1–4 are unblocked with respect to Box 6's actual trigger.

**Verifier over-reach, corrected.** The Box-6 verifier emitted `blocksWaves: [1,2,3,4]` — i.e. it treated a merge-triggered hazard as blocking non-merging work. The adjudicator narrowed the block to **Phase-2 dispatch** (the first phase that merges). Recording this because a future run reading the raw verifier output alone would wrongly conclude Phase 1 was blocked.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Phase-2 dispatch is HARD-BLOCKED pending the Linear Git-automation toggle — NEW ruling, no precedent — orchestrator judgment under PO standing authority (2026-07-15)

**Call: HARD BLOCK on Phase-2 dispatch.** Phase-2 dispatch does not open until the Linear Git-automation toggle (the setting that auto-flips issue status off Git/PR events) is turned off/fixed.

**Why this is a NEW ruling with no precedent to apply.** ADR-0033 ("Linear-status-as-claim") ratified only **this morning**. It makes Linear workflow status the work-claim arbiter — i.e. it puts the claim mechanism onto the *exact field* the unfixed Git-automation toggle forges. Before ADR-0033, a forged Done was a reporting-integrity problem (bad, recoverable, caught by census). After ADR-0033, a forged status is a **claim forgery**: a merge event can silently hand or revoke a work claim between agents. No prior po-decisions entry covers this because the coupling did not exist until today.

**Consequence if we dispatched anyway:** multi-agent claim collisions with no detection surface — precisely the failure the collision-discipline doctrine exists to prevent, but now un-detectable because the detector *is* the forged field.

**Not a gate relaxation candidate.** This one does not get narrowed the way Box 6 did: here the trigger (merge) and the phase (dispatch → merges) genuinely intersect.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Reinstate P1-guard revert-discipline as a standing per-merge gate — orchestrator judgment under PO standing authority (2026-07-15)

**Call: REINSTATED, effective now, as a standing per-merge gate** (not a per-phase or per-wave check).

**Precedent: it was routine practice through 2026-07-12** — see `po-decisions/2026-07-12.md`, where P1-guard revert-discipline is exercised per merge and logged. It then **lapsed for 3 days** (07-13, 07-14, 07-15) with no explicit decision to drop it. A practice that stops without a ruling is drift, not a decision.

**Evidence the lapse had real cost:** **8 unverified Done-flips** occurred on 07-14/07-15 with **no po-decisions record whatsoever** — the ledger itself stops at 07-13 (see the "live reads outrank the snapshot" entry below). Eight status transitions with zero ledger trail is exactly the state the guard exists to make impossible.

**Scope of the reinstated gate:** every merge into a P1-tracked lane re-checks the guard and either passes or reverts; the check and its result are logged here.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Wave 3 authors all 16 defects as OPEN until each Done is forensically verified — orchestrator judgment under PO standing authority (2026-07-15)

**Call: BIAS TO OPEN.** All 16 defects are authored as OPEN in the Wave-3 pack. A `Done` reading in `initiative.json` is **not** sufficient evidence to author a defect as closed; each Done must be forensically verified (real artifact, real verification) before it downgrades scope.

**Evidence — the Done readings are not trustworthy.** `ENG-2044 / ENG-2046 / ENG-2047 / ENG-2052 / ENG-2053` read **Done** in `initiative.json`. All five are among the **8 unverified Done-flips** on 07-14/07-15. Five tickets, across **four different projects**, all flipped inside a **20-second window: 2026-07-14 12:36:48Z – 12:37:08Z**. No human and no honest per-ticket verification closes five tickets across four projects in 20 seconds. That is a machine event — the signature of the auto-close hazard Box 6 flags.

**Reasoning:** the asymmetry is decisive. Authoring a genuinely-done defect as OPEN costs one cheap re-verification. Authoring a **forged** Done as closed **silently deletes real scope** from the program with no detection surface — the defect vanishes from the pack and nothing ever looks for it again. Under uncertainty, take the recoverable error.

**Precedent:** consistent with `po-decisions/fake-done-forensics.md` and the standing "not claiming done on a partial" discipline.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Live reads outrank the 2026-07-13 program-status.md snapshot — orchestrator judgment under PO standing authority (2026-07-15)

**Call: DO NOT re-derive entry state from `program-status.md`.** Where the 07-13 snapshot and a live read disagree, **the live read wins**, unconditionally. The snapshot is a historical record, not a state source.

**Evidence — it is demonstrably stale on two load-bearing facts:**
1. **Ratification.** It says the ADRs are **draft**. They are **canonical**.
2. **Prod posture.** It says **prod is vanilla**. Prod **flipped ON on 07-14** — commit `725090bd`, **8/8 acceptance accepted**.

Both are not cosmetic drift: an entry-state derivation off this snapshot would conclude the ADRs carry no authority (they do — including ADR-0033, which hard-blocks Phase-2 above) and that prod is untouched (it is not). Either error alone corrupts the phase plan.

**Reasoning:** the ledger's own 3-day gap (07-13 → 07-15, see the P1-guard entry) is itself the proof that snapshots age silently here. Nothing wrote "this snapshot is stale" — it just was.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Next-free ADR number = 0036 — orchestrator judgment under PO standing authority (2026-07-15)

**Call: next-free ADR = 0036.**

**Derivation:** a **live child-scan of registry `32Huug8U4B`** — i.e. the actual ADR pages that exist, not a table describing them.

**Authoritative over two conflicting sources, both rejected:**
- the **registry's own table** on `32Huug8U4B` — stale (it is missing rows; see the refresh follow-up below);
- a **memory note claiming next-free = 0029** — stale by seven numbers.

**Reasoning:** the pages are the ground truth; the table and the memory note are both derived caches of the pages, and both drifted. Same doctrine as the `program-status.md` call above — live read beats snapshot.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Prompt page yXUWpQpRjx CORRECTED under explicit operator authorization — human ratify OUTSTANDING — orchestrator judgment under PO standing authority (2026-07-15)

**Call: CORRECTED, kept at `status=draft`.** The prompt page `yXUWpQpRjx` was edited in **two PM-JSON surgery passes**. It was **not** self-ratified.

**Authorization:** explicit operator instruction — *"fix the prompt if there are mistakes"*. This is direct operator authority for the edit, not an inference from standing authority.

**What was corrected:**
- **Pass 1 — 8 staleness defects** (the prompt asserted facts that had since changed).
- **Pass 2 — ~15 repo-reality defects** (the prompt asserted things about the repo that were never true of it).

**Method:** PM-JSON surgery only — per the known edit-path corruption hazard, markdown write paths shred PM content on this engine.

**OUTSTANDING — recorded as an open item, not a closed one:** the corrected prompt page **remains at `status=draft`. A human ratify of `yXUWpQpRjx` is still required.** The operator authorized *fixing* the prompt; nobody authorized *promoting* it, and AI never self-promotes. Until a human ratifies, downstream runs must treat `yXUWpQpRjx` as a draft.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Surgery pass 1 exceeded its literal brief on one node — flagged, not buried — orchestrator judgment under PO standing authority (2026-07-15)

**Call: SCOPE EXCEEDED DELIBERATELY, on one node, disclosed.** Pass 1's brief named **two** sites of the milestone/MCP falsehood. Pass 1 corrected a **third**: **§9 exit box 7**.

**Reasoning — judged in-scope for the defect's intent, not its letter.** The defect being removed is the falsehood itself, not two specific coordinates of it. Leaving box 7 untouched would have (a) **re-asserted the very falsehood** the pass existed to delete, from an exit gate, and (b) let **Phase 1 "exit" with milestones uncreated** — a false-green on the phase's own exit criteria. Correcting two of three sites would have produced a prompt that contradicts itself and still exits wrong.

**Disclosure over silence:** flagged here rather than buried in the diff. The change is **trivially revertible** (one node, PM-JSON). If the PO judges the literal brief should have bound, revert that node and re-file the third site as its own defect — no other pass-1 node depends on it.

_orchestrator judgment under PO standing authority (2026-07-15)._

## UNRESOLVED — pack count 16 vs 17 vs 18 — ESCALATED, NOT decided — orchestrator judgment under PO standing authority (2026-07-15)

**Call: NO CALL MADE. This is escalated to the PO. The packs are NOT renumbered.** Logging it OPEN is the decision; deciding it silently would not be.

**Two coupled discrepancies, both unresolved:**
1. **The new §4 F1 gate implies the F1 delivery client enters as a NEW service with its own pack** — which on a **GO verdict** pushes the pack count from **16 → 18**.
2. **Standing, unexplained:** the exit criteria name **16 services**, while the initiative carries **17 component projects**. **The 17th is unaccounted for** — nobody has identified which project it is or whether it is in scope.

**Why this is not an orchestrator call.** Standing authority covers *non-product* ambiguity. Pack count is scope: it decides what gets built and what silently doesn't. Picking a number here would either invent a service or delete one, and the 17th project's identity is a fact nobody currently holds — a default would be a guess dressed as a ruling. This is the "escalate only physically-human-only / product items" carve-out, applied.

**Needed from the PO:** (a) identify the 17th component project and rule it in or out; (b) rule whether the F1 delivery client is its own pack on GO. **Until then: no renumbering, packs stay as authored.**

_orchestrator judgment under PO standing authority (2026-07-15)._

## Non-blocking follow-up — refresh the 32Huug8U4B ADR registry table — orchestrator judgment under PO standing authority (2026-07-15)

**Call: FOLLOW-UP, non-blocking.** Not done this session (the ledger-only write rule bound this pass); filed here so it is not lost.

**The registry table on `32Huug8U4B` needs three corrections:**
1. **Add missing rows:** **0029, 0030, 0032, 0033, 0034, 0035**.
2. **Correct 7 rows stale-marked `draft`** that are in fact **live-canonical** — including **ADR-0033**, which is currently hard-blocking Phase-2 dispatch (above) while its own registry row claims it is a draft.
3. **Bump next-free to 0036** (per the live child-scan call above).

**Why it matters despite being non-blocking:** the registry is the surface humans and future runs read to learn ADR authority. It currently under-reports canon — which is how the "next-free = 0029" memory note and the `program-status.md` "ADRs are draft" staleness both survived. Every consumer of this table is being told ratified rulings are drafts.

_orchestrator judgment under PO standing authority (2026-07-15)._

## Fake-done auto-close toggle — API PROBE (orchestrator judgment under PO standing authority)

Time: 2026-07-15T19:55:04
Context: Directed to fix "ENG git automation maps PR-merged -> Done" (owed since 2026-07-08; urgent under ADR-0033 which makes Linear status the work-claim substrate). Directive: PROBE the API first; do not inherit the "human-only" belief.

### What the probe found (evidence in scratchpad residuals/toggle/)
1. API IS CAPABLE — the inherited "human-web-UI-only, no API path" belief is FALSE. Linear GraphQL exposes:
   gitAutomationStateCreate, gitAutomationStateUpdate, gitAutomationStateDelete
   (+ gitAutomationTargetBranchCreate/Update/Delete). GitAutomationStateUpdateInput exposes a WRITABLE stateId (the target workflow state). Git automation is fully mutable via API.
2. Workspace = Orvex AI (orvexai). Two teams only: ENG (Engineering, b19ad9ba-65d2-4e7a-8ff0-3eaf8dfcbd73) and DNI (Dniasoff, c5321fc4-9c27-4aaa-ab61-191390566e9d).
3. ENG team gitAutomationStates: EMPTY. Built-in git states (draft/start/review/merge/mergeableWorkflowState): ALL NULL. => There is NO PR-merged -> Done automation on ENG. A merged PR on an eng-<id>-* branch is NOT auto-forced to Done by any Linear-native git automation. The hazard, as named on ENG, is not present.
4. The merge -> Done automation that DOES exist is on the DNI team:
   gitAutomationState id=535d9179-b65c-4f7e-930f-eab8d923e471  event=merge  -> state="Done" (type=completed).
   DNI also has start->In Progress and review->In Review. DNI governs only DNI-### issues (Daniel's personal team) — NOT eng-<id> tickets, NOT the ADR-0033 claim substrate (spine is ENG-2091..2110).

### Decision (no change applied)
- Did NOT modify ENG: there is nothing to repoint — ENG has no merge automation, so the desired end-state ("a merged PR does not force Done") ALREADY HOLDS for the ENG claim substrate.
- Did NOT modify DNI: task rules explicitly forbid changing any other team ("Do NOT change any other team"); and DNI's merge->Done does not govern eng-<id> tickets or the claim arbiter's field, so changing it would not serve the stated goal.
- Net: both inherited beliefs were wrong — (a) it is NOT human-only (API is fully capable), and (b) the offending automation is NOT on ENG.

### Escalation to PO (premise mismatch — needs adjudication)
- Confirm ENG intentionally has NO merge automation (hazard already neutralized for the claim substrate), OR
- If policy wants DNI's merge->Done also made non-terminal, authorize touching DNI (currently out of scope by the task's own rule). One reversible API call would do it (see revert recipe).

### Revert / apply recipe (if PO authorizes acting on DNI's merge->Done)
- To repoint DNI merge to a non-terminal state: mutation gitAutomationStateUpdate(id:"535d9179-b65c-4f7e-930f-eab8d923e471", input:{ stateId:"<non-terminal DNI state id>" }) — or delete just that automation: gitAutomationStateDelete(id:"535d9179-b65c-4f7e-930f-eab8d923e471").
- To restore DNI original: gitAutomationStateUpdate(id:"535d9179-...", input:{ stateId:"<DNI Done state id>" }), or gitAutomationStateCreate with event=merge -> Done if deleted.
- Nothing was changed in this run, so nothing needs reverting.

## Interactive PO rulings — the 5 Definition-Factory must-resolves (2026-07-15)

PO adjudicated the five surfaced must-resolves live. Recorded here as the authoritative rulings; follow-up amendments to the (now-canonical) packs are queued.

1. **Extension legal counsel (FR-PA4) — DEFER to pre-launch; BUILD FIRST.** Writing/testing the extension distributes nothing → no ToS/store exposure while building. Hard line: build ≠ launch — do NOT enable it for real users in real ChatGPT/Gemini sessions until counsel clears. Lead providers = the spike's GO set (ChatGPT, Gemini); Claude/Grok stay DEGRADE; re-check Google ToS (rev 2026-07-30) before Gemini go-live.

2. **Extension store posture — CHROME WEB STORE ONLY.** Least to build; store-rejection risk explicitly ACCEPTED. The self-host/enterprise-sideload fallback (ENG-2726) is NOT built now — kept as a documented contingency lever to pull if Google rejects under the ~Aug-2026 prohibited-products policy.

3. **Clerk/tenant lifecycle ownership — WORKFLOWS OWNS EVERYTHING.** orvex-studio-workflows is the single control-plane owner of provision + purge/deprovision + ALL lifecycle workflows (upgrade/rebuild/reconcile/billing-lifecycle). Decisively settles the contested identity↔workflows seam. Design constraint for the ADR: identity still holds Clerk creds + the tenant→cell registry, so workflows gets its OWN scoped credentials to Clerk + the stores via the ADR-0034 lanes (not broad standing access). Follow-ups: write the lifecycle-ownership ADR; amend the identity + workflows canonical packs to record this ruling (supersedes the earlier "provision calls identity" arrangement as the ownership statement).

4. **orvex-ds tokens — EXTRACT TO ONE VERSIONED PACKAGE.** Source = crew/yafet apps/web/src/styles/orvex-ds.css (ADR-0005 = design-system-of-record). Extract into @orvex/ds-tokens (versioned); UI + extension both import it; single source, no drift. Wave-0 UI build task, before any surface is ported. The UI-canon amendment (UwsOKqter2) already anticipates this.

5. **ENG-1929 (lib Citation type not in contracts) — EARLY PHASE-2, BUNDLED.** Land it with the first real v0.1.4 seam-pin cut (add Citation → contracts draft seam → cut v0.1.4 → re-pin lib). Blocks nothing today (draft seams are OK to build against in-flight); becomes a hard GA blocker so lands well before. ALSO add a lib CI determinism gate (vendored == pin) so this drift can't recur silently.

_orchestrator judgment recording explicit PO rulings under standing authority (2026-07-15)._
