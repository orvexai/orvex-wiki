# PO Decisions — 2026-07-11

## ENG-1948 — Done-gate DoD checklist derived retroactively — 2026-07-11

**Judgment call (orchestrator, PO standing authority).** ENG-1948 (cost-cap 402
cutover, PR #29 merged 7e1d2eb) was blocked at the Done gate: review1 verdict
PASS with mutation-tested verifiedAcs on record (scope-1-preflight,
scope-2-readentitlement, scope-3-route-cutover, NFR-AI4-fail-closed,
AC11-budget-gate-reachable, AC4-casl-403, F1-edge-auth-401, FR-AI16-image-route),
but the ticket body — authored pre-standards, recovered mid-stream from
ENG-1575's M8 escalation — carried zero native DoD checkboxes, so the gate
correctly refused to self-tick a checklist that did not exist.

Ruling applied: derive (not invent) a DoD checklist from the ticket's own
"Scope" section plus review1's already-verified ACs, one box per verified AC,
plus a dated deferred-annotation line for items 4-5 (LiteLLM tape corpus /
ENG-1575 re-dispatch) under the moved/deferred exemption — those two were
already PO-ratified out-of-scope on 2026-07-10 and moved to ENG-1575 T1/T4.
Appended to the ticket description as a dated "DoD (derived 2026-07-11...)"
section, explicitly labeled non-fabricated (retroactive application of the
Issue Authoring Prompt with evidence already in hand). 8/10 boxes ticked, 2
left unchecked with the deferred annotation. Ticket advanced to Done, cache
refreshed (`_bmad/lnr/tools/linear-sync.sh issue ENG-1948` — confirmed
`status: Done`).

Logged, no action: review1's non-blocking note that the implementer
force-removed `/tmp/worktrees/dev-check` (not its own worktree) during
baseline-diffing. No code impact; worktree ownership/cleanup left for its
owning pass — out of ENG-1948's scope.

Evidence: /tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/act3/ENG-1948-review1.md;
Linear comment c99915d3-5df7-4220-8d4b-f92897332aa9 on ENG-1948.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

**Judgment call (orchestrator, PO standing authority, 2026-07-11).** ENG-1949
(M14 CAP + DEGRADE probes) — Deliverable 1 (design) delivered + reviewed PASS
2026-07-10, 6/6 §2 boxes ticked. Adversarial review of the Deliverable-2 build
attempt (`orvex-studio-lib` PR #30) found: code clean+green (all CI gates
re-run independently), but delivering ONLY the lib-side `TestM14Degrade`
fail-loud test scaffold — neither probe endpoint (CAP on billing, DEGRADE on
api) exists, so zero Deliverable-2 ACs are met. Reviewer verdict: ESCALATE,
not FINDINGS — no code defect, the gap is (a) missing cross-repo infra and
(b) the split-and-close call the ticket's own §1 already prescribes ("the
choice [of owning service] is itself a design decision, not a given" — now
resolved by Deliverable 1; §1 explicitly frames unreviewed dual-repo probe
work under launch-gate pressure as the exact fabrication risk this ticket
was split out to avoid).

Ruling applied (split-and-close precedent, same pattern as prior ticket
splits): (1) merged PR #30 — `ci-success` (the only required check on
`orvex-studio-lib`'s `dev` branch protection) was SUCCESS; the two RED checks
(`M14 closing gate`, `M14 AC9 DEGRADE probe leg`) are by-design
continue-on-error named-DoD gates, honest-RED until their endpoints ship,
matching the already-merged `m14-gate-ci` precedent — not a merge blocker.
(2) Filed **ENG-1952** (CAP probe endpoint, `GET /internal/rehearsal/cap-status`,
project **Orvex Studio Billing**, set on create + verified by re-read) and
**ENG-1953** (DEGRADE probe endpoint, `GET /internal/rehearsal/degrade-status`,
project **Orvex Studio API**, set on create + verified by re-read). Both
quote ENG-1949 §2.1/§2.2's reviewed design verbatim as design authority (do
not re-litigate owning-repo/real-path/status-masking rulings), both
`--blocks ENG-1578` + `--relates-to ENG-1949`, both confirmed via
`linearis issues read` post-create (project, relations). (3) Reconciled
ENG-1949: appended dated `[MOVED → ENG-195x, 2026-07-11]` annotations to all
4 unticked §3 boxes (none ticked — no code in this ticket's scope satisfies
them), added a §5 split-executed closure note, posted a closing comment
citing both new tickets + the PR #30 merge, moved status **Done**. ENG-1578's
AC9/AC10 now track directly against ENG-1952/ENG-1953 (both `blocks
ENG-1578`), not just transitively via ENG-1949.

No rate-limit hit this pass — `staged/` stayed empty, no replay needed.

Evidence: `orvex-studio-lib` PR #30 (merged, commit `00f05e35`);
`/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/act3/ENG-1949-review1.md`;
Linear ENG-1949 close comment `c94c9933-14ed-4b7c-b46e-0ffbd5a0c2bf`; ENG-1952
(`d14c2df5…`), ENG-1953 (`eecbc63f…`);
`/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass17/1949-split.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

**Judgment call (orchestrator, PO standing authority, 2026-07-11).** ENG-1535
(M10 e2e closing gate) armer pass, following ENG-1944 (identity token
surface) landing. Read PR #19's merged `.github/workflows/e2e.yml` +
`tests/e2e/env.ts` for the exact 6 env var names (`E2E_BFF_BASE_URL`,
`E2E_AUTH_TOKEN`, `E2E_CAPPED_AUTH_TOKEN`, `E2E_CROSS_PRINCIPAL_AUTH_TOKEN`,
`E2E_CROSS_TENANT_AUTH_TOKEN`, `E2E_SCOPE_LIMITED_AUTH_TOKEN`).

**In-job minting — investigated, genuinely not possible today.** Read
`orvex-studio-identity @ bf2f602` (ENG-1944) `internal/server/server.go` +
`internal/token/*` directly (source, not guesswork). Both live token paths
require an ALREADY-verified Clerk principal: `POST /v1/tokens` is
`requireSession(mintToken)` (needs an existing verified bearer to mint
another); `POST /v1/exchange` requires a real Clerk IdP session token
verified via `clerk.Resolver.Verify` (RS256/JWKS) — there is no
client-credentials / service-account grant type on this surface. The one
route that looks like a service-mint (`POST /api/clerk/mint-session`) is
explicitly S2S-impersonation gated by mesh/network policy (not app-layer
auth) and mints an HS256 JWT signed with an in-process ephemeral
`magicLinkSecret` for the magic-link iframe round trip — NOT a real
identity-verified token; using it here would be exactly the fake/self-signed
substitution `tests/e2e/env.ts`'s own error message and CS §11 forbid.
Conclusion: ALL 5 personas fall back to static (no partial win) — documented
on the ticket, nothing fabricated.

**BFF base URL — resolved the FQDN, but found the Service is not actually
live.** ArgoCD MCP reads (list/get_application, get_application_resource_tree,
get_resources — no `argocd sync` / `kubectl apply`) show `orvex-studio-api-dev`
Application is `OutOfSync`/`Degraded`: `ExternalSecret orvex-studio-api-secrets`
(OpenBao path `apps/orvex-studio-api-dev/api`, store `openbao-apps`) has
never synced (`Ready=False`, "could not get secret data from provider",
`refreshTime: null` since creation), blocking every downstream sync-wave —
Service/Deployment/ServiceAccount/ConfigMap/PDB are all `Missing`. Filed
**ENG-1954** (project **Orvex Studio API**, set on create + verified by
re-read, `--blocks ENG-1535`) rather than attempting any fix myself
(never-kubectl / never-argocd-sync discipline holds). Wired
`E2E_BFF_BASE_URL` = `http://orvex-studio-api.orvex-studio-api-dev.svc.cluster.local:8080`
(derived from `deploy/kustomize/app-manifests/service-api.yaml` port 8080 +
the Application's actual destination namespace) as a GH Environment secret
regardless — correct and ready the moment ENG-1954 resolves and the Service
comes up; not a credential, so no ALL-REAL violation to pre-wire it.

**Executed.** Merged PR #19 (`orvex-studio-api`, squash, harness-only diff —
9 files, no product code, 8/8 CI green, no required review outstanding,
branch protection `ci-success` only) → `cccdc5f3`. Created GH Environment
`staging` on `orvex-studio-api` (did not exist — `environments` was empty
before this pass) and set `E2E_BFF_BASE_URL` as its one secret (piped via
`gh secret set`, value never echoed; the 5 token secrets deliberately left
UNSET per the finding above, not silently skipped). Triggered
`workflow_dispatch` (`cell=staging`) once: run `29128392326` — `static-gate`
✓ SUCCESS (`no-sibling-fake-on-gate-path.spec` 2/2 green — the grep gate that
needs no deployed cell); `e2e` job ✗ FAILURE, and the failure is the honest,
designed one — `no-sibling-fake-on-gate-path` passed inside that job too,
and the 4 real specs (`M10BffProductFlowE2EAgainstRealSatellites`,
`m10-cap-enforcement`, `m10-degrade`, `m10-isolation-probe`) threw
`loadE2ECellConfig`'s loud FAIL-never-SKIP error naming exactly the 5 still-
missing token vars (confirms `E2E_BFF_BASE_URL` is no longer in the missing
list — the wiring took). Recorded verbatim on ENG-1535. No DoD/AC checkbox
ticked — the named test is still not GREEN (blocked on ENG-1954 +
real-token provisioning, both now explicit, tracked blockers, not vague
"pending infra").

**Discipline:** linearis CLI only (`issues discuss`, `issues create` ×1 with
`--project` on create + `issues read` re-verify, `issues relations add`);
no `rate_limited` hit, `staged/` stayed empty; temp-file body writes
(`Write` tool) for both the ENG-1954 description and the ENG-1535 comment;
refresh-on-write; secret values piped only, never printed (`gh secret set`
stdin; only secret *names* read back via `gh api .../secrets`); trailer
exactly `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the
Linear comment, ENG-1954, and this repo entry. No `kubectl apply` /
`argocd sync` at any point. No gate relaxed — the 501-stub M10 handlers are
untouched and their honest-501/degrade behavior is exactly what the harness
is designed to observe once tokens land, not something this pass papered
over.

Evidence: PR https://github.com/orvexai/orvex-studio-api/pull/19 (merged
`cccdc5f3`); run
https://github.com/orvexai/orvex-studio-api/actions/runs/29128392326; ENG-1954
(`62664dc7…`); `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass17/1535.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1405 contract ruling + enactment — cell-routing claim vocabulary codified on the frozen Principal (2026-07-11)

_Orchestrator judgment under PO standing authority (2026-07-11)._

**Blocker.** `orvex-studio-mcp` cell-routing reads `sid`/`cell`/`cell_epoch`, but the FROZEN Principal contract (`identity/principal.schema.json`, `additionalProperties:false`, ENG-1537) lacked `cell`/`cell_epoch`; the cell-vocabulary ADR was mooted.

**Ruling.** The contract follows what identity ACTUALLY MINTS (contract-first precedent ENG-1359/PR #34; identity-as-source `8e610cc`). Amend the Principal ADDITIVELY to codify exactly that vocabulary; `sid` stays out.

**Verified (what ENG-1944 emits).** ENG-1944 (`orvex-studio-identity` PR #16 @ `bf2f602`) landed the token surface. The shared verifier `orvex-studio-lib/pkg/auth` already PRODUCES `Principal.Cell`/`.CellEpoch` (`principal.go:44-45`, `omitempty`) and the stale-cell 421 guard already READS them (`cell_claims.go`, `ErrStaleCell`); authtest vectors mint both; `gen/registry.go` is the `CellID`/`CellEpoch` source. Identity does NOT mint `sid` — that is the retiring Studio-session id (`SessionTokenClaims`), a separate contract. So the codified vocabulary is exactly `{cell, cell_epoch}`; the contract had drifted behind the code.

**Enacted.**
- **Contracts PR #35 MERGED** (`orvex-studio-contracts` → dev, squash `35`): `principal.schema.json` gains OPTIONAL `cell` (string, unpatterned so the `solo` sentinel validates) + `cell_epoch` (`integer`, `minimum:0`). `required` / `additionalProperties:false` / `x-status:pinned` all UNCHANGED — the additive-optional extension the freeze note (D-CON-1) reserves. NO version bump (`VERSION` stays `0.1.2`, M0 seam-freeze `test_version_frozen` honoured; documented under CHANGELOG `[Unreleased]`, cut on next tag). Conformance vectors `identity/vectors/principal-cell/` (3 valid + 2 invalid, incl. `principal-with-sid.invalid.json` locking `sid` OUT) + a `TestPrincipalClaimSchemaContract.test_principal_cell_claims_additive` gate. All 15 CI checks green; local `make validate` + `make schemas` 91/91.
- **ADR-0030 filed** (`orvexstudioarch`, slug `yOyavcWHEz`, `doc_type:adr`, status **draft — pending PO doc-ratify**): "Cell-routing claim vocabulary on the Principal (`cell`/`cell_epoch` additive; `sid` stays out)". Complements ADR-0029 (`WZWmazrlS0` — the edge-verifier cutover, which settled `sid` via `tenant:subject` rebind but was silent on `cell`/`cell_epoch`); not a contradiction.
- **Consumer aligned** — `orvex-studio-mcp` `src/transport/http.ts` + `src/auth/types.ts` on `eng-1405-work` already read exactly `claims.cell`/`claims.cell_epoch`; comment-only pin (commit `b957064`, zero behaviour change) records the ADR-0030 codification + the deferred `accountFromClaims` `sid`→`tenant` repoint (the SessionTokenClaims→Principal cutover, ENG-1405 F1/F2/F3, stays open on the ticket).
- **ENG-1405 commented** (discussion `778725f5`) with the ruling + what remains.

**Number-collision guard (scan-registry-first).** ADR-0030 claimed only after a LIVE `orvexstudioarch` scan showed 0029 already taken by `WZWmazrlS0` (edge-verifier cutover) and 0030 free — the scan caught the collision and redirected the number. The family Decision-Records registry `32Huug8U4B` numbering-table update is DEFERRED (its own "next-free" line is already stale per the ADR-0021-renumber pass note, and PM table surgery on the canonical registry carries corruption risk that prior passes explicitly held out-of-scope) — the ratify ledger is the operative registration for a draft ADR.

**Ratify ledger.** ADR-0030 (`yOyavcWHEz`) — draft, pending human doc-ratify; ratification formalizes but does not gate delivery (drafts operative per precedent). Bound follow-up: a future pass reconciles the `32Huug8U4B` registry numbering table (0017/0019 collisions + next-free line) and, at the next contracts release cut, the additive Principal change becomes a consumable minor tag.

**Discipline.** linearis CLI only for the Linear write (temp-file body via `"$(cat …)"`); no MCP. Contracts change on a branch, gates proven locally BEFORE push, CI green BEFORE merge. ADR authored at draft via docmost-cli, verified lossless (H1 + all sections + inline code intact). http.ts change is comment-only, diff-verified as no-code. Trailer `Co-Authored-By: Claude Sonnet 5`. No `kubectl apply` / `argocd sync`. No secret printed. No gate relaxed (the M0 seam-freeze VERSION pin honoured, `additionalProperties:false` kept closed). Appended under `flock` on `po-decisions/2026-07-11.md.lock`.

Evidence: PR https://github.com/orvexai/orvex-studio-contracts/pull/35 (merged); ADR-0030 https://docs.eu-central-1.myidp.cloud/s/orvexstudioarch/p/adr-0030-cell-routing-claim-vocabulary-on-the-principal-cell-cell-ep-yOyavcWHEz; MCP commit `b957064` (`eng-1405-work`); ENG-1405 comment `778725f5`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass17/1405-contract.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1419 finisher — AC5 split + AC2 reworded, advanced to Done (2026-07-11)

_Orchestrator judgment under PO standing authority (2026-07-11)._

**State at pickup.** orvex-cli routing spine delivered (PR #5, `orvex-cli` repo); review1 + review2 (reviewer≠implementer, independent) both re-ran all repo CI gates green and proved `TestOrvexCliNamespaceRouting` non-tautological by mutation (wiki route swap, dfm corruption, bearer drop each fail as expected; `TestNoDaemonZeroCache` landed in fix-pass 1, also mutation-proven). review2 verdict was ESCALATE on exactly two items: AC2's literal "admin→console" wording, and AC5's scope/completeness. Everything else (AC1/AC3/AC4/AC6/AC7/AC8) was independently VERIFIED by both reviewers.

**RULING 1 — AC5 split (split-and-close precedent).** Bearer-forwarding (the transport-attaches-bearer half of AC5) is delivered and mutation-proven in ENG-1419; the OIDC RP handshake + persistent `auth.Store` (keyring/encrypted-file, ≥2 impls — `internal/auth` untouched, only the in-memory `ForwardStore` exists, `login`/`logout`/`status` are `NOT_IMPLEMENTED` stubs) is genuinely out of reach for this scaffold ticket. Split to a new follow-up: **ENG-1956** ("orvex-cli auth — OIDC RP login + persistent auth.Store (keyring/encrypted-file)"), created via `linearis issues create` with `--project "Orvex CLI" --status Todo --blocked-by ENG-1443 --relates-to ENG-1419` set on create, verified by re-read (project/status/relations all confirmed live). Authored to the Issue Authoring standard (9VUHxAcoXw): quotes ENG-1419's T4 scope verbatim + both review findings, defines its own DoD test (`TestAuthOidcLoginAndPersist`), 4 ACs, T1-T5 tasks, tier/seam/mocking sections. ENG-1443 was verified BEFORE the split (`_bmad/lnr/tools/linear-sync.sh issue ENG-1443` → status Done) — noted on ENG-1956 that its landed RP contract may already unblock the codegen dependency that blocked ENG-1419's implementer, and that the live dev-cell identity service may serve as a fixture source (capture/replay, not a live test-time dependency). ENG-1419's AC5 checkbox, T4 checkbox, and the `TestAuthBearerAttached` §5b line all carry a dated moved-to annotation naming ENG-1956; none were ticked (moved out of this ticket's scope, not satisfied here).

**RULING 2 — AC2 reworded to the real invariant (per the ENG-1604 AC1 precedent).** The ticket's literal "admin→console" mapping is unsatisfiable and wrong: `orvex-studio-console` is a UI-only troubleshooting satellite over LGTM (obs contract FR-C18) — never a routable API service, and landed config's `AllServices()` never named one. The real invariant AC2 protects — every admin verb routes to the domain service that OWNS the operation (`reindex`→knowledge), preserving the no-monolith invariant — is what the implementation does and what both reviewers independently verified (sibling bases pinned to `127.0.0.1:1`, never hit; admin hits knowledge's `/v1/admin/reindex`). The AC2 line in the ticket body was reworded in place to state that invariant, the literal "admin→console" text struck with a dated note, and the box ticked citing review2.

**DoD/AC sweep.** Every remaining checkbox in ENG-1419's body was ticked with a specific evidence citation (review1/review2/build/fix1, all dated 2026-07-11) or left unticked with a dated non-blocking annotation: the AC5-family boxes (moved, per Ruling 1), the §9 stage-cadence box (CLAIM + an ERRORS/ESCALATIONS comment evidenced; discrete PLAN/PROGRESS-per-step cadence not independently evidenced — logged as a process gap, not re-litigated, non-blocking since the technical gates are load-bearing here), and the AC7 `TestSupersetPayloadTolerated`/`TestTransportSeamInjectable` box (review2: seam exists, exported, compiles, invariants hold on inspection — but no dedicated injection test; a coverage gap, not a correctness defect). Nothing ticked without an evidence trail; the two prose mentions of the `- [ ]`→`- [x]` convention itself (TICKABILITY intro, §9 item 7) are not checkboxes and were left untouched. Final tally in the cache: 35 ticked, 5 genuinely open (all dated-annotated).

**Non-blocking finding logged, not filed as a ticket.** review2: the transport maps every non-2xx response to `CodeServiceUnreachable` — frozen-vocabulary-compliant (typed errorCode + non-zero exit + no stack) but coarse: a 404 and a 500 read identically to the user. Logged as a candidate for a future small fix in the ENG-1419 comment thread; explicitly not filed as its own ticket per the finisher instruction.

**Enacted.** Comment posted recording both rulings + the DoD sweep + the non-blocking candidate (`linearis issues discuss ENG-1419`). ENG-1419 advanced Todo/In Progress → **Done** (`linearis issues update ENG-1419 --status Done`), cache refreshed (`_bmad/lnr/tools/linear-sync.sh issue ENG-1419` → confirmed `status: Done`). ENG-1956 confirmed `status: Todo`, blockedBy ENG-1443 + relates ENG-1419 both live in `.cache/linear/`.

**Discipline.** linearis CLI only (no MCP) for every write; description/comment bodies written via temp file + `"$(cat …)"`, never inline heredoc-in-flag. Linear quota checked before starting (`linear-sync.sh quota`: 15 requests remaining at pickup — budget was planned to 7 calls total: verify ENG-1443, create ENG-1956, verify-by-reread, update ENG-1419 description, comment, status→Done, final refresh; all 7 completed without hitting rate-limit, no staging needed). Refresh-on-write after every Linear write that touched cache-relevant state. New ticket set project + status on create, verified by re-read. Trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the Linear comment. No code touched this pass (Linear-only finisher). Appended under `flock` on `po-decisions/2026-07-11.md.lock`.

Evidence: ENG-1419 https://linear.app (identifier ENG-1419, now Done); ENG-1956 (identifier ENG-1956, Todo, project Orvex CLI, blockedBy ENG-1443, relates ENG-1419); orvex-cli PR #5; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/act3/ENG-1419-review1.md`, `ENG-1419-review2.md`, `ENG-1419-build.md`, `ENG-1419-fix1.md`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass17/1419-finish.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1950 Cloudflare token hunt — existing token found, scope-insufficient for Worker deploy (2026-07-11)

_Orchestrator judgment under PO standing authority (2026-07-11)._

**Task.** ENG-1950's live-deploy AC1/AC3/AC4 (edge `/api`→`/v1` cutover via `wrangler`) escalated: the seat has no `CF_*` env, unauthenticated wrangler. Hunted for an existing platform-owned Cloudflare API token before asking a human to mint a fresh one.

**Found.** `cloudflare-api-token` — a K8s Secret already live in both `cert-manager` and `external-dns` namespaces on the `eu-central-1` cluster (133d old). Not in OpenBao KV (checked `apps/*` and `platform/*` listings — no Cloudflare path); its source of truth is AWS SSM `/my-idp/eu-central-1/bootstrap/credentials` → `.cloudflare.api_token`, applied by `my-idp/scripts/bootstrap/setup-platform-credentials.sh:create_cloudflare_credentials`.

**Scope assessed honestly.** Per the manifests that consume it (`my-idp/platform/apps/cert-manager/resources/letsencrypt-clusterissuer.yaml` DNS-01 solver comment, and `my-idp/platform/apps/external-dns/values.yaml` Cloudflare provider config), the token is `Zone:Zone:Read` + `Zone:DNS:Edit` only — provisioned for ACME DNS-01 challenges and external-dns record sync (domains `myidp.cloud`, `orvex.ai`, `orvex.dev`). **No `Account.Workers Scripts:Edit` grant.** This is exactly the "cert-manager DNS-edit token lacking Workers deploy scope" case flagged as the honesty check for this pass — confirmed true, not forced.

**Cross-checked against repo canon.** `orvex-studio-ui/README.md` (§Cloudflare Worker Static-Assets serving path, ENG-1567) states the M14 DNS/cert cutover Issue "decides when traffic actually moves" — the live cutover is explicitly a separate gate, consistent with the ENG-1950 build-pass escalation's M14-conflict note. `gh secret list` on `orvexai/orvex-studio-ui` confirms no `CF_API_TOKEN` (or any `CF_*`) secret is wired yet — nothing armed, nothing to un-arm.

**Enacted.** Posted a comment on ENG-1950 (discussion `1b883fa6-34a0-4b3e-9dc4-175cc7e1364a`, verified live by re-read) naming the existing token, its exact scope, the gap, and the precise ask: mint a new token scoped `Account.Workers Scripts:Edit` + `Zone.DNS:Edit` for `orvex.dev`/`orvex.ai`, store in OpenBao, wire as `CF_API_TOKEN` on `orvexai/orvex-studio-ui` CI. Not wired as a CI secret — scope-insufficient, wiring it would silently gate-pass a deploy lane that can't actually push a Worker. Added to the end-of-run human ledger, `humanItems=1`.

**Discipline.** linearis CLI only (no MCP) for the Linear write; comment body via temp-file `"$(cat …)"`. OpenBao/kubectl used read-only (`bao kv list`, `kubectl get secret` metadata-only, never `-o yaml`/describe with data) — no secret value read or printed anywhere in this pass. No `kubectl apply` / `argocd sync`. Trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the Linear comment. Appended under `flock` on `po-decisions/2026-07-11.md.lock`.

Evidence: ENG-1950 comment `1b883fa6-34a0-4b3e-9dc4-175cc7e1364a`; `my-idp/scripts/bootstrap/setup-platform-credentials.sh:845-857`; `my-idp/platform/apps/cert-manager/resources/letsencrypt-clusterissuer.yaml`; `my-idp/platform/apps/external-dns/values.yaml`; `orvex-studio-ui/README.md` §Cloudflare Worker Static-Assets serving path; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass18/1950-cf.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1405 dependency wiring — orchestrator judgment under PO standing authority (2026-07-11)

- Verified ENG-1442 live-Done; confirmed its scope is mint/scope/verify only (AC1-AC8), NOT the token.revoked outbox producer. The actual gating ticket for that surface is ENG-1498 ("Token revocation + enforce-SSO surface"), currently Todo.
- Wired blockedBy: ENG-1498 blocks ENG-1405 (relation 0ccbe0c7-d3f8-470a-bf16-7d021e8762ed). Left the pre-existing ENG-1442->ENG-1405 blockedBy in place (satisfied, but not the live gate).
- Reviewed orvexai/orvex-studio-mcp PR #18 (da9931d three-repo-e2e cluster-CI job) on merits: all required checks green, three-repo-e2e correctly dispatch-only/excluded from ci-success, test scaffold honest (it.todo/it.skipIf, no false green). Merged as 217ce4011493ac0f25a7307c03a4a1997cd82618, branch eng-1405-work deleted.
- Flagged (not fixed) a silent-rot risk in the merged job: it currently fetches a STATIC pre-provisioned OpenBao secret (apps/orvex-studio-mcp-dev/e2e/identity-token) as the test bearer. Documented the honest recipe on ENG-1405: mint a Clerk test-tenant session via M11_CLERK_* secrets, then POST /v1/exchange against identity to obtain a real minted bearer -- never a static stored token. Left as a named follow-up, not executed in this pass (out of scope for dependency-wiring pass; ENG-1944 confirms /v1/exchange is live and AC2-complete).
- ENG-1405 status held at In Progress, 0/9 DoD -- no ticks; comment posted on ENG-1405 with full rationale.


## ENG-1559 ownership triage — engine-side ACL/export/resolve/ai-search escalation filed as ENG-1957 (2026-07-11)

_Orchestrator judgment under PO standing authority (2026-07-11)._

**Escalation at pickup.** A prior build attempt on ENG-1559 (M5 Wave A gate, `TestM5KnowledgeE2E`) found the engine-side internal API `orvex-studio-knowledge`'s `internal/clients.Engine` needs (`POST /internal/acl/filter`, `GET /internal/pages/{id}/export`, `GET /internal/pages/{id}/resolve`, `GET /internal/settings/ai-search`) absent from `orvex-wiki`/`orvex-wiki-api` at HEAD — live-verified (`kubectl port-forward svc/orvex-wiki -n orvex-wiki-dev` → `POST /internal/acl/filter` returns `404 Cannot POST /internal/acl/filter`; `/internal/pages/{id}/export` serves the SPA `index.html` catch-all, not a real route). ENG-1478/1451/1437 were flagged as needing re-verification against landed reality.

**Ownership search (before filing anything new).** Checked `.cache/linear/` for any OPEN ticket already owning this build:
- ENG-1478 ("Retrieval scope-intersection egress") — Done, but is the knowledge-side *client* of this seam (`internal/clients.Engine`), not the engine-side implementation.
- wiki/wiki-api open-set per the escalation: ENG-1368 (Done), ENG-1374 (Done), ENG-1468 (Done), ENG-1481 (Todo — but scoped to MCP `/mcp` transport decommission, unrelated to ACL/export/resolve/ai-search).
- Re-checked ENG-1451 ("Keep /search/suggest + delete tsvector") and ENG-1437 ("Tika admin config + remove tsvector write") bodies in full — both Done, neither ever targeted this internal API surface (1451 = search/suggest ILIKE + FTS-column decommission; 1437 = Tika/attachment-indexing pipeline decommission). Their Done status genuinely does not overlap this seam — confirmed, not just asserted.
- Broadened the search: grepped every issue referencing `clients.Engine`/`internal/clients` (33 files) for open, non-Done tickets — found ENG-1467 and ENG-1547 also touch `internal/clients.Engine`, but both are *consumers* of the engine's HTTP API (bulk page ops facade, console admin fan-out), not builders of this specific internal surface.
- Exact-path grep across the FULL `.cache/linear/issues/` corpus for the four literal route strings (`internal/acl/filter`, `internal/settings/ai-search`, `internal/pages/{id}/export`, `internal/pages/{id}/resolve`): zero hits anywhere, open or closed.

**Conclusion: nobody owns it.** Filed **ENG-1957** ("Engine-side internal API for knowledge's ACL/export/resolve/ai-search seam"), project "Orvex Wiki" (`0d2ce4ec-2e8a-4d98-ae95-343e1cf501f0` — the engine repo per the task's framing), team ENG, status Todo — all set on create and verified by re-read. Authored with the concrete 4-endpoint list, 7 ACs, dev-context pointing at the existing `page-permission.controller.ts` seam to compose over (not reimplement), and the full live-404 evidence trail. Wired `--blocks ENG-1559` on create; verified live via `linearis issues read ENG-1957` → `relations.nodes[0] = {type: blocks, relatedIssue: ENG-1559}`.

**ENG-1559 reconciled honestly.** Cache showed `status: In Progress` (from the prior build-attempt claim). No build work is active or can honestly proceed until ENG-1957 lands (and until a real Clerk/identity dev credential exists for AC2/AC3 — the other still-open half of the original escalation, not re-solved this pass). Posted a follow-up comment on ENG-1559 naming ENG-1957 + the full ownership-search trail, then moved status **In Progress → Todo** (verified by re-read: `state.name: Todo`) — leaving it "In Progress" with no live work and a hard external blocker would be a dishonest status. Cache refreshed for both tickets post-write.

**Discipline.** linearis CLI only (no MCP) for every write; description/comment bodies via temp-file `"$(cat …)"`. New ticket (ENG-1957): project + status set on create, both verified by re-read; `blocks` edge verified by re-read, not assumed from the create response. Refresh-on-write after every Linear write that touched cache-relevant state (`_bmad/lnr/tools/linear-sync.sh issue ENG-1957` and `... issue ENG-1559`, both confirmed). No `kubectl apply` / `argocd sync` (only a pre-existing live-probe result was read from the prior build log, nothing re-run this pass). No secret value read or printed. Trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the Linear comment. Appended under `flock` on `po-decisions/2026-07-11.md.lock`.

Evidence: ENG-1957 (identifier ENG-1957, Todo, project Orvex Wiki, blocks ENG-1559); ENG-1559 comment `fcfdd19c-f4da-4997-a7e0-1ee21f3d3561`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/act3/ENG-1559-build.md`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/act3/ENG-1559-comment.md`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass18/new-ticket-body.md`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass18/1559-triage.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>


## ENG-1575 credential wiring — Clerk + LiteLLM both real and now wired, no true-external human item (2026-07-11)

_Orchestrator judgment under PO standing authority (2026-07-11)._

**Task.** ENG-1575's `TestGateM8E2E` was escalated credential-blocked (comment `40ac3740`): (a) LITELLM_URL/LITELLM_API_KEY test creds or a committed recorded-tape corpus (AC4, must be REAL); (b) CLERK_JWKS_URL + a Clerk poc dev test-tenant credential (every route RS256/JWKS-gated). Journal-grounded via `wf_8197e8bd-43a/journal.jsonl` (repo `orvex-studio-ai`, test `TestGateM8E2E`, escalation already posted, no code authored).

**Clerk — real, already provisioned platform-wide, just needed piping.** `apps/orvex-studio/clerk` in OpenBao carries `jwks-url`/`secret-key`/`publishable-key` — the same poc dev instance (`tough-hornet-12.clerk.accounts.dev`) ENG-1944's identity surface and the M11 lane (`orvex-studio-ui` `M11_CLERK_*` secrets) already use. Wired `CLERK_JWKS_URL` + `CLERK_SECRET_KEY` as CI secrets on `orvexai/orvex-studio-ai`, piped directly from `bao kv get` via stdin (never in argv, never printed). Live-validated the secret-key against `api.clerk.com/v1/users` (HTTP 200) — not just copied blind. Reused the `ensureTestUser` pattern verbatim from `orvex-studio-ui` PR #16 (idempotent lookup-then-create) to provision a fixed M8-gate test-tenant user `eng1575-m8-gate@orvex.dev`: did not exist, created it (HTTP 200); wired `CLERK_TEST_USER_EMAIL`/`CLERK_TEST_USER_PASSWORD` as CI secrets. Noted for the future e2e-lane author: `cmd/server`'s actual runtime var is `ORVEX_AUTH_JWKS_URL` (`internal/config/config.go`), not `CLERK_JWKS_URL` — the secret is named to match `.env.example`/the ticket ask; needs a rename-map in the job's `env:` block.

**LiteLLM — real proxy confirmed deployed on the dev cell, not a true-external gap.** ArgoCD app `litellm` (`argocd` ns, source `my-idp` `platform/apps/litellm`) is `Synced`, running `berriai/litellm:v1.86.0` + a model-sync sidecar — a genuine live gateway, not a placeholder. `apps/shared/litellm/bearer-token` in OpenBao (managed-by external-secrets) carries `base-url=https://litellm.eu-central-1.myidp.cloud` + a client-facing `bearer-token` (deliberately not `master-key`/`viewer-key`, which are admin/observability-scoped). Wired `LITELLM_URL`/`LITELLM_API_KEY` as CI secrets on `orvexai/orvex-studio-ai` from these values. Per `.env.example`'s own contract, AC4 wants a **committed recorded-tape corpus**, not live dialing at test time — these creds are exactly what the recording harness needs per its own recipe to produce that tape once against the real proxy; recording it is a T4 test-authoring task, not part of this credential-wiring pass, so not fabricated here.

**Kafka (AC2 `ai.usage.recorded`)** — flagged but not resolved: the `integration` CI lane provisions Postgres only, no Kafka service block. This is CI-lane-wiring work for whoever authors the `e2e` job (§4a of the ticket), not a missing credential, so it is NOT added to the human ledger.

**No true-external human-only item resulted from this escalation.** Both named blockers turned out to be real, already-live platform infrastructure that only needed discovery + piping — `humanItems=0` for this pass.

**Enacted.** Comment posted to ENG-1575 (discussion `270f3810-7671-4074-b808-27ee6c6a879f`) naming exactly what's wired, the config-var rename note, and the Kafka gap. Cache refreshed (`_bmad/lnr/tools/linear-sync.sh issue ENG-1575` → confirmed `status: In Progress`, left as-is — no code/tests landed, so no status change is honest here).

**Discipline.** linearis CLI only (no MCP) for the Linear write; comment body via temp-file `"$(cat …)"`. All 6 CI secrets (`CLERK_JWKS_URL`, `CLERK_SECRET_KEY`, `CLERK_TEST_USER_EMAIL`, `CLERK_TEST_USER_PASSWORD`, `LITELLM_URL`, `LITELLM_API_KEY`) piped via stdin into `gh secret set`, confirmed present via `gh secret list --repo orvexai/orvex-studio-ai`, no value ever echoed/printed/left on disk (temp probe files removed after use). OpenBao/ArgoCD used read-only throughout; no `kubectl apply` / `argocd sync`. The one live-mutating call was the Clerk `POST /v1/users` create — a deliberate, idempotent, in-scope test-fixture provisioning step matching the explicit `ensureTestUser` instruction, not an out-of-scope infra mutation. Trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the Linear comment. Appended under `flock` on `po-decisions/2026-07-11.md.lock`.

Evidence: ENG-1575 comment `270f3810-7671-4074-b808-27ee6c6a879f`; `gh secret list --repo orvexai/orvex-studio-ai` (6 secrets, timestamped 2026-07-10T23:55); ArgoCD app `litellm` (Synced, `berriai/litellm:v1.86.0`); `orvex-studio-ai/.env.example`; `orvex-studio-ai/internal/config/config.go`; `orvex-studio-ui` PR #16 (`e2e/support/clerkTestUser.ts`, `e2e/support/m11Env.ts`); `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass18/1575-creds.md`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass18/1575-comment-body.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

## ENG-1954 runtime verification — root cause fixed + PR merged; ArgoCD stale self-heal retry loop blocks convergence, boxes left unticked (2026-07-11)

_Orchestrator judgment under PO standing authority (2026-07-11)._

**Task premise contradicted by evidence.** Handed "the infra port merged"; live check (`gh pr view 21`) showed PR #21 (`eng-1954-work` → `dev`) was still OPEN (`mergedAt: null`). Cluster read confirmed pre-fix state: ExternalSecret `orvex-studio-api-secrets` Ready=False (SecretSyncedError, old key `apps/orvex-studio-api-dev/api`), ArgoCD app OutOfSync/Degraded, only the Knative Broker present in `orvex-studio-api-dev` — no BFF workloads.

**Enacted.** PR #21 was already build-validated + gate-green from the prior adversarial review (kustomize/kubeconform/tsc/vitest/lint/build all clean, root-caused OpenBao double-prefix path bug verified against live `openbao-apps` ClusterSecretStore, no defects). Mechanical + green → updated branch (`gh api PUT pulls/21/update-branch`), waited for CI green on rebased head, squash-merged: `c7a217963566447d0d83468ac58b625c34544ad1`.

**Post-merge convergence not yet observed — genuinely stuck, not a manifest defect this time.** ArgoCD Application `orvex-studio-api-dev` had a pre-existing in-flight self-heal operation retrying the OLD revision (`468edcd8`) with exponential backoff (cap 3min, limit 30 retries) that started before this pass began. `comparedTo.source.revision` updated to `c7a217` immediately (ArgoCD sees the fix) but the operation-lock means it won't start a fresh sync until the stale retry loop terminates. Polled read-only across 3 bounded rounds (~38 min total, retryCount 8→17/30): no forward progress, none of the new wave 1-3 resources (Password generators, PushSecrets, Postgres/RedisInstanceClaim) created yet, ExternalSecret still Ready=False on the old key. Est. ~40 more minutes for the stale operation's retry budget to exhaust. Forcing it (terminate-op / argocd sync) is out of scope under never-kubectl-apply/never-argocd-sync doctrine.

**All 3 ACs left unticked** (OpenBao real values / ExternalSecret Ready=True / App Synced+Healthy+workloads) — none honestly provable yet. Status left `In Progress` (unchanged, already In Progress; no dishonest advance to Done). ENG-1535 remains blocked — no action taken there since ENG-1954 did not reach green. Recommend a follow-up read-only re-verify in ~45-60 min; the fix itself is confirmed correct so convergence is expected to be automatic once ArgoCD's stale operation clears.

**Discipline.** linearis CLI only for the Linear write (comment `48789224-7dbb-4dd9-add4-272e5f52c774`); body via temp-file. All cluster reads via kubectl get/describe + argocd-mcp get_application (read-only) — zero `kubectl apply` / `argocd sync`. Secret values never printed (only condition/reason/status fields read). `gh pr merge`/`update-branch` on PR #21 is a GitOps repo-level merge action, not a cluster mutation — matches "fix via GitOps PR (build-validated, merge if mechanical+green)" instruction. Cache refreshed post-comment (`linear-sync.sh issue ENG-1954` → confirmed `status: In Progress`). Trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on the Linear comment. Appended under `flock` on `po-decisions/2026-07-11.md.lock`.

Evidence: PR https://github.com/orvexai/orvex-studio-api/pull/21 (merged `c7a217963566447d0d83468ac58b625c34544ad1`); ENG-1954 comment `48789224-7dbb-4dd9-add4-272e5f52c774`; ArgoCD app `orvex-studio-api-dev` (operationState.retryCount 17/30 on stale revision `468edcd8` at time of writing); `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass18/1954-verify.md`; `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/77ba52f2-3d57-4198-8c37-ac219579b139/scratchpad/pass18/1954-comment-body.md`.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
