// synthetic fixture upstream file — NOT real source (fr30 fixture discipline)
// fixture ifVersion CAS guard — the tagged upstream anchor this hardening
// item was recorded against; the edit below stays WITHIN this anchor.
export const upstreamHardened = 'fork-hardening-edit';
