// synthetic fixture upstream file — NOT real source (fr30 fixture discipline)
// the recorded anchor marker was refactored away — the hardening item's
// anchor no longer resolves, so the gate must fail LOUDLY naming it.
export const upstreamHardened = 'fork-edit-outside-any-recorded-anchor';
