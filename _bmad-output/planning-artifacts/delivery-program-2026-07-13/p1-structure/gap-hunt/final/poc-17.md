## 🎯 Story

As a **signed-in user**, I want **a sectioned, deep-linkable Account IA reached from the sidebar footer menu — Connections (GitHub claim identity + claimed-skills + Your-Wiki card), Your AIs (cross-AI export preferences), Notifications (per-event × per-channel matrix), Appearance (theme), and a Danger zone (sign-out + account deletion)** so that **I can manage my identity, delivery preferences, notifications, theme, and account lifecycle in one honest, autosaving settings surface** (secondary-screens-and-shell-spec.md §3; account.jsx panels).

**Definition of Done:** ONE named test `TestAccountSettingsPanelsAutosaveAndDangerGates` — an integration test asserting: (a) each net-new panel (Connections / Your-AIs / Notifications / Appearance / Danger) deep-links via its own /account/{section} route inside the shared settings shell, (b) every non-destructive control autosaves per-control with an inline aria-live 'Saved' pill (no global Save button), (c) the Notifications matrix renders each event×channel cell as a LIVE toggle, an honest 'coming' pill, or a structural '—' — never a fake working toggle, (d) the Your-AIs panel governs the Five-AI export step (only enabled providers show; disabling the default reassigns it; ChatGPT param toggles inert when ChatGPT is off; model-pref honestly non-enforcing), and (e) account delete requires a re-auth gate plus typed-confirmation (email-or-DELETE) and renders the ratified unclaim-not-delete consequence copy. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1 (deep-linkable shell)** — Given the Account IA, When a user deep-links to /account/{connections,your-ais,notifications,appearance,danger}, Then each renders its own section content within the shared 2-column settings shell (rail + content pane on desktop, full-screen sub-view on mobile), reachable via the sidebar footer menu (not a 7th sidebar destination), with every non-destructive control autosaving per-control (inline aria-live 'Saved', no global Save button). *Assertion: each net-new route renders distinct content; per-control autosave, no Save button.* [Source: POC secondary-screens-and-shell-spec.md §3.1; POC account.jsx L9-11, L642]
- [ ] **AC2 (Connections)** — Given the Connections panel, When rendered, Then it shows the GitHub connection status as one of Connected/Needs-reconnect/Not-connected (text+icon), a claimed-skills list with real 'Used by N' or 'New' (never 'Used by 0'), the reputation chip on claimed rows, and a compact Your-Wiki deep-link card — with 3 distinct OAuth-failure inline messages (cancelled/unreachable/failed), never a half-connected state. *Assertion: connection status + claimed list + 3 distinct OAuth-failure messages render.* [Source: POC secondary-screens-and-shell-spec.md §3.3]
- [ ] **AC3 (Your AIs — provider set + default)** — Given the Your-AIs panel, When a provider's 'Show in Open it in your AI' toggle is switched, Then that provider appears/disappears from the Five-AI export step (E5-S3) on next render; When a default is set, only enabled providers are selectable, and disabling the current default falls back to the next enabled provider automatically (never a dead default); if no provider is enabled, the export step renders an honest 'no AI selected' state, never a broken/empty button row. *Assertion: export list matches toggle state; default reassigns on disable.* [Source: POC account.jsx L302-485, L309-326, L367-395; POC screens/useit.jsx L224-231]
- [ ] **AC4 (Your AIs — ChatGPT params + honest model pref)** — Given ChatGPT is enabled, When 'Temporary chat' or 'Start with web search on' is toggled, Then the export deep-link's query params change (`&temporary-chat=true`, `&hints=search`); when ChatGPT is OFF both toggles render dimmed and non-interactive. Given a preferred-model picker, When a non-'auto' model is chosen, Then the preference persists and the UI states plainly the deep-link cannot enforce it — no false claim of enforcement. *Assertion: link param presence matches toggle state; toggles inert when off; model-pref disclosure renders.* [Source: POC account.jsx L328-361, L420-449]
- [ ] **AC5 (Notifications matrix)** — Given the Notifications panel, When rendered, Then events are grouped (e.g. 'Feedback on your skills' / 'Your skills' health' / 'Digest') from a canon-derived list (no invented events); each row×channel cell renders a LIVE toggle (persists on change, aria-live Saved) when its `wired` flag is true, an honest 'coming' pill when false (never a switch bound to nothing), or a '—' when the channel is structurally N/A (e.g. no in-app for a digest-only row). *Assertion: LIVE/COMING/'—' correctly distinguished; autosave per-toggle; wired flag BFF-declared.* [Source: POC secondary-screens-and-shell-spec.md §3.5; POC account.jsx L65-89, L609-677]
- [ ] **AC6 (Danger zone — sign-out + re-auth + typed-confirm)** — Given the Danger zone, When Sign out is pressed, Then the session ends immediately with no confirmation gate and the user lands on the public marketing route; When Delete account is initiated, Then a re-authentication stage renders first (no faster route to the destructive path), then typed-confirmation requires the account's own email OR the literal 'DELETE' before the destructive button enables. *Assertion: sign-out one-click; delete visits reauth before confirm; button enabled iff exact match.* [Source: POC secondary-screens-and-shell-spec.md §3.7, SC-2/SC-2b; POC account.jsx L737-818]
- [ ] **AC7 (Danger zone — unclaim-not-delete copy + Memory deep-link)** — Given confirmed deletion, When the consequence copy renders, Then it states plainly that Library/drafts/Memory are permanently deleted but published-and-used skills are unclaimed (kept in the catalogue, attributed to source) — never silently deleted out from under other users; and 'Delete just your Memory' deep-links to the existing Memory-scoped hard-delete flow (E3-S4), never a second implementation. *Assertion: consequence copy names both outcomes correctly; Memory link reuses E3-S4.* [Source: POC secondary-screens-and-shell-spec.md §3.7; POC account.jsx L757-768]
- [ ] **AC8 (Appearance)** — Given the Appearance panel, When rendered, Then it offers a Light/Dark/System radiogroup, each option applying `<html data-theme>` instantly (persisted to localStorage, set before first paint, no flash), matching orvex-ds.css exactly in both themes. *Assertion: theme selection persists + applies without flash.* [Source: POC secondary-screens-and-shell-spec.md §3.6]

## 🔨 Tasks

- [ ] RED: `TestAccountSettingsPanelsAutosaveAndDangerGates` (AC1/AC5/AC6).
- [ ] GREEN: sectioned settings shell + deep-linkable panels + per-control autosave (AC1); Connections GitHub status + claimed list + reputation chip + Wiki card + 3 OAuth-failure messages (AC2); Your-AIs provider toggles + default fallback (AC3) + ChatGPT param toggles + honest model pref (AC4); Notifications event×channel matrix with LIVE/COMING/'—' (AC5); Danger-zone sign-out + re-auth-gated typed-confirm delete (AC6) + unclaim-not-delete copy + Memory hard-delete deep-link (AC7); Appearance theme radiogroup (AC8).

## 🧠 Context

React-front settings surface (CS §6). E2-S1 (Signed-in shell) already builds the account-FOOTER MENU (the entry point); this story builds the settings PAGES it links to. E3-S4 already covers the Privacy panel's memory-specific sensitive-field behavior + the Memory hard-delete this Danger zone deep-links to — this story's Connections/Your-AIs/Notifications/Appearance/Danger panels are the remaining net-new IA and must reuse (not re-spec) E3-S4's Privacy panel and Clerk's Profile panel. The Notifications panel is the UI consumer of the notification-preferences BACKEND (separate api ticket); Your-AIs is consumed by E5-S3 Five-AI export. Confirmed distinct from extension E2-S3 (browser-extension MCP-agent connection status) and ui E7-S2 (MCP/agent token issuance, FR-UI25). Seam: BFF account/notification-prefs/GitHub-status/prefs endpoints + Clerk delete-account + re-auth. Sibling dependency: E2-S1 (entry menu), E3-S4 (Privacy panel + Memory hard-delete reuse), E5-S3 (consumes Your-AIs prefs), E1-S3 (theme tokens), E1-S5 (honest-states primitives for Coming/'—').

**🧾 Gap provenance (2026-07-15):** POC completeness sweep — the UI corpus was authored from the service PRD, not the POC design source. Consolidates four independent finds of the same account-settings surface: secondary-screens-and-shell-spec.md §3 (6-panel /account IA), account.jsx YourAisPanel (L302-485), account.jsx NotificationsPanel (L65-89, L609-677), and account.jsx DangerPanel (L737-818). The only prior trace is E2-S1's one-line account-FOOTER MENU summary; no story builds the settings pages. (The notification-preferences BACKEND is a separate api ticket.)

## 🧪 Testing

`TestAccountSettingsPanelsAutosaveAndDangerGates` (integration) + unit tests on the LIVE/COMING/'—' matrix resolution, the default-AI fallback logic, the typed-confirm validator, and the re-auth gate. CS §5 mocking: BFF account/notification-prefs/prefs fixtures + Clerk re-auth (true-external mock); never mock own panel/matrix/stage-gate rendering.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honesty (no fake switch — the canonical POC example; no dead default; no false model-enforcement claim; accurate unclaimed-vs-deleted copy) · §3 naming.
- SE-Arch `8sYi523i4t`: honesty lens (dead-control anti-pattern is what the Notifications matrix exists to prevent); irreversible-action lens (re-auth + typed-confirm for delete); no-collateral-damage lens (other users' skill usage must not break).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (Account/settings family, incl. FR-UI8 extended to whole-account scope + FR-UI16 provider set) · POC `secondary-screens-and-shell-spec.md` §3 (PO ratifications 2026-06-17 incl. S2 reputation-chip-on-Connections + the SC-2b delete-copy ruling) · POC `web/src/prototype/screens/account.jsx` (ground truth, YourAis/Notifications/Danger panels).

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Related:** E2-S1 (account footer menu, entry point), E3-S4 (Privacy panel + Memory hard-delete, reused not re-built), E5-S3 (Five-AI export consumes Your-AIs prefs), the api notification-preferences backend ticket (Notifications panel's data source).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
