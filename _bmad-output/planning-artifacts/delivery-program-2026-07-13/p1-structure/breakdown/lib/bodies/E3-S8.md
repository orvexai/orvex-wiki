## 🎯 Story

**As** workflows activities and any satellite touching identity lifecycle, **I want** `pkg/identityclient` — a typed client for identity's non-verifier surface (`POST /v1/introspect`, token mint, and the studio-lifecycle mirror provision/deprovision, mode `disable|delete`, body `{scope,id}`) — **so that** the introspect surface is a shared typed client (FR-L2 dependency factored here), workflows drops its hand-rolled `studio_activities.go` studio client, and the provision/deprovision two-unrelated-types guarantee is realized inside lib (OQ-L5 closed) (FR-L30; Arch A-CLIENTS).

**Definition of Done:** ONE named test `TestIdentityClientTwoUnrelatedTypes` (unit, in `pkg/identityclient`, driving introspect/mint/lifecycle against a fake identity server) asserting the identity mirror type `{scope,id}`/`disable|delete` is a **distinct Go type with no conversion** to the engine controller type `{clerkOrgId,…}`/`deactivate|delete`, and that high-privilege lifecycle calls route through the FR-L5 `RequireFresh` path. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `POST /v1/introspect`, token mint, and the studio-lifecycle mirror, When called, Then each maps to the contracts identity shape. *assert typed round-trip per method* [Source: FR-L30]
- [ ] **AC2** (vocabulary trap) — Given the identity mirror type `{scope,id}`/`disable|delete`, When placed beside the engine controller type `{clerkOrgId,…}`/`deactivate|delete`, Then no conversion between them compiles. *assert two unrelated Go types, no cross-wiring* [Source: FR-L30 / A-CLIENTS]
- [ ] **AC3** — Given a high-privilege lifecycle call (provision/deprovision, mint), When executed, Then it routes through the FR-L5 `RequireFresh` path. *assert fresh, no-grace introspection on these paths* [Source: FR-L30 / FR-L5]
- [ ] **AC4** (discipline) — Given any call, When executed, Then E3-S3 client discipline applies (fail-closed, deadline, obs). *assert deadline + span; failure ⇒ deny* [Source: FR-L30 / FR-L14]

## 🔨 Tasks

- [ ] RED: `TestIdentityClientTwoUnrelatedTypes` + `TestLifecycleRequiresFresh` (AC1–AC3).
- [ ] GREEN: introspect/mint/lifecycle-mirror methods over the discipline harness; distinct mirror type; RequireFresh routing (AC1–AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** partial — Introspect + WhoAmI + ResolveCell are implemented and tested; but the mint and lifecycle-mirror surfaces of FR-L30 are absent, so the introspect/mint/lifecycle-mirror triad is only partially built.

`pkg/identityclient` (non-service; no six-tier shape, CS §6). Closes OQ-L5 (2026-07-05) — the identity-side provision/deprovision type now has a lib home. The introspect method is the shared client E1-S2 depends on. Seam crossed: identity non-verifier OpenAPI (contracts). Sibling deps: E1-S2 (introspection), E1-S4 (RequireFresh), E4-S1 (workflows activities import these).

## 🧪 Testing

`TestIdentityClientTwoUnrelatedTypes` (unit, offline). CS §5 mocking: fake identity server (foreign — allowed, NFR-L5); **never mock `pkg/identityclient`/`pkg/auth`** (own packages).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§5 fail-closed; §9 no-cross-wiring types). SE-Arch `[[8sYi523i4t]]`: vocabulary-trap lens, high-privilege-freshness lens. cell-lint `[[JGAUQRsw2g]]` (injected BaseURL).

## 🔗 References

PRD `fG2jKeRGiu` FR-L30, FR-L2, FR-L5; Arch `0NPvDR3C0m` A-CLIENTS (OQ-L5 closed), §1 topology; identity FR-I14 (constant-200 mirror); contracts identity OpenAPI.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — identity non-verifier OpenAPI + lifecycle mirror shape; the TAG is the dispatch gate).
- [ ] Parent epic: **E3**. Ships alongside E1-S1 (shares introspect surface); depends on E3-S3, E1-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
