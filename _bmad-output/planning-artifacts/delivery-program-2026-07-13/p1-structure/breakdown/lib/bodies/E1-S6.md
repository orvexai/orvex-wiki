## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** the family, **I want** `pkg/auth/authtest` — an importable conformance suite encoding the full verifier contract (V1–V12) against golden token fixtures — that runs as a release gate in lib CI, in identity's CI against the authority, and in every satellite's CI against its wired middleware, **so that** the verifier's security ceiling is proved identical everywhere and a lib release that fails conformance does not tag (FR-L7, NFR-L2; Arch A-CEILING).

**Definition of Done:** ONE named test `TestAuthConformanceSuite` (the `authtest` runner, executed in lib CI as a release gate, driving the wired verifier+router through every V1–V12 case) asserting all fail-closed guarantees hold, including the reduced-scope-with-warm-cache case (V6), frozen-claims org-A-vs-org-B (V8), the route-class cross cases (V11), and the cell-guard 421 (V12). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the `authtest` package, When imported by a consumer, Then it exposes a runner that takes the consumer's wired middleware and executes V1–V12 against contracts golden fixtures. *assert a satellite can wire it in tens of lines* [Source: FR-L7 / G4]
- [ ] **AC2** — Given lib CI, When `authtest` fails any case, Then the release does not tag. *assert CI release gate blocks on any conformance failure* [Source: FR-L7 / NFR-L2 / Arch A-CEILING]
- [ ] **AC3** — Given the V6 case, When a reduced-scope token's introspection cache is still warm from before the reduction, Then its elevated request is rejected no later than TTL expiry. *assert reject within TTL* [Source: FR-L7 / Arch V6]
- [ ] **AC4** — Given V8 frozen claims, When an org-A-minted token is presented after an org switch, Then it never verifies as org-B. *assert reject (identity A-TOKEN CI case, run here too)* [Source: FR-L7 / Arch V8]
- [ ] **AC5** — Given the route-class + cell cross cases (V11/V12), When run, Then `WorkloadIdentityOnly` rejects bearer, scoped rejects workload-only, and wrong-cell ⇒ 421. *assert all three* [Source: FR-L7 / Arch V11/V12]
- [ ] **AC6** — Given the wrong-audience fixture, When run, Then it follows the audience model contracts FR-C13 pins (OQ-L7). *assert case reads the pinned audience model, not an arbitrary value* [Source: FR-L7 / OQ-L7]

## 🔨 Tasks

- [ ] RED: encode V1–V12 cases in `authtest` against golden fixtures (AC1, AC3–AC6).
- [ ] GREEN: importable runner taking a consumer's middleware; static-JWKS signer + fake introspect/registry doubles (AC1).
- [ ] GREEN: wire `authtest` into lib CI as a release gate (AC2).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — Shipped conformance suite (authtest.go 22KB + fixtures.go) with TestAuthConformanceV1ToV12 wired as a CI release gate plus a no-skip gate (ci.yml lines 94-97); authtest_gate_test.go.

`pkg/auth/authtest` (non-service; no six-tier shape, CS §6). This is the shipped artifact that makes the ceiling real (G2, NFR-L2). Seam crossed: contracts golden token fixtures + audience model (OQ-L7). Sibling deps: exercises E1-S1..S5 end-to-end; consumed by identity CI and every satellite CI.

## 🧪 Testing

`TestAuthConformanceSuite` (the runner itself, run in CI). Tiers: unit/conformance (offline). CS §5 mocking: shipped static-JWKS signer + fake introspect/registry (NFR-L5); **never mock `pkg/auth`** — the whole point is running the real verifier.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§5 fail-closed; §3 release-gate discipline). SE-Arch `[[8sYi523i4t]]`: acceptance-auditor lens (does the suite actually prove each guarantee?), blind-hunter on gaps between V-cases. cell-lint `[[JGAUQRsw2g]]` (V12 cell-guard case).

## 🔗 References

PRD `fG2jKeRGiu` FR-L7, NFR-L2; Arch `0NPvDR3C0m` A-CEILING, §3 V1–V12; identity A-TOKEN; contracts FR-C13 (audience), golden token fixtures.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — golden token fixtures + audience model OQ-L7; the TAG is the dispatch gate; the V1 wrong-audience fixture is not frozen until then).
- [ ] Parent epic: **E1**. Depends on E1-S1..S5; wires into E7-S3 (aggregate CI).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
