## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** the family, **I want** a CI parity gate that round-trips the contracts golden DfM/ProseMirror fixtures + the pinned engine block-schema catalog through the Go serializer on every lib CI run (and on every catalog/fixture pin bump), where the PM→DfM oracle is the engine's shipped DfM read-path renderer, **so that** the two independent serializers (Go here, AGPL TS in the engine) cannot silently drift (FR-L28; Arch A-DFM).

**Definition of Done:** ONE named test `TestDFMParityAgainstEngineCatalog` (CI parity gate, in `pkg/dfm`, running the Go serializer against the pinned catalog + golden fixtures) asserting divergence between the Go serialization and the fixture-pinned engine behavior fails the build, that the oracle is the engine's DfM read-path renderer (not legacy `jsonToHtml→turndown`), and that the corpus covers the required cases. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the golden fixtures + pinned catalog, When the parity gate runs, Then Go-serialization divergence from the fixture-pinned engine behavior fails the build. *assert divergence ⇒ CI red* [Source: FR-L28]
- [ ] **AC2** — Given the PM→DfM direction, When parity is checked, Then the oracle is the engine's shipped DfM read-path renderer, not the legacy `jsonToHtml→turndown` export path. *assert oracle source* [Source: FR-L28]
- [ ] **AC3** — Given the corpus, When the gate runs, Then it covers all 21 engine embeds, opaque round-trips (excalidraw/drawio baked+inline, `pendingMermaid`), table-cell marks, and mention `[[slugId]]`/`@label`. *assert corpus coverage; a block without a catalog entry + golden fixture is flagged as invisible* [Source: FR-L28]
- [ ] **AC4** — Given a catalog/fixture pin bump, When the pin changes, Then the parity gate re-runs (per FR-L24). *assert re-run on pin bump* [Source: FR-L28 / FR-L24]
- [ ] **AC5** (negative) — Given a residual `linear*` block, When parity runs, Then no fixture depends on a Linear renderer (opaque round-trip only). *assert no Linear-renderer dependency* [Source: FR-L28 / D-S11]

## 🔨 Tasks

- [ ] RED: `TestDFMParityAgainstEngineCatalog` over the expanded golden corpus (AC1–AC3, AC5).
- [ ] GREEN: parity harness with the engine DfM read-path renderer as oracle; wire into lib CI + pin-bump re-run (AC2, AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — Golden-corpus parity harness: corpus/ manifest + node/mark/mention/opaque fixtures loaded by corpus_loader.go, with parity_test.go asserting mutation-detects-drift and the D-S11 linear-free cleanroom invariant (runs under go test ./...).

`pkg/dfm` (non-service; no six-tier shape, CS §6). The gate is only as strong as its corpus — today's 6 core-GFM pairs are inadequate; corpus expansion is a **contracts prerequisite** (CLI gaps G9/G10) pinned by ENG-2092. Seam crossed: contracts golden fixtures + engine catalog pin. Sibling deps: E6-S2 (the serializer under test), E7-S1 (catalog pin), E7-S4 (AGPL guard runs alongside).

## 🧪 Testing

`TestDFMParityAgainstEngineCatalog` (CI parity gate, offline against pinned artifacts). CS §5 mocking: none — real serializer vs pinned fixtures/catalog; **never mock `pkg/dfm`** (own package).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§9 drift-gate; §0 licence-by-contract-not-code). SE-Arch `[[8sYi523i4t]]`: fixture-corpus-adequacy lens, oracle-correctness lens, invisible-block lens. cell-lint `[[JGAUQRsw2g]]` (n/a).

## 🔗 References

PRD `fG2jKeRGiu` FR-L28, FR-L24; Arch `0NPvDR3C0m` A-DFM, §6; CLI gaps G9/G10; D-S11.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — expanded golden corpus to all 21 embeds + catalog pin; the TAG is the dispatch gate; corpus expansion is a contracts prerequisite).
- [ ] Parent epic: **E6**. Depends on E6-S2 (serializer), E7-S1 (catalog pin). **Must be green before the Orvex CLI swaps its serializer.**

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
