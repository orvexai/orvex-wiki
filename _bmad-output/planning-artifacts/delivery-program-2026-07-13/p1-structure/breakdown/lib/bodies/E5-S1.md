## 🎯 Story

**As** every satellite, **I want** one `pkg/obs.Setup(ctx, serviceName)` that wires the full house observability (OTLP traces + metrics + logs to the cluster otel-collector, resource attrs from env, W3C propagation, runtime metrics, graceful flush) plus a `/healthz`+`/livez` helper that echoes `CELL_ID` + `CLUSTER_NAME`, **so that** no satellite configures otel by hand, telemetry lands in the Loki/Mimir/Tempo stack console fronts, and the cell contract Rule 4 liveness echo is uniform (FR-L20; Arch A-OBS, cell Rule 4).

**Definition of Done:** ONE named test `TestObsSetupAndHealthzCellEcho` (unit, in `pkg/obs`, driving `Setup` + the `/healthz` handler against a fake OTLP collector) asserting one call wires traces+metrics with env-derived resource attrs and W3C propagation, and that `/healthz` echoes both `CELL_ID` and `CLUSTER_NAME` as distinct parameters. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `Setup(ctx, serviceName)`, When called, Then it wires OTLP traces + metrics (+ logs where accepted) to `OTEL_EXPORTER_OTLP_ENDPOINT` with resource attrs from `OTEL_SERVICE_NAME`, W3C propagation, and runtime metrics. *assert exporter targets + propagator + runtime metrics registered* [Source: FR-L20]
- [ ] **AC2** — Given shutdown, When the ctx cancels, Then telemetry flushes gracefully. *assert flush-on-shutdown, no dropped spans* [Source: FR-L20]
- [ ] **AC3** (cell) — Given `/healthz`, When probed, Then it echoes `CELL_ID` and `CLUSTER_NAME` as **distinct** params; `/livez` and `/healthz` are the CS §10 split. *assert both fields present + distinct; liveness ≠ health* [Source: FR-L20 / cell Rule 4]
- [ ] **AC4** — Given service-name + attribute conventions, When emitted, Then they match the stable names console keys dashboards on (contracts appendix). *assert convention adherence* [Source: FR-L20]
- [ ] **AC5** — Given the OTel SDK, When imported, Then it is confined to `pkg/obs` (a `pkg/auth`-only importer links none of it). *assert dep confinement* [Source: FR-L20 / NFR-L4]

## 🔨 Tasks

- [ ] RED: `TestObsSetupAndHealthzCellEcho` (AC1–AC3).
- [ ] GREEN: `Setup` wiring (exporters, propagation, runtime metrics, flush) + `/healthz`+`/livez` cell echo (AC1–AC4).
- [ ] GREEN: OTel SDK confined to `pkg/obs` (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** absent — obs.Setup is a stdlib-only no-op stub returning a nil-flush shutdown; no OTLP exporter wiring, no /healthz+/livez handlers, no CELL_ID/CLUSTER_NAME echo — the otel SDK dependency is not added.

`pkg/obs` (non-service; no six-tier shape, CS §6). Producer side of the console observability contract (telemetry → Loki/Mimir/Tempo). Seam crossed: cluster otel-collector; `CELL_ID`/`CLUSTER_NAME` env. Sibling deps: E5-S2 (middleware/transports), E2-S3 (publisher propagation), E4-S1 (worker interceptors).

## 🧪 Testing

`TestObsSetupAndHealthzCellEcho` (unit, offline). CS §5 mocking: fake OTLP collector endpoint (foreign — allowed); **never mock `pkg/obs`** (own package).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§10 liveness-vs-health split; §4 dep confinement; §6 non-service). SE-Arch `[[8sYi523i4t]]`: no-cluster-names-in-code lens, flush-on-shutdown lens. cell-lint `[[JGAUQRsw2g]]` (Rule 4 echo, CELL_ID≠CLUSTER_NAME).

## 🔗 References

PRD `fG2jKeRGiu` FR-L20, NFR-L4; Arch `0NPvDR3C0m` A-OBS, §1 topology; cell contract `[[JGAUQRsw2g]]` Rule 4; URL/env scheme `[[86CiGucQwU]]`.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — obs service-name/attribute conventions appendix; the TAG is the dispatch gate).
- [ ] Parent epic: **E5**. Launch-line (usable immediately by any satellite); precedes E5-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
