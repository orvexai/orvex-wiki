## 🎯 Story

**As** every satellite, **I want** the family event-type constants and payload types (`wiki.*` content/permission/purge, `clerk.*` lifecycle, identity's own lifecycle emissions + the push-revocation broadcast type) generated from orvex-studio-contracts codegen with the thin/rich envelope-profile split, **so that** satellites pin the same catalog and the scaffold's hand-written placeholder constants die at the first codegen drop (FR-L10; Arch A-CODEGEN).

**Definition of Done:** ONE named test `TestEventCatalogGeneratedTypes` (unit, in `pkg/events`, asserting through the exported catalog constants + payload types) that every `wiki.*`/`clerk.*`/identity-lifecycle/revocation type resolves to a generated type matching the contracts pin, and that no hand-written placeholder constant survives (they are absent post-codegen). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the contracts pin, When the catalog is generated, Then `wiki.*`, `clerk.*`, identity-lifecycle, and revocation-broadcast type constants + payload types exist under `gen/`. *assert each catalog member is a generated symbol* [Source: FR-L10]
- [ ] **AC2** — Given the thin/rich envelope-profile split, When a payload type is used, Then it maps to the correct profile. *assert thin vs rich profile per type (knowledge FR-K2/K15)* [Source: FR-L10]
- [ ] **AC3** (negative) — Given the scaffold's hand-written placeholder `wiki.page.*` constants, When codegen drops, Then they are removed and no hand-edited generated file can land. *assert no placeholder constant present; regen-diff gate green* [Source: FR-L10 / A-CODEGEN]
- [ ] **AC4** — Given Linear is removed (D-S11), When the catalog is generated, Then no `linear.*` event type exists. *assert absent* [Source: FR-L10 / D-S11]

## 🔨 Tasks

- [ ] RED: `TestEventCatalogGeneratedTypes` + `TestNoPlaceholderConstants` (AC1, AC3).
- [ ] GREEN: wire the codegen output for the event catalog under `pkg/events/gen*` per E7-S1; delete placeholders (AC1–AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** partial — Generated event-type constants (eventtypes_gen.go) + AllEventTypes, gate-tested; but the typed payload structs (thin/rich profiles) are NOT generated (0 payload structs) — only the type-name constants exist.

`pkg/events` (non-service; no six-tier shape, CS §6). Depends on the codegen pipeline (E7-S1). Seam crossed: contracts CloudEvent catalog (FR-C6). Consumers: workflows (`clerk.*`), knowledge (`wiki.*`), auth deny-list (revocation type).

## 🧪 Testing

`TestEventCatalogGeneratedTypes` (unit, offline) + regen-diff gate (E7-S1). CS §5 mocking: none needed — asserts against generated symbols; **never mock `pkg/events`**.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§9 generated-not-hand-edited). SE-Arch `[[8sYi523i4t]]`: drift lens (hand-edit detection), catalog-completeness lens. cell-lint `[[JGAUQRsw2g]]` (`orvexcell` extension in the generated envelope profile).

## 🔗 References

PRD `fG2jKeRGiu` FR-L10; Arch `0NPvDR3C0m` A-CODEGEN; knowledge FR-K1/K2/K15; contracts FR-C6; D-S11 (Linear removed).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — the contracts release tag + event catalog codegen; the TAG is the dispatch gate).
- [ ] Parent epic: **E2**. Blocked by E7-S1 (gen/ trees + regen-diff); rides E2-S1 envelopes.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
