## 🎯 Story

**As** mcp/ai/workflows, **I want** `pkg/knowledgeclient` typed methods for the knowledge query plane — `Query` (keyword|semantic|hybrid, facets, fields projection, `partial` flag), `Retrieve` (RAG top-k chunks with scores), `Related`, `Duplicates`, `AttachmentsSearch` — matching the contracts-authored OpenAPI knowledge conforms to, **so that** every retrieval caller pins the same shapes and no satellite hand-rolls a knowledge call (FR-L15; Arch §1 topology `pkg/knowledgeclient`).

**Definition of Done:** ONE named test `TestKnowledgeQueryPlane` (unit, in `pkg/knowledgeclient`, driving each method against a fake knowledge server) asserting each method maps to the contracts OpenAPI shape, that `Query` honors mode/facets/projection/`partial`, and that `Retrieve` returns scored top-k chunks. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `Query` with mode keyword|semantic|hybrid, facets, and a fields projection, When called, Then the request/response matches the contracts OpenAPI and the `partial` flag round-trips. *assert typed round-trip per mode; `partial` preserved* [Source: FR-L15]
- [ ] **AC2** — Given `Retrieve`, When called, Then it returns RAG top-k chunks each with a score. *assert scored chunks, k respected* [Source: FR-L15]
- [ ] **AC3** — Given `Related`, `Duplicates`, `AttachmentsSearch`, When called, Then each maps to its contracts shape. *assert typed round-trip per method* [Source: FR-L15]
- [ ] **AC4** (discipline) — Given any method, When called, Then the E3-S3 client discipline (deadline, bounded retry, breaker, obs, fail-closed) applies. *assert deadline + span present* [Source: FR-L15 / FR-L14]

## 🔨 Tasks

- [ ] RED: `TestKnowledgeQueryPlane` per method against a fake knowledge server (AC1–AC3).
- [ ] GREEN: typed Query/Retrieve/Related/Duplicates/AttachmentsSearch over the client-discipline harness (AC1–AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** partial — Only unified Search is real (Client.Search in doc.go, tested). Retrieve, Related, Duplicates and AttachmentsSearch are documented as not built (doc.go:23) and unimplemented.

`pkg/knowledgeclient` (non-service; no six-tier shape, CS §6). Grows method-by-method as knowledge/mcp consume it (no speculative surface, PRD §7). Seam crossed: knowledge OpenAPI (contracts). Sibling deps: E3-S3 (discipline harness), E3-S5 (delta cursor), E3-S6 (rebuild step-API).

## 🧪 Testing

`TestKnowledgeQueryPlane` (unit, offline). CS §5 mocking: in-memory knowledge client double (foreign service — shipped NFR-L5); **never mock `pkg/knowledgeclient`**.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§6 non-service; §5 fail-closed). SE-Arch `[[8sYi523i4t]]`: projection-leak lens, partial-result-handling lens. cell-lint `[[JGAUQRsw2g]]` (injected BaseURL).

## 🔗 References

PRD `fG2jKeRGiu` FR-L15, FR-L14; Arch `0NPvDR3C0m` §1 topology; knowledge PRD (query plane); contracts FR-C1/C2.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — knowledge OpenAPI; the TAG is the dispatch gate).
- [ ] Parent epic: **E3**. Depends on E3-S3, E1-S7, E5-S2, E7-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
