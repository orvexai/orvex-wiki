## 🎯 Story

**As** the family, **I want** `pkg/obs` standard HTTP server middleware and client transports so cross-satellite traces stitch end-to-end, with `pkg/engineclient`, `pkg/knowledgeclient`, and the FR-L11 publisher instrumented through it by default, **so that** a request traced across two satellites is one continuous trace without any satellite wiring spans by hand (FR-L21; Arch A-OBS).

**Definition of Done:** ONE named test `TestTraceStitchingAcrossTransports` (unit, in `pkg/obs`, driving a request through the server middleware then an instrumented client transport) asserting the inbound span context propagates to the outbound request (single trace id continuity) and that engine/knowledge client calls emit child spans by default. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the server middleware, When a request with a `traceparent` arrives, Then the inbound span joins that trace and injects context onward. *assert trace-id continuity* [Source: FR-L21]
- [ ] **AC2** — Given the instrumented client transport, When engineclient/knowledgeclient/publisher call out, Then a child span is emitted by default (no per-call wiring). *assert child span present, parent = inbound* [Source: FR-L21]
- [ ] **AC3** — Given two satellites in a chain, When a request flows through, Then the end-to-end trace is continuous. *assert single trace across the hop* [Source: FR-L21]
- [ ] **AC4** — Given no active trace, When a request arrives, Then a root span is started. *assert root span created* [Source: FR-L21]

## 🔨 Tasks

- [ ] RED: `TestTraceStitchingAcrossTransports` (AC1–AC4).
- [ ] GREEN: server middleware (context extraction/injection) + instrumented client transport; default-wire into E3 clients + E2-S3 publisher (AC1–AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** absent — HTTP server middleware and client transports for trace stitching do not exist — obs contains only the stub Setup; no middleware/transport code.

`pkg/obs` (non-service; no six-tier shape, CS §6). Depends on `Setup` (E5-S1). Seam crossed: W3C trace context across HTTP hops. Sibling deps: E3-S3 (clients auto-instrumented), E2-S3 (publisher propagation), E4-S1 (worker interceptors reuse the same obs).

## 🧪 Testing

`TestTraceStitchingAcrossTransports` (unit, offline). CS §5 mocking: in-memory span exporter + fake downstream server (foreign — allowed); **never mock `pkg/obs`/`pkg/engineclient`** (own packages).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§7 propagation; §6 non-service). SE-Arch `[[8sYi523i4t]]`: broken-trace lens, default-on lens (no opt-out that silently drops spans). cell-lint `[[JGAUQRsw2g]]`.

## 🔗 References

PRD `fG2jKeRGiu` FR-L21; Arch `0NPvDR3C0m` A-OBS, §1 topology.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — trace-propagation conventions; the TAG is the dispatch gate).
- [ ] Parent epic: **E5**. Depends on E5-S1; consumed by E3-S3, E2-S3, E4-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
