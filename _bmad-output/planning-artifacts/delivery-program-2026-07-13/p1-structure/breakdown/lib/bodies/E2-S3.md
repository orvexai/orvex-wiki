## 🎯 Story

**As** a Go satellite that emits its own lifecycle events (e.g. identity FR-I15), **I want** a publisher helper that POSTs a CloudEvent to the `studio-spine` Broker ingress (`KNATIVE_BROKER_URL`) with retries and OTel propagation, **so that** satellites emit onto the Kafka-backed spine day one without hand-rolling delivery — while lib carries **no** engine outbox-relay plumbing (the relay is infra, canon P2) (FR-L11; Arch A-KAFKA).

**Definition of Done:** ONE named test `TestBrokerPublisherRetryAndPropagation` (unit, in `pkg/events`, driving the publisher against a fake Broker ingress) asserting a CloudEvent is POSTed to the ingress with bounded retries on transient failure, W3C trace context propagated, and the `orvexcell` stamp present — and that no outbox-relay code path exists in the package. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an event to publish, When the publisher runs, Then it POSTs the CloudEvent to `KNATIVE_BROKER_URL` with the `orvexcell` stamp. *assert POST target + stamped envelope* [Source: FR-L11 / cell Rule 6]
- [ ] **AC2** — Given a transient ingress failure, When publishing, Then bounded retries with backoff are attempted (no unbounded loop). *assert retry count capped, backoff applied* [Source: FR-L11 / FR-L14]
- [ ] **AC3** — Given an active trace, When publishing, Then W3C trace context is propagated to the ingress request. *assert `traceparent` header present* [Source: FR-L11 / FR-L21]
- [ ] **AC4** (boundary) — Given the package, When scanned, Then it carries **no** outbox-relay/bridge plumbing (relay is infra, engine's concern). *assert no relay code; publisher targets Broker ingress only* [Source: FR-L11 / canon P2]

## 🔨 Tasks

- [ ] RED: `TestBrokerPublisherRetryAndPropagation` (AC1–AC3) + `TestNoRelayPlumbing` (AC4).
- [ ] GREEN: publisher targeting `KNATIVE_BROKER_URL`, bounded retries, OTel propagation via `pkg/obs`, `orvexcell` stamp via E2-S1 (AC1–AC3).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** partial — BrokerPublisher produces structured-mode JSON to the per-type cell topic with RequireAll acks (kafka-go built-in retries); but OTel trace propagation is absent — no traceparent/headers injected on the Kafka message.

`pkg/events` (non-service; no six-tier shape, CS §6). Kafka-first: the publisher is launch-line, not deferred; the engine outbox→relay→Kafka relay is a launch prerequisite (infra), never lib code. Seam crossed: the `studio-spine` Broker ingress. Sibling deps: E5-S1/S2 (obs propagation), E2-S1 (stamp).

## 🧪 Testing

`TestBrokerPublisherRetryAndPropagation` (unit, offline). CS §5 mocking: fake Broker ingress HTTP server (foreign — allowed); **never mock `pkg/events`/`pkg/obs`** (own packages).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§5 bounded retries; §6 non-service; §7 propagation). SE-Arch `[[8sYi523i4t]]`: retry-storm lens, at-least-once-delivery lens. cell-lint `[[JGAUQRsw2g]]` (no host literals, Rule 9 — Broker URL injected).

## 🔗 References

PRD `fG2jKeRGiu` FR-L11; Arch `0NPvDR3C0m` A-KAFKA (P2, no Redis→Kafka bridge); identity FR-I15; canon `[[CxjFpIVUZY]]` P2.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — event catalog + Broker ingress contract; the TAG is the dispatch gate).
- [ ] Parent epic: **E2**. Depends on E2-S1 (stamp), E5-S1 (obs propagation).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
