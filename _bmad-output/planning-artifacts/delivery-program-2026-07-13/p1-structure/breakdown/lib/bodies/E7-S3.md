## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** the family, **I want** lib to keep its library shape — no `cmd/`, no Dockerfile, no deploy/kustomize; `go build/test/vet ./...` + lint + the conformance/regen/parity/cell-guard gates as the whole CI; heavy SDKs (Temporal, franz-go, OTel, JWT/JWKS) confined to their packages — **so that** an importer of `pkg/auth` links none of the heavy SDKs and lib never becomes a service or a dumping ground (FR-L25, NFR-L4; Arch A-DEPS, §5).

**Definition of Done:** ONE named test/gate `TestLibraryShapeAndDepConfinement` (CI gate, run in lib CI) asserting no `cmd/`/Dockerfile/deploy manifest exists, that an import-direction lint passes (no package imports a satellite; clients/temporalutil may import only `pkg/obs`/`pkg/auth`), and that a synthetic `pkg/auth`-only importer links none of franz-go/Temporal/OTel. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the repo, When inspected, Then no `cmd/`, Dockerfile, or deploy/kustomize exists. *assert absent* [Source: FR-L25]
- [ ] **AC2** — Given a `pkg/auth`-only importer, When built, Then it links none of franz-go / Temporal SDK / OTel SDK. *assert dep graph excludes heavy SDKs for the auth-only importer* [Source: FR-L25 / NFR-L4]
- [ ] **AC3** — Given the import graph, When linted, Then no package imports a satellite; import-direction rules hold (clients + temporalutil may import only `pkg/obs`/`pkg/auth`). *assert import-direction lint green* [Source: FR-L25 / Arch §1]
- [ ] **AC4** — Given CI, When it runs, Then it is exactly build/test/vet + golangci-lint + the conformance/regen/parity/apidiff/AGPL-guard/cell-guard gates — nothing else. *assert the aggregate CI shape* [Source: FR-L25]
- [ ] **AC5** — Given a new direct dependency, When added, Then it is reviewed against NFR-L4 (and is a mandatory-ADR trigger once the registry exists). *assert dep-review gate* [Source: FR-L25 / NFR-L4]

## 🔨 Tasks

- [ ] GREEN: import-direction lint + heavy-SDK confinement check + auth-only-importer link test (AC2, AC3).
- [ ] GREEN: aggregate CI wiring (build/test/vet/lint + all gates) (AC4); no-service-artifacts assertion (AC1); dep-review gate (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — Library shape holds: no root cmd/ or Dockerfile (repo builds no images per ci.yml), heavy-SDK packages confined, sealtest/shared_lib_seams_test.go locks the seams, and ci-success is the single aggregate fan-in gate.

Module-level (non-service; no six-tier shape, CS §6 non-service note — a domain rule landing here is a family-wide bug). This is where every other epic's CI gate aggregates. Middleware is plain `net/http` (A-DEPS, no framework adapters phase 1). Seam crossed: the CI pipeline. Sibling deps: E1-S6 (authtest), E6-S3 (parity), E7-S1 (regen-diff), E7-S4 (AGPL guard), E1-S5 (cell-guard router-coverage).

## 🧪 Testing

`TestLibraryShapeAndDepConfinement` (CI gate). Tiers: CI. CS §5 mocking: none — static analysis + a synthetic importer build.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§4 dep weight/stdlib-first; §6 non-service note — no domain logic). SE-Arch `[[8sYi523i4t]]`: dumping-ground lens, heavy-SDK-bleed lens. cell-lint `[[JGAUQRsw2g]]` (cell-guard router-coverage in the aggregate CI).

## 🔗 References

PRD `fG2jKeRGiu` FR-L25, NFR-L4; Arch `0NPvDR3C0m` A-DEPS, A-VERSION (CI list), §1/§5.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: **E7**. Aggregates E1-S6, E6-S3, E7-S1, E7-S4, E1-S5 gates.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
