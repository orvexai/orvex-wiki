## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** every satellite touching the spine, **I want** CloudEvents 1.0 envelope helpers (construct/parse/validate, structured + binary mode) that stamp the `orvexcell` extension and make consumers fail closed on a cell mismatch, **so that** no satellite hand-rolls envelope parsing and a foreign-cell event is refused rather than processed (FR-L9; Arch A-CELL Rule 6, §1 topology `pkg/events`).

**Definition of Done:** ONE named test `TestEnvelopeCellStampAndFailClosed` (unit, in `pkg/events`, driving construct→parse round-trips and a foreign-cell consume) asserting every constructed CloudEvent carries `orvexcell = CELL_ID`, that structured and binary modes round-trip, that a malformed envelope is a typed error (never a silent drop), and that a consume path fails closed on `orvexcell != CELL_ID`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a CloudEvent constructed via the helper, When emitted, Then it stamps `orvexcell` = the local `CELL_ID`. *assert extension attr present + equals CELL_ID* [Source: FR-L9 / cell Rule 6]
- [ ] **AC2** — Given structured mode (Trigger POST body) and binary mode (`ce-*` headers), When parsed, Then both round-trip to the same event. *assert equality across both modes* [Source: FR-L9]
- [ ] **AC3** (negative) — Given a malformed envelope, When parsed, Then a typed error is returned, never a silent drop. *assert `err != nil`, typed; no event yielded* [Source: FR-L9]
- [ ] **AC4** (cell) — Given a consume path and an event whose `orvexcell != CELL_ID`, When consumed, Then it fails closed (event not processed). *assert reject, handler not invoked* [Source: FR-L9 / cell Rule 6]

## 🔨 Tasks

- [ ] RED: `TestEnvelopeCellStampAndFailClosed` covering AC1–AC4.
- [ ] GREEN: construct/parse/validate for structured + binary mode; typed malformed-envelope error (AC2, AC3).
- [ ] GREEN: `orvexcell` stamp on construct + fail-closed cell-mismatch check on consume (AC1, AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — CloudEvents envelope helpers, orvexcell stamp, pinned-schema validation, and fail-closed Consume/CheckCell that drops foreign-cell events (ErrCellMismatch); events_test.go, consumer_kafka_test.go, nodegradation_test.go, schema_gate_test.go.

`pkg/events` (non-service; no six-tier shape, CS §6). This is *the* shared envelope helper — no hand-rolled envelope parsing may exist elsewhere in the family. Seam crossed: contracts envelope profiles (thin/rich, FR-C9), `CELL_ID` env. Sibling deps: E1-S3 (revocation Trigger uses this parse), E5-S3 (kafkautil consume stamps/validates via this).

## 🧪 Testing

`TestEnvelopeCellStampAndFailClosed` (unit, offline). CS §5 mocking: fabricated CloudEvent bytes/headers (fixtures); **never mock `pkg/events`** (own package).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§5 fail-closed; §6 non-service). SE-Arch `[[8sYi523i4t]]`: cross-cell-leak lens, silent-drop lens. cell-lint `[[JGAUQRsw2g]]` (Rule 6 stamp + fail-closed consume; helper test + publish lint).

## 🔗 References

PRD `fG2jKeRGiu` FR-L9; Arch `0NPvDR3C0m` A-CELL Rule 6, §1 topology; cell contract `[[JGAUQRsw2g]]` §5; knowledge FR-K2/K15 (envelope profiles).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — CloudEvents envelope profiles + `orvexcell` extension schema; the TAG is the dispatch gate).
- [ ] Parent epic: **E2**. Precedes E2-S2 (types ride these envelopes), E2-S3 (publisher).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
