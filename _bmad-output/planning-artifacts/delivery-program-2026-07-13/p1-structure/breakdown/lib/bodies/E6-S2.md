## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** orvex-wiki-api and the Orvex CLI, **I want** `pkg/dfm` — a clean-room, bidirectional, lossless DfM↔ProseMirror serializer exported as exactly four functions (`PMToDfM`, `DfMToPM`, `MarkdownToPM`, `MarkdownToDfM`) plus a block-level op-list variant, where block types DfM cannot render round-trip through opaque-node handles — **so that** both first-party embedders convert in-process (never calling any engine `/v1/convert`) and an edit around an unrenderable block never corrupts it (FR-L26; Arch A-DFM).

**Definition of Done:** ONE named test `TestDFMRoundTripLossless` (unit, in `pkg/dfm`, driving the four-function surface over golden documents) asserting the package invariant `DfMToPM(PMToDfM(doc)) ≡ doc` up to declared normalizations, that unrenderable blocks (excalidraw/drawio baked+inline, `pendingMermaid`, residual `linear*`) reattach verbatim via opaque handles, and that the offline `MarkdownToPM` path works with no engine reachable. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a ProseMirror document, When `DfMToPM(PMToDfM(doc))` runs, Then it equals `doc` up to declared normalizations. *assert round-trip equality* [Source: FR-L26]
- [ ] **AC2** — Given a block DfM cannot render (drawio/excalidraw/deep embed/unknown future node), When round-tripped, Then it survives as an opaque-node handle reattached verbatim. *assert byte-identical reattach; edit-around does not corrupt* [Source: FR-L26]
- [ ] **AC3** — Given `MarkdownToPM` / `MarkdownToDfM`, When the engine is unreachable, Then conversion succeeds in-process (no engine `/v1/convert` or `/api/markdown/to-*` call). *assert offline path works; zero engine calls* [Source: FR-L26]
- [ ] **AC4** (negative) — Given a residual `linear*` block (Linear removed, D-S11), When round-tripped, Then it degrades to an opaque-preserve handle — never dropped or hard-failed. *assert preserved as opaque; no Linear renderer* [Source: FR-L26 / D-S11]
- [ ] **AC5** — Given the package, When built, Then it is pure computation — stdlib-only, no I/O, no network; block shapes come from the pinned catalog under `gen/`, never fetched at runtime. *assert no I/O imports; gen/ catalog used* [Source: FR-L26 / A-DFM]

## 🔨 Tasks

- [ ] RED: `TestDFMRoundTripLossless` + opaque-handle + offline-markdown + linear-opaque cases (AC1–AC5).
- [ ] GREEN: four-function surface + op-list variant, written from the documented PM schema + engine block-schema catalog (never AGPL TS) (AC1–AC5).
- [ ] GREEN: opaque-node handle mechanism (stable handle, verbatim reattach) (AC2, AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — Four-function clean-room serializer (Serialize/Parse + PmToDfm/DfmToJson) with opaque-node round-trip handles (opaque.go serialize/parse fences) and a type registry; dfm_test.go, equal_test.go, mention_test.go cover it.

`pkg/dfm` (non-service; no six-tier shape, CS §6). Pure computation — no I/O. Replaces the CLI's hand-mirrored serializer and delivers its missing offline `markdown→PM` path + fixes the baked-Excalidraw drop by construction. Seam crossed: engine block-schema catalog (`GET /api/schemas/blocks`) as a **spec input, never a code source**. Sibling deps: **blocked by E6-S1** (provenance audit), E7-S1 (gen/ catalog), E6-S3 (parity gate).

## 🧪 Testing

`TestDFMRoundTripLossless` (unit, offline). CS §5 mocking: none — pure functions over golden fixtures; **never mock `pkg/dfm`** (own package). Fixtures from contracts.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§0 licence; §6 non-service; §4 stdlib-only). SE-Arch `[[8sYi523i4t]]`: lossless-round-trip lens, opaque-handle-corruption lens, unknown-node-degradation lens. cell-lint `[[JGAUQRsw2g]]` (n/a). Provenance: contributors work from spec/catalog only.

## 🔗 References

PRD `fG2jKeRGiu` FR-L26; Arch `0NPvDR3C0m` A-DFM, §1 topology, §5; CLI gaps G9/G20; D-S11.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — pinned engine block-schema catalog + golden DfM/PM fixtures; the TAG is the dispatch gate).
- [ ] Parent epic: **E6**. **Blocked by E6-S1** (provenance audit) and E7-S1 (gen/ catalog); parity-gated by E6-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
