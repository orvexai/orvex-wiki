## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** the family's AGPL boundary owner, **I want** an AGPL-provenance audit of the legacy docmost-cli's hand-mirrored Go serializer to pass **before** `pkg/dfm` lands, establishing that no ported-from-`@docmost/editor-ext` lineage is laundered into the shared closed package, **so that** the clean-room guarantee is real and the contamination path the AGPL boundary forbids is closed (FR-L27; Arch A-DFM; WS-11 pre-task, D-A11).

**Definition of Done:** ONE named test/gate `TestAGPLProvenanceAuditGate` (a CI gate + documented audit artifact, verified against the legacy serializer source) asserting the provenance audit is signed off and the family AGPL-import guard (NFR-L7) is green on the CLI **before** `pkg/dfm` is permitted to land — the audit is a gate on WS-11 start, not a post-hoc check. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the legacy docmost-cli hand-mirrored serializer, When audited, Then a documented provenance audit establishes no ported-from-AGPL lineage is carried forward. *assert audit artifact exists + signed off* [Source: FR-L27]
- [ ] **AC2** — Given the audit, When `pkg/dfm` lands (E6-S2), Then the audit has already passed (gate on WS-11 start, not post-hoc). *assert E6-S2 cannot merge before this gate is green* [Source: FR-L27]
- [ ] **AC3** — Given the family AGPL-import guard (NFR-L7), When run on the CLI before the serializer swap, Then it is green. *assert AGPL guard green on the CLI* [Source: FR-L27 / NFR-L7]
- [ ] **AC4** (negative) — Given evidence of ported AGPL lineage, When found, Then the gate fails and blocks `pkg/dfm`. *assert a contaminated-lineage finding blocks the land* [Source: FR-L27]

## 🔨 Tasks

- [ ] Conduct the provenance audit of the legacy serializer (contributors work from spec/catalog artifacts only, not engine code) (AC1).
- [ ] GREEN: wire the audit-signoff + AGPL-guard-green as a hard gate blocking E6-S2 merge (AC2–AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — The AGPL-provenance audit is captured as the load-bearing clean-room-provenance charter in dfm/doc.go (legacy @docmost/editor-ext is licence-inheriting; contributors work only from the public contract) and enforced by the gates/agpl-import guard in CI.

`pkg/dfm` (non-service; no six-tier shape, CS §6). This is a **process/gate** story, not runtime code — it clears the old code out of the lineage so the from-scratch Orvex CLI can import `pkg/dfm`. Seam crossed: the legacy docmost-cli serializer, the family AGPL-import guard (E7-S4). Sibling deps: E6-S2 (blocked by this), E7-S4 (AGPL guard).

## 🧪 Testing

`TestAGPLProvenanceAuditGate` (CI gate). Tiers: CI/process. CS §5 mocking: none — this is a source-provenance audit + guard. Evidence is the audit artifact + a green AGPL-import guard run.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§0 licence posture; §9 costly-to-reverse decision → A-DFM ADR trigger). SE-Arch `[[8sYi523i4t]]`: contamination-laundering lens, clean-room-provenance lens. cell-lint `[[JGAUQRsw2g]]` (n/a — provenance gate).

## 🔗 References

PRD `fG2jKeRGiu` FR-L27, NFR-L7; Arch `0NPvDR3C0m` A-DFM (WS-11 pre-task, D-A11), §5/§6; AGPL boundary (USER 2026-07-03).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — the contract TAG is the dispatch gate; the audit posture / AGPL-import guard rules are pinned there).
- [ ] Parent epic: **E6**. **Blocks E6-S2** (pkg/dfm cannot land until this passes). Depends on E7-S4 (AGPL guard).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
