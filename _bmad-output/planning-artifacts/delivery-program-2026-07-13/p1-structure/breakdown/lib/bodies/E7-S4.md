## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** the closed-program licence owner, **I want** the family AGPL-import CI guard running in lib — failing any code in the module that is derived from, ported from, or dependent on `@docmost/*` or other AGPL sources — **so that** no AGPL contamination reaches this shared closed library (its real targets are vendored, copied, or ported code, since a literal Go import of an npm package is impossible) (NFR-L7; Arch A-DFM, §5).

**Definition of Done:** ONE named test/gate `TestAGPLImportGuard` (CI gate, run in lib CI) asserting that a synthetic `@docmost/*` / AGPL-lineage import or vendored/ported artifact fails the build, that `pkg/dfm`'s clean-room provenance note is present in-package, and that the guard is the same family-wide guard contracts hosts as a family NFR. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** (negative) — Given a synthetic `@docmost/*` or AGPL-sourced/vendored/ported artifact in the module, When CI runs, Then the guard fails the build. *assert AGPL lineage ⇒ CI red* [Source: NFR-L7]
- [ ] **AC2** — Given a clean module, When the guard runs, Then it passes. *assert green on clean tree* [Source: NFR-L7]
- [ ] **AC3** — Given `pkg/dfm`, When inspected, Then its clean-room provenance note lives in the package `doc.go`. *assert provenance note present* [Source: NFR-L7 / FR-L27]
- [ ] **AC4** — Given Studio imports (orvex-wiki-api, Orvex CLI intentionally import this Studio lib), When the guard runs, Then Studio-namespaced imports are **not** targeted — only `@docmost/*` / AGPL (D-S1/D-S9 Studio-reuse boundary). *assert Studio imports pass* [Source: NFR-L7 / D-S1/D-S9]

## 🔨 Tasks

- [ ] GREEN: AGPL-import guard (scans for `@docmost/*` + vendored/ported/derived AGPL lineage) wired into lib CI (AC1, AC2).
- [ ] GREEN: `pkg/dfm` provenance note in `doc.go`; Studio-reuse allowlist so Studio imports pass (AC3, AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — AGPL-import CI guard implemented as gates/agpl-import/agpl-import-guard.sh, invoked by the ci.yml agpl-guard job and the make agpl-guard target, forbidding any @docmost/* import in-module.

Module-level (non-service; no six-tier shape, CS §6). The guard also gates E6-S1 (provenance audit) and must be green on the CLI before the serializer swap. Go-vs-npm makes a literal import impossible; the guard's real targets are vendored/copied/ported code. Seam crossed: the family AGPL-import guard NFR (contracts hosts it). Sibling deps: E6-S1 (audit), E6-S2/S3 (dfm), E7-S3 (aggregate CI).

## 🧪 Testing

`TestAGPLImportGuard` (CI gate). Tiers: CI. CS §5 mocking: none — static source/dep scan with a synthetic contaminated fixture.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§0 licence posture; §4 dependency review). SE-Arch `[[8sYi523i4t]]`: contamination-laundering lens, Studio-vs-AGPL-boundary lens. cell-lint `[[JGAUQRsw2g]]` (n/a).

## 🔗 References

PRD `fG2jKeRGiu` NFR-L7, FR-L27; Arch `0NPvDR3C0m` A-DFM, §5; AGPL boundary + Studio-reuse (D-S1/D-S9, USER 2026-07-03/05).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — the family AGPL-import guard rules + Studio-reuse allowlist; the TAG is the dispatch gate).
- [ ] Parent epic: **E7**. **Gates** E6-S1/S2/S3; part of the E7-S3 aggregate CI.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
