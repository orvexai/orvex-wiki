## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** any satellite gating on entitlements (and console for display), **I want** `pkg/billingclient` — a typed, fail-closed read client for orvex-studio-billing (the plans/entitlements SoR): resolve a tenant/org's active plan (Free / £7 Personal / Teams-teaser / hidden Enterprise), its entitlement values (Free caps, F-QUOTA quota values, AI spend cap), and plan/entitlement change events — **so that** the SoR is single-sourced and a billing outage denies the gated capability rather than silently allowing it (FR-L29; Arch A-CLIENTS).

**Definition of Done:** ONE named test `TestBillingClientFailClosed` (unit, in `pkg/billingclient`, driving entitlement reads against a fake billing server) asserting a resolved plan/entitlement round-trips the contracts `billing.yaml` shape, that billing-unreachable or a malformed response ⇒ the gated capability is **denied** (never allowed), and that a short entitlement cache only ever over-narrows (a stale deny is safe, a stale allow past its bound fails closed). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tenant/org, When plan+entitlements are read, Then they match the contracts `billing.yaml` shape (plan tier + entitlement values + change events). *assert typed round-trip* [Source: FR-L29]
- [ ] **AC2** (error path) — Given billing unreachable or a malformed entitlement response, When a gated capability checks, Then it is denied. *assert deny; no "entitled" representable on failure* [Source: FR-L29 / Arch A-CLIENTS]
- [ ] **AC3** (cache) — Given a short tenant-keyed entitlement cache, When a stale entry is used, Then it only over-narrows (stale deny safe; stale allow past its bound fails closed). *assert over-narrowing only; tenant-keyed* [Source: FR-L29 / FR-L3/NFR-L6]
- [ ] **AC4** (boundary) — Given the client, When used, Then it only reads — it never mints a plan, changes a cap, or bills. *assert no write/mutation methods exist* [Source: FR-L29]

## 🔨 Tasks

- [ ] RED: `TestBillingClientFailClosed` covering read + fail-closed + over-narrowing cache (AC1–AC3).
- [ ] GREEN: typed read methods over the E3-S3 discipline harness; deny-on-failure; short tenant-keyed cache (over-narrow only) (AC1–AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — GetEntitlement is real and fail-closed (typed Entitlement, ErrContractShape on undecodable body, real transport error on unreachable host); TestGetEntitlement covers shape-bind + fail-closed cases.

`pkg/billingclient` (non-service; no six-tier shape, CS §6). Billing owns the values (SoR); the engine enforces quota (F-QUOTA), ai enforces spend cap (LiteLLM `max_budget`), console displays — all read through this typed shape. Seam crossed: billing entitlement-read OpenAPI (contracts `billing.yaml`). Sibling deps: E3-S3 (discipline), E1-S7 (credential).

## 🧪 Testing

`TestBillingClientFailClosed` (unit, offline). CS §5 mocking: fake billing server + injected unreachable/malformed (foreign — allowed, NFR-L5); **never mock `pkg/billingclient`**.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§5 fail-closed; §6 non-service). SE-Arch `[[8sYi523i4t]]`: stale-allow lens, read-only-boundary lens. cell-lint `[[JGAUQRsw2g]]` (tenant-keyed cache; injected BaseURL).

## 🔗 References

PRD `fG2jKeRGiu` FR-L29; Arch `0NPvDR3C0m` A-CLIENTS, §1 topology; billing PRD; contracts `billing.yaml`.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — billing entitlement-read OpenAPI; the TAG is the dispatch gate).
- [ ] Parent epic: **E3**. Lands when billing exposes its seam (PRD §7); depends on E3-S3, E1-S7.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
