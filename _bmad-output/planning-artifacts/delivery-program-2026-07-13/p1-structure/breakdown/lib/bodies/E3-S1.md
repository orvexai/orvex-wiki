## 🎯 Story

**As** knowledge/mcp/ai/workflows/wiki-api, **I want** `pkg/engineclient` typed methods for the engine primitives and nothing else — block chokepoint read/write, FR-13 ACL evaluate/narrow (batch ceiling 500), FR-18/38 export, FR-35 export-policy, realtime-invalidate, resolve-slug, provision/deprovision (distinct-typed), and the F-QUOTA quota-status read-back — **so that** every satellite calls the same door with the same shapes and no one hand-rolls an engine call (FR-L12; Arch A-CLIENTS).

**Definition of Done:** ONE named test `TestEngineClientPrimitives` (unit, in `pkg/engineclient`, driving each method against a fake engine) asserting each primitive maps to the contracts-authored shape, the ACL batch honors the 500-ID ceiling, and quota-status read-back returns per-tenant counters vs entitlement values without provoking a 402. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given each engine primitive, When called, Then the request/response matches the contracts `engine-primitives.yaml` shape. *assert typed round-trip per method* [Source: FR-L12]
- [ ] **AC2** — Given a `filterAccessiblePageIds` batch > 500 IDs, When called, Then it respects the 500-ID batch ceiling (knowledge A1). *assert batching at ≤ 500* [Source: FR-L12]
- [ ] **AC3** — Given the quota-status primitive, When called, Then it returns pages/storage/files/members counters vs entitlement values without a 402. *assert read-only headroom pre-check* [Source: FR-L12]
- [ ] **AC4** (vocabulary trap) — Given the engine provision/deprovision controller shape `{clerkOrgId, clerkUserId, orgName, email, name}`, mode `deactivate|delete`, When typed, Then it is a **distinct Go type** from identity's mirror `{scope,id}`/`disable|delete` with no conversion. *assert no cross-type conversion compiles* [Source: FR-L12 / A-CLIENTS]
- [ ] **AC5** — Given export/export-policy, When called, Then FR-18 markdown + FR-38 embed-resolved `text_repr` and the FR-35 permission evaluation are typed. *assert export shapes present* [Source: FR-L12]

## 🔨 Tasks

- [ ] RED: `TestEngineClientPrimitives` per method against a fake engine (AC1–AC5).
- [ ] GREEN: typed methods for chokepoint/ACL/export/export-policy/realtime-invalidate/resolve-slug/provision-deprovision/quota-status (AC1–AC5).
- [ ] GREEN: distinct provision/deprovision Go type, no conversion to identity's (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** partial — Only the resolve-slug primitive is real (Client.ResolveSlug in doc.go, tested). The chokepoint (GetBlocks/PatchBlocks), ACLEvaluate/ACLNarrow, Export and quota-status primitives are documented as 'not yet built' (doc.go:25) and not implemented.

`pkg/engineclient` (non-service; no six-tier shape, CS §6). NestJS engine analogue: the engine's `apply-ops` chokepoint + CASL ACL + export controllers are the primitives this wraps (engine is the SoR; lib narrows, never widens). Seam crossed: engine OpenAPI (contracts FR-C1). Sibling deps: E3-S2 (F-QUOTA terminal), E3-S3 (narrow typing + discipline), E1-S7 (api-key), E5 (obs).

## 🧪 Testing

`TestEngineClientPrimitives` (unit, offline). CS §5 mocking: fake/in-memory engine client (foreign service — shipped per NFR-L5); **never mock `pkg/engineclient`** (own package).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§5 fail-closed; §6 non-service). SE-Arch `[[8sYi523i4t]]`: batch-ceiling lens, vocabulary-trap (cross-wiring) lens. cell-lint `[[JGAUQRsw2g]]` (injected BaseURL, Rule 9).

## 🔗 References

PRD `fG2jKeRGiu` FR-L12; Arch `0NPvDR3C0m` A-CLIENTS, §1 topology; engine PRD `EPsdD7uK8e`; knowledge A1; contracts FR-C1.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — engine-primitives OpenAPI; the TAG is the dispatch gate).
- [ ] Parent epic: **E3**. Pairs with E3-S2, E3-S3; depends on E1-S7, E5-S2, E7-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
