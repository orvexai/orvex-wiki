## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As** the whole family, **I want** the codegen relationship implemented — generated contract types (event catalog constants + payload types, envelope profiles incl. `orvexcell` + cell-claim schema, engine/knowledge/identity/step-API shapes, scope schema, golden fixtures) committed under `gen/`-suffixed trees inside the owning packages, pinned to a contracts release tag in one in-repo manifest, with a regenerate-and-diff CI gate — **so that** satellites import contract types via lib (never running codegen themselves) and hand-editing generated code is impossible to land (FR-L24; Arch A-CODEGEN).

**Definition of Done:** ONE named test/gate `TestRegenDiffGate` (CI gate, run in lib CI) asserting that regenerating against the pinned contracts tag produces byte-identical committed `gen/` trees, that any hand-edit to a generated file fails the build, and that the contracts pin is recorded in a single in-repo manifest. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the pinned contracts tag, When codegen regenerates, Then the committed `gen/` trees are byte-identical (regen-diff green). *assert zero diff on regen* [Source: FR-L24]
- [ ] **AC2** (negative) — Given a hand-edited generated file, When CI runs, Then the build fails. *assert hand-edit ⇒ CI red* [Source: FR-L24 / A-CODEGEN]
- [ ] **AC3** — Given the contracts pin, When recorded, Then it lives in a single in-repo manifest; bumping it is a reviewed PR and ≥ minor release. *assert one manifest; bump = ≥ minor* [Source: FR-L24 / FR-L23]
- [ ] **AC4** — Given satellites, When they consume contract types, Then they import lib's committed `gen/` and never run codegen themselves (D-CON-2 working reading). *assert satellite path imports gen/, no codegen step* [Source: FR-L24 / OQ-L3]
- [ ] **AC5** — Given placeholder hand-written constants, When the first codegen drops, Then they are removed. *assert no placeholder survives* [Source: FR-L24]

## 🔨 Tasks

- [ ] GREEN: codegen toolchain producing `gen/` trees under owning packages; single pin manifest (AC1, AC3).
- [ ] GREEN: regen-diff CI gate + hand-edit detection (AC1, AC2).
- [ ] GREEN: delete placeholder constants at first drop (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** present — Pinned gen/ trees regenerated from gen/contracts/ (pinned SHA in PIN) via scripts/codegen.sh, with gen_test.go asserting committed output matches regeneration (drift/stale gate) and deterministic regen — runs in the CI test job.

`gen/`-suffixed trees across `pkg/events`/`pkg/engineclient`/`pkg/knowledgeclient`/`pkg/identityclient`/`pkg/billingclient`/`pkg/dfm` (non-service; no six-tier shape, CS §6). Unblocks E2-S2, E3-*, E6-S2/S3 (they import committed `gen/`). OQ-L3 (generator toolchain) pinned by ENG-2092. Seam crossed: the contracts release tag. Sibling deps: every generated-type consumer.

## 🧪 Testing

`TestRegenDiffGate` (CI gate). Tiers: CI. CS §5 mocking: none — runs the real generator against the pinned tag. Evidence is a zero-diff regen + a failing hand-edit case.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§9 generated-not-hand-edited; §6 non-service). SE-Arch `[[8sYi523i4t]]`: drift-gate lens, pin-discipline lens. cell-lint `[[JGAUQRsw2g]]` (`orvexcell` extension + cell-claim schema are generated here).

## 🔗 References

PRD `fG2jKeRGiu` FR-L24, FR-L23; Arch `0NPvDR3C0m` A-CODEGEN, §4; contracts D-CON-2; OQ-L3.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — the contracts release tag lib pins is minted at pack certification; OQ-L3 toolchain pinned there — the TAG is the dispatch gate).
- [ ] Parent epic: **E7**. **Unblocks** E2-S2, E3-S1/S4/S6/S7/S8, E6-S2/S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
