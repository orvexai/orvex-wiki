## 🎯 Story

**As** orvex-studio-workflows (and only it), **I want** `pkg/temporalutil/worker` — client dial (`temporal-frontend.temporal.svc`, per-app logical namespace convention), worker bootstrap with graceful shutdown, activity helpers (heartbeats, typed retries, step-API call wrappers, obs interceptors) that map the FR-L19 typed error envelope onto Temporal retry classification — **so that** central retries neither mask failures as false-green nor stall on transient errors, and no satellite ever runs an in-satellite worker (FR-L18; Arch A-STEPAPI, D-WF-1).

**Definition of Done:** ONE named test `TestWorkerRetryClassificationMapping` (unit, in `pkg/temporalutil/worker`, driving an activity wrapper) asserting a retryable step error maps to a Temporal retryable failure and a terminal step error maps to a non-retryable `ApplicationError`, plus a repo-level CI grep proving a satellite importing `temporalutil/worker` fails its build. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a retryable step-API error, When the activity wrapper handles it, Then it surfaces a Temporal **retryable** failure. *assert retryable classification* [Source: FR-L18]
- [ ] **AC2** — Given a terminal step-API error, When the activity wrapper handles it, Then it surfaces a non-retryable `ApplicationError`. *assert non-retryable classification* [Source: FR-L18]
- [ ] **AC3** (D-WF-1) — Given any satellite repo (not workflows), When CI runs, Then importing `temporalutil/worker` fails the build (CI grep). *assert grep gate rejects the worker import* [Source: FR-L18 / Arch A-STEPAPI]
- [ ] **AC4** — Given worker bootstrap, When shut down, Then it drains gracefully and flushes obs. *assert graceful shutdown + flush* [Source: FR-L18]
- [ ] **AC5** — Given the Temporal Go SDK, When imported, Then it is confined to `temporalutil/worker` (a `pkg/auth`-only importer links none of it). *assert dep confinement* [Source: FR-L18 / NFR-L4]

## 🔨 Tasks

- [ ] RED: `TestWorkerRetryClassificationMapping` (AC1, AC2) + the D-WF-1 grep test (AC3).
- [ ] GREEN: client dial, worker bootstrap + graceful shutdown, activity helpers with retry mapping + obs interceptors (AC1–AC5).
- [ ] GREEN: package boundary confining the Temporal SDK to the worker sub-package (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** partial — Charter + KEDA-friendly WorkerConfig type exist, but RunWorker is a stdlib-only stub returning ErrNotImplemented (Temporal SDK not added, no activity helpers). The D-WF-1 import ban is enforced in satellite repos, not here.

`pkg/temporalutil/worker` (non-service; no six-tier shape, CS §6). Consumed **only** by workflows. Uses the E3-S6 rebuild step types as call wrappers. Seam crossed: Temporal frontend, the satellite step-APIs (E4-S2). Sibling deps: E4-S2 (envelope it maps), E5-S1 (obs interceptors).

## 🧪 Testing

`TestWorkerRetryClassificationMapping` (unit, offline) + CI grep. CS §5 mocking: Temporal SDK test env / fake activity (foreign SDK — allowed); **never mock `pkg/temporalutil`/`pkg/knowledgeclient`** (own packages).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§6 non-service; §4 dep confinement). SE-Arch `[[8sYi523i4t]]`: false-green-retry lens, D-WF-1-enforcement (grep-not-compiler-wall) lens. cell-lint `[[JGAUQRsw2g]]`.

## 🔗 References

PRD `fG2jKeRGiu` FR-L18, NFR-L4; Arch `0NPvDR3C0m` A-STEPAPI, §1 topology; D-WF-1.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — step-API retry-classification contract; the TAG is the dispatch gate).
- [ ] Parent epic: **E4**. Depends on E3-S6 (rebuild types), E4-S2 (error envelope), E5-S1 (obs). Lands with the workflows PRD/build.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
