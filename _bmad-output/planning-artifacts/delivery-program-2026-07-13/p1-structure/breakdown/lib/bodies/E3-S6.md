## 🎯 Story

**As** orvex-studio-workflows activities and the knowledge server, **I want** the rebuild step-API types + client (`namespace / walk?cursor / index-batch / delta-repull / alias-swap / retire`) defined once in `pkg/knowledgeclient`, **so that** the workflow and the knowledge server pin the same shapes and the rebuild leg cannot drift between the two (FR-L17; knowledge A8; Arch A-STEPAPI — "the rebuild step shapes live in `pkg/knowledgeclient`").

**Definition of Done:** ONE named test `TestRebuildStepAPITypes` (unit, in `pkg/knowledgeclient`, driving each rebuild call against a fake knowledge step-API) asserting every step (`namespace/walk/index-batch/delta-repull/alias-swap/retire`) maps to its contracts shape and that the same types are what `pkg/temporalutil` activities consume. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given each rebuild step, When called, Then the request/response matches the contracts-authored step-API shape. *assert typed round-trip per step* [Source: FR-L17]
- [ ] **AC2** — Given the same rebuild types, When imported by a workflow activity (E4), Then they are the identical Go types (single home). *assert no parallel type definition; activities import these* [Source: FR-L17 / Arch A-STEPAPI]
- [ ] **AC3** (discipline) — Given any rebuild call, When executed, Then E3-S3 client discipline applies (deadline, bounded retry, breaker, obs, fail-closed). *assert deadline + span* [Source: FR-L17 / FR-L14]
- [ ] **AC4** — Given `walk?cursor`, When paginating, Then the cursor is opaque/epoch-bearing consistent with E3-S5. *assert opaque cursor handling* [Source: FR-L17]

## 🔨 Tasks

- [ ] RED: `TestRebuildStepAPITypes` per step against a fake step-API (AC1, AC4).
- [ ] GREEN: rebuild step types + client over the discipline harness; exported for `pkg/temporalutil` reuse (AC1–AC3).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** absent — The rebuild step-API (namespace/walk/index-batch/alias-swap) types and client are not implemented anywhere in knowledgeclient.

`pkg/knowledgeclient` (non-service; no six-tier shape, CS §6). These are the shapes the D-WF-1 step-API split (E4) pins from the client side. Seam crossed: knowledge rebuild step-API (contracts, knowledge A8). Sibling deps: E4-S1/S2 (worker+stepapi consume these types), E3-S3 (discipline).

## 🧪 Testing

`TestRebuildStepAPITypes` (unit, offline). CS §5 mocking: fake knowledge step-API server (foreign — allowed); **never mock `pkg/knowledgeclient`/`pkg/temporalutil`** (own packages).

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§9 single-home types). SE-Arch `[[8sYi523i4t]]`: type-drift-across-repos lens, idempotency lens (pairs with E4-S2). cell-lint `[[JGAUQRsw2g]]`.

## 🔗 References

PRD `fG2jKeRGiu` FR-L17, FR-L14; Arch `0NPvDR3C0m` A-STEPAPI, §1 topology; knowledge A8; contracts FR-C1/C2.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — rebuild step-API shapes; the TAG is the dispatch gate).
- [ ] Parent epic: **E3**. Consumed by E4-S1/S2; depends on E3-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
