## 🎯 Story

**As** a delta-consuming caller (CLI sync, workflows), **I want** `pkg/knowledgeclient`'s projection-delta client with the cursor treated as opaque and epoch-bearing, where an epoch bump or `stream.gap`-class response surfaces as a typed full-resync condition, **so that** a caller can never mistake a gap for data and never parse the cursor (FR-L16; knowledge FR-K20; Arch §1 topology).

**Definition of Done:** ONE named test `TestProjectionDeltaOpaqueCursor` (unit, in `pkg/knowledgeclient`, driving delta pulls against a fake knowledge server) asserting a normal delta advances the opaque cursor, an epoch bump surfaces a typed `ErrFullResync` (never data), and the caller never substrings/parses the cursor. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2092); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a cursor-based delta pull, When called, Then it returns the delta and an opaque advanced cursor. *assert cursor is an opaque token, not inspected* [Source: FR-L16]
- [ ] **AC2** (resync) — Given an epoch bump or `stream.gap`-class response, When received, Then a typed full-resync condition is surfaced, never data. *assert `ErrFullResync`, no partial data yielded* [Source: FR-L16 / knowledge FR-K20]
- [ ] **AC3** (negative) — Given the cursor, When handled, Then the client never parses/substrings it (it is opaque and epoch-bearing). *assert no cursor introspection in code path* [Source: FR-L16]
- [ ] **AC4** (discipline) — Given any delta call, When executed, Then E3-S3 client discipline applies. *assert deadline + span + fail-closed* [Source: FR-L16 / FR-L14]

## 🔨 Tasks

- [ ] RED: `TestProjectionDeltaOpaqueCursor` covering normal advance + epoch-bump resync (AC1–AC3).
- [ ] GREEN: opaque epoch-bearing cursor delta client over the discipline harness; typed full-resync condition (AC1–AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 8c77c28, 2026-07-14):** absent — The opaque epoch-bearing projection-delta cursor client does not exist — no cursor type or method in knowledgeclient.

`pkg/knowledgeclient` (non-service; no six-tier shape, CS §6). Mirrors the kafkautil gap semantics (E5-S3) so the family has one resync vocabulary. Seam crossed: knowledge projection-delta API (FR-K20), SSE cursor scheme A6a. Sibling deps: E3-S4 (query plane), E5-S3 (cursor gap parallel).

## 🧪 Testing

`TestProjectionDeltaOpaqueCursor` (unit, offline). CS §5 mocking: fake knowledge server emitting epoch bumps (foreign — allowed); **never mock `pkg/knowledgeclient`**.

## 📏 Guidance

CS `[[6aMAzsYeQb]]` §§0/3/4/5/6/7/10/11 (§5 gap-is-not-data). SE-Arch `[[8sYi523i4t]]`: silent-skip lens, cursor-opacity lens. cell-lint `[[JGAUQRsw2g]]`.

## 🔗 References

PRD `fG2jKeRGiu` FR-L16, FR-L14; Arch `0NPvDR3C0m` §1 topology; knowledge FR-K20, A6a; contracts FR-C1.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2092** (Definition Pack — projection-delta shape + SSE cursor scheme A6a; the TAG is the dispatch gate).
- [ ] Parent epic: **E3**. Depends on E3-S3, E3-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
