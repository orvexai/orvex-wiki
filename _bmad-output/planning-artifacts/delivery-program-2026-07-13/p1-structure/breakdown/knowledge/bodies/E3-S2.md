## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the permanent ranking home (D-S14) I want RRF fusion + freshness ordering + an optional nil-safe rerank stage so that hybrid/multi-corpus results rank correctly and ai only consumes ranked results.

**Definition of Done:** `TestHybridRetrieveRanksAndQuarantines` (integration, `internal/ranking`+`internal/search`) — RRF fuses keyword+vector (and cross-corpus) by RANK not raw score, freshness re-orders, and an attached-but-unreachable reranker returns the RRF+freshness order unmodified (never white-screens, never fabricates a score). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given keyword + vector (and multi-corpus) candidates, When fused, Then `internal/ranking.FuseRRF` fuses by RANK — so per-corpus vector spaces need no shared score scale. *Assert: single RRF path; rank-based fusion.* [Source: FR-K11]
- [ ] **AC2** — Given fused hits, When ordered, Then a freshness pass re-orders (fresh outranks stale, `TestSearchFreshnessOutranksStale` class). *Assert: fresher doc ranks above stale duplicate.* [Source: FR-K11]
- [ ] **AC3** (rerank off) — Given a fresh `New()` orchestrator, When queried, Then it is RRF-terminal (rerank OFF by default; `WithReranker` must be called explicitly). *Assert: no rerank without explicit attach.* [Source: FR-K11]
- [ ] **AC4** (rerank fail-safe) — Given an attached reranker that errors/is unreachable, When queried, Then the RRF+freshness order is returned unmodified and no fabricated score is injected (`TestRerankerUnavailableGracefulSkip`). *Assert: never white-screens; order intact.* [Source: FR-K11]
- [ ] **AC5** — Given ranking, When executed, Then it lives entirely in knowledge (D-S14) — ai consumes ranked results only. *Assert: no ranking exported/deferred to ai.* [Source: FR-K11]

## 🔨 Tasks
- [ ] RED: RRF-by-rank fusion across keyword+vector+corpora (AC1). (AC: 1)
- [ ] GREEN: freshness re-order pass (AC2). (AC: 2)
- [ ] RED→GREEN: nil-safe off-by-default reranker port (AC3, AC4). (AC: 3,4)
- [ ] GREEN: single-fusion-path assertion (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** present — internal/ranking/ranking.go FuseRRF + ComputeFreshnessBonus real; nil-safe optional rerank (clients.Reranker, workflow.Search step 5); tested ranking/ranking_test.go + clients/reranker_test.go.

Tier: request-serving compute (`internal/ranking`, `internal/clients.Reranker` true-external port — NOT `internal/event`). Engine analogue: none. Seams: reranker (true-external, injected), store candidates. Sibling: E3-S1 (query), E10 (cross-corpus RRF).

## 🧪 Testing
DoD `TestHybridRetrieveRanksAndQuarantines` + `TestRerankerUnavailableGracefulSkip`. Tiers: unit (fusion) + integration. CS §5: mock the reranker true-external; never mock `internal/ranking` (own package).

## 📏 Guidance
CS §§3/4/5/10/11; SE-Arch honesty lens (never fabricate score, never white-screen — CS §10/§11); ADR trigger J7 recorded; cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K11 (D-S14); Architecture `azRwTCZMqw` A7/J7.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: reranker port shape if externalized).
- [ ] Parent epic: E3. Depends on E3-S1, E2-S2.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
