## 🎯 Story
As the vendor seam I want a store-adapter Go interface (Upsert/Query/DeleteNamespace/DeleteDocs/AliasSwap) with a conformance suite that runs against a `memindex` reference so that a store swap stays a config + adapter change.

**Definition of Done:** `TestStoreAdapterConformance` (integration, `internal/store`) — the conformance suite (Upsert/Query hybrid|keyword|vector/DeleteNamespace/DeleteDocs/AliasSwap + isolation probes) passes identically against BOTH the turbopuffer adapter and the `memindex` reference adapter. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the adapter interface, When defined, Then it exposes `Upsert`, `Query(hybrid|keyword|vector, tenantNS, prefilter)`, `DeleteNamespace`, `DeleteDocs`, `AliasSwap`. *Assert: interface methods present; no store type leaks outside the package.* [Source: NFR-K7]
- [ ] **AC2** — Given two implementations, When the conformance suite runs in CI, Then both `turbopuffer` and `memindex` pass identically. *Assert: same suite, both green.* [Source: NFR-K7, FR-K9]
- [ ] **AC3** — Given `AliasSwap`, When invoked, Then it is a single atomic pointer update (`tenant → active-ns` map in Postgres). *Assert: swap is atomic; no dual-active window.* [Source: NFR-K7, FR-K9]
- [ ] **AC4** (isolation) — Given the conformance suite, When run, Then it includes namespace-isolation probes (cross-namespace read = 0). *Assert: isolation probe green on both adapters.* [Source: NFR-K7, NFR-K3]
- [ ] **AC5** (negative) — Given code that references a concrete store type outside the adapter package, When linted, Then CI fails. *Assert: no-store-type-outside-adapter lint enforced.* [Source: NFR-K7]

## 🔨 Tasks
- [ ] RED: conformance suite skeleton against memindex (AC1, AC2). (AC: 1,2)
- [ ] GREEN: memindex reference adapter (AC2). (AC: 2)
- [ ] RED→GREEN: AliasSwap atomic pointer (AC3). (AC: 3)
- [ ] RED→GREEN: isolation probes in suite (AC4). (AC: 4)
- [ ] RED→GREEN: no-store-type-outside-adapter lint (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — turbopuffer.Adapter seam (Upsert/Query/DeleteNamespace/DeleteDocs/AliasSwap) + memindex reference + shared conformance_test.go present; but DeleteNamespace/DeleteDocs are errNotWired stubs in the real Client (turbopuffer.go).

Tier: seam (`internal/store` adapter interface + `memindex`). Engine analogue: none. Seams: store adapter is the seam. Sibling: E2-S3 (turbopuffer impl), E8 (AliasSwap in rebuild).

## 🧪 Testing
DoD `TestStoreAdapterConformance`. Tiers: unit + integration (both adapters). CS §5: `memindex` is an in-repo reference (own package — do NOT mock it away); mock nothing of knowledge's own store.

## 📏 Guidance
CS §§3/4/5/7/10/11; SE-Arch seam + reliability lenses (conformance-across-adapters); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K9/NFR-K7; Architecture `azRwTCZMqw` A3.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: adapter interface shape).
- [ ] Parent epic: E2. Depends on E2-S1 (alias pointer table).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
