## 🎯 Story
As Memory retrieval I want a Turbopuffer namespace wall keyed by the card's `owner_scope` plus per-card ordering and read-your-writes so that org memories never leak or vanish and "confirmed = visible" holds for the confirming user.

**Definition of Done:** `TestMemoryNamespaceWallOwnerScopeAndReadYourWrites` (integration, `internal/sources/memory`) — memory retrieval uses a namespace wall keyed by the card `owner_scope` (`user-id` for user cards, `org-id` for org cards), queries `{caller user ns} ∪ {caller org ns filtered by the firewall}`, applies monotonic per-card version for idempotent last-writer-wins projection, and delivers read-your-writes for the confirming user. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** (AD-7) — Given memory retrieval, When executed, Then it uses a Turbopuffer **namespace wall** (not attribute-scoped filtering) keyed on the card's `owner_scope` identity — `user-id` for a user card, `org-id` for an org card, derived at projection and carried on the `memory.*` event. *Assert: namespace key = owner_scope; cross-namespace read = 0.* [Source: AD-7]
- [ ] **AC2** (AD-7 union) — Given a caller, When querying, Then retrieval covers `{caller user namespace} ∪ {caller org namespace filtered by the AD-8 firewall}`. *Assert: user + firewall-filtered org memories returned; no other user's org-card leaked.* [Source: AD-7]
- [ ] **AC3** (AD-11 ordering) — Given `memory.*` CloudEvents, When projected, Then they are partitioned by `card-id` for per-card ordering and carry a monotonic card version for idempotent last-writer-wins — a `memory.superseded` never overtakes its `memory.created`. *Assert: out-of-order supersede does not resurrect stale card.* [Source: AD-11]
- [ ] **AC4** (AD-11 RYW) — Given a user just confirmed a card, When they read, Then delivery is read-your-writes — the Composer reads the api SoR for that user's just-confirmed cards until projection catches up (or the confirm ack is withheld until it does). *Assert: confirmed card immediately visible to the confirming user.* [Source: AD-11]
- [ ] **AC5** — Given the AD-2 card schema, AD-5a compose port, and MCP memory additions, When pinned, Then they live in orvex-studio-contracts and `memory.*` rides studio-spine against golden fixtures. *Assert: seam pinned in contracts; golden-fixture test green.* [Source: AD-11]

## 🔨 Tasks
- [ ] RED: owner_scope namespace wall (AD-7) (AC1, AC2). (AC: 1,2)
- [ ] RED→GREEN: per-card partitioned monotonic version (AC3). (AC: 3)
- [ ] RED→GREEN: read-your-writes for confirming user (AC4). (AC: 4)
- [ ] GREEN: contracts-pinned seam + golden fixtures (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — No memory retrieval namespace wall (AD-7 owner_scope), no per-card ordering, no read-your-writes path.

Tier: ingest→retrieval (`internal/sources/memory`, Turbopuffer namespace). Engine analogue: none. Seams: memory.* events (api SoR), Turbopuffer, contracts golden fixtures. Sibling: E10-S1/S2, E3-S2 (RRF), api (SoR for RYW).

## 🧪 Testing
DoD `TestMemoryNamespaceWallOwnerScopeAndReadYourWrites`. Tiers: integration. CS §5: mock the api SoR + memory.* publisher; never mock own sources/store. AD-7 namespace-per-user proven at scale (Turbopuffer 250M+ ns).

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security + consistency lenses (namespace wall is the stronger grade; RYW promise); Memory isolation GRADE was OPEN — this story lands the namespace-wall decision (AD-7 resolves it); cell-lint rule 10.

## 🔗 References
PRD `dCbFzRQGDr` Memory fold-in (AD-7/AD-11, PRD `g9vWbSYplh`/spine `iiCcKhGptV`/map `vBvVDFklZo`); Architecture `azRwTCZMqw` AD-7/AD-11.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: Memory card schema + compose port + MCP memory additions + memory.* golden fixtures).
- [ ] Parent epic: E10. Depends on E10-S1/S2, E2-S3 (namespace), orvex-studio-api (SoR).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
