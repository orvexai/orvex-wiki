## 🎯 Story
As an admin I want a reindex API to enqueue tenant/space rebuilds, inspect indexing lag and DLQ depth, and trigger extraction retries so that operators can heal the index without shell access.

**Definition of Done:** `TestAdminReindexEnqueueInspectAndRetry` (integration, `/internal/v1/reindex`) — an authorized admin enqueues a tenant/space rebuild (reusing the E8 step-APIs), reads indexing lag + DLQ depth, and triggers an extraction retry; all under cell-admin authorization with the target tenant validated. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `POST /internal/v1/reindex`, When called by a cell-admin, Then a tenant/space rebuild is enqueued (delegating to the E8 rebuild step-APIs). *Assert: rebuild enqueued for the target.* [Source: FR-K25]
- [ ] **AC2** — Given the admin surface, When queried, Then it reports indexing lag and DLQ depth. *Assert: lag + DLQ metrics returned.* [Source: FR-K25]
- [ ] **AC3** — Given an extraction-failure state, When retried via the admin API, Then the extraction re-runs. *Assert: retry re-enqueues the failed doc.* [Source: FR-K25]
- [ ] **AC4** (authz) — Given the admin op, When invoked, Then it runs under explicit cell-admin authorization with the target tenant validated (likeliest raw-query path per A2b) + `Idempotency-Key`. *Assert: non-admin rejected; tenant validated; missing key → 4xx.* [Source: FR-K25, NFR-K8]
- [ ] **AC5** (audit) — Given admin operations, When performed, Then they are audited to the family audit stream. *Assert: audit record emitted.* [Source: FR-K25, NFR-K8]

## 🔨 Tasks
- [ ] RED: reindex enqueue delegating to step-APIs (AC1). (AC: 1)
- [ ] GREEN: lag + DLQ inspection (AC2). (AC: 2)
- [ ] GREEN: extraction retry trigger (AC3). (AC: 3)
- [ ] RED→GREEN: cell-admin authz + tenant validation + Idempotency-Key (AC4). (AC: 4)
- [ ] GREEN: admin audit (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Rebuild step-APIs + ReextractAttachment/RebuildIndexBatch (extraction retry) present; but no admin API to enqueue/inspect indexing lag or DLQ (no /admin/reindex route, no lag/DLQ inspection endpoint).

Tier: internal admin step-API (`internal/admin`, delegates to `internal/rebuild`). Engine analogue: relocates engine bulk-reembed/reconciler. Seams: E8 rebuild step-APIs, obs, audit. Sibling: E8-S1, E9-S3.

## 🧪 Testing
DoD `TestAdminReindexEnqueueInspectAndRetry`. Tiers: integration. CS §5: mock auth + audit stream; never mock own rebuild/store.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch operability + security lenses (admin-authorized, audited, tenant-validated); cell-lint rule 11.

## 🔗 References
PRD `dCbFzRQGDr` FR-K25; Architecture `azRwTCZMqw` §3/A2b/A10.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: reindex admin OpenAPI).
- [ ] Parent epic: E9. Depends on E8-S1, E1-S7 (DLQ), E4-S3 (auth).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
