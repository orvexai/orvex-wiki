## 🎯 Story
As long-horizon DR I want an event-archive sink (S3 object stream + Postgres archive append table) preserving the spine's CloudEvents so that a cold rebuild or a late-joining corpus can replay beyond Kafka retention.

**Definition of Done:** `TestEventArchiveSinkReplayBeyondKafkaRetention` (integration, `internal/archive`) — spine CloudEvents are durably written to S3 + a Postgres archive append table, and a delta re-pull for a walk window whose events have aged out of Kafka is served from the archive sink. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given spine CloudEvents, When ingested, Then they are preserved in an S3 object stream + a Postgres archive append table (range-partitioned, RLS-covered). *Assert: event durably archived in both.* [Source: NFR-K4]
- [ ] **AC2** — Given a delta re-pull whose window exceeds Kafka retention, When rebuilding, Then it is served from the event-archive sink (not live topic retention). *Assert: aged-out window replayed from archive.* [Source: NFR-K4]
- [ ] **AC3** — Given the three replay sources (spine retention ≥ 24 h + slack, archive sink, or engine `updated-since`), When RebuildCell ships, Then at least one exists — the archive sink is the durable default. *Assert: archive-sink replay path exercised.* [Source: NFR-K4]
- [ ] **AC4** (tenancy) — Given the archive append table, When read, Then it carries the tenant column + RLS like every other journal. *Assert: cross-tenant archive read = 0.* [Source: NFR-K4, NFR-K3]
- [ ] **AC5** (retention) — Given archive growth, When managed, Then S3 lifecycle + partition retention (DROP PARTITION) bound cost. *Assert: retention policy present.* [Source: NFR-K4, NFR-K6]

## 🔨 Tasks
- [ ] RED: archive write to S3 + Postgres append table (AC1). (AC: 1)
- [ ] GREEN: replay-from-archive for aged-out window (AC2, AC3). (AC: 2,3)
- [ ] GREEN: RLS on archive table (AC4). (AC: 4)
- [ ] RED→GREEN: retention/lifecycle (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — internal/event/event.go ArchiveSink.Append returns 'not implemented'; store.AppendEventArchive returns 'not implemented'; no event_archive migration.

Tier: durability sink (`internal/archive`, S3 + Postgres append table). Engine analogue: none (replaces the struck Mongo event store, D-S12). Seams: Kafka spine, S3, Postgres. Sibling: E8-S1/S2 (delta re-pull consumer), E2-S1 (journal tables).

## 🧪 Testing
DoD `TestEventArchiveSinkReplayBeyondKafkaRetention`. Tiers: integration. CS §5: mock S3 + Kafka in unit; never mock own archive/store.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch reliability lens (replay beyond retention; Mongo struck); cell-lint rule 10 (S3 archive in store inventory).

## 🔗 References
PRD `dCbFzRQGDr` §1 (event-archive sink, D-S12)/NFR-K4; Architecture `azRwTCZMqw` A2/A8.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: CloudEvent archive format).
- [ ] Parent epic: E8. Depends on E2-S1, E1-S1 (spine events).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
