## 🎯 Story
As the CLI cache I want the delta download to return explicit page-level ACL tombstones as hard-DELETE records on permission loss so that a page the principal lost access to is removed from the cache/FTS/disk, not left lingering as "trashed".

**Definition of Done:** `TestPageLevelACLTombstoneHardDelete` (integration, `internal/projection`) — on a page-level permission loss the delta returns an explicit per-page tombstone typed as a hard-DELETE (not a soft `trashed_at`), and a client applying it removes the page entirely; the surface is in the NFR-K3 intra-tenant zero-byte probe scope. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a principal loses page-level access, When the next delta is fetched, Then it returns an explicit per-page tombstone record. *Assert: tombstone present for the lost page.* [Source: FR-K20b]
- [ ] **AC2** (hard-DELETE) — Given the tombstone, When specified, Then it is a hard-DELETE (like a purge), NOT a soft `trashed_at`. *Assert: tombstone type = hard-delete; a client removes the page from cache/FTS/disk.* [Source: FR-K20b]
- [ ] **AC3** (defect closed) — Given the old behaviour (page lingers as "trashed"), When the new path runs, Then the data-exposure + storage defect cannot occur. *Assert: no residual trashed row for a lost-access page.* [Source: FR-K20b]
- [ ] **AC4** (probe scope) — Given the projection-download surface, When probed, Then page-level tombstones are in the NFR-K3 intra-tenant restricted zero-byte probe. *Assert: lost-access page → 0 bytes post-sync.* [Source: FR-K20b, NFR-K3]
- [ ] **AC5** (security defense) — Given client cursors, When cleanup is needed, Then the server tracks per-principal revocation watermarks and forces full-resync when a cursor predates a revocation — never trusting client cursors for security cleanup. *Assert: pre-revocation cursor → full-resync.* [Source: FR-K20b]

## 🔨 Tasks
- [ ] RED: per-page tombstone emitted on permission loss (AC1). (AC: 1)
- [ ] GREEN: hard-DELETE typing (not soft trashed) (AC2, AC3). (AC: 2,3)
- [ ] GREEN: include in NFR-K3 probe scope (AC4). (AC: 4)
- [ ] RED→GREEN: revocation-watermark full-resync (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — No ACL-tombstone/delta path; store.Delta unimplemented. internal/corpus ReconcileDelete hard-deletes on delete EVENTS only, not permission-loss delta tombstones.

Tier: request-serving (`internal/projection`). Engine analogue: none (NEW capability — today only whole-space pruning + full-crawl set-difference exists). Seams: permission events (E1-S6), store. Sibling: E7-S1, E4-S4 (probe), E9-S1 (purge semantics analogue).

## 🧪 Testing
DoD `TestPageLevelACLTombstoneHardDelete`. Tiers: integration. CS §5: mock permission-event source; never mock own projection.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security lens (hard-DELETE not soft; never trust client cursors for cleanup); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K20b; Architecture `azRwTCZMqw` §5 (client-cursor tombstone suppression).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: tombstone hard-DELETE record spec).
- [ ] Parent epic: E7. Depends on E7-S1, E1-S6.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
