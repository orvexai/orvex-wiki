## 🎯 Story
As the CLI cache I want the projection download to carry the full field set — content_pm (per the OQ6 parity ruling), full lifecycle metadata, verification stamps, presentational fields, and byte-stable markdown — so that offline reads, `page list --status`, supersession resolution, and content-hash skip do not regress.

**Definition of Done:** `TestProjectionFieldSetAndContentPmParityRuling` (integration, `internal/projection`) — the download carries the pinned field set, honors the pack's OQ6 content_pm ruling (either PM-JSON included OR markdown-only degrade with PM-JSON explicitly excluded from FR-K22 parity), and returns byte-stable markdown so the client-side `content_hash` skip holds. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** (content_pm — OQ6) — Given the pack's OQ6 ruling, When downloading, Then EITHER raw ProseMirror `content_pm` is included in the delta OR a markdown-only degrade is applied with PM-JSON explicitly excluded from FR-K22 parity. *Assert: behaviour matches the pinned ruling; if excluded, `page get --prosemirror` degrade is documented, not silently broken.* [Source: FR-K20a]
- [ ] **AC2** — Given lifecycle needs, When downloaded, Then `status`, `doc_type`, `supersedes[]`, `superseded_by`, `redirect_from[]`, `owner`, `last_reviewed_at` are present. *Assert: `page list --status` + supersession resolution work offline.* [Source: FR-K20a]
- [ ] **AC3** — Given verification, When downloaded, Then engine-stamped `verified_against`, `verified_at`, `spec_confirmed` are carried through the projection (never computed here). *Assert: stamps pass through unmodified.* [Source: FR-K20a]
- [ ] **AC4** — Given presentational fields, When downloaded, Then `icon`, `cover_url` are present. *Assert: presentational fields returned.* [Source: FR-K20a]
- [ ] **AC5** (hash) — Given byte-stable markdown, When downloaded, Then the client can compute a stable `content_hash` over markdown (projection need not supply the hash but MUST return byte-stable markdown). *Assert: repeated download yields byte-identical markdown; hash-skip holds.* [Source: FR-K20a]

## 🔨 Tasks
- [ ] RED: field-set presence test (lifecycle/verification/presentational) (AC2, AC3, AC4). (AC: 2,3,4)
- [ ] GREEN: content_pm behaviour branch per pinned OQ6 ruling (AC1). (AC: 1)
- [ ] RED→GREEN: byte-stable markdown for client hash (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — No projection delta field set; store.Delta unimplemented. (internal/onboard page.md gives DfM markdown but that is a distinct llms.txt surface, not the CLI projection field set.)

Tier: request-serving (`internal/projection`). Engine analogue: fields exist today only in engine responses. Seams: store, engine (for content_pm if the ruling includes it). Sibling: E7-S1 (download), E7-S4 (parity), E1-S4 (text_repr/markdown source).

## 🧪 Testing
DoD `TestProjectionFieldSetAndContentPmParityRuling`. Tiers: integration. CS §5: mock engine (if content_pm sourced live); never mock own projection. OQ6 is pinned by the pack — do NOT invent the ruling; branch on the pinned decision.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch honesty lens (parity claim must be exact, no silent degrade); OQ6 gate before CUTOVER; cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K20a + OQ6; Architecture `azRwTCZMqw` §5 (content_pm parity risk).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: OQ6 content_pm ruling + field set schema — the single biggest FR-K22 parity decision).
- [ ] Parent epic: E7. Depends on E7-S1.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
