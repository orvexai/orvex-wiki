## 🎯 Story
As the cell I want query-latency budgets plus per-principal/tenant QoS fairness limits so that UI search meets p95 < 300 ms (Free-tier) and no one tenant's agent traffic can degrade the cell.

**Definition of Done:** `TestQueryLatencyBudgetAndFairnessQoS` (integration, `internal/search`+`internal/limiter`) — UI search meets the seeded-corpus latency budget (NFR-K11 code-level ≤ 50 ms; product ceiling p95 < 300 ms), and per-principal/tenant token buckets + QoS classes (interactive > CLI sync > bundles/rebuilds) shed/degrade one tenant's burst without starving interactive traffic. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the LOCKED Free-tier corpus (200 pages/tenant), When UI search runs, Then p95 < 300 ms in-cell (NFR-K11); the code-level seeded budget is ≤ 50 ms. *Assert: seeded query < 50 ms; product ceiling asserted with pending-ratify annotation.* [Source: FR-K14, NFR-K11]
- [ ] **AC2** — Given ask/chat retrieval, When run, Then the retrieval sub-call p95 < 500 ms. *Assert: retrieve budget met.* [Source: FR-K14, NFR-K11]
- [ ] **AC3** (fairness) — Given per-principal/tenant token buckets (Redis), When one tenant bursts, Then QoS classes (interactive > CLI sync > bundles/rebuilds) protect interactive traffic. *Assert: interactive p95 unaffected by a bulk burst.* [Source: NFR-K10]
- [ ] **AC4** — Given knowledge does NOT enforce quota, When sizing, Then it only sizes its index/projection against the caps; billing owns entitlement values (SoR), engine enforces at F-QUOTA write. *Assert: no quota enforcement in the query path.* [Source: FR-K14]
- [ ] **AC5** (negative) — Given a rate-limit breach, When exceeded, Then the response is a typed 429 with `Retry-After`, never a silent drop. *Assert: 429 + Retry-After.* [Source: NFR-K10]

## 🔨 Tasks
- [ ] RED: seeded-corpus latency assertion (≤ 50 ms) + pending-ratify product ceiling (AC1, AC2). (AC: 1,2)
- [ ] GREEN: per-principal/tenant token buckets + QoS classes (AC3). (AC: 3)
- [ ] GREEN: no-quota-enforcement guard (AC4). (AC: 4)
- [ ] RED→GREEN: 429 + Retry-After on breach (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Query-latency budget probe real (internal/metrics + workflow/nfr_latency_probe_test.go NFR-K11); per-principal/tenant QoS fairness/rate limits absent.

Tier: request-serving fairness (`cmd/query`, `internal/limiter` Redis buckets). Engine analogue: none. Seams: Redis buckets, billing SoR (values only, not enforced here). Sibling: E3-S1 (query), E4-S2 (bulkhead governs downstream engine; A9 governs own compute).

## 🧪 Testing
DoD `TestQueryLatencyBudgetAndFairnessQoS`. Tiers: unit (limiter) + integration (latency/QoS). CS §5: mock Redis in unit; never mock own search. Assert NFR-K11 budgets with pending-ratify annotations (draft amendment).

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch performance + reliability lenses; NFR-K11/K13 are DRAFT (ENG-1933) — assert with pending-ratify notes, never invent numbers; cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K14/NFR-K10/NFR-K11 (draft); Architecture `azRwTCZMqw` A9.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: NFR budgets pinned at ratify; billing entitlement values).
- [ ] Parent epic: E3. Depends on E3-S1.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
