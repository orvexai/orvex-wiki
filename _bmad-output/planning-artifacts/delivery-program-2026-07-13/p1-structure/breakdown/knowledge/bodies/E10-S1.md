## 🎯 Story
As the one search service (Decision 4) I want to index the Studio source events `studio.skill.*`, `memory.*`, `chat.*` as their own corpora under the multi-source model, each with per-corpus ready-made chunks and its own embedding model, so that Studio skills/Memory/chat retire their standalone search stacks into knowledge.

**Definition of Done:** `TestStudioCorporaIngestionPerCorpusChunksAndModels` (integration, `internal/sources`) — knowledge subscribes to `studio.skill.*`/`memory.*`/`chat.*` on the studio-spine broker, indexes each as `source_type ∈ {studio-skill, memory, chat-message}` under the FR-K7 model, consumes publisher-emitted ready-made chunks (D-R9-1) and each corpus's registered embedding model (D-R9-2), and RRF fuses across corpora by rank. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the studio-spine broker, When Studio source events arrive, Then knowledge subscribes via Knative Triggers to `studio.skill.*`, `memory.*`, `chat.*` and indexes each as its own corpus. *Assert: each event indexed under its source_type.* [Source: FR-K31b]
- [ ] **AC2** — Given the multi-source model, When ingesting, Then `source_type ∈ {studio-skill, memory, chat-message}` is registered via the FR-K12a source-adapter contract with no schema break. *Assert: new corpora need no documents-table migration.* [Source: FR-K31b]
- [ ] **AC3** (D-R9-1) — Given Studio publishers, When ingesting, Then knowledge consumes their ready-made chunks (studio-api/ai chunk before publishing) — knowledge does NOT chunk them (it chunks only the wiki corpus). *Assert: no knowledge-side chunking of Studio corpora.* [Source: FR-K31b]
- [ ] **AC4** (D-R9-2) — Given per-content-type embeddings, When indexing, Then each corpus pins its own embedding model+version+dimension in the corpus registry; cross-corpus fusion is by RANK (RRF), never raw score. *Assert: per-corpus model used; RRF-by-rank fusion.* [Source: FR-K31b]
- [ ] **AC5** — Given purge (FR-K27) + staleness (FR-K28), When a Studio corpus doc is deleted/stale, Then the invariants extend to every corpus unchanged. *Assert: memory/chat/skill purge + staleness parity.* [Source: FR-K31b]

## 🔨 Tasks
- [ ] RED: subscribe + index each Studio corpus by source_type (AC1, AC2). (AC: 1,2)
- [ ] GREEN: consume ready-made chunks, no knowledge-side chunking (AC3). (AC: 3)
- [ ] GREEN: per-corpus embedding model + RRF-by-rank fusion (AC4). (AC: 4)
- [ ] GREEN: purge/staleness parity for Studio corpora (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — gen types + event prefixes exist (SourceStudioSkill/SourceMemory/SourceChatMessage, TypeStudioSkillPrefix etc.) but no studio/memory/chat source-adapter package; workflow.Ingest leaves studio.* as ErrNotImplemented.

Tier: ingest (`cmd/indexer`, `internal/sources/{skill,memory,chat}`). Engine analogue: none. Seams: studio-spine (publishers = orvex-studio-api/ai), corpus registry (E2-S4), store. Sibling: E10-S2 (ACL), E3-S2 (RRF), E1-S4 (wiki chunking contrast).

## 🧪 Testing
DoD `TestStudioCorporaIngestionPerCorpusChunksAndModels`. Tiers: integration. CS §5: mock studio-spine publishers; never mock own sources/store. R-9 D-R9-1/2 are BINDING.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch seam lens (multi-source, no schema break); R-9 `0mFImku9Gp`; cell-lint rule 6.

## 🔗 References
PRD `dCbFzRQGDr` FR-K31b (R-9); Architecture `azRwTCZMqw` A7/A12; R-9 `0mFImku9Gp`.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: studio.skill.*/memory.*/chat.* CloudEvents + source-adapter entries).
- [ ] Parent epic: E10. Depends on E2-S4 (registry), E1 (ingest), orvex-studio-api/ai publishers live.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
