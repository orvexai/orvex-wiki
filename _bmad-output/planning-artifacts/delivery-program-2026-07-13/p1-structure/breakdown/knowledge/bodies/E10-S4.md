## 🎯 Story
As the quality owner I want an offline retrieval eval harness reporting recall@10 and answer-correctness against a committed baseline, a regression gate that blocks merges, and the raw wiki store kept searchable alongside distilled Memory so that Orvex can stand behind a quality number.

**Definition of Done:** `TestRetrievalEvalHarnessRecallAndRegressionGate` (integration, `internal/eval`) — the harness measures memory retrieval on representative task sets, reports recall@10 (baseline ≥ 0.80 [ASSUMPTION]) and answer-correctness, a retrieval change that drops recall below baseline fails the CI regression gate, and the raw kept-content (wiki) store stays searchable alongside distilled Memory. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** (FR-E1) — Given representative task sets, When the offline harness runs, Then it reports recall@10 and answer-correctness with a committed baseline (recall@10 ≥ 0.80 [ASSUMPTION]). *Assert: harness emits recall@10 + correctness vs baseline.* [Source: FR-E1, AD-9]
- [ ] **AC2** (FR-E3) — Given a retrieval change, When it regresses recall below the committed baseline, Then the CI regression gate blocks the merge. *Assert: below-baseline change → gate fails.* [Source: FR-E3, AD-9]
- [ ] **AC3** (FR-E4) — Given distilled Memory, When retrieving, Then the raw kept-content (wiki) store remains searchable alongside it (verbatim-beats-extracted ablation honored). *Assert: raw wiki retrievable in the same query path.* [Source: FR-E4]
- [ ] **AC4** (FR-E2) — Given external credibility, When tracked, Then a public-comparable benchmark (LongMemEval / LoCoMo-style [ASSUMPTION, selection in OQ4]) is measured. *Assert: a public benchmark harness runs (selection TBD — pinned by the Definition Pack / OQ4).* [Source: FR-E2]
- [ ] **AC5** (ownership boundary) — Given the eval split (AD-9), When gating, Then knowledge owns the RETRIEVAL eval (recall/correctness); ai owns the proposal-quality eval — knowledge does not evaluate proposal precision. *Assert: harness scope = retrieval only.* [Source: AD-9]

## 🔨 Tasks
- [ ] RED: recall@10 + answer-correctness harness vs baseline (AC1). (AC: 1)
- [ ] RED→GREEN: CI regression gate (AC2). (AC: 2)
- [ ] GREEN: raw-wiki-searchable-alongside-Memory (AC3). (AC: 3)
- [ ] GREEN: public benchmark harness (selection TBD — pack) (AC4). (AC: 4)
- [ ] GREEN: retrieval-only scope guard (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — No retrieval eval harness -- no recall@10 metric, regression gate, raw-store-searchable assertion, or public benchmark anywhere in the tree/tests.

Tier: quality harness (`internal/eval`, offline + CI gate). Engine analogue: none. Seams: query API (system under eval), golden task sets. Sibling: E3 (query under eval), E10-S1/S3 (memory corpus), OQ5 eval-set ownership (seed from real queries, purge scope applies).

## 🧪 Testing
DoD `TestRetrievalEvalHarnessRecallAndRegressionGate`. Tiers: integration + CI gate. CS §5: exercise the real query path (do not mock the system under eval); benchmark selection is [ASSUMPTION] — pinned by the pack (OQ4), never invented.

## 📏 Guidance
CS §§3/4/5/10/11; SE-Arch honesty lens (a number Orvex can stand behind; no fabricated recall); AD-9 owned decision; recall ≥ 0.80 is [ASSUMPTION] — annotate pending-ratify; cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` Memory fold-in FR-E1..E4 + AD-9 (PRD `g9vWbSYplh`); Architecture `azRwTCZMqw` AD-9.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: eval task-set fixtures + benchmark selection OQ4 + recall baseline ratify).
- [ ] Parent epic: E10. Depends on E3-S1/S2, E10-S1/S3.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
