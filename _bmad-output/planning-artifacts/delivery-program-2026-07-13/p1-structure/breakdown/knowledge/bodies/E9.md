## 📖 Epic — Purge, operations & cell-contract conformance

The operational spine: **purge** (FR-K27 — on `wiki.workspace.deleted`/`user.deleted`/`space.deleted`, delete every copy knowledge holds: projection rows, Turbopuffer namespace, chunk stores, embeddings, S3 bundles, SSE delivery logs, eval-set entries — verified by a zero-residual probe), the **admin reindex API** (FR-K25), the **liveness/readiness split** + Prometheus/OTel observability + residency (FR-K26, NFR-K9, A10), and **day-1 cell-contract conformance** (§6: cluster-local peer DNS rule 1, cell-guard/421 rule 3, healthz echoes CELL_ID+CLUSTER_NAME rule 4, orvexcell fail-closed rule 6, tenant-move typed step-API rule 10, Idempotency-Key rule 11, no-KEDA rule 13).

Why it exists: purge closes the historical residual-data class (D-SYNC-1 gate) and is a legal/tenancy invariant. The liveness-vs-readiness split (O2) ensures a dead consumer never reads green. The tenant-move typed contract (rule 10) is **non-retrofittable** — knowledge is among the most per-tenant-stateful services in the family (namespace, RLS rows, tenant-keyed cache, bundles, cursors/epochs), so the typed quiesce/export/import/activate shape + `TENANT_MOVE.md` store inventory must be day-1.

**Story roster**
- [ ] E9-S1 — Purge FR-K27: delete every copy on workspace/user/space delete + zero-residual probe
- [ ] E9-S2 — Admin reindex API: enqueue rebuilds, inspect indexing lag & DLQ, trigger extraction retries
- [ ] E9-S3 — Liveness/readiness split per binary + Prometheus/OTel observability + residency
- [ ] E9-S4 — Cell-contract conformance: cluster-local DNS, cell-guard/421, healthz echoes, Idempotency-Key, tenant-move step-API

**FR ids owned:** FR-K25, FR-K26, FR-K27; NFR-K8, NFR-K9.

## ✅ Definition of Done
- [ ] All four stories Done.
- [ ] Family-E2E share green: a workspace-delete purge leaves zero residual across all stores (probe green), cell-lint (JGAUQRsw2g §5) passes for rules 1/3/4/5/6/10/11/13, and a dead-dependency pod reads NOT-ready while liveness stays 200.
- [ ] SDD lines evidenced: zero-residual purge probe (FR-K27), tenant-move typed contract + TENANT_MOVE.md coverage-checked, Idempotency-Key on all /internal/* (rule 11), admin operations audited to family audit stream (NFR-K8).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: purge event set + tenant-move step-API typed contract + admin reindex OpenAPI + cell-lint spec).
- [ ] Depends on E1 (purge events), E2 (all stores), E8 (reindex reuses rebuild step-APIs).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
