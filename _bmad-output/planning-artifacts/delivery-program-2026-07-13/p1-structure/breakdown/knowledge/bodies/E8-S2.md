## 🎯 Story
As DR I want projection epochs that force delta clients to full-resync after a rewrite, plus a resumable checkpointed rebuild that meets the RTO budgets, so that index loss is an availability event, never data loss.

**Definition of Done:** `TestRebuildMeetsRTOBudget` (integration, `internal/workflow`) — a rebuild bumps the tenant's projection epoch (delta clients full-resync), is resumable via per-tenant checkpoints, meets tenant < 15 min RTO (seeded-corpus budget with headroom), and event-loss = 0 across crash-inject + replay. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a rebuild rewrites projection rows, When it completes, Then it bumps the tenant's projection epoch and FR-K20 delta clients full-resync (bounded by NFR-K2 ≥ 20 pages/s). *Assert: epoch bump → client full-resync.* [Source: FR-K24]
- [ ] **AC2** (RTO) — Given the RTO budgets, When rebuilding, Then a single tenant re-indexes < 15 min and a full cell < 24 h (the sizing input). *Assert: seeded budget honored (`TestRebuildMeetsRTOBudget` < 5 s local vs the RTO ceiling).* [Source: FR-K24, NFR-K4]
- [ ] **AC3** (resumable) — Given a mid-rebuild failure, When resumed, Then it continues from the per-tenant checkpoint (measured + resumable). *Assert: resume does not restart from zero.* [Source: FR-K24, NFR-K4]
- [ ] **AC4** (no-KEDA scaling) — Given scaling, When rebuilding, Then the central worker is a fixed-replica Deployment and knowledge's step-APIs autoscale via KPA — never KEDA task-queue-depth (A-W6/C19). *Assert: no KEDA; KPA on step-APIs.* [Source: FR-K24]
- [ ] **AC5** (durability) — Given crash-inject + replay, When rebuilding, Then event-loss = 0 and index loss is availability-only, never data loss. *Assert: zero event loss across crash-inject.* [Source: NFR-K4]

## 🔨 Tasks
- [ ] RED: epoch bump → full-resync (AC1). (AC: 1)
- [ ] GREEN: RTO-budgeted rebuild (AC2). (AC: 2)
- [ ] RED→GREEN: checkpointed resume (AC3). (AC: 3)
- [ ] GREEN: no-KEDA/KPA scaling posture (AC4). (AC: 4)
- [ ] RED→GREEN: crash-inject event-loss = 0 (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Resumable rebuild (walk offset cursor) + delta-repull + RTO-budget assertion (NFRK4RebuildRTOBudget, rebuild_test.go) present; projection epochs + delta full-resync (store.Delta/cursors) absent.

Tier: rebuild orchestration surface (`internal/workflow`, step-APIs). Engine analogue: none. Seams: store, workflows. Sibling: E8-S1 (steps), E7-S1 (epoch cursor consumer), E8-S3 (replay source).

## 🧪 Testing
DoD `TestRebuildMeetsRTOBudget`. Tiers: integration (seeded corpus). CS §5: mock workflows caller; never mock own store. RTO is a measured budget with documented headroom (NFR-K4 pattern).

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch reliability lens (RTO, resumable, zero-loss); no-KEDA cell rule 13; cell-lint rule 13.

## 🔗 References
PRD `dCbFzRQGDr` FR-K24/NFR-K4; Architecture `azRwTCZMqw` A8 (C3-resolved).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: RTO budgets pinned; step-API contract).
- [ ] Parent epic: E8. Depends on E8-S1, E8-S3.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
