## 🎯 Story
As an agent/user I want an always-fresh tar/zip bundle builder with a pre-stream non-erasable egress audit and live fail-closed export-policy evaluation so that bulk export is ACL-first, audited, and never a confused deputy.

**Definition of Done:** `TestBundleBuilderACLFirstAuditedNoConfusedDeputy` (integration, `cmd/bundler`) — a bundle is built from the projection with `userId` derived from the verified principal only (never a request param), each item passes a live push-invalidated export-policy/export-space evaluation, and a pre-stream non-erasable audit row is written before any bytes stream. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `POST /v1/bundles`, When built, Then `userId` is derived from the verified principal ONLY — never a request parameter (no confused deputy). *Assert: a request-supplied userId is ignored/rejected.* [Source: FR-K18]
- [ ] **AC2** (audit) — Given a build, When streaming, Then a non-erasable egress audit row (`bundle_audit`, FR-35 spine) is written PRE-stream. *Assert: audit exists before first byte; append-only.* [Source: FR-K18, NFR-K8]
- [ ] **AC3** (ACL) — Given export policy, When evaluating, Then a live fail-closed export-policy / export-space evaluation via an engine primitive (push-invalidated) gates each item; `ignorePermissions` is forbidden. *Assert: a page the principal cannot export is absent; ignorePermissions rejected.* [Source: FR-K18]
- [ ] **AC4** (fairness) — Given per-principal throttles, blast-radius caps, and partial-result counts, When a build is large, Then throttles/caps apply and partial-result counts are reported; bundle ACL load draws through the A1a bulk bulkhead. *Assert: caps enforced; bulkhead used, not interactive budget.* [Source: FR-K18, NFR-K10]
- [ ] **AC5** (negative) — Given a cross-tenant target, When requested, Then the build is hard tenant-scoped and refuses. *Assert: no cross-tenant bytes in bundle.* [Source: FR-K18, NFR-K3]

## 🔨 Tasks
- [ ] RED: principal-derived userId, reject request param (AC1). (AC: 1)
- [ ] GREEN: pre-stream non-erasable audit (AC2). (AC: 2)
- [ ] RED→GREEN: live export-policy eval, ignorePermissions forbidden (AC3). (AC: 3)
- [ ] GREEN: throttles/caps/partial counts + bulkhead (AC4). (AC: 4)
- [ ] RED→GREEN: hard tenant-scope (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — server.createBundle is a notImplemented 501 stub; no bundle_registry/bundle_audit migration and no egress-audit / export-policy code.

Tier: on-demand build (`cmd/bundler` planned Deployment). Engine analogue: relocates engine FR-19 + FR-35 audit. Seams: engine export-policy primitive (mock), S3, projection. Sibling: E6-S2 (owner binding), E4-S2 (bulkhead).

## 🧪 Testing
DoD `TestBundleBuilderACLFirstAuditedNoConfusedDeputy`. Tiers: integration. CS §5: mock engine export-policy + S3; never mock own bundler/store.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security lens (confused-deputy, non-erasable audit, fail-closed); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K18; Architecture `azRwTCZMqw` A-bundle/A1a.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: /v1/bundles OpenAPI + FR-35 audit shape + engine export-policy primitive).
- [ ] Parent epic: E6. Depends on E2-S1 (bundle_registry/audit), E4-S2.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
