## 🎯 Story
As the reliability owner I want FR-13 hot-path overload protection — single-flight coalescing, an interactive/bulk bulkhead, and an explicit engine-side rate contract — so that the shared engine ACL endpoint never becomes a self-inflicted single point of overload, without weakening A1.

**Definition of Done:** `TestFR13SingleFlightBulkheadAndRateShed` (integration, `internal/acl`) — identical in-flight ACL batches for the same principal+candidate-set coalesce to one engine call (holding no decision past return), bulk ACL load (bundles/rebuilds/delta) runs through a separate bounded pool isolated from interactive search, and exceeding the engine's published rate budget sheds/degrades (partial + retry-after) rather than saturating. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given identical in-flight ACL batches (same principal + candidate-ID set), When concurrent, Then they coalesce to one engine call and the result fans out to waiters. *Assert: N concurrent → 1 engine call.* [Source: FR-K12]
- [ ] **AC2** (not a cache) — Given single-flight, When the call returns, Then no decision is held past the call — A1's no-cached-allow invariant is intact. *Assert: next request re-calls the engine.* [Source: FR-K12]
- [ ] **AC3** (bulkhead) — Given bulk ACL load (FR-K18 bundles, A8 rebuild walks, FR-K20 delta re-pull), When running, Then it uses a separate bounded connection pool / concurrency limiter, isolated from the interactive engine-ACL budget. *Assert: a bundle/rebuild storm cannot starve interactive retrieval.* [Source: FR-K12, NFR-K5]
- [ ] **AC4** (rate contract) — Given the engine FR-13 per-cell rate/concurrency budget (published in contracts), When exceeded, Then knowledge sheds/degrades (partial + retry-after) rather than saturating the engine. *Assert: over-budget → degrade, not saturate.* [Source: FR-K12, NFR-K5]
- [ ] **AC5** (obs) — Given the controls, When running, Then coalescing hit-rate + bulk-bulkhead saturation are exported as SLIs (A10). *Assert: metrics present.* [Source: FR-K12]

## 🔨 Tasks
- [ ] RED: single-flight coalescing N→1 (AC1, AC2). (AC: 1,2)
- [ ] GREEN: interactive/bulk bulkhead pool (AC3). (AC: 3)
- [ ] RED→GREEN: rate-shed/degrade on budget breach (AC4). (AC: 4)
- [ ] GREEN: coalescing + saturation SLIs (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Only the aclBatchCeiling (500) batch bulkhead in clients.go + fail-closed FR-13; no single-flight, no interactive/bulk bulkhead split, no rate contract (grep for singleflight/bulkhead empty).

Tier: cross-cutting reliability (`internal/acl` limiter, downstream engine). Engine analogue: protects the engine FR-13 endpoint. Seams: engine FR-13 (mock). Sibling: E4-S1 (post-filter), E6/E7/E8 (bulk callers).

## 🧪 Testing
DoD `TestFR13SingleFlightBulkheadAndRateShed`. Tiers: unit (coalescing/bulkhead) + integration. CS §5: mock engine FR-13; never mock own acl/limiter.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch reliability lens (choke point must not be self-inflicted; single-flight is not a cache); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K12/NFR-K5; Architecture `azRwTCZMqw` A1a/A9/A10.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: engine FR-13 per-cell rate/concurrency budget).
- [ ] Parent epic: E4. Depends on E4-S1.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
