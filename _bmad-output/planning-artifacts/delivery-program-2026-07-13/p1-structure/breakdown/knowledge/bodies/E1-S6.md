## 🎯 Story
As the ACL layer I want permission-change events to re-scope affected documents' ACL metadata within the freshness SLO so that access changes propagate — and staleness can only over-narrow, never over-expose.

**Definition of Done:** `TestPermissionChangeReScopeOverNarrowsOnly` (integration, indexer + store) — a permission-change event re-scopes affected docs' projected ACL metadata within the freshness SLO, and any staleness window results in over-narrowing (excluded from results) rather than over-exposure. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a space-level permission-change event, When consumed, Then affected documents' projected ACL metadata (space membership, restriction flags) is re-scoped within the freshness SLO (NFR-K1). *Assert: restriction flag reflects the event within p95 < 5 s.* [Source: FR-K6]
- [ ] **AC2** (over-narrow invariant) — Given ACL staleness, When a query runs mid-propagation, Then the projected metadata acts as an over-narrowing pre-filter only — it can exclude, never admit; restriction-flagged pages always defer to the live engine decision. *Assert: no over-expose possible from stale metadata.* [Source: FR-K6, FR-K12]
- [ ] **AC3** — Given a page-level permission delta (named engine delta), When it ships, Then per-page ACL is re-scoped; until it ships, restriction re-scoping rides 60 s rebuild semantics and A1's live allow prevents over-expose. *Assert: interim path documented + live-allow test green.* [Source: FR-K6]
- [ ] **AC4** (purge overlap) — Given a permission loss that is effectively a removal, When consumed, Then downstream tombstone/removal derivatives are triggered (FR-K27 boundary). *Assert: derivative removal enqueued.* [Source: FR-K6, FR-K27]
- [ ] **AC5** (negative) — Given an out-of-order permission event, When applied, Then version/watermark ordering prevents an older grant from resurrecting a revoked scope. *Assert: stale grant no-op.* [Source: FR-K6, FR-K3]

## 🔨 Tasks
- [ ] RED: re-scope within SLO on space-level event (AC1). (AC: 1)
- [ ] GREEN: over-narrow pre-filter semantics; restriction flag defers to live engine (AC2). (AC: 2)
- [ ] RED→GREEN: page-level delta path + interim 60 s rebuild note (AC3). (AC: 3)
- [ ] RED→GREEN: removal-derivative trigger (AC4). (AC: 4)
- [ ] RED→GREEN: ordering guard against stale grant (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — No permission-change re-scope flow; workflow.Ingest handles content_updated/page_created/attachment_created/deletes only. acl_meta prefilter column (migration 004) is substrate but nothing re-scopes on a permission event.

Tier: ingest→store ACL metadata (`cmd/indexer`, `internal/store`). Engine analogue: consumes engine permission events. Seams: engine FR-13 (E4-S1 does live allow), store. Sibling: E4-S1 (live allow), E1-S7.

## 🧪 Testing
DoD `TestPermissionChangeReScopeOverNarrowsOnly`. Tiers: unit (over-narrow semantics) + integration (SLO propagation). CS §5: mock engine event source; never mock own ACL/store.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security lens (over-narrow invariant is load-bearing); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K6/K12; Architecture `azRwTCZMqw` A1 (projected metadata is over-narrowing pre-filter only).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: permission-change event set incl. page-level named delta).
- [ ] Parent epic: E1. Depends on E4-S1 (live allow), E1-S2.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
