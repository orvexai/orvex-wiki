## 🎯 Story
As the hybrid store I want a Turbopuffer adapter with a namespace per tenant so that keyword + vector search share one committed store with a tenant isolation wall.

**Definition of Done:** `TestTurbopufferAdapterHybridAndNamespaceWall` (integration, `internal/store/turbopuffer`) — hybrid keyword+vector queries return correctly-fused candidates from the tenant's namespace, and a query scoped to tenant A cannot read tenant B's namespace. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given Turbopuffer BYOC (committed, D-S9), When a document is upserted, Then it lands in a namespace keyed by tenant. *Assert: namespace = tenant identity.* [Source: FR-K9]
- [ ] **AC2** — Given hybrid mode, When queried, Then keyword and vector candidates are returned for RRF fusion (E3-S2). *Assert: both signal types present in candidate set.* [Source: FR-K9]
- [ ] **AC3** (isolation) — Given tenant A's query, When executed, Then only tenant A's namespace is read (namespace-per-tenant wall). *Assert: cross-namespace bytes = 0.* [Source: FR-K9, NFR-K3]
- [ ] **AC4** — Given the adapter, When run against the E2-S2 conformance suite, Then it passes identically to memindex. *Assert: conformance green.* [Source: FR-K9, NFR-K7]
- [ ] **AC5** (negative) — Given a Turbopuffer outage, When queried, Then the adapter surfaces a typed error (not a fabricated empty result) so the query path can degrade honestly. *Assert: error propagated, not swallowed.* [Source: FR-K9, NFR-K5]

## 🔨 Tasks
- [ ] RED: namespace-per-tenant upsert+read (AC1, AC3). (AC: 1,3)
- [ ] GREEN: hybrid query returning keyword+vector candidates (AC2). (AC: 2)
- [ ] GREEN: pass conformance suite (AC4). (AC: 4)
- [ ] RED→GREEN: typed-error on outage (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — turbopuffer.go Upsert + Query (BM25/ANN/hybrid RRF) + AliasSwap + per-tenant Namespace real & tested (client_http_test.go, conformance_test.go); DeleteNamespace/DeleteDocs stubbed (errNotWired).

Tier: persistence adapter (`internal/store/turbopuffer`, true-external Turbopuffer). Engine analogue: none. Seams: Turbopuffer (true-external, live in integration/staging). Sibling: E2-S2 (interface), E3 (query consumer).

## 🧪 Testing
DoD `TestTurbopufferAdapterHybridAndNamespaceWall`. Tiers: integration (real/staging Turbopuffer). CS §5: Turbopuffer is true-external (mock only in unit tiers); never mock own adapter package in the conformance run.

## 📏 Guidance
CS §§3/4/5/7/10/11; SE-Arch seam + honesty lenses; cell-lint rule 10 (namespace in tenant-move inventory).

## 🔗 References
PRD `dCbFzRQGDr` FR-K9 (D-S9); Architecture `azRwTCZMqw` A2/A3.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: adapter interface); OQ2 Turbopuffer procurement is commercial, not a build blocker.
- [ ] Parent epic: E2. Depends on E2-S2 (interface + conformance suite).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
