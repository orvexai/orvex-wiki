## 🎯 Story
As an operator I want a liveness/readiness split per binary plus Prometheus metrics + OTel traces and per-cell residency so that a dead consumer never reads green and every surface is observable in-region.

**Definition of Done:** `TestLivenessReadinessSplitPerBinary` (integration, all binaries) — `/healthz` is liveness-only always-200 echoing BOTH `CELL_ID` and `CLUSTER_NAME`, `/readyz` does real per-binary dependency round-trips (query→store+engine+identity JWKS; sse→Kafka; indexer→store+broker; bundler→store+S3), and a pod that cannot reach its dependencies reads NOT-ready while liveness stays green. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `GET /healthz`, When called, Then it is liveness-only, always 200, and echoes BOTH `CELL_ID` and `CLUSTER_NAME`. *Assert: 200 + both fields (cell rule 4).* [Source: FR-K26]
- [ ] **AC2** — Given `GET /readyz`, When called per binary, Then it does real dependency round-trips (query→store+engine FR-13+identity JWKS; sse→Kafka; indexer→store+broker; bundler→store+S3). *Assert: unreachable dep → NOT-ready.* [Source: FR-K26]
- [ ] **AC3** — Given a dead consumer, When it cannot reach dependencies, Then it reads NOT-ready (removed from the Service) while liveness stays green. *Assert: NOT-ready ≠ liveness failure.* [Source: FR-K26]
- [ ] **AC4** (obs) — Given the house pattern, When running, Then Prometheus metrics + OTel traces expose the A10 SLIs (indexing lag, query p95 by surface + cache-hit ratio, engine-ACL round-trip p95, isolation-probe results, DLQ depth, SSE connections, cache eviction lag, FR-13 coalescing hit-rate, bulk-bulkhead saturation, readiness-probe failures). *Assert: SLIs present.* [Source: FR-K26]
- [ ] **AC5** (residency) — Given per-cell deployment, When configured, Then LiteLLM is in-region and Kafka topics are region-local. *Assert: no cross-region egress on the hot path.* [Source: FR-K26, NFR-K9]

## 🔨 Tasks
- [ ] RED: /healthz always-200 + CELL_ID+CLUSTER_NAME (AC1). (AC: 1)
- [ ] GREEN: /readyz per-binary dependency round-trips (AC2, AC3). (AC: 2,3)
- [ ] GREEN: Prometheus/OTel SLIs (AC4). (AC: 4)
- [ ] GREEN: in-region LiteLLM + region-local topics (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Liveness/readiness split on all 3 binaries + cell echo real & tested (server healthz/readyz, cmd/indexer, cmd/sse/main_test.go); Prometheus scrape annotations in deploy/ but no /metrics endpoint or OTel export in code (internal/metrics is an in-process p95 recorder only).

Tier: cross-cutting operability (all binaries, otel-collector classify pattern). Engine analogue: house observability pattern. Seams: Prometheus/OTel, all deps. Sibling: E1-S7 (SLI feeders), E9-S4 (cell config).

## 🧪 Testing
DoD `TestLivenessReadinessSplitPerBinary`. Tiers: integration. CS §5: mock deps for readyz negative cases; never mock own health handlers.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch operability lens (liveness-vs-health, dead-consumer-never-green); cell-lint rule 4.

## 🔗 References
PRD `dCbFzRQGDr` FR-K26/NFR-K9; Architecture `azRwTCZMqw` §3 (O2)/A10.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: healthz/readyz contract + cell config keys).
- [ ] Parent epic: E9. Depends on E9-S4 (config), E1-S7 (SLIs).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
