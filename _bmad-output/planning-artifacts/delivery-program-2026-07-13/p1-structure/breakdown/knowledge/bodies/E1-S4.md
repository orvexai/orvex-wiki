## 🎯 Story
As the indexer I want to chunk the embed-resolved `text_repr` and embed each chunk via LiteLLM with a content-hash skip so that tables/diagrams/embeds are searchable and unchanged chunks are never re-embedded.

**Definition of Done:** `TestChunkAndEmbedContentHashSkip` (integration, `internal/search`/indexer) — heading-aware ~500-token/15%-overlap chunks are produced from `text_repr`, each chunk embedded via the per-corpus LiteLLM model, and a chunk whose `content_hash` is unchanged is skipped (no embed call). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `text_repr` (embed-resolved, FR-38), When chunked, Then heading-aware chunks (~500 tokens, 15% overlap) are produced and the jsonToText atom-drop failure class cannot occur (conversion is engine-side against golden fixtures). *Assert: table/diagram text present in chunk output.* [Source: FR-K8]
- [ ] **AC2** — Given the wiki corpus, When embedding, Then calls ride the ai-provisioned **sibling scoped LiteLLM key** on a **separate nested embedding budget** (sized from page quota), never drawing the user-action cap. *Assert: embedding call uses the sibling key; no user-action-cap decrement.* [Source: FR-K10]
- [ ] **AC3** — Given an unchanged chunk, When re-processed, Then content-hash skip is applied (mandatory, D-S15) and no embed call is made. *Assert: embed-call count = 0 for unchanged chunk.* [Source: FR-K10]
- [ ] **AC4** — Given per-corpus config, When embedding, Then the model+version+dimension pinned in the corpus registry is used (per-content-type, D-R9-2). *Assert: wiki corpus uses its registered model.* [Source: FR-K8, FR-K10]
- [ ] **AC5** (error path) — Given a LiteLLM embedding failure, When embedding, Then it retries with backoff → DLQ, sets `embedding_state`, and never writes a fabricated vector. *Assert: no zero/garbage vector persisted.* [Source: FR-K10, FR-K28]

## 🔨 Tasks
- [ ] RED: heading-aware chunker over text_repr with table/diagram fixture (AC1). (AC: 1)
- [ ] GREEN: LiteLLM embed client on sibling key + nested budget (AC2). (AC: 2)
- [ ] RED→GREEN: content-hash skip (AC3). (AC: 3)
- [ ] GREEN: per-corpus model lookup from registry (AC4). (AC: 4)
- [ ] RED→GREEN: embed-failure retry/DLQ + embedding_state, no fabricated vector (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Embeddings (clients.LiteLLM.Embed) + content-hash skip present & tested (workflow.go, clients/litellm_test.go); but no real text_repr chunker (whole-body 8000-rune snippet via workflow.snippetOf) and per-corpus model is a single pinned constant EmbeddingModel, not a per-corpus registry.

Tier: ingest compute (`cmd/indexer`, `internal/search`/chunker + `internal/clients` LiteLLM port). Engine analogue: replaces in-fork `page_embeddings` (deleted from engine). Seams: LiteLLM (true-external, mock in unit), corpus registry (E2-S4). Sibling: E1-S2 (upsert consumes chunks), E10 (Studio corpora emit their own chunks — knowledge chunks only wiki).

## 🧪 Testing
DoD `TestChunkAndEmbedContentHashSkip`. Tiers: unit (chunker + hash-skip) + integration (LiteLLM stub). CS §5: mock LiteLLM (true-external); never mock own chunker/store.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch honesty lens (no fabricated vectors) + efficiency (hash-skip mandatory); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K8/K10 (D-S15); Architecture `azRwTCZMqw` A7 (D-R9-1/2).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: golden text_repr fixtures + corpus-registry embedding-model entry).
- [ ] Parent epic: E1. Depends on E2-S4 (registry), orvex-studio-ai (sibling LiteLLM key provisioning).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
