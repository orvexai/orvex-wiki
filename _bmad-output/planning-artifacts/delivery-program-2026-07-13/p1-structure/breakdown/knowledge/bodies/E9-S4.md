## 🎯 Story
As the cell fabric I want day-1 cell-contract conformance — cluster-local peer DNS, cell-guard/421, healthz echoes, Idempotency-Key on /internal/*, and the typed tenant-move step-API — so that knowledge passes cell-lint and can be relocated between cells.

**Definition of Done:** `TestCellContractConformanceAndTenantMove` (integration, config + `/internal/v1/tenant-move/*`) — hot-path engine+identity calls resolve to cluster-local Service DNS, every route sits behind cell-guard returning 421 on `principal.cell != CELL_ID`, `/internal/*` require `Idempotency-Key`, and the typed `tenant-move/{quiesce,export,import,activate}` contract + `TENANT_MOVE.md` store inventory pass cell-lint coverage. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** (rule 1) — Given hot-path peer calls (engine FR-13, identity verify), When made, Then they resolve to cluster-local Service DNS, not `docs.…`/`auth.orvex.ai` public edge; config gains `DATABASE_URL`, identity JWKS URL, LiteLLM endpoint, `CLUSTER_NAME`. *Assert: no public-edge host on the hot path.* [Source: NFR-K8]
- [ ] **AC2** (rule 3) — Given any route, When `principal.cell != CELL_ID`, Then it returns 421; `solo` sentinel no-ops the guard for dev/standalone. *Assert: mismatch → 421; solo → pass-through.* [Source: NFR-K8]
- [ ] **AC3** (rule 11) — Given `/internal/*` step-APIs, When called, Then `Idempotency-Key` is mandatory (keyword-upsert, rebuild, reindex, tenant-move). *Assert: missing key → 4xx.* [Source: NFR-K8]
- [ ] **AC4** (rule 10 — tenant-move) — Given the typed step-API, When invoked, Then `quiesce/export/import/activate` behave per contract, and `TENANT_MOVE.md` store inventory (Postgres+Turbopuffer+S3+Redis-derived-NOT-exported+cursors) is coverage-checked by cell-lint. *Assert: Redis excluded from export; inventory coverage green.* [Source: NFR-K8]
- [ ] **AC5** (rules 5/6/13) — Given the cell rules, When linted, Then single-partition topic (rule 5), orvexcell fail-closed (rule 6), and no-KEDA (rule 13) are satisfied. *Assert: cell-lint passes rules 1/3/4/5/6/10/11/13.* [Source: NFR-K8]

## 🔨 Tasks
- [ ] RED: cluster-local DNS config + missing config keys added (AC1). (AC: 1)
- [ ] GREEN: cell-guard/421 middleware + solo sentinel (AC2). (AC: 2)
- [ ] GREEN: Idempotency-Key middleware on /internal/* (AC3). (AC: 3)
- [ ] RED→GREEN: typed tenant-move step-API + TENANT_MOVE.md inventory (AC4). (AC: 4)
- [ ] GREEN: cell-lint green across rules (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — healthz echoes CELL_ID+CLUSTER_NAME, cell-guard fail-closed (CheckCell/CellMatches), Idempotency-Key required on rebuild step-APIs (decodeRebuildStep) -- all real & tested; but no 421 cell-misdirect HTTP response and no tenant-move step-API.

Tier: cross-cutting cell fabric (config + `internal/tenantmove` + middleware). Engine analogue: none. Seams: cell-lint (JGAUQRsw2g), all stores (tenant-move inventory). Sibling: E9-S3 (config), E1-S3/E8-S1 (Idempotency-Key surfaces).

## 🧪 Testing
DoD `TestCellContractConformanceAndTenantMove`. Tiers: integration + cell-lint gate. CS §5: mock stores for the tenant-move typed shape; never mock own middleware.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch reliability + operability lenses; cell-lint JGAUQRsw2g §5 rules 1/3/4/5/6/10/11/13 (rule 10 typed contract is non-retrofittable — day-1).

## 🔗 References
PRD `dCbFzRQGDr` NFR-K8; Architecture `azRwTCZMqw` §6 (cell-contract conformance), §4 (deploy deltas R2).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: tenant-move step-API typed contract + cell-lint spec).
- [ ] Parent epic: E9. Depends on E2 (stores for inventory), E9-S3 (config).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
