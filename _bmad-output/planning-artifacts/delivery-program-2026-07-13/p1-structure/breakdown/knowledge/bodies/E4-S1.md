## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the universal egress gate I want a per-query ACL post-filter (engine FR-13 ∩ C5 token-scope) with an over-narrowing projected-metadata prefilter so that every content-bearing surface narrows through engine primitives and can never widen.

**Definition of Done:** `TestACLPostFilterEngineIntersectTokenScope` (integration, `internal/acl`) — candidates pass through an over-narrowing projected-metadata prefilter, then the engine FR-13 batch primitive on every request, then an unconditional C5 token-scope post-filter after every candidate read; no allow-decision is cached and counts reflect only survivors. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given projected ACL metadata, When prefiltering, Then it is over-narrowing only (can exclude, never admit); restriction-flagged pages always flow to the engine for the decision. *Assert: stale metadata never admits.* [Source: FR-K12]
- [ ] **AC2** — Given surviving candidates, When narrowing, Then the engine FR-13 batch primitive runs on every request AND the C5 token-scope intersection is an unconditional post-filter after every candidate read (cache hit or miss). *Assert: token-scope never satisfied by prefilter alone.* [Source: FR-K12]
- [ ] **AC3** (fail-closed) — Given the engine is down, When narrowing, Then retrieval fails closed (unavailable, never unfiltered). *Assert: engine-down → 0 results, typed unavailable.* [Source: FR-K12, NFR-K5]
- [ ] **AC4** (count-oracle) — Given facet/total/result counts, When computed, Then they use the POST-allow, POST-token-scope survivor set only; `partial:true` does not distinguish ACL-filtered from no-more-matches; residual counts capped/obscured. *Assert: restricted-doc existence not inferable via count deltas.* [Source: FR-K12, NFR-K3]
- [ ] **AC5** (budget) — Given the over-fetch budget (4×k, hard requery cap 2, FR-13 batch ceiling 500 IDs), When candidates < k survive, Then `partial:true` is flagged. *Assert: budget honored; partial flagged on cap.* [Source: FR-K12]

## 🔨 Tasks
- [ ] RED: over-narrow prefilter (never admits) (AC1). (AC: 1)
- [ ] GREEN: engine FR-13 batch + unconditional token-scope post-filter (AC2). (AC: 2)
- [ ] RED→GREEN: engine-down fail-closed (AC3). (AC: 3)
- [ ] RED→GREEN: count-oracle-safe counts (AC4). (AC: 4)
- [ ] RED→GREEN: over-fetch/requery-cap budget + partial flag (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** present — workflow.narrow (engine FR-13 per-source) + internal/admission.Admit (token-scope, deny-by-default) + quarantine prefilter, facets computed post-admission (no count oracle); tested workflow/isolation_probe_test.go + retrieve_test.go + admission tests.

Tier: cross-cutting security (`internal/acl`, applied inside `cmd/query`/bundler/sse/projection). Engine analogue: wraps engine FR-13 batch primitive. Seams: engine FR-13 (mock), token claims (E4-S3). Sibling: every egress epic (E3/E5/E6/E7) calls this.

## 🧪 Testing
DoD `TestACLPostFilterEngineIntersectTokenScope`. Tiers: unit (over-narrow/count-oracle) + integration (engine batch). CS §5: mock engine FR-13; never mock own acl package.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security lens (the load-bearing confidentiality control; no cached allow; count-oracle); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K12/NFR-K3/NFR-K5; Architecture `azRwTCZMqw` A1.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: engine FR-13 primitive shape + C5 token-scope claim).
- [ ] Parent epic: E4. Depends on E2 (candidates), E4-S3 (principal/scope).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
