## 🎯 Story
As the Orvex CLI SSE consumer I want the gateway to preserve the full shipped wire contract — replay-then-tail, `Last-Event-ID` resume, heartbeats, space-ACL-filtered frames, and control frames — so that CLI freshness never silently breaks.

**Definition of Done:** `TestSSEWireContractReplayTailResumeAndControlFrames` (integration, `cmd/sse`) — a client gets replay-then-tail, resumes from `Last-Event-ID`, receives heartbeats, sees space-ACL-filtered frames per key type (workspace-key-sees-all vs user-key space filter), and receives `stream.gap`/backpressure/`stream.terminated` control frames on the right conditions. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a connection, When opened, Then it replays the resume window then tails live. *Assert: replay precedes tail; no gap between.* [Source: FR-K15]
- [ ] **AC2** — Given `Last-Event-ID`, When reconnecting, Then the stream resumes from that id. *Assert: resume point honored.* [Source: FR-K15]
- [ ] **AC3** (ACL) — Given key type, When frames are filtered, Then a workspace key sees all and a user key gets space-ACL filtering (as shipped); the page-level-filter gap is a recorded risk. *Assert: user key sees only its spaces' frames.* [Source: FR-K15, FR-K12]
- [ ] **AC4** — Given control conditions, When they occur, Then `stream.gap` (retention/legacy cursor), backpressure, and `stream.terminated` control frames are emitted; a legacy Redis-format cursor resolves to `stream.gap`→re-sync, never an error. *Assert: correct control frame per condition.* [Source: FR-K15]
- [ ] **AC5** — Given a 60 s permission rebuild, When permissions change, Then the frame filter rebuilds within the shipped 60 s. *Assert: filter refresh ≤ 60 s.* [Source: FR-K15]

## 🔨 Tasks
- [ ] RED: replay-then-tail + Last-Event-ID resume (AC1, AC2). (AC: 1,2)
- [ ] GREEN: space-ACL frame filter by key type (AC3). (AC: 3)
- [ ] RED→GREEN: control frames (gap/backpressure/terminated) (AC4). (AC: 4)
- [ ] GREEN: 60 s permission-rebuild filter refresh (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Cursor v2 parse + stream.gap control frame + cross-pod fanout tail real (internal/sse/sse.go, cmd/sse/main.go /events); replay-then-tail historical replay is TODO and there is no periodic heartbeat loop.

Tier: streaming (`cmd/sse`). Engine analogue: preserves the shipped engine events controller behavior. Seams: E5-S1 (raw topic), E4-S1 (ACL). Sibling: E5-S3 (cursor deltas).

## 🧪 Testing
DoD `TestSSEWireContractReplayTailResumeAndControlFrames`. Tiers: integration (SSE client harness). CS §5: mock Kafka; never mock own gateway/ACL.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch reliability + honesty (gap not error); cell-lint rule 3.

## 🔗 References
PRD `dCbFzRQGDr` FR-K15; Architecture `azRwTCZMqw` A6.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: full SSE wire contract extracted from the shipped controller + EVENTS.md).
- [ ] Parent epic: E5. Depends on E5-S1, E4-S1.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
