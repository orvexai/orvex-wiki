## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the isolation guarantee I want automated probes — cross-tenant bytes = 0 and intra-tenant restricted-page bytes = 0 (plus count-oracle) — running in CI and against every deploy on query, projection-download, bundle, and SSE surfaces so that G5 is enforced structurally.

**Definition of Done:** `TestIsolationProbesAllEgressSurfaces` (integration/CI, `test/isolation`) — a probe suite proves (a) cross-tenant result bytes = 0 and (b) intra-tenant restricted-page bytes = 0 for an unauthorized principal, on query, projection-download, bundle, AND SSE surfaces, including a count/facet existence-oracle test. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given two tenants, When each queries, Then cross-tenant result bytes = 0 on every egress surface. *Assert: tenant A never sees tenant B's bytes (query, projection, bundle, SSE).* [Source: NFR-K3]
- [ ] **AC2** — Given a restricted page and an unauthorized principal in the same tenant, When retrieved, Then intra-tenant restricted-page bytes = 0. *Assert: unauthorized principal gets 0 restricted bytes on all four surfaces.* [Source: NFR-K3]
- [ ] **AC3** (count-oracle) — Given facet/total counts, When probed, Then a restricted doc's existence is not inferable via facet/total deltas. *Assert: count delta = 0 for a doc the principal cannot read.* [Source: NFR-K3]
- [ ] **AC4** — Given the probes, When wired, Then they run in CI AND against every deploy (not just once). *Assert: probe is a deploy gate.* [Source: NFR-K3]
- [ ] **AC5** (page-level tombstone scope) — Given a page-level ACL loss, When probed on projection-download, Then the FR-K20b hard-DELETE tombstone path is in the zero-byte probe scope. *Assert: lost-access page = 0 bytes post-sync.* [Source: NFR-K3, FR-K20b]

## 🔨 Tasks
- [ ] RED: cross-tenant zero-byte probe across 4 surfaces (AC1). (AC: 1)
- [ ] RED: intra-tenant restricted zero-byte probe (AC2). (AC: 2)
- [ ] RED→GREEN: count-oracle probe (AC3). (AC: 3)
- [ ] GREEN: wire probes into CI + deploy gate (AC4). (AC: 4)
- [ ] GREEN: page-level tombstone in probe scope (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** present — internal/workflow/isolation_probe_test.go runs cross-tenant + intersective-admission + namespace/scope-from-principal probes in CI (TestCrossTenantIsolationBytesZero, TestNamespaceDerivedFromPrincipalOnly, TestScopeFromVerifiedPrincipalOnly, TestAdmissionIsIntersectiveGolden). NB: unit/replay-fixture level; no separate per-deploy runtime probe job.

Tier: cross-cutting test harness (`test/isolation`, exercises all egress surfaces). Engine analogue: none. Seams: all egress epics. Sibling: E4-S1 (the control under test), E6/E7 (surfaces), E10 (memory/chat corpus probes extend here).

## 🧪 Testing
DoD `TestIsolationProbesAllEgressSurfaces`. Tiers: integration + CI gate. CS §5: exercise real ACL post-filter (do not mock the control being verified); mock only true-externals feeding candidates.

## 📏 Guidance
CS §§3/4/5/10/11; SE-Arch security lens (this is the G5 proof — probes are adversarial); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` NFR-K3/FR-K20b; Architecture `azRwTCZMqw` A1 (count-oracle test), A-bundle, A12.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: fixtures for restricted/unauthorized principals).
- [ ] Parent epic: E4. Depends on E4-S1, and the surfaces E3/E5/E6/E7 exist to probe.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
