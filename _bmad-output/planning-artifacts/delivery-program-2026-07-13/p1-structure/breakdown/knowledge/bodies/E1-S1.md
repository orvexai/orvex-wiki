## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the retrieval backbone I want to subscribe to the authoritative `wiki.*` CloudEvent catalog via Knative Triggers so that every wiki mutation reaches the indexer as an ordered, cell-verified event.

**Definition of Done:** `TestIndexerConsumesWikiCatalogFailClosedOnCellMismatch` (integration, indexer binary) — publishes each phase-1 `wiki.*` event to the `studio-spine` broker and asserts the indexer enacts the correct handler, that an event stamped with a foreign `orvexcell` is dropped + DLQ'd (never indexed), and that `page.content_updated` (not `page.updated`) is the body-index trigger. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the phase-1 subscription set, When each of `wiki.page.{content_updated,created,updated,deleted,moved,restored,status_changed}`, `wiki.space.deleted`, `wiki.attachment.{created,deleted}`, `wiki.comment.created`, permission-change, and purge events is published, Then a Trigger routes it to the correct handler. *Assert: every catalog name in the pinned contract has a registered handler; unknown → DLQ, not panic.* [Source: FR-K1]
- [ ] **AC2** — Given ordered delivery, When events arrive, Then Triggers carry `delivery.order: ordered`. *Assert: Trigger spec annotation present; a reordered pair for one key is reconciled by version guard (E1-S2).* [Source: FR-K1]
- [ ] **AC3** — Given an event whose `orvexcell` extension ≠ `CELL_ID`, When consumed, Then it is dropped + sent to `wiki-events-dlq.<cell>` and never indexed. *Assert: index unchanged; DLQ depth +1.* [Source: FR-K1, cell rule 6]
- [ ] **AC4** (negative) — Given a malformed/unparseable envelope, When consumed, Then it DLQs with an alertable reason, never silently acked. *Assert: DLQ record carries failure reason.* [Source: FR-K1, FR-K28]
- [ ] **AC5** — Given `page.content_updated` vs `page.updated`, When both arrive, Then only `content_updated` triggers body re-index; `updated` is metadata-only. *Assert: metadata event does not enqueue a chunk/embed pass.* [Source: FR-K1]

## 🔨 Tasks
- [ ] RED: table-test every catalog name → handler dispatch (AC1). (AC: 1)
- [ ] GREEN: Trigger manifests with `delivery.order: ordered` + DLQ sink (AC1, AC2). (AC: 1,2)
- [ ] RED→GREEN: parse `orvexcell`, fail-closed drop+DLQ on mismatch (AC3). (AC: 3)
- [ ] RED→GREEN: malformed-envelope DLQ path (AC4). (AC: 4)
- [ ] RED→GREEN: content_updated-only body-index gate (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** present — cmd/indexer/main.go consumes the raw wiki.* Kafka topic via libevents.Consumer; internal/indexer/handler.go cell-guards (CheckCell) fail-closed + DLQs, internal/event/event.go CellMatches; tested internal/indexer/handler_test.go + internal/event/kafkasink_test.go. NB: direct Kafka consumer-group, not Knative Triggers (deliberate M5-AC8 pivot).

Tier: event-ingress (`cmd/indexer`, Knative Service min-scale 0, Trigger-fed). NestJS engine analogue: none — this is the Go consumer side of the outbox relay. Seams crossed: Kafka `studio-spine` broker (mock the broker per CS §5), engine export API (E1-S2). Sibling deps: E2 (upsert target), engine outbox+relay live.

## 🧪 Testing
DoD `TestIndexerConsumesWikiCatalogFailClosedOnCellMismatch` (integration). Tiers: unit (dispatch table) + integration (broker→handler). CS §5 mocking: mock the Kafka broker + engine HTTP; never mock knowledge's own `internal/sources`/`internal/store`.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t reliability + honesty lenses (fail-closed, no silent drop); cell-lint JGAUQRsw2g rules 5/6/13.

## 🔗 References
PRD `dCbFzRQGDr` FR-K1/K16; Architecture `azRwTCZMqw` A6/§4/§6 rule 6; contracts CloudEvent catalog (pinned by pack).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG is the dispatch gate — `wiki.*` catalog + orvexcell extension spec).
- [ ] Parent epic: E1. Intra-service: precedes E1-S2 (both needed for a findable page).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
