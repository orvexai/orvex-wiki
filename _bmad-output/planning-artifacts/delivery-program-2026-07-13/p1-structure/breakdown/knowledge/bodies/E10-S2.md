## 🎯 Story
As the Studio-corpora confidentiality guarantee (A12) I want owner-scoped admission, an employer/org-admin firewall, delegated-principal-only retrieval, and ingestion-side privacy exclusion so that a user's Memory and chat are invisible to their employer by construction.

**Definition of Done:** `TestStudioCorporaOwnerScopedEmployerFirewallDelegatedOnly` (integration, `internal/acl`) — a Memory/chat/skill document is served only to its owning principal within its tenant; no org role (incl. admin/owner) admits a non-owner to another user's Memory/chat; `cmd/query` rejects service/api-key tokens on the memory/chat corpora; and content in a private/excluded state never enters the index. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** (owner-scoped) — Given a Memory atom / chat turn / skill doc, When retrieved, Then it is served only to its owning principal within its tenant — ACL is owner-derived, NOT space/role-derived. *Assert: non-owner in same tenant → 0 bytes.* [Source: FR-K31b, NFR-K3]
- [ ] **AC2** (employer firewall) — Given an org admin/owner, When retrieving another user's Memory/chat, Then NO org role admits them; the org-visibility the wiki path grants does not extend to memory/chat. *Assert: org admin → 0 bytes on memory/chat.* [Source: FR-K31b, NFR-K3]
- [ ] **AC3** (delegated-only) — Given a service-principal / api-key token on the memory or chat corpora, When queried, Then `cmd/query` REJECTS it (no owner to scope to) fail-closed; skill retrieval may accept a tenant-scoped service token where the skill is a tenant asset. *Assert: service token on memory/chat → denied.* [Source: FR-K31b]
- [ ] **AC4** (ingestion exclusion) — Given content in an excluded/private state, When published, Then it NEVER enters the index (filtered at ingestion, not read) — so it cannot leak via a count/facet oracle (A1) or a rebuild walk (A8). *Assert: private content absent from index + oracle.* [Source: FR-K31b]
- [ ] **AC5** (fail-closed default) — Given a Studio corpus that has not declared its owner-scoped primitive, When queried, Then it is never served. *Assert: undeclared-primitive corpus → 0 bytes.* [Source: FR-K31b, FR-K12a]

## 🔨 Tasks
- [ ] RED: owner-scoped admission (non-owner 0 bytes) (AC1). (AC: 1)
- [ ] RED→GREEN: employer/org-admin firewall (AC2). (AC: 2)
- [ ] RED→GREEN: delegated-principal-only rejection of service tokens (AC3). (AC: 3)
- [ ] RED→GREEN: ingestion-side privacy exclusion (AC4). (AC: 4)
- [ ] GREEN: per-corpus fail-closed default (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — No owner-scoped admission / employer-firewall / delegated-only / ingestion privacy-exclusion logic; internal/admission.Admit is space-based only (gen.Principal has a Delegated flag but nothing consumes it for A12).

Tier: cross-cutting security (`internal/acl` corpus primitives, `cmd/query`). Engine analogue: none. Seams: source-adapter primitives (E2-S4), query. Sibling: E10-S1 (ingestion), E4-S4 (probe extended to memory/chat), E10-S3 (namespace).

## 🧪 Testing
DoD `TestStudioCorporaOwnerScopedEmployerFirewallDelegatedOnly`. Tiers: integration. CS §5: exercise real ACL primitives (do not mock the control); mock only publishers.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security lens (A12 REINSTATED S1 — employer-invisible by construction, delegated-only, ingestion-exclusion); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K31b; Architecture `azRwTCZMqw` A12 (REINSTATED); R-9 `0mFImku9Gp`.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: owner-scoped ACL primitive schema per corpus).
- [ ] Parent epic: E10. Depends on E10-S1, E4-S1/S4.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
