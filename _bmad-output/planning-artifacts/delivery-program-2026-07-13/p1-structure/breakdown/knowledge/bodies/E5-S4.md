## 🎯 Story
As the CLI/admin I want the events admin surfaces (stream settings, live connections, delivery log) relocated from the engine at the exact shipped paths so that existing admin/CLI callers keep working unchanged.

**Definition of Done:** `TestEventsAdminSurfacesExactPaths` (integration, `cmd/sse`) — `GET /api/orvex/events/{connections,log,settings}` serve live connections, the delivery log, and stream settings at the exact shipped paths under admin authorization. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `GET /api/orvex/events/connections`, When called by an admin, Then it returns live SSE connections. *Assert: connection list served.* [Source: FR-K17]
- [ ] **AC2** — Given `GET /api/orvex/events/log`, When called, Then it returns the delivery log (from the `sse_delivery_log` append table). *Assert: delivery log served.* [Source: FR-K17]
- [ ] **AC3** — Given `GET /api/orvex/events/settings`, When called, Then it returns/updates stream settings. *Assert: settings served.* [Source: FR-K17]
- [ ] **AC4** — Given the paths, When served, Then they are byte-for-byte the shipped endpoint paths the CLI/admin already call. *Assert: no path drift from the engine's events-admin endpoints.* [Source: FR-K17]
- [ ] **AC5** (negative) — Given a non-admin principal, When calling any events-admin surface, Then it is rejected. *Assert: 403 for non-admin.* [Source: FR-K17, NFR-K8]

## 🔨 Tasks
- [ ] RED: connections/log/settings handlers at exact paths (AC1, AC2, AC3, AC4). (AC: 1,2,3,4)
- [ ] RED→GREEN: admin-authorization gate (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** absent — No /api/orvex/events/{connections,log,settings} routes; cmd/sse mux only exposes /events, /events/head, /healthz, /readyz.

Tier: streaming admin (`cmd/sse`). Engine analogue: relocates engine events-admin endpoints. Seams: delivery-log store (E2-S1 append table), auth (E4-S3). Sibling: E5-S1/S2.

## 🧪 Testing
DoD `TestEventsAdminSurfacesExactPaths`. Tiers: integration. CS §5: mock auth introspection; never mock own store.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch operability lens (exact-path relocation, admin-audited); cell-lint rule 3.

## 🔗 References
PRD `dCbFzRQGDr` FR-K17; Architecture `azRwTCZMqw` §3.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: events-admin path set).
- [ ] Parent epic: E5. Depends on E2-S1 (delivery log), E4-S3 (auth).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
