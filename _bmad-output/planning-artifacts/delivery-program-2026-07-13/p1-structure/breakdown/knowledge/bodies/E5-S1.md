## 🎯 Story
As the SSE substrate I want a KafkaSink bridging rich CloudEvents onto a single-partition raw topic per cell, consumed by each SSE pod as a standalone assigned consumer, so that offset cursors are total-order-correct and every pod sees all events.

**Definition of Done:** `TestSSERawTopicSinglePartitionAssignedConsumer` (integration, `cmd/sse`) — the KafkaSink writes rich-profile events to `wiki-events.<cell>` (1 partition), each SSE pod runs an `assign()`+`seek` standalone consumer (no shared group) that tails all events, and an `orvexcell` mismatch is dropped+DLQ'd (never emitted). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given rich-profile CloudEvents, When bridged, Then a KafkaSink writes them to `wiki-events.<cell>` with exactly 1 partition per cell and `delivery.order: ordered`. *Assert: single-partition total order preserved.* [Source: FR-K16]
- [ ] **AC2** — Given N SSE pods, When consuming, Then each runs a standalone assigned consumer (`assign()`+`seek`, no shared consumer group) so all pods see all events. *Assert: every pod receives every event.* [Source: FR-K16]
- [ ] **AC3** — Given a per-connection replay, When requested, Then per-connection `seek` serves the replay window from the offset. *Assert: replay from offset works per connection.* [Source: FR-K16]
- [ ] **AC4** (cell) — Given a frame stamped with a foreign `orvexcell`, When consumed, Then it is dropped + DLQ'd, never emitted to a client. *Assert: no cross-cell frame emitted.* [Source: FR-K16, cell rule 6]
- [ ] **AC5** — Given raw-topic retention, When configured, Then it is ≥ the max client resume window (≥ 7 d); a valid-but-expired offset resolves to `stream.gap`. *Assert: retention ≥ 7 d; expired offset → gap.* [Source: FR-K16]

## 🔨 Tasks
- [ ] RED: single-partition ordered sink write (AC1). (AC: 1)
- [ ] GREEN: assigned-consumer tail across pods (AC2). (AC: 2)
- [ ] GREEN: per-connection seek replay (AC3). (AC: 3)
- [ ] RED→GREEN: orvexcell fail-closed drop (AC4). (AC: 4)
- [ ] RED→GREEN: retention ≥ 7 d + expired-offset gap (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — KafkaSink spine consumer (internal/event/spine_consumer.go durable Redis-stream append + cross-pod fanout) real & tested (kafkasink_test.go); but the raw-topic SSE consumer is a consumer-group (event.NewSpineSubscriber -> libevents.NewConsumer), NOT the specified standalone assign/seek per-pod consumer; internal/sse Kafka consume is a stub.

Tier: streaming substrate (`cmd/sse` plain Deployment, KafkaSink). Engine analogue: relocates engine events controller's transport. Seams: Kafka (true-external), spine bridge Trigger. Sibling: E5-S2 (wire contract), E5-S3 (cursor).

## 🧪 Testing
DoD `TestSSERawTopicSinglePartitionAssignedConsumer`. Tiers: integration (Kafka). CS §5: mock Kafka in unit; never mock own consumer.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch reliability lens (total-order, fail-closed on cell); cell-lint rules 5/6.

## 🔗 References
PRD `dCbFzRQGDr` FR-K16; Architecture `azRwTCZMqw` A6/§6 rule 5.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: raw-topic + rich envelope profile).
- [ ] Parent epic: E5. Depends on E1-S1 (spine).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
