## 🎯 Story
As the purge guarantee I want purge events to delete every copy knowledge holds — projection rows, Turbopuffer namespace, chunk stores, embeddings, S3 bundles, SSE delivery logs, and derived eval-set entries — verified by a zero-residual probe so that the D-SYNC-1 residual-data class is closed.

**Definition of Done:** `TestPurgeDeletesEveryCopyZeroResidual` (integration, `internal/purge`) — on `wiki.workspace.deleted`/`wiki.user.deleted`/`wiki.space.deleted`, knowledge deletes projection rows, the Turbopuffer namespace contents, chunk stores, embeddings, S3 bundles, SSE delivery logs, and derived eval-set entries; a per-purge zero-residual probe confirms nothing remains. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `wiki.workspace.deleted`/`user.deleted`/`space.deleted`, When consumed, Then every store is purged: projection rows, Turbopuffer namespace contents, chunk stores, embeddings, S3 bundles, SSE delivery logs, and any eval-set entries derived from that tenant's queries. *Assert: each store returns zero rows/objects for the tenant.* [Source: FR-K27]
- [ ] **AC2** (Redis) — Given the derived cache, When purging, Then `cand:{tenant}:*`, `doc:{tenant}:*`, `rel:{tenant}:*`, `emb:{tenant}:*` are deleted by tenant-prefix scan. *Assert: Redis tenant prefix cleared.* [Source: FR-K27]
- [ ] **AC3** (probe) — Given a purge, When it completes, Then a zero-residual probe verifies no residual across all stores. *Assert: probe green; any residual fails the purge.* [Source: FR-K27, NFR-K3]
- [ ] **AC4** (Studio corpora) — Given Studio corpora (memory/chat/skill), When a tenant/user purges, Then the purge invariant extends to every corpus unchanged. *Assert: memory/chat/skill docs also zero-residual.* [Source: FR-K27]
- [ ] **AC5** (idempotency/negative) — Given a redelivered purge, When re-applied, Then it is idempotent (already-empty is success), and a partial-store failure retries → DLQ rather than reporting success. *Assert: no false "purged" on partial failure.* [Source: FR-K27, FR-K28]

## 🔨 Tasks
- [ ] RED: multi-store delete on each purge event (AC1). (AC: 1)
- [ ] GREEN: Redis tenant-prefix scan delete (AC2). (AC: 2)
- [ ] RED→GREEN: zero-residual probe (AC3). (AC: 3)
- [ ] GREEN: extend to Studio corpora (AC4). (AC: 4)
- [ ] RED→GREEN: idempotent + partial-failure DLQ (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — Delete-reconcile hard-deletes the Postgres documents row (internal/corpus/reconcile.go + store.DeleteBySource) real & tested; but store.DeleteTenant unimplemented, turbopuffer DeleteNamespace/DeleteDocs stubbed, cache.EvictTenant exists yet there is no orchestrated multi-store purge and no zero-residual probe.

Tier: ingest→multi-store delete (`internal/purge`, `cmd/indexer`). Engine analogue: FR-37 gate (D-SYNC-1). Seams: all stores. Sibling: E3-S3 (Redis eviction), E4-S4 (probe), E10 (corpus purge).

## 🧪 Testing
DoD `TestPurgeDeletesEveryCopyZeroResidual`. Tiers: integration (all stores). CS §5: mock true-external stores only in unit; the zero-residual probe runs against real stores.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security lens (zero-residual is a legal/tenancy invariant); cell-lint rule 10 (all stores in inventory).

## 🔗 References
PRD `dCbFzRQGDr` FR-K27; Architecture `azRwTCZMqw` A11 (FR-K27 Redis scope), A12 (corpora).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: purge event set).
- [ ] Parent epic: E9. Depends on E1-S1 (events), E2 (stores).

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
