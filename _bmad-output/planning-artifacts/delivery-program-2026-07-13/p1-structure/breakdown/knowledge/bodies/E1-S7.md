## 🎯 Story
As an operator I want export-pull / Tika / embedding failures to retry with backoff then DLQ with alerting, every document to carry a freshness watermark, and a staleness SLI to alert on lag so that the historical RAG-staleness bug class is structurally closed.

**Definition of Done:** `TestIngestionFailureRetryDLQAndStalenessSLI` (integration, indexer) — a repeatedly-failing ingest step retries with backoff, DLQs with an alertable record, and the per-document freshness watermark drives a staleness SLI that alerts when lag breaches NFR-K1. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given an export-pull/Tika/embedding failure, When it recurs, Then it retries with backoff and, on exhaustion, DLQs with alerting. *Assert: DLQ record + alert emitted; no silent loss.* [Source: FR-K28]
- [ ] **AC2** — Given each document, When indexed, Then it carries a freshness watermark. *Assert: watermark present + monotonic per key.* [Source: FR-K28]
- [ ] **AC3** — Given watermark lag, When it breaches NFR-K1 (async p95 < 5 s / p99 < 30 s), Then the indexing-lag SLI alerts. *Assert: SLI crosses threshold → alert fires.* [Source: FR-K28, NFR-K1]
- [ ] **AC4** (freshness floor) — Given the async keyword path, When measured, Then p95 < 5 s / p99 < 30 s from event publish; embeddings p95 < 30 s. *Assert: seeded-corpus budget honored (NFR-K12 draft, ×headroom).* [Source: NFR-K1]
- [ ] **AC5** (negative) — Given a poison message, When retried, Then it does not block the ordered partition indefinitely (bounded retry → DLQ). *Assert: head-of-line not permanently stalled.* [Source: FR-K28]

## 🔨 Tasks
- [ ] RED: retry/backoff → DLQ with alert record (AC1). (AC: 1)
- [ ] GREEN: per-doc watermark stamping (AC2). (AC: 2)
- [ ] RED→GREEN: staleness SLI threshold + alert (AC3, AC4). (AC: 3,4)
- [ ] RED→GREEN: poison-message bounded retry (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — DLQ + retry/backoff real & tested (internal/event/deadletter.go, internal/indexer/handler.go recovery, clients.go 429 backoff); but no freshness watermark and no staleness SLI.

Tier: ingest reliability (`cmd/indexer`, `internal/workflow` + obs). Engine analogue: none. Seams: Kafka DLQ sink, Prometheus (obs), all ingest steps. Sibling: E1-S2/S4/S5 (the failing steps), E9-S3 (obs).

## 🧪 Testing
DoD `TestIngestionFailureRetryDLQAndStalenessSLI`. Tiers: unit (backoff/DLQ) + integration (SLI). CS §5: mock the failing true-external (LiteLLM/Tika/engine); never mock own workflow.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch reliability + operability lenses (DLQ + SLI + alert); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K28 + NFR-K1 (+ NFR-K12 draft); Architecture `azRwTCZMqw` A7/A10.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: DLQ topic + envelope; NFR budget pinned).
- [ ] Parent epic: E1. Depends on E1-S2/S4/S5.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
