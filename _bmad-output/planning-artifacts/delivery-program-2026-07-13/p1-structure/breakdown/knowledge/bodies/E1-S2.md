## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the indexer I want to treat content events as thin (identity + version), pull the authoritative body from the engine export API, and upsert version-guarded so that redelivery/reordering never overwrites newer content.

**Definition of Done:** `TestUpsertDocumentVersionGuardedCAS` (integration, `internal/store/postgres`) — an event older than the indexed document version is a no-op; a newer one wins via a single `UPDATE … WHERE id=$1 AND version<$2`, and the Turbopuffer write follows the Postgres CAS win (A2a order). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a thin content event, When received, Then the indexer pulls FR-18 markdown + FR-38 embed-resolved `text_repr` from the engine export API using AGPL-clean api-key auth. *Assert: export call made with api-key; payload = resolved text_repr.* [Source: FR-K2]
- [ ] **AC2** — Given an event with version ≤ the indexed document version, When applied, Then it is a no-op (stale-overwrite protection). *Assert: row version unchanged; CAS affected-rows = 0.* [Source: FR-K3]
- [ ] **AC3** — Given a newer version, When applied, Then Postgres CAS wins FIRST, then Turbopuffer is written with chunk IDs carrying the version (A2a). *Assert: on a lost race the mismatch is detectable by the next event/FR-K28 sweeper.* [Source: FR-K3]
- [ ] **AC4** (error path) — Given the engine export API is unreachable, When pulling, Then the event retries with backoff and never corrupts the projection. *Assert: no partial doc written; retry scheduled.* [Source: FR-K2, FR-K28]
- [ ] **AC5** — Given concurrent indexers for one doc, When both apply, Then a per-doc Redis lock (`lock:{tenant}:{doc}`) bounds the window and a lock-fail re-queues (never drops). *Assert: exactly one winner; loser re-queued.* [Source: FR-K3]

## 🔨 Tasks
- [ ] RED: CAS version-guard test with stale/newer/equal versions (AC2, AC3). (AC: 2,3)
- [ ] GREEN: export-pull resolver client (api-key) (AC1). (AC: 1)
- [ ] GREEN: two-store write order Postgres→Turbopuffer (AC3). (AC: 3)
- [ ] RED→GREEN: export-unreachable retry/backoff without partial write (AC4). (AC: 4)
- [ ] RED→GREEN: per-doc Redis lock + re-queue on loss (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** present — internal/workflow/workflow.go ingestOnePage does engine export-pull (clients.Engine.ResolveBody via wiki adapter) then content-hash then version-guarded CAS (store/postgres/postgres.go UpsertDocument, WHERE version<EXCLUDED.version) then turbopuffer.Upsert; tested workflow/ingest_test.go + store/postgres/postgres_test.go.

Tier: ingest→store (`cmd/indexer`, `internal/sources/wiki`, `internal/store`). Engine analogue: consumes the engine export API (FR-18/38). Seams: engine HTTP (mock), Postgres store (real in integration), Turbopuffer adapter (memindex in unit). Sibling deps: E2-S1 (schema), E1-S1 (event arrival).

## 🧪 Testing
DoD `TestUpsertDocumentVersionGuardedCAS`. Tiers: unit (CAS logic) + integration (Postgres+adapter). CS §5: mock engine export + Redis-lock only where not the unit under test; never mock own store package.

## 📏 Guidance
CS §§3/4/5/6/7/10/11; SE-Arch reliability lens (CAS not read-check-then-write) + honesty (no partial writes); cell-lint rule 11 (Idempotency-Key on the internal upsert path shares this CAS).

## 🔗 References
PRD `dCbFzRQGDr` FR-K2/K3; Architecture `azRwTCZMqw` A2/A2a.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: envelope thin profile + version-field obligation + engine export API shape).
- [ ] Parent epic: E1. Depends on E2-S1 (schema), follows E1-S1.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
