## 🎯 Story
As the storage foundation I want a Postgres projection schema (documents/chunks-metadata/sources/cursors/bundle_registry/journals) with fail-closed RLS so that every read is tenant-walled structurally and chunk text never lives in Postgres.

**Definition of Done:** `TestProjectionRLSFailClosedAndNoChunkText` (integration, `internal/store/postgres`) — a query under principal A's RLS GUC returns zero of principal B's rows; raw table access outside the repository fails a CI lint; and the `chunks` table stores metadata only (no chunk text column). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the multi-source model, When the schema is created, Then `documents` carries `{tenant, source_type, source_id, space, version, acl_meta, watermark, extraction_state, epoch, source_meta jsonb}` and `chunks` carries **metadata only** (vectors in Turbopuffer). *Assert: no chunk-text column exists.* [Source: FR-K7]
- [ ] **AC2** (RLS) — Given a per-request principal GUC (`app.tenant`), When any table is read, Then RLS fail-closed excludes other tenants' rows on projection AND journal append tables. *Assert: cross-tenant select returns 0 rows.* [Source: FR-K7, NFR-K3]
- [ ] **AC3** — Given the CAS write, When upserting, Then it is a single `UPDATE … WHERE id=$1 AND version<$2` (never read-check-then-write). *Assert: atomic op; no TOCTOU.* [Source: FR-K7]
- [ ] **AC4** — Given event/usage journals, When stored, Then they are range-partitioned append tables with retention = DROP PARTITION and the same tenant column + RLS. *Assert: partition-management path present; journals RLS-covered.* [Source: FR-K7, NFR-K6]
- [ ] **AC5** (lint) — Given the repository layer sets `app.tenant` per request, When raw table access is attempted outside it, Then a CI lint forbids it. *Assert: lint fails on out-of-repo raw access.* [Source: FR-K7, NFR-K3]

## 🔨 Tasks
- [ ] RED: RLS cross-tenant zero-row test (AC2). (AC: 2)
- [ ] GREEN: schema migrations (documents/chunks-meta/sources/cursors/bundle_registry) (AC1). (AC: 1)
- [ ] GREEN: CAS write helper (AC3). (AC: 3)
- [ ] RED→GREEN: range-partitioned journals + DROP PARTITION cronjob (AC4). (AC: 4)
- [ ] RED→GREEN: raw-access CI lint (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — documents projection schema (migrations 001-004) + store methods real & tested (store/postgres/postgres_test.go); tenancy is WHERE tenant=$1, NOT Postgres RLS -- no ROW LEVEL SECURITY/POLICY/SET LOCAL app.tenant despite the doc comment. RLS fail-closed backstop absent.

Tier: persistence (`internal/store/postgres`, per-cell CNPG). Engine analogue: replaces engine tsvector/pgvector (deleted). Seams: Postgres (real in integration). Sibling: foundational for E1/E3/E7/E8/E9/E10.

## 🧪 Testing
DoD `TestProjectionRLSFailClosedAndNoChunkText`. Tiers: unit (CAS) + integration (RLS). CS §5: never mock own store — use a real Postgres fixture.

## 📏 Guidance
CS §§3/4/5/6/7/10/11; SE-Arch security lens (RLS is the backstop, not the only control) + the chunk-text-never-in-Postgres sizing invariant; cell-lint rule 10 (store inventory).

## 🔗 References
PRD `dCbFzRQGDr` FR-K7 (D-S12); Architecture `azRwTCZMqw` A2/A2b.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: document model field set + source_meta schema).
- [ ] Parent epic: E2. Foundational — no intra-service predecessor.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
