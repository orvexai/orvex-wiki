## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the multi-publisher backbone I want a source-adapter contract registry plus the wiki source adapter so that any publisher joins by registering its CloudEvent types, content-resolver, and ACL-narrowing primitive — and a source with no declared primitive is never served.

**Definition of Done:** `TestSourceAdapterRegistryWikiTemplateAndFailClosed` (integration, `internal/sources`) — the wiki source registers (FR-18/38 resolver, FR-13 ACL, FR-37 purges), documents are stamped `source_type` from the registry (never payload), and a source with no declared ACL primitive serves zero documents (fail closed). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the registry, When a source registers, Then it declares CloudEvent types + version semantics, a content-resolver endpoint, an ACL-narrowing primitive, and purge events. *Assert: registration validated against contract schema.* [Source: FR-K7, FR-K12a]
- [ ] **AC2** — Given the wiki adapter, When ingesting, Then `source_type` is stamped from the registry identity, never from payload. *Assert: forged payload source_type ignored.* [Source: FR-K7]
- [ ] **AC3** (fail-closed) — Given a source with no declared ACL-narrowing primitive, When a query would return its docs, Then zero are served. *Assert: undeclared-primitive source → 0 bytes.* [Source: FR-K12a]
- [ ] **AC4** (security) — Given ACL-primitive registration, When reviewed, Then permissive/no-op primitives are rejected and each source's ACL + resolver endpoints are pinned/allow-listed + SSRF-guarded + fail-closed on error. *Assert: unreachable ACL endpoint → serves nothing.* [Source: FR-K12a, NFR-K8]
- [ ] **AC5** — Given a new publisher, When added, Then it needs only a contracts entry + `internal/sources/<name>` client — no pipeline changes. *Assert: adding a source touches no core ingest code.* [Source: FR-K7]

## 🔨 Tasks
- [ ] RED: registry validation + fail-closed default (AC1, AC3). (AC: 1,3)
- [ ] GREEN: wiki source adapter (resolver/ACL/purge) as template (AC2). (AC: 2)
- [ ] RED→GREEN: permissive-primitive rejection + SSRF-guard + fail-closed-on-error (AC4). (AC: 4)
- [ ] GREEN: no-pipeline-change extension test (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** present — internal/sources/sources.go Registry (fail-closed Get) + internal/sources/wiki adapter (Resolve/Narrow via engine FR-13); tested sources/sources_test.go + sources/wiki/attachment_test.go.

Tier: seam (`internal/sources`, registry). Engine analogue: wiki adapter wraps engine export/ACL/purge APIs. Seams: contracts registry, engine. Sibling: E10 (Studio corpora register here), E1 (ingest uses adapters).

## 🧪 Testing
DoD `TestSourceAdapterRegistryWikiTemplateAndFailClosed`. Tiers: unit (registry/fail-closed) + integration (wiki adapter). CS §5: mock engine endpoints; never mock own registry/sources.

## 📏 Guidance
CS §§3/4/5/7/10/11; SE-Arch security lens (fail-closed default, reject permissive primitives, SSRF-guard); cell-lint N/A.

## 🔗 References
PRD `dCbFzRQGDr` FR-K7/K12a; Architecture `azRwTCZMqw` A4/A12.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: source-adapter registry entry schema).
- [ ] Parent epic: E2. Depends on E2-S1 (store). Enables E10.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
