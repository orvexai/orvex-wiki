## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As DR I want idempotent rebuild step-APIs (namespace/walk/index-batch/delta-repull/alias-swap/retire) that the central Temporal workflows drive so that a tenant/cell index can be rebuilt from the engine with no in-service Temporal worker.

**Definition of Done:** `TestRebuildStepAPIsIdempotentDriveByCentralWorkflow` (integration, `internal/rebuild`) — each step-API (`rebuild/namespace`, `walk?cursor`, `index-batch`, `delta-repull`, `alias-swap`, `retire`) is idempotent under `Idempotency-Key`, the walk writer uses the identical A2 CAS as live indexers, and live Trigger upserts continue against the old namespace during the walk (no events lost at swap). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the central `RebuildTenant`/`RebuildCell` workflows (in orvex-studio-workflows), When they run, Then they call knowledge's idempotent step-APIs; knowledge runs NO in-service Temporal worker. *Assert: no Temporal worker in knowledge; step-APIs are plain HTTP.* [Source: FR-K23]
- [ ] **AC2** — Given the sequence, When rebuilding, Then it is fresh-namespace → checkpointed walk → walk watermark → delta re-pull of events landed during the walk → AliasSwap → retire old. *Assert: sequence order enforced.* [Source: FR-K23]
- [ ] **AC3** (idempotency) — Given a retried step, When re-invoked with the same `Idempotency-Key`, Then it is a single logical apply. *Assert: duplicate step → no double-effect (cell rule 11).* [Source: FR-K23]
- [ ] **AC4** — Given live upserts during the walk, When a Trigger fires, Then it continues against the OLD namespace so no events are lost at swap. *Assert: event during walk applied; present after swap.* [Source: FR-K23]
- [ ] **AC5** — Given bulk ACL load, When walking, Then rebuild runs through the A1a bulk bulkhead, not the interactive engine-ACL budget. *Assert: bulkhead used.* [Source: FR-K23]

## 🔨 Tasks
- [ ] RED: each step-API idempotent under Idempotency-Key (AC1, AC3). (AC: 1,3)
- [ ] GREEN: sequenced rebuild with CAS walk writer (AC2). (AC: 2)
- [ ] RED→GREEN: live-upsert-during-walk no-loss (AC4). (AC: 4)
- [ ] GREEN: bulk bulkhead routing (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** present — internal/workflow/rebuild.go implements all 6 idempotent step-APIs (namespace/walk/index-batch/delta-repull/alias-swap/retire) + RunRebuild + server handlers; tested rebuild_test.go + rebuild_replay_test.go. NB: RebuildNamespace/RebuildRetire call turbopuffer.DeleteNamespace, errNotWired against the real Client (passes against memindex in tests).

Tier: internal step-API (`internal/rebuild`, KPA HTTP surfaces). Engine analogue: relocates engine reconciler. Seams: orvex-studio-workflows (caller, mock), store AliasSwap (E2-S2). Sibling: E8-S2 (epochs), E9-S2 (reindex reuses these).

## 🧪 Testing
DoD `TestRebuildStepAPIsIdempotentDriveByCentralWorkflow`. Tiers: integration. CS §5: mock the Temporal caller + engine walk source; never mock own rebuild/store.

## 📏 Guidance
CS §§3/4/5/6/9/10/11; SE-Arch reliability lens (idempotent steps, no-loss swap); cell-lint rule 11; ADR 0005 (central-Temporal).

## 🔗 References
PRD `dCbFzRQGDr` FR-K23 (D-WF-1); Architecture `azRwTCZMqw` A8.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: rebuild step-API contract + workflows step-client shape).
- [ ] Parent epic: E8. Depends on E2-S2 (AliasSwap), orvex-studio-workflows.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
