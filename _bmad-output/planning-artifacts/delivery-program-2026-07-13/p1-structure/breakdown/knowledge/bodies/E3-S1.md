## 🎯 Story
As every retrieval consumer I want a query surface (keyword|semantic|hybrid search, RAG top-k, related, duplicates, attachment search) with facets and fields projection so that one API serves the UI box, MCP, CLI, and AI.

**Definition of Done:** `TestUnifiedSearchReadYourWritesAndCitations` (integration, `internal/search`) — `/v1/query` returns hybrid results with facets + fields projection, a just-sync-upserted page is found (read-your-writes), and RAG retrieval returns top-k chunks with scores + citations. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2099); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `POST /v1/query`, When called with `mode ∈ {keyword,semantic,hybrid}`, `facets`, `fields`, `corpora:[]`, Then results match the mode with facet counts + projected fields. *Assert: hybrid returns fused set; facets present.* [Source: FR-K11]
- [ ] **AC2** — Given `POST /v1/retrieve`, When called, Then it returns RAG top-k chunks with scores. *Assert: k chunks, descending score, with source citations.* [Source: FR-K11]
- [ ] **AC3** — Given `/v1/related`, `/v1/duplicates`, `/v1/attachments/search`, When called, Then each returns its shaped result. *Assert: related-by-page, duplicate-check, attachment hits respectively.* [Source: FR-K11]
- [ ] **AC4** (counts) — Given facet/total counts, When computed, Then they are derived from the POST-allow, POST-token-scope surviving set only — never the over-fetched candidate pool (no existence oracle). *Assert: counts reflect only served docs.* [Source: FR-K11, FR-K12]
- [ ] **AC5** (negative) — Given an empty/invalid corpora selector, When queried, Then a typed 4xx is returned (never a silent all-corpora scan). *Assert: invalid selector rejected.* [Source: FR-K11]

## 🔨 Tasks
- [ ] RED: /v1/query hybrid + facets + fields projection (AC1). (AC: 1)
- [ ] GREEN: /v1/retrieve RAG top-k with scores/citations (AC2). (AC: 2)
- [ ] GREEN: /v1/related + /v1/duplicates + /v1/attachments/search (AC3). (AC: 3)
- [ ] RED→GREEN: counts from post-allow survivors only (AC4). (AC: 4)
- [ ] RED→GREEN: invalid-selector 4xx (AC5). (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 4a20a49, 2026-07-14):** partial — /v1/query (hero.Search), /v1/retrieve, /v1/duplicates (POST+GET), /v1/attachments/search are real handlers & tested (server/query_test.go, retrieve_test.go, duplicates_test.go, attachments_search_test.go); /v1/related is a notImplemented 501 stub.

Tier: request-serving (`cmd/query`, always-warm HPA Deployment, `internal/search`). Engine analogue: replaces engine tsvector search. Seams: store adapter (E2), ACL post-filter (E4-S1), LiteLLM query-embedding. Sibling: E3-S2 (ranking), E3-S3 (cache).

## 🧪 Testing
DoD `TestUnifiedSearchReadYourWritesAndCitations`. Tiers: unit (shape) + integration (end-to-end query). CS §5: mock LiteLLM query-embedding + engine ACL; never mock own search/store.

## 📏 Guidance
CS §§3/4/5/6/10/11; SE-Arch security lens (count-oracle) + honesty (no silent all-scan); cell-lint rule 3 (cell-guard on routes).

## 🔗 References
PRD `dCbFzRQGDr` FR-K11; Architecture `azRwTCZMqw` §3/A1.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2099** (contract TAG: query OpenAPI).
- [ ] Parent epic: E3. Depends on E2, E4-S1.

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
