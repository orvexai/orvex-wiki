## 🎯 Story

As **the only write door from Stripe into the system-of-record**, I want **`cmd/webhook` to verify, journal-raw, dedupe, re-fetch authoritative state, and apply**, so that **no Stripe event is lost, duplicated, or able to regress state (A-HOOK, NFR-B3)**.

**Definition of Done:** `TestWebhookPipelineIdempotentOutOfOrderProof` — an integration test asserting the pipeline: verify `Stripe-Signature` (reject on failure + timestamp-tolerance replay guard) → journal raw payload to a Postgres JSONB append table BEFORE processing → idempotency check against `processed_webhook_events` (duplicate ⇒ constant 200) → authoritative re-fetch from the Stripe API → state-machine apply (one entitlement version + subscription upsert in one transaction). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a delivery, When received, Then the signature is verified unconditionally; failure ⇒ reject; a payload outside the signed-timestamp tolerance ⇒ rejected replay. *assert reject on bad sig + stale timestamp* [Source: FR-B10, A-HOOK, NFR-B6]
- [ ] **AC2** — Given a verified event, When processed, Then the raw payload is journaled to a Postgres JSONB append table BEFORE any interpretation. *assert raw row exists before state apply* [Source: FR-B10, D-S12]
- [ ] **AC3** — Given a duplicate event id, When re-delivered, Then it is a constant 200 no-op against the `processed_webhook_events` ledger. *assert duplicate ⇒ 200, no second version write* [Source: FR-B10, NFR-B3]
- [ ] **AC4** — Given an event, When applied, Then handlers re-fetch the authoritative object from the Stripe API and apply current truth, so a delayed/reordered `updated` cannot regress a later state. *assert out-of-order fixture cannot regress state* [Source: FR-B10, NFR-B3]
- [ ] **AC5** — Given the subscribed set, When configured, Then it matches FR-B10 exactly (checkout.session.completed, customer.subscription.created/updated/deleted, invoice.paid/payment_failed/payment_action_required, charge.refunded/dispute.created, customer.updated/deleted) and `trial_will_end` is NOT subscribed (card-free trial); a CI check pins config↔code drift. *assert subscribed set == FR-B10; `trial_will_end` absent* [Source: FR-B10, D-B7]
- [ ] **AC6 (negative)** — Given a failure after journaling, When it errors, Then a non-2xx returns so Stripe retries (72h); poison events raise via the ledger failure count. *assert non-2xx on post-journal failure + poison alert* [Source: FR-B10]

## 🔨 Tasks

- [ ] RED: `TestWebhookPipelineIdempotentOutOfOrderProof` with duplicate + out-of-order + bad-sig fixtures (AC1, AC3, AC4).
- [ ] GREEN: `cmd/webhook` pipeline in `internal/webhook`; raw JSONB journal, `processed_webhook_events` ledger, Stripe re-fetch, transactional apply (AC1–AC5).
- [ ] RED→GREEN: post-journal-failure non-2xx + poison alert (AC6); config↔code drift CI check (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — webhook verify + idempotency ledger + subscription apply + v2 authoritative re-fetch real+tested (server.go:614, workflow.go); checkout.session.completed/invoice.* branches are ErrNotImplemented and no journal-raw step exists.

Tier: `cmd/webhook` transport → `internal/webhook` → Stripe port + store. NestJS analogue: a signature-verified ingest controller. Seam: Stripe webhook (signature over the untouched forwarded body — E4-S4 forwards; this binary is the SOLE verifier for its cell). Sibling: E4-S4 (forwarder), E4-S5 (flip semantics), E6-S4 (subscription sweep covers gaps).

## 🧪 Testing

Named DoD test as integration; Postgres testcontainer for journal+ledger; Stripe port mocked for re-fetch. CS §5: mock the Stripe external; never mock own `internal/webhook`/store. Duplicate + out-of-order fixtures required (feeds the E4-S6 gate).

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (store-driver confinement + determinism), §11 (poison-event handling stated). SE-Arch: idempotent-out-of-order-proof lens (evidence-before-interpretation). cell-lint: rule #11 (idempotency), home-cell sole verifier.

## 🔗 References

PRD Blcvui4UIn FR-B10, NFR-B3, D-S12, D-B7; Architecture o2ZqSrZjE6 A-HOOK.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: webhook-set + processing gates/fixtures).
- [ ] Parent epic: E4. Intra-service: after E2-S1, E8-S1; precedes E4-S4/S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (OQ-B8 dispute/refund entitlement policy — manual assumption).
