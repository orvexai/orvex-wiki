## 🎯 Story

As **the family's server-side gates (BFF, MCP, ai, wiki engine)**, I want **a contracts-owned gated-feature-key enum and delegation rule**, so that **every edge enforces the same Alpha capabilities and a delegated call resolves the acted-for principal's org**.

**Definition of Done:** `TestGatedFeatureKeyEnumAndDelegation` — a service-tier test asserting the five Alpha feature keys (`composer`, `curator_distillation`, `ask_wiki`, `improve_with_ai`, `memory_coach`) are a contracts-owned enum surfaced in `features[]`, and an act-as-user check resolves entitlements for the acted-for principal's org, never the service identity. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the check response, When read, Then `features[]` reflects the contracts-owned enum {composer, curator_distillation, ask_wiki, improve_with_ai, memory_coach}. *assert enum values match contracts; unknown key rejected at compile/contract time* [Source: FR-B6]
- [ ] **AC2** — Given a Free token, When it attempts a paid capability at a consumer edge, Then the gate denies (e.g. MCP `submit_to_curator` blocked). *assert deny for Free on each paid capability* [Source: FR-B6, FR-65/MR-3/4]
- [ ] **AC3 (delegation)** — Given an MCP act-as-user call / ai agentic hop / workload identity acting for a user, When the check runs, Then it keys on the acting delegated principal's org, never the service's own identity or the token minter. *assert resolved principal == acted-for principal* [Source: FR-B6]
- [ ] **AC4** — Given each gated capability, When tested, Then an API-level test exists per capability (client hiding is cosmetic only). *assert one API gate test per capability* [Source: FR-B6, Epic 2.1]
- [ ] **AC5 (negative)** — Given a paid token, When it invokes a paid capability, Then it is allowed (no false lockout — a false lockout is sev-2). *assert allow for paid; false-lockout counter == 0* [Source: FR-B6, G1 counter-metric]

## 🔨 Tasks

- [ ] RED: `TestGatedFeatureKeyEnumAndDelegation` with a delegated-principal fixture (AC1, AC3).
- [ ] GREEN: feature-key enum wired from contracts into the check response `features[]`; delegation-resolution helper in `internal/entitlement` (AC1–AC3).
- [ ] RED→GREEN: per-capability API gate tests (AC4); false-lockout guard (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — GatedFeature enum + AllFeatures() present+tested (gen/catalog.go); act-as-user delegation resolution and per-capability API gates are absent.

Tier: `internal/entitlement` (feature projection + delegation resolver) + transport. NestJS analogue: an authorization guard reading the entitlement. Seam: contracts feature-key enum; delegation crosses the identity principal spine. Sibling: E2-S2 (primitive), E2-S3 (client resolves delegated principal), consumers enforce at their edges.

## 🧪 Testing

Named DoD test at the service tier; per-capability API tests. CS §5: mock no own packages; use real Redis/Postgres containers.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3 (deny-by-default), §11 (false-lockout is a stated sev-2 risk). SE-Arch: authz-consistency lens (one semantics family-wide). cell-lint: delegated checks resolve in-cell.

## 🔗 References

PRD Blcvui4UIn FR-B6, FR-66; Architecture o2ZqSrZjE6 A-CHECK.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: gated-feature-key enum).
- [ ] Parent epic: E2. Intra-service: after E2-S2/S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
