## 🎯 Story

As **the correctness net for every webhook-gap class**, I want **central Temporal reconcile schedules — subscription sweep, cap re-assert, journal checksum — with drift alerts**, so that **no paid entitlement exists without a Stripe state and any drift closes ≤24h (NFR-B5)**.

**Definition of Done:** `TestReconcileSweepClosesDrift` — a service-tier test asserting the `reconcile-subscriptions` and `reconcile-caps` steps detect and close injected drift: (a) subscription records vs the Stripe API list; (b) pushed caps vs billing's policy; (c) Redis counters vs the Postgres journal — raising an ops alert on drift beyond threshold. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the subscription sweep, When it runs, Then billing's subscription records are compared vs the Stripe API list, catching webhook gaps beyond Stripe's 72h retry. *assert an injected missing subscription is detected + reconciled* [Source: FR-B21, A-METER]
- [ ] **AC2** — Given the cap re-assert, When it runs, Then pushed caps are compared vs billing's policy (FR-B7) and re-pushed on divergence. *assert a stale ai cap is re-asserted* [Source: FR-B21, A-CAP]
- [ ] **AC3** — Given the journal checksum, When it runs, Then Redis counters are compared vs the Postgres journal append table; divergence rebuilds counters from the journal. *assert counter/journal drift detected + counters rebuilt* [Source: FR-B21, D-S12]
- [ ] **AC4** — Given drift beyond threshold, When detected, Then an ops alert is raised and reconcile closes it ≤24h (NFR-B5). *assert alert fired + closure within SLA* [Source: FR-B21, NFR-B5]
- [ ] **AC5 (invariant)** — Given the money-correctness invariant, When checked, Then no paid entitlement exists without a corresponding Stripe subscription state or an explicit attributed override. *assert the sweep flags any paid-without-Stripe entitlement* [Source: FR-B21, NFR-B5]

## 🔨 Tasks

- [ ] RED: `TestReconcileSweepClosesDrift` with injected drift per class (AC1, AC2, AC3, AC5).
- [ ] GREEN: `reconcile-subscriptions`/`reconcile-caps` step handlers (E7-S2) with the three comparisons; drift alerting (AC1–AC4).
- [ ] RED→GREEN: money-correctness invariant check (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — orphan-sweep + meter reconcile real+tested (spendcap/orphan_sweep.go, billing/meter.go); subscription/dunning sweep is 'not implemented' (dunning.go:111) and no journal-checksum/drift alerts.

Tier: `internal/reconcile` + step-API handlers (E7-S2). NestJS analogue: scheduled reconciler steps. Seam: schedules are central Temporal in workflows; billing exposes the idempotent steps (D-WF-1). Sibling: E4 (subscription records), E3-S4 (cap push), E6-S1 (journal/counters), E8-S4 (drift alert wiring).

## 🧪 Testing

Named DoD test at the service tier; Postgres + Redis containers; Stripe API list mocked (true external). CS §5: mock Stripe; never mock own `internal/reconcile`.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §10 (invariant-checked money correctness), §11 (reconcile is the correctness net, not optional hardening). SE-Arch: drift-net lens (every gap class covered). cell-lint: reconcile runs per-cell.

## 🔗 References

PRD Blcvui4UIn FR-B21, NFR-B5; Architecture o2ZqSrZjE6 A-METER, A-CAP, §5 (reconcile-is-correctness-net).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: reconcile step envelopes).
- [ ] Parent epic: E6. Intra-service: after E4 (subscriptions), E3-S4 (cap push), E6-S1, E7-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
