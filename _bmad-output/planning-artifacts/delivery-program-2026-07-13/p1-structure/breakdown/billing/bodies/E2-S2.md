## 🎯 Story

As **every server-side gate in the family**, I want **one check primitive `GET /v1/entitlements/{principal_type}/{principal_id}` served fast from an event-evicted Redis cache**, so that **enforcement never sits on a billing round-trip (G3)**.

**Definition of Done:** `TestEntitlementCheckPrimitiveShapeAndEviction` — a service-tier test asserting the two-segment polymorphic route returns the frozen `{plan, plan_version, features[], caps{}, trial{state,endsAt}, throttle{...}, version, evaluatedAt}` shape, is served from Redis, and the cache entry is evicted on an entitlement-change signal before the new version is served. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a principal, When `GET /v1/entitlements/{principal_type}/{principal_id}` is called, Then it returns the contracts-owned shape incl. `version` + `evaluatedAt`. *assert body matches the contracts schema; route is two-segment (the scaffold single-segment literal is corrected)* [Source: FR-B5, arch §8]
- [ ] **AC2** — Given a warm cache, When checked, Then the response is served from Redis (`ent:{principal}`) at p95 ≤ 50 ms service-side. *assert cache hit path + latency budget under load fixture* [Source: FR-B5, NFR-B1]
- [ ] **AC3** — Given an entitlement change, When `billing.entitlement.changed` fires (or the local flip path runs), Then the cache is evicted before the new version is served. *assert the next read returns the new `version`, never a stale one* [Source: FR-B5, A-CHECK]
- [ ] **AC4** — Given the response, When computed, Then allow/deny is NOT cached detached from the entitlement version it derives from. *assert only entitlement state (not a decision) is cached* [Source: FR-B5]
- [ ] **AC5 (negative)** — Given a never-seen principal, When checked, Then the primitive resolves Free (fail-closed for paid features). *assert body.plan == "free" for an unknown principal* [Source: FR-B5, NFR-B4]

## 🔨 Tasks

- [ ] RED: `TestEntitlementCheckPrimitiveShapeAndEviction` (AC1, AC3, AC5).
- [ ] GREEN: handler + `internal/entitlement` service reading the projection (E2-S1) with a Redis cache-fill; event-evict hook (AC1–AC4).
- [ ] RED→GREEN: unknown-principal-Free path (AC5); latency load fixture (AC2).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — check primitive real+tested (server.go:266, entitlement.go Check); Redis event-evicted cache is a scaffold — Get always misses, Set/Evict return ErrNotImplemented (cache/redis.go), Check never consults it.

Tier: transport handler → `internal/entitlement` service → store projection + Redis cache. NestJS analogue: a cache-aside read service. Seam: contracts entitlement-check wire shape. Sibling: E2-S1 (projection), E3-S3 (eviction event), E2-S3 (client consumes this).

## 🧪 Testing

Named DoD test at the service tier; Redis + Postgres testcontainers. CS §5: mock no own packages; Redis/Postgres are real containers. Latency assertion via a benchmark fixture.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §7 (cache-aside + eviction), §10 (never cache decisions). SE-Arch: contract-shape-is-law (two-segment route), G3 hot-path lens. cell-lint: checks resolve in-cell, no cross-cell reads (A-CELL-ROUTING).

## 🔗 References

PRD Blcvui4UIn FR-B5, NFR-B1; Architecture o2ZqSrZjE6 A-CHECK, §8 (entitlement-route literal debt).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: entitlement-check wire shape).
- [ ] Parent epic: E2. Intra-service: after E2-S1; precedes E2-S3/S4, E3-S3, and all consumer gating.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
