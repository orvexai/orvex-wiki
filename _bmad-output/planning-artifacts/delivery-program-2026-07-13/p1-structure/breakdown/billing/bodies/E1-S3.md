## 🎯 Story

As a **UK customer viewing prices**, I want **prices shown VAT-inclusive in GBP with the annual "2 months free" framing carried as catalog metadata**, so that **every surface states the same price and tax posture identically**.

**Definition of Done:** `TestCatalogRendersGBPInclusiveWithAnnualFraming` — a service-layer test asserting `GET /v1/plans` returns GBP-only tax-inclusive display data and the annual plan carries the "2 months free" framing metadata so all surfaces render it identically. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given launch currency is GBP-only (D-S19), When the catalog renders, Then prices display tax-inclusive using Stripe Tax data and only GBP is offered. *assert response currency == "GBP" and `tax_display == "inclusive"`* [Source: FR-B3, D-S19]
- [ ] **AC2** — Given Checkout, When a session is created, Then `automatic_tax: true` is set. *assert checkout session request carries `automatic_tax.enabled == true`* [Source: FR-B3]
- [ ] **AC3** — Given the annual plan, When rendered, Then it carries the "2 months free" framing as catalog display metadata (not computed per-surface). *assert `body.plans[personal].annual.display_metadata` contains the framing string* [Source: FR-B3]
- [ ] **AC4** — Given `currency_options` scaffolds multi-currency, When a second currency (EUR/USD) is not yet added, Then the ≥2-jurisdiction VAT verification is deferred and is NOT a launch blocker. *assert only GBP is returned and no EUR/USD path is exercised at launch* [Source: FR-B3, OQ-B6 closed by D-S19]
- [ ] **AC5 (negative)** — Given a plan-version missing GBP `currency_options`, When rendered, Then it fails closed (500 + alert), never renders a currency-less price. *assert 500 on a currency-less plan-version* [Source: FR-B3, CS §11]

## 🔨 Tasks

- [ ] RED: `TestCatalogRendersGBPInclusiveWithAnnualFraming` (AC1, AC3).
- [ ] GREEN: GBP-only inclusive display data on the catalog DTO; annual framing metadata; `automatic_tax` on the checkout-session builder shared with E4-S1 (AC1–AC3).
- [ ] RED→GREEN: currency-less-plan fail-closed (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — automatic_tax=true wired+tested in CreateCheckoutSession (stripe.go:275); pricing display (needs /v1/plans stub) and annual '2 months free' metadata are absent.

Tier: `internal/catalog` display projection + the shared Stripe checkout-session builder. NestJS analogue: a presentation DTO mapper. Seam: contracts plan-catalog DTO carries display metadata. Sibling: E1-S1 (catalog), E4-S1 (checkout `automatic_tax`).

## 🧪 Testing

Named DoD test at the service tier. CS §5: mock only the Stripe port; no own-package mocks. The framing string is data, verified against the contracts fixture.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §10 (framing/currency are data), §11 (currency-less failure stated). SE-Arch: no-fake-done (multi-currency deferral is explicit, not silently missing). cell-lint: display route cluster-internal.

## 🔗 References

PRD Blcvui4UIn FR-B3, D-S19, OQ-B6; Architecture o2ZqSrZjE6 A-PRICE.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG).
- [ ] Parent epic: E1. Intra-service: after E1-S1; shares the checkout-session builder with E4-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (OQ-B9 pricing-page ownership stays product-owned).
