## 🎯 Story

As **orvex-studio-workflows driving the trial saga**, I want **billing to expose `trial-start` and `trial-expire` step APIs and flip on expiry exactly like a lapse**, so that **the trial lifecycle is a central Temporal saga and billing runs no worker (D-WF-1)**.

**Definition of Done:** `TestTrialExpiryFlipEqualsLapse` — a service-tier test asserting the `trial-expire` step flips the entitlement to Free exactly like an FR-66 lapse (AI features lock, nothing deleted, over-quota tenant goes read-only via the graduated ladder), and both `trial-start`/`trial-expire` are idempotent `WorkloadIdentityOnly` steps. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given workflows' dispatcher, When `studio.user.graduated` fires (fallback: first-sign-in), Then it calls the `trial-start` step which grants via E5-S1. *assert trial-start invoked on graduation event* [Source: FR-B14, A-TRIAL]
- [ ] **AC2** — Given the one-month window, When the workflow's timer fires, Then the `trial-expire` step flips the entitlement to Free — AI features lock, nothing deleted. *assert Free flip + no deletion on expire* [Source: FR-B14, G5]
- [ ] **AC3** — Given expiry, When flipped, Then the trial clock does NOT pause for pending imports; the Curator's staging-stop is ITS consumption of `billing.trial.expired`/`billing.entitlement.changed`, not billing logic. *assert billing emits the events; no Curator logic in billing* [Source: FR-B14]
- [ ] **AC4** — Given both step APIs, When called, Then they are `WorkloadIdentityOnly` + idempotent (a replay is a no-op). *assert workload-identity enforced + replay no-op* [Source: FR-B14, FR-B25, NFR-B3]
- [ ] **AC5 (negative)** — Given billing, When the trial saga runs, Then billing runs NO Temporal worker — it exposes step APIs only. *assert no worker binary; steps are HTTP callbacks* [Source: FR-B14, D-WF-1]

## 🔨 Tasks

- [ ] RED: `TestTrialExpiryFlipEqualsLapse` (AC2, AC4).
- [ ] GREEN: `trial-start`/`trial-expire` step handlers on `/internal/v1/steps/*` (E7-S2 framework); expiry flip reusing E4-S5 flip shape (AC1–AC4).
- [ ] RED→GREEN: emit `billing.trial.started`/`billing.trial.expired` events (AC3); confirm no worker (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — step API is a 501 stub (server.go:174); no trial-start/trial-expire, no expiry-flip lapse.

Tier: `internal/trial` + step-API handlers (E7-S2). NestJS analogue: a callback-target controller. Seam: workflows dispatcher consumes `studio.user.graduated` + `billing.*`; billing exposes idempotent step APIs (D-WF-1). Sibling: E5-S1 (grant), E5-S3 (throttle), E7-S2 (step framework), E4-S5 (flip shape).

## 🧪 Testing

Named DoD test at the service tier; step-API idempotency exercised. CS §5: no own-package mocks; workflows is a true external caller (workload-identity fixture).

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3 (callback-target pattern), §11 (no-worker discipline). SE-Arch: central-saga lens (D-WF-1), lapse-shape reuse. cell-lint: rule #11 idempotency on `/internal/*`.

## 🔗 References

PRD Blcvui4UIn FR-B14, FR-B25, D-WF-1; Architecture o2ZqSrZjE6 A-TRIAL.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `studio.user.graduated` type + step envelopes).
- [ ] Parent epic: E5. Intra-service: after E5-S1, E7-S2, E4-S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
