## 🎯 Story

As **billing's finance record**, I want **`cmd/metering` to consume `ai.usage.recorded` into an append-only Postgres journal plus Redis counters**, so that **usage is a billing-grade finance record and throttle/budget counters are maintained without any enforcement role**.

**Definition of Done:** `TestMeteringJournalsAppendOnlyAndCounts` — an integration test asserting the Trigger-fed worker consumes `ai.usage.recorded {tenant, principal, feature, model, tokens, cost, latency}` into a range-partitioned Postgres append table (never updated/deleted) AND increments Redis fast counters (throttle windows, curator budget, demo counts) keyed by cost-center. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `ai.usage.recorded` on the spine, When `cmd/metering` consumes it, Then it appends to a range-partitioned Postgres journal table (DROP-PARTITION retention; Mongo struck, D-S12). *assert append-only row written; no Mongo* [Source: FR-B18, D-S12]
- [ ] **AC2** — Given the same event, When processed, Then Redis fast counters (trial throttle windows, curator monthly budget, demo action counts) increment keyed by cost-center. *assert counters incremented per cost-center* [Source: FR-B18]
- [ ] **AC3** — Given the journal, When written, Then it is never updated, never deleted, and pseudonymizable per FR-B22 (carries principal ids, never message/content payloads). *assert no update/delete path; no content payload column* [Source: FR-B18, NFR-B7]
- [ ] **AC4** — Given a journal/Redis outage, When it occurs, Then enforcement is unaffected (LiteLLM + ai pre-flight unaffected; events buffer via spine retry/DLQ; Redis loss rebuilds from the journal). *assert outage is a staleness event, not a correctness event* [Source: FR-B18, A-METER]
- [ ] **AC5 (negative)** — Given a poison `ai.usage.recorded`, When retries exhaust, Then it lands on the Trigger's `deadLetterSink` DLQ with depth alerting, never silently dropped. *assert poison event → DLQ + alert* [Source: FR-B18, arch §4]

## 🔨 Tasks

- [ ] RED: `TestMeteringJournalsAppendOnlyAndCounts` (AC1, AC2).
- [ ] GREEN: `cmd/metering` Trigger consumer + `internal/meter` journal writer (range-partitioned) + counter increments (AC1–AC3).
- [ ] RED→GREEN: DLQ `deadLetterSink` + depth alert (AC5); outage-tolerance path (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — metering worker → Postgres usage journal append real+tested (cmd/metering, migration 0004, record_usage_test); Redis fast counters not implemented (prior spend summed from Postgres).

Tier: `cmd/metering` (Knative min-scale-0) → `internal/meter` → Postgres append table + Redis. NestJS analogue: an event consumer. Seam: `studio-spine` Trigger; the DLQ deadLetterSink is a reconciliation-debt item (arch §4/§8). Sibling: E6-S2 (cost-center), E5-S3 (throttle reads counters), E8-S1/S2 (persistence + spine).

## 🧪 Testing

Named DoD test as integration; Postgres + Redis containers + a spine test double. CS §5: the spine broker is a true external (test double); never mock own `internal/meter`/store.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (append-only journal, partition retention), §11 (DLQ, poison handling). SE-Arch: finance-never-enforcement lens (A-METER). cell-lint: journal partitions are cell-local (feeds TENANT_MOVE.md).

## 🔗 References

PRD Blcvui4UIn FR-B18, D-S12, NFR-B7; Architecture o2ZqSrZjE6 A-METER, §4 (DLQ debt).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `ai.usage.recorded` schema).
- [ ] Parent epic: E6. Intra-service: after E8-S1/S2; feeds E5-S3, E6-S2/S3/S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
