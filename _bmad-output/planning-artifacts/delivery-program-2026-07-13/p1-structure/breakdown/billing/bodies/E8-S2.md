## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As **the event-driven billing service**, I want **to publish/consume CloudEvents on `studio-spine` via the transactional outbox with the `orvexcell` fail-closed extension**, so that **billing ships event-driven from day one (D-B10) with no Redis→Kafka bridge**.

**Definition of Done:** `TestSpinePublishConsumeOrvexcellFailClosed` — an integration test asserting billing publishes `billing.subscription.updated`/`billing.entitlement.changed`/`billing.trial.started`/`billing.trial.expired`/`billing.payment.failed` via the transactional outbox and consumes `ai.usage.recorded`/`ai.cap.reached`/`ai.cap.warning`/`studio.user.graduated`, with every published event carrying the `orvexcell` extension and a consumer receiving one lacking it failing closed. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given billing flips/lifecycle events, When they occur, Then it publishes the five `billing.*` CloudEvents on `studio-spine` (schemas in contracts), superseding the trashed docmost-server-owned `billing.subscription.updated`. *assert the five event types publish* [Source: FR-B27]
- [ ] **AC2** — Given the spine, When consumed, Then billing consumes `ai.usage.recorded`, `ai.cap.reached`, `ai.cap.warning`, `studio.user.graduated`. *assert all four consumed types wired to `cmd/metering`* [Source: FR-B27]
- [ ] **AC3** — Given the outbox mechanism (D-B10), When an event is published, Then the outbox row is written in the same DB transaction as the change and a relay publishes to the Broker (the Redis→Kafka bridge is retired — the outbox IS the mechanism). *assert transactional outbox write + relay publish; no bridge* [Source: FR-B27, D-B10]
- [ ] **AC4** — Given a published CloudEvent, When emitted, Then it carries the `orvexcell` extension; a consumer receiving one lacking it fails closed (cell contract rule #6). *assert `orvexcell` present; consumer rejects on absence* [Source: FR-B27, arch §6]
- [ ] **AC5 (negative)** — Given the sync cap push (FR-B7), When the spine is degraded, Then money correctness is unaffected (the money path has zero spine dependency; the spine degrades throttle/journal freshness only). *assert money path independent of spine* [Source: FR-B27, arch §5]

## 🔨 Tasks

- [ ] RED: `TestSpinePublishConsumeOrvexcellFailClosed` (AC1, AC2, AC4).
- [ ] GREEN: transactional outbox + relay/SinkBinding publisher in `internal/event`; Trigger consumers on `cmd/metering`; `orvexcell` extension on publish (AC1–AC4).
- [ ] RED→GREEN: spine-degraded money-path independence assertion (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** present — events published+consumed on the spine as CloudEvents with the orvexcell extension (event/relay.go, workspace_delete_consumer.go), real+integration-tested; broker + kafka-trigger manifests present.

Tier: `internal/event` (outbox + relay) + `cmd/metering` consumers. NestJS/engine analogue: the engine transactional-outbox relay is billing's launch prerequisite (D-B10). Seam: `studio-spine` Broker/Triggers (schemas in contracts `events/`); the outbox is the mechanism. Sibling: E3-S3 (publishes entitlement.changed), E6-S1 (consumes usage), E8-S3 (cell contract).

## 🧪 Testing

Named DoD test as integration; Postgres testcontainer for the outbox + a spine test double (in-memory Broker or Kafka container). CS §5: the Broker is a true external (test double); never mock own `internal/event`.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (outbox determinism), §11 (spine-degraded posture stated). SE-Arch: outbox-not-bridge lens (Principle 2, D-B10). cell-lint: rule #6 `orvexcell` fail-closed.

## 🔗 References

PRD Blcvui4UIn FR-B27, D-B10; Architecture o2ZqSrZjE6 §6 (rule #6), §5 (money-path spine-independence).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `billing.*` + consumed event schemas). D-B10 engine outbox relay prerequisite (program critical path).
- [ ] Parent epic: E8. Intra-service: foundational for E3-S3, E6-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (D-B10 outbox-relay delivery slipping slips billing's launch — owned in the program plan).
