## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As **a subscribing user**, I want **`POST /v1/checkout` to mint a Stripe Checkout Session bound to my tenant with the principal set server-side**, so that **I pay for the right plan and no card data ever touches Orvex (SAQ-A)**.

**Definition of Done:** `TestCheckoutSessionServerSetPrincipalAndAuthz` — a service-tier test asserting `POST /v1/checkout {plan_interval, success_url, cancel_url}` creates a `subscription`-mode session using the interval's lookup-key Price with `automatic_tax: true`, binds an existing-or-new Customer, sets `metadata.principal` from the VERIFIED identity principal (never client-supplied), and requires the tenant owner/admin role. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an authenticated owner, When `POST /v1/checkout` is called, Then a Checkout Session is created: mode `subscription`, the interval's lookup-key Price, `automatic_tax: true`, existing-or-new Customer bound to the tenant. *assert session request fields* [Source: FR-B8, A-PRICE]
- [ ] **AC2** — Given the session, When built, Then `metadata.principal` is set server-side from the verified identity principal, never from the request body. *assert principal derived from the token, not the payload* [Source: FR-B8, NFR-B6]
- [ ] **AC3** — Given the caller, When authorized, Then they must hold the tenant's owner/admin role per the identity principal (trivially satisfied for a user tenant billing itself). *assert non-owner → 403* [Source: FR-B8]
- [ ] **AC4** — Given the standard free-month trial, When it is active, Then NO card is required and NO Checkout session/Stripe subscription exists for the trial; a card is collected only when the user actively subscribes to £7 Personal. *assert trial path does not create a Checkout session* [Source: FR-B8, D-B7]
- [ ] **AC5** — Given a re-subscribe after lapse, When checkout runs, Then it reuses the existing Customer (payment-history continuity). *assert existing Customer reused, not duplicated* [Source: FR-B8]
- [ ] **AC6 (negative)** — Given no card data path, When any surface is used, Then card data never touches Orvex (Stripe-hosted only, SAQ-A). *assert no PAN fields cross the service boundary* [Source: FR-B8, NFR-B6]

## 🔨 Tasks

- [ ] RED: `TestCheckoutSessionServerSetPrincipalAndAuthz` with a spoofed body principal (AC2) + non-owner (AC3).
- [ ] GREEN: `POST /v1/checkout` handler + `internal/checkout` service calling the Stripe port; server-set principal; owner/admin authz (AC1–AC3, AC5).
- [ ] RED→GREEN: trial-no-card path (AC4); SAQ-A boundary assertion (AC6).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** present — POST /v1/checkout real+tested — server-set metadata.principal, purchase authz via verified bearer, CreateCheckoutSession (server.go:437, stripe.go:237).

Tier: transport handler → `internal/checkout` → Stripe port. NestJS analogue: an authenticated command controller over an external-client adapter. Seam: Stripe true-external port; identity principal for authz + `metadata.principal`. Sibling: E1-S2 (lookup keys), E1-S3 (`automatic_tax`), E4-S3 (webhook binds Customer↔principal).

## 🧪 Testing

Named DoD test at the service tier; Stripe port mocked (true external). CS §5: never mock own packages; the identity verifier is via `lib/auth` (own family lib — use a real test verifier/fixture, not a mock of billing's own code).

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3.2 (network seam), §6 (server-set trust boundary — never trust client principal), §11. SE-Arch: SAQ-A / no-card lens, authz-at-edge. cell-lint: `/v1/*` cluster-internal within the cell.

## 🔗 References

PRD Blcvui4UIn FR-B8, NFR-B6, D-B7; Architecture o2ZqSrZjE6 A-PRICE, A-AUTH.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: checkout request/response shape).
- [ ] Parent epic: E4. Intra-service: after E1-S2/S3, E7-S1 (auth); precedes E4-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
