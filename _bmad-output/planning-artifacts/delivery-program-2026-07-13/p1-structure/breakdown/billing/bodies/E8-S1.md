## 🎯 Story

As **the billing datastore layer**, I want **Postgres (CNPG) + Redis only, with no Mongo and purge-safe cache keys**, so that **the persistence split matches D-S12 and the reconciliation debt (the struck-Mongo apparatus) is deleted, not deferred**.

**Definition of Done:** `TestPersistenceSplitNoMongoPurgeSafeKeys` — a store/integration test asserting all billing state lives in Postgres tables + the former-Mongo stores as Postgres append tables, Redis holds only cache/counters/idempotency-keys, every cache key embeds the principal (purge-safe), allow/deny is never cached, and NO Mongo driver/credential exists anywhere. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given persistence, When provisioned, Then Postgres (CNPG) holds plans/plan_versions, stripe_customers, subscriptions, entitlement_versions + current projection, trial_grants, cap_policies, processed_webhook_events, teams_interest, PLUS raw webhook payloads (JSONB), usage journal, decision audit as range-partitioned append tables. *assert all tables present in Postgres* [Source: FR-B26, D-S12]
- [ ] **AC2** — Given Redis, When used, Then it holds only the event-evicted check cache, usage fast counters, throttle windows, and step idempotency keys. *assert Redis usage is limited to these; no source-of-truth in Redis* [Source: FR-B26]
- [ ] **AC3** — Given every cache key, When formed, Then it embeds the principal (purge-safe) and allow/deny outcomes are never cached. *assert principal in every key; no decision cached* [Source: FR-B26]
- [ ] **AC4 (reconciliation debt)** — Given the struck-Mongo apparatus, When the repo is scanned, Then `MongoURI`, the `studio-billing-mongodb-credentials` ExternalSecret, `mongo-claim.yaml` NOTEs, and Mongo README/header text are DELETED (not deferred). *CI/static assert: no `mongo` reference outside a historical note* [Source: FR-B26, D-S12, arch §8]
- [ ] **AC5 (negative)** — Given a store-driver, When placed, Then `pgx`/redis clients are confined to `internal/store/*` (no driver import outside its package, CS §5). *static assert: no store-driver import outside `internal/store`* [Source: FR-B26, arch §4]

## 🔨 Tasks

- [ ] RED: `TestPersistenceSplitNoMongoPurgeSafeKeys` + a no-Mongo static gate (AC1, AC4, AC5).
- [ ] GREEN: consolidate all tables on the CNPG claim; Redis-only-cache discipline; principal-embedded cache keys (AC1–AC3).
- [ ] RED→GREEN: delete the Mongo apparatus (AC4); store-driver confinement gate (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — Postgres persistence real+tested (schema.go, 5 migrations, no Mongo); Redis is a scaffold (no go-redis client wired) though keys are purge-safe by design.

Tier: `internal/store/*` + deploy manifests. NestJS analogue: a repository layer + infra claims. Seam: D-S12 (Postgres-only) — but the family-wide Mongo canon reconcile (CS §5–10 vs Principle 5/D-S12) is an OPEN decision billing cannot settle alone (arch §9); billing follows D-S12. Sibling: every store-writing story; E6-S1 (journal partitions).

## 🧪 Testing

Named DoD test at the store tier against Postgres + Redis testcontainers. CS §5: NEVER mock own store; real containers. Static no-Mongo + store-driver-confinement gates in CI.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (store-driver confinement), §7 (Redis-as-cache-only), §11 (family Mongo reconcile is OPEN, stated). SE-Arch: D-S12 store-engine ADR trigger (arch §7); strike-not-defer lens. cell-lint: rule #10 — per-cell datastores (feeds TENANT_MOVE.md, E8-S3).

## 🔗 References

PRD Blcvui4UIn FR-B26, D-S12; Architecture o2ZqSrZjE6 §4, §8 (Mongo strike debt), §9 (family reconcile OPEN).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG). D-S12 store-engine ADR (arch §7).
- [ ] Parent epic: E8. Intra-service: foundational — precedes E2/E4/E6 store work.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (family-wide Mongo canon reconcile — cannot be settled billing-only).
