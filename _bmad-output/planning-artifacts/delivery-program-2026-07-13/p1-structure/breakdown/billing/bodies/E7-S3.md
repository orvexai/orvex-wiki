## 🎯 Story

As **the family user-purge saga**, I want **a billing purge step that cancels the subscription, deletes the Stripe Customer, and pseudonymizes the principal**, so that **GDPR Art. 17 is honored while finance aggregates survive the legal-retention carve-out**.

**Definition of Done:** `TestPurgeStepPseudonymizesFinanceSurvives` — a service-tier test asserting the `purge-stripe-linkage` step cancels any live subscription, deletes the Stripe Customer (invoices retained Stripe-side), erases the local Customer↔principal linkage, and pseudonymizes the principal across the usage journal + entitlement history (aggregates survive; the person does not). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the purge saga's billing step, When run, Then it cancels any live subscription and deletes the Stripe Customer (Stripe cascades cancellation; invoices retained Stripe-side under accounting law). *assert subscription cancelled + Customer deleted* [Source: FR-B22, A-GDPR]
- [ ] **AC2** — Given the step, When run, Then the local Customer↔principal linkage is erased and the principal is pseudonymized across the usage journal + entitlement history (finance/audit aggregates survive; the person does not — Art. 17(3)(b) carve-out documented in contracts). *assert principal pseudonymized; aggregate rows retained* [Source: FR-B22, NFR-B7]
- [ ] **AC3** — Given trial-dedupe, When purged, Then the trial-dedupe email hash is deleted with the purge [ASSUMPTION: once-per-email protection does not survive account deletion — flagged to PO]. *assert email-HMAC row deleted; assumption flagged* [Source: FR-B22]
- [ ] **AC4** — Given Stripe as a subprocessor, When compliance is assessed, Then Stripe is on the DPA/subprocessor list (a legal deliverable, tracked launch-blocking). *assert Stripe listed in the DPA register* [Source: FR-B22]
- [ ] **AC5 (negative)** — Given the pseudonymization, When applied, Then no message/content payload was ever stored to leak (journal carries principal ids only), and the step is idempotent. *assert no content payloads exist; replay is a no-op* [Source: FR-B22, NFR-B7]

## 🔨 Tasks

- [ ] RED: `TestPurgeStepPseudonymizesFinanceSurvives` (AC1, AC2, AC5).
- [ ] GREEN: `purge-stripe-linkage` step handler (E7-S2) — Stripe cancel+delete, linkage erase, journal+history pseudonymize (AC1–AC3).
- [ ] Coordinate: Stripe DPA/subprocessor listing (AC4); flag the trial-dedupe-hash assumption to PO (AC3).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — no GDPR purge step (no cancel-sub/delete-Customer/pseudonymize flow anywhere).

Tier: `internal/purge` + step handler. NestJS analogue: a saga-step controller. Seam: the family purge saga is central Temporal (workflows); billing exposes the idempotent step (D-WF-1). Sibling: E7-S2 (step framework), E6-S1 (journal), E2-S1 (history), E4-S1 (Customer linkage).

## 🧪 Testing

Named DoD test at the service tier; Stripe port mocked (true external); Postgres testcontainer for pseudonymization. CS §5: mock Stripe; never mock own `internal/purge`.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §6 (privacy, pseudonymization, no PII/content at rest), §11 (the trial-dedupe-hash assumption stated, not papered over). SE-Arch: GDPR-carve-out lens (finance survives, person does not). cell-lint: purge runs per-cell over cell-local rows.

## 🔗 References

PRD Blcvui4UIn FR-B22, NFR-B7; Architecture o2ZqSrZjE6 A-GDPR.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: purge step + Art. 17(3)(b) carve-out doc).
- [ ] Parent epic: E7. Intra-service: after E7-S2, E6-S1, E2-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (FR-B22 trial-dedupe-hash-on-purge assumption — flag to PO).
