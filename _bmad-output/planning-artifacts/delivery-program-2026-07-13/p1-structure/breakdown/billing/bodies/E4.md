## 🗺️ Feature area

The money path — "a proven fact" (G2, Epic 2.5). This epic owns the **Stripe integration**: `POST /v1/checkout` and `POST /v1/portal` session mint, the **webhook receiver** (`cmd/webhook`) as the *only* write door from Stripe into the system-of-record (A-HOOK: verify → journal-raw → idempotency ledger → authoritative re-fetch → state-machine apply), the **global-tier thin forwarder + home-cell routing** (A-CELL-ROUTING, D-B12 — one global Stripe, per-cell billing), the **flip semantics** (one entitlement version per lifecycle transition + `GET /v1/billing-state`), and the **red-blocking test-clock CI gate**. Out-of-order-proof by authoritative re-fetch; idempotent by Stripe event id; SAQ-A — no card data ever touches Orvex.

Governing canon: FR-B8/B9/B10/B11/B12, A-HOOK, A-CELL-ROUTING, A-PRICE, NFR-B2/B3/B6.

## 📋 Story roster

- [ ] E4-S1 — `POST /v1/checkout` — Checkout session, server-set `metadata.principal`, purchase authz [FR-B8, NFR-B6]
- [ ] E4-S2 — `POST /v1/portal` — Customer Portal session + config-as-code proration policy [FR-B9]
- [ ] E4-S3 — Webhook pipeline: verify → journal-raw → idempotency ledger → authoritative re-fetch → apply [FR-B10, NFR-B3]
- [ ] E4-S4 — Global-tier thin Stripe forwarder + home-cell routing via registry (A-CELL-ROUTING) [FR-B10]
- [ ] E4-S5 — Flip semantics — one version per lifecycle transition + `GET /v1/billing-state` [FR-B11, NFR-B2]
- [ ] E4-S6 — Test-clock money-path E2E CI gate (monthly+annual+dunning+cancel+re-subscribe+retro), red-blocking [FR-B12]

## 🎯 FR ids owned

FR-B8, FR-B9, FR-B10, FR-B11, FR-B12.

## ✅ Definition of Done

- [ ] All six stories Done.
- [ ] Family-E2E share green: signup → Graduation → trial → checkout (monthly AND annual) → entitled → cancel → lapse → re-subscribe → retroactive-trial fixture, live against Stripe test mode with test clocks.
- [ ] SDD lines evidenced: a duplicate webhook is a constant-200 no-op; a delayed/reordered `updated` cannot regress state (authoritative re-fetch); `metadata.principal` is server-set from the verified identity principal, never client-supplied; the home cell is the sole signature verifier (the forwarder holds no secret, verifies nothing); dunning holds 14 days in `past_due` then auto-downgrades to Free, never deletes (G5, D-S19).
- [ ] The test-clock E2E gate is red-blocking with no override (FR-74 discipline).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: webhook-processing gates/fixtures, billing.* event schemas, cell-routing registry client shape). D-B10 spine prerequisite (engine outbox relay) on the program critical path.
- [ ] Intra-service: E1-S2 (lookup keys), E2-S1/S2 (entitlement store + primitive), E3-S3 (propagation) precede this; E4-S3 precedes E4-S4/S5; E4-S6 gates the whole epic.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (OQ-B8 dispute policy, OQ-B12 dev/crew webhook delivery).
