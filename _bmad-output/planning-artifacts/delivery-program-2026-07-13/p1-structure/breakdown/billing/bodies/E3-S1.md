## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As **the plan→cap policy owner**, I want **named caps riding each plan-version, including two SEPARATE AI cap lines**, so that **finance can retune caps as auditable data and background embeddings never draw down the user-action cap**.

**Definition of Done:** `TestCapPolicyTwoAILinesPerPlanVersion` — a store/service test asserting each plan-version carries `ai_monthly_budget` (user-action) AND a SEPARATE sibling `embedding_monthly_budget` (sized from page quota), plus `curator_distillation_monthly: 1000`, `trial_weekly_actions: 100/150`, `demo_ai_actions: 20`, served in the `caps{}` block and never nested. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a plan-version, When cap policy is read, Then `caps{}` carries `ai_monthly_budget` and a SEPARATE `embedding_monthly_budget` as sibling lines (D-S15), not one nested figure. *assert two distinct cap keys, neither nested under the other* [Source: FR-B17, D-S15]
- [ ] **AC2** — Given Free, When read, Then it exposes the ~zero-cost AI set (cheap models + free/near-free embeddings, no frontier, no lifetime action count) per D-B13; Personal carries the spend cap. *assert Free carries no user-action count cap and no frontier access; Personal carries `ai_monthly_budget`* [Source: FR-B17, D-B13]
- [ ] **AC3** — Given `curator_distillation_monthly`, `trial_weekly_actions` (100 advisory / 150 throttle), `demo_ai_actions` (20), When read, Then all are served in `caps{}` as data. *assert all named caps present with the pinned Alpha values* [Source: FR-B17]
- [ ] **AC4** — Given a cap retune, When applied, Then it is a NEW plan-version (auditable), never an in-place UPDATE, and requires ADR + human sign-off. *assert a retune produces a new plan-version row (CS ❌#10)* [Source: FR-B17, A-CAP]
- [ ] **AC5 (negative)** — Given demo actions, When journaled, Then they hit a platform-funded cost-center, never billed against a user entitlement. *assert demo usage is not drawn from `ai_monthly_budget`* [Source: FR-B17]

## 🔨 Tasks

- [ ] RED: `TestCapPolicyTwoAILinesPerPlanVersion` (AC1, AC3).
- [ ] GREEN: `cap_policies` embedded per plan-version in `internal/catalog`; served in the check `caps{}` (AC1–AC3).
- [ ] RED→GREEN: new-plan-version-on-retune path (AC4); demo cost-center separation (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** present — cap policy block (two AI cap lines + curator/trial/demo caps) per plan + resolver, tested (gen/catalog.go). Note: static code catalog, not a per-plan-version DB registry.

Tier: `internal/catalog` (cap policy on plan-version) + check projection. NestJS analogue: a policy value object on the plan aggregate. Seam: caps ride A-ENT plan-versions; the embedding budget's LiteLLM enforcement lives in ai (sibling scoped key + content-hash skip). Sibling: E3-S2 (wiki quota siblings), E3-S4 (pushes both numbers).

## 🧪 Testing

Named DoD test at the store/service tier; Postgres testcontainer. CS §5: no own-package mocks.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §10 (caps are data + a raise is ADR+sign-off, CS ❌#10), §11 (Free AI-cost doctrine stated). SE-Arch: no-nested-cap lens (D-S15). cell-lint: caps served in-cell.

## 🔗 References

PRD Blcvui4UIn FR-B17, D-S15, D-B13; Architecture o2ZqSrZjE6 A-CAP.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: cap-key names in the check shape).
- [ ] Parent epic: E3. Intra-service: after E1-S1/E2-S1; precedes E3-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (OQ-B3 cap-reached UX/reset; OQ-B4 embedding-key seam — co-owned with ai OQ-AI2).
