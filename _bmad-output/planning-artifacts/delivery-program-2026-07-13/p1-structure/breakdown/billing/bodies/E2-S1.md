## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **billing system-of-record**, I want **the entitlement stored as append-only versioned records keyed on a polymorphic user|org principal in migration 001**, so that **grandfathering is un-retrofittable and history is never rewritten (G4)**.

**Definition of Done:** `TestEntitlementVersionAppendOnlyProjection` — a store-tier test asserting a new entitlement version inserts (never updates) and the `current_entitlements` projection is maintained transactionally, carrying `principal_type` (user|org) + `plan_version_ref` + `source` + `superseded_by`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given migration 001, When applied, Then `entitlement_versions` (`principal_type`, `principal_id`, `plan_version_ref`, `trial_state`, `source ∈ {stripe_event,trial,admin_override,migration}`, cause ref, `effective_from`, `superseded_by`) + a `current_entitlements` projection exist. *assert schema present + `principal_type` is a load-bearing NOT-NULL column* [Source: FR-B4, A-ENT]
- [ ] **AC2** — Given an entitlement change, When written, Then it inserts a new version and updates the projection in one transaction; no prior version row is mutated. *assert version count increments, prior rows byte-identical, projection points to the new version* [Source: FR-B4, A-ENT]
- [ ] **AC3** — Given a free consumer, When a version is written, Then `principal_type == user` (Clerk personal account, NO org); org rows are also representable (Teams). *assert both `user` and `org` rows insert with no schema migration between them* [Source: FR-B4, D-B2, D-S17]
- [ ] **AC4** — Given every version write, When persisted, Then it records its cause (Stripe event id / saga step id / admin actor). *assert `cause_ref` non-null on every version* [Source: FR-B4]
- [ ] **AC5 (negative)** — Given an attempt to UPDATE a superseded version's caps/features, When executed, Then it is rejected (append-only invariant). *assert an UPDATE on a historical version errors or is blocked by constraint/trigger* [Source: FR-B4, G4]

## 🔨 Tasks

- [ ] RED: `TestEntitlementVersionAppendOnlyProjection` (AC2, AC5).
- [ ] GREEN: migration 001 + `internal/store/entitlement` repository with transactional insert-and-project (AC1–AC4).
- [ ] RED→GREEN: append-only enforcement (constraint/trigger or repo guard) (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** present — entitlement_versions schema (migration 0001) + append-only projection (postgres.go:112-218) + polymorphic principal, with flip/append integration tests.

Tier: store (`internal/store/entitlement`) + migrations. NestJS analogue: a versioned aggregate repository. Seam: the polymorphic principal is the D-IDENT-1 tenant key. Sibling: E2-S2 reads the projection; E3 writes cap-carrying versions; E4-S5/E5 write flip/trial versions. Note: the personal→Teams upgrade pass (org mint + re-key) is a named identity+billing joint workstream — schema supports it here; the migration flow itself is *TBD — pinned by the Definition Pack (ENG-2100)* and tracked with identity.

## 🧪 Testing

Named DoD test at the store tier against a Postgres testcontainer. CS §5: NEVER mock own store packages — use a real Postgres. Migration idempotency test included.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (store-driver `pgx` confined to `internal/store/*`), §10 (append-only, no destructive update), §11 (upgrade-pass gap stated). SE-Arch: the un-retrofittable-property lens (A-ENT lands in migration 001). cell-lint: rule #10 — these are cell-local tenant rows (feeds `TENANT_MOVE.md`, E8-S3).

## 🔗 References

PRD Blcvui4UIn FR-B4, D-B2, D-S17; Architecture o2ZqSrZjE6 A-ENT, §8 (tenancy-literal reconciliation debt).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: entitlement DTO + source enum).
- [ ] Parent epic: E2. Intra-service: after E1-S1 (plan-versions); precedes E2-S2 and all version-writing stories.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (personal→Teams upgrade-pass design — joint with identity).
