## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As **a subscriber managing my plan**, I want **`POST /v1/portal` to mint a Stripe Customer Portal session**, so that **I can manage/cancel/switch interval/update payment method on Stripe-hosted surfaces**.

**Definition of Done:** `TestPortalSessionConfigAsCode` — a service-tier test asserting `POST /v1/portal` mints a Portal session for the caller's tenant Customer, using config-as-code Portal configuration (allowed operations + monthly↔annual proration behavior) pinned in code and named in the contracts delta. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an authenticated tenant owner with a Customer, When `POST /v1/portal` is called, Then a Portal session is minted (manage/cancel/interval-switch/payment-method). *assert a Portal session URL is returned for the caller's Customer* [Source: FR-B9]
- [ ] **AC2** — Given Portal configuration, When applied, Then which operations are allowed and the monthly↔annual proration behavior are pinned in config-as-code and named in the contracts delta. *assert config is code-defined, not console-set ad hoc* [Source: FR-B9]
- [ ] **AC3 (authz)** — Given a caller without owner/admin on the tenant, When they request a portal, Then 403 (no cross-tenant portal access). *assert non-owner → 403* [Source: FR-B9, FR-B24]
- [ ] **AC4 (negative)** — Given a tenant with no Stripe Customer, When they request a portal, Then a clean error (no Customer to manage), never a foreign Customer. *assert 409/404 with no leaked Customer* [Source: FR-B9]

## 🔨 Tasks

- [ ] RED: `TestPortalSessionConfigAsCode` (AC1, AC2).
- [ ] GREEN: `POST /v1/portal` handler + `internal/portal` service calling the Stripe port with config-as-code Portal config; owner/admin authz (AC1–AC3).
- [ ] RED→GREEN: no-Customer error path (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** present — POST /v1/portal real+tested (server.go:571, stripe.go:372); config-as-code proration policy not evidenced in-repo.

Tier: transport handler → `internal/portal` → Stripe port. NestJS analogue: an authenticated command controller. Seam: Stripe Portal config-as-code (named in contracts delta). Sibling: E4-S1 (Customer bind), E7-S1 (authz).

## 🧪 Testing

Named DoD test at the service tier; Stripe port mocked. CS §5: no own-package mocks; real `lib/auth` verifier fixture.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3.2 (network seam), §10 (Portal config as code, not clicked-in). SE-Arch: config-as-code lens, authz-at-edge. cell-lint: `/v1/*` cluster-internal.

## 🔗 References

PRD Blcvui4UIn FR-B9, FR-B24; Architecture o2ZqSrZjE6 A-AUTH.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: portal request/response + Portal-config delta).
- [ ] Parent epic: E4. Intra-service: after E4-S1, E7-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
