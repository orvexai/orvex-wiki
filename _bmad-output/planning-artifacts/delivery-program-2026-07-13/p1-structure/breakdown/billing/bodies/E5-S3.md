## 🎯 Story

As **billing computing trial fair-use**, I want **a rolling-week action throttle (100 advisory → 150 throttle) computed from Redis counters and surfaced in the check response**, so that **consumers enforce it while billing never sits in the request path (G3)**.

**Definition of Done:** `TestTrialThrottleStateFromCounters` — a service-tier test asserting the `throttle` block in the FR-B5 check response transitions `none → advisory` at 100 and `advisory → throttled` at 150 actions/week (rolling, per principal), computed from Redis fast counters, with `windowResetAt`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given trial usage, When counted, Then the rolling-week action count is computed from Redis fast counters (FR-B18) per principal. *assert count derives from counters, not a DB scan* [Source: FR-B15, A-METER]
- [ ] **AC2** — Given 100 actions/week, When reached, Then the `throttle` block reports `advisory`; at 150 it reports `throttled`, with `windowResetAt`. *assert state transitions at 100 and 150 + reset time present* [Source: FR-B15]
- [ ] **AC3** — Given the throttle state, When served, Then it appears in the FR-B5 check response `throttle` block; consumers enforce (ai pre-flight consults its cached copy; the BFF surfaces the advisory). *assert throttle block in the check response* [Source: FR-B15, FR-B5]
- [ ] **AC4** — Given the values are provisional (finance-retunable), When retuned, Then they are cap-policy data (E3-S1), not code constants. *assert 100/150 come from `trial_weekly_actions` cap data* [Source: FR-B15, FR-B17]
- [ ] **AC5 (negative)** — Given billing computes the state, When enforced, Then billing never sits in the request path — the state is a cached field, consumers gate. *assert no billing round-trip on the enforcement path* [Source: FR-B15, G3]

## 🔨 Tasks

- [ ] RED: `TestTrialThrottleStateFromCounters` walking 0→100→150 (AC2).
- [ ] GREEN: rolling-week counter read in `internal/trial`/`internal/meter`; `throttle` projection into the check response (AC1–AC3).
- [ ] RED→GREEN: values sourced from `trial_weekly_actions` cap data (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — catalog carries advisory/throttle values + a ThrottleState enum, but no Redis weekly counters and Check always returns ThrottleNone.

Tier: `internal/meter` (Redis counters) + check projection. NestJS analogue: a rolling-window rate state service. Seam: counters populated by `cmd/metering` (E6-S1); consumers enforce. Sibling: E6-S1 (counters), E2-S2 (check response), E3-S1 (cap values).

## 🧪 Testing

Named DoD test at the service tier; Redis testcontainer for the rolling window. CS §5: no own-package mocks; real Redis.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §7 (Redis rolling window, no per-request DB scan), §10 (values are data), §11. SE-Arch: compute-not-enforce lens (G3). cell-lint: counters cell-local.

## 🔗 References

PRD Blcvui4UIn FR-B15, FR-B18, FR-B17; Architecture o2ZqSrZjE6 A-METER, A-CHECK.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `throttle` block shape).
- [ ] Parent epic: E5. Intra-service: after E6-S1 (counters), E2-S2 (check).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (100/150 are provisional — finance-retuned).
