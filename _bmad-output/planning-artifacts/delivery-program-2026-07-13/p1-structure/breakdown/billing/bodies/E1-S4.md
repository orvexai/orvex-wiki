## 🎯 Story

As a **prospective Teams customer**, I want **a rate-limited interest-capture endpoint behind the Teams teaser card**, so that **marketing gets a launch-interest list without a purchase path existing yet**.

**Definition of Done:** `TestTeamsInterestCaptureRateLimitedNoAuth` — a service-layer test asserting `POST /v1/plans/teams/interest {email}` records a signup with no account required, is rate-limited, and the row is exportable via the admin interest-list. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the Teams card, When `POST /v1/plans/teams/interest {email}` is called anonymously, Then a `teams_interest` row is recorded (no account required). *assert 202 + one `teams_interest` row with the email* [Source: FR-B3a]
- [ ] **AC2** — Given repeated calls from one source, When the rate limit is exceeded, Then further calls are throttled (429). *assert 429 after the configured threshold* [Source: FR-B3a]
- [ ] **AC3** — Given recorded interest, When `GET /admin/v1/interest-list` is called with admin scope, Then the cohort is returned for marketing export. *assert admin list returns the recorded rows; non-admin → 403* [Source: FR-B3a, FR-B24]
- [ ] **AC4 (negative)** — Given a malformed/empty email, When posted, Then it is rejected 400 without a row written. *assert 400 + zero rows* [Source: FR-B3a]

## 🔨 Tasks

- [ ] RED: `TestTeamsInterestCaptureRateLimitedNoAuth` with an over-limit burst (AC1, AC2).
- [ ] GREEN: `teams_interest` table + handler + rate-limit middleware; admin interest-list read behind admin scope (AC1–AC3).
- [ ] RED→GREEN: email-validation reject path (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — POST /v1/plans/teams/interest is a 501 stub (server.go:150), no interest table or export.

Tier: transport handler + `internal/interest` domain + store. NestJS analogue: a public form-capture controller + admin export controller. Seam: admin route class fronted by console (E7-S1). Sibling: E7-S1 supplies admin-scope enforcement.

## 🧪 Testing

Named DoD test at the service tier; Postgres testcontainer for the store; rate-limit exercised in-process. CS §5: no own-package mocks.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §7 (rate-limit an anonymous public write), §11 (validation failure explicit). SE-Arch: no-fake-done. cell-lint: public POST is NOT `billing.orvex.ai` (that host is the Stripe webhook only) — this is a `/v1/*` route reached via the product BFF, cluster-internal within the cell.

## 🔗 References

PRD Blcvui4UIn FR-B3a, FR-67; Architecture o2ZqSrZjE6 §3 (admin interest-list).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG).
- [ ] Parent epic: E1. Intra-service: admin read depends on E7-S1 auth scaffolding (can stub scope until then).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
