## 🎯 Story

As **orvex-studio-workflows driving every saga**, I want **billing's `/internal/v1/steps/*` surface to be idempotent, workload-identity-only, with a typed retryable/terminal envelope**, so that **billing runs no worker and every step replay is a provable no-op (D-WF-1, NFR-B3)**.

**Definition of Done:** `TestStepAPIIdempotentWorkloadIdentityEnvelope` — a service-tier test asserting each `/internal/v1/steps/*` route (trial-start, trial-expire, backfill-batch, purge-stripe-linkage, reconcile-subscriptions, reconcile-caps) enforces `WorkloadIdentityOnly` (lib FR-L6), replays via idempotency-key (lib FR-L19) as a no-op, and returns the typed retryable/terminal envelope. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the step surface, When defined, Then it exposes `POST /internal/v1/steps/{trial-start, trial-expire, backfill-batch, purge-stripe-linkage, reconcile-subscriptions, reconcile-caps}` (+ the tenant-move step class from E8-S3). *assert all step routes present* [Source: FR-B25, A-AUTH]
- [ ] **AC2** — Given a step call, When authenticated, Then it is `WorkloadIdentityOnly` (lib FR-L6) + NetworkPolicy-restricted to workflows; a non-workflow caller is rejected. *assert non-workload-identity → 403* [Source: FR-B25]
- [ ] **AC3** — Given a replayed step (same idempotency key), When re-invoked, Then it is a provable no-op (lib FR-L19 replay). *assert replay produces no duplicate side effect* [Source: FR-B25, NFR-B3]
- [ ] **AC4** — Given a step result, When returned, Then it uses the typed retryable/terminal envelope so the saga can distinguish retry from terminal failure. *assert envelope carries retryable-vs-terminal classification* [Source: FR-B25]
- [ ] **AC5 (negative)** — Given billing, When sagas run, Then billing originates no workflows and runs no Temporal worker — saga starts ride spine events consumed by workflows' dispatcher. *assert no worker binary; no workflow origination in billing* [Source: FR-B25, D-WF-1]

## 🔨 Tasks

- [ ] RED: `TestStepAPIIdempotentWorkloadIdentityEnvelope` (AC2, AC3, AC4).
- [ ] GREEN: step-API framework in `internal/steps` — workload-identity middleware, idempotency-key store, typed envelope; register the six step handlers (delegating to E5/E6/E7 domains) (AC1–AC4).
- [ ] RED→GREEN: confirm no worker binary (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — step-API surface is a 501 stub (server.go:174); no workload-identity gate, idempotency, or retryable/terminal envelope.

Tier: `internal/steps` framework + step handlers. NestJS analogue: a callback-target controller family (the Studio `/api/clerk/provision`, docmost deprovision pattern). Seam: workflows is the sole caller (workload-identity + NetworkPolicy); the framework is reused by E5 (trial), E6-S4 (reconcile), E7-S3 (purge), E8-S3 (tenant-move). Sibling: all step-consuming stories.

## 🧪 Testing

Named DoD test at the service tier; the workload-identity verifier is via `lib` (own family lib — real fixture, not a mock, CS §5). Idempotency store on Redis testcontainer.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3 (dual-gated callback-target pattern), §5 (idempotency determinism). SE-Arch: no-worker / central-saga lens (D-WF-1). cell-lint: rule #11 idempotency-key on `/internal/*`.

## 🔗 References

PRD Blcvui4UIn FR-B25, NFR-B3, D-WF-1; Architecture o2ZqSrZjE6 A-AUTH, §3, §6 (rule #11); lib FR-L6/L19.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: step envelopes + lib FR-L6/L19).
- [ ] Parent epic: E7. Intra-service: after E7-S1; precedes E5-S2/S4, E6-S4, E7-S3, E8-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
