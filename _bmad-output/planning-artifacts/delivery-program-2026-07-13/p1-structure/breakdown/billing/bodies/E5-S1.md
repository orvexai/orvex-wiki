## 🎯 Story

As **a newly-graduated user**, I want **a no-card standard free month granted as a temporary entitlement, once per verified email**, so that **I get full £7 entitlements for a month with no card and no re-arming via fresh accounts**.

**Definition of Done:** `TestTrialGrantNoCardOncePerEmail` — a service-tier test asserting the `trial-start` grant writes a `source=trial`, time-boxed entitlement version to the USER tenant (no personal-org mint), captures NO card and creates NO Stripe subscription, and enforces once-per-verified-email via a peppered-HMAC row in `trial_grants`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given trial eligibility, When granted, Then a `source=trial`, time-boxed entitlement version carrying full £7 Personal entitlements is written to the USER tenant (no org mint, D-S17). *assert version `source == trial`, `principal_type == user`, £7 caps* [Source: FR-B13, D-B7, D-S17]
- [ ] **AC2** — Given the no-card ruling, When the trial starts, Then NO card is captured and NO Stripe subscription exists during the trial (billing-managed temporary entitlement). *assert no Stripe subscription/Customer created for the trial* [Source: FR-B13, D-B7]
- [ ] **AC3** — Given once-per-verified-email, When a grant is attempted, Then `trial_grants` dedupes on a peppered HMAC of the identity-verified email (no raw email at rest); a fresh account with the same email cannot re-arm. *assert second grant for the same email-HMAC is refused* [Source: FR-B13]
- [ ] **AC4** — Given starter/demo content, When the trial is active, Then the tiny starter set (3–5 pages) counts against the wiki page quota like any content (not quota-exempt). *assert starter pages counted in quota* [Source: FR-B13, OQ-B11 closed]
- [ ] **AC5 (negative)** — Given a raw email, When dedupe runs, Then no raw email is persisted for this purpose — only the peppered hash. *assert no plaintext email column in `trial_grants`* [Source: FR-B13, NFR-B7]

## 🔨 Tasks

- [ ] RED: `TestTrialGrantNoCardOncePerEmail` with a duplicate-email attempt (AC1, AC3).
- [ ] GREEN: `trial_grants` table (peppered HMAC) + `internal/trial` grant service writing the temporary entitlement version (AC1–AC3, AC5).
- [ ] RED→GREEN: starter-content-counts-quota wiring/documentation (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — only SourceTrial/TrialState enums + an unimplemented VerifiedEmail; no trial_grants table, grant, or email-HMAC dedupe.

Tier: `internal/trial` + `internal/entitlement` (version write) + store. NestJS analogue: a grant service on the entitlement aggregate. Seam: the grant is invoked by the `trial-start` step API (E5-S2) from workflows; the email-verified fact comes from the identity principal, not from billing probing an IdP. Sibling: E5-S2 (step API), E2-S1 (version store).

## 🧪 Testing

Named DoD test at the service tier; Postgres testcontainer. CS §5: no own-package mocks; the pepper is a secret via ExternalSecrets.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §6 (peppered hash, no raw PII at rest), §11 (light abuse posture — measure before gating). SE-Arch: no-card-trial lens (D-B7 amended), grant-to-user-tenant (D-S17). cell-lint: trial rows are cell-local (feeds TENANT_MOVE.md).

## 🔗 References

PRD Blcvui4UIn FR-B13, D-B7, D-S17, OQ-B11; Architecture o2ZqSrZjE6 A-TRIAL.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: trial grant + `trial_grants` shape).
- [ ] Parent epic: E5. Intra-service: after E2-S1; feeds E5-S2/S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (OQ-B7 dedupe strictness — literal email-hash assumption).
