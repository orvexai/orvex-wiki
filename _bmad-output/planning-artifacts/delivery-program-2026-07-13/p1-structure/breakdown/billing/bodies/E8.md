## 🗺️ Feature area

The platform substrate every other epic stands on. It owns the **persistence split** (Postgres CNPG + Redis, **no Mongo** — D-S12; the raw-webhook / usage-journal / decision-audit stores are Postgres append tables; every cache key embeds the principal for purge-safety), the **event spine** (CloudEvents published/consumed on `studio-spine`, the engine transactional-outbox relay as launch prerequisite D-B10, the `orvexcell` fail-closed extension), the **cell-contract compliance** obligations (healthz echoes CELL_ID+CLUSTER_NAME, the 421 cell-guard, the tenant-move step class + `TENANT_MOVE.md` store inventory — JGAUQRsw2g §5 day-1 obligations), the **observability + SLI** wiring to LGTM for console, and the **NFR conformance harness** that proves latency/flip-lag/idempotency/fail-consistency/money-correctness/security/privacy hold. This epic also discharges the reconciliation debt (arch §8): the struck-Mongo apparatus is deleted, not deferred.

Governing canon: FR-B26/B27/B28, NFR-B1–B8, arch §6 (cell contract), §8 (reconciliation debt), D-S12, D-B10.

## 📋 Story roster

- [ ] E8-S1 — Persistence split — Postgres tables + Redis, no Mongo, purge-safe cache keys [FR-B26]
- [ ] E8-S2 — Events published/consumed on studio-spine (CloudEvents, `orvexcell` fail-closed) [FR-B27]
- [ ] E8-S3 — Cell-contract compliance — healthz CELL_ID+CLUSTER_NAME, 421 guard, tenant-move step class + `TENANT_MOVE.md` [NFR-B8, FR-B28]
- [ ] E8-S4 — Observability & SLIs — OTel/Prometheus flip-lag, check-latency, cache-hit, journal + webhook SLIs, Alertmanager [FR-B28]
- [ ] E8-S5 — NFR conformance harness — latency, flip-lag, idempotency, fail-consistency, money-correctness, security, privacy [NFR-B1–B8]

## 🎯 FR ids owned

FR-B26, FR-B27, FR-B28, NFR-B1, NFR-B2, NFR-B3, NFR-B4, NFR-B5, NFR-B6, NFR-B7, NFR-B8.

## ✅ Definition of Done

- [ ] All five stories Done.
- [ ] Family-E2E share green: the spine round-trips a `billing.entitlement.changed` event with the `orvexcell` extension; a consumer receiving an event lacking it fails closed.
- [ ] SDD lines evidenced: no Mongo anywhere (D-S12 — the `studio-billing-mongodb-credentials` ExternalSecret + `mongo-claim.yaml` NOTEs deleted, arch §8); allow/deny outcomes are never cached; `/healthz` echoes both CELL_ID and CLUSTER_NAME with a CI assertion; the sole public exposure is `billing.orvex.ai`, every other surface cluster-internal (NFR-B8).
- [ ] The `TENANT_MOVE.md` store inventory exists and is coverage-checked before the first stateful tenant row lands (cell contract rule #10).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: billing.* + consumed event schemas, cell-lint JGAUQRsw2g §5 obligations). D-B10 spine prerequisite (engine outbox relay) on the program critical path.
- [ ] Intra-service: E8-S1/S2 are foundational and precede E2/E4/E6; E8-S5 depends on all other epics landing to assert their NFRs.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (family-wide Mongo canon reconcile CS §5–10 vs D-S12 — cannot be settled billing-only).
