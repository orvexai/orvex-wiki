## 🎯 Story

As **the program's quality gate**, I want **an NFR conformance harness proving every NFR-B invariant**, so that **latency, flip-lag, idempotency, fail-consistency, money-correctness, security, and privacy are measured facts, not hopes**.

**Definition of Done:** `TestNFRConformanceHarness` — a suite asserting each NFR-B holds through its interface: the check primitive latency budgets (NFR-B1), flip-lag bounds (NFR-B2), idempotency/ordering (NFR-B3), fail-consistency (NFR-B4), money-correctness invariant (NFR-B5), security posture (NFR-B6), privacy posture (NFR-B7), and family conformance (NFR-B8). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1 (NFR-B1)** — Given the check primitive, When benchmarked, Then p95 ≤ 50 ms service-side, ≤ 5 ms satellite-side cache-hit; checkout/portal mint p95 ≤ 1 s; billing is never invoked per LLM call/render/write/search. *assert latency budgets under a load fixture* [Source: NFR-B1]
- [ ] **AC2 (NFR-B2)** — Given a flip, When measured, Then webhook receipt → version served p95 ≤ 10 s, hard ≤ 60 s; trial expiry executes within 5 min of the deadline. *assert flip-lag + trial-expiry timing (numbers provisional — ASSUMPTION)* [Source: NFR-B2]
- [ ] **AC3 (NFR-B3)** — Given a replayed webhook/step/backfill batch, When re-run, Then it is a provable no-op; out-of-order delivery cannot regress state. *assert duplicate + out-of-order fixtures in the money-path gate* [Source: NFR-B3]
- [ ] **AC4 (NFR-B4/B5)** — Given billing down, When consumers check, Then last-known-good within the bound; never-seen ⇒ Free (fail-closed for paid); and no paid entitlement exists without a Stripe state or attributed override; journal append-only; drift closes ≤24 h. *assert fail-consistency + money-correctness invariants* [Source: NFR-B4, NFR-B5]
- [ ] **AC5 (NFR-B6/B7)** — Given security/privacy, When audited, Then Stripe secrets via ExternalSecrets/OpenBao, signature verification unconditional, no card data touches Orvex (SAQ-A), secrets never in logs; GDPR purge within SLA, trial-dedupe emails only as peppered hashes, journal carries no content payloads. *assert security + privacy posture* [Source: NFR-B6, NFR-B7]
- [ ] **AC6 (NFR-B8)** — Given family conformance, When gated, Then Go, no `@docmost/*`/AGPL import (CI-guarded), no in-service Temporal worker, all wire shapes contracts-authored, per-cell deployment, sole public exposure `billing.orvex.ai`. *assert no-AGPL-import gate + no-worker + single-public-host* [Source: NFR-B8]

## 🔨 Tasks

- [ ] RED: `TestNFRConformanceHarness` scaffolding per NFR (starts red until the referenced stories land).
- [ ] GREEN: latency benchmarks (AC1), flip-lag timing (AC2), reuse duplicate/out-of-order fixtures (AC3), fail-consistency + money-correctness assertions (AC4), security/privacy audits (AC5), no-AGPL-import + no-worker gates (AC6).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — partial NFR harness — fail-consistency DoD (nfr_b4_dod_test), idempotency + money-correctness integration + smoke tests; no consolidated latency/flip-lag/security/privacy conformance harness.

Tier: cross-cutting conformance suite (`test/nfr`) spanning all epics. NestJS analogue: n/a. Seam: aggregates the NFR assertions each epic contributes. Sibling: every epic — this harness references their signals (E2 latency, E4 flip/idempotency, E6 money-correctness, E7 privacy, E8-S1/S2 conformance).

## 🧪 Testing

Named DoD test as an NFR conformance suite; benchmarks + integration fixtures + static CI gates (no-AGPL-import, store-driver confinement, cross-tenant isolation probe). CS §5: mock only true externals; the no-AGPL-import guard is a static gate.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §11 (NFR numbers NFR-B2/B4 are provisional ASSUMPTIONS until first cohort data — stated, not papered over). SE-Arch: no-fake-done lens across all NFRs; the counter-metrics (false-lockout=0, webhook-loss=0). cell-lint: single-public-host + per-cell conformance.

## 🔗 References

PRD Blcvui4UIn NFR-B1–B8 (§5), counter-metrics (§2); Architecture o2ZqSrZjE6 §4 (CI gates), §5 (risk dials).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG). Depends on all other epics landing their measurable behavior.
- [ ] Parent epic: E8. Intra-service: the terminal conformance gate.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (NFR-B2/B4 budgets provisional until first cohort data).
