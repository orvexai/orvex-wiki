## 🎯 Story

As **orvex-studio-console operators**, I want **OTel + Prometheus SLIs feeding LGTM plus Alertmanager rules**, so that **flip-lag, check latency, cache-hit, journal lag, and webhook health are observable and the correctness alarms fire**.

**Definition of Done:** `TestObservabilitySLIsAndAlerts` — a service-tier test asserting the SLIs are emitted (webhook processing lag + failure rate, entitlement-flip lag, check-primitive latency + cache-hit ratio, journal delivery lag, reconcile drift count, trial-grant rate) and the Alertmanager rules (webhook failure/poison, DLQ depth, reconcile drift, flip-lag SLO breach, trial-grant anomaly) are wired. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the binaries, When running, Then health/readiness per binary + OTel + Prometheus per house pattern feed LGTM for console (NFR-8). *assert metrics + traces exported to the LGTM spine* [Source: FR-B28, NFR-8]
- [ ] **AC2** — Given the SLIs, When emitted, Then they include webhook processing lag + failure rate, entitlement-flip lag (webhook receipt → new version served), check-primitive latency + cache-hit ratio, journal delivery lag, reconcile drift count, and trial-grant rate (abuse signal). *assert each named SLI is emitted* [Source: FR-B28]
- [ ] **AC3** — Given readiness, When probed, Then it does real dependency round-trips (Postgres/Redis reachability) distinct from the always-200 liveness probe (CS §10; the scaffold's shared trivial `/healthz` is corrected). *assert readiness fails when a dependency is down; liveness stays 200* [Source: FR-B28, arch §4/§8]
- [ ] **AC4** — Given Alertmanager, When configured, Then rules exist for webhook failure/poison-event, DLQ depth, reconcile drift, flip-lag SLO breach, and trial-grant-rate anomaly. *assert each alert rule present* [Source: FR-B28, arch §4]
- [ ] **AC5 (negative)** — Given logging, When domain code lands, Then structured `slog` + OTLP replaces `log.Printf`; secrets never appear in logs (NFR-B6). *static assert: no `log.Printf`; no secret in log output* [Source: FR-B28, arch §4, NFR-B6]

## 🔨 Tasks

- [ ] RED: `TestObservabilitySLIsAndAlerts` asserting the named SLIs (AC1, AC2).
- [ ] GREEN: OTel/Prometheus wiring in each binary; real-dependency readiness probes; Alertmanager rules (AC1–AC4).
- [ ] RED→GREEN: `slog`+OTLP replaces `log.Printf`; secret-redaction (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — in-memory SLI recorder + ServiceMonitor/PrometheusRule/GrafanaDashboard manifests present; the /metrics OTel/Prometheus exposition endpoint is not implemented (nothing to scrape yet).

Tier: cross-cutting (`internal/observability`) + deploy manifests. NestJS analogue: an interceptor + Prometheus registry. Seam: LGTM/OTel spine (NFR-8) consumed by console. Sibling: E4-S3/S5 (webhook + flip SLIs), E6 (journal/reconcile SLIs), E5 (trial-grant SLI), E7-S4 (audit spine).

## 🧪 Testing

Named DoD test at the service tier; in-memory OTel exporter + a Prometheus registry assertion. CS §5: no own-package mocks. Alert rules validated as config.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §10 (structured slog, real readiness probe, no secrets in logs). SE-Arch: observability-on-the-NFR-8-spine lens; readiness-vs-liveness reconciliation debt (arch §8). cell-lint: SLIs tagged with cell/cluster.

## 🔗 References

PRD Blcvui4UIn FR-B28, NFR-8, NFR-B6; Architecture o2ZqSrZjE6 §4 (observability + readiness debt), §8.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG).
- [ ] Parent epic: E8. Intra-service: after the domains whose SLIs it measures (E4, E5, E6, E7-S4) land their signals.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
