## 🎯 Story

As **the entitlement system-of-record**, I want **every flip to publish `billing.entitlement.changed` as the canonical record and evict consumer caches**, so that **there is one truth and consumers converge without a second store**.

**Definition of Done:** `TestEntitlementChangePublishesCanonicalAndEvicts` — a service-tier test asserting each entitlement version write publishes exactly one `billing.entitlement.changed` CloudEvent (canonical) and evicts the local Redis cache before the new version is served. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an entitlement version write, When committed, Then exactly one `billing.entitlement.changed` event publishes to `studio-spine` (canonical record). *assert one event per version write, carrying the new `version`* [Source: FR-B7, D-B5]
- [ ] **AC2** — Given the event, When published, Then the local Redis `ent:{principal}` cache is evicted before the new version is served (event is canonical; sync push is the fast path; reconcile heals). *assert eviction precedes the next served version* [Source: FR-B7, A-CHECK]
- [ ] **AC3** — Given the three propagation legs, When a flip occurs, Then the event is canonical, the sync cap push (E3-S4) is the low-latency fast path, and the reconcile schedule (E6-S4) re-asserts — not one substituting for another. *assert all three legs invoked/scheduled on a flip* [Source: FR-B7, D-B5]
- [ ] **AC4** — Given the CloudEvent, When published, Then it carries the `orvexcell` extension (cell contract rule #6); a consumer receiving one lacking it fails closed. *assert `orvexcell` extension present* [Source: FR-B7, arch §6]
- [ ] **AC5 (negative)** — Given a version write that fails to commit, When rolled back, Then NO event is published (no phantom flips). *assert zero events on a rolled-back write* [Source: FR-B7, NFR-B5]

## 🔨 Tasks

- [ ] RED: `TestEntitlementChangePublishesCanonicalAndEvicts` (AC1, AC2, AC5).
- [ ] GREEN: publish-on-commit via the transactional-outbox pattern (E8-S2) + cache-evict hook in `internal/entitlement` (AC1–AC4).
- [ ] RED→GREEN: rollback-no-publish path (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — outbox publish of billing.entitlement.changed + relay real+tested (event/relay.go); Redis cache-evict is ErrNotImplemented and the reconcile path is absent.

Tier: `internal/entitlement` + `internal/event` (outbox publish) + Redis cache. NestJS analogue: a domain-event publisher on the aggregate save. Seam: `studio-spine` CloudEvents (schemas in contracts); the outbox is the mechanism (D-B10). Sibling: E3-S4 (sync push), E3-S5 (UI push), E6-S4 (reconcile), E8-S2 (spine wiring).

## 🧪 Testing

Named DoD test at the service tier; Postgres + Redis + a spine test double (Kafka/Broker via testcontainer or in-memory Broker). CS §5: the spine broker is a true external — may be a test double; never mock own `internal/*` packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (outbox determinism), §7 (event-evicted cache). SE-Arch: event-canonical lens (D-B5 both-recommendation), rollback-safety. cell-lint: rule #6 `orvexcell` fail-closed.

## 🔗 References

PRD Blcvui4UIn FR-B7, D-B5; Architecture o2ZqSrZjE6 A-CHECK, A-CAP, §6 (rule #6).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `billing.entitlement.changed` schema). D-B10 outbox relay prerequisite.
- [ ] Parent epic: E3. Intra-service: after E2-S1/S2, E8-S2; precedes E3-S4/S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
