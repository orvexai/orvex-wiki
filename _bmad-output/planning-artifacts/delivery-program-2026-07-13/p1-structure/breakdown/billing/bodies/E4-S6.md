## 🎯 Story

As **the program**, I want **a red-blocking Stripe test-clock money-path E2E in CI**, so that **the money path is a proven fact from Alpha, not a hope (G2, Epic 2.5)**.

**Definition of Done:** `TestMoneyPathE2ETestClock` — a CI E2E asserting the full path against Stripe test mode with test clocks: signup → Graduation → trial → Checkout (monthly AND annual) → entitled → cancel → lapse → re-subscribe → retroactive-trial fixture, including annual renewal, dunning with Stripe's default retry schedule, and duplicate + out-of-order webhook fixtures. Red = not shippable, no override. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the money-path gate, When it runs in CI, Then it drives signup → Graduation → trial → checkout (monthly AND annual) → entitled → cancel → lapse → re-subscribe → retroactive-trial fixture against Stripe test mode with test clocks. *assert full path green end-to-end* [Source: FR-B12, G2]
- [ ] **AC2** — Given test clocks, When advanced, Then the annual renewal + dunning (Stripe default retry) cycles are exercised. *assert annual renewal `invoice.paid` + dunning path both hit* [Source: FR-B12]
- [ ] **AC3** — Given resilience fixtures, When run, Then duplicate-delivery and out-of-order webhook fixtures are included and pass. *assert duplicate + out-of-order fixtures green* [Source: FR-B12, NFR-B3]
- [ ] **AC4** — Given a red gate, When CI runs, Then the pipeline is blocked with no override (FR-74 discipline). *assert the gate is required + non-overridable* [Source: FR-B12]
- [ ] **AC5 (negative)** — Given a broken flip (e.g. a lapsed principal still entitled), When the gate runs, Then it fails red (no fake-done). *assert an injected leak fails the gate* [Source: FR-B12, NFR-B5]

## 🔨 Tasks

- [ ] RED: author the E2E scenario skeleton `TestMoneyPathE2ETestClock` (AC1) — starts red until E4-S1..S5 land.
- [ ] GREEN: wire the test-clock harness against Stripe test mode; monthly + annual + dunning + cancel + re-subscribe + retro fixture (AC1–AC3).
- [ ] GREEN: mark the gate required/non-overridable in CI (AC4); leak-injection negative fixture (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — real money-path E2E gate present (test/e2e/gate_m4_...) proving sub→webhook→flip→outbox→projection; no Stripe test-clocks and no dunning/cancel/re-subscribe/annual/retro fixtures (those legs absent).

Tier: CI E2E / `test/e2e`. NestJS analogue: the family money-path gate. Seam: exercises the whole E4 + E5 + E3 path against real Stripe test mode. Sibling: every E4/E5 story feeds this; the lookup-key↔mode + webhook-set drift checks (E1-S2, E4-S3) are companion CI gates.

## 🧪 Testing

Named DoD test is itself the E2E gate — real Stripe test mode + test clocks (the true external; NOT mocked here, unlike unit layers). CS §5: this is the integration/E2E tier; it exercises real Stripe, real Postgres/Redis. OQ-B12: registered test-mode endpoint for dev, Stripe CLI forwarding for crew (assumption — confirm before wiring).

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (deterministic E2E via test clocks), §11 (no-override discipline, no fake-done). SE-Arch: no-fake-done lens is the whole point (the fake-done-forensics precedent). cell-lint: single-cell-degenerate routing exercised.

## 🔗 References

PRD Blcvui4UIn FR-B12, G2 (Epic 2.5), OQ-B12; Architecture o2ZqSrZjE6 §4 (CI gates).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG). Depends on E4-S1..S5 + E5-S1..S4 + E3-S3/S4 landing.
- [ ] Parent epic: E4. Intra-service: the terminal gate of the money path.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (OQ-B12 dev/crew webhook delivery mechanism).
