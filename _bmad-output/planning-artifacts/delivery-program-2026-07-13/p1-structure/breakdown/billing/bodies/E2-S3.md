## 🎯 Story

As **every consumer (satellites + the AGPL wiki engine)**, I want **a caching entitlements client with normative fail-consistent semantics**, so that **a billing outage degrades to bounded last-known-good, never to blocked usage or fail-open paid access**.

**Definition of Done:** `TestEntitlementClientFailConsistentSemantics` — a contract-conformance test asserting: billing unreachable ⇒ serve last-known-good within the staleness bound then degrade honestly; never-seen principal ⇒ Free (fail-closed for paid); run against BOTH the Go `lib/entitlements` client and the engine's TS client via the contracts `gates/` fixtures. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given billing is reachable, When a satellite checks, Then `lib/entitlements` returns a cached copy at p95 ≤ 5 ms and refills on TTL/eviction. *assert cache-hit latency + refill on evict* [Source: FR-B5, NFR-B1]
- [ ] **AC2** — Given billing is unreachable, When a check runs, Then last-known-good is honored within the bounded staleness window (target 5 min), then degrades honestly. *assert LKG served under outage up to the bound, then a defined degrade* [Source: FR-B5, NFR-B4]
- [ ] **AC3** — Given a never-seen principal, When checked, Then the client resolves Free (fail-closed for paid, fail-open for free product). *assert Free on unknown principal* [Source: FR-B5, NFR-B4]
- [ ] **AC4** — Given the AGPL engine cannot import the closed lib, When it needs a client, Then a conforming TS client over plain HTTP + its own Redis cache passes the SAME `gates/` fixtures. *assert both clients pass identical conformance fixtures* [Source: FR-B5, A-CHECK]
- [ ] **AC5 (negative)** — Given a delegated call, When resolved, Then the client keys on the acting delegated principal's tenant, never the service identity/token minter. *assert delegated-principal resolution, not service-identity* [Source: FR-B6, A-CHECK]

## 🔨 Tasks

- [ ] RED: `TestEntitlementClientFailConsistentSemantics` against the contracts fixtures (AC2, AC3, AC4).
- [ ] GREEN: `orvex-studio-lib/entitlements` Go client (cache + TTL + LKG + Free-default); publish the normative semantics + fixtures into contracts `gates/` (AC1–AC3).
- [ ] GREEN: a conforming TS reference client spec/impl for the engine (AC4).
- [ ] RED→GREEN: delegated-principal keying (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — contract-schema validator + golden/trace + nfr_b4 fail-consistency tests present; no standalone lib/entitlements Go client and no engine TS client fixtures.

Tier: client library (`orvex-studio-lib/entitlements`) — a delta filed with lib — plus a TS reference client for the engine. NestJS analogue: n/a (client-side); the engine analogue is a NestJS provider over HTTP + Redis. Seam: fail-consistent semantics are **normative in orvex-studio-contracts** (weakest consumer is not the ceiling). Sibling: E2-S2 (the served primitive), lib PRD (client delta).

## 🧪 Testing

Named DoD test as a contracts-conformance suite run against both client impls. CS §5: mock the billing HTTP endpoint (a true external boundary from the client's view); never mock the client's own cache logic. Do not mock across into billing's own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3.7 design-it-twice (semantics authored in contracts before both impls), §5 (conformance fixtures), §11 (staleness bound stated, monitored). SE-Arch: license-boundary lens (AGPL engine cannot import closed lib). cell-lint: client resolves in-cell.

## 🔗 References

PRD Blcvui4UIn FR-B5, FR-B6, NFR-B4; Architecture o2ZqSrZjE6 A-CHECK.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: fail-consistent conformance fixtures in `gates/`).
- [ ] Parent epic: E2. Intra-service: after E2-S2; the lib client delta is filed with orvex-studio-lib.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (NFR-B4 staleness bound is provisional until first cohort data).
