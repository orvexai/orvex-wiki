## 🎯 Story

As **the launch team**, I want **a one-shot backfill step that offers the standard free month to every pre-paywall account**, so that **the FR-66 canon change is softened and no existing user is worse off (launch-blocking)**.

**Definition of Done:** `TestRetroactiveBackfillIdempotentResumable` — a service-tier test asserting the `backfill-batch` step grants the standard free month to a batch of pre-paywall principals idempotently (a re-run grants nothing twice), resumably, and rate-limited, and that billing provides the cohort count + per-account trial state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the launch day, When the one-shot central Temporal backfill runs, Then every account predating the paywall is offered the same standard free month via the `backfill-batch` step. *assert each pre-paywall principal receives a trial grant* [Source: FR-B16, A-TRIAL]
- [ ] **AC2** — Given the card-free trial, When backfilling, Then it is a straightforward GRANT (no card-capture step) that reverts to Free at month-end like any trial. *assert grant path, no card step* [Source: FR-B16, D-B7]
- [ ] **AC3** — Given idempotency, When a batch is re-run, Then it grants nothing twice (resumable mid-run, rate-limited so the flip storm doesn't stampede cap pushes/cache evictions). *assert re-run is a no-op per already-granted principal* [Source: FR-B16, NFR-B3]
- [ ] **AC4** — Given reporting, When the backfill runs, Then billing provides the cohort count + per-account trial state (the honest announcement is a product/marketing deliverable). *assert cohort count + per-account state queryable* [Source: FR-B16]
- [ ] **AC5 (negative)** — Given a principal already on a paid plan, When backfilled, Then no trial is granted over their existing entitlement. *assert no downgrade/overwrite of a paying principal* [Source: FR-B16]

## 🔨 Tasks

- [ ] RED: `TestRetroactiveBackfillIdempotentResumable` with a re-run (AC3, AC5).
- [ ] GREEN: `backfill-batch` step handler (E7-S2) iterating principals via E5-S1 grant; rate-limit + resume cursor (AC1–AC4).
- [ ] RED→GREEN: already-paid skip (AC5); cohort-count query (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — no backfill-batch step at all (step API is a stub).

Tier: `internal/trial` batch + step-API handler. NestJS analogue: a resumable batch step. Seam: the one-shot workflow lives in orvex-studio-workflows; billing exposes the idempotent `backfill-batch` step (D-WF-1). Sibling: E5-S1 (grant), E7-S2 (step framework), E3 (rate-limited propagation to avoid the flip storm).

## 🧪 Testing

Named DoD test at the service tier; Postgres testcontainer; re-run idempotency + rate-limit exercised. CS §5: no own-package mocks.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §11 (blast-radius stated: rate-limited, rehearsed on fixtures, resumable). SE-Arch: idempotent-resumable-batch lens; one-day-blast-radius risk. cell-lint: batch runs per-cell over cell-local principals.

## 🔗 References

PRD Blcvui4UIn FR-B16, D-B7, NFR-B3; Architecture o2ZqSrZjE6 A-TRIAL, §5 (backfill blast-radius risk).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `backfill-batch` step envelope).
- [ ] Parent epic: E5. Intra-service: after E5-S1, E7-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
