## 🗺️ Feature area

Billing decides *what the cap/quota is*; the enforcing service decides *whether a call/write proceeds* (D-B6, G3). This epic owns the **plan→cap-and-quota policy registry** (`cap_policies` riding plan-versions, A-CAP) — the two sibling AI cap lines (`ai_monthly_budget` user-action + `embedding_monthly_budget` sized-from-page-quota, D-S15), the curator/trial/demo caps, AND the **wiki quota siblings** (`wiki_max_pages` … `wiki_history_retention`, FR-B17a, D-B11) — plus the **three-way propagation** on every flip: (1) `billing.entitlement.changed` canonical event + cache evict; (2) synchronous workload-identity cap push to ai's LiteLLM backstop; (3) the reconcile re-assert. It also drives the **<60s paywall dissolve in both products** (D-S20). This executes ai's OQ-AI8 / control's OQ-CT8 handoff clause on their own terms.

Governing canon: FR-B7/B17/B17a, A-CAP, D-S15, D-S7, D-S20, D-B11, provenance `H5NzkdsOzK`.

## 📋 Story roster

- [ ] E3-S1 — `cap_policies` registry per plan-version (two AI cap lines + curator/trial/demo) [FR-B17]
- [ ] E3-S2 — Wiki quota values in catalog (Free LOCKED, Personal pending-freeze) served in `caps{}` [FR-B17a]
- [ ] E3-S3 — `billing.entitlement.changed` publish + Redis cache evict + reconcile canonical-record path [FR-B7]
- [ ] E3-S4 — Synchronous cap push to ai (`PUT /admin/v1/tenants/{tenant}/cap` workload-identity variant) — two sibling lines [FR-B7]
- [ ] E3-S5 — Paywall-dissolve UI push <60s both products (BFF SSE + engine cache-evict semantics) [FR-B7]

## 🎯 FR ids owned

FR-B7, FR-B17, FR-B17a.

## ✅ Definition of Done

- [ ] All five stories Done.
- [ ] Family-E2E share green: a flip pushes both cap numbers to ai and evicts the wiki engine's cached quota copy.
- [ ] SDD lines evidenced: the embedding budget is a SEPARATE sibling line, never nested under the user-action cap (D-S15); background embeddings never draw down the user-action cap; Free wiki quotas match `H5NzkdsOzK` verbatim (200 pages / 1 GiB / 10 MB-file / 2,000 files / 25 members / min(10,180d)); a cap retune is a new plan-version (ADR + human sign-off, CS ❌#10), never a code fix.
- [ ] The ai cap-endpoint workload-identity delta is filed in orvex-studio-contracts + an ADR before E3-S4 dispatches (arch §7).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG incl. the ai cap-endpoint workload-identity delta + the 402 QUOTA_EXCEEDED shared contract). Personal wiki-quota freeze is OQ-B10 (pending canon freeze — flagged, not invented).
- [ ] Intra-service: E2-S1/S2 precede all; E3-S1/S2 precede E3-S3/S4/S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (Personal quota freeze OQ-B10; cap-reached UX OQ-B3).
