## 🎯 Story

As **a per-cell stateful service on the multi-region cell contract**, I want **the day-1 cell obligations — healthz echoing CELL_ID+CLUSTER_NAME, the 421 cell-guard, and the tenant-move step class + store inventory**, so that **billing conforms to JGAUQRsw2g §5 before its first stateful tenant row lands**.

**Definition of Done:** `TestCellContractHealthzAndGuard` — a service-tier test asserting all three binaries' `/healthz` return `{status, cell, cluster}` (echoing both opaque `CELL_ID` and `CLUSTER_NAME`), a misrouted request returns 421, and the `TENANT_MOVE.md` store inventory enumerates every cell-local datastore holding tenant rows. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1 (rule #4)** — Given `/healthz` on each binary, When called, Then it returns `{status, cell, cluster}` echoing both `CELL_ID` and `CLUSTER_NAME`; config carries both keys; a CI check asserts both present + echoed. *assert healthz echoes both; config has `ClusterName`* [Source: NFR-B8, arch §6]
- [ ] **AC2 (rule #3)** — Given a request to the wrong home cell, When received, Then a 421 Misdirected Request is returned (not stale/foreign state) via the cell-guard middleware on `/v1/*` + `/internal/*`. *assert misrouted → 421* [Source: NFR-B8, arch §6]
- [ ] **AC3 (rule #10)** — Given billing is a per-cell stateful service, When the tenant-move contract is authored, Then `/internal/v1/steps/{tenant-quiesce, tenant-export, tenant-import, tenant-activate}` exist AND a `TENANT_MOVE.md` store inventory enumerates every cell-local datastore (entitlement versions, subscriptions, ledger, journal partitions, Redis counters/cache) coverage-checked against the declared datastores. *assert step class present + store inventory coverage-checked* [Source: NFR-B8, arch §6]
- [ ] **AC4 (rule #13)** — Given `CellID`/`ClusterName`, When used, Then they are opaque, echoed as-is — no substring parse, no KEDA cell-id math. *static assert: no cell-id substring parsing* [Source: NFR-B8, arch §6]
- [ ] **AC5 (negative)** — Given a new cell-local datastore added later, When it is not in `TENANT_MOVE.md`, Then the coverage check fails CI (no un-inventoried tenant store). *assert coverage-check fails on an un-listed datastore* [Source: NFR-B8, FR-B28]

## 🔨 Tasks

- [ ] RED: `TestCellContractHealthzAndGuard` (AC1, AC2).
- [ ] GREEN: `ClusterName` config + `{status,cell,cluster}` healthz on all three binaries; 421 cell-guard middleware; tenant-move step class (E7-S2 framework, bodies deferred); `TENANT_MOVE.md` store inventory + coverage check (AC1–AC4).
- [ ] RED→GREEN: coverage-check-fails-on-unlisted-store (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — healthz echoes CELL_ID+CLUSTER_NAME (server.go:253); no 421 guard, no tenant-move step class, and no TENANT_MOVE.md.

Tier: config + transport middleware + step-API stubs + `TENANT_MOVE.md`. NestJS analogue: n/a (platform contract). Seam: multi-region cell contract JGAUQRsw2g §5. Sibling: E7-S2 (step framework hosts the tenant-move class — bodies deferred), E8-S1 (the cell-local datastores the inventory enumerates), E4-S4 (cell routing).

## 🧪 Testing

Named DoD test at the service tier; healthz + guard exercised in-process; the `TENANT_MOVE.md` coverage check is a CI static gate. CS §5: no own-package mocks.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §10 (opaque CellID, no math), §11 (obligations in-scope-now even though impl deferred). SE-Arch: cell-contract-day-1 lens. cell-lint JGAUQRsw2g §5: rules #3/#4/#10/#11/#13 are the whole point of this story.

## 🔗 References

PRD Blcvui4UIn NFR-B8, FR-B28; Architecture o2ZqSrZjE6 §1, §6 (cell-contract compliance).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG + cell-lint JGAUQRsw2g §5 obligations).
- [ ] Parent epic: E8. Intra-service: after E7-S2 (step framework), E8-S1 (datastores).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
