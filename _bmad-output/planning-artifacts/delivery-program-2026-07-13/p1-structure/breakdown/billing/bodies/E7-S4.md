## 🎯 Story

As **an auditor / compliance reviewer**, I want **every entitlement-decision and admin/destructive action journaled to a Postgres append-only audit table and the family audit stream**, so that **every entitlement version write is attributable and traceable on the observability spine**.

**Definition of Done:** `TestEntitlementDecisionAuditAttributable` — a service-tier test asserting every entitlement version write is attributable (Stripe event id / saga step id / admin actor + `RequireFresh` introspection), journaled to a Postgres append table (Mongo struck, D-S12), and admin/destructive calls emit to the family audit stream. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given any entitlement version write, When persisted, Then it is attributable to its cause (Stripe event id / saga step / admin actor); admin paths carry `RequireFresh` introspection (identity FR-I3c). *assert every version has a non-null attributed cause* [Source: FR-B23, FR-B4]
- [ ] **AC2** — Given an entitlement decision, When journaled, Then it lands in a Postgres append-only audit table (range-partitioned; Mongo struck, D-S12). *assert append-only audit row; no Mongo* [Source: FR-B23, D-S12]
- [ ] **AC3** — Given admin ops + destructive calls, When executed, Then they emit to the family audit stream. *assert admin/destructive op emits an audit event* [Source: FR-B23]
- [ ] **AC4** — Given the observability spine, When decisions + webhook processing occur, Then they join the LGTM/OTel spine (NFR-8 verbatim). *assert OTel spans for entitlement decisions + webhook processing* [Source: FR-B23, NFR-8]
- [ ] **AC5 (negative)** — Given an unattributed version write attempt, When executed, Then it is rejected (no anonymous entitlement changes). *assert a cause-less version write is blocked* [Source: FR-B23, NFR-B5]

## 🔨 Tasks

- [ ] RED: `TestEntitlementDecisionAuditAttributable` (AC1, AC2, AC5).
- [ ] GREEN: entitlement-decision audit append table + writer in `internal/audit`; family-audit-stream emit on admin/destructive ops (AC1–AC3).
- [ ] RED→GREEN: OTel spans (AC4); cause-less-write rejection (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — cap-change audit ledger real+tested (migration 0003, spendcap_repo.go) + version source/cause_ref trail; no dedicated entitlement-decision audit table and no admin/destructive audit stream.

Tier: `internal/audit` + store + OTel wiring. NestJS analogue: an audit interceptor. Seam: family audit stream + LGTM/OTel spine (NFR-8). Sibling: E2-S1 (attribution on versions), E7-S1 (admin ops), E8-S4 (observability).

## 🧪 Testing

Named DoD test at the service tier; Postgres testcontainer; OTel spans asserted via an in-memory exporter. CS §5: no own-package mocks.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (append-only audit), §10 (attribution invariant), §11. SE-Arch: attributability lens (no anonymous flips). cell-lint: audit rows cell-local.

## 🔗 References

PRD Blcvui4UIn FR-B23, FR-B4, NFR-8, D-S12; Architecture o2ZqSrZjE6 A-ENT, A-AUTH.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: audit event schema).
- [ ] Parent epic: E7. Intra-service: after E2-S1, E7-S1; feeds E8-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
