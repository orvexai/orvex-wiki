## 🎯 Story

As **console, the Studio in-app billing state, and Beta's upgrade moments**, I want **`GET /v1/usage/{principal_type}/{principal_id}` reporting period actions/spend by feature**, so that **the trial-end summary and admin views query one journal (billing provides data, product owns the moment)**.

**Definition of Done:** `TestUsageReportingByFeature` — a service-tier test asserting `GET /v1/usage/{principal_type}/{principal_id}` returns period actions/spend by feature, trial-week consumption, and Curator budget remaining, computed over the usage journal, tenant-scoped. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a principal, When `GET /v1/usage/{principal_type}/{principal_id}` is called, Then it returns period actions/spend by feature, trial-week consumption, and Curator budget remaining. *assert all three sections present in the response* [Source: FR-B20]
- [ ] **AC2** — Given consumers, When they read usage, Then console's billing/admin pane, Studio in-app billing state, and Beta's FR-70 upgrade moments all query this surface (billing provides data, product owns the moment). *assert the shape serves all three consumers* [Source: FR-B20]
- [ ] **AC3** — Given the trial-end "what the AI did during your trial" summary, When rendered, Then it is a query over this journal — billing provides the data only. *assert the summary is derivable from the usage query* [Source: FR-B20]
- [ ] **AC4 (authz)** — Given a caller, When they read usage, Then it is tenant-membership-scoped (a caller reads only a tenant their principal places them in; detail may require owner/admin for Teams). *assert cross-tenant read → 403* [Source: FR-B20, FR-B24]
- [ ] **AC5 (negative)** — Given a principal with no usage, When queried, Then a well-formed empty report (zeros), never a 500 or a foreign principal's data. *assert empty-but-valid report* [Source: FR-B20]

## 🔨 Tasks

- [ ] RED: `TestUsageReportingByFeature` (AC1, AC4).
- [ ] GREEN: `GET /v1/usage` handler + `internal/meter` aggregation over the journal; tenant-scoped authz (AC1–AC4).
- [ ] RED→GREEN: empty-report path (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — GET /v1/usage/{principal} is a 501 stub (server.go:160).

Tier: transport handler → `internal/meter` aggregation over the journal. NestJS analogue: a read-model query controller. Seam: two-segment polymorphic route (same convention as the check primitive). Sibling: E6-S1/S2 (journal + cost-centers), E7-S1 (tenant-scoped authz).

## 🧪 Testing

Named DoD test at the service tier; Postgres testcontainer with seeded journal rows. CS §5: no own-package mocks; real `lib/auth` verifier fixture for scoping.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3 (tenant-scoped reads), §11 (empty-report handling). SE-Arch: read-model lens; billing-provides-data/product-owns-moment separation. cell-lint: `/v1/*` cluster-internal, resolves in-cell.

## 🔗 References

PRD Blcvui4UIn FR-B20, FR-B24; Architecture o2ZqSrZjE6 A-METER, §3 (usage route).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: usage response shape).
- [ ] Parent epic: E6. Intra-service: after E6-S1/S2, E7-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
