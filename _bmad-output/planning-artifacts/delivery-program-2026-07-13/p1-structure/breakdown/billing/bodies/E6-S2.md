## 🎯 Story

As **finance measuring per-feature COGS**, I want **usage keyed on cost-center = call-site × principal, with `ai.cap.*` consumed for reporting only**, so that **SM-C5 ("free-tier ≈ 0") is a measured fact and cap events never flip entitlements**.

**Definition of Done:** `TestCostCenterCallSiteAndReportOnlyCapEvents` — a service-tier test asserting the usage event's `feature` field carries the contracts-owned call-site id (Studio ADR-0002 registry) forming cost-center = call-site × principal, and that `ai.cap.reached`/`ai.cap.warning` update finance/reporting counters ONLY — never flipping an entitlement or pushing a cap. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a usage event, When journaled, Then its `feature` field is the call-site id from the contracts-owned call-site enum (Studio's ADR-0002 registry); cost-center = call-site × principal. *assert `feature ∈ call-site enum`; cost-center composed of both* [Source: FR-B19, D-B9]
- [ ] **AC2** — Given aggregation, When finance reports, Then spend aggregates by cost-center, making per-feature COGS (SM-C5) measurable. *assert per-cost-center aggregation query works* [Source: FR-B19]
- [ ] **AC3** — Given `ai.cap.reached`/`ai.cap.warning`, When consumed, Then they update finance/usage counters + billing-state + Alertmanager signal ONLY — they NEVER flip an entitlement or push a cap (ai is the enforcer; A-CAP owns the flip path). *assert no entitlement write / cap push on a cap.* event* [Source: FR-B19, A-METER]
- [ ] **AC4 (negative)** — Given a usage event whose `feature` is not a registered call-site, When received, Then it is flagged (governance ceiling), not silently accepted as a new cost-center. *assert unknown call-site rejected/flagged* [Source: FR-B19]

## 🔨 Tasks

- [ ] RED: `TestCostCenterCallSiteAndReportOnlyCapEvents` (AC1, AC3).
- [ ] GREEN: cost-center composition in `internal/meter`; `ai.cap.*` report-only consumer path (AC1–AC3).
- [ ] RED→GREEN: unknown-call-site flag (AC4); per-cost-center aggregation query (AC2).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** partial — cost-center dims (call-site×principal: feature/model + principal) captured in usage_events; ai.cap.* report-only journaling not wired (cmd/metering ignores those types).

Tier: `internal/meter` + `cmd/metering` consumer. NestJS analogue: an event consumer with a governed enum. Seam: the call-site enum is contracts-owned (CI-AST-enforced ceiling in the Studio repo). Sibling: E6-S1 (journal), E6-S3 (reporting reads cost-centers), E3-S4 (the actual cap push — distinct from these report-only events).

## 🧪 Testing

Named DoD test at the service tier; Postgres + Redis containers. CS §5: no own-package mocks; the spine is a true external.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §5 (governed enum), §11 (report-only discipline stated — cap events never enforce). SE-Arch: report-only-vs-enforce separation (A-METER vs A-CAP). cell-lint: cost-centers cell-local.

## 🔗 References

PRD Blcvui4UIn FR-B19, D-B9; Architecture o2ZqSrZjE6 A-METER.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: call-site enum + `ai.cap.*` schemas).
- [ ] Parent epic: E6. Intra-service: after E6-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
