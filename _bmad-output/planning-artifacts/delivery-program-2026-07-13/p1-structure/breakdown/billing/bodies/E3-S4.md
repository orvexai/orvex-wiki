## 🎯 Story

As **billing pushing caps to ai's LiteLLM backstop**, I want **a synchronous workload-identity call to ai's cap endpoint on every flip carrying both sibling cap lines**, so that **the `max_budget` backstop updates within seconds of an entitlement change (NFR-B2 fast path)**.

**Definition of Done:** `TestSyncCapPushTwoLinesWorkloadIdentity` — a service-tier test asserting a flip synchronously calls ai's `PUT /admin/v1/tenants/{tenant}/cap` (workload-identity route variant) pushing BOTH `ai_monthly_budget` and the sibling `embedding_monthly_budget`, keyed on the tenant id by construction. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a flip, When propagation runs, Then billing synchronously calls ai's `PUT /admin/v1/tenants/{tenant}/cap` over the workload-identity route variant (not the human `RequireFresh` admin path). *assert the call targets the workload-identity variant* [Source: FR-B7, A-CAP]
- [ ] **AC2** — Given the push, When sent, Then BOTH cap numbers are pushed as sibling lines — the user-action `ai_monthly_budget` and the separate `embedding_monthly_budget`. *assert two distinct numbers in the payload, not one nested figure* [Source: FR-B7, D-S15]
- [ ] **AC3** — Given `{tenant}`, When resolved, Then it equals the principal (tenant = user OR org) by construction — no mapping table. *assert tenant key == principal id* [Source: FR-B7, D-B2]
- [ ] **AC4** — Given the sync push fails/times out, When it errors, Then the flip is NOT lost: the canonical event (E3-S3) and reconcile (E6-S4) still converge ai's applied value. *assert push failure does not roll back the entitlement version; reconcile re-asserts* [Source: FR-B7, D-B5]
- [ ] **AC5 (negative)** — Given the ai cap-endpoint workload-identity delta is unfiled in contracts, When build is attempted, Then this story is blocked until the delta + ADR land. *assert dispatch gate references the filed contract delta* [Source: A-CAP, arch §7]

## 🔨 Tasks

- [ ] RED: `TestSyncCapPushTwoLinesWorkloadIdentity` with a stubbed ai endpoint (AC1, AC2, AC4).
- [ ] GREEN: cap-push client in `internal/cap` calling ai via lib workload identity; push both lines; failure-tolerant (AC1–AC4).
- [ ] Coordinate: ensure the ai cap-endpoint workload-identity delta + ADR are filed in contracts (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented — clients.AI.PushCap returns 'not implemented' (clients.go:56); only the two-line CapPush shape exists, no workload-identity PUT.

Tier: `internal/cap` push client (a true-external ai call) + `internal/entitlement` propagation. NestJS analogue: an outbound admin-client adapter. Seam: **cross-service contract delta to ai** — billing's highest-blast-radius delta (arch §7 ADR trigger); today ai's endpoint admits only the control-fronted `RequireFresh` admin path. Sibling: E3-S3 (canonical event), E6-S4 (reconcile re-assert).

## 🧪 Testing

Named DoD test at the service tier; the ai endpoint is a true external — stub/mock it (CS §5 network seam). Never mock own `internal/*`. Contract fixture round-trips the cap-push payload.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3.7 design-it-twice (the ai delta authored in contracts first) + §9 (ADR before build). SE-Arch: highest-blast-radius cross-service delta lens; defense-in-depth (event + push + reconcile). cell-lint: push is in-cell to the co-located ai.

## 🔗 References

PRD Blcvui4UIn FR-B7, D-S15, D-B5; Architecture o2ZqSrZjE6 A-CAP, §7 (ADR triggers).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG **must** include the ai cap-endpoint workload-identity delta + its ADR).
- [ ] Parent epic: E3. Intra-service: after E3-S1/S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (the ai cap-endpoint delta is a cross-service ADR — coordinate with ai + contracts owners).
