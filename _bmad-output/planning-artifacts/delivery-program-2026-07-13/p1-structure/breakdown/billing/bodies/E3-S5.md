## 🎯 Story

As **a user who just subscribed**, I want **the paywall to dissolve in under 60 seconds without a reload in both products**, so that **the flip is felt immediately (D-S20)**.

**Definition of Done:** `TestPaywallDissolvePushBothProducts` — an integration test asserting `billing.entitlement.changed` drives a <60s no-reload paywall dissolve in BOTH products: Studio via one BFF SSE endpoint re-pushing entitlement state, and the wiki via engine cache-evict → Socket.IO invalidate (billing carries the semantic in its event; the surfaces consume it). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a flip, When `billing.entitlement.changed` fires, Then Studio's BFF SSE endpoint re-pushes entitlement state to the app so the paywall dissolves <60s without reload. *assert SSE push within 60s carrying the new entitlement* [Source: FR-B7, D-S20]
- [ ] **AC2** — Given the same event, When the wiki consumes it, Then the engine evicts its cached copy → Socket.IO invalidate, dissolving the wiki paywall <60s without reload. *assert engine evict + Socket.IO invalidate path triggered* [Source: FR-B7, D-S20]
- [ ] **AC3** — Given billing's role, When the UI dissolves, Then billing carries the semantic in its event only — the SSE/Socket.IO surfaces are the consumers' responsibility (billing does not render). *assert billing emits the event; no billing-owned UI* [Source: FR-B7, non-goal (no custom billing UI)]
- [ ] **AC4 (negative)** — Given the event is dropped/delayed, When the deadline lapses, Then the reconcile/cache-TTL bound still converges the UI within the staleness window (no permanently stale paywall). *assert convergence within the bound on a dropped event* [Source: FR-B7, NFR-B2]

## 🔨 Tasks

- [ ] RED: `TestPaywallDissolvePushBothProducts` with a flip fixture and both consumer stubs (AC1, AC2).
- [ ] GREEN: ensure `billing.entitlement.changed` carries the fields the BFF SSE + engine evict need; document the consumer contract (AC1–AC3).
- [ ] RED→GREEN: dropped-event convergence via TTL/reconcile (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** absent — not implemented in this repo — no BFF SSE push, cache-evict is ErrNotImplemented; only the outbox event exists (the SPA/engine legs live elsewhere).

Tier: `internal/event` (billing's emit) + consumer contract docs. NestJS/engine analogue: the wiki engine's Socket.IO invalidate + BFF's SSE endpoint (both consumer-owned; billing supplies the event). Seam: `billing.entitlement.changed` schema in contracts is the only coupling. Sibling: E3-S3 (canonical publish), E2-S2 (cache TTL).

## 🧪 Testing

Named DoD test as an integration test with consumer stubs (BFF SSE + engine evict). CS §5: mock the consumer surfaces as true externals; never mock billing's own event publisher. Billing side asserts the event shape carries the needed fields.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §3 (billing emits, doesn't render — respect the no-custom-UI non-goal), §11 (dropped-event convergence stated). SE-Arch: seam-is-the-event lens (product-family-agnostic). cell-lint: consumers are co-located per-cell.

## 🔗 References

PRD Blcvui4UIn FR-B7, D-S20; Architecture o2ZqSrZjE6 A-CAP.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `billing.entitlement.changed` schema fields for UI dissolve).
- [ ] Parent epic: E3. Intra-service: after E3-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
