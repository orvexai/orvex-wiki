## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As **the orvex-wiki engine's quota enforcer**, I want **billing to own the per-plan wiki quota values in the same catalog and serve them in `caps{}`**, so that **the engine reads one source of truth for pages/storage/files/members/history and enforces at its own chokepoints**.

**Definition of Done:** `TestWikiQuotaValuesServedInCaps` — a service-tier test asserting `caps{}` carries `wiki_max_pages`, `wiki_storage_bytes_aggregate`, `wiki_max_file_bytes`, `wiki_max_files`, `wiki_max_members`, `wiki_history_retention` with the LOCKED Free values from provenance `H5NzkdsOzK`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2100); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the Free plan-version, When `caps{}` is read, Then the wiki quota siblings equal the LOCKED values: 200 pages / 1 GiB aggregate / 10 MB per file / 2,000 files / 25 members / history min(10 versions, 180 d) (provenance `H5NzkdsOzK`). *assert each value byte-exact* [Source: FR-B17a, D-B11]
- [ ] **AC2** — Given the £7 Personal plan-version, When `caps{}` is read, Then it carries 20,000 pages / 50 GiB / 50 MB-file / 20,000 files / 25 members / min(100 versions, 730 d) per D-S7, flagged PENDING CANON FREEZE (OQ-B10). *assert Personal values present + a `pending_freeze` marker; do NOT treat as final-frozen* [Source: FR-B17a, D-S7, OQ-B10]
- [ ] **AC3** — Given the engine reads quotas, When it enforces, Then billing serves values only (pull-only) and evicts on change; there is no push analogous to the ai cap endpoint. *assert no wiki push path; `billing.entitlement.changed` evicts the engine's cached copy* [Source: FR-B17a, A-CAP]
- [ ] **AC4** — Given over-quota, When the engine denies, Then the response is the shared frozen 402 `QUOTA_EXCEEDED` contract (authored once in contracts, never cached, never partial-written-around); downloads/exports always work; nothing auto-deleted. *assert the 402 shape matches the shared contract; read/export paths remain 200* [Source: FR-B17a, G5]
- [ ] **AC5 (negative)** — Given Teams/Enterprise, When quota values are requested, Then they are absent (deferred), not defaulted. *assert Teams/Enterprise wiki quotas are unset, not zero-defaulted* [Source: FR-B17a]

## 🔨 Tasks

- [ ] RED: `TestWikiQuotaValuesServedInCaps` for Free (AC1) + Personal pending-freeze (AC2).
- [ ] GREEN: wiki quota keys on plan-version cap policy; served in `caps{}`; pending-freeze marker on Personal (AC1–AC3).
- [ ] RED→GREEN: shared 402 `QUOTA_EXCEEDED` contract wiring for the engine's use (AC4); Teams/Enterprise-absent (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ a20d72f, 2026-07-14):** present — wiki quota values in the catalog (FreeCatalog locked, PersonalCatalog) served in caps{}, tested (gen/catalog_caps_test.go).

Tier: `internal/catalog` cap policy + check projection. NestJS/engine analogue: the wiki engine's F-QUOTA (engine PRD EPsdD7uK8e, FR-W10–W15) reads these into its Redis fast-counter chokepoints (page create, attachment upload, collab/Yjs). Seam: the 402 `QUOTA_EXCEEDED` shared contract (authored once in orvex-studio-contracts). Sibling: E3-S1 (AI caps), E3-S3 (eviction).

## 🧪 Testing

Named DoD test at the service tier; Postgres testcontainer. CS §5: no own-package mocks; the engine-side enforcement is out of scope here (engine repo) — this story serves values + the shared error contract only.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 — §10 (quota values are data, Free LOCKED, Personal pending-freeze — do not fabricate the freeze), §11 (OQ-B10 stated). SE-Arch: shared-contract lens (402 authored once). cell-lint: values served in-cell.

## 🔗 References

PRD Blcvui4UIn FR-B17a, D-B11, D-S7, OQ-B10, provenance `H5NzkdsOzK`; Architecture o2ZqSrZjE6 A-CAP; engine PRD EPsdD7uK8e (F-QUOTA, cross-ref).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2100 (contract TAG: `wiki_*` cap keys + the shared 402 QUOTA_EXCEEDED contract). Personal freeze OQ-B10 pending canon.
- [ ] Parent epic: E3. Intra-service: after E3-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE (OQ-B10 Personal quota freeze; Teams/Enterprise deferred).
