## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** auth edge, **I want** a cell-scoped **raw in-process Kafka consumer** on identity's revocation topic (`{domain}-events.{cell}` on `studio-spine`) — ADR-0013's canon-tier exception to Principle 2 — that applies each `token.revoked` broadcast to the deny-list evict-before-serve, replicating the ordering (partition-key = tenant id, ADR-0007), DLQ, and at-least-once guarantees a Knative Trigger would provide, **so that** revoked tokens stop verifying within the FR-I3 TTL bound.

**Definition of Done:** one named test `TestRevocationConsumerAppliesDenyListEvictBeforeServe` (integration — assert a `token.revoked` CloudEvent consumed off the cell-scoped topic adds the token to the deny-list before the next verify, that consumption is tenant-partition-ordered with DLQ on poison messages, and that a mis-celled event is rejected fail-closed). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a `token.revoked` CloudEvent, When the consumer applies it, Then the token is on the deny-list before the next verify (evict-before-serve). *assert: post-event verify of the revoked token → denied* [Source: FR-M11, NFR-M3]
- [ ] **AC2** — Given the consumer, When it subscribes, Then it is a raw in-process Kafka consumer (consumer group), not a Knative Trigger (ADR-0013 exception). *assert: subscription is a Kafka consumer group on `studio-spine`* [Source: NFR-M3]
- [ ] **AC3** — Given ordering guarantees, When events flow, Then consumption is partition-keyed by tenant id (ADR-0007) with DLQ + at-least-once. *assert: per-tenant order preserved; a poison message routes to DLQ, not silent drop* [Source: NFR-M3]
- [ ] **AC4** (negative) — Given a CloudEvent whose `orvexcell` extension does not match this cell, When consumed, Then it is rejected fail-closed. *assert: mis-celled event rejected; deny-list unmodified* [Source: FR-M11, NFR-M3]
- [ ] **AC5** — Given the revocation transport, When traced, Then it arrives via identity's transactional-outbox relay straight to Kafka — no Redis→Kafka bridge, no Redis Streams stage (D-S13). *assert: no intermediate Redis hop in the consume path* [Source: NFR-M3]

## 🔨 Tasks

- [ ] RED: write `TestRevocationConsumerAppliesDenyListEvictBeforeServe` (AC1–AC5).
- [ ] GREEN: implement the raw in-process Kafka consumer group on the cell-scoped topic (AC2).
- [ ] GREEN: apply revocations to the deny-list evict-before-serve; tenant-partition order + DLQ (AC1, AC3).
- [ ] GREEN: `orvexcell` fail-closed check on each event; no Redis bridge (AC4, AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — raw in-process kafkajs revocation consumer (src/events/revocation-consumer.ts + src/events/kafka-driver.ts) applies each token.revoked CloudEvent to the shared deny-list, idempotent + DLQ on malformed; test/events/revocation-consumer.test.ts (9).

Tier: edge/auth seam + A-REACTIVE (Arch §1, §9 OD-6 resolved by ADR-0013). Seam: MCP↔Kafka `studio-spine` (consume only), MCP↔identity (outbox relay, D-S13). Sibling deps: E4-S2 (deny-list check), E5-S3 (cell-scoped topic name), E7-S2 (consumer-lag SLI). This is the sole resident subscription.

## 🧪 Testing

`TestRevocationConsumerAppliesDenyListEvictBeforeServe` (integration). Tiers: unit (deny-list apply) + integration (consumer). CS §5: fake Kafka + deny-list store from fixtures; never mock own consumer logic.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (canon-exception justification, fail-closed, dead-consumer lenses); cell-lint `JGAUQRsw2g`. Consumer must replicate Trigger guarantees (ADR-0013).

## 🔗 References

PRD `k1sWjtJq3x` §6 dependencies, NFR-M3, Change log 2026-07-08 (D-M9); Arch `BE0jTNoweS` §1, A-REACTIVE, §9 OD-6; ADR-0013 `F7tTjMNVAr`; canon `CxjFpIVUZY` P2, D-S13, ADR-0007.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — CloudEvent envelope + `orvexcell` extension); parent epic E5. Intra-service: E4-S2, E5-S3, E7-S2. Externally: identity outbox relay.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
