## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** platform, **I want** the golden-tape KPI eval harness — every common task ≤ 2 calls and ≤ ~1k tokens round-trip, live-counted, plus verified-write receipts and the cited-ask K5 shape — to run against **committed fixtures** in CI and post-deploy, blocking promotion on any regression, **so that** the context-light contract is executable and defended, not aspirational.

**Definition of Done:** one named test `TestGoldenTapeKpiBlocksOnRegression` (integration — assert the harness counts calls + tokens per common task against committed fixtures, asserts the ≤2-calls / ≤~1k-tokens budgets + receipt/K5 shapes, and that a deliberately inflated task reddens the suite and blocks promotion). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a common task, When the tape runs, Then it live-counts calls and tokens and asserts ≤ 2 calls / ≤ ~1k tokens. *assert: per-task call count ≤ 2 and token count ≤ ~1k* [Source: FR-M5, NFR-M6]
- [ ] **AC2** — Given a write task, When the tape runs, Then it asserts the verified-write receipt `{url,id,version,persisted:true}` (never a body). *assert: receipt shape verified in tape* [Source: FR-M5]
- [ ] **AC3** — Given an ask task, When the tape runs, Then it asserts the K5 cited-ask shape unchanged across upstream repoints. *assert: K5 six-field shape asserted; unchanged pre/post ask-repoint* [Source: FR-M5, NFR-M6]
- [ ] **AC4** — Given committed golden fixtures (not live dev backstage), When the tape runs in CI, Then it uses them (CS §5). *assert: tape runs against committed fixtures; the live-dev-backstage posture retires* [Source: FR-M5, NFR-M6]
- [ ] **AC5** (gate) — Given a tape regression, When CI/post-deploy runs, Then promotion is blocked. *assert: an inflated task reddens the suite and blocks the pipeline* [Source: NFR-M6]

## 🔨 Tasks

- [ ] RED: write `TestGoldenTapeKpiBlocksOnRegression` (AC1–AC5).
- [ ] GREEN: implement/port the golden-tape harness with live call+token counting (AC1).
- [ ] GREEN: assert receipt + K5 shapes (AC2, AC3).
- [ ] GREEN: commit golden fixtures replacing live-dev-backstage runs (AC4); wire CI + post-deploy blocking gate (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — golden-tape KPI harness (≤2 calls/≤~1k tokens) with committed fixtures and tape-accounting helper (test/golden-tape-harness.test.ts, golden-tape-r0.test.ts, test/helpers/tape-accounting.ts); wired as the golden-tape-kpi CI release gate (ci.yml:90).

Tier: envelope shaping + CI (Arch A-SHIM, A-CLIENT). Seam: MCP↔CI. Named tech debt: the shipped tape runs against live dev Docmost backstage — this story commits fixtures (retires at R2 with the codegen path). Sibling deps: E3 (upstreams exercised), E1-S2 (hero budget), E1-S3 (whoami 1-RT).

## 🧪 Testing

`TestGoldenTapeKpiBlocksOnRegression` (integration + CI gate). Tiers: integration (tape). CS §5: committed golden fixtures for all upstreams; never mock own harness. This is the counter-metric to "just add a helper tool."

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11 (esp. §5 committed fixtures); SE-Arch `8sYi523i4t` (KPI-as-gate, fake-done-antibody lenses); cell-lint `JGAUQRsw2g`.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M5, §5 NFR-M6, G2; Arch `BE0jTNoweS` A-SHIM, A-CLIENT §8.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — fixtures pin to the contract surface); parent epic E7. Intra-service: E3-*, E1-S2/S3.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
