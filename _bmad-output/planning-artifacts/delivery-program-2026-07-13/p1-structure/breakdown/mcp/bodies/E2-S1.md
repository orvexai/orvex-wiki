## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** agent, **I want** `get_page` to expose a graduated read ladder (info → outline with `token_estimate` and no body → `blocks/:id?format=dfm`, ranged reads) with flexible locators (`{id | slugId | url | title}`), **so that** I read exactly the slice I need within the token budget instead of pulling a whole page.

**Definition of Done:** one named test `TestGetPageReadLadderAndLocatorResolution` (integration — assert the info rung returns `token_estimate` with no body, the outline rung returns structure only, the blocks rung returns DfM for a requested block id, and each locator form resolves via the upstream resolver). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a page locator, When `get_page` requests the info rung, Then it returns `token_estimate` and metadata with no page body. *assert: `getPage(info).body === undefined && typeof token_estimate === 'number'`* [Source: FR-M3]
- [ ] **AC2** — Given the outline rung, When requested, Then structure/headings return without full block bodies. *assert: outline response has heading nodes, no block text bodies* [Source: FR-M3]
- [ ] **AC3** — Given a block id, When `blocks/:id?format=dfm` is requested, Then that block returns as DfM. *assert: `getPage(blocks,id).format === 'dfm'` and body present* [Source: FR-M3]
- [ ] **AC4** — Given each locator form `{id|slugId|url|title}`, When passed, Then resolution delegates to wiki-api/engine resolvers. *assert: all four forms resolve to the same page id via the upstream resolver; title-locator honors the ~100-recent window caveat* [Source: FR-M10]
- [ ] **AC5** (negative) — Given an unresolvable locator, When passed, Then a typed not-found error returns, never a fabricated page. *assert: unknown title yields a typed not-found* [Source: FR-M10, NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestGetPageReadLadderAndLocatorResolution` (AC1–AC5).
- [ ] GREEN: implement the three-rung read ladder mapping to the wiki-api read verbs (AC1–AC3).
- [ ] GREEN: pass all four locator forms to the upstream resolver; carry the shipped title-window caveat (AC4).
- [ ] RED→GREEN: unresolvable-locator typed not-found (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — get_page read ladder (outline→section→full, auto-degrade over token_budget) with id/slug/url/title locators at src/server/tools.ts:729; ≤2-call accounting exercised by test/golden-tape-harness.test.ts.

Tier: tool registry/dispatch over backstage adapter (Arch §6). Seam: MCP↔wiki-api (resolution + read verbs live upstream; MCP adds no resolution logic). Sibling deps: E3-S1/S2 (client), E4-S3 (token).

## 🧪 Testing

`TestGetPageReadLadderAndLocatorResolution` (integration). Tiers: unit (ladder mapping) + integration (locator resolution). CS §5: fake wiki-api from golden fixtures; never mock own dispatch. Locator resolution is asserted through the fixture, not reimplemented.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (token-budget lens); cell-lint `JGAUQRsw2g`. Read-ladder truncation is presentation, retires into wiki-api shaping at R2 (A-SHIM) — TBD, pinned by ENG-2102.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M3, FR-M10; Arch `BE0jTNoweS` §3 `get_page` row, A-SHIM §6.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E2. Intra-service: E3-S1/S2, E4-S3.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
