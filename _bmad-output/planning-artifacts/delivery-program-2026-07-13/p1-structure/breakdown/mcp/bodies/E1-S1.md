## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** MCP-speaking agent (Claude Code/Desktop or a peer), **I want** the gateway to serve MCP over Streamable-HTTP (`/mcp`) and stdio, self-describing on connect, **so that** I can orient — grammar, verbs, capabilities — in one connection without out-of-band docs.

**Definition of Done:** one named test `TestMcpServerSelfDescribesOnConnect` (integration layer — drive the MCP SDK client against the server core, assert connect-time `instructions` are non-empty, the `orvex://authoring-guide` static resource is readable, and both `/mcp` Streamable-HTTP and stdio transports complete an initialize handshake). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a fresh MCP client, When it connects over Streamable-HTTP `/mcp`, Then `initialize` succeeds and connect-time `instructions` are returned. *assert: `initializeResult.instructions.length > 0`* [Source: FR-M1]
- [ ] **AC2** — Given a local process, When it launches the stdio entrypoint, Then the same server core serves and completes `initialize`. *assert: stdio transport `initialize` returns the same `serverInfo.name`* [Source: FR-M1]
- [ ] **AC3** — Given a connected client, When it reads resource `orvex://authoring-guide`, Then the static DfM-grammar/verb-reference document returns. *assert: `readResource('orvex://authoring-guide').contents[0].text` contains the DfM grammar marker* [Source: FR-M1, FR-M15]
- [ ] **AC4** — Given stdio mode serves the authoring-guide resource unauthenticated, When any non-static or tenant-derived resource is added, Then a standing gate blocks ship until an stdio auth re-audit. *assert: a test-registered tenant-derived resource fails a `assertStaticOnly` guard* [Source: FR-M15]
- [ ] **AC5** (negative) — Given a malformed initialize payload, When the client connects, Then the server rejects with a typed protocol error, never a partial session. *assert: error code is an MCP protocol error, session not established* [Source: FR-M1]

## 🔨 Tasks

- [ ] RED: write `TestMcpServerSelfDescribesOnConnect` driving both transports (AC1, AC2, AC3).
- [ ] GREEN: wire the official MCP SDK server core; register connect-time `instructions` + `orvex://authoring-guide` static resource (AC1, AC3).
- [ ] GREEN: add the stdio entrypoint sharing the same server core (AC2).
- [ ] GREEN: add `assertStaticOnly` resource guard as a standing gate (AC4).
- [ ] RED→GREEN: malformed-initialize rejection path (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — Streamable-HTTP transport (src/transport/http.ts, /mcp initialize gated) + stdio (src/transport/stdio.ts); connect-time instructions SERVER_INSTRUCTIONS (src/server/index.ts:27) and orvex://authoring-guide resource (src/server/resources.ts:150); tests test/transport/standalone-boot.test.ts + stdio-verify.test.ts.

Tier: tool registry/dispatch + envelope shaping (Arch §6). Seam: MCP↔agent (protocol). Sibling deps: none inbound; E1-S2/S3/S4 register onto this core. No engine analogue — TS satellite on the official MCP SDK (D-M1).

## 🧪 Testing

`TestMcpServerSelfDescribesOnConnect` (integration). Tiers: unit (resource guard) + integration (transport handshakes). CS §5 mocking: mock the transport peer only; never mock own server core or SDK. No upstream calls in this story.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` lenses (self-description honesty, fail-closed on malformed); cell-lint `JGAUQRsw2g`. Keep handlers thin (A-SHIM).

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M1, FR-M15; Arch `BE0jTNoweS` §1 (stdio row), §3, A-SHIM; Thin Build Plan `6e2jqBdknZ`.

## 🔗 Dependencies

Blocked by: ENG-2102 (Definition Pack — contract TAG is the dispatch gate); parent epic E1. Intra-service: none.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
