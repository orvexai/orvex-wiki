## 🗺️ Feature area

The MCP protocol surface is the agent-facing front door: a stateless TypeScript gateway on the official MCP SDK that serves **Streamable-HTTP (`/mcp`)** and **stdio**, self-describing so an agent can orient in one connection. This epic owns the transport core, the self-description (connect-time `instructions` + the static `orvex://authoring-guide` resource + a `whoami` capability probe), the curated hero tool surface with a `list_tools` long tail, and the DfM-only agent contract at the boundary. It exists because the whole product thesis is "one agent door, context-light" — the surface must be legible, small, and honest about what it can do before any verb behind it matters (PRD §4 F1; Arch §3, A-SHIM).

Nothing here changes what is *true* (no ranking, no serialization, no ACL — those live upstream). This epic makes the door itself correct: right transports, right hero budget, right self-description, right block-format contract.

## 📋 Story roster

- [ ] E1-S1 — Streamable-HTTP + stdio server core with connect-time instructions & authoring-guide resource
- [ ] E1-S2 — Curated hero surface (≤~13 eager tools) + list_tools(category?) progressive disclosure
- [ ] E1-S3 — whoami: verified principal + capability probe in one round-trip
- [ ] E1-S4 — DfM-only agent contract: reject raw markdown, opaque-node handle passthrough

## 🎯 FR ids owned

FR-M1 (protocol surface + self-description), FR-M2 (hero surface + progressive disclosure), FR-M4 (DfM-only contract), FR-M14 (whoami), FR-M15 (stdio resource posture — shared with E4-S5).

## ✅ Definition of Done

- [ ] All four stories Done (reviewer ≠ implementer on each).
- [ ] Family six-surface E2E share green for the `mcp` surface (connect → `whoami` → `list_tools` on a fresh tenant).
- [ ] SDD lines evidenced: each story's named `Test<Name>` green in CI; hero-set budget asserted by test, not by eyeball.
- [ ] Golden-tape KPI (E7-S1) unaffected by the surface — connect + orient stays inside the call/token budget.

## 🧠 Context

TS non-engine satellite — no CS §6 six-tier model applies; target tier boundaries are Edge/auth · Backstage adapter · Tool registry/dispatch · Envelope shaping (Arch §6, ADR-0004 proposed). This epic lives in the **tool registry/dispatch** and **envelope shaping** tiers. Seam crossed: MCP↔agent (protocol). Sibling deps: E4 (auth edge feeds `whoami` principal), E2/E3 (verbs the hero set fronts).

## 🔗 Dependencies

- **Blocked by:** ENG-2102 (Definition Pack — the contract TAG is the dispatch gate).
- Intra-service: E1-S3 consumes E4-S1's principal; otherwise independent.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.

## 🔗 References

PRD `k1sWjtJq3x` §4 F1; Arch `BE0jTNoweS` §3, §6, A-SHIM; Thin Build Plan `6e2jqBdknZ`.
