## 🎯 Story

**As an** agent, **I want** `marketplace_search` and `skill_get` to re-home off the Studio backend onto orvex-studio-knowledge (Decision 4 / C21 — knowledge is the family's one multi-corpus search service), reached MCP→knowledge-direct on my token, **so that** all search flavors sit behind the single ACL-enforcing retrieval choke and the Studio backend is narrowed to product tools only.

**Definition of Done:** one named test `TestSearchPairReHomesToKnowledge` (integration — assert `marketplace_search` and `skill_get` target the knowledge base URL directly carrying the caller token, no longer `STUDIO_BACKEND_BASE_URL`, and that knowledge enforces admission on the caller's principal). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `marketplace_search`, When called, Then it targets knowledge directly (corpus read on the caller's token), not the Studio backend. *assert: request targets `KNOWLEDGE_BASE_URL`; Studio backend not called* [Source: FR-M19]
- [ ] **AC2** — Given `skill_get`, When called, Then it likewise reaches knowledge as a corpus read on the caller's token. *assert: `skill_get` targets `KNOWLEDGE_BASE_URL`* [Source: FR-M19]
- [ ] **AC3** — Given the re-home, When either runs, Then knowledge enforces ACL ∩ token-scope admission on the caller's principal (MCP does no filtering). *assert: caller token rides to knowledge; MCP returns results unfiltered* [Source: FR-M19]
- [ ] **AC4** — Given `studio-wrapper.ts`, When reconciled, Then the two search routes are removed from the Studio-backend wiring, leaving the six product tools. *assert: wrapper no longer wires the two search tools to `STUDIO_BACKEND_BASE_URL`* [Source: FR-M19]
- [ ] **AC5** (negative) — Given knowledge down, When either search tool is called, Then it fails typed, never fabricated. *assert: knowledge-down search → typed error* [Source: FR-M19, NFR-M2]

*Note: whether the two search flavors converge on one knowledge endpoint is a contracts-seam item — TBD, pinned by ENG-2102.*

## 🔨 Tasks

- [ ] RED: write `TestSearchPairReHomesToKnowledge` (AC1–AC5).
- [ ] GREEN: route `marketplace_search`/`skill_get` to the knowledge client (reuse E3-S4 plumbing) (AC1, AC2).
- [ ] GREEN: caller-token pass-through; assert no MCP-side filtering (AC3).
- [ ] GREEN: reconcile `studio-wrapper.ts` to six product tools (AC4); typed failure on knowledge-down (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** partial — studio_marketplace_search (src/server/studio-tools.ts:71) and studio_skill_get (:110) exist but route to STUDIO_BACKEND_BASE_URL via StudioWrapper; the Decision-4/C21 re-home onto orvex-studio-knowledge is NOT implemented (no knowledge routing exists in the MCP).

Tier: backstage adapter + tool registry (Arch §3 search-pair row). Seam: MCP↔knowledge (direct, D-M8). Sibling deps: E3-S4 (knowledge-direct plumbing), E6-S1 (leaves six product tools). Decision 4 / C21.

## 🧪 Testing

`TestSearchPairReHomesToKnowledge` (integration). Tiers: unit (routing) + integration (direct-knowledge). CS §5: fake knowledge from golden fixtures; never mock own routing.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (single-choke, ACL lenses); cell-lint `JGAUQRsw2g`. Reuse the E3-S4 knowledge client; do not add a second retrieval path.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M19, D-M8; Arch `BE0jTNoweS` §3 `marketplace_search`/`skill_get` row; family Decision 4 / C21.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — knowledge multi-corpus search endpoint(s)); parent epic E6. Intra-service: E3-S4, E6-S1.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
