## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** agent, **I want** `save_page` and `edit` to be CAS-guarded writes (`ifVersion` → `409 VERSION_MISMATCH`) that return a verified read-after-write receipt `{url,id,version,persisted:true}` and surface `402 QUOTA_EXCEEDED` verbatim, **so that** I never overwrite blindly, never mistake a rendered echo for a persist, and always see quota denials honestly.

**Definition of Done:** one named test `TestWriteChokepointCasReceiptAndQuotaPassthrough` (integration — assert a stale `ifVersion` yields `409 VERSION_MISMATCH` verbatim, a successful write returns `{url,id,version,persisted:true}` (never a re-rendered body), and an upstream `402 QUOTA_EXCEEDED` is passed through unretried/unmasked). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a write with a stale `ifVersion`, When submitted, Then `409 VERSION_MISMATCH` is returned verbatim, no retry. *assert: response code == `409 VERSION_MISMATCH`, upstream called exactly once* [Source: FR-M3]
- [ ] **AC2** — Given a successful `save_page`/`edit`, When it persists, Then the response is the verified receipt `{url,id,version,persisted:true}`, never a re-rendered body. *assert: response set-equals the receipt shape; no `body`/`content` field present* [Source: FR-M3]
- [ ] **AC3** — Given `edit` with an atomic block-patch batch, When submitted, Then it forwards as one atomic upstream call, never decomposed client-side. *assert: multi-block `edit` maps to a single `PATCH …/blocks` call* [Source: FR-M3]
- [ ] **AC4** — Given the engine F-QUOTA denial, When the write hits the quota cap, Then `402 QUOTA_EXCEEDED` is surfaced verbatim — not retried, partially applied, or masked as success. *assert: response == `402 QUOTA_EXCEEDED`, no partial write* [Source: FR-M3]
- [ ] **AC5** (negative) — Given the MCP, When any write is attempted, Then it composes no quota logic and no CAS logic of its own. *assert: no MCP-side quota/version computation; both come from the upstream response* [Source: FR-M3]

## 🔨 Tasks

- [ ] RED: write `TestWriteChokepointCasReceiptAndQuotaPassthrough` (AC1–AC5).
- [ ] GREEN: map `save_page`/`edit` to the wiki-api write verbs with `ifVersion` CAS; return the verified receipt (AC1, AC2).
- [ ] GREEN: forward atomic block-patch batches as one upstream call (AC3).
- [ ] GREEN: pass `409`/`402` through verbatim; no retry, no mask (AC4, AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — save_page (src/server/tools.ts:1003) requires ifVersion for update, verifies via read-after-write receipt {persisted}; edit (:1351) requires CAS on every mutation mode; 402 QUOTA_EXCEEDED surfaced verbatim in src/backstage/wrapper.ts:646; test/tools/eng-2041-save-page-persisted.test.ts.

Tier: tool registry/dispatch over backstage adapter (Arch §6). Seam: MCP↔wiki-api (atomicity, DfM, ACL upstream — FR-A6). Sibling deps: E1-S4 (DfM contract), E3-S1/S2 (client), E4-S3 (token).

## 🧪 Testing

`TestWriteChokepointCasReceiptAndQuotaPassthrough` (integration). Tiers: unit (receipt mapping) + integration (CAS/quota passthrough). CS §5: fake wiki-api from golden fixtures incl. 409/402 fixtures; never mock own dispatch.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (no-mask / no-fallback lens); cell-lint `JGAUQRsw2g`. Receipt is `persisted:true`, never a rendered body — the honesty invariant.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M3; Arch `BE0jTNoweS` §3 `save_page`/`edit` rows, A-SHIM; wiki-api FR-A6, engine F-QUOTA.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E2. Intra-service: E1-S4, E3-S1/S2, E4-S3.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
