## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** agent, **I want** `get_changes` (changes-since / history / `blocks/diff` → `changedBlockIds`) to be a pull on my own scoped token — no SSE subscription, no admin-token dependency, no resident worker — **so that** reactivity never requires a credential broader than mine.

**Definition of Done:** one named test `TestGetChangesIsScopedPullNoElevatedCredential` (integration — assert `get_changes` returns changes-since / `changedBlockIds` via the wiki-api surface carrying only the caller's scoped token, and that no admin token or SSE subscription is used). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a since-cursor, When `get_changes` is called, Then changes-since / history returns via wiki-api on the caller's scoped token. *assert: upstream call carries the caller token; response includes `changedBlockIds`* [Source: FR-M9]
- [ ] **AC2** — Given the reactivity model, When `get_changes` runs, Then it uses pull only — no SSE subscription, no push channel, no resident worker. *assert: no subscription established; no admin-token header on the upstream call* [Source: FR-M9]
- [ ] **AC3** — Given `blocks/diff`, When requested, Then it returns `changedBlockIds` mapped from the wiki-api diff verb. *assert: `get_changes(diff)` returns `changedBlockIds[]`* [Source: FR-M9]
- [ ] **AC4** (negative) — Given a scoped key that cannot reach a resource, When `get_changes` is called, Then it fails closed with a typed denial (the Phase-3 403 lesson: a scoped key never needs an admin token). *assert: insufficient-scope `get_changes` returns a typed denial, no admin escalation* [Source: FR-M9, NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestGetChangesIsScopedPullNoElevatedCredential` (AC1–AC4).
- [ ] GREEN: map `get_changes` to wiki-api changes-since / history / `blocks/diff` on the caller token (AC1, AC3).
- [ ] GREEN: assert no subscription/worker/admin-token path exists (AC2).
- [ ] RED→GREEN: fail-closed on insufficient scope (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — get_changes (src/server/tools.ts:2485) list mode (changes-since) + block mode (history→diff→changedBlockIds) on the caller's forwarded token, no SSE/admin token; admin-firehose reactive subsystem removed (src/server/reactive.ts).

Tier: tool registry/dispatch over backstage adapter (Arch §6, A-REACTIVE). Seam: MCP↔wiki-api. The abandoned SSE-firehose is a permanent constraint, not interim. Sibling deps: E3-S1/S2, E4-S3.

## 🧪 Testing

`TestGetChangesIsScopedPullNoElevatedCredential` (integration). Tiers: unit (mapping) + integration (scoped-token pull). CS §5: fake wiki-api from golden fixtures; never mock own dispatch. Assert absence of subscription code path.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (least-privilege lens); cell-lint `JGAUQRsw2g`. No MCP feature may require a credential broader than the caller's (A-REACTIVE).

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M9; Arch `BE0jTNoweS` §3 `get_changes` row, A-REACTIVE.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E2. Intra-service: E3-S1/S2, E4-S3.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
