## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** wiki platform, **I want** human-gated actions to be transport-only: the MCP returns `needs_human_publish` / `needs_human_confirm` plus a Studio deep-link, transports server-minted RATIFY/CONFIRM tokens **verbatim** when a human supplies them, and never promotes draft→canonical on any path — **so that** "AI never self-promotes" survives the re-plumbing end-to-end.

**Definition of Done:** one named test `TestHumanGatedActionsTransportOnlyNeverPromote` (integration — assert a gated write returns `needs_human_*` + a Studio deep-link, a supplied RATIFY/CONFIRM token is forwarded verbatim without being parsed/minted/cached, and no MCP path can promote draft→canonical). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a write requiring human publish, When submitted without a token, Then the MCP returns `needs_human_publish` plus a Studio deep-link, unshaped. *assert: response == `needs_human_publish` with a deep-link; no promotion occurred* [Source: FR-M13]
- [ ] **AC2** — Given a human-supplied RATIFY/CONFIRM token, When a gated action is retried, Then the token is transported verbatim to the engine chokepoint (via wiki-api), never parsed/minted/cached/logged. *assert: outbound RATIFY/CONFIRM byte-equals the supplied token; not logged* [Source: FR-M13]
- [ ] **AC3** — Given any path, When the MCP processes a request, Then it MUST NOT promote draft→canonical. *assert: no code path issues a promote; a promote attempt is impossible by construction* [Source: FR-M13]
- [ ] **AC4** — Given a `needs_human_confirm` response, When it passes through, Then it is unshaped (opaque strings from the MCP's view). *assert: `needs_human_confirm` passes through unmodified* [Source: FR-M13]
- [ ] **AC5** (negative) — Given a forged/absent RATIFY token, When a gated action is attempted, Then the engine chokepoint denies it; the MCP never substitutes or mints one. *assert: absent-token gated action stays gated; MCP mints nothing* [Source: FR-M13, NFR-M3]

## 🔨 Tasks

- [ ] RED: write `TestHumanGatedActionsTransportOnlyNeverPromote` (AC1–AC5).
- [ ] GREEN: return `needs_human_*` + Studio deep-link on gated writes (AC1, AC4).
- [ ] GREEN: transport RATIFY/CONFIRM verbatim, no parse/mint/cache/log (AC2).
- [ ] GREEN: assert no promote path exists anywhere (AC3, AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — edit governance modes publish/ratify/supersede/delete_page (src/server/tools.ts:1351) return needs_human_*/needs_human_confirm and only transport a supplied confirm_token/ratify_token verbatim to the backstage, never self-mint or self-promote; test/tools/ac8-human-gating.test.ts.

Tier: tool registry/dispatch + edge/auth (Arch §6, A-TOKENS). Seam: MCP↔wiki-api (RATIFY/CONFIRM minted at engine chokepoint, D-A8), MCP↔identity (human-gating resolved by identity). Sibling deps: E2-S2 (gated write statuses), E4-S3 (pass-through).

## 🧪 Testing

`TestHumanGatedActionsTransportOnlyNeverPromote` (integration). Tiers: unit (transport) + integration (gated flow). CS §5: fake wiki-api chokepoint from golden fixtures; never mock own transport. Assert no promote code path (structural test).

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (AI-never-self-promotes, no-mint lenses); cell-lint `JGAUQRsw2g`. RATIFY/CONFIRM are opaque strings from the MCP's point of view.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M13, G3; Arch `BE0jTNoweS` A-TOKENS; wiki-api D-A8.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E4. Intra-service: E2-S2, E4-S3.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
