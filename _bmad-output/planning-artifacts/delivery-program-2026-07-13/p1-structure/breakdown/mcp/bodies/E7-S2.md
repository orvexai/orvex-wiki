## 🎯 Story

**As an** operator (via orvex-studio-console), **I want** the MCP's operability surface — `/health` liveness-only, `/healthz` cell echo, readiness that degrades when the revocation consumer stalls, and OTel traces + Prometheus metrics feeding LGTM (per-tool latency/error, shim-overhead SLI, revocation-consumer lag / last-applied-offset, introspection hit/miss + staleness, deny-by-default scope rejections, the golden-tape KPI as a metric) — **so that** a dead consumer never reads green and the family health board sees the MCP honestly.

**Definition of Done:** one named test `TestOperabilitySignalsShimOverheadAndConsumerLag` (integration — assert the shim-overhead SLI is exported, that readiness degrades when the revocation consumer stalls (no offset progress within the window), and that per-tool + revocation-lag SLIs reach the LGTM exporter). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the MCP, When metrics are scraped, Then per-tool latency/error + the shim-overhead SLI (MCP time minus wiki-api time) are exported; shim overhead p95 ≤ 20 ms in-cell. *assert: shim-overhead metric present; p95 ≤ 20 ms under the test load* [Source: FR-M18, NFR-M1]
- [ ] **AC2** — Given a stalled revocation consumer, When readiness is probed, Then it degrades (no offset progress within a bounded window). *assert: stalled consumer → readiness not-ready; healthy consumer → ready* [Source: FR-M18]
- [ ] **AC3** — Given the consumer, When it runs, Then revocation-consumer lag / last-applied-offset is exported to LGTM. *assert: lag + last-applied-offset metrics present* [Source: FR-M18]
- [ ] **AC4** — Given the golden-tape KPI, When exported, Then it appears as a metric consumed by console; the MCP emits no audit of its own (audit stays engine/identity-side). *assert: golden-tape KPI metric present; no MCP-side audit stream* [Source: FR-M18]
- [ ] **AC5** (negative) — Given a dead upstream, When `/health` is probed, Then it stays green (liveness-only, no dependency round-trips). *assert: dead upstream leaves `/health` green, readiness reflects the real dependency state* [Source: FR-M18]

## 🔨 Tasks

- [ ] RED: write `TestOperabilitySignalsShimOverheadAndConsumerLag` (AC1–AC5).
- [ ] GREEN: export per-tool + shim-overhead SLIs via OTel/Prometheus to LGTM (AC1).
- [ ] GREEN: readiness degrades on consumer stall; export lag/offset (AC2, AC3).
- [ ] GREEN: export the golden-tape KPI metric; keep `/health` liveness-only (AC4, AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** partial — /health + /healthz (src/transport/http.ts:286,294) + shim-overhead SLI gated in CI (test/golden-tape-sli.test.ts, p95≤20ms) + revocation-lag surfaced in cellHealth. Missing: NO OTel/Prometheus instrumentation or /metrics export to LGTM (grep for otel/prometheus/metrics under src/ is empty) — SLIs are test-measured, not emitted as live telemetry.

Tier: operability (Arch §8). Seam: MCP↔console (telemetry), MCP↔LGTM. Sibling deps: E5-S2 (consumer lag source), E5-S3 (`/healthz`), E7-S1 (KPI metric). Fixes the dead-consumer-reads-green risk (Arch §5).

## 🧪 Testing

`TestOperabilitySignalsShimOverheadAndConsumerLag` (integration). Tiers: unit (metric emission) + integration (readiness/stall). CS §5: fake Kafka/LGTM exporter from fixtures; never mock own metrics layer.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11 (esp. §10 operability); SE-Arch `8sYi523i4t` (operational-excellence, no-fake-green lenses); cell-lint `JGAUQRsw2g`.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M18, §5 NFR-M1; Arch `BE0jTNoweS` §5 revocation-liveness, §8 operability.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E7. Intra-service: E5-S2, E5-S3, E7-S1.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
