## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** platform, **I want** the MCP's TypeScript verifier binding to verify caller tokens at the edge per the identity/lib-auth contract — multi-issuer JWKS (Clerk global / Keycloak realm, selected by `iss` against identity's registry) → claims + `aud` + frozen-claim checks → deny-list check → deny-by-default scope gate → the org-scoped principal `{idp,subject,tenant,org_or_realm,roles,token_scope}` — **so that** every downstream call resolves tenancy at org granularity and a token can never exceed its scope through the MCP.

**Definition of Done:** one named test `TestEdgeVerifierEmitsOrgScopedPrincipalDenyByDefault` (integration — assert a valid multi-issuer token yields the full org-scoped principal, an unknown-issuer/expired/wrong-audience token is denied, and a request without a matching scope is denied by default). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Clerk-global or Keycloak-realm token, When verified, Then the issuer resolves against identity's registry and JWKS validates the signature. *assert: each `iss` selects the correct JWKS; valid signature passes* [Source: FR-M11]
- [ ] **AC2** — Given a valid token, When verification completes, Then the principal `{idp,subject,tenant,org_or_realm,roles,token_scope}` is emitted, org-scoped (solo user = personal org). *assert: principal set-equals the expected org-scoped shape* [Source: FR-M11]
- [ ] **AC3** — Given a request whose scope does not cover the operation, When the scope gate runs, Then it is denied by default. *assert: scope-insufficient request → typed denial; unknown scope defaults deny* [Source: FR-M11]
- [ ] **AC4** (negative) — Given an unknown-issuer / expired / wrong-audience / missing-`exp` token, When verified, Then it is denied fail-closed. *assert: each case returns a typed denial, never a principal* [Source: FR-M11, NFR-M3]
- [ ] **AC5** — Given the MCP, When verifying, Then it never contacts Clerk/Keycloak directly (only identity's verification surfaces). *assert: no direct IdP call; JWKS/registry come from identity* [Source: FR-M11]

## 🔨 Tasks

- [ ] RED: write `TestEdgeVerifierEmitsOrgScopedPrincipalDenyByDefault` (AC1–AC5).
- [ ] GREEN: build the multi-issuer JWKS resolver + claims/aud/frozen-claim checks (AC1, AC2, AC4).
- [ ] GREEN: deny-by-default scope gate → principal in request context (AC3).
- [ ] GREEN: point JWKS/registry at identity's verification surfaces only (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — multi-issuer JWKS verify + aud/frozen-claim checks + deny-by-default token_scope gate in src/auth/identity-verifier.ts and src/auth/scope-gate.ts (assertTokenScope/intersectScope); tests test/auth/identity-verifier.test.ts (22) + scope-gate.test.ts (11).

Tier: edge/auth seam `src/auth/*` — never shapes content (Arch §6, A-AUTH). Seam: MCP↔identity (verification only). The shipped Phase-0 RS256/JWKS + `jti`-replay verifier is the substrate. Sibling deps: E4-S2 (introspection + conformance), E5-S2 (deny-list feed), E1-S3 (whoami consumes principal).

## 🧪 Testing

`TestEdgeVerifierEmitsOrgScopedPrincipalDenyByDefault` (integration). Tiers: unit (each check) + integration (full pipeline). CS §5: fake identity JWKS/registry from FR-C13 vectors; never mock own verifier binding (it IS the code under test).

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (auth-flow, fail-closed, confused-deputy lenses); cell-lint `JGAUQRsw2g`. This TS binding may never drift from lib's Go semantics unnoticed (E4-S2 gate).

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M11; Arch `BE0jTNoweS` A-AUTH, §6; identity FR-I1–I7; lib FR-L1–L7; contracts FR-C13.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — FR-C13 claims/scope vectors + audience model); parent epic E4. Intra-service: feeds E1-S3, all upstream stories. Externally: identity JWKS/registry.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
