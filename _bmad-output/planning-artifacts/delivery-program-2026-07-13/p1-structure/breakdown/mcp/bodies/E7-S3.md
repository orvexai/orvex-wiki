## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** family's one TypeScript satellite, **I want** the contracts FR-C21 AGPL-import guard running in my CI from day one (blocking any `import '@docmost/*'` or other AGPL package) plus a typed-degradation posture proven across every dependency-down scenario (never fail open, never fabricate), **so that** the AGPL boundary holds where an npm convenience-import is actually one line away and no upstream outage ever produces a degraded-but-served response.

**Definition of Done:** one named test `TestAgplGuardBlocksImportAndDegradationIsAlwaysTyped` (integration — assert a planted `@docmost/*` import reddens CI via the FR-C21 guard, and that each upstream-down scenario (wiki-api / ai / knowledge / identity introspect) yields a typed failure, never a fabricated result or fail-open). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a planted `@docmost/*` (or other AGPL) import, When CI runs, Then the FR-C21 guard fails the build. *assert: planted AGPL import reddens CI* [Source: NFR-M5]
- [ ] **AC2** — Given the guard runs from day one, When the repo builds clean, Then no AGPL package is present. *assert: guard passes on the real tree; no `@docmost/*` in the graph* [Source: NFR-M5]
- [ ] **AC3** — Given wiki-api down, When wiki tools are called, Then they fail typed while ai/knowledge tools keep working (independent degradation). *assert: wiki-api-down → typed wiki-tool failure, ai/knowledge unaffected* [Source: NFR-M2]
- [ ] **AC4** — Given identity introspection unreachable, When verifying, Then the MCP rides cached within the FR-I3 bound then fails closed — never fail open. *assert: past-bound introspect-down → typed denial* [Source: NFR-M2, NFR-M3]
- [ ] **AC5** (negative) — Given any upstream down, When a tool is called, Then no fabricated/degraded-but-served result is ever returned (fail-open incidents = 0). *assert: no scenario returns a synthesized result; every failure is typed* [Source: NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestAgplGuardBlocksImportAndDegradationIsAlwaysTyped` (AC1–AC5).
- [ ] GREEN: wire the FR-C21 AGPL-import guard into CI (AC1, AC2).
- [ ] GREEN: prove independent typed degradation per upstream (AC3, AC5).
- [ ] GREEN: cached-then-fail-closed on introspect-down (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — AGPL import-grep gate in CI agpl-boundary job (.github/workflows/ci.yml:49); typed-degradation posture: AiUpstreamError typed degrade (src/upstreams/ai.ts:64) and fail-closed introspection (src/auth/http-introspector.ts) — never fail open (CS §11).

Tier: CI + edge/auth + backstage adapter (Arch A-RUNTIME, NFR-M5). Seam: MCP↔contracts (guard), MCP↔all upstreams (degradation). Sibling deps: E4-S2 (introspection staleness), E3-* (upstream clients), E7-S1 (CI gates). This is the only closed repo where the guard is load-bearing.

## 🧪 Testing

`TestAgplGuardBlocksImportAndDegradationIsAlwaysTyped` (integration + CI gate). Tiers: unit (guard) + integration (degradation). CS §5: fake upstreams (down states) from fixtures; never mock own guard.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (AGPL-boundary, never-fail-open lenses); cell-lint `JGAUQRsw2g`. Guard is enforcing, not ceremonial (A-RUNTIME).

## 🔗 References

PRD `k1sWjtJq3x` §5 NFR-M5, NFR-M2, counter-metrics; Arch `BE0jTNoweS` A-RUNTIME; contracts FR-C21; identity FR-I3.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — FR-C21 guard definition); parent epic E7. Intra-service: E4-S2, E3-*.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
