## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** agent, **I want** `get_space_tree` (nav: `/tree`, backlinks, breadcrumbs) and the structural half of `get_neighborhood` fronted as thin shim tools over wiki-api, **so that** I can traverse the wiki graph without the MCP computing any structure itself.

**Definition of Done:** one named test `TestNavigationShimForwardsToWikiApi` (integration — assert `get_space_tree` returns the wiki-api nav response and `get_neighborhood`'s structural leg maps to the wiki-api related/nav verb, both under the caller token, with no MCP-side graph computation). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a space, When `get_space_tree` is called, Then `/tree` + backlinks + breadcrumbs return via wiki-api on the caller token. *assert: response mirrors the wiki-api nav fixture; single upstream call* [Source: FR-M3]
- [ ] **AC2** — Given a page, When `get_neighborhood` requests its structural leg, Then it maps to the wiki-api related/nav verb. *assert: structural neighborhood forwards to wiki-api related* [Source: FR-M6]
- [ ] **AC3** — Given the shim, When either nav tool runs, Then the MCP computes no adjacency/tree structure of its own. *assert: no MCP-side graph traversal; response is upstream-derived* [Source: FR-M3, FR-M6]
- [ ] **AC4** (negative) — Given a missing space, When `get_space_tree` is called, Then a typed not-found returns, never an empty-fabricated tree. *assert: unknown space yields typed not-found* [Source: NFR-M2]

*Note: the semantic half of `get_neighborhood` (knowledge-direct) is owned by E3-S4; this story is the structural/nav leg only.*

## 🔨 Tasks

- [ ] RED: write `TestNavigationShimForwardsToWikiApi` (AC1–AC4).
- [ ] GREEN: map `get_space_tree` to wiki-api nav; map `get_neighborhood` structural leg to related/nav (AC1, AC2).
- [ ] GREEN: assert no MCP-side structure computation (AC3).
- [ ] RED→GREEN: missing-space typed not-found (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — get_neighborhood (src/server/tools.ts:2172, backlinks/outgoing/breadcrumbs) + get_space_tree (:2302) navigation shim over wiki-api.

Tier: tool registry/dispatch over backstage adapter (Arch §6). Seam: MCP↔wiki-api. Sibling deps: E3-S4 owns the semantic neighborhood leg (knowledge-direct); coordinate the split so `get_neighborhood` composes both without MCP orchestration.

## 🧪 Testing

`TestNavigationShimForwardsToWikiApi` (integration). Tiers: unit (mapping) + integration (nav forwarding). CS §5: fake wiki-api from golden fixtures; never mock own dispatch.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (thin-shim lens); cell-lint `JGAUQRsw2g`. Logic upstream, protocol here (A-SHIM).

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M3, FR-M6; Arch `BE0jTNoweS` §3 `get_space_tree`/`get_neighborhood` rows.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E2. Intra-service: E3-S4 (semantic leg), E3-S1/S2, E4-S3.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
