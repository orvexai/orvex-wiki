## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** developer/CI harness running the MCP over stdio, **I want** the stdio transport to carry the same auth posture as HTTP, with the shipped "resources served unauthenticated" caveat turned into a standing gate — any non-static or tenant-derived resource requires an stdio auth re-audit before it ships — **so that** stdio never becomes an unauthenticated back door as the resource surface grows.

**Definition of Done:** one named test `TestStdioAuthParityAndStaticResourceGate` (integration — assert stdio tool calls enforce the same edge verification as HTTP, that the sole unauthenticated resource is the static `orvex://authoring-guide`, and that registering any tenant-derived resource trips the standing audit gate in CI). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an stdio tool call, When it executes, Then it runs the same edge verifier + scope gate as the HTTP path. *assert: stdio `callTool` on an authed tool goes through the same verification pipeline* [Source: FR-M15]
- [ ] **AC2** — Given stdio resources, When served, Then only the static `orvex://authoring-guide` is served unauthenticated. *assert: the only unauthenticated stdio resource is the static guide* [Source: FR-M15]
- [ ] **AC3** — Given a PR adding a non-static/tenant-derived resource, When CI runs, Then the standing audit gate blocks ship until an stdio auth re-audit. *assert: registering a tenant-derived resource reddens `assertStaticOnly` (shared with E1-S1's guard)* [Source: FR-M15]
- [ ] **AC4** (negative) — Given an invalid token on stdio, When an authed tool is called, Then it fails closed identically to HTTP. *assert: invalid-token stdio `callTool` → typed denial* [Source: FR-M15, NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestStdioAuthParityAndStaticResourceGate` (AC1–AC4).
- [ ] GREEN: route stdio tool calls through the shared edge verifier (AC1, AC4).
- [ ] GREEN: keep the static guide as the sole unauthenticated resource; wire the standing audit gate (AC2, AC3).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — stdio verifies the env token once at startup and caches the Principal (src/transport/stdio.ts), matching the HTTP boundary posture; resources.ts documents the static-only standing rule; CI statelessness gate (ci.yml:56); test/transport/stdio-verify.test.ts.

Tier: edge/auth seam + transport (Arch §1 stdio row, §5 standing gate). Seam: MCP↔identity (verification), MCP↔agent (stdio). Sibling deps: E1-S1 (server core + resource guard), E4-S1 (verifier). Complements E1-S1's `assertStaticOnly`.

## 🧪 Testing

`TestStdioAuthParityAndStaticResourceGate` (integration). Tiers: unit (gate) + integration (stdio auth parity). CS §5: fake identity JWKS from vectors; never mock own transport/verifier.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (standing-audit-gate, fail-closed lenses); cell-lint `JGAUQRsw2g`. Safe today only because the sole resource is static.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M15; Arch `BE0jTNoweS` §1 stdio row, §5 stdio resources standing gate.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E4. Intra-service: E1-S1, E4-S1.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
