## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** platform, **I want** the MCP to honor the day-1 cell contract (`JGAUQRsw2g`): every pod carries `CELL_ID` distinct from `CLUSTER_NAME`, exposes `/healthz` echoing both, subscribes only to the cell-scoped revocation topic `{domain}-events.{cell}`, and fails closed on any consumed CloudEvent whose `orvexcell` extension mismatches this cell — **so that** a mis-scheduled pod is detectable and a per-cell consumer can never cross-consume another cell's revocation topic.

**Definition of Done:** one named test `TestCellContractHealthzEchoAndScopedTopic` (integration — assert `/healthz` echoes both `CELL_ID` and `CLUSTER_NAME`, that the revocation subscription targets only the cell-scoped topic name, and that a CloudEvent with a mismatched `orvexcell` is rejected). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a pod, When `/healthz` is probed, Then it echoes both `CELL_ID` and `CLUSTER_NAME`, and `CELL_ID` is distinct from `CLUSTER_NAME`. *assert: `/healthz` body contains both fields, distinct values* [Source: FR-M18]
- [ ] **AC2** — Given the revocation subscriber, When it subscribes, Then it targets only the cell-scoped topic `{domain}-events.{cell}`. *assert: subscribed topic == the cell-scoped name for this `CELL_ID`* [Source: FR-M17, FR-M18]
- [ ] **AC3** — Given a consumed CloudEvent, When its `orvexcell` extension mismatches this cell, Then it is rejected fail-closed. *assert: mis-celled event rejected; no deny-list mutation* [Source: FR-M18]
- [ ] **AC4** — Given `/health`, When probed, Then it stays liveness-only (process-alive, no dependency round-trips). *assert: `/health` returns `{ok,service,ts}`-style liveness, no upstream call* [Source: FR-M18]
- [ ] **AC5** (negative) — Given a manifest missing `CELL_ID`, When the pod starts, Then it fails closed (does not serve as if single-cell). *assert: absent `CELL_ID` → startup fail-closed, not a silent `CLUSTER_NAME` fallback* [Source: FR-M18]

## 🔨 Tasks

- [ ] RED: write `TestCellContractHealthzEchoAndScopedTopic` (AC1–AC5).
- [ ] GREEN: add `CELL_ID` (distinct from `CLUSTER_NAME`) + `/healthz` echo to manifests + server (AC1, AC4).
- [ ] GREEN: derive the cell-scoped topic name; subscribe only there (AC2).
- [ ] GREEN: `orvexcell` fail-closed check; startup fail-closed on missing `CELL_ID` (AC3, AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — /healthz echoes CELL_ID+CLUSTER_NAME (src/health/cell.ts, src/transport/http.ts:294) and degrades on stalled consumer; orvexcell foreign-cell fail-closed drop in the consumer; cell-scoped topic revocationTopicName; discovery/resolver + 421/308 routing src/routing/*; tests test/routing/cell-resolver.test.ts (14) + test/transport/cell-routing.test.ts (9).

Tier: deploy manifests + server operability (Arch §1 named gap, §4 deploy deltas). Seam: MCP↔Kafka (cell-scoped topic), MCP↔platform (cell contract). Named gap: none of these are honored in deployed manifests today (crew appset sets only `CLUSTER_NAME=eu-central-1`). Sibling deps: E5-S2 (consumer), E7-S2 (readiness/SLIs).

## 🧪 Testing

`TestCellContractHealthzEchoAndScopedTopic` (integration). Tiers: unit (topic derivation, `orvexcell` check) + integration (`/healthz`). CS §5: fake Kafka + env from fixtures; never mock own health/topic logic.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (cell-isolation, fail-closed lenses); cell-lint `JGAUQRsw2g` (this is the lint's home requirement). Load-bearing before a second cell.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M17/M18; Arch `BE0jTNoweS` §1 day-1 cell contract, §4, §8; family canon `JGAUQRsw2g`.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — cell contract + CloudEvent `orvexcell` shape); parent epic E5. Intra-service: E5-S2, E7-S2.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
