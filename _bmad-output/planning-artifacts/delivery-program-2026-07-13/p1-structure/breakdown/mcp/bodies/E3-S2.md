## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** platform, **I want** the typed backstage client codegen'd from a local checkout of the pinned `orvex-studio-contracts` `openapi/wiki-api.yaml` tag — fail-loud, never from a live served descriptor — with one deliberate `/v1` regeneration at the contracts freeze (R2), **so that** the MCP's compile-time contract is a versioned, reviewed artifact and a contracts regression surfaces at regen-PR review, not silently at runtime.

**Definition of Done:** one named test `TestCodegenFromContractsTagFailsLoudOnUnknownFields` (build/unit — assert the client is generated from `ORVEX_STUDIO_CONTRACTS_DIR` (not a live fetch), that a removed/unknown field fails the build, and that the pinned commit+tag are recorded in `package.json` `orvexContractsPin`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given codegen, When the client is generated, Then the source is a local checkout of `orvex-studio-contracts` `openapi/wiki-api.yaml` (`ORVEX_STUDIO_CONTRACTS_DIR`), never a live fetch and never wiki-api's served `/v1/openapi.json`. *assert: gen-client uses the local contracts dir; the live-descriptor fetch path is deleted* [Source: FR-M8]
- [ ] **AC2** — Given a response field the client doesn't know, When the build runs, Then it fails (fail-loud, forward-compat guard). *assert: an unknown/removed field reddens the codegen build* [Source: FR-M8]
- [ ] **AC3** — Given a regen, When it lands, Then it is a deliberate reviewed PR with the pinned commit+tag recorded (`package.json orvexContractsPin`), never a background refresh. *assert: `orvexContractsPin` present and matches the checked-out tag* [Source: FR-M8, NFR-M4]
- [ ] **AC4** — Given R2, When the `/v1` regeneration runs, Then the client moves onto the verb grammar and the live-descriptor codegen path retires. *assert: post-R2 generated client references `/v1` verbs; no engine-322-path descriptor code remains* [Source: FR-M6, FR-M8]
- [ ] **AC5** (negative) — Given canon P3 (AGPL-derived codegen forbidden), When codegen runs, Then it never sources the engine's 322-path descriptor. *assert: no code path fetches the engine descriptor* [Source: FR-M8, NFR-M5]

## 🔨 Tasks

- [ ] RED: write `TestCodegenFromContractsTagFailsLoudOnUnknownFields` (AC1–AC5).
- [ ] GREEN: point `scripts/gen-client.ts` at `ORVEX_STUDIO_CONTRACTS_DIR`; delete the live-fetch path (AC1, AC5).
- [ ] GREEN: fail-loud on unknown/removed fields; record `orvexContractsPin` (AC2, AC3).
- [ ] GREEN (R2): regenerate onto `/v1`; retire the live-descriptor path (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — contracts-tag typed client src/backstage/generated.d.ts + src/routing/generated.d.ts via scripts/gen-client.ts; regen provenance tests test/ci/eng-1406-r2-regen.test.ts + test/golden-tape-r2-v1-provenance.test.ts. Note: the /v1 verb-grammar endpoints are documented as returning 501 typed sentinels upstream today.

Tier: backstage adapter (Arch §6, A-CLIENT). Seam: MCP↔contracts (codegen source is literally the pinned tag — the ENG-2102 dispatch gate). Sibling deps: E3-S1 precedes; ADR-0001 governs. Transitional hazard: until R2 the live-descriptor + live-dev-backstage tape is named tech debt — retires here (TBD, pinned by ENG-2102).

## 🧪 Testing

`TestCodegenFromContractsTagFailsLoudOnUnknownFields` (build/unit). Tiers: unit (codegen), build gate. CS §5: use committed contracts fixtures; never mock own codegen. No live fetch in tests.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (fail-loud, provenance lens); cell-lint `JGAUQRsw2g`. Regen = deliberate PR; the pin is the only contracts input (D-M6, NFR-M4).

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M8, FR-M6, §7 R2, NFR-M4/M5; Arch `BE0jTNoweS` A-CLIENT; ADR 0001 `lR3kpc0Ra2`; contracts FR-C19, canon P3.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — literally the codegen source); parent epic E3. Intra-service: E3-S1 precedes; feeds all verb stories.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
