## 🎯 Story

**As an** MCP-capable agent, **I want** the MCP memory-retrieval surface (AD-5b) — memory *read* as a `knowledge` tool call under my delegated principal, governed by I-4 (ACL ∩ scope) — **so that** MCP clients read Memory through the same permission-aware retrieval choke, and the MCP is never a write/compose adapter (that's the browser-extension client port AD-5a) or a stateful-sync surface (AD-5c).

**Definition of Done:** one named test `TestMcpMemoryRetrievalReadsViaKnowledgeUnderDelegatedPrincipal` (integration — assert `memory_get` reads Memory as a knowledge tool call under the caller's delegated principal with I-4 ACL ∩ scope enforced at knowledge, and that the MCP exposes no memory write/compose or stateful-sync path). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a memory read, When `memory_get` is called, Then it reads Memory as a `knowledge` tool call under the caller's delegated principal. *assert: `memory_get` targets the knowledge memory-read surface with the caller token* [Source: FR-M19]
- [ ] **AC2** — Given the read, When knowledge serves it, Then I-4 (ACL ∩ scope) is enforced at knowledge, not at the MCP. *assert: caller principal rides to knowledge; MCP applies no memory ACL of its own* [Source: FR-M19]
- [ ] **AC3** — Given the MCP surface, When inspected, Then it exposes no memory write/compose port and no stateful-sync surface (AD-5a/AD-5c are separate). *assert: no `composeInto`/persistent-sync path exists on the MCP* [Source: FR-M19]
- [ ] **AC4** (negative) — Given a private/insufficient-scope memory, When `memory_get` is called, Then knowledge denies and the MCP surfaces the typed denial. *assert: private/out-of-scope memory read → typed denial, never enriched into a result* [Source: FR-M19, NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestMcpMemoryRetrievalReadsViaKnowledgeUnderDelegatedPrincipal` (AC1–AC4).
- [ ] GREEN: route `memory_get` (read leg) to the knowledge memory-read surface under the delegated principal (AC1, AC2).
- [ ] GREEN: assert no write/compose or sync path is exposed by the MCP (AC3).
- [ ] RED→GREEN: private/insufficient-scope typed denial (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** partial — studio_memory_get (src/server/studio-tools.ts:145) + studio_memory_save (:178) exist but read/write the Studio backend /api/memory/formspec; the AD-5b/I-4 memory-retrieval-as-a-knowledge-tool-call surface is NOT implemented (no knowledge routing).

Tier: backstage adapter + tool registry (Memory gap-closure fold-in, AD-5b). Seam: MCP↔knowledge (memory read), governed by I-4. DeliveryAdapter contract pinned in contracts (TBD, ENG-2102). Sibling deps: E3-S4 (knowledge client), E4-S1/S3 (principal + pass-through), E6-S1 (`memory_get`/`memory_save` product tools — the write leg stays a Studio-product tool per FR-M19; the read leg is the knowledge retrieval surface).

## 🧪 Testing

`TestMcpMemoryRetrievalReadsViaKnowledgeUnderDelegatedPrincipal` (integration). Tiers: unit (routing) + integration (delegated-principal read). CS §5: fake knowledge memory-read from fixtures; never mock own dispatch. Assert absence of compose/sync path (structural).

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (delegated-principal, private-memory-consent lenses); cell-lint `JGAUQRsw2g`. MCP is not a compose adapter (AD-5).

## 🔗 References

PRD `k1sWjtJq3x` Memory gap-closure fold-in (AD-5b, I-4); Arch `BE0jTNoweS` Memory gap-closure (AD-5); source PRD `g9vWbSYplh` + spine `iiCcKhGptV` + map `vBvVDFklZo`.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — DeliveryAdapter / knowledge memory-read contract); parent epic E6. Intra-service: E3-S4, E4-S1/S3, E6-S1.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
