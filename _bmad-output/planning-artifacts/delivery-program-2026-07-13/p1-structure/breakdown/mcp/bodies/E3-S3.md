## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** agent, **I want** `ask` (and chat/inline/generate as they land) to call orvex-studio-ai directly under my token — `resolveAsk()` routing to `AI_BASE_URL` when configured, else the byte-compatible facade, with a configured-but-failing ai upstream propagated as a real error — **so that** the cited-ask loop runs where it belongs (ai owns K5 synthesis) and never silently falls back on a configured seam.

**Definition of Done:** one named test `TestAskRoutesToAiUpstreamK5PassthroughNoSilentFallback` (integration — assert `ask` POSTs `${AI_BASE_URL}/v1/ask` with the caller token verbatim, returns the K5 six-field shape `{answer,citations[],confidence,unanswered,gapNote,followups}` shaped-for-MCP but semantically unmodified, and that a configured-but-failing ai upstream yields a propagated error, not a facade fallback). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `AI_BASE_URL` configured, When `ask` is called, Then `resolveAsk()` routes to ai's `/v1/ask` carrying the caller token verbatim (verification-only, never minted). *assert: request targets `${AI_BASE_URL}/v1/ask` with the caller Authorization header* [Source: FR-M7]
- [ ] **AC2** — Given ai returns a K5 answer, When `ask` responds, Then all six fields pass through semantically unmodified (MCP composes no citations). *assert: response validated zod-strict against the K5 six-field shape; citation values byte-equal ai's* [Source: FR-M7]
- [ ] **AC3** — Given `AI_BASE_URL` is unset, When `ask` is called, Then it rides the byte-compatible wiki facade (OD-5 sequencing). *assert: unset `AI_BASE_URL` routes `ask` to the facade* [Source: FR-M7]
- [ ] **AC4** (negative) — Given a configured ai upstream that errors, When `ask` is called, Then a real error propagates — no silent facade fallback (CS §11). *assert: configured-but-failing ai yields a propagated typed error, facade never called* [Source: FR-M7, NFR-M2]
- [ ] **AC5** — Given ai down while wiki-api is up, When both tool families are used, Then AI tools fail typed while wiki tools keep working (independent upstreams degrade independently). *assert: ai-down leaves `get_page` succeeding, `ask` failing typed* [Source: NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestAskRoutesToAiUpstreamK5PassthroughNoSilentFallback` (AC1–AC5).
- [ ] GREEN: implement `AiClient.ask` POST `${AI_BASE_URL}/v1/ask`, caller token verbatim, K5 zod-strict validation (AC1, AC2).
- [ ] GREEN: implement `resolveAsk()` routing switch (configured→ai, else facade) (AC3).
- [ ] GREEN: propagate configured-but-failing ai as a real error; assert independent degradation (AC4, AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — resolveAsk switch + AiClient.ask POST /v1/ask carrying caller token verbatim, strict K5 schema (fail-loud, no synthesized fallback) in src/upstreams/ai.ts; tests test/upstreams/resolve-ask.test.ts, ai-client.test.ts, test/tools/eng-1406-ask-tool-wiring.test.ts.

Tier: backstage adapter (Arch §6, A-UPSTREAM, A-TOKENS). Seam: MCP↔ai (third upstream, D-M7). Sibling deps: E4-S3 (token pass-through), E3-S2 (client posture). Externally sequenced behind ai's `/v1/ask` shipping (until then `ask` rides the facade, OD-5).

## 🧪 Testing

`TestAskRoutesToAiUpstreamK5PassthroughNoSilentFallback` (integration). Tiers: unit (`resolveAsk`) + integration (K5 passthrough). CS §5: fake ai + facade from golden fixtures; never mock own routing switch or client.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (no-fallback-on-configured-seam lens); cell-lint `JGAUQRsw2g`. MCP composes no K5 fields (D-M7).

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M7, §7 R2 ask repoint, D-M7, NFR-M2; Arch `BE0jTNoweS` A-UPSTREAM, §3 `ask` row; ADR 0001 `lR3kpc0Ra2` decisions 2–3.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — ai ask surface pinned in contracts per FR-M8 posture); parent epic E3. Intra-service: E3-S2, E4-S3. Externally: ai `/v1/ask`.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
