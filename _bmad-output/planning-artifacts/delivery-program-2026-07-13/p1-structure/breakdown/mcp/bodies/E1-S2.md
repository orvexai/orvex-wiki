## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** agent, **I want** a small curated hero surface (≤ ~13 eager wiki tools) plus a `list_tools(category?)` long tail callable by name, **so that** my default `tools/list` stays context-light while the full surface remains discoverable.

**Definition of Done:** one named test `TestHeroSurfaceBudgetAndLongTailDiscovery` (integration — assert the eager `tools/list` contains exactly the shipped hero set and no more than the budget, and that a long-tail tool absent from `tools/list` is discoverable via `list_tools(category)` and callable by name). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a connected client, When it calls `tools/list`, Then only the shipped hero set (`whoami, ask, search, get_page, save_page, edit, get_neighborhood, get_space_tree, get_changes, related_pages` + `list_tools`) is returned. *assert: eager tool count ≤ ~13 and set-equals the hero roster* [Source: FR-M2]
- [ ] **AC2** — Given a long-tail tool not in the eager list, When the client calls `list_tools(category)`, Then the tool is returned with its schema. *assert: `list_tools('nav').tools` includes the queried long-tail name* [Source: FR-M2]
- [ ] **AC3** — Given a long-tail tool name, When the client calls it directly, Then it dispatches without having appeared in `tools/list`. *assert: direct `callTool(longTailName)` succeeds* [Source: FR-M2]
- [ ] **AC4** (guard) — Given a PR that adds an eager hero tool, When CI runs, Then the hero-budget test fails unless the roster fixture is deliberately updated. *assert: adding a tool to the eager set without updating the fixture reddens `TestHeroSurfaceBudgetAndLongTailDiscovery`* [Source: FR-M2, NFR-M4]
- [ ] **AC5** (negative) — Given `list_tools` with an unknown category, When called, Then it returns an empty typed result, never an error dump. *assert: `list_tools('bogus').tools === []`* [Source: FR-M2]

## 🔨 Tasks

- [ ] RED: write `TestHeroSurfaceBudgetAndLongTailDiscovery` asserting eager budget + long-tail discovery (AC1–AC3).
- [ ] GREEN: register the hero set eagerly; register the long tail behind `list_tools(category?)` (Phase-5 progressive disclosure) (AC1–AC3).
- [ ] GREEN: add a committed hero-roster fixture + budget assertion as the deliberate-change gate (AC4).
- [ ] RED→GREEN: unknown-category empty-result path (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — HERO_TOOL_NAMES=11 (≤13) in src/server/tool-catalog.ts; hero-only paginated tools/list in src/server/tool-visibility.ts; list_tools(category) reveal tool at src/server/tools.ts:2772; covered by test/transport/standalone-boot.test.ts.

Tier: tool registry/dispatch (Arch §6). Seam: MCP↔agent. Sibling deps: E7-S4 (`list_tools` long-tail parity is the FR-M16 decommission mechanism). The hero roster is repo-internal (D-M6, NFR-M4) — no contracts artifact.

## 🧪 Testing

`TestHeroSurfaceBudgetAndLongTailDiscovery` (integration). Tiers: unit (registry) + integration (`tools/list`). CS §5: mock nothing own; long-tail tools may stub their upstream client in unit scope. Never mock the registry itself.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (surface-creep counter-metric); cell-lint `JGAUQRsw2g`. Hero-set changes are deliberate contract events, not drive-by additions.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M2, NFR-M4, D-M6; Arch `BE0jTNoweS` §3 `list_tools` row; Thin Build Plan `6e2jqBdknZ` (Phase-5).

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E1. Intra-service: informs E7-S4.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
