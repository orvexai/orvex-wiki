## 🎯 Story

**As an** agent, **I want** `search`, `related_pages`, and the semantic half of `get_neighborhood` (and any future `duplicates`) to reach orvex-studio-knowledge **directly** on my token (D-M8, PO ruling 9) — the MCP holding a knowledge base URL, not routing this path through wiki-api — **so that** search/RAG retrieval matches the canonical roster while knowledge enforces ACL ∩ token-scope at its egress chokepoint.

**Definition of Done:** one named test `TestSearchAndRelatedReachKnowledgeDirect` (integration — assert `search`/`related_pages`/semantic `get_neighborhood` target the knowledge base URL directly carrying the caller token verbatim, and that the wiki verbs (get/save/edit/list/changes) still go through wiki-api, never MCP-direct to knowledge). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a search, When `search` is called, Then it targets the knowledge base URL directly with the caller token verbatim. *assert: `search` request targets `KNOWLEDGE_BASE_URL`, not `WIKI_API_BASE_URL`* [Source: FR-M7]
- [ ] **AC2** — Given a related/duplicates/semantic-neighborhood request, When called, Then it also reaches knowledge directly. *assert: `related_pages` and semantic `get_neighborhood` target `KNOWLEDGE_BASE_URL`* [Source: FR-M7]
- [ ] **AC3** — Given the D-M2 scope guard, When a wiki verb (get/save/edit/list/changes) runs, Then it still goes through wiki-api, never MCP-direct to knowledge. *assert: wiki verbs target `WIKI_API_BASE_URL` only* [Source: FR-M7]
- [ ] **AC4** — Given the removed wiki-api ACL intermediary, When knowledge is reached direct, Then the MCP's own edge scope-gate stays defense-in-depth (not authoritative); knowledge FR-K31b enforces admission. *assert: the caller token rides to knowledge for its egress ACL ∩ scope check; MCP does not filter results* [Source: FR-M7]
- [ ] **AC5** (negative) — Given knowledge down, When `search` is called, Then it fails typed, never fabricated results. *assert: knowledge-down `search` returns a typed error, no synthesized hits* [Source: FR-M7, NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestSearchAndRelatedReachKnowledgeDirect` (AC1–AC5).
- [ ] GREEN: add a knowledge client + `KNOWLEDGE_BASE_URL`; route search/related/semantic-neighborhood/duplicates there (AC1, AC2).
- [ ] GREEN: keep wiki verbs on wiki-api (D-M2 scope guard) (AC3).
- [ ] GREEN: pass caller token verbatim; assert MCP does no result filtering (AC4); typed failure on knowledge-down (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** partial — search (src/server/tools.ts:622), related_pages (:2676) and get_neighborhood exist and DO reach knowledge, but INDIRECTLY through the wiki-api facade (wrapper). The story's 'direct to orvex-studio-knowledge (D-M8)' routing is NOT implemented: by arch §A-UPSTREAM the MCP holds no knowledge base URL (src/config/config.ts:26, src/upstreams/wiki-api.ts:8). Superseding-decision divergence, not a functional gap.

Tier: backstage adapter (Arch §6, A-UPSTREAM as amended by D-M8). Seam: MCP↔knowledge (direct). Note: ADR-0001 decision 5 records the pre-ruling-9 "search via wiki-api" stance; D-M8/PO ruling 9 supersedes it — this story implements the ruling. Sibling deps: E2-S4 (structural neighborhood leg), E4-S3 (token).

## 🧪 Testing

`TestSearchAndRelatedReachKnowledgeDirect` (integration). Tiers: unit (routing) + integration (direct-knowledge path). CS §5: fake knowledge + wiki-api from golden fixtures; never mock own routing. Assert knowledge enforces admission (test its egress via fixture, not reimplemented).

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (ACL-regression + defense-in-depth lens); cell-lint `JGAUQRsw2g`. Removing the wiki-api ACL intermediary makes knowledge FR-K31b load-bearing.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M7, D-M8/PO-ruling-9 (ENG-1403), NFR-M2; Arch `BE0jTNoweS` A-UPSTREAM, §9 OD-7 (resolved); knowledge FR-K31b.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — knowledge search/related surface + cell-discovery host); parent epic E3. Intra-service: E2-S4, E4-S3. Externally: knowledge egress-ACL leg.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
