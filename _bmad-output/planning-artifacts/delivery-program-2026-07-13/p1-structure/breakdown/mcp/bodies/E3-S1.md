## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** platform, **I want** the MCP's wiki upstream repointed onto orvex-wiki-api's byte-compatible facade by base URL only (R0) — renaming `DOCMOST_BASE_URL` → `WIKI_API_BASE_URL` and purging every stale legacy host — **so that** the MCP holds no engine base URL and the "config-only repoint" is proven by a green golden tape.

**Definition of Done:** one named test `TestR0BaseUrlRepointHoldsNoEngineHost` (integration — assert every wiki verb targets `WIKI_API_BASE_URL`, no `DOCMOST_BASE_URL` / legacy `docs.eu-central-1.myidp.cloud` / `dev-docmost…` host survives in config or code, and the golden tape is green through the facade). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the R0 config, When any wiki verb executes, Then its base URL is `WIKI_API_BASE_URL` pointed at wiki-api's byte-compatible `/api/orvex/*` surface. *assert: every wiki-verb client call targets `WIKI_API_BASE_URL`* [Source: FR-M6]
- [ ] **AC2** — Given the rename, When the repo is scanned, Then no `DOCMOST_BASE_URL` name and no stale legacy host survive anywhere (kustomize configmaps, crew component, `cluster-config.yaml` comment). *assert: grep for the legacy names/hosts returns zero hits* [Source: FR-M6]
- [ ] **AC3** — Given the base-URL-only change, When codegen state is inspected, Then no client regeneration occurred at R0 (that is R2). *assert: generated client digest unchanged from pre-R0* [Source: FR-M6]
- [ ] **AC4** — Given the facade, When the golden tape runs, Then it is green through wiki-api (parity proven). *assert: golden-tape suite passes against the wiki-api facade fixture* [Source: FR-M6, NFR-M6]
- [ ] **AC5** (negative) — Given a wiki-api path drift, When the tape runs, Then any tape diff is treated as a wiki-api parity bug, not masked. *assert: an injected path drift reddens the tape (no silent accommodation)* [Source: FR-M6]

## 🔨 Tasks

- [ ] RED: write `TestR0BaseUrlRepointHoldsNoEngineHost` (AC1–AC5).
- [ ] GREEN: rename env `DOCMOST_BASE_URL` → `WIKI_API_BASE_URL`; point at the facade (AC1).
- [ ] GREEN: purge stale legacy hosts from kustomize/crew/comment (AC2).
- [ ] GREEN: confirm no regen at R0; wire the parity tape (AC3, AC4, AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — wikiApiUrl resolves from WIKI_API_BASE_URL only (src/config/config.ts:133), legacy engine-URL fallback removed (unconfigured is a typed error); test/config/wiki-api-repoint.test.ts.

Tier: backstage adapter + deploy config (Arch §4, A-UPSTREAM). Seam: MCP↔wiki-api. Externally sequenced behind wiki-api Phase-0 exit incl. the engine identity-token enabler (FR-A16). No-fallbacks rule: the rename lands at R0 so no stale name survives (MEMORY: hard cuts).

## 🧪 Testing

`TestR0BaseUrlRepointHoldsNoEngineHost` (integration). Tiers: unit (config resolution) + integration (golden tape through facade). CS §5: fake wiki-api from the byte-compatible fixture; never mock own config loader.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (no-fallback, config-drift lens); cell-lint `JGAUQRsw2g`. Any tape diff at R0 = wiki-api parity bug, not an MCP fix.

## 🔗 References

PRD `k1sWjtJq3x` §7 R0, FR-M6, D-M2; Arch `BE0jTNoweS` §4 deploy deltas, A-UPSTREAM; wiki-api D-A9/OQ-A3/FR-A16.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E3. Intra-service: precedes E3-S2, E2-*. Externally: wiki-api Phase-0.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
