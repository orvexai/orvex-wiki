## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** platform, **I want** cached introspection on identity's `POST /v1/introspect` (TTL-bounded, fail-closed past max staleness) plus the contracts FR-C13 / lib FR-L7 conformance vector suite running as a blocking CI gate, **so that** the MCP's TS verifier can never drift from lib's Go semantics unnoticed and revocation staleness is bounded only by identity's FR-I3 TTLs.

**Definition of Done:** one named test `TestVerifierConformanceVectorsAndIntrospectionStaleness` (integration — assert the full FR-C13/FR-L7 case list passes (expired / wrong-issuer / wrong-audience / revoked / scope-exceeding / scope-reduced-warm-cache / frozen-claims org-switch), and that introspection rides cached within the FR-I3 hard bound then fails closed). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the FR-C13/FR-L7 vector suite, When CI runs, Then every case passes (expired, wrong-issuer, wrong-audience, revoked, scope-exceeding, scope-reduced-warm-cache, frozen-claims org-switch). *assert: all named vectors green; a deliberately broken verifier reddens the suite* [Source: FR-M11, NFR-M3]
- [ ] **AC2** — Given introspection, When a token is verified within TTL (30–60 s), Then a cache hit is used; edge verification cache-hit < 5 ms. *assert: within-TTL verify uses cache; p-latency < 5 ms cache-hit* [Source: FR-M11, NFR-M1]
- [ ] **AC3** — Given introspection unreachable, When staleness exceeds the FR-I3 hard bound, Then verification fails closed. *assert: past-max-staleness verify → typed denial, never fail-open* [Source: FR-M11, NFR-M3]
- [ ] **AC4** — Given a `token.revoked` in the deny-list, When a matching token is verified, Then it is denied even on a warm cache (evict-before-serve). *assert: revoked token denied despite warm introspection cache* [Source: NFR-M3]
- [ ] **AC5** — Given the vector suite is a release gate, When it fails, Then CI blocks promotion. *assert: a red vector blocks the pipeline* [Source: FR-M11, NFR-M6]

## 🔨 Tasks

- [ ] RED: wire the FR-C13/FR-L7 vector suite as `TestVerifierConformanceVectorsAndIntrospectionStaleness` (AC1).
- [ ] GREEN: implement TTL-bounded introspection cache with fail-closed max-staleness (AC2, AC3).
- [ ] GREEN: deny-list evict-before-serve integration (AC4).
- [ ] GREEN: make the suite a blocking CI gate (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — POST /v1/introspect client with schema validation (src/auth/http-introspector.ts), cached in the verifier on a freshness cadence; FR-C13/FR-L7 conformance vector suite test/ci/conformance-gate.test.ts run as a CI gate (npm run test:conformance, ci.yml:87).

Tier: edge/auth seam `src/auth/*` (Arch §6, A-AUTH). Seam: MCP↔identity (introspect), MCP↔contracts (vectors). The audience model must land in contracts before the wrong-audience vector is meaningful (OD-3/OQ-M2 — TBD, pinned by ENG-2102). Sibling deps: E4-S1 (verifier), E5-S2 (deny-list feed).

## 🧪 Testing

`TestVerifierConformanceVectorsAndIntrospectionStaleness` (integration + CI gate). Tiers: unit (cache TTL) + integration (vectors). CS §5: fake identity introspect from FR-C13 fixtures; never mock own cache logic.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (drift-detection, staleness-bound lenses); cell-lint `JGAUQRsw2g`. No new Redis decision cache — the FR-I3 TTLs are the sole staleness backstop (PO ruling 8, NFR-M3).

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M11, NFR-M1/M3/M6; Arch `BE0jTNoweS` A-AUTH, §5 TS-binding-drift; identity FR-I3/I4; lib FR-L7; contracts FR-C13.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — FR-C13 vectors + audience model, OQ-M2); parent epic E4. Intra-service: E4-S1, E5-S2.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
