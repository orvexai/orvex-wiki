## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** platform, **I want** the embedded in-fork `packages/orvex-mcp` (73 tools) decommissioned once the contracts FR-C5 REST-gap ledger dispositions every wrapper tool (route to a wiki-api/contracts/ai/knowledge operation or explicitly retire — the 42 raw-Kysely tools have no network-boundary equivalent; the AI-tool subset routes to ai), `list_tools` long-tail parity is live on the standalone, and the golden tape is green — plus the 2827-line `tools.ts` split along the ADR-0004 tier boundaries — **so that** there is one agent door and the "thin" claim is enforceable rather than asserted.

**Definition of Done:** one named test `TestDecommissionParityAndTierSplit` (integration/structural — assert every one of the 73 wrapper tools has a ledger disposition (route/retire), that `list_tools` long-tail parity is live, and that `tools.ts` is split into edge/auth · backstage · registry/dispatch · envelope-shaping tiers with no file past the CS §8 smell threshold without justification). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the FR-C5 ledger, When decommission proceeds, Then all 73 wrapper tools are dispositioned: covered by a wiki-api/contracts/ai/knowledge operation or explicitly retired (42 raw-Kysely tools retire; AI subset → ai). *assert: ledger has 73 entries, each route-or-retire, no unmapped tool* [Source: FR-M16]
- [ ] **AC2** — Given `list_tools`, When the standalone runs, Then long-tail parity with the wrapper is live (FR-M2 mechanism). *assert: the wrapper's long tail is reachable by name on the standalone* [Source: FR-M16]
- [ ] **AC3** — Given the in-fork `/mcp` transport, When parity is reached, Then it deletes only at 73-tool disposition parity — not before. *assert: `/mcp` deletion gated on complete ledger; the wrapper-only CASL search gate is NOT ported* [Source: FR-M16]
- [ ] **AC4** — Given `tools.ts` (2827 lines), When split, Then it follows the ADR-0004 tiers (edge/auth · backstage · registry/dispatch · envelope-shaping) with no unjustified file past the CS §8 threshold. *assert: post-split, no tier file exceeds the smell threshold without a PR justification; the "thin" claim is now checkable* [Source: FR-M16, NFR-M4]
- [ ] **AC5** — Given the decommission, When the golden tape runs against wiki-api, Then it is green (FR-M16 gate c). *assert: golden tape green post-decommission* [Source: FR-M16, NFR-M6]

## 🔨 Tasks

- [ ] RED: write `TestDecommissionParityAndTierSplit` (AC1–AC5).
- [ ] GREEN: complete the FR-C5 ledger disposition for all 73 tools (AC1); route the AI subset to ai.
- [ ] GREEN: land `list_tools` long-tail parity; gate `/mcp` deletion on parity (AC2, AC3).
- [ ] GREEN: split `tools.ts` along the ADR-0004 tiers (AC4); confirm golden tape green (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — FR-C5 rest-gap ledger dispositioning all 73 in-fork tools (test/fixtures/rest-gap/rest-gap-ledger.yaml, in-fork-tool-surface.yaml) + parity gate test/parity-decommission.test.ts (448 lines) — the MCP-side completeness obligation that releases the engine deletion. The physical packages/orvex-mcp removal lives in the AGPL engine repo (out of this tree).

Tier: all four (this is the tier-discipline story) — ADR-0004 tier model (Arch §6, §7). Seam: MCP↔contracts (FR-C5 ledger), MCP↔fork (`packages/orvex-mcp`). Sibling deps: E1-S2 (`list_tools`), E3-* (all upstreams routed), E7-S1 (tape). ADR-0004 is proposed, not filed — capture as a service-tier ADR.

## 🧪 Testing

`TestDecommissionParityAndTierSplit` (integration + structural). Tiers: integration (parity/tape) + structural (tier split). CS §5 + §8: committed fixtures; never mock own tiers. A tool with no operation behind it does not exist post-split.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/8/10/11 (esp. §8 file-size smell, §9 ADR); SE-Arch `8sYi523i4t` (tier-discipline, decommission-parity lenses); cell-lint `JGAUQRsw2g`.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M16, §7 R3, NFR-M4; Arch `BE0jTNoweS` §5 decommission parity, §6 tier model, §7 ADR-0004; contracts FR-C5.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — FR-C5 REST-gap ledger); parent epic E7. Intra-service: E1-S2, E3-*, E7-S1. This is the long pole (R3).

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
