## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** Studio-product agent, **I want** the six `studio_*` product tools (`memory_get/save`, `library_list/save`, `librarian_session`, `comment_post`) to front the Studio backend carrying my token verbatim — verified by the same edge verifier + deny-by-default scope gate as the wiki path, with no service credential or placeholder Studio JWT — **so that** the product surface has the exact trust posture of the wiki surface and fails closed on absent/invalid/insufficient tokens.

**Definition of Done:** one named test `TestStudioProductToolsCallerTokenPassThroughFailClosed` (integration — assert each of the six tools forwards the caller token verbatim to `STUDIO_BACKEND_BASE_URL`, that no service credential is attached, and that scope-insufficient writes (`memory_save`/`library_save`/`comment_post`) fail closed). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given any of the six product tools, When called, Then the caller's token is forwarded verbatim to the Studio backend. *assert: outbound Authorization on all six byte-equals the caller's; no placeholder Studio JWT* [Source: FR-M19]
- [ ] **AC2** — Given a product tool call, When it runs, Then it passes through the same edge verifier + deny-by-default scope gate as the wiki path. *assert: verification pipeline identical to the wiki path* [Source: FR-M19]
- [ ] **AC3** — Given a write (`memory_save`/`library_save`/`comment_post`) with insufficient scope, When called, Then it fails closed. *assert: scope-insufficient write → typed denial, no backend call* [Source: FR-M19]
- [ ] **AC4** — Given no service credential is held, When any product tool runs, Then none is attached (posture, not deferred to OQ-M1). *assert: no service-token header on any studio_* path* [Source: FR-M19]
- [ ] **AC5** (frozen-seam) — Given the `studio_*` contract home is unpinned (OQ-M1), When the family is frozen, Then no new `studio_*` tool is added. *assert: the six-tool roster is fixed; contract home = TBD, pinned by ENG-2102 / OQ-M1* [Source: FR-M19]

## 🔨 Tasks

- [ ] RED: write `TestStudioProductToolsCallerTokenPassThroughFailClosed` (AC1–AC5).
- [ ] GREEN: wire the six product tools to `STUDIO_BACKEND_BASE_URL` with caller-token pass-through (AC1, AC4).
- [ ] GREEN: route them through the shared edge verifier + scope gate (AC2, AC3).
- [ ] GREEN: freeze the roster to six; reconcile `studio-wrapper.ts` (drop the two re-homed search tools) (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — src/server/studio-tools.ts registers 8 studio_* tools (marketplace_search, skill_get, memory_get/save, library_list/save, librarian_session, comment_post) with caller-token pass-through and fail-closed checkResourceAllowed('studio')+checkWriteAllowed scope gates; conditional on STUDIO_BACKEND_BASE_URL.

Tier: tool registry/dispatch over backstage adapter (Arch §3 studio_* rows). Seam: MCP↔Studio backend. `studio-wrapper.ts` still wires all 8 legacy routes — E6-S2 re-homes two, six remain here. Sibling deps: E4-S1/S3 (verifier + pass-through), E6-S2. Contract home is OQ-M1 (cutover-blocking before R2).

## 🧪 Testing

`TestStudioProductToolsCallerTokenPassThroughFailClosed` (integration). Tiers: unit (per-tool mapping) + integration (pass-through/fail-closed). CS §5: fake Studio backend from golden fixtures; never mock own dispatch/verifier.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (no-service-credential, fail-closed lenses); cell-lint `JGAUQRsw2g`. Credential posture is fixed now (FR-M19) — any deployed deviation is a defect.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M19, §8 OQ-M1; Arch `BE0jTNoweS` §3 studio_* rows, §9 OD-4.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG — OQ-M1 studio_* contract home); parent epic E6. Intra-service: E4-S1/S3, E6-S2.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
