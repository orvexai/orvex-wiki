## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As an** agent, **I want** `whoami` to return my verified principal plus a capability/space probe in one round-trip, **so that** my first call orients me to who I am and what I can do without a second request.

**Definition of Done:** one named test `TestWhoamiReturnsPrincipalAndCapabilitiesInOneCall` (integration — assert `whoami` returns the edge-verified principal `{idp, subject, tenant, org_or_realm, roles, token_scope}` plus wiki-api capabilities/spaces, in exactly one MCP round-trip). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a valid identity-minted token, When `whoami` is called, Then the response carries the org-scoped principal from the edge verifier. *assert: response principal set-equals the verifier's `{idp,subject,tenant,org_or_realm,roles,token_scope}`* [Source: FR-M14, FR-M11]
- [ ] **AC2** — Given the same call, When it executes, Then capabilities/spaces come from wiki-api **under the caller's token** and are included. *assert: `response.capabilities` populated from a single wiki-api call carrying the caller token* [Source: FR-M14]
- [ ] **AC3** — Given the round-trip budget, When `whoami` runs, Then it stays exactly one MCP round-trip. *assert: golden-tape call count for `whoami` == 1* [Source: FR-M14, NFR-M6]
- [ ] **AC4** (negative) — Given an absent/invalid token, When `whoami` is called, Then it fails closed with a typed denial, never a partial principal. *assert: unauthenticated `whoami` returns a typed auth error, no principal fields* [Source: FR-M14, NFR-M2]

## 🔨 Tasks

- [ ] RED: write `TestWhoamiReturnsPrincipalAndCapabilitiesInOneCall` (AC1–AC3).
- [ ] GREEN: compose `whoami` from the edge principal (E4-S1) + one wiki-api capability call under the caller token (AC1, AC2).
- [ ] GREEN: assert single-round-trip via the golden-tape counter (AC3).
- [ ] RED→GREEN: fail-closed unauthenticated path (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — whoami (src/server/tools.ts:419) does one probe (getUserMe+listSpaces in parallel), derives can_edit from token_scope, reports live spaces + principal; exercised in test/enforcement.test.ts and test/e2e/*.

Tier: tool registry/dispatch, consuming edge/auth seam output (Arch §6). Seam: MCP↔identity (principal), MCP↔wiki-api (capabilities). Sibling deps: E4-S1 (principal), E4-S3 (pass-through token), E3-S1 (wiki-api base URL).

## 🧪 Testing

`TestWhoamiReturnsPrincipalAndCapabilitiesInOneCall` (integration). Tiers: unit (composition) + integration (round-trip). CS §5: fake wiki-api from golden fixtures (sibling service — mock at network boundary); never mock own verifier binding.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (fail-closed lens); cell-lint `JGAUQRsw2g`. `whoami` is the agent's first call — one round-trip is an invariant.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M14, FR-M11; Arch `BE0jTNoweS` §3 `whoami` row, A-AUTH.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E1. Intra-service: E4-S1, E4-S3, E3-S1.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
