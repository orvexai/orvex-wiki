## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As the** wiki platform, **I want** the MCP boundary to be DfM-only — raw ProseMirror JSON never crosses in either direction, raw `format:"markdown"` writes rejected, opaque nodes travelling as typed handles — **so that** the agent contract stays a stable structured grammar and the engine's serialization invariants are never bypassed.

**Definition of Done:** one named test `TestDfmOnlyBoundaryRejectsRawMarkdownAndPreservesOpaqueHandles` (integration — assert a `format:"markdown"` write is rejected with the upstream typed error verbatim, a `dfm`/`json` structured write passes, and opaque nodes round-trip as `:::info{id=<uuid>}:::`-style typed handles without body mutation). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2102); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a write with `format:"markdown"`, When submitted, Then it is rejected with the upstream typed error, verbatim, no fallback path. *assert: `save_page({format:'markdown'})` returns the upstream rejection code unchanged* [Source: FR-M4]
- [ ] **AC2** — Given a write in `dfm` or structured-block `json`, When submitted, Then it is accepted and forwarded. *assert: `save_page({format:'dfm'})` reaches the upstream client* [Source: FR-M4]
- [ ] **AC3** — Given content containing an opaque node, When it crosses the boundary in either direction, Then it travels only as a typed handle with its `id`, never as raw ProseMirror JSON. *assert: opaque node serializes to `:::…{id=<uuid>}:::` and its inner bytes are byte-preserved* [Source: FR-M4]
- [ ] **AC4** (negative) — Given raw ProseMirror JSON in a request, When submitted, Then the boundary rejects it (enforcement is upstream; MCP surfaces the typed error, adds no coercion). *assert: raw-PM input yields a typed error, no silent conversion* [Source: FR-M4]

## 🔨 Tasks

- [ ] RED: write `TestDfmOnlyBoundaryRejectsRawMarkdownAndPreservesOpaqueHandles` (AC1–AC4).
- [ ] GREEN: enforce `dfm|json`-only at request validation (zod); reject `markdown` and raw PM (AC1, AC2, AC4).
- [ ] GREEN: opaque-node typed-handle passthrough with byte preservation (AC3).
- [ ] GREEN: ensure the MCP surfaces upstream typed errors verbatim (no local fallback).

## 🧠 Context

**🧾 Code audit (origin/main @ be3cd8b, 2026-07-14):** present — save_page/edit accept only content at format:'dfm' (no raw markdown field); opaque-node guard isOpaqueBlock/OPAQUE_BLOCK_TYPES at src/server/tools.ts:373 refuses text-editing embed/excalidraw/etc.; test/enforcement.test.ts.

Tier: tool registry/dispatch + envelope shaping (Arch §6). Seam: MCP↔wiki-api (enforcement lives upstream, FR-A2; MCP adds no fallback). Sibling deps: E2-S2 (write chokepoint applies this contract).

## 🧪 Testing

`TestDfmOnlyBoundaryRejectsRawMarkdownAndPreservesOpaqueHandles` (integration). Tiers: unit (validation) + integration (round-trip byte-preservation). CS §5: fake wiki-api from golden fixtures; never mock the DfM validator (own code).

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/10/11; SE-Arch `8sYi523i4t` (no-fallback lens — the family "no silent rot" rule); cell-lint `JGAUQRsw2g`. Enforcement is upstream; the MCP adds no coercion path.

## 🔗 References

PRD `k1sWjtJq3x` §4 FR-M4; Arch `BE0jTNoweS` §3, A-SHIM; wiki-api FR-A2.

## 🔗 Dependencies

Blocked by: ENG-2102 (contract TAG dispatch gate); parent epic E1. Intra-service: consumed by E2-S2.

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
