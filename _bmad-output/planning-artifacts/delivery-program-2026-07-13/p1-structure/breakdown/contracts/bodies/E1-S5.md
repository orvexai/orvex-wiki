## 🎯 Story
As the AGPL-boundary guardian, I want the authored-not-generated provenance guard — a PR-template attestation plus a generator-signature lint that rejects files matching known generator banners (swagger-autogen) or recorded-payload markers — so that no engine-derived (AGPL) artifact ever enters a closed consumer's codegen path.

**Definition of Done:** ONE named test `TestGeneratorSignatureRejected` — a CI-layer test asserting, through `validate.yaml`, that committing a file bearing a swagger-autogen banner or a recorded-payload marker exits non-zero, while a hand-authored equivalent passes. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a PR, When it opens, Then the PR template carries a provenance attestation the author signs (no engine-generated dumps / introspected schemas / recorded payloads). *Assert: PR template contains the attestation checkbox* [Source: NFR-C1, A-AUTHOR].
- [ ] **AC2** — Given a file with a known generator signature (negative path), When CI runs, Then it fails as a licensing incident. *Assert: exit ≠ 0 naming the file + signature* [Source: A-AUTHOR, NFR-C1].
- [ ] **AC3** — Given a hand-authored artifact, When CI runs, Then it passes. *Assert: exit 0* [Source: NFR-C1].
- [ ] **AC4** — Given the already-authored `k5-cited-answer`/`write-receipt` schemas "verified against" fork DTOs, When the guard runs, Then it confirms they carry no generator/recorded-payload markers. *Assert: both pass the signature lint* [Source: SE-Arch T5 "keeps honest the schemas already authored"].

## 🔨 Tasks
- [ ] RED: `TestGeneratorSignatureRejected` over a banner-bearing fixture — fails until the lint exists.
- [ ] GREEN: add the generator-signature lint to `validate.yaml` (banner list + recorded-payload markers).
- [ ] GREEN: add the PR-template provenance attestation.
- [ ] REFACTOR: document the review rule (cite engine behavior as *evidence*, never paste engine code).

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**. Seam crossed: the licensing boundary (AGPL engine vs Apache-2.0 contracts). Sibling deps: complements E6-S1 (the family AGPL-import guard runs in consumer repos; this guards *this* repo's own authoring). Distinct from NFR-C7 which is the family denylist.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — the .github/pull_request_template.md A-AUTHOR attestation exists, but the mechanical generator-signature lint (swagger-autogen/recorded-payload rejection) and TestGeneratorSignatureRejected are absent; the agpl-guard checks imports, not provenance.

## 🧪 Testing
Named DoD test `TestGeneratorSignatureRejected` (CI/validation layer). CS §5 mocking: none — real file scanning; never mock own packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §9 (licensing is an ADR-grade concern), §10 (zero-trust), §11 (honesty); SE-Arch 8sYi523i4t security-licensing lens (A-AUTHOR guards, T5); cell-lint JGAUQRsw2g. Any lapse = a licensing incident, not a style nit (Architecture §5).

## 🔗 References
- PRD (jwF4VLHfNs) NFR-C1, §9 Decisions (A-AUTHOR carried)
- Architecture (o2waDNw3ix) A-AUTHOR, §5 Risks; SE-Arch review (nngOgO0CGO) T5

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E1. Order: after E1-S2 (rides validate.yaml).
- [ ] Related: E6-S1 (family AGPL-import guard — different scope).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any suspected AGPL contamination (licensing incident).
