**Feature area.** The two family-wide vocabularies that servers and the CLI conform to verbatim: the frozen ~58-code cross-service `errorCode` set + the CLI 0–9 exit-code buckets (with the namespaced-extension policy), and the MCP REST-gap ledger — the audited map from the ~73 MCP tools (incl. the 42 raw Kysely uses) to engine/knowledge/ai primitives, every gap dispositioned.

**Why it exists.** The errorCode/exit-code vocabulary is *"the one artifact wiki-api/knowledge/ai/CLI all conform to"* (D-CON-9) — without it each service invents its own codes and the CLI cannot transport a stable exit contract. The REST-gap ledger is *"a contracts-repo deliverable"* named by the split canon that *"gates the MCP satellite's tool-parity cutover"* (FR-C5) — MCP is at ~19/73 tools and cannot repoint until every tool resolves to a pinned primitive or an explicit retirement.

**Story roster.**
- [ ] E5-S1 — Frozen errorCode + CLI exit-code vocabulary (errors/vocabulary.yaml + errors/exit-codes.yaml)
- [ ] E5-S2 — MCP REST-gap ledger (mcp/rest-gap-ledger.yaml)

**FR/NFR ids this epic owns:** FR-C23, FR-C5.

**Definition of Done.** Both stories Done ∧ `errors/vocabulary.yaml` contains the frozen set incl. `QUOTA_EXCEEDED` (402, superset envelope), `BUDGET_EXCEEDED`, `MODEL_UNAVAILABLE`, `VERSION_MISMATCH` (409) and the `RATIFY_*` family, with **no** `LINEAR_NOT_CONNECTED` (D-S11) ∧ every exit-code maps to a 0–9 bucket and namespaced `AI_*`/`SEARCH_*` codes extend without renumbering ∧ every MCP tool in the ledger resolves to exactly one disposition (covered / newly-authored FR-C1 primitive / explicitly retired) ∧ family-E2E share: the api-key behavioural-parity spec (E6-S4) references this vocabulary and passes engine-side ∧ SDD lines evidenced.

## 🔗 Dependencies
- [ ] **Blocked by:** ENG-2091 (Definition Pack — contract TAG is the dispatch gate).
- [ ] E5-S1 blocks every E2 OpenAPI file's error-response references and E2-S5's `QUOTA_EXCEEDED` passthrough.
- [ ] E5-S2 disposition (b) *"newly authored primitive"* feeds back into E2-S1 (engine-primitives); parity input is the ~73-tool MCP surface.
- [ ] OQ-C5 (is the full 73-tool JSON pinned here vs MCP-internal) gates E5-S2 scope.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if a REST-gap resolves to a new engine primitive (feeds E2-S1; may be a seam-reshaping ADR-lane change).
