## 🎯 Story
As the SSE bridge and every indexer, I want `PROFILES.md` authored — declaring per type whether it is thin (identity + version; body pulled from the source content resolver) or rich (full entity; what the SSE bridge fans out), the explicit CloudEvent-type ↔ legacy-SSE-frame-name mapping table (`wiki.page.created` ↔ `page.created`, etc.), and the rich-frame integrity posture (consumers MUST integrity-verify/re-resolve content-bearing envelopes, knowledge A4 wins) — so that the profile split and frame mapping are contract terms, not implicit conventions.

**Definition of Done:** ONE named test `TestProfileAndFrameMapConsistent` — a CI-layer test asserting, through `validate.yaml`, that every catalog type has a `profile` entry in `PROFILES.md`, that the type↔frame mapping table covers every `wiki.*` type with its unprefixed legacy frame name, and that rich-frame types carry the re-resolution obligation note. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given every catalog type, When `PROFILES.md` lands, Then each is declared thin or rich with a rationale. *Assert: profile coverage == catalog type set* [Source: FR-C7, A-PROFILES].
- [ ] **AC2** — Given the mapping table, When authored, Then every `wiki.*` type maps to its unprefixed legacy frame name (`wiki.page.created`↔`page.created`). *Assert: bijective map covers all wiki.* types* [Source: FR-C7, A-PROFILES].
- [ ] **AC3** — Given a rich, content-bearing event, When a consumer receives it, Then the contract states it MUST integrity-verify/re-resolve rather than blindly trust (knowledge A4 wins over "pure re-encoder"). *Assert: re-resolution obligation present; recorded in RISKS.md* [Source: FR-C7, D-CON-6].
- [ ] **AC4** — Given a catalog type with no PROFILES.md entry (negative path), When the check runs, Then it fails. *Assert: exit ≠ 0 naming the type* [Source: FR-C7].

## 🔨 Tasks
- [ ] RED: `TestProfileAndFrameMapConsistent` — fails until PROFILES.md covers the catalog.
- [ ] GREEN: author `PROFILES.md` (thin/rich per type + rationale).
- [ ] GREEN: author the type↔frame mapping table.
- [ ] GREEN: state the rich-frame integrity obligation; cross-reference RISKS.md (E3-S6).
- [ ] RED: negative path — uncovered type fails.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is the SSE gateway re-encoder + knowledge indexers. Seam crossed: spine↔SSE gateway↔CLI, spine↔knowledge. Sibling deps: depends on E3-S1 (catalog types); feeds E3-S5 (WIRE.md uses the frame mapping as authority) + E3-S6 (RISKS.md).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — events/PROFILES.md is authored with the thin/rich/standard profiles + type<->legacy-frame mapping + rich re-resolution posture, but there is no TestProfileAndFrameMapConsistent coverage test (validate_events checks catalog<->EVENTS.md parity only).

## 🧪 Testing
Named DoD test `TestProfileAndFrameMapConsistent` (CI/validation layer). CS §5 mocking: none — real PROFILES.md + catalog parse; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §10 (zero-trust — re-resolution not trust), §12; SE-Arch 8sYi523i4t security lens (rich-frame integrity, D-CON-6); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C7, D-CON-6; Architecture (o2waDNw3ix) A-PROFILES
- knowledge FR-K2/K15/A4

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E3. Depends on E3-S1; feeds E3-S5, E3-S6.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
