## 🎯 Story
As orvex-studio-billing (the plans/entitlements system of record) and its consumers (engine F-QUOTA, ai spend-cap, console display), I want `openapi/billing.yaml` + `billing/plan.schema.json` + `billing/entitlements.schema.json` authored — the per-tenant entitlement-read surface + the Stripe-webhook-driven plan/entitlement admin surface, with the locked Free / £7 Personal quota values and the Teams-teaser / hidden-Enterprise rows present but quota-deferred, entitlements versioned with grandfathering — so that the family's single biggest completeness gap is closed and no consumer hard-codes plan numbers.

**Definition of Done:** ONE named test `TestEntitlementSchemaLockedValues` — a CI-layer test asserting, through `validate.yaml`, that `billing/plan.schema.json` encodes the Free (200 pages · 1 GiB · 2,000 files · 25 members · 10 MB/file) and £7 Personal (20,000 pages · 50 GiB · 20,000 files · 25 members) values, that Teams/Enterprise rows exist with quota slots empty (OQ-C9 deferred), and that an entitlement record pins its plan-version (grandfathering). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the entitlement-read op, When a tenant reads its effective entitlement, Then quota values + AI spend-cap are returned; it is bearer-tier for tenant reads. *Assert: read op present, bearer scheme* [Source: FR-C24, D-S7].
- [ ] **AC2** — Given `plan.schema.json`, When authored, Then Free + £7 Personal values are locked exactly as canon; Teams-teaser + hidden-Enterprise rows present with quota values DEFERRED (schema slot, no number). *Assert: Free/Personal values present; Teams/Enterprise slots null* [Source: FR-C24, OQ-C9].
- [ ] **AC3** — Given an entitlement record, When minted, Then it pins the plan-version it was minted under (grandfathering). *Assert: entitlements.schema.json requires `plan_version`* [Source: FR-C24].
- [ ] **AC4** — Given the admin/write surface (negative path), When a bearer token is presented, Then it is rejected — admin writes are workload-identity (Stripe-webhook-driven). *Assert: admin ops use workload-identity scheme* [Source: FR-C24, A-OPENAPI].
- [ ] **AC5** — Given a plan upgrade/downgrade, When it commits, Then billing publishes `billing.entitlement.changed` (engine + wiki-api cache-evict; <60 s propagation). *Assert: publisher obligation cross-referenced to E3-S1 catalog* [Source: FR-C24, FR-C6].

## 🔨 Tasks
- [ ] RED: `TestEntitlementSchemaLockedValues` — fails until schemas encode the locked values.
- [ ] GREEN: author `billing.yaml` (entitlement-read bearer + admin workload-identity).
- [ ] GREEN: author `plan.schema.json` (Free/Personal locked, Teams/Enterprise deferred) + `entitlements.schema.json` (versioned+grandfathered).
- [ ] RED: negative path — bearer rejected on admin; deferred slots empty.
- [ ] Add SEAMS.md row; cross-reference `billing.entitlement.changed` to E3-S1.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is billing's Go handlers + Stripe webhook ingestion. Seam crossed: billing↔engine (quota values), billing↔ai (spend-cap), billing↔console (display), billing→spine (`billing.entitlement.changed`). Sibling deps: feeds E2-S1 (engine reads these values) + E2-S6 (ai reads cap); depends on E1-S6, E5-S1.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — billing/plan.schema.json encodes the LOCKED Free/Personal quota values + deferred Teams/Enterprise slots + planVersion grandfathering, and billing.yaml exists, but there is no TestEntitlementSchemaLockedValues. NOTE: origin/dev #40 ratifies the canonical entitlement-check/AI-budget-preflight — NOT on main.

## 🧪 Testing
Named DoD test `TestEntitlementSchemaLockedValues` (CI/validation layer). CS §5 mocking: none for schema value assertions; fake the billing server as a golden fixture for drift; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams — billing SoR), §9 (Teams/Enterprise values = ADR-grade ruling), §12; SE-Arch 8sYi523i4t seam lens; cell-lint JGAUQRsw2g. §6 tiers N/A. NEVER invent Teams/Enterprise quota values — canon defers them (OQ-C9); leave the slot "TBD — pinned by the Definition Pack (ENG-2091)" / OQ-C9.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C24, OQ-C9, D-S7; Architecture (o2waDNw3ix) A-OPENAPI, A-LAYOUT (`billing/`)
- billing PRD Blcvui4UIn (SoR); brief pricing canon (Free / £7 / Teams £70 framing)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E2. Depends on E1-S6, E5-S1.
- [ ] Feeds: E2-S1 (engine F-QUOTA values), E2-S6 (ai cap value), E3-S1 (`billing.entitlement.changed`).
- [ ] Gated: OQ-C9 (Teams/Enterprise values) — deferred, slot only.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-C9 Teams/Enterprise value ruling.
