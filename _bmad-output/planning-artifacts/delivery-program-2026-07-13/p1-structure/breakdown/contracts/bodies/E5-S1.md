## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As wiki-api, knowledge, ai, and the Orvex CLI, I want the frozen family vocabulary authored — `errors/vocabulary.yaml` (the ~58-code cross-service errorCode set incl. `SEARCH_MODE_UNAVAILABLE`, `AI_EMBED_PROVIDER_NOT_CONFIGURED`, `DUPLICATE_CANDIDATE`, `DATE_SLUG_NOT_ALLOWED`, `DOC_TYPE_NOT_ALLOWED`, `DRIFT_DETECTED`, `CACHE_STALE`, `VERSION_MISMATCH` (409), the `RATIFY_*` family, `QUOTA_EXCEEDED` (402, superset envelope `{quota,state,limit,usage,percent,graceEndsAt?,plan,upgradeUrl,message}`), `BUDGET_EXCEEDED`, `MODEL_UNAVAILABLE`; NO `LINEAR_NOT_CONNECTED`) + `errors/exit-codes.yaml` (the frozen CLI 0–9 buckets each code maps into, + the namespaced-extension policy) — so that every server and the CLI conform to one vocabulary.

**Definition of Done:** ONE named test `TestErrorVocabularyFrozenAndMapped` — a CI-layer test asserting, through `validate.yaml`, that every errorCode maps to exactly one 0–9 exit bucket, that `QUOTA_EXCEEDED` carries the 402 superset envelope, that no `LINEAR_NOT_CONNECTED` exists, and that a namespaced `AI_*`/`SEARCH_*` addition does not renumber the exit buckets. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `errors/vocabulary.yaml`, When authored, Then it contains the frozen ~58-code set reconciled across wiki-api/knowledge/ai incl. the named codes. *Assert: named codes present; count matches canon* [Source: FR-C23, D-CON-9].
- [ ] **AC2** — Given `QUOTA_EXCEEDED`, When authored, Then it is 402 with the superset envelope `{quota,state,limit,usage,percent,graceEndsAt?,plan,upgradeUrl,message}`. *Assert: 402 + full envelope* [Source: FR-C23, F-QUOTA].
- [ ] **AC3** — Given `errors/exit-codes.yaml`, When authored, Then every errorCode maps to exactly one 0–9 bucket the CLI transports verbatim. *Assert: total mapping; buckets 0–9* [Source: FR-C23].
- [ ] **AC4** — Given `LINEAR_NOT_CONNECTED` (negative path), When anyone adds it, Then the gate rejects it — it is removed, not reserved. *Assert: no LINEAR_NOT_CONNECTED* [Source: FR-C23, D-S11].
- [ ] **AC5** — Given a namespaced `AI_*`/`SEARCH_*` code addition, When applied, Then exit buckets are not renumbered (additive-extension policy). *Assert: extension does not renumber* [Source: FR-C23].

## 🔨 Tasks
- [ ] RED: `TestErrorVocabularyFrozenAndMapped` — fails until vocab + exit-codes exist.
- [ ] GREEN: author `errors/vocabulary.yaml` (frozen set + QUOTA_EXCEEDED envelope).
- [ ] GREEN: author `errors/exit-codes.yaml` (0–9 buckets + extension policy).
- [ ] RED: negative paths — LINEAR_NOT_CONNECTED rejected; extension renumbering rejected.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is each server's error-mapping layer + the CLI exit handler. Seam crossed: every server↔CLI (error/exit contract). Sibling deps: referenced by all E2 OpenAPI error responses + E2-S5 QUOTA passthrough + E2-S6 BUDGET_EXCEEDED/MODEL_UNAVAILABLE; consumed by E6-S4 (api-key behavioural-parity).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — errors/vocabulary.yaml + exit-codes.yaml are authored and tests/test_errorcode_contract.py::TestErrorCodeExitBucketContract directly realizes the DoD (buckets complete, code->bucket, 402 superset, no LINEAR_, order frozen, namespaced-no-renumber).

## 🧪 Testing
Named DoD test `TestErrorVocabularyFrozenAndMapped` (CI/validation layer). CS §5 mocking: none — real vocab/mapping parse; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §9, §12 (contract-shape — error/exit vocabulary is a seam contract); SE-Arch 8sYi523i4t seam lens; cell-lint JGAUQRsw2g. §6 tiers N/A. NFR-A6 freezes only the wiki-api subset; this is the family-wide superset.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C23, D-CON-9, D-S11; Architecture (o2waDNw3ix) A-LAYOUT (`errors/`)
- CLI-plan G6

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E5. Blocks E1-S6 (envelope), all E2 error responses, E6-S4.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any exit-bucket renumbering (breaking → ADR lane).
