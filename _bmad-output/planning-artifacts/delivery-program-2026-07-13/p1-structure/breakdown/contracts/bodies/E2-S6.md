## 🎯 Story
As orvex-studio-ai (server) and MCP (codegen client), I want `openapi/ai.yaml` authored — the cited-ask/RAG-loop contract with the K5 answer schema `{answer, citations, confidence, unanswered, gapNote, followups}` (retrieval stated normatively as a tool call into knowledge's surface under the caller's delegated principal), plus the per-tenant spend-cap read/enforce operations (cap VALUE owned by billing FR-C24; ai reads + enforces via LiteLLM `max_budget` + per-caller scoped keys/budgets) — so that the ask loop is the single home of K5 and MCP's third upstream has a pinned surface.

**Definition of Done:** ONE named test `TestAiK5SingleHome` — a CI-layer test asserting, through `validate.yaml` + the drift gate, that `k5-cited-answer.schema.json` is referenced by `ai.yaml` and by no other OpenAPI file as an owner, and that the spend-cap ops READ (not own) the cap value from the billing entitlement. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the cited-ask op, When authored, Then the response is the K5 schema `{answer,citations,confidence,unanswered,gapNote,followups}`. *Assert: K5 shape matches; schema resolves* [Source: FR-C22].
- [ ] **AC2** — Given the RAG loop, When retrieval happens, Then it is normatively a tool call into knowledge's FR-C2 surface carrying the CALLER's delegated principal (ACL ∩ token-scope binds every hop). *Assert: normative note present; principal is caller-delegated* [Source: FR-C22, D-ASK].
- [ ] **AC3** — Given the spend-cap read op, When authored, Then it READS the cap value from the billing entitlement (refreshed on `billing.entitlement.changed`) — ai is NOT the SoR (negative: no cap-value storage op on ai). *Assert: no cap-value write/own op; read-from-billing documented* [Source: FR-C22, D-CAP, D-S7].
- [ ] **AC4** — Given enforcement, When over cap, Then a `BUDGET_EXCEEDED` (and `MODEL_UNAVAILABLE` where relevant) is defined per the frozen vocabulary; enforcement is LiteLLM `max_budget` + per-caller scoped keys. *Assert: codes ⊆ errors/vocabulary.yaml* [Source: FR-C22, FR-C23].
- [ ] **AC5** — Given writes from the ask loop, When authored, Then they go through wiki-api's FR-C19 block chokepoint (ai is a bearer-tier client of wiki-api). *Assert: normative write-path note present* [Source: FR-C22, D-ASK].

## 🔨 Tasks
- [ ] RED: `TestAiK5SingleHome` — fails if K5 is owned anywhere but ai.yaml.
- [ ] GREEN: author the cited-ask/RAG op + K5 answer schema reference.
- [ ] GREEN: author the spend-cap read/enforce ops (read from billing; `BUDGET_EXCEEDED`/`MODEL_UNAVAILABLE`).
- [ ] RED: negative path — no cap-value-own op on ai; K5 owned only here.
- [ ] Add SEAMS.md row (blocks MCP AI-tool cutover + console cap-display per NFR-C4).

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is ai's Go handlers + LiteLLM gateway. Seam crossed: MCP↔ai (AI tools), ai↔knowledge (delegated retrieval), ai↔wiki-api (writes), ai↔billing (cap read). Sibling deps: depends on E1-S6, E5-S1, E2-S7 (billing cap value), E2-S5 (wiki-api write path); resolves K5 single-home (T3) with E2-S5.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — openapi/ai.yaml (ask/chat/inline/spend-cap) + schemas/k5-cited-answer.schema.json are authored and gated draft (test_envelope_contract::test_ai_dto_stubs_draft), but there is no TestAiK5SingleHome ownership/spend-cap-reads-billing test. NOTE: origin/dev advances ai.yaml AI-DTO stubs beyond main.

## 🧪 Testing
Named DoD test `TestAiK5SingleHome` (CI/validation+drift layer). CS §5 mocking: fake the ai server + billing entitlement as golden fixtures; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams — T3 single-home, D-ASK topology), §9, §12; SE-Arch 8sYi523i4t seam lens (T3); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C22, FR-C24, D-ASK/D-CAP; Architecture (o2waDNw3ix) A-OPENAPI; SE-Arch review (nngOgO0CGO) T3

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E2. Depends on E1-S6, E5-S1, E2-S7, E2-S5.
- [ ] Gated: OPEN DECISION #3 (Phase-0 proxy `ask` retire vs coexist).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OPEN DECISION #3.
