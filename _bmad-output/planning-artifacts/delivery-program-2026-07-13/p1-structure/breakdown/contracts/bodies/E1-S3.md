## 🎯 Story
As the program's chokepoint-risk owner, I want `SEAMS.md` — the NFR-C4 seam inventory listing every cross-repo call/event/frame and the artifact that pins it, with a `referenced-but-unpinned` row being build-blocking for the referencing satellite's cutover — so that no seam is ever silently vaporware and the repo's declared health metric is measurable.

**Definition of Done:** ONE named test `TestUnpinnedSeamBlocksCutover` — a CI-layer test asserting, through `validate.yaml`, that a `SEAMS.md` row in state `referenced-but-unpinned` marked for a given satellite's cutover causes the completeness check to exit non-zero for that satellite. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `SEAMS.md` with columns `seam · transport · pinned-artifact · state`, When it lands, Then every cross-repo call/event/frame in the family has exactly one row. *Assert: no duplicate seam keys; every A-OPENAPI/A-CATALOG seam present* [Source: NFR-C4, SE-Arch T5].
- [ ] **AC2** — Given a row in state `pinned`, When the completeness check runs, Then its `pinned-artifact` path resolves to a real file. *Assert: file exists* [Source: NFR-C4].
- [ ] **AC3** — Given a row `referenced-but-unpinned` flagged for satellite X's cutover (negative path), When the check runs for X, Then it exits non-zero (build-blocking). *Assert: exit ≠ 0 naming the seam + satellite* [Source: NFR-C4, PRD FR-C19/FR-C22 "SEAMS.md entry lands with the file"].
- [ ] **AC4** — Given the identity JWKS-mirror seam at `x-status: draft`, When it is listed, Then it is present in `SEAMS.md` from day one even while unpinned. *Assert: JWKS-mirror row exists* [Source: FR-C3].

## 🔨 Tasks
- [ ] RED: `TestUnpinnedSeamBlocksCutover` with a synthetic unpinned row — fails until the check exists.
- [ ] GREEN: author `SEAMS.md` seeded from the §6 dependencies table + A-OPENAPI/A-CATALOG seams.
- [ ] GREEN: add the `validate.yaml` completeness check (pinned-artifact resolves; unpinned-for-cutover blocks).
- [ ] REFACTOR: document the state vocabulary (`pinned` | `referenced-but-unpinned`).

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**. Seam crossed: SEAMS.md *is* the family seam inventory; it is the completeness invariant behind every E2/E3 story ("its SEAMS.md entry lands with it"). Sibling deps: rows are added by E2/E3/E4 stories as they pin surfaces; the check is wired into E1-S2's validate.yaml.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — SEAMS.md is authored with the pinned vs referenced-but-unpinned states + build-blocking note and test_seam_freeze.py::test_seams_all_pinned gates the six pinned rows, but there is no TestUnpinnedSeamBlocksCutover completeness gate.

## 🧪 Testing
Named DoD test `TestUnpinnedSeamBlocksCutover` (CI/validation layer, through validate.yaml). CS §5 mocking: none — real `SEAMS.md` + filesystem; never mock own packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams — this repo IS the pinned seam artifact), §11 (honesty — SEAMS.md makes the health metric measurable, T5), §10; SE-Arch 8sYi523i4t completeness lens (NFR-C4, T5); cell-lint JGAUQRsw2g.

## 🔗 References
- PRD (jwF4VLHfNs) NFR-C4, §6 Dependencies table, FR-C19/FR-C22/FR-C24 "SEAMS.md entry lands with it"
- Architecture (o2waDNw3ix) §1, SE-Arch review (nngOgO0CGO) T5

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E1. Order: after E1-S2 (check runs inside validate.yaml).
- [ ] Fed by: every E2/E3/E4 pinning story adds its row.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
