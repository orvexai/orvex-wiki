## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As the orvex-wiki engine (server) and its consumers, I want `openapi/engine-primitives.yaml` fleshed from titles-only stubs to full operations — block read/patch with `ifVersion`/409 `VERSION_MISMATCH`, the FR-13 ACL primitives (permissions/evaluate, accessible-space-IDs, batch `filterAccessiblePageIds` with the 500-ID ceiling), slug resolve, per-page + space export (markdown + embed-resolved `text_repr` + `updated-since`), api-key validation, FR-15 exchange-token consume, engine-side Clerk provision/deprovision, realtime-invalidate, FR-35 export-policy evaluate, and the F-QUOTA quota-status read primitive — so that the engine conforms to a pinned primitive surface it does not own the plan-numbers for.

**Definition of Done:** ONE named test `TestEnginePrimitivesConforms` — a CI-layer test asserting, through the served-OpenAPI-diff drift gate (E6-S3) run against a conforming engine stub, that the engine's exposed surface matches `engine-primitives.yaml` including the 402 `QUOTA_EXCEEDED` superset envelope and the 409 `VERSION_MISMATCH` shape. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given block patch with `ifVersion`, When the version mismatches, Then a 409 `VERSION_MISMATCH` envelope is returned. *Assert: op defines 409 referencing the shared envelope + `VERSION_MISMATCH` code* [Source: FR-C1].
- [ ] **AC2** — Given `filterAccessiblePageIds`, When called with >500 IDs (negative path), Then the ceiling is enforced. *Assert: op documents the 500-ID batch ceiling* [Source: FR-C1, engine FR-13].
- [ ] **AC3** — Given the F-QUOTA quota-status read, When a tenant is over limit at the write chokepoint, Then a 402 `QUOTA_EXCEEDED` with envelope `{quota,state,limit,usage,percent,graceEndsAt?,plan,upgradeUrl,message}` is defined; the engine reads (does not own) the limit values from billing. *Assert: 402 envelope matches FR-C23 superset; no plan numbers hard-coded* [Source: FR-C1, FR-C24, D-S7].
- [ ] **AC4** — Given the api-key validation op, When it errors, Then it uses the frozen errorCode vocabulary (FR-11 behavioural-parity). *Assert: error codes ⊆ errors/vocabulary.yaml* [Source: FR-C1, FR-C23].
- [ ] **AC5** — Given the engine Clerk provision/deprovision op, When authored, Then it is `{clerkOrgId,…}` mode `deactivate|delete` — distinct from the studio `{scope,id}`/`disable|delete` shape, cross-referenced so no caller wires one to the other. *Assert: cross-ref note present* [Source: FR-C1, FR-C3, identity FR-I14].
- [ ] **AC6** — Given internal-facing engine ops, When authored, Then bearer for public + workload-identity for any internal surface (`$ref` `_components`). *Assert: no inlined auth* [Source: A-OPENAPI].

## 🔨 Tasks
- [ ] RED: `TestEnginePrimitivesConforms` against a conforming stub — fails on titles-only stubs.
- [ ] GREEN: author the operation set (blocks, ACL batch, slug, export, api-key, exchange consume, provision/deprovision, realtime-invalidate, export-policy, F-QUOTA quota-status).
- [ ] GREEN: wire 402/409 envelopes to the shared `_components` + `errors/vocabulary.yaml`.
- [ ] RED: negative paths — over-ceiling batch, quota-exceeded, version-mismatch.
- [ ] Add the `SEAMS.md` rows.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the NestJS-engine analogue is the seam the engine's controllers conform to. Seam crossed: engine↔everyone (primitives), engine↔billing (reads entitlement). Sibling deps: depends on E1-S6 (`_components`), E5-S1 (errorCodes), E2-S7 (billing entitlement it reads); feeds E5-S2 (REST-gap disposition (b) adds primitives here).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — openapi/engine-primitives.yaml is fully fleshed (15 ops: blocks/ACL/export/api-key/exchange/provision/realtime-invalidate/export-policy/quota-status) and tests/test_engine_primitives_contract.py::TestEnginePrimitivesQuotaContract gates the 402 QUOTA_EXCEEDED superset + 409 VERSION_MISMATCH.

## 🧪 Testing
Named DoD test `TestEnginePrimitivesConforms` (CI/drift-gate layer). CS §5 mocking: mock the engine server surface as a golden fixture (sibling faked — CS mock-boundary rule); never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §9 (any breaking primitive change = ADR), §12 (contract-shape); SE-Arch 8sYi523i4t seam + honesty lenses; cell-lint JGAUQRsw2g (host/servers hygiene, T6). §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C1, FR-C23, FR-C24; engine PRD EPsdD7uK8e (F-QUOTA)
- Architecture (o2waDNw3ix) A-OPENAPI, §3, §4

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E2. Depends on E1-S6, E5-S1, E2-S7 (billing entitlement values).
- [ ] Feeds: E5-S2 (new-primitive dispositions), E6-S3/E6-S4 (drift + api-key parity gates).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any breaking primitive reshape (ADR-0008 ADR lane).
