## 🎯 Story
As every conforming server + consumer repo, I want the contract-drift gate definitions + reference tooling authored under `gates/drift/` — (a) served-OpenAPI diff (each server's exposed surface matches its pinned spec), (b) emitted-event validation (relay/publisher output validates against `events/schemas/` + the FR-C8 obligations), (c) fixture reproduction (exporter output byte-identical to `fixtures/`), and (d) SSE conformance (a replay harness driving the FR-C10 resume/gap/filter behaviors incl. the key-type ∩ token-scope intersection) — each deterministic, no flaky network, and run in each repo's CI pinned to a release tag — so that drift is impossible to miss.

**Definition of Done:** ONE named test `TestDriftGatesCatchDivergence` — a gate-definition test asserting, run against synthetic diverging fixtures, that each of the four gates (a/b/c/d) fails when its side stops matching the pin and passes when it matches, deterministically and offline. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given served-OpenAPI diff, When a server's surface drifts from its pinned spec (negative path), Then the gate fails. *Assert: openapi-diff gate fails on drift* [Source: FR-C16(a), OQ-C4].
- [ ] **AC2** — Given emitted-event validation, When a publisher emits an event missing `version` (negative path), Then it fails against `events/schemas/` + FR-C8. *Assert: event-validation gate fails on missing version* [Source: FR-C16(b), FR-C8].
- [ ] **AC3** — Given fixture reproduction, When exporter output is not byte-identical to `fixtures/` (negative path), Then it fails. *Assert: byte-compare gate fails on mismatch* [Source: FR-C16(c)].
- [ ] **AC4** — Given SSE conformance, When a serving impl breaks resume/gap or leaks to a scoped token (negative path), Then the replay harness fails — filter cases include key-type ∩ token-scope, not key-type alone. *Assert: conformance harness fails on leak/broken-resume* [Source: FR-C16(d), D-CON-7].
- [ ] **AC5** — Given all gates, When run, Then they are deterministic with no flaky network dependence and pinned to a contracts release tag. *Assert: no network syscalls; tag-pinned; false-positive rate tracked* [Source: FR-C16, NFR-C5, A-GATES].

## 🔨 Tasks
- [ ] RED: `TestDriftGatesCatchDivergence` over synthetic diverging fixtures — fails until the gates exist.
- [ ] GREEN: author `gates/drift/` — openapi-diff, emitted-event validation, fixture reproduction, SSE conformance (OQ-C4 tooling: openapi-diff + JSON-Schema validation + golden replay, no Pact broker).
- [ ] GREEN: wire report-only into engine + knowledge CI at the pinned tag.
- [ ] RED: negative paths for each of a/b/c/d.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; DEFINITIONS + reference tooling here, EXECUTION in consumer/server CI (A-GATES). Seam crossed: every seam this repo pins. Sibling deps: consumes E2 (OpenAPI to diff), E3-S4 (event schemas), E3-S5 (WIRE.md conformance harness), E4-S3 (fixtures); OQ-C4 gates the harness tech.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — gates/drift/gates.yaml DECLARES the four drift gates and drift_report.py runs report-mode, but three of four are x-status:draft with NO reference tooling and there is no TestDriftGatesCatchDivergence on synthetic diverging fixtures (only dfm-parity is wired).

## 🧪 Testing
Named DoD test `TestDriftGatesCatchDivergence` (gate-definition layer, synthetic fixtures). CS §5 mocking: fake serving impls + emitters as golden fixtures; never mock own contract packages. Deterministic, offline (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §5 (gates), §11 (honesty — closes the FR-31 harness definition home), §12; SE-Arch 8sYi523i4t reliability lens (deterministic, false-positive tracked — a gate that cries wolf gets disabled); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C16, NFR-C5, OQ-C4, D-CON-7; Architecture (o2waDNw3ix) A-GATES
- engine FR-31 (contract harness)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E6. Depends on E2, E3-S4, E3-S5, E4-S3.
- [ ] Gated: OQ-C4 (harness technology assumption).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if a gate proves non-deterministic (must be fixed or it gets disabled — NFR-C5).
