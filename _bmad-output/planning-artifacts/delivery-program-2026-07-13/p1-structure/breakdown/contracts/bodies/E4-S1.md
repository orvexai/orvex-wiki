## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As orvex-studio-lib/auth and every satellite verifier, I want the shared identity JSON Schemas authored under `identity/` — the IdP-agnostic principal `{idp,subject,tenant,org_or_realm,roles,token_scope}` (idp ∈ clerk|keycloak), the token-scope schema (space-scoped C5 semantics, deny-by-default, absent/invalid ⇒ no access), the role enum (coarse workspace roles + the space-role set enumerated from the engine's shipped CASL roles), and `token-claims.schema.json` (per-IdP `iss`, accepted `aud` values, frozen-at-mint custom-claim layout `{tenant,clerk_org,subject,scopes,exp}`) — plus synthetic conformance vectors — so that lib's wrong-audience and frozen-claim test vectors have a contract to verify against and there is zero per-satellite drift.

**Definition of Done:** ONE named test `TestTokenClaimsVectorsConform` — a CI-layer test asserting, through `validate.yaml`, that the synthetic conformance vectors validate against `token-claims.schema.json`, that a wrong-`aud` vector fails, and that all vectors are synthetic (no recorded real token markers). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the principal schema, When authored, Then it is `{idp,subject,tenant,org_or_realm,roles,token_scope}` with idp ∈ {clerk,keycloak}. *Assert: principal schema shape matches* [Source: FR-C13].
- [ ] **AC2** — Given the token-scope schema, When a scoped token requests outside its spaces (negative path), Then it can never read outside (deny-by-default; absent/invalid ⇒ no access). *Assert: C5 space-scope semantics pinned; deny-by-default* [Source: FR-C13].
- [ ] **AC3** — Given `token-claims.schema.json`, When authored, Then per-IdP `iss` expectations + accepted `aud` values + the frozen custom-claim layout `{tenant,clerk_org,subject,scopes,exp}` are pinned. *Assert: claim layout + iss/aud model present* [Source: FR-C13, identity A-TOKEN].
- [ ] **AC4** — Given a wrong-audience vector (V1) and a frozen-claim vector (V8), When validated, Then wrong-aud fails and frozen-claim passes. *Assert: lib V1 fails, V8 passes* [Source: FR-C13, lib FR-L7].
- [ ] **AC5** — Given the role enum, When enumerated from shipped CASL roles, Then no product change is smuggled in (OQ-C7). *Assert: role set == shipped CASL set at authoring time* [Source: FR-C13, OQ-C7].
- [ ] **AC6** — Given the vectors, When authored, Then they are synthetic example claims/scopes — never recorded real tokens. *Assert: no recorded-token markers (A-AUTHOR guard green)* [Source: FR-C13, NFR-C1].

## 🔨 Tasks
- [ ] RED: `TestTokenClaimsVectorsConform` — fails until schemas + vectors exist.
- [ ] GREEN: author principal/token-scope/roles/token-claims schemas.
- [ ] GREEN: author synthetic conformance vectors (incl. V1 wrong-aud, V8 frozen-claim).
- [ ] RED: negative paths — wrong-aud fails, scoped-token-outside-spaces denied.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; identity implements these, lib/auth enforces them, every satellite OpenAPI `$ref`s them for auth. Seam crossed: identity↔lib↔every satellite verifier. Sibling deps: blocks E1-S6 (bearer scheme `$ref`s these) + all E2 bearer surfaces + E2-S3 (identity implements them).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — identity/{principal,token-scope,roles,token-claims}.schema.json + conformance vectors are authored and gated by tests/test_authtest_corpus_contract.py::TestAuthConformanceTokenCorpusContract (wrong-aud fails, synthetic-not-recorded) + test_identity_schema_contract.py.

## 🧪 Testing
Named DoD test `TestTokenClaimsVectorsConform` (CI/validation layer). CS §5 mocking: none — real JSON-Schema validation of synthetic vectors; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams — one auth shape), §10 (zero-trust — deny-by-default token scope), §12; SE-Arch 8sYi523i4t security lens (frozen claims, wrong-aud); cell-lint JGAUQRsw2g. §6 tiers N/A. NEVER use recorded real tokens (A-AUTHOR / NFR-C1).

## 🔗 References
- PRD (jwF4VLHfNs) FR-C13, OQ-C7, NFR-C1; Architecture (o2waDNw3ix) A-OPENAPI, A-LAYOUT (`identity/`)
- identity A-TOKEN; lib FR-L7 (V1/V8 vectors)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E4. Blocks E1-S6 + all E2 bearer surfaces + E2-S3.
- [ ] Gated: OQ-C7 (role enum from shipped CASL).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-C7 (confirm no product change smuggled into the role enum).
