**Feature area.** The gate definitions + reference tooling that run in *other* repos' CI pinned to a contracts release tag: the family AGPL-import guard (day-one licence police), the FR-30 divergence gate (upstream-fork allow-list), the contract-drift gates (served-OpenAPI diff, emitted-event validation, fixture reproduction, SSE conformance), the engine-side FR-31 harness definitions (api-key behavioural-parity + crash-injection/replay-completeness), and the DfM parity gate. *"Definitions here, execution there, pinned always"* (A-GATES).

**Why it exists.** *"Drift is impossible to miss"* (G3) is the whole point of a pinned seam — a gate that fails a build the moment either side stops matching the pin. Today `gates/agpl-import/` is an empty README (the family's *"day-one licence police"*) and no drift gates exist. Big-bang integration risk is maximal (risk #4); these gates are the antibody. The AGPL guard enforces from day one (NFR-C7); the divergence gate ships report-only (red-day-one reality) and flips at v1.0.

**Story roster.**
- [ ] E6-S1 — AGPL-import guard config (gates/agpl-import/)
- [ ] E6-S2 — FR-30 divergence-gate config (gates/allowlist/baseline/weighting)
- [ ] E6-S3 — Contract-drift gates: served-OpenAPI diff + emitted-event + fixture reproduction + SSE conformance
- [ ] E6-S4 — Engine-side FR-31 harness defs: api-key parity + crash-injection/replay-completeness
- [ ] E6-S5 — DfM parity gate definition (Go pkg/dfm + engine TS, fixture-mediated)

**FR/NFR ids this epic owns:** FR-C21, FR-C15, FR-C16, FR-C20 (parity-gate execution), NFR-C7, NFR-C5.

**Definition of Done.** All five stories Done ∧ gate definitions are deterministic with no flaky network dependence (NFR-C5) and every consuming CI job references a **release tag**, never floating main ∧ AGPL guard enforces from day one (a hit is a licensing incident, never an allow-list entry) ∧ divergence gate ships report-only pending OQ-C2 ∧ family-E2E share: the drift gates wire report-only into engine + knowledge CI at the pinned tag; the DfM parity gate runs green in both lib CI (Go) and engine CI (TS) against one corpus ∧ SDD lines evidenced.

## 🔗 Dependencies
- [ ] **Blocked by:** ENG-2091 (Definition Pack — contract TAG is the dispatch gate).
- [ ] E6-S3 depends on E2 (OpenAPI surfaces to diff), E3 (event schemas + WIRE.md to validate), E4-S3 (fixtures to reproduce); E6-S5 depends on E4-S4 (DfM corpus + pkg/dfm signature); E6-S4 depends on E5-S1 (errorCode vocab for api-key parity).
- [ ] OQ-C2 (FR-30 allow-list content, entangled with stale ledger REM-4) gates E6-S2 enforce-mode; OQ-C4 (harness tech assumption) gates E6-S3 tooling.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any cross-repo CI-wiring change (gate execution lives in consumer repos, not here).
