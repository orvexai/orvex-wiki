## 🎯 Story
As a satellite build agent, I want the A-LAYOUT directory tree scaffolded with one home per contract kind and the README layout-table matching it, so that every published artifact path resolves and no consumer link is ever dead.

**Definition of Done:** ONE named test `TestLayoutMatchesReadme` — a CI-layer test asserting, through the repo's `validate.yaml` interface, that every directory named in the README layout table exists on disk and every on-disk contract directory appears in the README table (bidirectional set equality). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the A-LAYOUT tree (`openapi/`, `openapi/internal/`, `openapi/_components/`, `events/`, `sse/`, `sources/`, `identity/`, `billing/`, `errors/`, `fixtures/`, `fixtures/dfm/`, `gates/`, `gates/agpl-import/`, `gates/drift/`, `mcp/`, `obs/`), When the repo is checked out, Then every directory exists with a `.gitkeep` or seed file. *Assert: `set(dirs on disk) ⊇ A-LAYOUT set`* [Source: Architecture A-LAYOUT].
- [ ] **AC2** — Given the README layout table, When `TestLayoutMatchesReadme` runs, Then table rows and on-disk dirs are set-equal. *Assert: `symmetric_difference == ∅`* [Source: Architecture §4 "Update the README layout table to match"].
- [ ] **AC3** — Given a new directory added without a README row (negative path), When CI runs, Then the test fails. *Assert: exit code ≠ 0 with message naming the orphan dir* [Source: NFR-C6].
- [ ] **AC4** — Given the scaffold README's present-tense framing, When the tree lands, Then a `Status: scaffold` line + per-empty-dir `Status: empty` markers are present (honesty, SE-Arch T7). *Assert: README contains `Status: scaffold`* [Source: SE-Arch T7].

## 🔨 Tasks
- [ ] RED: write `TestLayoutMatchesReadme` asserting bidirectional dir/table equality; it fails on the current partial scaffold (AC2).
- [ ] GREEN: create the full A-LAYOUT tree with seed/`.gitkeep` files (AC1).
- [ ] GREEN: rewrite the README layout table to match + add honesty markers (AC2, AC4).
- [ ] RED: add an orphan-dir fixture case proving the negative path fails (AC3).

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo** (no runtime tiers; artifacts only). The NestJS-engine analogue does not apply. Seam crossed: none at runtime; this establishes the on-disk homes every later story writes into. Sibling dependency: blocks E1-S2 (validate.yaml scans these dirs).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — full A-LAYOUT tree + README layout table + a status marker exist (README.md), but there is NO TestLayoutMatchesReadme dir/table set-equality test or orphan-dir negative case; validate_contracts.py scans dirs without diffing them against the README table.

## 🧪 Testing
Named DoD test `TestLayoutMatchesReadme` (CI/validation layer, asserts through the `validate.yaml` runner). CS §5 mocking: none needed — pure filesystem + README parse; never mock own packages. Deterministic, no network (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0 (scope), §3.7 (design-it-twice on any contracts change), §11 (honesty — scaffold state stated, not papered), §10 (operability = the self-validation CI); SE-Arch 8sYi523i4t honesty lens (T7 present-tense framing); cell-lint JGAUQRsw2g (README/layout hygiene). §6 tiers N/A per the CS §6 non-service note.

## 🔗 References
- Architecture: orvex-studio-contracts (o2waDNw3ix) A-LAYOUT, §4 Deltas
- Architecture Audit — SE-Arch review (nngOgO0CGO) T5/T7

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E1.
- [ ] Blocks: E1-S2, E1-S6, and every artifact-authoring story (they write into these dirs).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
