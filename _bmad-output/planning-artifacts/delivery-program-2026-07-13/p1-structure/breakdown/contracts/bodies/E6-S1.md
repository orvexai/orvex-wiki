## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As every CLOSED repo in the family, I want the AGPL-import guard config authored under `gates/agpl-import/` — a shared denylist (`@docmost/*` and every engine workspace package incl. `@docmost/editor-ext`) plus license-scan rules catching any AGPL-licensed dependency, with the CI-wiring spec every closed repo runs pinned to a contracts tag — so that a closed repo's build fails the moment it imports, vendors, or links a denylisted package, enforcing from day one (a hit is a licensing incident to remove, never an allow-list entry).

**Definition of Done:** ONE named test `TestAgplGuardBlocksDenylistedImport` — a gate-definition test asserting, run against a synthetic closed-repo fixture, that importing `@docmost/editor-ext` (or any AGPL-licensed dep) fails the guard, that the engine repo is exempt, and that there is NO allow-list mechanism (a hit is always an incident). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the denylist, When authored, Then it lists `@docmost/*` + every engine workspace package (incl. `@docmost/editor-ext`, `private:true` → inherits AGPL) + AGPL-license-scan rules. *Assert: denylist complete* [Source: FR-C21, NFR-C7].
- [ ] **AC2** — Given a closed repo importing a denylisted package (negative path), When the guard runs, Then the build fails as a licensing incident. *Assert: exit ≠ 0; no allow-list entry option* [Source: FR-C21].
- [ ] **AC3** — Given the guard, When wired into a consumer CI, Then it is pinned to a contracts release tag, never floating main. *Assert: CI-wiring spec references a tag* [Source: FR-C21, A-GATES].
- [ ] **AC4** — Given the engine repo, When the guard would run, Then it is exempt (it IS the AGPL side). *Assert: engine exemption documented* [Source: FR-C21].
- [ ] **AC5** — Given enforcement mode, When compared to the divergence gate, Then this guard enforces from DAY ONE (unlike FR-30's report-only). *Assert: enforce-from-day-one stated* [Source: FR-C21].

## 🔨 Tasks
- [ ] RED: `TestAgplGuardBlocksDenylistedImport` against a synthetic closed-repo fixture — fails until the guard config exists.
- [ ] GREEN: author `gates/agpl-import/` denylist + license-scan rules + CI-wiring spec.
- [ ] GREEN: document the engine exemption + day-one enforcement + no-allow-list rule.
- [ ] RED: negative path — denylisted import fails.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the guard DEFINITION lives here, EXECUTION in every closed repo's CI (A-GATES). Seam crossed: the AGPL licensing boundary family-wide. Sibling deps: complements E1-S5 (this repo's own authoring guard); the config is consumed by every closed satellite's CI. No prerequisites — lands NOW (v0.1, closed repos wire it enforcing immediately).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — gates/agpl-import/{agpl-import-guard.sh,denylist.yaml} + tests/test_agpl_guard_contract.py::TestAgplImportGuardContract realize the DoD (go+ts violations exit1, engine-exempt, no-allowlist-mechanism); the guard is also wired live in validate.yaml.

## 🧪 Testing
Named DoD test `TestAgplGuardBlocksDenylistedImport` (gate-definition layer, run against a synthetic fixture). CS §5 mocking: fake a closed-repo dependency tree as a fixture; never mock own contract packages. Deterministic, no network (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §5 (gates), §9, §10 (zero-trust licensing); SE-Arch 8sYi523i4t security-licensing lens (family day-one licence police, T5); cell-lint JGAUQRsw2g. §6 tiers N/A. A hit is a licensing incident, not a style nit (Architecture §5).

## 🔗 References
- PRD (jwF4VLHfNs) FR-C21, NFR-C7; Architecture (o2waDNw3ix) A-GATES; SE-Arch review (nngOgO0CGO) T5

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E6. No contract prerequisites (lands NOW). Executed in every closed repo's CI.
- [ ] Related: E1-S5 (this repo's own provenance guard).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE any real denylist hit as a licensing incident.
