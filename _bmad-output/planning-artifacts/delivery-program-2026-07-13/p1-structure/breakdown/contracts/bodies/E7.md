**Feature area.** The two cross-cutting governance/convention artifacts: the family observability conventions appendix (service-name + shared OTel trace/metric attribute keys the console dashboards key on), and the change-authority governance wiring that operationalizes ratified ADR-0008 — the layered automated-merge (additive fast-track) vs ADR-gated-reshaping lanes, with the semver/drift-gate as the classifier and the NFR-C3 review SLA as the counter-metric control.

**Why it exists.** The obs conventions are the appendix lib's A-OBS promises *"become a contracts appendix"* (FR-C18) — descriptive conventions satellites conform to when naming telemetry, so console can key dashboards on one vocabulary (observability-architecture: troubleshooting UI = orvex-studio-console over LGTM). The change-authority lane is the resolution of *"contract change-authority is a live unresolved contradiction"* (risk #2, P0 #1): P3 no-ratify vs CS §9 ADR+ratify, settled by ADR-0008 as binding *different layers* — this story wires the classifier and the SLA so the chokepoint repo doesn't stall (NFR-C3).

**Story roster.**
- [ ] E7-S1 — obs/CONVENTIONS.md family service-name + OTel attribute conventions
- [ ] E7-S2 — Change-authority governance: ADR-0008 lane classifier + additive-fast-track review SLA

**FR/NFR ids this epic owns:** FR-C18, NFR-C3.

**Definition of Done.** Both stories Done ∧ `obs/CONVENTIONS.md` enumerates the shared attribute keys (tenant, source_id, contract tag) + service-naming rule + the label vocabulary console keys on ∧ the change-authority lane classifier is wired to FR-C17 semver (MAJOR → ADR lane, MINOR/PATCH → automated lane, ambiguity → ADR lane fail-safe) with the human ratifier named per OQ-C1 ∧ additive PRs carry a documented review-within-one-working-day SLA (NFR-C3) ∧ family-E2E share: a satellite naming its telemetry conforms to CONVENTIONS.md and a synthetic additive-vs-breaking PR pair routes to the correct lane ∧ SDD lines evidenced (ADR-0008 layered table applied).

## 🔗 Dependencies
- [ ] **Blocked by:** ENG-2091 (Definition Pack — contract TAG is the dispatch gate).
- [ ] E7-S2 depends on FR-C17 (E1-S4 semver policy — the classifier) and the drift gate (E6-S3, the MAJOR/breaking detector); OQ-C1 (repo owner/ratifier) must be named before the ADR lane is operable.
- [ ] E7-S1 conforms to the family canon obs contract (FR-C18 / lib A-OBS).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on the OQ-C1 human-ratifier naming (physically-human decision, ADR-0008 residual follow-up).
