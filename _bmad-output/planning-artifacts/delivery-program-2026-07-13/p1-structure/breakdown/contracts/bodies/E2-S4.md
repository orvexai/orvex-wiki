## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As the engine (keyword-upsert client) and orvex-studio-workflows (rebuild-activity client), I want the internal cross-seam OpenAPI tier authored under `openapi/internal/` — knowledge's `POST /internal/v1/keyword-upsert` and the rebuild step-APIs (`/internal/v1/rebuild/{namespace,walk,index-batch,delta-repull,alias-swap,retire}`) — all on the workload-identity scheme (mTLS/SPIFFE, bearer-rejecting), with the keyword-upsert `version` pinned as the SAME version space as the FR-C8 event version — so that knowledge's A5 watermark-bound check compares like with like.

**Definition of Done:** ONE named test `TestInternalTierRejectsBearer` — a CI-layer test asserting, through `validate.yaml` + the drift gate, that every `openapi/internal/` operation uses the workload-identity scheme (`x-rejects-bearer: true`), never bearer, and that keyword-upsert's `version` field references the same persisted-write-commit counter as FR-C8. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `POST /internal/v1/keyword-upsert`, When authored, Then it carries the payload, the ≤50 ms budget note, and version-bound semantics tying `version` to FR-C1's `ifVersion` counter. *Assert: op documents same version space* [Source: FR-C4, D-CON-5].
- [ ] **AC2** — Given the rebuild step-APIs, When authored, Then all six operations exist (namespace/walk/index-batch/delta-repull/alias-swap/retire), workflows' activities as client. *Assert: 6 rebuild ops present* [Source: FR-C4, D-WF-1].
- [ ] **AC3** — Given an internal-tier op presented a bearer token (negative path), When conformance runs, Then it is rejected outright. *Assert: `x-rejects-bearer: true` on every internal op* [Source: FR-C4, A-OPENAPI].
- [ ] **AC4** — Given internal-tier files, When public codegen runs, Then they are excluded from the public codegen set. *Assert: `x-tier: internal` marker + codegen exclusion documented* [Source: FR-C4, A-OPENAPI].

## 🔨 Tasks
- [ ] RED: `TestInternalTierRejectsBearer` — fails until internal tier exists.
- [ ] GREEN: author `openapi/internal/keyword-upsert.yaml` + `rebuild.yaml` on workload-identity.
- [ ] GREEN: pin the version-space equivalence note (keyword-upsert version == FR-C8 event version).
- [ ] RED: negative path — bearer-on-internal rejected; codegen-set exclusion.
- [ ] Add SEAMS.md rows.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is knowledge's internal Go handlers + workflows' Temporal activities. Seam crossed: engine↔knowledge (keyword-upsert), workflows↔knowledge (rebuild). Sibling deps: depends on E1-S6 (workload-identity scheme); the version-space tie couples to E3-S3 (FR-C8 obligations).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — openapi/internal/knowledge-internal.yaml is authored on the workload-identity scheme (common.yaml workloadIdentity x-rejects-bearer:true) and tests/test_knowledge_internal_rebuild_contract.py gives the rebuild step-APIs a golden round-trip; the isolated reject-bearer + version-space assertions are not named as such.

## 🧪 Testing
Named DoD test `TestInternalTierRejectsBearer` (CI/validation+drift layer). CS §5 mocking: fake the internal servers as golden fixtures; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §10 (zero-trust — workload identity only), §12; SE-Arch 8sYi523i4t seam + security lenses (mTLS/registry-coherence); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C4, D-CON-5; Architecture (o2waDNw3ix) A-OPENAPI
- knowledge A5 (watermark bound), workflows D-WF-1

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E2. Depends on E1-S6; couples to E3-S3 (version-space).
- [ ] Related: E2-S2 (knowledge public surface).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
