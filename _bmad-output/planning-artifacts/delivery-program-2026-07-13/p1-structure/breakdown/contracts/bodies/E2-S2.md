## 🎯 Story
As orvex-studio-knowledge (server) and its CLI/consumers, I want `openapi/knowledge-query.yaml` + `openapi/knowledge-projection.yaml` authored — `POST /v1/query|retrieve|related|duplicates`, `POST /v1/attachments/search`, `GET /v1/projection/delta` (epoch-bearing cursor + tombstones), `POST /v1/bundles` + `GET /v1/bundles/:id` (owner-bound), `GET /events` + `GET /events/head`, `GET /llms.txt` — with post-ACL-only count semantics and the admin stream-ops at `x-status: draft` — so that knowledge conforms to a pinned public surface every isolated build compiles against.

**Definition of Done:** ONE named test `TestKnowledgeQueryConforms` — a CI-layer test asserting, through the served-OpenAPI-diff drift gate, that knowledge's exposed surface matches these two files, including the partial-result flag semantics (counts post-ACL only) and the projection-delta epoch cursor + tombstone shape. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `POST /v1/query`, When mode ∈ {keyword,semantic,hybrid} with facets + fields projection, Then partial-result counts are documented as post-ACL only. *Assert: op notes counts are post-ACL* [Source: FR-C2].
- [ ] **AC2** — Given `GET /v1/projection/delta`, When called with an epoch-bearing cursor, Then tombstone semantics are defined. *Assert: delta op defines cursor + tombstones* [Source: FR-C2].
- [ ] **AC3** — Given `GET /v1/bundles/:id` (negative path), When a non-owner requests, Then it is owner-bound (denied). *Assert: op documents owner-binding* [Source: FR-C2].
- [ ] **AC4** — Given `GET /events/head`, When the CLI daemon polls, Then it is pinned (CLI-consumed). *Assert: `/events/head` op present* [Source: FR-C2, FR-C10].
- [ ] **AC5** — Given knowledge's admin stream-ops (FR-K17), When no named external consumer exists, Then they carry `x-status: draft`. *Assert: admin ops marked draft* [Source: FR-C2].

## 🔨 Tasks
- [ ] RED: `TestKnowledgeQueryConforms` against a conforming stub — fails until files exist.
- [ ] GREEN: author `knowledge-query.yaml` (query/retrieve/related/duplicates/attachments-search/llms.txt).
- [ ] GREEN: author `knowledge-projection.yaml` (projection delta + bundles + events + events/head).
- [ ] RED: negative paths — non-owner bundle access, ACL-count semantics.
- [ ] Add `SEAMS.md` rows; reference `_components` bearer scheme.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; knowledge's Go handlers conform to this. Seam crossed: knowledge↔CLI (events/head, projection delta), knowledge↔ai (retrieval under delegated principal). Sibling deps: depends on E1-S6, E5-S1; the SSE `GET /events` behavior is pinned by E3-S5 (WIRE.md).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — openapi/knowledge-query.yaml (query/retrieve/related/duplicates/attachments-search/bundles/events/llms.txt) + knowledge-projection.yaml are authored, but there is no TestKnowledgeQueryConforms and no post-ACL-count / projection-delta-cursor test.

## 🧪 Testing
Named DoD test `TestKnowledgeQueryConforms` (CI/drift-gate layer). CS §5 mocking: fake the knowledge server as a golden fixture (sibling faked); never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §9, §12; SE-Arch 8sYi523i4t seam lens; cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C2, FR-C10; Architecture (o2waDNw3ix) A-OPENAPI, §3
- knowledge PRD/arch (its `_bmad-output`) — declares this repo the author

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E2. Depends on E1-S6, E5-S1.
- [ ] Related: E3-S5 (SSE wire for `/events`), E2-S4 (internal keyword-upsert/rebuild).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ6 `content_pm` parity or SSE cursor scheme open items.
