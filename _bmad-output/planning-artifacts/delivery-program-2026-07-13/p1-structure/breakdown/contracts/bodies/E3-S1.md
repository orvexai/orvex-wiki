## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As every event publisher and consumer on the spine, I want `events/catalog.yaml` (CloudEvents 1.0) authored to full EVENTS.md-aligned semantics — the complete `wiki.*` set (page `created/updated/moved/deleted/restored/purged/content_updated/status_changed` with `content_updated` DISTINCT from metadata-only `updated`; comment; attachment `created/deleted` fixing the stale `attachment.updated`; space + membership; workspace; purge events), `clerk.*` (x-status draft, OQ-C6), identity lifecycle + push-revocation, and `billing.entitlement.changed`; each type carrying `type/subject/dataschema/profile/orderingkey/publisher/x-status` — with NO `linear.*` namespace — so that the spine has one machine-readable index.

**Definition of Done:** ONE named test `TestCatalogCorrectsStaleStubs` — a CI-layer test asserting, through `validate.yaml`, that `catalog.yaml` contains `wiki.attachment.created`/`.deleted` (not `.updated`), `wiki.page.content_updated` distinct from `.updated`, `billing.entitlement.changed`, and no `linear.*` type, with every type carrying `profile`, `orderingkey`, `publisher`, and a resolvable `dataschema`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the scaffold's stale `wiki.attachment.updated`, When the catalog lands, Then it is replaced by `wiki.attachment.created`/`.deleted`. *Assert: no `attachment.updated` type* [Source: FR-C6, Architecture §4].
- [ ] **AC2** — Given `page.updated` vs `page.content_updated`, When authored, Then `updated` is metadata-only and `content_updated` is distinct (fixing the wrong stub description). *Assert: two distinct types with correct descriptions* [Source: FR-C6].
- [ ] **AC3** — Given every type, When authored, Then it declares `profile` (thin|rich), `orderingkey` (page/space/tenant), `publisher` (a `sources/` identity), `x-status`, and a resolvable `dataschema`. *Assert: all fields present + dataschema resolves* [Source: FR-C6, A-CATALOG].
- [ ] **AC4** — Given `linear.*` (negative path), When anyone adds a `linear.*` type, Then the gate rejects it — Linear is removed, not reserved. *Assert: no `linear.*` namespace* [Source: FR-C6, D-S11].
- [ ] **AC5** — Given `clerk.*` types, When OQ-C6 (workflows PRD) is unresolved, Then they carry `x-status: draft`; `ai.*` is reserved (authored with ai deltas). *Assert: clerk.* draft; ai.* reserved not populated* [Source: FR-C6, OQ-C6].

## 🔨 Tasks
- [ ] RED: `TestCatalogCorrectsStaleStubs` — fails on the current 6-type stale stub.
- [ ] GREEN: author the full `wiki.*` set + `clerk.*` (draft) + identity lifecycle + `billing.entitlement.changed`.
- [ ] GREEN: add `profile`/`orderingkey`/`publisher`/`x-status`/`dataschema` per type.
- [ ] RED: negative paths — no `linear.*`, no `attachment.updated`.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is the engine's outbox emit sites + the relay. Seam crossed: every publisher↔spine↔consumer. Sibling deps: `publisher` field references E4-S2 (source registry); obligations header is E3-S3; per-type payload schemas are E3-S4; profiles are E3-S2.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — events/catalog.yaml is authored to full EVENTS.md semantics (attachment.created/.deleted, content_updated distinct, billing.entitlement.changed, no linear.*) and scripts/validate_events_contract.py::TestWikiEventCatalogContract is a real wired binary gate.

## 🧪 Testing
Named DoD test `TestCatalogCorrectsStaleStubs` (CI/validation layer). CS §5 mocking: none — real catalog + schema resolution; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams — the event spine), §9, §12; SE-Arch 8sYi523i4t honesty lens (fixing the wrong stub, forward-authored-vs-unbuilt-relay risk); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C6, D-S11; Architecture (o2waDNw3ix) A-CATALOG, §4
- EVENTS.md (engine repo `_bmad-output/event-propagation/EVENTS.md`)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E3. Depends on E4-S2 (`publisher` identities); couples to E3-S2/S3/S4.
- [ ] Gated: OQ-C6 (`clerk.*` scope from workflows PRD) → draft until resolved.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-C6.
