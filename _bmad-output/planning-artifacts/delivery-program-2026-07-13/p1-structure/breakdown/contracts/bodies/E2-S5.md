## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As orvex-wiki-api (server) and MCP/Orvex-CLI (codegen clients), I want `openapi/wiki-api.yaml` authored — the verb grammar `search/get/save/edit/list` over `{resource_type, locator}` (no `ask` verb), the write-receipt `{url,id,version,persisted}` (block writes add `changedBlockIds` + applied/skipped disposition, `version` MUST be populated), the CAS `ifVersion` as a monotonic version/etag (never a wall-clock `updated_at`), the spec-gate/ratify token transport, comment-CRUD + attachments-binary resources, and `QUOTA_EXCEEDED` propagated unchanged — with the flat public host `wiki-api.orvex.dev` and per-cell origin marked `x-origin:true`/`x-tier:internal` (T2) — so that the CLI's verified-write and three-baseline CAS gates have a like-with-like contract.

**Definition of Done:** ONE named test `TestWikiApiReceiptAndCas` — a CI-layer test asserting, through the drift gate, that every write op returns the `{url,id,version,persisted}` receipt with `version` populated, that `ifVersion` is typed as a monotonic version/etag (not a timestamp), and that no `ask` verb exists on this surface. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a page or block write, When it succeeds, Then the receipt is `{url,id,version,persisted}` with `version` populated (block writes add `changedBlockIds` + disposition). *Assert: receipt schema requires `version`; the shipped `{block_id,status}` gap is closed* [Source: FR-C19].
- [ ] **AC2** — Given `ifVersion`, When authored, Then it is a monotonic version/etag so the 409 `VERSION_MISMATCH` compares like with like — NOT a wall-clock `updated_at`. *Assert: ifVersion typed as version/etag, not timestamp* [Source: FR-C19, CLI-plan G3/G4].
- [ ] **AC3** — Given the verb grammar (negative path), When someone adds an `ask` verb, Then review/gate rejects it — cited-ask is ai's surface. *Assert: no `ask` path in wiki-api.yaml* [Source: FR-C19, D-ASK, SE-Arch T3].
- [ ] **AC4** — Given `QUOTA_EXCEEDED` from the engine, When propagated through wiki-api, Then it is unchanged, never cached, never partial-written-around (batch atomicity ⇒ whole patch fails), carrying the upgrade deep-link. *Assert: 402 passthrough documented; batch atomicity note present* [Source: FR-C19, CLI-plan G21].
- [ ] **AC5** — Given the spec-gate/ratify transport, When authored, Then `/spec-gate/check {satisfied,token,reason}` + `ratify-token`/`confirm-token`/`ratify-gate` + codes `RATIFY_FORCE_NOT_ALLOWED`/`GATE_UNSATISFIED`/`FORCE_TOKEN_REQUIRED` are pinned (CLI transports verbatim). *Assert: spec-gate ops + RATIFY_* codes present* [Source: FR-C19, CLI-plan G5].
- [ ] **AC6** — Given the servers block, When authored, Then it expresses the flat public host `wiki-api.orvex.dev`(dev)/`.ai`(prod) and the per-cell `origin.{cell}` marked `x-origin:true`/`x-tier:internal`. *Assert: no bare cell literal as public server (T6); flat host present* [Source: SE-Arch T2/T6].

## 🔨 Tasks
- [ ] RED: `TestWikiApiReceiptAndCas` — fails until wiki-api.yaml pins receipt + CAS.
- [ ] GREEN: author verb grammar, receipts, block-op/DfM write path, comment + attachment resources.
- [ ] GREEN: pin CAS `ifVersion` as version/etag; spec-gate/ratify token transport; QUOTA passthrough.
- [ ] GREEN: T2 host topology (flat host + `x-origin` cell); mark Phase-0 proxy `ask`/`related`/`search` paths `x-deprecated:true`/`x-superseded-by: ai.yaml` (T3).
- [ ] Add SEAMS.md row (NFR-C4 blocks the wiki-api cutover otherwise).

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is orvex-wiki-api's Go composition handlers. Seam crossed: wiki-api↔MCP/CLI (codegen), wiki-api↔ai (inline-edit writes), wiki-api↔engine (block chokepoint). Sibling deps: depends on E1-S6, E4-S4 (DfM shape via `pkg/dfm`), E5-S1 (error + RATIFY_* codes); the K5/ask single-home is resolved with E2-S6.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — openapi/wiki-api.yaml realizes the search/get/save/edit/list verb grammar (no 'ask' verb) with schemas/write-receipt.schema.json and tests/test_wiki_api_verb_grammar_contract.py::TestWikiApiVerbGrammarCoverage.

## 🧪 Testing
Named DoD test `TestWikiApiReceiptAndCas` (CI/drift-gate layer). CS §5 mocking: fake the wiki-api server as a golden fixture; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams — T2 host topology, T3 ai vs wiki-api ownership), §9, §12; SE-Arch 8sYi523i4t seam lens (T2/T3); cell-lint JGAUQRsw2g (servers-block scan, T6). §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C19, FR-C23; Architecture (o2waDNw3ix) A-OPENAPI; SE-Arch review (nngOgO0CGO) T2/T3/T6
- CLI-plan G3/G4/G5/G15/G16/G21 (via wiki-api arch)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E2. Depends on E1-S6, E4-S4, E5-S1; co-resolves single-home with E2-S6.
- [ ] Gated: OPEN DECISION #2 (Worker vs Traefik edge) — residual, does not block the pinned shape; OPEN DECISION #3 (proxy retire vs coexist).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OPEN DECISION #2/#3.
