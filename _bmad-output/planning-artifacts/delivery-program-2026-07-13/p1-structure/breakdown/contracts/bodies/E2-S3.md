## 🎯 Story
As orvex-studio-identity (server) and every Go satellite verifier, I want `openapi/identity.yaml` authored — `POST /v1/introspect`, `POST /v1/tokens` + `GET/DELETE /v1/tokens`, `POST /v1/exchange` (FR-15 mint, format + TTL pinned), the studio-side provision/deprovision (`{scope,id,mode}`, scope `org|user`, mode `disable|delete`, idempotent), and the issuer-registry / JWKS-mirror surface (issuer/tenant resolution, JWKS mirror, forced-refresh/invalidation) at `x-status: draft` gated on lib OQ-L1 — so that every verifier consults one pinned identity seam.

**Definition of Done:** ONE named test `TestIdentitySurfaceConforms` — a CI-layer test asserting, through the served-OpenAPI-diff drift gate, that identity's exposed surface matches `identity.yaml`, that the exchange token format+TTL are pinned (the engine consumes them), and that the JWKS-mirror ops carry `x-status: draft` yet appear in `SEAMS.md`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `POST /v1/exchange`, When it mints an exchange token, Then format + TTL are pinned (engine-consumed). *Assert: exchange op pins token format + TTL* [Source: FR-C3, FR-15].
- [ ] **AC2** — Given the studio provision/deprovision op, When authored, Then it is `{scope,id}` scope `org|user` mode `disable|delete`, idempotent, uniform responses — and explicitly documents it is DISTINCT from the engine `{clerkOrgId,…}`/`deactivate|delete` shape. *Assert: two-shape cross-ref note present* [Source: FR-C3, identity FR-I14].
- [ ] **AC3** — Given the JWKS-mirror surface, When lib OQ-L1 (mirror-vs-direct) is unresolved, Then the ops ship `x-status: draft` yet the seam is in `SEAMS.md` day one. *Assert: JWKS ops marked draft + present in SEAMS.md* [Source: FR-C3, NFR-C4].
- [ ] **AC4** — Given the lifecycle mirror surface (negative path), When a bearer token is presented, Then it is rejected (workload-identity tier). *Assert: lifecycle mirror uses workload-identity scheme* [Source: FR-C3, A-OPENAPI].
- [ ] **AC5** — Given the forced-refresh/invalidation signal, When authored, Then identity's FR-I3 push-revocation broadcast has a pinned shape. *Assert: invalidation op present* [Source: FR-C3].

## 🔨 Tasks
- [ ] RED: `TestIdentitySurfaceConforms` — fails until identity.yaml exists.
- [ ] GREEN: author introspect/tokens/exchange/studio-provision/deprovision ops (bearer/workload-identity per tier).
- [ ] GREEN: author the issuer-registry/JWKS-mirror ops at `x-status: draft`; add SEAMS.md rows.
- [ ] RED: negative path — bearer rejected on the lifecycle mirror.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; identity's Go handlers conform. Seam crossed: identity↔every Go satellite verifier (JWKS), identity↔engine (exchange consume), identity↔workflows (lifecycle). Sibling deps: depends on E1-S6, E4-S1 (principal/token-claims schemas identity implements); OQ-C7 (role enum) + OQ-C8 (JWKS freeze owner) gate the draft slots.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — openapi/identity.yaml is authored incl. issuer-registry/JWKS-mirror (introspect/tokens/exchange/provision/deprovision/issuers/jwks), but there is no TestIdentitySurfaceConforms served-OpenAPI test (test_identity_schema_contract covers the identity/ JSON schemas, not this surface).

## 🧪 Testing
Named DoD test `TestIdentitySurfaceConforms` (CI/drift-gate layer). CS §5 mocking: fake identity server as golden fixture; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §9, §10 (zero-trust — workload-identity tier), §12; SE-Arch 8sYi523i4t seam + security lenses; cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C3, FR-C13; Architecture (o2waDNw3ix) A-OPENAPI; OQ-C7/C8
- identity PRD/arch (its `_bmad-output`)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E2. Depends on E1-S6, E4-S1.
- [ ] Gated: OQ-C8 (JWKS freeze owner — identity vs lib) → draft until resolved.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-C8 owner ruling.
