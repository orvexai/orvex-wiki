## 🎯 Story
As the engine's exporters and every satellite parser/chunker, I want the golden markdown/`text_repr` fixture pairs authored under `fixtures/` — covering the representative block types (tables, embeds drawio/excalidraw/mermaid, mentions, code blocks, callouts, task lists), each an AUTHORED reference document (never an engine-output snapshot), with authority the engine block-schema catalog (`GET /api/schemas/blocks` + `blocks_snapshot.json`, 21 embed types post-Linear) — so that the engine proves its exporters reproduce them byte-for-byte and satellites prove their parsers consume them, closing the jsonToText atom-drop class.

**Definition of Done:** ONE named test `TestGoldenFixturesAuthoredNotSnapshot` — a CI-layer test asserting, through `validate.yaml` + the A-AUTHOR provenance guard, that every `fixtures/markdown|text_repr/` pair is well-formed, paired by case name, carries no engine-output/generator markers, and covers the 21-embed catalog. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the fixture set, When authored, Then it covers tables, embeds (drawio/excalidraw/mermaid), mentions, code blocks, callouts, task lists — each a markdown/`text_repr` pair by case name. *Assert: block-type coverage complete; pairs match by name* [Source: FR-C14].
- [ ] **AC2** — Given each pair, When checked, Then it is an authored reference — no engine-output snapshot / generator marker. *Assert: A-AUTHOR provenance guard green* [Source: FR-C14, NFR-C1].
- [ ] **AC3** — Given the embed catalog, When covered, Then it is the 21-embed set post-Linear-removal (the 8 `linear_*` snapshot entries removed). *Assert: 21 embed types; no linear_* entry* [Source: FR-C14, D-S11].
- [ ] **AC4** — Given a new engine block type (negative path), When it ships without a fixture PR here first, Then the contract-first gate blocks it. *Assert: F7 gate requires fixture-first* [Source: FR-C14].

## 🔨 Tasks
- [ ] RED: `TestGoldenFixturesAuthoredNotSnapshot` — fails until pairs exist + pass provenance.
- [ ] GREEN: author the markdown/`text_repr` pairs across the block types (from the block-schema catalog).
- [ ] GREEN: wire the fixture-first contract rule into the F7 gate reference (E6-S3).
- [ ] RED: negative path — a new block type without a fixture fails.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is the engine exporter (FR-18/38) + satellite chunkers. Seam crossed: engine exporter↔knowledge parser (structural exclusion of atom-drop). Sibling deps: parity inputs are the engine block-schema catalog + the ~73-tool MCP surface (E5-S2); consumed by E6-S3 (fixture reproduction gate).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — only a single trivial fixtures/markdown/paragraph.md + fixtures/text_repr/paragraph.txt pair exists; the 21-embed golden coverage and the TestGoldenFixturesAuthoredNotSnapshot provenance/coverage gate are absent (scaffold placeholder, not the corpus).

## 🧪 Testing
Named DoD test `TestGoldenFixturesAuthoredNotSnapshot` (CI/validation layer). CS §5 mocking: none — real fixture parse + provenance scan; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §9, §11 (honesty — authored not snapshot); SE-Arch 8sYi523i4t security-licensing lens (A-AUTHOR); cell-lint JGAUQRsw2g. §6 tiers N/A. NEVER snapshot engine output — authored from the documented schema + catalog only.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C14, D-S11, NFR-C1; Architecture (o2waDNw3ix) A-AUTHOR, A-LAYOUT (`fixtures/`)
- knowledge FR-K8 (jsonToText atom-drop)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E4. Parity inputs: engine block-schema catalog + E5-S2 MCP surface.
- [ ] Consumed by: E6-S3 (fixture reproduction gate).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if a fixture cannot be authored without reading AGPL TS (licensing incident).
