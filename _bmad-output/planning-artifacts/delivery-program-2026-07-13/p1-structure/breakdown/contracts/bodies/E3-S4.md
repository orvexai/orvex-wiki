## 🎯 Story
As the emitted-event drift gate and every event consumer, I want one JSON Schema (2020-12) per CloudEvent type under `events/schemas/`, referenced via `dataschema` — with the T4 tightening applied: every content-bearing / `profile: thin` schema lists `version` in `required`, pinned (non-draft) schemas set `additionalProperties: false`, and the spine-side `wiki.page.updated`/`content_updated` schemas are per-page (single `pageId` + `version`, per D-CON-4) — so that a publisher omitting `version` fails the gate red rather than passing green.

**Definition of Done:** ONE named test `TestContentEventRequiresVersion` — a CI-layer test asserting, through `validate.yaml`, that `wiki.page.content_updated.json` (and every content-bearing schema) lists `version` in `required` and sets `additionalProperties: false`, and that a stub omitting `version` fails validation. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given each catalog type, When schemas land, Then there is exactly one JSON Schema per type under `events/schemas/`, referenced by the catalog's `dataschema`. *Assert: schema-per-type coverage == catalog; every dataschema resolves* [Source: FR-C9].
- [ ] **AC2** — Given a content-bearing / thin-profile schema, When authored, Then `version` is in `required`, typed as the persisted write-commit counter. *Assert: `version` ∈ required* [Source: FR-C9, SE-Arch T4].
- [ ] **AC3** — Given a pinned (non-`x-status:draft`) schema, When authored, Then `additionalProperties: false`. *Assert: additionalProperties false on pinned schemas* [Source: SE-Arch T4].
- [ ] **AC4** — Given the spine-side `page.updated`/`content_updated` schemas, When authored, Then they are per-page (single `pageId` + `version`), not the batch `pageIds[]` shape (which survives only on the legacy SSE wire). *Assert: per-page shape; no `pageIds[]` on spine schema* [Source: FR-C9, D-CON-4].
- [ ] **AC5** — Given the current stub requiring only `[pageId, spaceId]` with `additionalProperties:true` (negative path), When the gate runs, Then it fails. *Assert: the old stub shape fails the version-required check* [Source: SE-Arch T4].

## 🔨 Tasks
- [ ] RED: `TestContentEventRequiresVersion` over the current stub — fails (stub lacks `version`).
- [ ] GREEN: extend `events/schemas/` to one schema per type; add `version` required + `additionalProperties:false`.
- [ ] GREEN: author per-page spine schemas (D-CON-4).
- [ ] RED: negative path — the old stub shape fails.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; these schemas are the emitted-event drift-gate reference. Seam crossed: spine↔every consumer's schema validation. Sibling deps: depends on E3-S1 (catalog types) + E3-S3 (obligations); consumed by E6-S3 (emitted-event validation gate).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — ~90 per-type events/schemas/*.json exist and version is largely required, but wiki.page.content_updated.json still has additionalProperties:true (not false) and there is no TestContentEventRequiresVersion enforcing version-required + additionalProperties:false with a failing stub.

## 🧪 Testing
Named DoD test `TestContentEventRequiresVersion` (CI/validation layer). CS §5 mocking: none — real JSON-Schema validation; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §5 (gates), §12 (contract-shape law — the required `version`), §11 (honesty — the stub is provisional but the pinned contract is normative); SE-Arch 8sYi523i4t reliability lens (T4 fail-closed); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C9, FR-C8; Architecture (o2waDNw3ix) A-CATALOG, §4; SE-Arch review (nngOgO0CGO) T4

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E3. Depends on E3-S1, E3-S3; enforced by E1-S2 validate.yaml; consumed by E6-S3.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
