**Feature area.** The shared shapes and proof corpora: the IdP-agnostic principal / token-scope / role / token-claims JSON Schemas every satellite verifier consumes (with synthetic conformance vectors), the multi-publisher source-adapter registry, the golden markdown/`text_repr` fixture pairs the engine's exporters and satellites' parsers prove against, and the DfM↔ProseMirror round-trip parity corpus (plus the pinned `pkg/dfm` Go API surface and opaque-node handle shape) the clean-room Go serializer is proven against.

**Why it exists.** The auth shapes are *"one shape, zero per-satellite drift"* — without `identity/token-claims.schema.json` pinned, lib's wrong-audience and frozen-claim test vectors have no contract to verify against (FR-C13). The DfM corpus is the belt-and-braces clean-room boundary: equivalence between the engine's AGPL TS serializer and the closed Go `pkg/dfm` flows *through authored fixtures, never through shared code* (A-DFM) — the fixtures are how the family reuses engine-equivalent behavior without AGPL contamination (NFR-C7). The source registry makes multi-publisher a contract, not a code change (G5).

**Story roster.**
- [ ] E4-S1 — identity schemas: principal/token-scope/roles/token-claims + conformance vectors
- [ ] E4-S2 — source-adapter registry SCHEMA.yaml + wiki.yaml
- [ ] E4-S3 — golden markdown/text_repr fixture pairs
- [ ] E4-S4 — DfM round-trip fixtures + pkg/dfm Go API surface + opaque-node handle shape

**FR/NFR ids this epic owns:** FR-C13, FR-C12, FR-C14, FR-C20.

**Definition of Done.** All four stories Done ∧ every fixture pair is an **authored** reference (never an engine-output snapshot — A-AUTHOR provenance guard green) ∧ the DfM corpus covers the full 21-embed catalog post-Linear-removal incl. opaque/atom nodes, table-cell marks, both mention formats ∧ legacy `linear_*` blocks round-trip opaque-preserve (D-S11) ∧ conformance vectors are synthetic (never recorded real tokens) ∧ family-E2E share: lib `pkg/dfm` CI parity gate + engine TS-serializer check both prove against the same pinned corpus (E6-S5) ∧ SDD lines evidenced (clean-room path, no `@docmost/editor-ext` read).

## 🔗 Dependencies
- [ ] **Blocked by:** ENG-2091 (Definition Pack — contract TAG is the dispatch gate).
- [ ] E4-S1 blocks E1-S6 auth-scheme `$ref`s and every E2 bearer surface.
- [ ] E4-S4 pins the `pkg/dfm` Go API signature co-owned with orvex-studio-lib; parity inputs are the engine block-schema catalog (`GET /api/schemas/blocks`) + the ~73-tool MCP surface (FR-C5).
- [ ] OQ-C7 (role enum enumerated from shipped CASL) gates E4-S1 role schema freeze.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if any fixture cannot be authored without reading AGPL TS (licensing incident, not a style nit — A-DFM).
