**Feature area.** The event spine's pinned agreement: the CloudEvents 1.0 type catalog (`wiki.*` + `clerk.*` + `billing.entitlement.changed` + identity lifecycle), thin/rich envelope profiles with the type↔legacy-SSE-frame mapping, the normative publisher obligations (monotonic per-document `version`, batch fan-out to one CloudEvent per pageId), the per-type payload JSON Schemas, the full Orvex-CLI SSE wire contract (transcribed from the shipped controller + EVENTS.md, with the authority flip), and the recorded-risk register.

**Why it exists.** *"The event spine — the integration substrate — is barely on"* (risk #7): prod emits zero events, the outbox→Kafka relay is a launch prerequisite that does not exist yet, and *"the first drift-gate run against the built relay is the verification moment"* (A-CATALOG). The catalog is forward-authored from EVENTS.md live emit sites; a missing `version` field would undermine the entire event-CAS / knowledge-watermark design (evidence digest HIGH gap; T4). This epic pins the substrate before the relay is built.

**Story roster.**
- [ ] E3-S1 — CloudEvent type catalog (events/catalog.yaml)
- [ ] E3-S2 — Envelope profiles PROFILES.md + type↔frame mapping table
- [ ] E3-S3 — Publisher obligations: version-carry + batch fan-out rule
- [ ] E3-S4 — Event payload schemas (version required, additionalProperties:false)
- [ ] E3-S5 — SSE wire contract WIRE.md + EVENTS.md authority flip
- [ ] E3-S6 — SSE recorded-risk register RISKS.md

**FR/NFR ids this epic owns:** FR-C6, FR-C7, FR-C8, FR-C9, FR-C10, FR-C11.

**Definition of Done.** All six stories Done ∧ `validate.yaml`'s version-required check (T4) green: every `profile: thin` / content-bearing type carries `version` in `required` and pinned payload schemas set `additionalProperties: false` ∧ no `linear.*` namespace (D-S11) ∧ EVENTS.md authority flip is a single PR that pointers the docmost-repo copy ∧ family-E2E share: the SSE conformance replay harness (E6-S3) drives resume/gap/filter against WIRE.md and passes ∧ SDD lines evidenced (batch fan-out D-CON-4, version-provenance D-CON-5, scope-intersection filter D-CON-7 all pinned).

## 🔗 Dependencies
- [ ] **Blocked by:** ENG-2091 (Definition Pack — contract TAG is the dispatch gate).
- [ ] E4-S2 (source registry — `publisher` field references a `sources/` identity) blocks E3-S1; E3-S3 obligations header is referenced by E3-S1 catalog and E3-S4 schemas.
- [ ] Cross-service: OQ-C6 (`clerk.*` scope from workflows PRD) gates the `clerk.*` catalog entries (`x-status: draft` until then).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on the EVENTS.md authority-flip window (freeze docmost-side edits the moment the import PR opens — A-SSE authority-flip hazard).
