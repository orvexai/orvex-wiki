## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As orvex-studio-lib (Go `pkg/dfm`) and the orvex-wiki engine (AGPL TS serializer), I want the DfM parity gate DEFINITION authored (A-DFM) — the reference tooling that proves each independent serializer round-trips the `fixtures/dfm/` corpus (plus the engine's published block-schema catalog) byte-identical, run in lib CI (Go side) and engine CI (TS side), equivalence flowing through the fixtures and never through shared code — so that the two clean-room implementations are proven equivalent without either reading the other.

**Definition of Done:** ONE named test `TestParityGateFixtureMediated` — a gate-definition test asserting, run against the E4-S4 corpus, that the Go and TS parity runs both byte-match every pair (incl. opaque nodes + `linear_*` opaque-preserve), that both pin one contracts tag, and that the gate has no code path reading the other serializer's source. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the Go `pkg/dfm` round-trip, When run against the corpus + catalog in lib CI, Then every pair byte-matches. *Assert: Go parity byte-identical* [Source: FR-C20, A-DFM].
- [ ] **AC2** — Given the engine TS serializer, When run against the SAME pairs in engine CI, Then every pair byte-matches. *Assert: TS parity byte-identical* [Source: FR-C20, A-DFM].
- [ ] **AC3** — Given a serializer that reads the other's source (negative path), When the gate is defined, Then it is forbidden — equivalence flows through fixtures only (clean-room, D-CON-8). *Assert: no cross-read code path; corpus lives here not lib* [Source: FR-C20, NFR-C7, D-CON-8].
- [ ] **AC4** — Given a corpus mismatch on one side (negative path), When the gate runs, Then that side fails and the fix is a contract-revision, never regeneration. *Assert: mismatch fails; A-AUTHOR bars regeneration* [Source: FR-C16, A-DFM].
- [ ] **AC5** — Given both runs, When wired, Then both pin one contracts release tag. *Assert: tag-pinned both sides* [Source: A-GATES, NFR-C2].

## 🔨 Tasks
- [ ] RED: `TestParityGateFixtureMediated` — fails until the parity-gate definition + tooling exist.
- [ ] GREEN: author the parity-gate definition (Go lib-CI side + TS engine-CI side, fixture-mediated).
- [ ] GREEN: document the clean-room no-cross-read rule + contract-revision-not-regeneration rule.
- [ ] RED: negative paths — cross-read forbidden; corpus mismatch fails.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; DEFINITION + corpus here, EXECUTION in lib CI (Go) + engine CI (TS). The Go analogue is `orvex-studio-lib/pkg/dfm`; the TS analogue is the engine's AGPL serializer — two independent implementations. Seam crossed: engine TS↔Go DfM equivalence. Sibling deps: depends on E4-S4 (corpus + `pkg/dfm` signature); part of the FR-C16 drift-gate family.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — gates/drift/dfm-parity-check.sh is authored, runnable reference tooling proving the corpus-side half fixture-mediated (no cross-serializer code path); the gate is x-status:active/report-only, the TS leg proven by packages/dfm and the Go leg runs in lib CI.

## 🧪 Testing
Named DoD test `TestParityGateFixtureMediated` (gate-definition layer). CS §5 mocking: none — real byte-compare against the authored corpus; never mock own contract packages, never read the other serializer. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §5 (gates), §7 (seams), §9, §11; SE-Arch 8sYi523i4t security-licensing lens (clean-room, NFR-C7 — the fast path (porting) is the blocked path); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C20, FR-C16, D-CON-8, NFR-C7; Architecture (o2waDNw3ix) A-DFM, A-GATES

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E6. Depends on E4-S4 (corpus + signature). Executed in lib CI + engine CI.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any cross-read or AGPL-porting temptation (licensing incident).
