## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As the clean-room Go serializer (`orvex-studio-lib/pkg/dfm`, shared by wiki-api + Orvex CLI) and the engine's AGPL TS serializer, I want the DfM↔ProseMirror round-trip parity corpus authored under `fixtures/dfm/` — authored PM-JSON ↔ DfM pairs across the 21-embed catalog including opaque/atom nodes (drawio/excalidraw/mermaid/embeds must round-trip byte-identical), table-cell marks, and both mention formats (`[[slugId]]`/`@label`) — plus the pinned `pkg/dfm` Go API surface (the exact exported function set) and the opaque-node handle wire shape `{block_id,type,summary}` + `ifVersion`-base reattach rule; with legacy `linear_*` blocks round-tripping opaque-preserve — so equivalence flows through fixtures, never shared code.

**Definition of Done:** ONE named test `TestDfmRoundTripByteIdentical` — a CI-layer test asserting, through `validate.yaml` + the parity-gate reference (E6-S5), that every `fixtures/dfm/` pair round-trips byte-identical including opaque nodes and `linear_*` opaque-preserve, and that the pinned `pkg/dfm` exported-function set is documented as a single signature both consumers bind. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the corpus, When authored, Then it covers the 21-embed catalog, opaque/atom nodes, table-cell marks, and both mention formats. *Assert: coverage complete; pairs authored (not snapshot)* [Source: FR-C20].
- [ ] **AC2** — Given an opaque/atom node (drawio/excalidraw/mermaid/embed), When round-tripped, Then it is byte-identical. *Assert: byte-equality on opaque nodes* [Source: FR-C20].
- [ ] **AC3** — Given a legacy `linear_*` block (negative path), When round-tripped, Then it survives as opaque-preserve — never a hard failure, never a silent drop. *Assert: linear_* opaque-preserve; no drop* [Source: FR-C20, D-S11].
- [ ] **AC4** — Given `pkg/dfm`, When authored, Then the exact exported function set + the opaque-node handle wire shape `{block_id,type,summary}` + the `ifVersion`-base reattach rule are pinned (both wiki-api and CLI bind one signature). *Assert: Go API surface + handle shape pinned* [Source: FR-C20, D-CON-10].
- [ ] **AC5** — Given a new engine block type, When it ships without a fixture-pair PR here first, Then either serializer is blocked. *Assert: fixture-first rule enforced* [Source: FR-C20].
- [ ] **AC6** — Given every pair, When checked, Then it is authored from the documented PM schema + catalog — never derived by reading AGPL TS. *Assert: A-AUTHOR provenance guard green* [Source: FR-C20, NFR-C7].

## 🔨 Tasks
- [ ] RED: `TestDfmRoundTripByteIdentical` — fails until the corpus + pkg/dfm signature exist.
- [ ] GREEN: author the PM-JSON↔DfM pairs (21-embed, opaque nodes, table-cell marks, mentions).
- [ ] GREEN: pin the `pkg/dfm` Go API surface + opaque-node handle wire shape + reattach rule.
- [ ] RED: negative paths — `linear_*` opaque-preserve, new-block-type-without-fixture blocked.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the Go analogue is `orvex-studio-lib/pkg/dfm` (co-owned signature) and the engine's AGPL TS serializer — two independent implementations that must never read each other (clean-room). Seam crossed: wiki-api↔CLI (shared serializer), engine TS↔Go equivalence. Sibling deps: parity inputs = engine block-schema catalog + E5-S2 MCP surface; the parity GATE is E6-S5; feeds E2-S5 (wiki-api DfM shape).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** present — fixtures/dfm/ carries the full 21-embed catalog + nodes/marks/mentions/opaque + manifest.json, and packages/dfm (TS reference serializer) proves byte-identical round-trip via __tests__/golden-corpus.spec.ts (coverage gate) + dfm.spec.ts.

## 🧪 Testing
Named DoD test `TestDfmRoundTripByteIdentical` (CI/validation + parity-gate reference layer). CS §5 mocking: none — real round-trip byte-compare; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §9, §11; SE-Arch 8sYi523i4t security-licensing lens (clean-room, NFR-C7 — porting `@docmost/editor-ext` is the blocked fast path); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C20, D-CON-8/D-CON-10, NFR-C7; Architecture (o2waDNw3ix) A-DFM
- CLI-plan G9/G10/G11; engine `GET /api/schemas/blocks`

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E4. Parity inputs: engine catalog + E5-S2.
- [ ] Feeds: E2-S5 (wiki-api DfM shape), E6-S5 (parity gate).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any AGPL-porting temptation (licensing incident, A-DFM).
