## 🎯 Story
As orvex-studio-knowledge and any future content publisher, I want the source-adapter registry authored under `sources/` — `SCHEMA.yaml` (registry-entry schema: `{source_id, event_types[] (catalog refs + version semantics), content_resolver (OpenAPI ref), acl_primitive (OpenAPI ref), purge_events[]}`) with fail-closed written into the schema, plus `wiki.yaml` (docmost: FR-18/38 resolver, FR-13 ACL, FR-37 purges) as the first entry and template — so that adding a publisher is one registry entry + a consumer-side client, zero pipeline changes.

**Definition of Done:** ONE named test `TestSourceRegistryFailClosed` — a CI-layer test asserting, through `validate.yaml`, that a registry entry with no `acl_primitive` (or an unreachable one) is rejected, that `wiki.yaml`'s resolver/ACL/purge refs resolve, and that a permissive/no-op ACL primitive is flagged. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `SCHEMA.yaml`, When authored, Then a registry entry declares `{source_id, event_types[], content_resolver, acl_primitive, purge_events[]}`. *Assert: schema shape matches* [Source: FR-C12, A-SOURCES].
- [ ] **AC2** — Given a source with no registered ACL primitive or an unreachable one (negative path), When a consumer would serve its documents, Then fail-closed applies — it MUST NOT serve. *Assert: fail-closed normative text in the schema* [Source: FR-C12, A-SOURCES].
- [ ] **AC3** — Given `wiki.yaml`, When authored, Then it references the FR-18/38 content resolver, the FR-13 ACL primitive, and the FR-37 purge events — all resolving. *Assert: wiki.yaml refs resolve* [Source: FR-C12].
- [ ] **AC4** — Given a permissive/no-op ACL primitive (negative path), When reviewed, Then it is rejected. *Assert: no-op ACL primitive flagged by the schema/review rule* [Source: FR-C12].
- [ ] **AC5** — Given the registry identity, When used, Then it is what the spine topic-authorization and knowledge's `source_type` stamping key on (one identity, three enforcement points). *Assert: registry identity documented as shared key* [Source: FR-C12, A-SOURCES].

## 🔨 Tasks
- [ ] RED: `TestSourceRegistryFailClosed` — fails until SCHEMA.yaml + fail-closed exist.
- [ ] GREEN: author `sources/SCHEMA.yaml` (entry schema + fail-closed normative text).
- [ ] GREEN: author `sources/wiki.yaml` as the first entry/template.
- [ ] RED: negative paths — missing/unreachable/no-op ACL primitive rejected.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is knowledge's source-adapter loader. Seam crossed: every publisher↔the spine↔knowledge. Sibling deps: the `publisher` field in E3-S1's catalog references these `source_id`s; blocks E3-S1.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — sources/SCHEMA.yaml makes acl_primitive required (fail-closed by construction) and sources/wiki.yaml is the first entry, but there is no TestSourceRegistryFailClosed asserting a missing/unreachable/permissive ACL primitive is rejected/flagged.

## 🧪 Testing
Named DoD test `TestSourceRegistryFailClosed` (CI/validation layer). CS §5 mocking: none — real schema + `$ref` resolution; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams — multi-publisher mechanism), §10 (zero-trust — fail-closed), §12; SE-Arch 8sYi523i4t security lens (fail-closed, no-op ACL rejection); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C12, G5; Architecture (o2waDNw3ix) A-SOURCES
- knowledge A4 (conforms)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E4. Blocks E3-S1 (catalog `publisher` refs).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
