## 🎯 Story
As the orvex-wiki engine's CI, I want the remaining engine-side FR-31 harness definitions authored as executable specs (run engine-side) — the api-key behavioural-parity spec (frozen errorCode vocabulary conformance, FR-11) and the crash-injection + replay-completeness test proving the outbox loses no event across a mid-write crash (engine NFR-6) — so that the two engine-only halves of the FR-31 contract harness have a definition home here and run against the serving implementation.

**Definition of Done:** ONE named test `TestOutboxNoLossOnCrash` — a harness-definition test asserting, run against a crash-injection fixture, that every event written pre-crash is replayed post-recovery with no loss and no duplication (exactly-once via `applied_events`), and that the api-key parity spec rejects any error not in the frozen vocabulary. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the api-key behavioural-parity spec, When the engine api-key validation errors, Then every error code is in the frozen FR-C23 vocabulary (FR-11 parity). *Assert: emitted codes ⊆ errors/vocabulary.yaml* [Source: FR-C16(e), FR-C23].
- [ ] **AC2** — Given a mid-write crash (crash-injection), When the engine recovers, Then every pre-crash outbox event is replayed. *Assert: replay-completeness — no event lost* [Source: FR-C16(e), engine NFR-6].
- [ ] **AC3** — Given replay, When a frame was already applied (negative path), Then it no-ops (exactly-once via `applied_events`). *Assert: no duplicate application on replay* [Source: FR-C16(e), FR-C10 §5.1.8].
- [ ] **AC4** — Given these specs, When authored, Then they are executable specs run ENGINE-SIDE (definition here, execution in engine CI). *Assert: engine-side execution documented; tag-pinned* [Source: FR-C16, A-GATES].

## 🔨 Tasks
- [ ] RED: `TestOutboxNoLossOnCrash` against a crash-injection fixture — fails until the harness spec exists.
- [ ] GREEN: author the api-key behavioural-parity spec (conforms to E5-S1 vocabulary).
- [ ] GREEN: author the crash-injection + replay-completeness spec (outbox no-loss + exactly-once).
- [ ] RED: negative paths — non-vocabulary error rejected; duplicate replay no-ops.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; DEFINITION here, EXECUTION in engine CI. Seam crossed: the engine outbox↔spine durability guarantee + the api-key error contract. Sibling deps: depends on E5-S1 (errorCode vocab) + E3-S3 (FR-C8 obligations the outbox satisfies); part of the FR-31 harness with E6-S2/E6-S3.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** absent — only two placeholder gate DECLARATIONS in gates/drift/gates.yaml (apikey-behavioural-parity, crash-injection-replay-completeness, x-status:draft, no reference_tooling); no TestOutboxNoLossOnCrash, no crash-injection fixture, no api-key parity spec.

## 🧪 Testing
Named DoD test `TestOutboxNoLossOnCrash` (harness-definition layer, crash-injection fixture). CS §5 mocking: fake the engine store + a crash point as a fixture; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §5 (gates), §10 (operability — durability), §11, §12; SE-Arch 8sYi523i4t reliability lens (outbox no-loss, exactly-once); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C16(e), FR-C23; Architecture (o2waDNw3ix) A-GATES
- engine FR-31 / NFR-6

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E6. Depends on E5-S1, E3-S3. Executed in engine CI.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
