## 🎯 Story
As a contract-PR author and the repo maintainer, I want the change-authority governance wired per ratified ADR-0008 — the layered scheme where the contract ARTIFACT merge (a schema/catalog/fixture edit) rides the automated path (CI drift-gate + AGPL import-guard + schema-validation, no human ratify, canon P3) while a breaking or seam-reshaping DESIGN change (CS §9 three-part test) is a mandatory ADR trigger with human doc-ratify (CS §9/§12) — with the FR-C17 semver/drift-gate as the classifier (MAJOR → ADR lane, MINOR/PATCH → automated lane, ambiguity → ADR lane fail-safe) and the NFR-C3 additive-fast-track review SLA — so that this chokepoint repo never stalls six+ repos and no breaking change slips the ADR gate.

**Definition of Done:** ONE named test `TestChangeLaneClassifier` — a CI-layer test asserting, through the classifier tooling, that a synthetic additive PR (new optional field / new draft type) routes to the automated lane and a synthetic breaking PR (field removal / required-set change / endpoint retire) routes to the ADR lane, with ambiguity resolving to the ADR lane. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given an additive edit (new optional field / new `x-status:draft` type / additive path), When classified, Then it rides the automated lane (CI-only, no human ratify, additive fast-track). *Assert: additive → automated lane* [Source: ADR-0008, NFR-C3].
- [ ] **AC2** — Given a breaking/seam-reshaping change (field remove/rename, required-set change, envelope required-attr change, error/exit semantics change, endpoint retire, multi-repo cutover), When classified, Then it is a mandatory ADR trigger with human doc-ratify before promotion + design-it-twice (§3.7). *Assert: breaking → ADR lane* [Source: ADR-0008, CS §9/§12].
- [ ] **AC3** — Given an ambiguous change (negative path), When classified, Then it resolves to the ADR lane (fail-safe toward review). *Assert: ambiguity → ADR lane* [Source: ADR-0008].
- [ ] **AC4** — Given an additive contract PR, When submitted, Then the NFR-C3 review SLA (within one working day) is the tracked control; a satellite blocked on a contract change is a process failure to track. *Assert: SLA documented + tracked* [Source: NFR-C3].
- [ ] **AC5** — Given the ADR lane, When operable, Then the human ratifier is named per OQ-C1 before the ADR lane can promote. *Assert: ratifier-named prerequisite documented* [Source: ADR-0008 residual, OQ-C1].

## 🔨 Tasks
- [ ] RED: `TestChangeLaneClassifier` over synthetic additive/breaking/ambiguous PRs — fails until the classifier exists.
- [ ] GREEN: wire the classifier to FR-C17 semver + the drift gate (MAJOR→ADR, MINOR/PATCH→automated, ambiguity→ADR).
- [ ] GREEN: document the NFR-C3 additive-fast-track SLA + the ADR-lane human-ratify requirement.
- [ ] RED: negative path — ambiguous change routes to ADR lane.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; this is governance wiring, not runtime. Seam crossed: the contract-change governance boundary (P3 automated vs CS §9 human-ratify — reconciled by ADR-0008 as binding different layers). Sibling deps: depends on E1-S4 (FR-C17 semver, the classifier) + E6-S3 (the drift gate, the MAJOR/breaking detector); OQ-C1 (repo owner/ratifier) is a physically-human residual follow-up.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — governance seeds exist (.github/pull_request_template.md versioning lane, CODEOWNERS, ADR-0008 precedent in DESIGN-NOTES.md), but the FR-C17 change-lane CLASSIFIER tooling and TestChangeLaneClassifier routing additive->automated / breaking->ADR are not implemented.

## 🧪 Testing
Named DoD test `TestChangeLaneClassifier` (CI/classifier layer). CS §5 mocking: none — real semver-diff + drift-gate classification over synthetic PR fixtures; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §3.7 (design-it-twice on every contracts change), §9 (ADR triggers — the classifier's ADR lane), §11, §12 (contract-shape law); SE-Arch 8sYi523i4t governance lens (T1 / ADR-0008 layered table); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) NFR-C3, FR-C17, OQ-C1; Architecture (o2waDNw3ix) Tightened revision T1 (ADR-0008)
- SE-Arch review (nngOgO0CGO) T1; ADR-0008 (orvexstudioarch)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E7. Depends on E1-S4 (semver classifier) + E6-S3 (drift gate).
- [ ] Gated: OQ-C1 (human ratifier named before the ADR lane promotes).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-C1 (repo owner/ratifier naming — physically-human decision).
