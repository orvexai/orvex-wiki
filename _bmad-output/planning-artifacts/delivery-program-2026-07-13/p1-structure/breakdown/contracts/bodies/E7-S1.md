## 🎯 Story
As orvex-studio-console dashboards and every satellite naming its telemetry, I want the observability conventions appendix authored at `obs/CONVENTIONS.md` — the family-wide service-naming rule, the shared trace/metric attribute keys (tenant, source_id, contract tag), and the label vocabulary console dashboards key on (the appendix lib's A-OBS promises "become a contracts appendix") — as descriptive conventions (not runtime config) satellites conform to when naming their telemetry.

**Definition of Done:** ONE named test `TestObsConventionsComplete` — a CI-layer test asserting, through `validate.yaml`, that `obs/CONVENTIONS.md` enumerates the service-naming rule + the required shared attribute keys (tenant, source_id, contract tag) + the console label vocabulary, and that a satellite telemetry-name fixture violating the naming rule is flagged. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `obs/CONVENTIONS.md`, When authored, Then it states the family service-naming rule + the shared trace/metric attribute keys (tenant, source_id, contract tag). *Assert: naming rule + attribute keys present* [Source: FR-C18].
- [ ] **AC2** — Given console dashboards, When they key on labels, Then the label vocabulary they use is enumerated here. *Assert: label vocabulary documented* [Source: FR-C18].
- [ ] **AC3** — Given a satellite naming telemetry, When it conforms, Then it uses these conventions (descriptive, not runtime config). *Assert: conventions marked descriptive; no runtime config coupling* [Source: FR-C18].
- [ ] **AC4** — Given a telemetry name violating the naming rule (negative path), When checked against a fixture, Then it is flagged. *Assert: violation flagged* [Source: FR-C18].

## 🔨 Tasks
- [ ] RED: `TestObsConventionsComplete` — fails until CONVENTIONS.md exists.
- [ ] GREEN: author `obs/CONVENTIONS.md` (service naming + attribute keys + console label vocabulary).
- [ ] RED: negative path — a violating telemetry-name fixture is flagged.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; descriptive conventions satellites conform to. Seam crossed: every satellite's telemetry↔console dashboards (over the LGTM stack; troubleshooting UI = orvex-studio-console). Sibling deps: fulfils lib A-OBS's promise; consumed by console (once that project lands) + every satellite.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — obs/CONVENTIONS.md is authored (service-naming table, shared attribute keys, console label vocabulary), but it is a DRAFT SKELETON with no TestObsConventionsComplete and no telemetry-name violation fixture.

## 🧪 Testing
Named DoD test `TestObsConventionsComplete` (CI/validation layer). CS §5 mocking: none — real markdown + fixture parse; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §10 (operability — observability conventions); SE-Arch 8sYi523i4t operability lens; cell-lint JGAUQRsw2g. §6 tiers N/A. Obs contract = FR-C18 (family obs architecture, message-tracing-across-outbox is the known gap).

## 🔗 References
- PRD (jwF4VLHfNs) FR-C18; Architecture (o2waDNw3ix) A-LAYOUT (`obs/`), §5
- lib A-OBS

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E7. Consumed by console + every satellite.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
