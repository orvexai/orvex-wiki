## 🎯 Story
As the orvex-wiki engine's CI, I want the FR-30 divergence-gate config authored under `gates/` — `allowlist.yaml` (the frozen ~11 irreducible upstream touch-points the fork may modify), `baseline.ref` (the upstream-baseline ref), `weighting.yaml` (the hunk-weighting rules), and the CI-wiring spec — running REPORT-ONLY first (an enforcing gate would red the build day one per the ledger) and flipping to enforce at v1.0 after OQ-C2 ratifies the allow-list — so that fork divergence from upstream Docmost is measured and bounded.

**Definition of Done:** ONE named test `TestDivergenceGateReportOnlyThenEnforce` — a gate-definition test asserting, run against a synthetic fork-diff fixture, that a touch-point outside the allow-list is reported (report-only mode, non-blocking) and that enforce-mode (post-OQ-C2) would block it. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `allowlist.yaml`, When authored, Then it lists the ~11 irreducible touch-points + `baseline.ref` + `weighting.yaml` hunk rules. *Assert: allow-list + baseline + weighting present* [Source: FR-C15].
- [ ] **AC2** — Given a modified file outside the allow-list, When report-only mode runs, Then it is reported but non-blocking. *Assert: reported, exit 0 in report-only* [Source: FR-C15].
- [ ] **AC3** — Given enforce mode (post-OQ-C2), When the same divergence occurs (negative path), Then the build fails. *Assert: enforce mode exits ≠ 0* [Source: FR-C15].
- [ ] **AC4** — Given the CI-wiring spec, When consumed by the engine CI, Then it is pinned to a contracts tag. *Assert: tag-pinned wiring* [Source: FR-C15, A-GATES].

## 🔨 Tasks
- [ ] RED: `TestDivergenceGateReportOnlyThenEnforce` against a synthetic fork-diff — fails until config exists.
- [ ] GREEN: author `allowlist.yaml` + `baseline.ref` + `weighting.yaml` + CI-wiring spec (report-only).
- [ ] GREEN: implement the report-only→enforce flip switch gated on OQ-C2.
- [ ] RED: negative path — out-of-allow-list divergence blocks in enforce mode.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the DEFINITION here, EXECUTION in engine CI only (A-GATES). Seam crossed: the fork↔upstream-Docmost divergence boundary. Sibling deps: OQ-C2 (allow-list content, entangled with the stale divergence ledger `jb65enpnNh`/REM-4) gates enforce mode; part of the engine FR-31 contract harness with E6-S3/E6-S4.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — gates/{allowlist,weighting}.yaml + baseline.ref + report-mode drift_report.py exist, but the allow-list is explicitly PLACEHOLDER (2 of ~11, awaiting OQ-C2) and there is no fork-diff fixture or TestDivergenceGateReportOnlyThenEnforce.

## 🧪 Testing
Named DoD test `TestDivergenceGateReportOnlyThenEnforce` (gate-definition layer, synthetic fixture). CS §5 mocking: fake a fork-diff fixture; never mock own contract packages. Deterministic, no network (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §5 (gates), §11 (honesty — red-day-one reality, report-only first); SE-Arch 8sYi523i4t reliability lens (gate that cries wolf gets disabled — counter-metric); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C15, OQ-C2; Architecture (o2waDNw3ix) A-GATES, §5
- engine FR-30; divergence ledger jb65enpnNh (stale, REM-4)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E6. Executed in engine CI. Enforce-mode gated on OQ-C2.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-C2 allow-list ratification (blocks enforce flip).
