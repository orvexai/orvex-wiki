## 🎯 Story
As the orvex-studio-mcp satellite planning its tool-parity cutover, I want the MCP REST-gap ledger authored at `mcp/rest-gap-ledger.yaml` — the audited map from the ~73 MCP tools (incl. the 42 raw Kysely uses) to engine/knowledge/ai primitives, every gap resolving to exactly one disposition: (a) covered by an existing FR-C1/FR-C2/FR-C22 operation (MCP's AI tools → FR-C22 ai ops), (b) a newly authored primitive added to FR-C1, or (c) explicitly retired with rationale — so that the MCP satellite's tool-parity cutover has a completeness gate.

**Definition of Done:** ONE named test `TestRestGapEveryToolDispositioned` — a CI-layer test asserting, through `validate.yaml`, that every one of the ~73 tools in the ledger resolves to exactly one disposition (a/b/c), that disposition (b) entries reference a real FR-C1 primitive, and that a tool left undispositioned fails the gate. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the ~73 MCP tools, When the ledger lands, Then each maps to exactly one disposition (a covered / b new-primitive / c retired). *Assert: 1:1 disposition coverage over the tool set* [Source: FR-C5].
- [ ] **AC2** — Given MCP's AI tools (ask/chat/inline/generate), When mapped, Then they resolve to FR-C22 ai operations. *Assert: AI tools → ai.yaml ops* [Source: FR-C5, D-ASK].
- [ ] **AC3** — Given a disposition (b) entry, When authored, Then it references a real newly-authored FR-C1 primitive. *Assert: (b) targets resolve to engine-primitives ops* [Source: FR-C5].
- [ ] **AC4** — Given a disposition (c) retirement, When authored, Then it carries a rationale. *Assert: (c) entries carry rationale text* [Source: FR-C5].
- [ ] **AC5** — Given an undispositioned tool (negative path), When the gate runs, Then it fails (blocks MCP tool-parity cutover). *Assert: exit ≠ 0 naming the tool* [Source: FR-C5, NFR-C4].

## 🔨 Tasks
- [ ] RED: `TestRestGapEveryToolDispositioned` — fails until the ledger is complete.
- [ ] GREEN: author `mcp/rest-gap-ledger.yaml` mapping the ~73 tools (incl. 42 Kysely) to dispositions.
- [ ] GREEN: feed disposition (b) primitives back to E2-S1 (engine-primitives).
- [ ] RED: negative path — an undispositioned tool fails the gate.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is the MCP gateway's tool→primitive routing. Seam crossed: MCP↔engine/knowledge/ai. Sibling deps: feeds E2-S1 (new primitives) + is a parity input to E4-S3/E4-S4 (the ~73-tool surface enumerates block types to round-trip); OQ-C5 gates whether the full 73-tool JSON is pinned here.

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — mcp/rest-gap-ledger.yaml has the disposition vocabulary + ~12 representative rows but is a DRAFT SKELETON (self-declares completion in v0.3) and has no TestRestGapEveryToolDispositioned. NOTE: origin/dev #38 completes FR-C5 to 73/73 dispositions — NOT on main.

## 🧪 Testing
Named DoD test `TestRestGapEveryToolDispositioned` (CI/validation layer). CS §5 mocking: none — real ledger + primitive-ref resolution; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §11 (honesty — every gap dispositioned, no vaporware), §12; SE-Arch 8sYi523i4t completeness lens (NFR-C4 cutover gate); cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C5, OQ-C5, D-ASK; Architecture (o2waDNw3ix) §5 OPEN DECISION #6
- mcp PRD/arch (its `_bmad-output`)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E5. Feeds E2-S1; parity input to E4-S3/E4-S4.
- [ ] Gated: OQ-C5 (full 73-tool JSON pinned here vs MCP-internal).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-C5 scope + any disposition (b) that reshapes an engine seam (ADR lane).
