## 🎯 Story
As a future envelope-tightening reviewer, I want `sse/RISKS.md` — the recorded-risk register naming the space-level (not page-level) frame-filter gap, the single-partition raw-topic throughput ceiling, and the rich-frame integrity dependency (publisher authorization is the first-line control, the FR-C7 consumer re-resolution the second, and which is load-bearing where) — so that a future change to these envelopes is deliberate, not accidental.

**Definition of Done:** ONE named test `TestRisksRegisterCoversNamedGaps` — a CI-layer test asserting, through `validate.yaml`, that `RISKS.md` contains the three named risks (space-level filter gap, 1-partition throughput ceiling, rich-frame integrity) each with a cross-reference to the artifact it constrains. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the space-level filter gap, When recorded, Then RISKS.md names it and cross-references the SSE wire filter (WIRE.md). *Assert: filter-gap entry present + cross-ref* [Source: FR-C11].
- [ ] **AC2** — Given the single-partition raw-topic throughput ceiling, When recorded, Then RISKS.md names it (knowledge A6's documentation obligation lands here). *Assert: throughput-ceiling entry present* [Source: FR-C11].
- [ ] **AC3** — Given rich-frame integrity, When recorded, Then RISKS.md states publisher authorization (first control) + FR-C7 consumer re-resolution (second) and which is load-bearing where. *Assert: two-control note present* [Source: FR-C11, D-CON-6].
- [ ] **AC4** — Given a named risk with no cross-reference (negative path), When the check runs, Then it fails. *Assert: exit ≠ 0 naming the dangling risk* [Source: FR-C11].

## 🔨 Tasks
- [ ] RED: `TestRisksRegisterCoversNamedGaps` — fails until RISKS.md exists.
- [ ] GREEN: author `sse/RISKS.md` (three risks + cross-references).
- [ ] RED: negative path — a risk without a cross-reference fails.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**. Seam crossed: documents the seam-envelope risks the SSE wire + profiles carry. Sibling deps: depends on E3-S2 (rich-frame integrity posture) + E3-S5 (WIRE.md filter); named-by knowledge PRD/arch as "recorded in contracts".

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — sse/RISKS.md covers all three named risks (space-level filter gap, single-partition throughput ceiling, rich-frame integrity) with cross-refs, but there is no TestRisksRegisterCoversNamedGaps test.

## 🧪 Testing
Named DoD test `TestRisksRegisterCoversNamedGaps` (CI/validation layer). CS §5 mocking: none — real markdown parse; never mock own contract packages. Deterministic (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §11 (honesty — risks recorded not papered); SE-Arch 8sYi523i4t honesty + reliability lenses; cell-lint JGAUQRsw2g. §6 tiers N/A.

## 🔗 References
- PRD (jwF4VLHfNs) FR-C11, D-CON-6; Architecture (o2waDNw3ix) A-SSE, A-PROFILES
- knowledge A6 (documentation obligations)

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E3. Depends on E3-S2, E3-S5.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
