## 🎯 Story
As the Orvex CLI (docmost-cli) SSE consumer, I want `sse/WIRE.md` + `sse/EVENTS.md` authored — the full wire contract transcribed from the shipped controller + EVENTS.md: `/events/head` cold-start bootstrap `{stream_head}`; cursor v1 (legacy Redis IDs) / v2 (`<topic-epoch>:<offset>`) monotonic AND epoch-ms-bearing (v1/unparseable/retention-expired ⇒ `stream.gap` ⇒ re-sync); legacy UNPREFIXED frame names; thin `content_updated {pageIds[]}` no-body; `permission.changed {affectedUserId,changeType∈{removed,role_changed,added}}` where `added` forces whole-workspace re-sync; 1 MiB max line + 512-frame queue; <45 s heartbeat + the full backoff constants; `applied_events` exactly-once dedup; the `page.status_changed` mapping decision; effective visibility = key-type filter ∩ C5 token scope; and a 7-day minimum resume window — with the EVENTS.md authority flip (docmost copy gains a pointer, stops being edited) — so that a gateway that fails to reproduce any term silently breaks CLI freshness.

**Definition of Done:** ONE named test `TestSseWireConformance` — the SSE conformance replay harness (definition co-owned with E6-S3) driving resume, `stream.gap`, and the key-type ∩ token-scope filter against a conforming serving stub, asserting the CLI's documented behaviors hold. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2091); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given cold start, When the client bootstraps, Then `GET /events/head` returns `{stream_head}` seeding `last_event_id`. *Assert: `/events/head` term pinned* [Source: FR-C10 §5.1.1].
- [ ] **AC2** — Given the cursor, When authored, Then v1/v2 are monotonic AND carry epoch-ms; v1/unparseable/retention-expired ⇒ `stream.gap` ⇒ re-sync, never an error. *Assert: monotonic + epoch-ms + gap-on-expiry pinned* [Source: FR-C10 §5.1.2, D-CON-7].
- [ ] **AC3** — Given the wire, When frames are emitted, Then they use legacy UNPREFIXED names (`page.created` not `wiki.page.created`); `content_updated` is thin `{pageIds[]}` no-body; `permission.changed added` forces whole-workspace re-sync. *Assert: unprefixed frames + thin content_updated + added-resync pinned* [Source: FR-C10 §5.1.3/4/5].
- [ ] **AC4** — Given bounds, When authored, Then 1 MiB max line, 512-frame queue (queue-full ⇒ reconnect-from-cursor), <45 s heartbeat, the backoff constants (401/403→5 min, 503/404→polling honouring Retry-After else 1 h, 429→Retry-After else 60 s, reconnect 1 s×2 cap 60 s, ±20 % jitter, 6 h reconcile), and `applied_events` exactly-once dedup. *Assert: all constants pinned verbatim* [Source: FR-C10 §5.1.6/7/8].
- [ ] **AC5** — Given effective visibility, When a space-scoped token connects (negative path), Then it never receives frames outside its spaces (key-type filter ∩ C5 token scope), aligning with knowledge FR-K12 — the key-type-only model is retired. *Assert: intersection filter pinned; 60 s rebuild applies to both inputs* [Source: FR-C10, D-CON-7].
- [ ] **AC6** — Given the authority flip, When WIRE.md/EVENTS.md land, Then it is a single PR that pointers the docmost-repo copy and freezes its edits. *Assert: authority-flip note + pointer present* [Source: FR-C10, A-SSE].

## 🔨 Tasks
- [ ] RED: `TestSseWireConformance` replay harness against a conforming stub — fails until WIRE.md pins the terms.
- [ ] GREEN: author `sse/EVENTS.md` (verbatim import) + `sse/WIRE.md` (controller-extracted behavior + §5.1 terms + cursor + resume floor).
- [ ] GREEN: execute the authority flip (pointer the docmost copy).
- [ ] RED: negative path — scoped token leak; retention-expired cursor error instead of gap.

## 🧠 Context
Expected tier placement: **none — CS §6 non-service repo**; the analogue is the engine SSE controller (`internal/daemon/sse.go`) + the CLI daemon. Seam crossed: SSE gateway↔CLI. Sibling deps: depends on E3-S2 (frame mapping is authority) + E4-S1 (C5 token-scope schema); feeds E3-S6 (RISKS.md) + E6-S3 (the conformance gate uses this harness).

**🧾 Code audit (origin/main @ cde2151, 2026-07-14):** partial — sse/WIRE.md + sse/EVENTS.md + EVENTS.md.placeholder (authority flip) are authored, but the TestSseWireConformance replay harness (resume/stream.gap/key-type-cap-token-scope filter) does not exist; the sse-conformance drift gate is x-status:draft with no reference tooling.

## 🧪 Testing
Named DoD test `TestSseWireConformance` (CI/conformance-replay layer). CS §5 mocking: fake the serving SSE implementation as a golden replay fixture; never mock own contract packages. Deterministic, no live network (NFR-C5).

## 📏 Guidance
CS 6aMAzsYeQb §0, §7 (seams), §9, §11 (honesty — transcription of shipped behavior), §12; SE-Arch 8sYi523i4t seam + reliability lenses (authority-flip hazard, D-CON-7 scope-intersection); cell-lint JGAUQRsw2g. §6 tiers N/A. Licensing basis for transcription: D-CON-3 (we hold copyright on the fork-added controller; relicense the spec text; machine-derived dumps stay forbidden).

## 🔗 References
- PRD (jwF4VLHfNs) FR-C10, D-CON-3/D-CON-7; Architecture (o2waDNw3ix) A-SSE
- CLI-plan §5.1 / G13; engine `internal/daemon/sse.go`, EVENTS.md

## 🔗 Dependencies
- [ ] Blocked by: ENG-2091 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Parent epic: E3. Depends on E3-S2, E4-S1; feeds E3-S6, E6-S3.
- [ ] Gated: `page.status_changed` mapping decision (D3 — keep-as-`page.updated` vs distinct frame).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-2091", never `closes`) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE at the authority-flip window (freeze docmost-side EVENTS.md edits the moment the import PR opens).
