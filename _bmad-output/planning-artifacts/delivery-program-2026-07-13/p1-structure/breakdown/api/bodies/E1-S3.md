## 🎯 Story

As a **platform operator**, I want **fail-closed cell isolation enforced inside studio-api**, so that an edge/KV misroute can never leak cross-cell and every downstream call stays cluster-local.

**Definition of Done:** one named test **`TestCellGuardRejectsCrossCell`** (integration layer — drives a request whose `principal.cell != CELL_ID` and asserts a **421** with no handler execution, and asserts every configured downstream URL resolves to a same-cell `*.svc` name, through the middleware + config interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a request whose `principal.cell != CELL_ID`, When it hits the cell-guard middleware, Then it returns 421 and no handler runs. *Assertion: cross-cell request → 421, handler not invoked (rule 3).* [Source: A-CELL-COMPLIANCE]
- [ ] **AC2** — Given prod, When CELL_ID is a distinct param from CLUSTER_NAME, Then both are required and echoed on readiness. *Assertion: readiness body contains both; boot fails if CELL_ID unset in prod (rule 4).* [Source: A-CELL-COMPLIANCE]
- [ ] **AC3** — Given dev/crew/standalone, When the sentinel cell `solo` is active, Then the guard no-ops. *Assertion: `CELL_ID=solo` → guard passes all.* [Source: A-CELL-COMPLIANCE rule 4]
- [ ] **AC4** — Given the downstream `STUDIO_*_URL` slots (AI/KNOWLEDGE/MCP/IDENTITY/BILLING/CONSOLE/WORKFLOWS/WIKI), When config is read, Then each resolves to a cluster-local `*.svc` name, never a public `orvex.(ai|dev)` host. *Assertion: cell-lint greps config and fails on any peer public URL (rules 1 & 14).* [Source: A-CELL-COMPLIANCE]
- [ ] **AC5** (negative) — Given a request path, When it would fan out cross-cell, Then it is forbidden. *Assertion: any cross-cell downstream call in a request path → blocked/erroring.* [Source: A-CELL-COMPLIANCE]
- [ ] **AC6** (error-path) — Given the scaffold's `api.studio.orvex.ai` HTTPRoute, When superseded, Then routing is same-origin `/api` behind the anycast edge. *Assertion: no independent public HTTPRoute for studio-api.* [Source: A-CELL, §4 deploy deltas]

## 🔨 Tasks
- [ ] Add cell-guard middleware (421 on `principal.cell != CELL_ID`), mounted before all routes (AC: 1)
- [ ] Wire CELL_ID/CLUSTER_NAME params + readiness echo + `solo` sentinel no-op (AC: 2,3)
- [ ] Fill the eight `STUDIO_*_URL` slots with `*_SVC` cluster-local DNS (AC: 4)
- [ ] Forbid cross-cell request-path fan-out; add cell-lint config grep to CI (AC: 4,5)
- [ ] Supersede the scaffold HTTPRoute with same-origin `/api` posture (AC: 6)
- [ ] Write `TestCellGuardRejectsCrossCell` (RED→GREEN) (AC: 1,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — cell-guard cross-cell typed-421 reject fully implemented + tested (`src/middleware/cellGuard.ts`, `src/cell/compliance.ts`, `test/cell-guard.test.ts`); CELL_ID/CLUSTER_NAME in config. Missing: same-cell service resolution (siblings are static ORVEX_*_URL, no cell-aware discovery).

Tier placement: edge/middleware + infra config. Seam crossed: all downstream satellites (cluster-local DNS). Sibling dependency: E1-S2 (principal.cell); E1-S4/S5 (the `orvexcell` event extension attr, rule 6). This is defence-in-depth atop the anycast edge — the edge is necessary but not sufficient.

## 🧪 Testing
- Named DoD test: `TestCellGuardRejectsCrossCell` (integration). Plus cell-lint CI job (config grep).
- Tiers: unit (guard predicate) + integration (routing) + CI-lint. CS §5 mocking: no external mocks needed; drive with synthetic principals.

## 📏 Guidance
CS 6aMAzsYeQb §§0/7/10 (config, boundaries, ops); SE-Arch 8sYi523i4t lenses (cell-contract binding); cell-lint JGAUQRsw2g §5 (rules 1/3/4/6/11/14 — the authoritative source).

## 🔗 References
PRD `85qj2wwU2L` (D-SA11); Architecture `ekTh7nDQqo` (A-CELL, A-CELL-COMPLIANCE, §4); cell contract `JGAUQRsw2g`.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E1**; E1-S2 (principal.cell).
- [ ] Blocks: E1-S4/S5 (event cell-stamp), all request-serving routes.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
