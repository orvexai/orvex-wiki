## 🎯 Story

As a **Studio platform engineer**, I want the **strangle carried out in the fixed A-SEQ order behind behaviour-invariant ports, with a parity gate at every satellite cutover**, so that each in-process implementation becomes a satellite call in the safe sequence — extract-first, greenfields satellite-native, then AI → Temporal → retrieval/admin — without a big-bang migration slipping Alpha 07-23.

**Definition of Done:** one named test **`TestStrangleSeqPortParity`** (integration layer — exercises a domain flow through a seam port with the in-process adapter and then the satellite adapter and asserts observable behaviour is identical across the swap, plus asserts the A-SEQ ordering gate rejects a cutover whose upstream step has not landed, through the port/adapter + cutover-gate interfaces). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given step 1 (extract + split), When `apps/bff`→studio-api lands, Then every satellite call stays the current in-process implementation behind a port with no behaviour change. *Assertion: each seam has a port; in-process adapter passes the parity test.* [Source: A-SEQ step 1]
- [ ] **AC2** — Given step 2, When billing (entitlement-check) and Search Service (skill/Memory/wiki search → knowledge) land, Then both are satellite-native from day one, never added to the monolith. *Assertion: no in-process billing/search impl is ever committed; day-one satellite calls.* [Source: A-SEQ step 2]
- [ ] **AC3** — Given steps 3→4→5, When AI migration (5 `callModel()` sites + embedding → ai), Temporal migration (`studio-queue`/`provisionUserSpace` → central workflows), and retrieval+admin cutover (local RRF → knowledge; admin → console; DocmostPort → wiki-api) execute, Then they run in that order, each behind its port. *Assertion: ordering gate enforces 3-before-4-before-5.* [Source: A-SEQ steps 3,4,5]
- [ ] **AC4** — Given any satellite cutover, When the in-process adapter is swapped for the satellite adapter, Then the parity test proves observable behaviour is identical. *Assertion: same inputs → same observable outputs across the adapter swap.* [Source: A-SEQ, A-STRANGLE]
- [ ] **AC5** (negative) — Given a cutover attempted before its A-SEQ predecessor step has landed, When the ordering gate runs, Then the cutover is rejected. *Assertion: out-of-order cutover fails the gate loudly.* [Source: A-SEQ]
- [ ] **AC6** (error-path) — Given a satellite adapter fails its parity test, When cutover is attempted, Then the port stays on the in-process adapter and the cutover does not proceed. *Assertion: parity failure blocks cutover; no silent half-migration.* [Source: A-SEQ, A-STRANGLE]

## 🔨 Tasks
- [ ] Define the port at every A-SEQ seam (billing, search, ai, workflows, retrieval, admin, DocmostPort) (AC: 1,2,3)
- [ ] Land step 2 greenfields as satellite-native from day one — no in-process fallback (AC: 2)
- [ ] Sequence steps 3→4→5, each behind its port, gated by predecessor completion (AC: 3,5)
- [ ] Build the port-parity harness: in-process vs. satellite adapter, identical-behaviour assertion (AC: 4,6)
- [ ] Add the A-SEQ ordering gate (reject out-of-order cutover) (AC: 5)
- [ ] Write `TestStrangleSeqPortParity` (RED→GREEN) (AC: 4,5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no strangle-sequence ordering or port-parity cutover gates anywhere in the tree.

Tier placement: port/adapter tier — the seam layer E1-S1 stands up is where A-SEQ operates. Seams crossed: all of them, but one at a time in order — billing/search (step 2), ai (step 3), workflows (step 4), knowledge/console/wiki-api (step 5). Sibling dependency: E1-S1 (extract + split = step 1, the ports-first substrate this sequences on top of); downstream, A-SEQ is the ordering these satellite stories inherit — E9-S1/S2 (billing), E8-S2 (search→knowledge), E9-S4/E7-S2 (Temporal), E8-S1 (DocmostPort→wiki-api). This is the *ordering* invariant over A-STRANGLE's *extraction* ruling; the two are sibling decisions (arch §2/§5).

## 🧪 Testing
- Named DoD test: `TestStrangleSeqPortParity` (integration).
- Tiers: unit (ordering-gate predecessor check) + integration (adapter-swap parity). CS §5 mocking: exercise the real in-process adapter; stub the satellite endpoint for the parity comparison; never mock the port itself.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/7 (ports/adapters, dependency boundaries, contract seam, no-half-migration); SE-Arch 8sYi523i4t lenses (big-bang-migration risk, behaviour-drift-on-cutover, out-of-order strangle); cell-lint JGAUQRsw2g (satellite calls stay same-cell across every cutover).

## 🔗 References
Architecture `ekTh7nDQqo` (§5 A-SEQ strangle sequence; §2 A-STRANGLE sibling ruling; §6 satellite-hop latency); PRD `85qj2wwU2L` (FR-SA sequencing context).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; satellite port contracts pinned there).
- [ ] Blocked by: parent epic **E1**; E1-S1 (extract + split = A-SEQ step 1 substrate).
- [ ] Blocks: E9-S1/E9-S2 (billing, step 2), E8-S2 (search→knowledge, step 2), E9-S4 + E7-S2 (Temporal, step 4), E8-S1 (DocmostPort→wiki-api, step 5) — each inherits its A-SEQ position.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
