## 🎯 Story

As a **Studio BFF**, I want to **consume the CloudEvents I depend on with idempotent dedupe**, so that redelivered billing/trial/ai-cap events are applied exactly once and never corrupt entitlement or feature-degrade state.

**Definition of Done:** one named test **`TestConsumedEventsAreIdempotent`** (integration layer — delivers `billing.entitlement.changed` twice with the same `ce-id` and asserts the entitlement cache is evicted once with no double-apply, through the consumer interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the `studio-events` triggers, When configured, Then studio-api consumes `billing.entitlement.changed`, `billing.trial.started/expired`, `ai.cap.reached/warning`. *Assertion: each subscribed topic has a handler.* [Source: FR-SA31]
- [ ] **AC2** — Given `billing.entitlement.changed`, When consumed, Then the entitlement cache is evicted (and re-push to the SPA is triggered — E9-S1). *Assertion: post-event, next entitlement read misses cache.* [Source: FR-SA31, FR-SA27]
- [ ] **AC3** — Given a redelivered event with the same `ce-id`, When consumed again, Then it dedupes (upsert-not-insert against the local store). *Assertion: second delivery is a no-op (rule 11).* [Source: A-CELL-COMPLIANCE]
- [ ] **AC4** — Given `ai.cap.reached/warning`, When consumed, Then the affected feature degrades + a user notice is queued. *Assertion: cap event → feature marked degraded.* [Source: FR-SA31, NFR-SA5]
- [ ] **AC5** (negative) — Given an event with a mismatched `orvexcell`, When consumed, Then it fails closed. *Assertion: wrong-cell event → rejected, not applied (rule 6).* [Source: A-CELL-COMPLIANCE]
- [ ] **AC6** (error-path) — Given a handler throws, When processing, Then the event is retried/parked, never silently dropped. *Assertion: failing handler → retry/DLQ, no data loss.* [Source: FR-SA31, CS §7]

## 🔨 Tasks
- [ ] Wire `studio-events` triggers + handlers for the three consumed families (AC: 1)
- [ ] Implement `ce-id` dedupe store (upsert-not-insert) (AC: 3)
- [ ] Evict entitlement cache on `billing.entitlement.changed`; hook re-push (AC: 2)
- [ ] Degrade features on `ai.cap.*` + queue notice (AC: 4)
- [ ] Fail-closed on `orvexcell` mismatch; retry/park on handler error (AC: 5,6)
- [ ] Write `TestConsumedEventsAreIdempotent` (RED→GREEN) (AC: 3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — inbound CloudEvent consumption real -- `cloudEventHandler` POST `/internal/events` acks 202 + fans out to SSE (`src/events/entitlement.ts`), plus `deploy/.../kafka-trigger-entitlement.yaml`. Missing: idempotent event-dedupe store.

Tier placement: adapter (consumer) → application (state effects). Seam crossed: billing + ai (inbound events). Sibling dependency: E1-S4 (dedupe store shares the outbox DB); E9-S1 (entitlement cache + SSE re-push). Consumed events are the canonical propagation; the sync entitlement-check (E9-S1) is the low-latency read path.

## 🧪 Testing
- Named DoD test: `TestConsumedEventsAreIdempotent` (integration).
- Tiers: unit (dedupe predicate, cell check) + integration (consume loop). CS §5 mocking: mock the broker delivery; use real Postgres dedupe store; never mock own consumer.

## 📏 Guidance
CS 6aMAzsYeQb §§0/7/10 (idempotency, boundaries, ops); SE-Arch 8sYi523i4t lenses (redelivery/replay safety); cell-lint JGAUQRsw2g (rule 6 fail-closed, rule 11 idempotency).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA31, FR-SA27); Architecture `ekTh7nDQqo` (§4 triggers, A-CELL-COMPLIANCE rule 11).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; consumed event schemas pinned there).
- [ ] Blocked by: parent epic **E1**; E1-S3 (cell), E1-S4 (dedupe/outbox DB).
- [ ] Blocks: E9-S1 (cache eviction + SSE re-push depends on this).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
