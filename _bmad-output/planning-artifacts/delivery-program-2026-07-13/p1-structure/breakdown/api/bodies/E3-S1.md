## 🎯 Story

As a **Studio user**, I want the **Memory card as a frozen, versioned, human-editable schema plus the AI-declared FormSpec model**, so that my curated facts are a legible portrait — never an opaque blob — and every downstream slice reads one pinned shape.

**Definition of Done:** one named test **`TestMemoryCardSchemaFrozenAndVersioned`** (unit layer — constructs a Memory card carrying content, provenance/source, owner_scope, sensitivity, confidence, bi-temporal timestamps and state, asserts it validates against the pinned contract, round-trips its human-editable representation, and that a schema version bump is forward-compatible, through the card domain type). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the Memory card, When defined, Then it carries content, provenance/source, owner/scope, sensitivity, confidence, timestamps and lifecycle state, frozen as a contract. *Assertion: card validates against the ENG-2098 pinned schema; all fields present.* [Source: FR-M1, AD-2]
- [ ] **AC2** — Given any memory, When rendered, Then it has a human-readable, human-editable representation. *Assertion: card → editable form → card round-trips losslessly.* [Source: FR-M2]
- [ ] **AC3** — Given the schema, When versioned, Then it is forward-compatible (older-version cards still read). *Assertion: prior-version card deserializes under the new version.* [Source: FR-M3]
- [ ] **AC4** — Given firewall/consent/lifecycle needs, When the schema is built, Then it carries scope, sensitivity, validity window, and state. *Assertion: fields required by FR-S5/consent/F4 exist and are typed.* [Source: FR-M4]
- [ ] **AC5** — Given the FormSpec (structure only over the fixed 9-type palette), When declared, Then it holds ordered sections/questions and NO AI-authored fact value. *Assertion: FormSpec has no value slots the AI fills; values are user-supplied.* [Source: FR-SA5, NFR-SA4]
- [ ] **AC6** (negative) — Given a card bearing an unknown/unversioned shape, When validated, Then it is rejected with a typed error. *Assertion: malformed card → typed schema error, not silent coercion.* [Source: AD-2]

## 🔨 Tasks
- [ ] Define the Memory card domain type against the pinned contract (AC: 1,4)
- [ ] Human-editable representation + round-trip (AC: 2)
- [ ] Version + forward-compat deserialization (AC: 3,6)
- [ ] FormSpec model (9-type palette, structure-only) (AC: 5)
- [ ] Write `TestMemoryCardSchemaFrozenAndVersioned` (RED→GREEN) (AC: 1,3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — MemoryEntry model + Privacy types + own Postgres `memory` schema (`src/store/postgres/repository.ts`), privacy domain tested. Missing: the FormSpec 9-type palette and a frozen versioned card schema.

Tier placement: domain (Memory card + FormSpec). The card schema is authored in orvex-studio-contracts (AD-2) — studio-api conforms; do not invent fields. Seam crossed: contracts (schema home). Sibling dependency: E1 (persistence, JSONB). This is the shape E3-S2..S5, E4, E5, E9-S3 all read/write. AD-1: this is the sole system-of-record for the product Memory card.

## 🧪 Testing
- Named DoD test: `TestMemoryCardSchemaFrozenAndVersioned` (unit).
- Tiers: unit (schema, versioning, round-trip). CS §5 mocking: none — pure domain; load golden card fixtures from contracts.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/7 (contract-first types, forward-compat, DfM/typed structs never `any`); SE-Arch 8sYi523i4t lenses (schema drift across services); cell-lint JGAUQRsw2g (per-cell store).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA5, FR-M1–4); Architecture `ekTh7nDQqo` (AD-1, AD-2); Memory PRD `g9vWbSYplh`, spine `iiCcKhGptV`.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; the Memory card schema IS a Definition-Pack artifact).
- [ ] Blocked by: parent epic **E3**; E1 (persistence).
- [ ] Blocks: E3-S2/S3/S4/S5, E4, E5, E9-S3.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any schema-field addition (contracts-first).
