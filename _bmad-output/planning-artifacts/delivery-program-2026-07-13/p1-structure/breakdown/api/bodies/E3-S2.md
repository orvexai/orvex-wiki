## 🎯 Story

As a **Studio SPA**, I want the **FormSpec declaration SSE wire contract owned by studio-api**, so that FormSpec streams to the UI with a stable `scaffold_committed`/`section`/`done`/`error` frame protocol, PacingGate, and a deterministic fallback — independent of which model produced it.

**Definition of Done:** one named test **`TestFormSpecSseFrameContract`** (integration layer — subscribes to the FormSpec declare SSE and asserts the frame sequence `scaffold_committed`→`section*`→`done`, PacingGate ordering, and that an ai-side failure yields a clean `error` frame + deterministic fallback rather than a broken stream, through the SSE endpoint). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a FormSpec declaration request, When streamed, Then frames arrive as `scaffold_committed` then ordered `section` then `done`. *Assertion: frame order matches the contract.* [Source: FR-SA5]
- [ ] **AC2** — Given the model call behind declaration, When it runs, Then it is delegated to ai's streaming endpoint but the wire contract to the UI stays studio-api's. *Assertion: ai is called via the delegated port; the SSE shape is studio-api's, not passthrough.* [Source: FR-SA5, FR-SA16]
- [ ] **AC3** — Given streaming, When frames emit, Then PacingGate governs cadence. *Assertion: frame timing respects PacingGate bounds.* [Source: FR-SA5]
- [ ] **AC4** — Given an ai failure/timeout mid-stream, When it occurs, Then an `error` frame is sent and the deterministic fallback engages. *Assertion: ai error → `error` frame + fallback, stream closes cleanly.* [Source: FR-SA5, NFR-SA5]
- [ ] **AC5** — Given a completed declaration, When applied, Then the FormSpec is structure-only (no AI-authored values). *Assertion: resulting FormSpec has zero AI-filled fact values.* [Source: FR-SA5, NFR-SA4]
- [ ] **AC6** (negative) — Given a client disconnect, When mid-stream, Then resources release cleanly (no leaked ai stream). *Assertion: disconnect → ai stream cancelled.* [Source: FR-SA5]

## 🔨 Tasks
- [ ] Implement the FormSpec declare SSE endpoint + frame protocol (AC: 1,3)
- [ ] Delegate the model call to ai's streaming port, keep the wire contract local (AC: 2)
- [ ] Deterministic fallback + `error` frame on ai failure (AC: 4)
- [ ] Enforce structure-only outcome (AC: 5)
- [ ] Cancel-on-disconnect (AC: 6)
- [ ] Write `TestFormSpecSseFrameContract` (RED→GREEN) (AC: 1,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no FormSpec SSE declaration wire contract or PacingGate (only the entitlement SSE channel exists).

Tier placement: route (SSE) → application (declaration orchestration) → ai port. Seam crossed: ai (streaming FormSpec, propose-only). Sibling dependency: E3-S1 (FormSpec model), E1-S2 (auth). The SPA↔BFF wire contract is studio-api's; the model call behind it is ai's — the split is the point.

## 🧪 Testing
- Named DoD test: `TestFormSpecSseFrameContract` (integration).
- Tiers: unit (frame builder, PacingGate) + integration (SSE). CS §5 mocking: mock the ai streaming *external service* (incl. a failure mode); never mock own SSE layer.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/6/7 (streaming boundaries, delegation, contract seam); SE-Arch 8sYi523i4t lenses (stream failure cleanliness); cell-lint JGAUQRsw2g (ai via cluster-local svc).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA5, FR-SA16); Architecture `ekTh7nDQqo` (A-DELEGATE ai, §3 SSE contract).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; ai streaming contract + SSE frame schema pinned there).
- [ ] Blocked by: parent epic **E3**; E3-S1 (FormSpec model), E1-S2 (auth).
- [ ] Blocks: E3-S3 (declared cards flow into privacy pipeline).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
