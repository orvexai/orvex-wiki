## 🎯 Story
**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.


As a **new user**, I want **Demo World lifecycle orchestration**, so that on declaring my profession a persona pack seeds instantly (≤60s), every path respects the demo flag, editing forks a real copy, and demo AI actions are capped and isolated from real data.

**Definition of Done:** one named test **`TestDemoSeedFlaggedIsolatedCapped`** (integration layer — declares "teacher", asserts the persona pack seeds Studio rows flagged Demo Data in the user's own tables + the demo wiki space + indexing within ≤60s within quota, that every query/export/enrichment path respects the demo flag, edit-of-demo forks a user-owned copy, and demo AI actions cap at ≈20 and never touch real-data namespaces, through the demo interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a profession declaration, When the pack seeds, Then Studio rows (Memory, Library) are flagged Demo Data in the user's own tables + the demo wiki space (Demo Engine FR-72) + indexing, within ≤60s. *Assertion: seed completes ≤60s; rows carry demo flag (not a separate tenant).* [Source: FR-SA21]
- [ ] **AC2** — Given demo data, When any query/export/enrichment runs, Then the demo flag is respected. *Assertion: every read path filters on the demo flag.* [Source: FR-SA21]
- [ ] **AC3** — Given a demo item, When edited, Then it forks a user-owned copy. *Assertion: edit-of-demo → new user-owned row, demo original intact.* [Source: FR-SA21]
- [ ] **AC4** — Given demo AI actions, When run, Then they go live through the standard delegated paths under the demo hard cap (≈20 actions, via the entitlement/metering seam) and never touch real-data namespaces. *Assertion: 21st demo AI action → capped; demo AI never reads real namespaces.* [Source: FR-SA21]
- [ ] **AC5** — Given starter/demo content, When seeded, Then it counts against tenant quota and is kept tiny (3–5 pages); over-quota seed handles the engine 402. *Assertion: demo seed within quota; over-quota → 402 handled (D-S23).* [Source: FR-SA21, OQ-SA11]
- [ ] **AC6** (negative) — Given demo data, When a real-data query runs, Then no demo bleed occurs. *Assertion: real-data path returns zero demo-flagged rows.* [Source: FR-SA21, NFR-SA3]

## 🔨 Tasks
- [ ] Demo seed orchestration (Studio rows flagged + demo wiki via Demo Engine + indexing, ≤60s) (AC: 1)
- [ ] Demo-flag discipline on every query/export/enrichment path (AC: 2,6)
- [ ] Edit-of-demo → fork user-owned copy (AC: 3)
- [ ] Demo AI hard cap (≈20) via entitlement/metering; namespace isolation (AC: 4)
- [ ] Quota-counting demo seed + 402 handling (AC: 5)
- [ ] Write `TestDemoSeedFlaggedIsolatedCapped` (RED→GREEN) (AC: 1,4,6)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** present — demo lifecycle + flag discipline implemented -- `src/demo/seed.ts` (DEMO_TENANT_ID, `Demo:`-prefixed fixtures), read-only `src/demo/guard.ts`, `src/routes/demo.ts`, own `demoRepository`. Tested: `test/demo/seed.test.ts`, `guard.test.ts`, `routes.test.ts`, `test/integration/demo-world.spec.ts`.

Tier placement: application (demo orchestration) → Memory/Library domain + DocmostPort (Demo Engine) + knowledge index. Seam crossed: engine (Demo Engine FR-72), knowledge (indexing), billing/ai (demo cap metering, E9-S2). Sibling dependency: E3 (Memory), E8-S1 (DocmostPort), E9-S2 (cap). Persona packs lead with teachers (Brief wedge).

## 🧪 Testing
- Named DoD test: `TestDemoSeedFlaggedIsolatedCapped` (integration, adversarial demo-bleed).
- Tiers: unit (flag filter, cap counter) + integration (seed + isolation). CS §5 mocking: mock engine/knowledge/billing externals; never mock own demo domain.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/11 (flag discipline, isolation, quota); SE-Arch 8sYi523i4t lenses (demo-bleed, cap-bypass); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA21, OQ-SA11/D-S23); Architecture `ekTh7nDQqo` (A-OWN Demo World); Brief audience (teachers wedge).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E7**; E3, E8-S1 (DocmostPort + Demo Engine), E9-S2 (cap); engine Demo Engine (FR-72).
- [ ] Blocks: E7-S2 (Graduation clears the seeded demo).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
