## 🗺️ Epic — Skill domain, library, marketplace & social

The skill is Studio's core object and its Free-tier moat. This epic owns the **Item→Section→Skill** deterministic model (craft2 v3.2.3), Base skill + Delta personalisation, versioning with never-delete-published-and-used, the **in-process deterministic render** (ADR-0008: pure TS, byte-stable, <50ms, zero model egress — G2/NFR-SA4), Library/Collections persistence and the Builder edit surface, the marketplace + community + social graph with the append-only `reputation_ledger`, and every Prompt-Use surface (copy-as-prompt, five-AI export, permalinks, prompt-via-us launch).

This is the reused heart of the shipped poc (Phase-1 closed 83/83 stories, 736 tests green) — extracted, not rebuilt. The epic exists to preserve those invariants under the satellite era: render stays model-free even as everything AI-shaped delegates to `orvex-studio-ai`, and search delegates to `orvex-studio-knowledge`.

**Why (cites):** FR-SA1–SA4, NFR-SA4 (PRD `85qj2wwU2L`); A-OWN (arch `ekTh7nDQqo`); Brief "marketplace seeding + Orvex rating + core social layer IN [RULED 2026-07-13]."

### Stories
- [ ] E2-S1 — Skill data model + deterministic byte-stable render
- [ ] E2-S2 — Library, Collections & Builder edit surface
- [ ] E2-S3 — Marketplace, social & append-only reputation ledger
- [ ] E2-S4 — Prompt-Use surfaces (copy, five-AI export, permalinks, launch)

### FR ids owned
FR-SA1, FR-SA2, FR-SA3, FR-SA4, NFR-SA4.

### Definition of Done
- [ ] All four stories Done (reviewer ≠ implementer each).
- [ ] Family-E2E share green: a fresh tenant can create → publish → fork → save → export-as-prompt a skill, byte-stable render verified, on the dev cell — observed, not reported.
- [ ] SDD lines evidenced: egress-throws CI gate proves `deterministic/` bans the ai client (NFR-SA4); reputation ledger append-only (no update/delete path) proven by test.

### 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: E1 (auth principal, persistence, events) for every story; E2-S3 social signals index via knowledge (E8/E9 seam) but ledger itself is local.

### 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
