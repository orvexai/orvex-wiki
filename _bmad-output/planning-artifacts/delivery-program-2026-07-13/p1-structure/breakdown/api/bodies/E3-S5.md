## 🎯 Story

As a **Free-tier user**, I want the **deterministic FR-31 Memory weave and a team-aware-but-not-team-functional schema**, so that Memory enriches my prompts with zero model calls (the Free path) and Teams shared/official Memory can land later without a schema rewrite.

**Definition of Done:** one named test **`TestFr31WeaveDeterministicAndTeamAware`** (unit layer — weaves Memory into a render with `sensitiveIncluded` enforced and asserts pure-function determinism + provenance computed inside, and that `owner_scope` is a nullable `{user|org}` with free CAS concurrency so a team memory writes against the fixed field, through the weave + card interfaces). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given Memory + a skill render, When woven, Then the FR-31 weave runs as a pure function with provenance computed inside and `sensitiveIncluded` enforced. *Assertion: same inputs → same woven output; sensitive items excluded unless allow-listed.* [Source: FR-SA8]
- [ ] **AC2** — Given the £7 Composer is active, When present, Then it supersedes the FR-31 weave; otherwise the weave is the Free path. *Assertion: Composer-active → weave not used; Composer-off → weave runs.* [Source: FR-SA8]
- [ ] **AC3** — Given the data model, When defined, Then it is minimally team-aware: `owner_scope` nullable `{user|org}` with free CAS concurrency from day one. *Assertion: owner_scope nullable; concurrent writes CAS-safe.* [Source: FR-T4, AD-12]
- [ ] **AC4** — Given team-aware fields, When a personal user writes, Then personal memory never leaks across the firewall. *Assertion: nullable owner_scope write does not expose personal memory org-wide.* [Source: FR-T4, NFR-SA3]
- [ ] **AC5** (phased, SHOULD) — Given Teams, When shared/official Memory + RBAC (read/propose/approve/admin), a moderation queue, and setup-chosen governance models are specified, Then they write against the fixed field without migration. *Assertion: the phased team surface (FR-T1/T2/T3) binds to `owner_scope`, no schema change.* [Source: FR-T1, FR-T2, FR-T3, AD-12]
- [ ] **AC6** (negative) — Given the AI is down, When the FR-31 weave runs, Then it still weaves. *Assertion: weave succeeds with ai unreachable (Free path model-free).* [Source: FR-SA8, G2]

## 🔨 Tasks
- [ ] Port the deterministic FR-31 weave (pure, provenance-inside, `sensitiveIncluded`) (AC: 1,6)
- [ ] Composer-supersedes-weave switch (AC: 2)
- [ ] Nullable `owner_scope {user|org}` + free CAS from day one (AC: 3,4)
- [ ] Specify (not build) the phased team surface binding to the fixed field (AC: 5)
- [ ] Write `TestFr31WeaveDeterministicAndTeamAware` (RED→GREEN) (AC: 1,3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no FR-31 deterministic weave or team-aware schema.

Tier placement: pure `deterministic/` weave (no egress) + domain (owner_scope). Seam crossed: none in the Free path (the point). Sibling dependency: E2-S1 (render), E3-S1 (schema), E4 (Composer supersedes). AD-1: this service is the Memory system-of-record; ai's ephemeral chat-recall is renamed off "memory" (not owned here). Team functionality is deferred (Brief: team/shared curation v1 minimally team-aware only).

## 🧪 Testing
- Named DoD test: `TestFr31WeaveDeterministicAndTeamAware` (unit).
- Tiers: unit (weave purity, CAS, firewall). CS §5 mocking: none in the weave (pure); mock Postgres port for CAS tests. Never mock own packages.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/6 (purity, CAS, forward-compat schema); SE-Arch 8sYi523i4t lenses (firewall leak, premature team-build); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA8, FR-T1–4); Architecture `ekTh7nDQqo` (AD-1, AD-12); Brief Scope (team memory minimal in v1).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E3**; E2-S1 (render), E3-S1 (schema).
- [ ] Blocks: E4-S1 (Composer builds on the weave path).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
