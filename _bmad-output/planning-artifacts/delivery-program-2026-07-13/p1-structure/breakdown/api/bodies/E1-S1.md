## 🎯 Story

As a **Studio platform engineer**, I want the poc BFF **physically extracted into a properly layered, CI-gated `orvex-studio-api` service with split liveness/readiness probes**, so that every downstream epic builds on a real service instead of the audit's 59-line honest scaffold.

**Definition of Done:** one named test **`TestServiceBootAndReadiness`** (integration layer — boots the Hono app, asserts liveness returns 200 while the process is up with no dependency round-trip, and readiness reads NOT-ready when Postgres or identity is down and 200 when both are up, through the real HTTP probe interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the poc `apps/bff` source, When extracted into `orvex-studio-api`, Then the tree has layered `src/` (routes/handlers → application/service → domain → ports), a lockfile, and `npm ci` succeeds in the distroless Dockerfile. *Assertion: `npm ci` exit 0; `src/` has ≥4 layer dirs.* [Source: A-STRANGLE]
- [ ] **AC2** — Given the extracted repo, When CI runs, Then lint is a real linter (not `tsc --noEmit`), unit+integration tests run, and the build passes. *Assertion: `.github/workflows` present; lint script ≠ `tsc --noEmit` (CS §4).* [Source: NFR-SA7, Audit build-defects]
- [ ] **AC3** — Given the service is up, When the liveness probe is hit, Then it returns 200 without any dependency round-trip. *Assertion: probe latency independent of DB state; returns 200 with DB down.* [Source: FR-SA34]
- [ ] **AC4** — Given Postgres OR identity is unreachable, When the readiness probe is hit, Then it reads NOT-ready and the pod is pulled from rotation. *Assertion: readiness returns non-200 when either hard dep is down.* [Source: FR-SA34, CS §10]
- [ ] **AC5** (negative) — Given any source file, When CI greps imports, Then no `@docmost/*`/AGPL import and no `@temporalio/*` client origination exist. *Assertion: CI grep gate fails the build on either match.* [Source: NFR-SA7, NFR-SA8]
- [ ] **AC6** (error-path) — Given readiness echoes CELL_ID + CLUSTER_NAME, When CELL_ID is unset in prod, Then boot fails loudly (no baked default). *Assertion: prod boot without CELL_ID throws a startup error.* [Source: A-CELL-COMPLIANCE rule 4]

## 🔨 Tasks
- [ ] Extract `apps/bff` → layered `src/`; add lockfile + distroless Dockerfile (AC: 1)
- [ ] Stand up CI: real lint, unit + integration test jobs, build (AC: 2)
- [ ] Split the always-200 `/healthz` into liveness + dependency-round-tripping readiness (AC: 3,4)
- [ ] Add the AGPL + Temporal import-grep CI gates (AC: 5)
- [ ] Wire CELL_ID/CLUSTER_NAME required-in-prod startup check + readiness echo (AC: 6)
- [ ] Write `TestServiceBootAndReadiness` (RED→GREEN) (AC: 3,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — extracted, layered `src/` (routes to domain to store/ports), lockfile, distroless `Dockerfile`, real CI (`.github/workflows/ci.yml`: lint/typecheck/coverage/build/audit/integration). Missing: liveness/readiness split (`/healthz` is a single always-200, `src/server.ts`) and the AGPL/@temporalio import grep-gate.

Tier placement: the whole six-tier spine is stood up here (edge/route → application → domain → port/adapter → infra). No NestJS analogue — this is the one TS/Hono satellite; keep the poc's Hono idioms. Seams crossed: none functional yet — this is the substrate E1-S2..S6 and all other epics depend on. Sibling dependency: none upstream; everything downstream.

## 🧪 Testing
- Named DoD test: `TestServiceBootAndReadiness` (integration). Plus unit tests for the readiness dependency checker.
- Tiers: unit (probe logic) + integration (boot). CS §5 mocking: mock Postgres/identity *ports* (external systems) to simulate down states; never mock own packages.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (layering, lint, telemetry split, dependency boundaries); SE-Arch 8sYi523i4t lenses (build-state honesty, health-probe correctness — the audit's own findings); cell-lint JGAUQRsw2g (CELL_ID vs CLUSTER_NAME, rule 4).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA34); Architecture `ekTh7nDQqo` (§1, §3 health split, A-STRANGLE); Audit `TpxkDsKTkC` (build defects: lockfile, lint, naming drift).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (Definition Pack — contract TAG is the dispatch gate).
- [ ] Blocked by: parent epic **E1**.
- [ ] Blocks: every other story in this service (substrate).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
