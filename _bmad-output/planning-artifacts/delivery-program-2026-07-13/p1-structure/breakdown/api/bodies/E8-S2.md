## 🎯 Story

As a **user**, I want a **wiki omnibox with instant search (Free) and Ask (£7)**, so that I can find pages fast via knowledge's wiki corpus and ask my wiki a question answered by the cited-ask loop.

**Definition of Done:** one named test **`TestOmniboxInstantFreeAskGated`** (integration layer — runs instant search over knowledge's wiki corpus scoped to the user's space and asserts Free results return, and that Ask proxies orvex-studio-ai's cited-ask loop under the caller's forwarded token rendering `{answer, citations, unanswered, gapNote}`, with Ask locked for Free (entitlement-checked server-side), through the omnibox interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the omnibox, When instant search runs (Free), Then it queries knowledge's wiki corpus scoped to the user's space. *Assertion: search returns user-space results; scope enforced.* [Source: FR-SA24]
- [ ] **AC2** — Given Ask (£7), When invoked, Then it proxies orvex-studio-ai's cited-ask loop (D-ASK: ai owns retrieval-via-knowledge under the caller's delegated principal + K5 synthesis); studio-api forwards the caller's token and renders `{answer, citations, unanswered, gapNote}`. *Assertion: Ask returns the four-field shape; token forwarded, not re-minted.* [Source: FR-SA24]
- [ ] **AC3** — Given a no-answer state, When Ask returns, Then it is honest (unanswered + gapNote populated). *Assertion: no-answer → honest empty, not a hallucinated answer.* [Source: FR-SA24]
- [ ] **AC4** — Given Free tier, When Ask is attempted, Then it is locked (entitlement-checked server-side). *Assertion: Free Ask → 402/403 server-side.* [Source: FR-SA24, FR-SA27]
- [ ] **AC5** — Given the search-fronting hop, When first results fetch, Then ≤300ms p95 warm. *Assertion: instant search p95 ≤300ms.* [Source: FR-SA24, NFR-SA1]
- [ ] **AC6** (negative) — Given knowledge down, When instant search runs, Then it degrades per NFR-SA5 (honest empty / FTS fallback where retained). *Assertion: knowledge-down search → honest degraded result, no error.* [Source: NFR-SA5]

## 🔨 Tasks
- [ ] Instant search over knowledge's wiki corpus, user-space scoped (AC: 1,5)
- [ ] Ask proxy to ai cited-ask, token-forwarded, four-field render (AC: 2,3)
- [ ] Server-side Ask entitlement gate (AC: 4)
- [ ] Degraded-mode search fallback (AC: 6)
- [ ] Write `TestOmniboxInstantFreeAskGated` (RED→GREEN) (AC: 2,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — instant-search BFF relay to knowledge POST `/v1/query` present + tested (`src/routes/query.ts`, `test/query.test.ts`). Missing: the Ask (7 pound, gated) proxy half.

Tier placement: route → application (omnibox) → knowledge port (search) + ai port (Ask). Seam crossed: knowledge (wiki corpus), ai (cited-ask D-ASK — ai owns retrieval-via-knowledge, not studio-api). Sibling dependency: E1-S2 (token forward), E9-S1 (Ask gate), E4 (shared retrieval mechanism). studio-api forwards the token; ai does the retrieval + synthesis.

## 🧪 Testing
- Named DoD test: `TestOmniboxInstantFreeAskGated` (integration).
- Tiers: unit (render shape, gate) + integration (search + Ask). CS §5 mocking: mock knowledge + ai externals (incl. no-answer + down); never mock own omnibox.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/6/7 (token forwarding, honest-empty, tier gate); SE-Arch 8sYi523i4t lenses (hallucinated-answer, fail-open Ask); cell-lint JGAUQRsw2g (satellites cluster-local).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA24, NFR-SA1/SA5); Architecture `ekTh7nDQqo` (A-DELEGATE ai/knowledge, D-ASK).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; cited-ask response shape pinned there).
- [ ] Blocked by: parent epic **E8**; E1-S2 (auth), E9-S1 (Ask gate); ai cited-ask, knowledge wiki corpus.
- [ ] Blocks: E4-S1 (shares the wiki retrieval seam).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
