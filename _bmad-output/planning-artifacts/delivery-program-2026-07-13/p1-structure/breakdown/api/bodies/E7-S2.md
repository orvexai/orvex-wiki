## 🎯 Story

As a **graduating user**, I want **Graduation as a central saga**, so that my demo data is cleared across Studio, wiki, and knowledge atomically-from-my-perspective, my real creations survive, and my trial clock starts.

**Definition of Done:** one named test **`TestGraduationSagaAtomicSurvivesForks`** (integration layer — triggers Graduation via the step-APIs and asserts Studio demo-row clear + Demo Engine wiki clear + knowledge index removal happen atomic-from-the-user's-perspective with visible retry, that user-created items incl. forks verifiably survive, the saga is idempotent, and `studio.user.graduated` is emitted to start the trial clock, through the step-API + event interfaces). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given Graduation, When run, Then Studio demo-row clear + Demo Engine wiki clear + knowledge index removal execute as one central saga (workflows), atomic-from-the-user's-perspective with visible retry. *Assertion: all three clears complete or the saga retries visibly; no half-graduated state surfaced.* [Source: FR-SA22]
- [ ] **AC2** — Given user-created items (incl. forks of demo), When Graduation runs, Then they verifiably survive. *Assertion: post-Graduation, user-created + forked items remain.* [Source: FR-SA22]
- [ ] **AC3** — Given the saga, When replayed, Then it is idempotent. *Assertion: re-running Graduation is a no-op after completion.* [Source: FR-SA22]
- [ ] **AC4** — Given Graduation completes, When done, Then `studio.user.graduated` is emitted and billing's trial state machine consumes it (the D-S23 7-day card-required trial clock starts). *Assertion: event emitted; trial clock starts at Graduation.* [Source: FR-SA22]
- [ ] **AC5** — Given the step-APIs (clear-studio-rows, verify-forks), When called by workflows, Then they are idempotent + dual-gated. *Assertion: step-APIs replay-safe + workload-identity gated.* [Source: FR-SA22, FR-SA32]
- [ ] **AC6** (error-path) — Given a mid-saga failure, When it occurs, Then it retries/compensates without losing user-created items. *Assertion: injected failure → retry, zero user-data loss.* [Source: FR-SA22]

## 🔨 Tasks
- [ ] Expose Graduation step-APIs (clear-studio-rows, verify-forks), idempotent + dual-gated (AC: 1,5)
- [ ] Coordinate the three clears atomic-from-user-perspective with visible retry (AC: 1)
- [ ] Verify user-created + fork survival (AC: 2)
- [ ] Idempotent replay (AC: 3)
- [ ] Emit `studio.user.graduated` (trial clock start) (AC: 4)
- [ ] Failure compensation without data loss (AC: 6)
- [ ] Write `TestGraduationSagaAtomicSurvivesForks` (RED→GREEN) (AC: 1,2)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — `WorkflowsClient.startGraduation` throws NotImplemented; no graduation saga step-APIs.

Tier placement: step-API surface (consumed by workflows) → Memory/Library domain + DocmostPort + knowledge port + outbox. Seam crossed: workflows (saga orchestration), engine (Demo Engine clear), knowledge (index removal), billing (trial start). Sibling dependency: E7-S1 (demo seed), E9-S4 (step-API discipline), E1-S4 (event). The saga is central (D-WF-1) — studio-api runs no worker.

## 🧪 Testing
- Named DoD test: `TestGraduationSagaAtomicSurvivesForks` (integration, failure injection).
- Tiers: unit (step-API idempotency, survival check) + integration (saga). CS §5 mocking: mock workflows/engine/knowledge/billing externals; never mock own step-APIs.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7/11 (saga atomicity, idempotency, dual-gate); SE-Arch 8sYi523i4t lenses (partial-graduation, data-loss); cell-lint JGAUQRsw2g (satellites cluster-local).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA22, FR-SA32, D-S23); Architecture `ekTh7nDQqo` (A-DELEGATE workflows).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; Graduation step-API + `studio.user.graduated` pinned there).
- [ ] Blocked by: parent epic **E7**; E7-S1 (seed), E9-S4 (step-APIs), E1-S4 (event); workflows Graduation saga.
- [ ] Blocks: none (completes the demo lifecycle).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
