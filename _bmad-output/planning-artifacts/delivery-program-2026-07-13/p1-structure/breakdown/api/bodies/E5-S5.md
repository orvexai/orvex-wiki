## 🎯 Story

As the **platform**, I want the **Curator→Librarian supersession — `classifyOnSave` becomes a capture on-ramp forwarding to `orvex-studio-staging` submit**, so that there is one classification path, Card Contract v1 carries forward verbatim as Proposal, and studio-api becomes a staging client with a loud migration error on any legacy path.

**Definition of Done:** one named test **`TestCuratorForwardsToStagingWithGoldens`** (integration layer — submits a capture and asserts `classifyOnSave` forwards to staging submit as a Proposal (Card Contract v1 mapped as a strict superset), the shipped Curator goldens pass against the mapping, and any residual legacy classification path raises a loud migration error, through the capture interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given cutover, When capture occurs, Then `classifyOnSave` becomes a capture on-ramp forwarding to staging submit (one classification path). *Assertion: capture → staging submit; no local classification fork.* [Source: FR-SA12 supersession, ADR-0024]
- [ ] **AC2** — Given Card Contract v1, When mapped to the staging Proposal, Then it carries forward verbatim as a strict superset. *Assertion: v1 fields map 1:1 into Proposal; superset validated.* [Source: ADR-0024]
- [ ] **AC3** — Given the shipped Curator goldens, When run against the mapping, Then they pass. *Assertion: Curator golden suite green against Proposal mapping.* [Source: ADR-0024]
- [ ] **AC4** — Given `/v1/memory` (E3), When cutover happens, Then it is explicitly NOT superseded and keeps writing locally. *Assertion: /v1/memory store unchanged post-cutover.* [Source: ADR-0025]
- [ ] **AC5** — Given commit of a staging ChangeSet, When applied, Then `api` applies the supersede atomically and never reconciles independently (AD-4). *Assertion: supersede applied atomically on commit only.* [Source: AD-4]
- [ ] **AC6** (negative) — Given a residual legacy classification path, When hit, Then it raises a loud migration error (no silent dual path). *Assertion: legacy path → explicit migration error, never a fallback.* [Source: ADR-0024, MEMORY no-fallbacks]

## 🔨 Tasks
- [ ] Convert `classifyOnSave` to a staging-submit on-ramp (AC: 1)
- [ ] Map Card Contract v1 → staging Proposal (strict superset) (AC: 2)
- [ ] Run the Curator goldens against the mapping (AC: 3)
- [ ] Leave `/v1/memory` store intact (AC: 4)
- [ ] Atomic supersede-on-commit apply (AC: 5)
- [ ] Loud migration error on legacy paths (AC: 6)
- [ ] Write `TestCuratorForwardsToStagingWithGoldens` (RED→GREEN) (AC: 1,3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — classify-on-save via the AI sibling implemented + tested -- `src/curator/classifyOnSave.ts`, `test/CuratorClassifiesSkillOnSaveViaAiSibling.test.ts` + `test/curator-*.test.ts`. Missing: the classifyOnSave to staging submit / supersession forwarding.

Tier placement: application (capture on-ramp) → staging client port. Seam crossed: orvex-studio-staging (submit) — a live double-ownership until this cutover (Curator in BFF vs staging). Sibling dependency: E5-S1/S4 (gate), E3-S4 (confirm-gate apply). Blocked on staging pack certification. No-fallbacks doctrine: required new path + loud migration error, no legacy shim (MEMORY).

## 🧪 Testing
- Named DoD test: `TestCuratorForwardsToStagingWithGoldens` (integration).
- Tiers: unit (mapping, migration-error) + integration (forwarding + goldens). CS §5 mocking: mock the staging submit external; never mock own gate; run real Curator goldens.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7 (migration discipline, contract carry-forward, no dual path); SE-Arch 8sYi523i4t lenses (double-ownership, silent-fallback); cell-lint JGAUQRsw2g (staging cluster-local).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA12 supersession, ADR-0024/0025); staging PRD `mOzNDhn322`; ADR-0024 `ulVURyc2pd`, ADR-0025 `AqXpAlgvIv`.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; staging Proposal mapping pinned there).
- [ ] Blocked by: parent epic **E5**; E5-S1/S4 (gate), E3-S4 (apply); **orvex-studio-staging pack certification**.
- [ ] Blocks: none downstream (final Curator cutover).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if staging pack is not yet certified.
