## 🎯 Story

As the **Curator**, I want **structure-evolution proposals plus delegated gate primitives (ai classify, knowledge dedup, propose-only, act-as-user apply) and async batched distillation**, so that the gate's implementation delegates to satellites while its authority stays local, and distillation never inlines unbounded spend.

**Definition of Done:** one named test **`TestDelegatedGatePrimitivesFailClosed`** (integration layer — runs the gate with ai classification and asserts a 2s-ceiling timeout/uncertain yields `{gated:true, source:"fail_safe"}`, that dedup candidates come from knowledge under the user's principal, that approved wiki changes apply as-the-user, and that distillation runs async/batched under the per-user budget with backpressure — through the gate + workflow interfaces). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given wiki structure proposals (sections/merges/splits/reorganizations), When submitted, Then they flow through the same queue with before/after previews and apply link-preservingly through the DocmostPort. *Assertion: structure proposal → previewed → link-preserving apply.* [Source: FR-SA15]
- [ ] **AC2** — Given sensitivity classification, When called, Then it uses ai's bounded verb (2s ceiling, fail-closed: uncertain/timeout ⇒ `{gated:true, source:"fail_safe"}`). *Assertion: timeout/uncertain → gated fail-safe.* [Source: FR-SA16]
- [ ] **AC3** — Given semantic dedup/find-before-create, When needed, Then candidates come from knowledge's Memory+wiki corpora under the user's principal; the divert decision + deterministic gates stay local. *Assertion: dedup queries knowledge as-user; divert computed locally.* [Source: FR-SA16]
- [ ] **AC4** — Given any content-bearing AI leg (FormSpec, Improve, Librarian-chat, wiki-title), When it runs, Then it is propose-only (draft requiring human accept). *Assertion: AI output is a draft, never auto-committed.* [Source: FR-SA16]
- [ ] **AC5** — Given approved wiki changes, When applied, Then they apply as-the-user (act-as-user delegation), never as an anonymous service actor. *Assertion: apply carries the user's delegated token.* [Source: FR-SA16, FR-SA26]
- [ ] **AC6** — Given distillation, When run, Then it is async/batched (never inline) under the per-user budget (provisional 1,000 LLM actions/mo) with backpressure ("still processing"), via central workflows calling step-APIs; budget rides ai's `ai.usage.recorded` keyed by call-site×principal. *Assertion: distillation never inline; over-budget → backpressure, no unbounded spend.* [Source: FR-SA17]

## 🔨 Tasks
- [ ] Structure-evolution proposals + link-preserving apply (AC: 1)
- [ ] Delegate sensitivity classify to ai's bounded fail-closed verb (AC: 2)
- [ ] Delegate dedup/find-before-create to knowledge as-user; keep divert local (AC: 3)
- [ ] Propose-only semantics on all content-bearing AI legs (AC: 4)
- [ ] Act-as-user apply for approved wiki changes (AC: 5)
- [ ] Async batched distillation via workflows step-APIs + budget/backpressure (AC: 6)
- [ ] Write `TestDelegatedGatePrimitivesFailClosed` (RED→GREEN) (AC: 2,6)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no structure evolution, delegated gate primitives, or async distillation.

Tier placement: application (gate delegation) → ai/knowledge/workflows ports + DocmostPort. Seam crossed: ai (classify verb, propose-only — a contract delta to file), knowledge (dedup corpora), workflows (distillation batches), engine (structure apply). Sibling dependency: E5-S1/S3, E8-S1 (DocmostPort), E9-S4 (step-APIs). The shipped `librarian/sensitivity/classify.ts` semantics become ai's contract.

## 🧪 Testing
- Named DoD test: `TestDelegatedGatePrimitivesFailClosed` (integration, incl. timeout injection).
- Tiers: unit (fail-closed logic, budget) + integration (delegation). CS §5 mocking: mock ai/knowledge/workflows externals incl. timeout; never mock own gate/divert.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7/11 (fail-closed, propose-only, act-as-user, budget); SE-Arch 8sYi523i4t lenses (fail-open classification, inline-spend); cell-lint JGAUQRsw2g (satellites cluster-local).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA15, FR-SA16, FR-SA17, D-SA8); Architecture `ekTh7nDQqo` (A-CURATOR).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; ai classify verb + propose-only mode + `ai.usage.recorded` pinned there).
- [ ] Blocked by: parent epic **E5**; E5-S1/S3, E8-S1 (DocmostPort), E9-S4 (step-APIs).
- [ ] Blocks: E5-S5 (staging cutover builds on the delegated gate).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on the OQ-SA1 ai-migration scope if unpinned.
