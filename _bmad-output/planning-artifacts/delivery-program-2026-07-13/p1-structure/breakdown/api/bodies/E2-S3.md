## 🎯 Story

As a **marketplace participant**, I want **publish/fork-with-lineage, comments/suggestions, use-gated reviews, following/profiles, the Orvex rating, and an append-only reputation ledger**, so that community contribution is transparent and reputation is itemized, never an opaque score.

**Definition of Done:** one named test **`TestReputationLedgerAppendOnlyWithClawback`** (integration layer — publishes, forks, and reverts a skill and asserts the contributor `reputation_ledger` records itemized counts append-only with a claw-back entry on revert — never an in-place score mutation — through the social API). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a skill, When published, Then it enters the marketplace; forks record lineage and saves increment. *Assertion: fork carries a parent link; saves counter increments.* [Source: FR-SA3]
- [ ] **AC2** — Given a marketplace skill, When reviewed, Then reviews are live and use-gated (only actual users can review). *Assertion: non-user review attempt → rejected.* [Source: FR-SA3, Brief gated reviews]
- [ ] **AC3** — Given social signals, When a user follows/creates a profile, Then following-graph + profile persist. *Assertion: follow edge persists and is queryable.* [Source: FR-SA3]
- [ ] **AC4** — Given contributor activity, When it occurs, Then the `reputation_ledger` appends itemized counts (no opaque aggregate score written). *Assertion: ledger rows are append-only; no UPDATE/DELETE path.* [Source: FR-SA3]
- [ ] **AC5** — Given a reverted contribution, When processed, Then a claw-back entry is appended. *Assertion: revert → negative/claw-back ledger row (not deletion of the prior row).* [Source: FR-SA3]
- [ ] **AC6** (negative) — Given the Orvex rating (structural quality + popularity/usage + upvotes), When computed, Then it is derived from ledger + signals, never hand-set. *Assertion: rating recomputes from source signals; no manual override path.* [Source: FR-SA3, Brief Orvex rating]

## 🔨 Tasks
- [ ] Port marketplace publish/fork-lineage/saves (AC: 1)
- [ ] Use-gated reviews + comments/suggestions (AC: 2)
- [ ] Following-graph + profiles (AC: 3)
- [ ] Append-only `reputation_ledger` with claw-back (AC: 4,5)
- [ ] Multi-factor Orvex rating derivation (AC: 6)
- [ ] Emit `studio.skill.published/updated` via the outbox (E1-S4)
- [ ] Write `TestReputationLedgerAppendOnlyWithClawback` (RED→GREEN) (AC: 4,5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — `src/routes/marketplace.ts` is a 501 stub, `discover.ts` returns empty; no social graph or append-only reputation ledger.

Tier placement: route → application → domain (social) → Postgres port + outbox. Seam crossed: knowledge (marketplace search index via `studio.skill.*` events); the *following-graph/creator-profiles* are IN per the Brief [RULED 2026-07-13] (note: community trust badges are deferred, but the ledger/reviews/forking-with-lineage core social layer is in). Sibling dependency: E2-S2 (publish from Library), E1-S4 (events).

## 🧪 Testing
- Named DoD test: `TestReputationLedgerAppendOnlyWithClawback` (integration).
- Tiers: unit (rating derivation, use-gate) + integration (ledger, events). CS §5 mocking: mock the knowledge index event sink and Postgres port; never mock own social domain.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/6/7 (append-only integrity, event contract); SE-Arch 8sYi523i4t lenses (opaque-score anti-pattern, use-gate abuse); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA3); Architecture `ekTh7nDQqo` (A-OWN reputation_ledger); Brief Scope (Orvex rating, core social layer IN; creator profiles deferred to community wave).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; `studio.skill.*` schema pinned there).
- [ ] Blocked by: parent epic **E2**; E2-S2, E1-S4 (events).
- [ ] Blocks: E2-S4 (prompt-use from marketplace skills).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
