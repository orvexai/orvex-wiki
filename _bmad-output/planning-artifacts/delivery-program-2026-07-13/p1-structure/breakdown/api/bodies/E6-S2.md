## 🎯 Story

As a **user importing history**, I want the **guided per-provider import wizard with resumable multi-GB upload and parse adapters running as a central workflow**, so that I can import ChatGPT/Claude (then Gemini/Grok/Copilot) archives idempotently with honest progress.

**Definition of Done:** one named test **`TestImportParseIdempotentViaWorkflow`** (integration layer — uploads a ChatGPT + Claude archive, asserts the per-provider parse adapters (ChatGPT `current_node` active-branch walk, Claude `conversations.json`) execute as a central-workflow-driven step-API call, ingest turns idempotently with dedupe-on-reimport, and expose visible progress + honest failure states, through the import interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the import wizard, When started, Then content-managed per-provider instructions + pending-import state persist across sessions. *Assertion: wizard state resumes after re-login.* [Source: FR-SA19]
- [ ] **AC2** — Given a multi-GB Archive, When uploaded, Then upload to S3 is resumable, with a Time Window filter. *Assertion: interrupted upload resumes; Time Window scopes the import.* [Source: FR-SA19]
- [ ] **AC3** — Given a provider, When parsed, Then the adapter is correct (ChatGPT `current_node` active-branch walk, Claude `conversations.json`; Gemini/Grok/Copilot at Beta). *Assertion: adapter output matches the provider's active-branch semantics.* [Source: FR-SA19]
- [ ] **AC4** — Given the parse pipeline, When it runs, Then it executes as a central workflow (resumable, idempotent) calling studio-api step-APIs — no in-service worker. *Assertion: parse runs via workflows step-APIs; no `@temporalio/*` here (NFR-SA8).* [Source: FR-SA19, NFR-SA8]
- [ ] **AC5** — Given a reimport, When run, Then dedupe-on-reimport prevents duplicates; £7/trial additionally feeds the Curator, Free fills Chat History only. *Assertion: reimport is idempotent; tier routing correct.* [Source: FR-SA19]
- [ ] **AC6** (error-path) — Given a parse failure, When it occurs, Then visible progress shows an honest failure state (no silent partial). *Assertion: failure → explicit user-visible failure, resumable.* [Source: FR-SA19]

## 🔨 Tasks
- [ ] Guided per-provider wizard + resumable pending-import state (AC: 1)
- [ ] Resumable S3 multi-GB upload + Time Window (AC: 2)
- [ ] Per-provider parse adapters (ChatGPT/Claude; Gemini/Grok/Copilot Beta) (AC: 3)
- [ ] Parse as central-workflow step-APIs; emit `studio.conversation.imported` (AC: 4)
- [ ] Dedupe-on-reimport + tier routing (Free→history, £7→Curator) (AC: 5)
- [ ] Honest progress + failure states (AC: 6)
- [ ] Write `TestImportParseIdempotentViaWorkflow` (RED→GREEN) (AC: 4,5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — deterministic transcript parse + enrich-degrade + draft staging/commit implemented + tested -- `src/import/parseTranscript.ts`, `enrichTitle.ts`, `src/routes/import.ts`, `test/chat-import-*`, `test/integration/ChatImportConvertsTranscriptToSkillDraft.spec.ts`. Missing: per-provider parse adapters + workflow-orchestrated idempotent import.

Tier placement: route → application (wizard) → step-API surface consumed by workflows → S3 port. Seam crossed: workflows (parse pipeline), knowledge (chat-history indexing), Curator (E5, £7 distillation feed). Sibling dependency: E6-S1 (store), E9-S4 (step-APIs), E3-S4 (import `valid_from` origin metadata). Search over Conversations is knowledge's corpus.

## 🧪 Testing
- Named DoD test: `TestImportParseIdempotentViaWorkflow` (integration, real archives).
- Tiers: unit (adapter parse, dedupe key) + integration (workflow legs). CS §5 mocking: mock workflows + knowledge + S3 externals; never mock own adapters.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7/10 (idempotency, resumability, no in-service worker); SE-Arch 8sYi523i4t lenses (partial-import, active-branch correctness); cell-lint JGAUQRsw2g (per-cell S3).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA19, NFR-SA8); Architecture `ekTh7nDQqo` (A-DELEGATE workflows).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; import step-API + `studio.conversation.imported` pinned there).
- [ ] Blocked by: parent epic **E6**; E6-S1 (store), E9-S4 (step-APIs), E3-S4 (`valid_from`); workflows import pipeline.
- [ ] Blocks: E6-S3 (paste import shares dedupe), E5-S4 (£7 distillation feed).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
