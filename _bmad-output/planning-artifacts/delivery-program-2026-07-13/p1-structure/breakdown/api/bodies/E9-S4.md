## 🎯 Story

As **central workflows**, I want **idempotent, dual-gated step-APIs and callbacks with strict delegation discipline, plus the family degradation matrix honored**, so that every durable leg (provision, Graduation, import, distillation, retention, purge) runs safely without studio-api ever running a worker, and satellite outages degrade features per contract.

**Definition of Done:** one named test **`TestStepApisIdempotentDelegationSafe`** (integration layer — calls a step-API twice with the same Idempotency-Key and asserts exactly-once effect, that the raw caller token never rides workflow inputs (durable legs carry identity-minted delegation), callbacks are `WorkloadIdentityOnly`/dual-gated, and that a simulated ai/knowledge/billing/engine/workflows outage degrades per the NFR-SA5 matrix while the manual product stays up, through the step-API + degradation interfaces). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the step-APIs (provision/deprovision, Graduation, import parse/ingest/dedupe, distillation batches, retention sweeps, purge extensions), When called, Then each is idempotent (lib FR-L19) with typed retryable/terminal envelopes. *Assertion: duplicate Idempotency-Key → single effect; envelope typed.* [Source: FR-SA32]
- [ ] **AC2** — Given callbacks, When invoked, Then they are `WorkloadIdentityOnly`/dual-gated. *Assertion: non-workload caller → refused.* [Source: FR-SA32]
- [ ] **AC3** — Given durable legs, When they run, Then the raw caller token never rides workflow inputs; identity-minted delegation is carried instead (ai D-AI10 discipline). *Assertion: workflow inputs contain no raw caller token.* [Source: FR-SA32]
- [ ] **AC4** — Given no in-service Temporal (D-WF-1/NFR-SA8), When scanned, Then no `@temporalio/client` origination and no worker exist. *Assertion: CI grep finds zero Temporal client origination.* [Source: FR-SA32, NFR-SA8]
- [ ] **AC5** — Given the NFR-SA5 matrix, When a satellite is down, Then: ai down ⇒ £7 AI features fail typed (Free unaffected); knowledge down ⇒ degraded search; billing down ⇒ last-known TTL then fail-closed on paid; engine down ⇒ wiki surfaces fail typed; workflows down ⇒ async legs queue+resume. *Assertion: each outage degrades exactly per the matrix; manual product stays up.* [Source: NFR-SA5]
- [ ] **AC6** — Given cost discipline, When any model spend occurs, Then it bills through ai's per-tenant/per-caller keys; studio-api holds no model credential post-cutover. *Assertion: no model credential in the repo; spend attributed to ai keys.* [Source: NFR-SA6]

## 🔨 Tasks
- [ ] Expose idempotent step-APIs (typed retryable/terminal envelopes) for all durable legs (AC: 1)
- [ ] Dual-gate callbacks (`WorkloadIdentityOnly`) (AC: 2)
- [ ] Delegation discipline: no raw token in workflow inputs (AC: 3)
- [ ] CI grep banning Temporal client origination (AC: 4)
- [ ] Implement + test the NFR-SA5 degradation matrix (AC: 5)
- [ ] Cost discipline: no model credential; spend via ai keys (AC: 6)
- [ ] Write `TestStepApisIdempotentDelegationSafe` (RED→GREEN) (AC: 1,3,5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — `WorkflowsClient` step methods throw NotImplemented; no step-APIs/callbacks mounted; no degradation matrix.

Tier placement: step-API surface (consumed by workflows) + application (degradation policy). Seam crossed: workflows (all durable legs), identity (delegation), ai/knowledge/billing/engine (degradation). Sibling dependency: E6-S2 (import legs), E7-S2 (Graduation), E6-S1 (purge), E5-S4 (distillation). studio-api runs no worker (D-WF-1) — it is callback/step-API target only.

## 🧪 Testing
- Named DoD test: `TestStepApisIdempotentDelegationSafe` (integration, multi-outage fault injection).
- Tiers: unit (idempotency, envelope, delegation) + integration (degradation matrix). CS §5 mocking: mock every satellite external incl. outage modes; never mock own step-APIs.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7/11 (idempotency, dual-gate, delegation, degradation); SE-Arch 8sYi523i4t lenses (token-in-workflow-input, fail-open degrade, hidden worker); cell-lint JGAUQRsw2g (satellites cluster-local, rule 11 idempotency).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA32, NFR-SA5, NFR-SA6, D-WF-1); Architecture `ekTh7nDQqo` (A-DELEGATE workflows, §1 no-worker).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; step-API shapes + delegation contract pinned there).
- [ ] Blocked by: parent epic **E9**; E1 (auth/events); workflows step-API/callback contracts.
- [ ] Blocks: E6-S2 (import legs), E7-S2 (Graduation), E6-S1 (purge), E5-S4 (distillation).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
