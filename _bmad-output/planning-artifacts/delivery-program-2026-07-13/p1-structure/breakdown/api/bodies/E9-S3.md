## 🎯 Story

As the **MCP gateway**, I want **the Studio domain APIs my tools terminate at, tier-enforced server-side and rate-limited**, so that `search_memory`/`add_memory`/`update_memory`/`submit_to_curator`/`capture_conversation` are safe defense-in-depth even though MCP also checks tiers at its boundary.

**Definition of Done:** one named test **`TestMcpDomainApisTierEnforcedPrivacyExcluded`** (integration layer — calls the MCP-facing memory search/add/update, capture/submit, and capture_conversation APIs and asserts memory search hard-excludes private items, tier checks enforce server-side here as well as at the MCP boundary, capture terminates through engineGate, and each API is rate-limited per user, through the domain API interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the MCP Studio tools, When they call, Then memory search/add/update, capture/submit (through `engineGate()`), and capture_conversation ingestion into Chat History are served as domain APIs. *Assertion: each tool's domain API exists and behaves per contract.* [Source: FR-SA30]
- [ ] **AC2** — Given `search_memory`, When called, Then private items are hard-excluded (never reached). *Assertion: MCP memory search returns zero private/shared-private items.* [Source: FR-SA30, FR-SA7]
- [ ] **AC3** — Given any MCP-facing API, When called, Then tier enforcement runs server-side here as well as at the MCP boundary (defense in depth). *Assertion: unentitled MCP call → denied at studio-api even if MCP passed it.* [Source: FR-SA30, NFR-SA3]
- [ ] **AC4** — Given capture/submit, When called, Then it terminates through `engineGate()` (Card Contract v1 conformance). *Assertion: MCP capture → engineGate, no bypass.* [Source: FR-SA30, FR-SA12]
- [ ] **AC5** — Given every MCP-facing API, When called, Then it is rate-limited per user. *Assertion: over-rate calls → throttled.* [Source: FR-SA30]
- [ ] **AC6** (negative) — Given a forwarded bearer for another principal, When used, Then cross-principal access is refused. *Assertion: token/principal mismatch → denied (no cross-principal read).* [Source: NFR-SA3]

## 🔨 Tasks
- [ ] Serve the MCP-facing domain APIs (memory search/add/update, capture/submit, capture_conversation) (AC: 1)
- [ ] Hard-exclude private in memory search (AC: 2)
- [ ] Server-side tier enforcement (defense in depth) (AC: 3)
- [ ] Route capture through engineGate (AC: 4)
- [ ] Per-user rate limiting (AC: 5)
- [ ] Cross-principal refusal (AC: 6)
- [ ] Write `TestMcpDomainApisTierEnforcedPrivacyExcluded` (RED→GREEN) (AC: 2,3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no MCP-facing domain APIs, tier-enforcement, or rate-limiting.

Tier placement: route (MCP-facing domain APIs) → application → Memory/Curator/Chat-History domains. Seam crossed: mcp (protocol front-door terminates here with the caller's forwarded bearer). Sibling dependency: E3-S3 (privacy exclusion), E5-S1 (engineGate), E6-S1 (chat history), E9-S1 (tier). Private never over MCP (FR-56/65 hard exclusions).

## 🧪 Testing
- Named DoD test: `TestMcpDomainApisTierEnforcedPrivacyExcluded` (integration).
- Tiers: unit (rate limit, tier gate) + integration (privacy exclusion, engineGate). CS §5 mocking: mock the MCP boundary + billing externals; never mock own domain APIs.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7/11 (defense-in-depth, privacy exclusion, rate limiting); SE-Arch 8sYi523i4t lenses (MCP privacy leak, tier-bypass); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA30, FR-SA7, NFR-SA3); Architecture `ekTh7nDQqo` (A-DELEGATE mcp, A-PRIVACY).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; MCP domain API + FR-65 conformance suite pinned there).
- [ ] Blocked by: parent epic **E9**; E3-S3 (privacy), E5-S1 (gate), E6-S1 (chat history), E9-S1 (tier).
- [ ] Blocks: none (mcp consumes this).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
