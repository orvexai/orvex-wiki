## 🎯 Story

As the **capture authority**, I want **`engineGate()` enforcing the frozen Card Contract v1 resolution order and invariants**, so that every capture path lands as a strict `CardProposal`, inside-resolved fields are recomputed by studio-api, and no write ever bypasses the gate.

**Definition of Done:** one named test **`TestEngineGateResolutionOrderAndInvariants`** (integration layer — submits a `CardProposal` and asserts studio-api strips + recomputes inside-resolved fields, runs find-before-create → op-reclassify → dedup → sensitivity → divert in that order, and enforces the invariants (sensitivity-gated always stages and overrides the trust dial; direct path is wiki-shelf adds-only; Memory/Trash never accept a direct write), through the capture API). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given any capture (MCP submit/capture, imports, quick-paste, native), When it enters, Then it lands as a `.strict()` `CardProposal` (outside-proposed fields only; other fields rejected). *Assertion: extra/inside fields → `.strict()` rejection.* [Source: FR-SA12]
- [ ] **AC2** — Given a valid proposal, When gated, Then studio-api strips + recomputes inside-resolved/system fields (target/dedup/sensitivity/status). *Assertion: caller-supplied inside fields are ignored and recomputed.* [Source: FR-SA12]
- [ ] **AC3** — Given the gate, When it runs, Then find-before-create → op-reclassify → dedup → sensitivity → divert execute in exactly the frozen order. *Assertion: order matches the frozen resolution sequence.* [Source: FR-SA12]
- [ ] **AC4** — Given sensitivity-gated content, When resolved, Then it always stages and overrides the trust dial. *Assertion: sensitive → staged regardless of dial position.* [Source: FR-SA12]
- [ ] **AC5** (negative) — Given a direct-write attempt to Memory or Trash, When submitted, Then it is refused; the direct path is wiki-shelf adds-only. *Assertion: direct Memory/Trash write → rejected.* [Source: FR-SA12]
- [ ] **AC6** (error-path) — Given a Card Contract change, When attempted, Then it requires `contract_version: 2` + coordinated migration; new Studio MCP tools conform, never fork. *Assertion: contract change without version bump → refused.* [Source: FR-SA12]

## 🔨 Tasks
- [ ] Port `engineGate()` + `.strict()` `CardProposal` schema (AC: 1)
- [ ] Strip + recompute inside-resolved/system fields (AC: 2)
- [ ] Enforce the frozen resolution order (AC: 3)
- [ ] Sensitivity-gated always-stage + dial override (AC: 4)
- [ ] Direct-path invariants (wiki-shelf adds-only; Memory/Trash never direct) (AC: 5)
- [ ] Card Contract v1 versioning guard (AC: 6)
- [ ] Write `TestEngineGateResolutionOrderAndInvariants` (RED→GREEN) (AC: 3,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — Card-Contract-v1 resolution present + tested (`src/cards/card.ts`, `test/cards/`). Missing: the `engineGate()` merge resolution-order + invariants.

Tier placement: application (gate orchestration) → domain (Card) → Memory/wiki ports. Seam crossed: ai classify + knowledge dedup are delegated (E5-S4 wires them; here the gate structure + deterministic gates are local). Sibling dependency: E3 (Memory target), E8-S1 (wiki-shelf apply). The Card Contract v1 schema home is shared with mcp (contracts).

## 🧪 Testing
- Named DoD test: `TestEngineGateResolutionOrderAndInvariants` (integration).
- Tiers: unit (resolution order, invariants) + integration (capture paths). CS §5 mocking: mock ai classify + knowledge dedup ports as external; never mock own gate. The shipped Curator goldens must pass.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/7 (frozen contract, authority model, strict validation); SE-Arch 8sYi523i4t lenses (gate-bypass, field-authority); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA12, D-SA7); Architecture `ekTh7nDQqo` (A-CURATOR).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; Card Contract v1 schema home pinned there).
- [ ] Blocked by: parent epic **E5**; E3 (Memory), E8-S1 (wiki apply).
- [ ] Blocks: E5-S2/S3/S4/S5, E9-S3 (MCP capture terminates here).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any Card Contract change (contract_version:2).
