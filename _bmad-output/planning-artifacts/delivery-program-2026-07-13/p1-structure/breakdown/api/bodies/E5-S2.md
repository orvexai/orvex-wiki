## 🎯 Story

As a **curating user**, I want a **Curation Queue with grouped proposals, per-group bulk approve/reject, diff previews, and provenance**, so that I can review capture efficiently at scale without a dangerous global approve-all.

**Definition of Done:** one named test **`TestCurationQueuePerGroupBulkNoGlobalApprove`** (integration layer — seeds 100+ grouped proposals and asserts per-group bulk approve/reject works, a global approve-all endpoint does not exist, diff previews + provenance render, and the queue stays interactive at scale, through the queue API). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given captured proposals, When queued, Then they are grouped by topic/conversation cluster. *Assertion: proposals carry a group key; queue lists by group.* [Source: FR-SA13]
- [ ] **AC2** — Given a group, When bulk-actioned, Then per-group-only bulk approve/reject works and no global approve-all exists (the Phase-1 endpoint retired/repurposed). *Assertion: global approve-all endpoint returns 404/removed.* [Source: FR-SA13]
- [ ] **AC3** — Given a proposal, When previewed, Then a diff-style preview + provenance to the source conversation renders. *Assertion: preview shows before/after diff + source link.* [Source: FR-SA13]
- [ ] **AC4** — Given an approval, When recorded, Then an approval event is emitted. *Assertion: approve → recorded approval event (audit).* [Source: FR-SA13]
- [ ] **AC5** — Given 100+ / 1k / 10k pending, When browsed, Then the queue stays interactive (≤200ms p95 interaction, virtualization). *Assertion: interaction p95 ≤200ms at the FR-54 scale gates.* [Source: FR-SA13, NFR-SA1]
- [ ] **AC6** (negative) — Given a bulk-approve spanning two groups, When attempted, Then it is refused (per-group-only). *Assertion: cross-group bulk → rejected.* [Source: FR-SA13]

## 🔨 Tasks
- [ ] Grouped-proposal model (topic/conversation cluster) (AC: 1)
- [ ] Per-group bulk approve/reject; remove global approve-all (AC: 2,6)
- [ ] Diff previews + provenance links (AC: 3)
- [ ] Recorded approval events via outbox (AC: 4)
- [ ] Virtualized queue interactive at 100/1k/10k (AC: 5)
- [ ] Write `TestCurationQueuePerGroupBulkNoGlobalApprove` (RED→GREEN) (AC: 2,5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no Curation Queue (grouped proposals / per-group bulk).

Tier placement: route → application (queue) → domain (proposals, CAS-bearing) → Postgres + outbox. Seam crossed: none new. Sibling dependency: E5-S1 (gate produces proposals), E1-S4 (approval events). Scale-seeded E2E lands in the closing gates (NFR-SA1).

## 🧪 Testing
- Named DoD test: `TestCurationQueuePerGroupBulkNoGlobalApprove` (integration + scale seed).
- Tiers: unit (grouping, per-group guard) + integration (queue, scale). CS §5 mocking: mock nothing own; seed real proposals.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/10 (scale, no dangerous bulk, audit); SE-Arch 8sYi523i4t lenses (global-approve foot-gun, queue perf); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA13, NFR-SA1); Architecture `ekTh7nDQqo` (A-CURATOR).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E5**; E5-S1 (gate), E1-S4 (events).
- [ ] Blocks: E5-S3 (write modes act on queue items).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
