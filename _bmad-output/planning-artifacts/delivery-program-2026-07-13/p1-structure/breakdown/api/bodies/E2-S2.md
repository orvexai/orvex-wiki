## 🎯 Story

As a **Studio user**, I want **Library and Collections persistence plus the Builder edit surface**, so that I can save, organize, bulk-export and edit my skills across devices with Base immutability and Delta autosave.

**Definition of Done:** one named test **`TestBuilderForkMintsDelta`** (integration layer — edits a Base skill via the Builder API and asserts a Delta is minted (Base unchanged), autosave persists, and a bulk-export returns the collection, through the library/builder API). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a user's skills, When saved/organized, Then Library + Collections persist to Postgres with cross-device last-write-wins. *Assertion: save then read from a second session returns the item.* [Source: FR-SA2]
- [ ] **AC2** — Given a collection, When bulk-export is requested, Then all member skills export together. *Assertion: export contains every collection member.* [Source: FR-SA2]
- [ ] **AC3** — Given a Base skill, When edited/forked in the Builder, Then a Delta is minted and the Base stays immutable. *Assertion: post-edit, Base bytes unchanged; a Delta row exists.* [Source: FR-SA2]
- [ ] **AC4** — Given the Builder, When a user types, Then autosave persists without an explicit save. *Assertion: after autosave interval, edits are durable.* [Source: FR-SA2]
- [ ] **AC5** (negative) — Given a direct write to a Base skill, When attempted, Then it is rejected (Base immutability). *Assertion: Base mutation → error.* [Source: FR-SA2]
- [ ] **AC6** (error-path) — Given a concurrent cross-device edit, When both save, Then last-write-wins resolves deterministically without data corruption. *Assertion: concurrent saves converge, no partial merge.* [Source: FR-SA2]

## 🔨 Tasks
- [ ] Port Library/Collections model + Postgres persistence (LWW) (AC: 1,6)
- [ ] Implement bulk-export (AC: 2)
- [ ] Builder edit surface: Delta mint on fork/edit, Base immutability (AC: 3,5)
- [ ] Autosave persistence (AC: 4)
- [ ] Write `TestBuilderForkMintsDelta` (RED→GREEN) (AC: 3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — own-skills Library feed (`src/routes/library.ts`) + generic draft/commit/discard edit surface (`src/drafts/`, `src/routes/drafts.ts`) present + tested (`test/draft-store.test.ts`, `test/drafts-*.test.ts`). Missing: the Collections model + Builder fork/delta minting.

Tier placement: route → application → domain (skill) → Postgres port. Seam crossed: none external (local domain). Sibling dependency: E2-S1 (skill model + Base/Delta). Search over Library is delegated to knowledge (E8-S2 pattern) — not owned here.

## 🧪 Testing
- Named DoD test: `TestBuilderForkMintsDelta` (integration).
- Tiers: unit (LWW, immutability) + integration (persistence, export). CS §5 mocking: mock the Postgres port only for unit; real DB for integration. Never mock own packages.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/6 (boundaries, concurrency, immutability); SE-Arch 8sYi523i4t lenses (LWW correctness); cell-lint JGAUQRsw2g (per-cell store).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA2); Architecture `ekTh7nDQqo` (A-OWN).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E2**; E2-S1 (skill model), E1 (persistence).
- [ ] Blocks: E2-S3 (publish from Library).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
