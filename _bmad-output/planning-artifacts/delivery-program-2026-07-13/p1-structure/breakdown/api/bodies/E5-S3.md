## 🎯 Story

As a **user across tiers**, I want **tiered Curator write modes (Free direct / £7 direct / £7 curated) with every write reversible**, so that the Free tier works with zero LLM calls and no write path ever reaches wiki/Memory around the gate.

**Definition of Done:** one named test **`TestFreeDirectWriteZeroLlmAndReversible`** (integration layer — runs a Free direct write and asserts only non-AI gates fire (deterministic dedup, find-before-create, pattern sensitivity heuristics, validation) with zero LLM calls, that the write is reversible via trash/undo, and that no write path reaches wiki/Memory around the gate — probed via MCP and import paths, through the write-mode interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given Free tier, When a direct write runs, Then only non-AI gates fire (deterministic dedup, find-before-create, pattern sensitivity heuristics, validation), adds + updates, zero LLM calls. *Assertion: Free write makes zero model calls (cost-asserted).* [Source: FR-SA14, G2]
- [ ] **AC2** — Given £7 direct, When a write runs, Then AI gates fire via delegated primitives; sensitive/edits stage. *Assertion: £7 direct sensitive/edit → staged.* [Source: FR-SA14]
- [ ] **AC3** — Given £7 curated (default), When a write runs, Then everything stages. *Assertion: curated mode → all writes staged.* [Source: FR-SA14]
- [ ] **AC4** — Given any write, When performed, Then it is reversible (trash/undo, non-AI). *Assertion: any write → undo restores prior state.* [Source: FR-SA14]
- [ ] **AC5** (negative) — Given an MCP or import path, When it attempts a write, Then no path reaches wiki/Memory around the gate. *Assertion: adversarial MCP + import writes all pass through the gate.* [Source: FR-SA14]
- [ ] **AC6** (error-path) — Given a tier check failure, When a write is attempted, Then it is refused server-side (client hiding is cosmetic). *Assertion: entitlement-denied write → 402/403 at the API.* [Source: FR-SA14, FR-SA27]

## 🔨 Tasks
- [ ] Implement the three write modes over the gate (AC: 1,2,3)
- [ ] Free-direct: non-AI gates only, cost-asserted zero LLM (AC: 1)
- [ ] Reversibility (trash/undo, non-AI) (AC: 4)
- [ ] Adversarial no-bypass proof via MCP + import (AC: 5)
- [ ] Server-side tier enforcement on write (AC: 6)
- [ ] Write `TestFreeDirectWriteZeroLlmAndReversible` (RED→GREEN) (AC: 1,5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — generic reversible draft/commit/discard (`src/drafts/`) + zero-LLM deterministic render present + tested. Missing: the tiered write-mode ladder itself.

Tier placement: application (write-mode policy over engineGate). Seam crossed: billing (tier check, E9-S1), ai/knowledge (£7 gates, E5-S4). Sibling dependency: E5-S1 (gate), E5-S2 (queue), E9-S1 (entitlement). The Free path is the SM-C5 ≈0 standing-cost guarantee.

## 🧪 Testing
- Named DoD test: `TestFreeDirectWriteZeroLlmAndReversible` (integration, adversarial).
- Tiers: unit (mode selection, cost assertion) + integration (no-bypass). CS §5 mocking: mock ai/knowledge/billing externals; never mock own gate.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/11 (tier enforcement server-side, no-bypass, cost); SE-Arch 8sYi523i4t lenses (gate-bypass adversarial); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA14, FR-SA27); Architecture `ekTh7nDQqo` (A-CURATOR).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E5**; E5-S1 (gate), E9-S1 (tier).
- [ ] Blocks: E5-S4 (delegated primitives feed £7 modes).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
