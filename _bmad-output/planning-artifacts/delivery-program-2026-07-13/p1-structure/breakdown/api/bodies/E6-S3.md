## 🎯 Story

As a **user**, I want **paste-prompt quick import**, so that I can drop a chunk of conversation text and have it parsed to memory-grade material, routed per tier, provenance-stamped, and dedupe-compatible with a later full import.

**Definition of Done:** one named test **`TestPasteImportRoutesPerTierDedupeCompatible`** (integration layer — pastes conversation text and asserts a universal extraction prompt parses it to memory-grade material, Free routes to direct Memory adds while £7/trial queues proposals, provenance is stamped, and the result dedupes against a later full import, through the paste-import interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given pasted text, When submitted, Then the universal extraction prompt parses it to memory-grade material. *Assertion: paste → structured memory-grade candidates.* [Source: FR-SA20]
- [ ] **AC2** — Given Free tier, When paste-import runs, Then it makes direct Memory adds (via the gate's non-AI path). *Assertion: Free paste → direct Memory add through engineGate.* [Source: FR-SA20, FR-SA14]
- [ ] **AC3** — Given £7/trial, When paste-import runs, Then it queues proposals. *Assertion: £7 paste → Curation Queue proposals.* [Source: FR-SA20]
- [ ] **AC4** — Given any paste-import, When ingested, Then it is provenance-stamped. *Assertion: each item carries paste provenance.* [Source: FR-SA20]
- [ ] **AC5** — Given a later full import overlapping the paste, When run, Then dedupe prevents duplicates. *Assertion: overlapping full import → no duplicate of the pasted item.* [Source: FR-SA20]
- [ ] **AC6** (negative) — Given unparseable paste, When submitted, Then a typed error is returned (no partial garbage memory). *Assertion: junk paste → typed error, no memory written.* [Source: FR-SA20]

## 🔨 Tasks
- [ ] Universal extraction prompt → memory-grade parse (AC: 1)
- [ ] Tier routing (Free direct add / £7 proposals) through the gate (AC: 2,3)
- [ ] Provenance stamping (AC: 4)
- [ ] Dedupe-compatibility with full import (AC: 5)
- [ ] Typed error on unparseable paste (AC: 6)
- [ ] Write `TestPasteImportRoutesPerTierDedupeCompatible` (RED→GREEN) (AC: 2,5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no paste-prompt quick-import per-tier routing (the single `/v1/import/chat` transcript path exists, but not this surface).

Tier placement: route → application (paste extraction) → engineGate (E5-S1) → Memory/queue. Seam crossed: ai (extraction for £7; Free uses deterministic non-AI path), knowledge (dedupe). Sibling dependency: E5-S1 (gate), E6-S2 (shared dedupe key), E9-S1 (tier). This is FR-47a (Beta).

## 🧪 Testing
- Named DoD test: `TestPasteImportRoutesPerTierDedupeCompatible` (integration).
- Tiers: unit (parse, dedupe key, tier route) + integration (gate + dedupe). CS §5 mocking: mock ai + knowledge externals; never mock own gate.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7 (tier routing, dedupe, provenance); SE-Arch 8sYi523i4t lenses (duplicate-on-reimport, junk-input); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA20, FR-47a); Architecture `ekTh7nDQqo` (A-CURATOR).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E6**; E5-S1 (gate), E6-S2 (dedupe), E9-S1 (tier).
- [ ] Blocks: none (leaf import surface).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
