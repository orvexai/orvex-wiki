## 🎯 Story

As a **£7 user**, I want **LLM-free enrichment orchestration under a context budget**, so that my prompts are enriched from Memory + wiki, relevance-ranked, in ≤500ms p95 added — and if over budget, they ship unenriched rather than stall.

**Definition of Done:** one named test **`TestEnrichmentBudgetAndLlmFree`** (integration layer — enriches a Prompt-Use action with Memory + wiki sources present and asserts retrieval-and-templating adds ≤500ms p95, over-budget ships unenriched, and the enrichment path makes zero model calls, through the enrichment interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a Prompt-Use action, When enriched, Then sources = Memory (first-class, local) + wiki (via knowledge), relevance-ranked under a context budget. *Assertion: both sources contribute; ranking + budget applied.* [Source: FR-SA9]
- [ ] **AC2** — Given the hot path, When enrichment runs, Then it is LLM-free (retrieval-and-templating only). *Assertion: enrichment makes zero model calls.* [Source: FR-SA9, NFR-SA6]
- [ ] **AC3** — Given the budget, When enrichment completes, Then it adds ≤500ms p95. *Assertion: added latency p95 ≤500ms warm.* [Source: FR-SA9, NFR-SA1]
- [ ] **AC4** — Given over-budget, When the deadline passes, Then the prompt ships unenriched, never stalling. *Assertion: over-budget → unenriched output returned within deadline.* [Source: FR-SA9]
- [ ] **AC5** — Given the search-fronting hop, When first results are fetched, Then ≤300ms p95 warm despite the knowledge hop. *Assertion: search fronting p95 ≤300ms.* [Source: NFR-SA1]
- [ ] **AC6** (negative) — Given no relevant sources, When enrichment runs, Then nothing is injected (no noise). *Assertion: below-threshold relevance → no injection.* [Source: FR-SA9, FR-SA11]

## 🔨 Tasks
- [ ] Build enrichment orchestration: retrieval (Memory local + wiki via knowledge) + templating (AC: 1,2)
- [ ] Relevance ranking under a context budget (AC: 1,6)
- [ ] Enforce the ≤500ms deadline → ship-unenriched (AC: 3,4)
- [ ] Hold the ≤300ms search-fronting budget (AC: 5)
- [ ] Write `TestEnrichmentBudgetAndLlmFree` (RED→GREEN) (AC: 2,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no Prompt Composer enrichment orchestration / context-budget logic (chat-import enrichTitle is a separate concern).

Tier placement: application (enrichment orchestrator) → Memory domain (local) + knowledge port (wiki). Seam crossed: knowledge (wiki corpus retrieval). Sibling dependency: E3 (Memory), E8-S2 (wiki corpus), E9-S1 (£7 gate). The stored artifact stays clean (E2-S4 enforces no-persist). This is the £7 Composer superseding the Free FR-31 weave (E3-S5) when active.

## 🧪 Testing
- Named DoD test: `TestEnrichmentBudgetAndLlmFree` (integration).
- Tiers: unit (ranking, budget deadline) + integration (Memory + knowledge). CS §5 mocking: mock the knowledge retrieval external service (incl. slow path for budget test); never mock own Memory/orchestrator.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/6/10 (deadline discipline, no-egress hot path, latency SLI); SE-Arch 8sYi523i4t lenses (budget stall, noise injection); cell-lint JGAUQRsw2g (knowledge via cluster-local svc).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA9, NFR-SA1); Architecture `ekTh7nDQqo` (§6 satellite-hop latency).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; knowledge retrieval contract + budget shape pinned there).
- [ ] Blocked by: parent epic **E4**; E3 (Memory), E8-S2 (wiki), E9-S1 (£7 gate).
- [ ] Blocks: E4-S2, E4-S3, E2-S4 (enriched surfaces).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
