## 🎯 Story

As a **Studio user**, I want **bi-temporal Memory with an explicit state machine, Reconcile-not-overwrite, retention/decay, scoped deletion, and the confirm gate as the sole mutation path**, so that superseded facts never surface as current and no un-curated write ever reaches canon.

**Definition of Done:** one named test **`TestReconcileInvalidatesByWorldTime`** (integration layer — submits a contradicting import card with a lower `valid_from` than an active card and asserts Reconcile orders strictly by `valid_from` (world-time), invalidates-not-deletes the older assertion reversibly, and that the change flows only through the confirm gate — a direct write is impossible — through the lifecycle interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a memory, When stored, Then it carries `observed_at` + `valid_from`/`valid_until`; `valid_from` is the fact's real-world time from the capture adapter's origin metadata, defaulting to `observed_at` only for live capture. *Assertion: temporal fields present; import uses origin `valid_from`.* [Source: FR-L1, AD-3]
- [ ] **AC2** — Given a contradiction, When resolved, Then Reconcile invalidates (never deletes) the older assertion and is reversible; ordering is strictly by `valid_from` (world-time), never ingest time. *Assertion: an import may never supersede a higher-`valid_from` active card.* [Source: FR-L2, AD-3]
- [ ] **AC3** — Given a memory, When its lifecycle runs, Then states are `active | superseded | archived` consistent with the wiki's draft→canonical discipline. *Assertion: state transitions match the machine.* [Source: FR-L3, AD-3]
- [ ] **AC4** — Given a retention/decay policy, When configured, Then staleness signals + optional expiry exist under user control. *Assertion: decay policy is user-settable and applied.* [Source: FR-L4]
- [ ] **AC5** — Given deletion, When invoked, Then every Orvex-controlled copy and every vendor-API-deletable synced-out memory is erased, and the injected-into-session boundary is disclosed at consent time. *Assertion: delete cascades to controllable copies; disclosure recorded.* [Source: FR-L5]
- [ ] **AC6** — Given the confirm gate, When any create/edit/delete/reconcile occurs, Then it flows through that single gate; a ChangeSet computed against version V is rebased (not force-committed) if the card moved to V+1; `api` applies the supersede atomically on commit and never reconciles independently. *Assertion: no direct-write side channel; stale ChangeSet rebases.* [Source: AD-4]

## 🔨 Tasks
- [ ] Add bi-temporal fields + origin-metadata `valid_from` sourcing (AC: 1)
- [ ] Reconcile by world-time, invalidate-not-delete, reversible (AC: 2)
- [ ] State machine active/superseded/archived (AC: 3)
- [ ] Retention/decay policy under user control (AC: 4)
- [ ] Scoped deletion cascade + consent-time disclosure (AC: 5)
- [ ] Route all mutations through the confirm gate; rebase-on-V+1; atomic supersede-on-commit (AC: 6)
- [ ] Write `TestReconcileInvalidatesByWorldTime` (RED→GREEN) (AC: 2,6)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no bi-temporal lifecycle, Reconcile, or confirm-gate sole-mutation logic.

Tier placement: domain (lifecycle/state machine) + application (confirm-gate apply). Seam crossed: staging (ChangeSets land in orvex-studio-staging's review queue per AD-4; `api` applies on commit) — for E5-S5 this becomes the forwarding path. Sibling dependency: E3-S1 (schema), E5 (Curator/gate), E6 (import supplies `valid_from`). Retrieved/captured content is untrusted data, never instructions (anti-poisoning).

## 🧪 Testing
- Named DoD test: `TestReconcileInvalidatesByWorldTime` (integration).
- Tiers: unit (state machine, world-time ordering) + integration (gate apply, rebase). CS §5 mocking: mock the staging queue as external; never mock own lifecycle.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/6 (state machine correctness, atomic apply, CAS/rebase); SE-Arch 8sYi523i4t lenses (silent-overwrite, ingest-vs-world-time, anti-poisoning); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-L1–5); Architecture `ekTh7nDQqo` (AD-3, AD-4); Memory PRD `g9vWbSYplh`.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; staging ChangeSet shape pinned there).
- [ ] Blocked by: parent epic **E3**; E3-S1 (schema).
- [ ] Blocks: E5-S5 (staging supersession), E6-S2 (import `valid_from`).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
