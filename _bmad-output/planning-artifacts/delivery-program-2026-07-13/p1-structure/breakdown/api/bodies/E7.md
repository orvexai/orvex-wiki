## 🗺️ Epic — Demo World

Demo World is how Priya/Laura get value in under 90 seconds without signup — persona demo-data onboarding leading with teachers (the launch wedge, Brief audience ruling). This epic owns the **demo lifecycle orchestration** (FR-SA21): on profession declaration, seed the persona pack — Studio rows (Memory, Library) flagged as **Demo Data in the user's own tables** (PO fact: flags, not a separate tenant) + the demo wiki space via the engine's Demo Engine (FR-72) + indexing, all within the **≤60s budget**; every query/export/enrichment path respects the demo flag; edit-of-demo forks a user-owned copy; demo AI actions run live through the standard delegated paths under the **demo hard cap (≈20 actions)** and never touch real-data namespaces. And it owns **Graduation as a central saga** (FR-SA22): Studio demo-row clear + Demo Engine wiki clear + knowledge index removal, atomic-from-the-user's-perspective with visible retry; user-created items (incl. forks) verifiably survive; idempotent; the trial clock starts at Graduation (billing consumes `studio.user.graduated`).

Per D-S23, demo/starter content **counts against tenant quota, kept tiny (3–5 pages)** — studio-api handles the engine's over-quota 402 on demo seed (OQ-SA11 closed, build to it).

**Why (cites):** FR-SA21–22 (PRD `85qj2wwU2L`); Brief "persona demo-data onboarding with ~8 persona versions (teachers first)"; D-S23.

### Stories
- [ ] E7-S1 — Demo lifecycle orchestration + demo-flag discipline
- [ ] E7-S2 — Graduation central saga step-APIs

### FR ids owned
FR-SA21, FR-SA22.

### Definition of Done
- [ ] All two stories Done (reviewer ≠ implementer each).
- [ ] Family-E2E share green: a fresh tenant declares "teacher", the persona pack seeds ≤60s within quota, demo AI actions cap at ≈20, Graduation clears demo rows across Studio+wiki+knowledge atomically while a user-created fork survives, trial clock starts — observed on the dev cell.
- [ ] SDD lines evidenced: every query path respects the demo flag (adversarial test — no demo bleed into real namespaces); Graduation saga is idempotent (replay test).

### 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; Graduation step-API shapes + `studio.user.graduated` event pinned there).
- [ ] Blocked by: E1, E3 (Memory), E8-S1 (DocmostPort + Demo Engine), E9-S1/S2 (entitlement/metering seam for the demo cap), E9-S4 (workflow step-APIs); engine Demo Engine (FR-72); workflows Graduation saga.

### 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
