## 🎯 Story

As a **Studio user**, I want **Prompt-Use surfaces — copy-as-prompt, Five-AI export, share permalinks, and prompt-via-us launch**, so that I can take any skill into any AI, with enrichment applied per tier but the stored artifact always the clean unenriched prompt.

**Definition of Done:** one named test **`TestPromptUseArtifactStaysClean`** (integration layer — invokes copy-as-prompt and Five-AI export with enrichment active and asserts the persisted artifact is the clean unenriched prompt while the returned/exported payload carries the enrichment, through the prompt-use API). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a skill, When copy-as-prompt is used, Then the rendered prompt is returned for the clipboard. *Assertion: response is the deterministic render (+ tier enrichment).* [Source: FR-SA4]
- [ ] **AC2** — Given Five-AI export, When invoked, Then Claude/ChatGPT/Gemini/Grok/Copilot adapters produce provider-shaped output on the same adapter seam. *Assertion: each of the five adapters returns valid output.* [Source: FR-SA4]
- [ ] **AC3** — Given a share permalink, When created, Then it resolves to the shared prompt under the sharer's permissions. *Assertion: permalink resolves; access respects permissions.* [Source: FR-SA4]
- [ ] **AC4** — Given prompt-via-us launch (Beta), When invoked, Then it launches into the target AI. *Assertion: launch hands off to the provider surface.* [Source: FR-SA4]
- [ ] **AC5** — Given enrichment is active at any surface, When the artifact is stored, Then the stored artifact is the clean unenriched prompt (enrichment never persists). *Assertion: persisted prompt has no enrichment injected.* [Source: FR-SA4, FR-SA9]
- [ ] **AC6** (negative) — Given an unsupported provider, When export is requested, Then a typed error names it, no partial garbage. *Assertion: unknown provider → typed error.* [Source: FR-SA4]

## 🔨 Tasks
- [ ] Copy-as-prompt surface over the deterministic render (AC: 1)
- [ ] Five-AI export adapter seam (Claude/ChatGPT/Gemini/Grok/Copilot) (AC: 2,6)
- [ ] Share permalinks under sharer permissions (AC: 3)
- [ ] Prompt-via-us launch (Beta-flagged) (AC: 4)
- [ ] Enforce enrichment-never-persists on every surface (AC: 5)
- [ ] Write `TestPromptUseArtifactStaysClean` (RED→GREEN) (AC: 5)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no prompt-use surfaces (copy / five-AI export / permalinks / launch) exist in the tree.

Tier placement: route → application (prompt-use) → domain render + enrichment port (E4). Seam crossed: E4 enrichment (per-tier). Sibling dependency: E2-S1 (render), E4 (enrichment), E9-S1 (tier gate for enriched surfaces). The five-AI export extends the shipped Claude/ChatGPT adapter seam.

## 🧪 Testing
- Named DoD test: `TestPromptUseArtifactStaysClean` (integration).
- Tiers: unit (adapter shaping) + integration (surfaces + persistence). CS §5 mocking: mock the enrichment port for the clean-artifact assertion; never mock own render.

## 📏 Guidance
CS 6aMAzsYeQb §§0/4/5/6 (adapter seam, no-persist invariant); SE-Arch 8sYi523i4t lenses (enrichment-persistence leak); cell-lint JGAUQRsw2g (per-cell).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA4, FR-SA55/63 lineage); Architecture `ekTh7nDQqo` (A-OWN).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E2**; E2-S1 (render), E4-S1 (enrichment), E9-S1 (tier).
- [ ] Blocks: none downstream (leaf surface).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
