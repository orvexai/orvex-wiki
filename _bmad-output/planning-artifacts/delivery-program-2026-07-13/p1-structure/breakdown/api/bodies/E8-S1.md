## 🎯 Story

As **Studio**, I want the **DocmostPort typed client covering the engine surface Studio depends on, with CAS, history, and first-class over-quota 402 handling**, so that Curator applies, Demo Engine calls, capture, and the read pane all drive the wiki safely as a programmatic client.

**Definition of Done:** one named test **`TestDocmostPortCasAndQuota402`** (integration layer — exercises createSpace/createPage/updatePage-with-CAS/archive/restore/purge-gated/getPageHistory/listPages/mintMagicOtp/recentPagesForUser, asserts a CAS conflict surfaces `LostUpdateError`, and that an over-quota write yields the typed 402 `QUOTA_EXCEEDED` handled first-class — surface upgrade, stage the proposal, never retry-loop — through the DocmostPort). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the engine surface, When the port is built, Then it covers createSpace/createPage/updatePage-with-CAS/archive/restore/purge-gated/getPageHistory/listPages/mintMagicOtp-transport/recentPagesForUser. *Assertion: every named method exists + round-trips against the engine.* [Source: FR-SA23]
- [ ] **AC2** — Given a concurrent update, When CAS fails, Then `LostUpdateError` surfaces (no lost write). *Assertion: stale-CAS update → LostUpdateError.* [Source: FR-SA23]
- [ ] **AC3** — Given an over-quota write, When it hits the engine F-QUOTA chokepoint, Then a typed 402 `QUOTA_EXCEEDED` returns, handled first-class: surface the upgrade moment, stage the pending proposal, never retry-loop. *Assertion: 402 → upgrade surfaced + proposal staged + no retry.* [Source: FR-SA23]
- [ ] **AC4** — Given studio-api, When it writes, Then it does NOT re-enforce quota (engine is the sole chokepoint). *Assertion: no local quota enforcement in the write path.* [Source: FR-SA23, FR-SA28]
- [ ] **AC5** — Given the port, When it calls the engine, Then it assumes no Studio-specific engine behaviour (wiki is product-family-agnostic). *Assertion: port carries no Studio-only assumption.* [Source: FR-SA23]
- [ ] **AC6** (negative) — Given a quota-exceeded proposal, When staged, Then it is never dropped. *Assertion: over-quota proposal persists in the queue, not lost.* [Source: FR-SA23]

## 🔨 Tasks
- [ ] Build the DocmostPort typed client over the engine surface (AC: 1)
- [ ] CAS + `LostUpdateError` propagation (AC: 2)
- [ ] First-class 402 `QUOTA_EXCEEDED` handling (surface/stage/no-retry) (AC: 3,6)
- [ ] No local quota re-enforcement (AC: 4)
- [ ] Keep the port engine-agnostic (no Studio assumptions) (AC: 5)
- [ ] Write `TestDocmostPortCasAndQuota402` (RED→GREEN) (AC: 2,3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — `src/clients/wikiApi.ts` is a seam-only scaffold -- `writePage` throws NotImplemented; no CAS, history, or quota-402 handling.

Tier placement: adapter (DocmostPort) → engine over the network. Seam crossed: orvex-wiki engine (F-QUOTA chokepoint, engine PRD `EPsdD7uK8e`); re-point to orvex-wiki-api is OQ-SA6 (verify coverage first). Sibling dependency: E1-S2 (act-as-user for writes, E8-S3), E5 (Curator applies), E7 (Demo Engine). AGPL guard: all engine reuse crosses the network (NFR-SA7).

## 🧪 Testing
- Named DoD test: `TestDocmostPortCasAndQuota402` (integration against a real/faked engine).
- Tiers: unit (402 handling policy) + integration (CAS, surface coverage). CS §5 mocking: mock the engine external (incl. 402 + CAS-conflict); never mock own port.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/7 (typed client, error propagation, no-re-enforce); SE-Arch 8sYi523i4t lenses (retry-loop foot-gun, dropped-proposal); cell-lint JGAUQRsw2g (engine cluster-local).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA23, OQ-SA6); Architecture `ekTh7nDQqo` (A-DELEGATE orvex-wiki); engine PRD `EPsdD7uK8e`.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; DocmostPort surface + F-QUOTA 402 pinned there).
- [ ] Blocked by: parent epic **E8**; E1-S2 (auth); engine F-QUOTA + Demo Engine.
- [ ] Blocks: E5 (Curator applies), E7 (Demo seed), E8-S3 (read pane writes).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on the OQ-SA6 re-point target if unpinned.
