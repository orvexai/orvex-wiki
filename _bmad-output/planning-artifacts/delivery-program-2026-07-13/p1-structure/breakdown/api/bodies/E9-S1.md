## 🎯 Story

As a **user**, I want the **entitlement-check primitive consumed as the single tier authority with a live SSE entitlement channel**, so that every £7 capability is gated server-side and my paywall dissolves <60s after an upgrade without a reload.

**Definition of Done:** one named test **`TestEntitlementSseDissolvesPaywall`** (integration layer — checks a £7 capability server-side, then delivers `billing.entitlement.changed`, asserting the entitlement cache evicts and studio-api re-pushes over one SSE channel so the SPA sees the change <60s without reload, and that on billing outage the last-known entitlement holds within TTL and never fails open to paid, through the entitlement + SSE interfaces). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a £7 capability (Composer, Curator+distillation, Ask, Improve-with-AI, Memory Coach), When invoked, Then billing's entitlement-check primitive (fast, cached, fail-consistent, principal-keyed {user|org}) gates it server-side. *Assertion: unentitled call → denied at the API; client hiding is cosmetic.* [Source: FR-SA27]
- [ ] **AC2** — Given `billing.entitlement.changed`, When consumed, Then the entitlement cache evicts and studio-api re-pushes over one SSE entitlement channel; the SPA dissolves the paywall <60s without reload. *Assertion: change → SSE push → <60s no-reload update.* [Source: FR-SA27, D-S20]
- [ ] **AC3** — Given a billing outage, When entitlement is read, Then last-known within TTL is served, then fail-closed on paid features; never fail-open beyond TTL. *Assertion: outage → last-known within TTL; past TTL → paid features closed.* [Source: FR-SA27, NFR-SA5]
- [ ] **AC4** — Given downgrade/lapse, When it occurs, Then AI features lock and nothing is deleted; dunning is 14-day grace → auto-downgrade to Free. *Assertion: lapse → AI locked, data intact (D-S19).* [Source: FR-SA27]
- [ ] **AC5** (negative) — Given MCP, When it enforces tiers, Then it checks the same primitive at its own boundary (defense in depth). *Assertion: consistent decision across app/BFF/MCP for the same principal.* [Source: FR-SA27]
- [ ] **AC6** (error-path) — Given a stale entitlement cache, When a change arrives, Then no paid feature serves the stale-entitled state after eviction. *Assertion: post-eviction read reflects the new entitlement.* [Source: FR-SA27]

## 🔨 Tasks
- [ ] Entitlement-check primitive client (cached, principal-keyed, fail-consistent) (AC: 1)
- [ ] Server-side £7 gates at the API layer (AC: 1)
- [ ] SSE entitlement channel + cache eviction on `billing.entitlement.changed` (AC: 2,6)
- [ ] TTL last-known + fail-closed-past-TTL (AC: 3)
- [ ] Dunning-lapse: lock AI, delete nothing (AC: 4)
- [ ] Write `TestEntitlementSseDissolvesPaywall` (RED→GREEN) (AC: 2,3)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — SSE entitlement channel fully implemented + tested -- `src/events/entitlement.ts` (EntitlementChannel, sseHandler, cloudEventHandler), `test/sse.test.ts`, `test/tiers.test.ts`. Missing: the entitlement-check client primitive (`BillingClient.checkEntitlement` throws NotImplemented).

Tier placement: middleware (entitlement gate) + route (SSE channel) → billing port + Redis cache. Seam crossed: billing (entitlement primitive + events), mcp (same primitive at its boundary). Sibling dependency: E1-S5 (event consumption drives eviction), E1-S6 (Redis cache). The sync check is the low-latency read path; events are canonical propagation.

## 🧪 Testing
- Named DoD test: `TestEntitlementSseDissolvesPaywall` (integration).
- Tiers: unit (TTL/fail-closed logic) + integration (SSE + eviction). CS §5 mocking: mock billing external (incl. outage); use real Redis; never mock own gate.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/6/11 (fail-closed, cache eviction, defense-in-depth); SE-Arch 8sYi523i4t lenses (fail-open-to-paid, stale-entitlement); cell-lint JGAUQRsw2g (billing cluster-local, tenant-keyed cache).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA27, D-S19/S20); Architecture `ekTh7nDQqo` (A-DELEGATE billing).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; entitlement-check contract pinned there).
- [ ] Blocked by: parent epic **E9**; E1-S5 (event consume), E1-S6 (Redis); billing entitlement primitive.
- [ ] Blocks: every £7-gated story (E4, E5-S3, E8-S2), E9-S2.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
