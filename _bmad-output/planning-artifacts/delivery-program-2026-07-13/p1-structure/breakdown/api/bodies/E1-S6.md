## 🎯 Story

As a **Studio platform engineer**, I want the **Postgres/Redis/S3 store topology and house observability wired with the audit stream on the spine**, so that Studio's system-of-record is correctly split (no Mongo, no local pgvector) and every SLI/audit signal flows to the LGTM stack via console.

**Definition of Done:** one named test **`TestStoreTopologyAndAuditEmit`** (integration layer — asserts relational writes land in Postgres, Archives/exports in S3, entitlement cache in Redis with tenant-embedded keys, and that an approval/consent/retention action emits a CloudEvent to the family audit stream rather than journaling locally, through the persistence + audit interfaces). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the store split, When data is written, Then relational truth → Postgres (CNPG), Archives/raw transcripts/exports → S3 (RGW), event-evicted cache → Redis. *Assertion: no Mongo dependency present; pgvector absent (moved to knowledge).* [Source: FR-SA33, A-DATA]
- [ ] **AC2** — Given Redis cache keys, When written, Then they embed the tenant/user principal and never carry allow/deny decisions. *Assertion: cache key regex includes principal; no allow/deny value cached.* [Source: FR-SA33]
- [ ] **AC3** — Given telemetry, When emitted, Then metrics → Mimir via OTLP, ops logs → Loki as structured JSON (never console.log), traces → Tempo. *Assertion: no `console.log` in `src/`; OTLP exporter configured.* [Source: FR-SA34, CS §10]
- [ ] **AC4** — Given an approval/consent-grant/withdrawal/retention/admin op, When it occurs, Then it is published as a CloudEvent to the family audit stream, not journaled locally. *Assertion: audit action → spine event, no local journal table.* [Source: FR-SA34, A-DATA]
- [ ] **AC5** — Given the SLI set, When scraped, Then enrichment-latency, gate-resolution, queue depth/age, import progress, entitlement-staleness, DocmostPort error rate are exposed. *Assertion: each SLI metric name is present.* [Source: FR-SA34]
- [ ] **AC6** (negative) — Given no standing free-tier generative spend, When Free flows run, Then no model credential is held and no model call bills. *Assertion: Free path holds zero model credential (NFR-SA6).* [Source: NFR-SA6]

## 🔨 Tasks
- [ ] Provision Postgres claim + Redis + S3 bucket; assert no Mongo, no pgvector (AC: 1)
- [ ] Enforce tenant-embedded Redis keys, no allow/deny cached (AC: 2)
- [ ] Wire OTel → Mimir/Loki/Tempo; ban console.log in CI (AC: 3)
- [ ] Route audit actions to the spine audit stream (AC: 4)
- [ ] Export the SLI metric set (AC: 5)
- [ ] Write `TestStoreTopologyAndAuditEmit` (RED→GREEN) (AC: 1,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** partial — own Postgres stores real (skills/memory/drafts/demo, inline DDL in `src/store/postgres/`); structured JSON logs. Missing: OTel/observability, an audit stream, and a migration framework.

Tier placement: infra/adapter + ops. Seam crossed: LGTM (via console's plane), spine (audit). Sibling dependency: E1-S1 (boot), E1-S4 (outbox for audit events). The `/debug` presenter panel retires in favour of house observability.

## 🧪 Testing
- Named DoD test: `TestStoreTopologyAndAuditEmit` (integration with real Postgres/Redis/S3-compat).
- Tiers: unit (key-shape, log-format) + integration (stores + audit emit). CS §5 mocking: use real store adapters or testcontainers; mock nothing own.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/10 (store boundaries, telemetry split, structured logs); SE-Arch 8sYi523i4t lenses (Mongo self-contradiction, audit-vs-log); cell-lint JGAUQRsw2g (per-cell stores).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA33, FR-SA34, NFR-SA6); Architecture `ekTh7nDQqo` (A-DATA, §4 telemetry split).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; audit event schema pinned there).
- [ ] Blocked by: parent epic **E1**; E1-S1 (boot), E1-S4 (outbox for audit).
- [ ] Blocks: all data-owning stories rely on this topology.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
