## 🗺️ Epic — The Curator: engineGate, queue & staging cutover

The Curator is capture-and-curation — the productized Librarian ritual on the Studio side. This epic owns **`engineGate()`**, the frozen **Card Contract v1** authority: all capture (MCP submit/capture, imports, quick-paste, native pull) lands as a `.strict()` `CardProposal` (outside-proposed fields only); studio-api strips + recomputes inside-resolved fields and runs the frozen resolution order (find-before-create → op-reclassify → dedup → sensitivity → divert), enforcing the invariants — sensitivity-gated always stages and overrides the trust dial; the direct path is wiki-shelf adds-only; Memory/Trash never accept a direct write (FR-SA12). It owns the **Curation Queue** (grouped, per-group-only bulk, diff previews, efficient at 100/1k/10k — FR-SA13/NFR-SA1), the **tiered write modes** (Free direct non-AI gates / £7 direct / £7 curated — FR-SA14), **structure evolution** proposals (FR-SA15), the **delegated gate primitives** (ai bounded fail-closed classify, knowledge semantic dedup, propose-only, act-as-user applies — FR-SA16), and **async/batched distillation** under the per-user budget (FR-SA17).

Critically, this epic carries the **Curator→Librarian supersession** (ADR-0024): at cutover `classifyOnSave` becomes a capture on-ramp forwarding to `orvex-studio-staging` submit — one classification path, loud migration error, Card Contract v1 carried forward verbatim as Proposal (strict superset), and studio-api becomes a staging client. The `/v1/memory` store (E3) is explicitly NOT superseded (ADR-0025).

**Why (cites):** FR-SA12–17 (PRD `85qj2wwU2L`); A-CURATOR (arch `ekTh7nDQqo`); ADR-0024/0025; Brief §2 "Curation — the Librarian ritual: propose-and-confirm, diff before every destructive change."

### Stories
- [ ] E5-S1 — engineGate() Card Contract v1 resolution order + invariants
- [ ] E5-S2 — Curation Queue — grouped proposals, per-group bulk, scale
- [ ] E5-S3 — Tiered write modes + reversibility
- [ ] E5-S4 — Structure evolution + delegated gate primitives + async distillation
- [ ] E5-S5 — Curator→staging supersession (classifyOnSave → staging submit)

### FR ids owned
FR-SA12, FR-SA13, FR-SA14, FR-SA15, FR-SA16, FR-SA17, AD-4, NFR-SA1.

### Definition of Done
- [ ] All five stories Done (reviewer ≠ implementer each).
- [ ] Family-E2E share green: capture via MCP + import lands as grouped proposals; per-group approve applies as-user through the DocmostPort; an adversarial probe proves no write reaches wiki/Memory around the gate (FR-SA14); staging on-ramp forwards with the goldens passing — observed on the dev cell.
- [ ] SDD lines evidenced: Card Contract v1 goldens pass verbatim against the Proposal mapping; sensitivity-gated always stages (test); Free direct path asserts zero LLM calls (cost test).

### 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; Card Contract v1 schema home + staging Proposal mapping pinned there).
- [ ] Blocked by: E3 (Memory store target), E8-S1 (DocmostPort applies), E9-S1 (tier gates); ai classify verb + knowledge dedup corpora contracts; orvex-studio-staging pack certification for E5-S5.

### 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any Card Contract change (requires contract_version:2).
