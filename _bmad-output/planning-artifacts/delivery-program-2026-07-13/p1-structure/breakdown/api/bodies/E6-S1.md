## 🎯 Story

As a **user importing my AI history**, I want a **per-user Chat History store with retention controls and purge**, so that my conversations are browsable, cross-linked to distilled items, and fully deletable including search-index entries.

**Definition of Done:** one named test **`TestChatHistoryRetentionAndPurge`** (integration layer — stores a conversation (metadata/turns in Postgres, bodies + raw Archive in S3 encrypted), asserts Archives auto-delete 30d post-import by default, and that a per-conversation hard-delete removes the record AND emits a knowledge purge event for its search-index entries, through the chat-history interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given an imported conversation, When stored, Then metadata/turns → Postgres, bodies + raw Archives → S3 (encrypted), per-user. *Assertion: store split matches A-DATA; S3 objects encrypted.* [Source: FR-SA18]
- [ ] **AC2** — Given the store, When browsed, Then provider/date browse + cross-links to distilled wiki/Memory items work. *Assertion: browse by provider/date returns; cross-links resolve.* [Source: FR-SA18]
- [ ] **AC3** — Given retention, When default, Then Archives auto-delete 30d post-import; keep-forever/delete-now are user options. *Assertion: 30d default deletes Archive; overrides honored.* [Source: FR-SA18]
- [ ] **AC4** — Given hard-delete (per-conversation/provider/all), When invoked, Then it removes the record AND search-index entries via knowledge purge events. *Assertion: delete → local removal + knowledge purge event.* [Source: FR-SA18]
- [ ] **AC5** — Given account deletion, When run, Then the central Temporal purge saga extends to Chat History, Archives, and search namespaces. *Assertion: account-delete cascades to all three.* [Source: FR-SA18, NFR-SA2]
- [ ] **AC6** (negative) — Given a purge, When a search-index entry would remain, Then it is guaranteed removed (no orphaned index). *Assertion: post-purge, knowledge returns zero entries for the deleted item.* [Source: FR-SA18]

## 🔨 Tasks
- [ ] Chat History store (Postgres metadata/turns + encrypted S3 bodies/Archives) (AC: 1)
- [ ] Provider/date browse + cross-links (AC: 2)
- [ ] Retention: 30d Archive auto-delete + keep-forever/delete-now (AC: 3)
- [ ] Hard-delete + knowledge purge events (AC: 4,6)
- [ ] Extend central purge saga to chat history/Archives/namespaces (AC: 5)
- [ ] Write `TestChatHistoryRetentionAndPurge` (RED→GREEN) (AC: 3,4)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no Chat History store, retention, or purge.

Tier placement: route → application → domain (conversation) → Postgres + S3 ports + outbox (purge events). Seam crossed: knowledge (chat-history corpus purge), workflows (purge saga). Sibling dependency: E1-S6 (S3), E6-S2 (import fills the store), E9-S4 (purge step-APIs). GDPR: imported third-party personal data covered wholesale by deletion rights.

## 🧪 Testing
- Named DoD test: `TestChatHistoryRetentionAndPurge` (integration, real S3-compat + Postgres).
- Tiers: unit (retention policy) + integration (store + purge). CS §5 mocking: mock knowledge purge + workflows externals; never mock own store.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/10/11 (encryption at rest, purge completeness, GDPR); SE-Arch 8sYi523i4t lenses (orphaned-index, deletion completeness); cell-lint JGAUQRsw2g (per-cell S3 prefix).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA18, NFR-SA2); Architecture `ekTh7nDQqo` (A-DATA).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate; knowledge purge event + purge step-API pinned there).
- [ ] Blocked by: parent epic **E6**; E1-S6 (S3), E9-S4 (purge step-APIs).
- [ ] Blocks: E6-S2 (import writes here).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
