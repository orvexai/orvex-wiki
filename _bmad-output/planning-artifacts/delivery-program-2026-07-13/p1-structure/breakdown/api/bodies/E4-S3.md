## 🎯 Story

As a **user during a partial outage**, I want **degradation-first enrichment**, so that when knowledge is down my prompts still enrich from Memory-only via the deterministic path, and I never see errors or noise.

**Definition of Done:** one named test **`TestEnrichmentDegradesToMemoryOnly`** (integration layer — simulates knowledge down/degraded and asserts enrichment falls back to Memory-only via the deterministic path with no user-facing error, and that below-threshold relevance yields no injection, through the enrichment interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2098); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given knowledge down/degraded, When enrichment runs, Then it falls back to Memory-only via the deterministic path. *Assertion: knowledge unreachable → Memory-only enrichment still returns.* [Source: FR-SA11, NFR-SA5]
- [ ] **AC2** — Given below-threshold relevance, When enrichment runs, Then no injection (never noise). *Assertion: low-relevance → nothing injected.* [Source: FR-SA11]
- [ ] **AC3** — Given any degraded mode, When enrichment runs, Then there are no user-facing errors. *Assertion: degraded path returns a clean (possibly unenriched) prompt, no error surfaced.* [Source: FR-SA11]
- [ ] **AC4** — Given the NFR-SA5 matrix, When knowledge is down, Then search surfaces fall back per R-9 degraded mode (Postgres FTS where retained, honest empty search). *Assertion: search fallback matches the matrix.* [Source: NFR-SA5]
- [ ] **AC5** (negative) — Given both knowledge and Memory unavailable, When enrichment runs, Then the prompt ships unenriched cleanly. *Assertion: total-degrade → clean unenriched output.* [Source: FR-SA11]
- [ ] **AC6** (error-path) — Given a knowledge timeout mid-enrichment, When it fires, Then the deadline still holds (ship unenriched or Memory-only). *Assertion: timeout never blocks past the budget.* [Source: FR-SA11, NFR-SA1]

## 🔨 Tasks
- [ ] Detect knowledge down/degraded; route to Memory-only deterministic path (AC: 1)
- [ ] Relevance threshold → no-injection (AC: 2)
- [ ] Suppress user-facing errors in degraded modes (AC: 3)
- [ ] Wire the NFR-SA5 search-fallback posture (AC: 4)
- [ ] Total-degrade + timeout paths hold the budget (AC: 5,6)
- [ ] Write `TestEnrichmentDegradesToMemoryOnly` (RED→GREEN) (AC: 1)

## 🧠 Context
**🧾 Code audit (origin/main @ 903e477, 2026-07-14):** absent — no composer degradation-first fallback (note: `src/composition/composeView.ts` has a generic per-pane degrade pattern, but that is the ENG-1531 composition tier, not the Composer).

Tier placement: application (degradation policy in enrichment). Seam crossed: knowledge (degrade detection). Sibling dependency: E4-S1 (enrichment), E3-S5 (deterministic weave = the Memory-only fallback). This is part of the family degradation matrix (E9-S4 owns the full matrix; this is the enrichment slice).

## 🧪 Testing
- Named DoD test: `TestEnrichmentDegradesToMemoryOnly` (integration).
- Tiers: unit (degrade policy) + integration (fault injection). CS §5 mocking: mock the knowledge external service into down/slow/empty states; never mock own enrichment.

## 📏 Guidance
CS 6aMAzsYeQb §§0/5/6/10 (graceful degradation, no error leak); SE-Arch 8sYi523i4t lenses (degraded-mode correctness, noise); cell-lint JGAUQRsw2g (n/a).

## 🔗 References
PRD `85qj2wwU2L` (FR-SA11, NFR-SA5, NFR-SA1); Architecture `ekTh7nDQqo` (§6 risks).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2098** (contract TAG = dispatch gate).
- [ ] Blocked by: parent epic **E4**; E4-S1 (enrichment), E3-S5 (weave fallback).
- [ ] Blocks: none (hardening slice).

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
