## 🎯 Story

**As a** privacy/cost owner, **I want** console's share of the external-processor privacy posture — render-side pseudonymity + residency honesty, the GDPR erasure-cascade fronting, and the PostHog egress cost SLI on the health board **so that** behavioural data leaving Orvex infra to PostHog Cloud EU stays DPA-gated, ID-only, residency-correct, and cost-monitored (the bridge + client capture being other services).

**Definition of Done:** one named test `TestErasureCascadeCallsDeletePersonsAndCostSliRenders` (workflow/handler layer — a `wiki.user.deprovision.requested` erasure resolves to PostHog's DELETE-Persons call keyed on the pseudonymous `distinct_id` (verified by probe), and the PostHog egress cost SLI renders on the health board, asserted through the erasure workflow + the SLI feed). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the GDPR right-to-erasure cascade, When `wiki.user.deprovision.requested` fires, Then PostHog's DELETE Persons API is called (events + recordings), keyed on the pseudonymous `distinct_id`, verified by probe. *Assert: DELETE-Persons invoked; erasure probe-verified.* [Source: FR-CT-UBA-7; NFR-CT-UBA-1]
- [ ] **AC2** — Given the external-processor posture, When rendered/served, Then PostHog is the data processor under a signed DPA (Orvex controller), IP-off (EU default), and only pseudonymous ID-only data is ever involved; a mis-scoped/non-consenting egress is an automatic fail. *Assert: no PII/raw-user-id path from console; consent + pseudonymity enforced at the bridge, not console (bridge = other service).* [Source: FR-CT-UBA-7/8; NFR-CT-UBA-1]
- [ ] **AC3** — Given residency, When a future US cell exists, Then behavioural data may not silently land in the Frankfurt EU project (bounded by console OQ-CT4/OQ-UBA-6). *Assert: residency guard; `orvexcell` respected; TBD — pinned by OQ-UBA-6.* [Source: NFR-CT-UBA-2]
- [ ] **AC4** — Given per-event metering above 1M/mo, When egress runs, Then the PostHog egress rate is an operator SLI + budget line on the health board. *Assert: cost SLI renders (E3-S7); alert threshold TBD — pinned by OQ-UBA-7.* [Source: NFR-CT-UBA-5; FR-CT-UBA-8a]

## 🔨 Tasks

- [ ] RED: `TestErasureCascadeCallsDeletePersonsAndCostSliRenders` (AC1,4). (AC: 1,4)
- [ ] GREEN: erasure-cascade fronting → DELETE-Persons call + probe verify. (AC: 1)
- [ ] Render-side pseudonymity/residency honesty; document bridge/frontend ownership. (AC: 2,3)
- [ ] Cost SLI feed to the health board (E3-S7). (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Workflow (erasure, the one privileged analytics write — A-UBA-READONLY) + domain + PostHog port. The **bridge** (consent-gate, pseudonymise, batch-POST, salt→distinct_id map) and **client capture** are OTHER services (OQ-UBA-1b, OQ-UBA-10) — console fronts the erasure and renders the SLI. Sibling: E3-S7 (health board), E8-S6 (recording erasure).

## 🧪 Testing

`TestErasureCascadeCallsDeletePersonsAndCostSliRenders` (workflow/handler integration). Tiers: workflow + domain + ports. CS §5: fake PostHog DELETE-Persons + the SLI source at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + §9 (adopt-PostHog ADR triggers) + §10 (privacy). SE-Arch lenses: never-over-expose, fail-closed, honesty. Cell-lint `JGAUQRsw2g` (residency/`orvexcell`).

## 🔗 References

- `Ii9q9kBNms` (§3 store, §5 privacy, §6 pipeline, §7 ADRs), `TYWAtnvliD` (FR-CT-UBA-7/8/8a, NFR-CT-UBA-1/2/5).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E8; E8-S1/S2, E3-S7.
- [ ] Cross-repo (HARD): adopt-PostHog ADR (OQ-UBA-1); the bridge (OQ-UBA-1b, holds the salt→distinct_id map); consent SoR (OQ-UBA-4); residency (OQ-UBA-6); cost threshold (OQ-UBA-7); retention (OQ-UBA-9).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
