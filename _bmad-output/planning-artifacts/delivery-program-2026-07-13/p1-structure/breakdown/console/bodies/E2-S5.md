## 🎯 Story

**As a** platform auditor, **I want** a browse/search pane over the family audit stream, filterable by actor/tenant/satellite/action/time, with reads of sensitive surfaces themselves audited **so that** the audit stream is inspectable and no privileged read is invisible.

**Definition of Done:** one named test `TestAuditPaneReadIsItselfAudited` (handler layer — a query against `/api/admin/audit` returns filtered records AND emits a sensitive-surface read-audit record, asserted through the handler + audit port). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/admin/audit`, When queried with filters, Then records filter by actor/tenant/satellite/action/time. *Assert: filter round-trips against the stream store.* [Source: FR-CT12]
- [ ] **AC2** — Given a read of the audit pane (a sensitive surface), When served, Then it is itself audited. *Assert: a read-audit record is emitted for the audit-pane read.* [Source: FR-CT12/CT14]
- [ ] **AC3** — Given every cache key is tenant-derived, When cached, Then the tenant is embedded and tenant identifiers render as identity's opaque IDs. *Assert: no invented slugs; tenant-embedded keys.* [Source: NFR-CT5]
- [ ] **AC4** — Given the durable stream is not yet live, When the pane loads, Then it degrades honestly (read-audit rides Loki interim). *Assert: typed unavailable state; pane gated on stream build-out — TBD pinned by ENG-2106.* [Source: FR-CT12; rollout Phase 2]

## 🔨 Tasks

- [ ] RED: `TestAuditPaneReadIsItselfAudited` (AC1,2). (AC: 1,2)
- [ ] GREEN: `/api/admin/audit` handler + query against the stream store. (AC: 1)
- [ ] Sensitive-read audit emit. (AC: 2)
- [ ] Tenant-embedded keys + opaque-ID rendering. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — Audit browse/search backend (durable Store with actor/satellite/action filtering) present and tested; web AuditViewerPage + retention component + spec exist. GAPS: the web pane is wired to TypedStubAuditListService (honest-empty) not a live BFF client; the audit-pane read is NOT itself audited (only break-glass reads are); tenant-embedded-keys handling not evidenced.

Handler → domain (query shaping) → store/port (audit-stream reader). NFR-CT5 tenancy hygiene lives in the key builder. Gated on the family audit-stream build-out (Phase 2). Sibling: E2-S4 (writer side).

## 🧪 Testing

`TestAuditPaneReadIsItselfAudited` (handler integration). Tiers: handler + domain + port. CS §5: fake the audit-stream reader at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: honesty, tenancy hygiene. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (§3, A-STATE), `kldiZu93EC` (FR-CT12, NFR-CT5).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E2; E2-S4, E1-S6.
- [ ] Cross-repo (HARD): family audit-stream Kafka build-out.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
