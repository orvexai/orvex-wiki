## 🎯 Story

**As a** privacy owner, **I want** analytics dashboards to render cohorts and aggregates only, suppressing any cohort cell below a ratified k-anonymity floor **so that** no single user can be re-identified from a behavioural chart.

**Definition of Done:** one named test `TestCohortBelowKAnonymityFloorIsSuppressed` (domain layer — a cohort cell whose count is below the k-anonymity floor is suppressed on render (panel suppression under query; PostHog cohort-size config under embed), and engagement dots carry no identity, asserted through the render/suppression function). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given aggregate-first rendering, When a surface loads, Then dashboards render cohorts and aggregates, not individual-user activity streams. *Assert: funnels/retention are counts; no per-user stream.* [Source: FR-CT-UBA-4]
- [ ] **AC2** — Given the engagement scatter, When rendered, Then points are anonymous per-user within a cohort (no identity on a dot). *Assert: no identity attribute on any plotted point.* [Source: FR-CT-UBA-4]
- [ ] **AC3** — Given a cohort cell below the ratified k-anonymity floor, When rendered, Then it is suppressed. *Assert: below-floor cell suppressed (query: panel-suppression; embed: cohort-size config). Floor value TBD — pinned by the Definition Pack (ENG-2106).* [Source: FR-CT-UBA-4]
- [ ] **AC4** — Given per-individual drill-down, When requested, Then it is out of scope / gated (OQ-UBA-3). *Assert: no per-user drill-down surface; typed "gated — OQ-UBA-3".* [Source: FR-CT-UBA-4; OQ-UBA-3]

## 🔨 Tasks

- [ ] RED: `TestCohortBelowKAnonymityFloorIsSuppressed` (AC3). (AC: 3)
- [ ] GREEN: aggregate-first render + k-anonymity suppression (pure domain). (AC: 1,3)
- [ ] Anonymous scatter (no identity on a dot). (AC: 2)
- [ ] Gate per-individual drill-down (OQ-UBA-3). (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Domain tier (`internal/analytics/` pure suppression) + render. Applies under both OQ-UBA-2 outcomes. Sibling: E8-S3 (surfaces), E8-S8 (privacy posture).

## 🧪 Testing

`TestCohortBelowKAnonymityFloorIsSuppressed` (domain unit). Tiers: domain. CS §5: pure functions over fixture cohorts; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + §10 (privacy). SE-Arch lenses: never-over-expose, fail-closed. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `Ii9q9kBNms` (§5 k-anonymity), `TYWAtnvliD` (FR-CT-UBA-4, OQ-UBA-3).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate — k-anonymity floor value); parent epic E8; E8-S1/S3.
- [ ] Intra-service: E8-S8.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
