## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform operator, **I want** console deployed as an always-warm multi-replica Deployment with every backend endpoint injected as config **so that** an ops pane never cold-starts mid-incident and the per-cell-vs-global decision (OQ-CT4) stays a values change, not a rearchitecture.

**Definition of Done:** one named test `TestBackendEndpointsAreInjectedConfigNotLiterals` (config/lint layer — the Go source and HTTPRoute contain no host literal for LGTM/workflows-proxy/satellite endpoints; all resolve from injected config, asserted by a source scan + a config-load test). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the manifests, When deployed, Then a plain multi-replica Deployment + Service + HTTPRoute for `console.orvex.ai` (prod) / `console.orvex.dev` (dev) exist, non-root, read-only rootfs, caps dropped. *Assert: replicas>1; no KEDA/Knative/Temporal worker.* [Source: Arch §1/§4; NFR-CT3]
- [ ] **AC2** — Given the operator host, When source/HTTPRoute is scanned, Then the host is injected config, never a literal (no `dev-studio.` infix). *Assert: no host string literal in Go or HTTPRoute.* [Source: Arch §4, cell rule 9, SE-Arch F1]
- [ ] **AC3** — Given cross-cell visibility (NFR-CT6), When endpoints are configured, Then LGTM/proxy/satellite endpoints are explicit injected config so a global instance is a values change. *Assert: all backend endpoints externalised; no in-cell hard-binding.* [Source: NFR-CT6; Arch §1, OQ-CT4]
- [ ] **AC4** — Given NetworkPolicy, When applied, Then egress grants LGTM + workflows-authorizing-proxy + satellite-admin, and **denies** `temporal-frontend`; `/api` ingress via the gateway only. *Assert: no `temporal-frontend` egress rule.* [Source: Arch §4; D-CTRL]

## 🔨 Tasks

- [ ] RED: `TestBackendEndpointsAreInjectedConfigNotLiterals` source scan (AC2,3). (AC: 2,3)
- [ ] GREEN: kustomize manifests (Deployment/Service/HTTPRoute) per the house pattern. (AC: 1)
- [ ] Externalise every backend endpoint to config. (AC: 3)
- [ ] NetworkPolicies (LGTM + proxy + satellite egress; no temporal-frontend). (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Always-warm multi-replica Deployment (replicas>=2, no HPA/KEDA, enforced by celllint) plus endpoint-as-config cross-cell posture — every sibling reached only through its configured URL, no baked host.

Deploy tier (kustomize). NFR-CT6 cross-cell posture kept cheap via endpoint-as-config; OQ-CT4 (global vs per-cell) + rule-14 reconciliation is ADR-gated (Arch §7) — this story writes the per-cell-shaped default. Sibling: E1-S1 (`/healthz`), E1-S6 (Redis claim).

## 🧪 Testing

`TestBackendEndpointsAreInjectedConfigNotLiterals` (config/source scan). Tiers: config/deploy. CS §5: no service mocks; static scan + config-load. Do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: cell-contract (rule 9/14), honesty. Cell-lint `JGAUQRsw2g` rules 9/14; URL/env scheme `86CiGucQwU`.

## 🔗 References

- `L1NVnmRgpy` (§1/§4/§7), `kldiZu93EC` (NFR-CT3/CT6), `JGAUQRsw2g`, `86CiGucQwU`.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E1; E1-S1.
- [ ] Intra-service: E1-S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
