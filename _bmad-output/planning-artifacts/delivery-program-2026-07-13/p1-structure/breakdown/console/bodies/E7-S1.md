## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** break-glass operator, **I want** a separate break-glass route class verified ONLY by identity's offline-sealed path, provably importing nothing from the Clerk-verified middleware chain **so that** emergency recovery works with both IdPs down and cannot silently ride the normal auth plane.

**Definition of Done:** one named test `TestBreakGlassHandlersImportNothingFromClerkVerifiedChain` (dependency-direction CI test — the break-glass handler package imports none of the Clerk-verified middleware chain, and a booted break-glass path verifies via identity's offline path with Clerk+Keycloak+normal-session all down, asserted through the import graph + an offline-verify integration test). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/breakglass/*`, When invoked, Then verification delegates ONLY to identity's offline-sealed credential path (FR-I12) — no Clerk, no Keycloak, no admin-org lookup, no cached-introspection dependency. *Assert: offline-verify only.* [Source: FR-CT15; Arch A-BREAKGLASS]
- [ ] **AC2** — Given the dependency-direction CI test, When run, Then break-glass handlers import nothing from the Clerk-verified chain. *Assert: import-graph test green; a seeded import fails it.* [Source: FR-CT15; Arch A-BREAKGLASS]
- [ ] **AC3** — Given both IdPs + the normal identity session plane are down, When a break-glass action runs, Then the path works (console + identity offline verify only). *Assert: end-to-end with IdPs blackholed.* [Source: NFR-CT3; FR-CT15]
- [ ] **AC4** — Given break-glass is a restricted class, When invoked, Then it is heavily audited (read-audit interim over Loki acceptable). *Assert: every invocation audited.* [Source: FR-CT15; FR-CT14]

## 🔨 Tasks

- [ ] RED: `TestBreakGlassHandlersImportNothingFromClerkVerifiedChain` (AC2). (AC: 2)
- [ ] GREEN: separate router group + middleware stack delegating to identity offline verify. (AC: 1,3)
- [ ] Dependency-direction CI test. (AC: 2)
- [ ] Audit every invocation. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Break-glass route class with identity-offline-only verification (orvex-studio-lib offlineauth adapter) separated from the normal Clerk-verified plane; dependency-direction asserted. Present and tested.

Handler/middleware tier — a distinct router group with its own stack (A-BREAKGLASS). Ports: identity offline-verify client (no Clerk/Keycloak). Phase 4: lands when identity resolves OQ-I5. Sibling: E7-S2 (rate-limit/alerting/allow-list).

## 🧪 Testing

`TestBreakGlassHandlersImportNothingFromClerkVerifiedChain` (import-graph + offline integration). Tiers: CI gate + middleware. CS §5: fake identity offline-verify at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + §9 (A-BREAKGLASS ADR trigger). SE-Arch lenses: concentrated-privilege, dependency-direction, fail-closed. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-BREAKGLASS, §5), `kldiZu93EC` (FR-CT15, NFR-CT3), identity FR-I12/OQ-I5.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E7; E1-S1/S4, E2-S1.
- [ ] Cross-repo (HARD): identity break-glass offline verification (FR-I12 / OQ-I5) — Phase 4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
