## 🎯 Story

**As a** product/growth staffer, **I want** the five product-insight surfaces — activation funnel, feature adoption, engagement distribution, retention (Day-N + WAU/MAU), and plan/tenant mix — rendered from PostHog scoped by tenant/plan/cell/feature/time **so that** I can see how users behave without console emitting or computing anything.

**Definition of Done:** one named test `TestFiveInsightSurfacesRenderFromPosthogNotConsoleEmitter` (handler/front — each of the five surfaces renders from PostHog (embed or query) scoped correctly, and console emits no `product.*` event and is not the bridge, asserted through the surfaces' data interface + a no-emitter assertion). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the five surfaces, When loaded, Then activation funnel, feature adoption, engagement distribution, retention (Day-N + WAU/MAU), and plan/tenant mix render, scoped by tenant/plan/cell/feature/time. *Assert: five surfaces render scoped.* [Source: FR-CT-UBA-1]
- [ ] **AC2** — Given the metrics derive from the family CloudEvent stream (reused `wiki.*`/`ai.*`/`billing.*` + new `product.*`), When rendered, Then console consumes/renders them but is NEVER the emitter and NEVER the bridge. *Assert: no `product.*` publish from console; metrics read from PostHog.* [Source: FR-CT-UBA-2; §3 non-goal/FR-CT9]
- [ ] **AC3** — Given plan/tenant mix, When rendered, Then it carries billing's plan property (`billing.entitlement.changed`, D-P3 SoR — reused). *Assert: plan property sourced from billing.* [Source: FR-CT-UBA-2]
- [ ] **AC4** — Given PostHog is unreachable, When a surface loads, Then it degrades with a typed error; other surfaces + console panes unaffected. *Assert: NFR-CT-UBA-3 typed degradation.* [Source: FR-CT-UBA-1; NFR-CT-UBA-3]

## 🔨 Tasks

- [ ] RED: `TestFiveInsightSurfacesRenderFromPosthogNotConsoleEmitter` (AC1,2). (AC: 1,2)
- [ ] GREEN: the five surfaces (embed or query per OQ-UBA-2) over `/api/analytics/*`. (AC: 1)
- [ ] Assert no console emitter/bridge path. (AC: 2)
- [ ] Plan property from billing; typed degradation. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Front (or embed) tier over the E8-S1 BFF. The `product.*` types + emitters + the bridge are OTHER services (contracts OQ-UBA-1a; bridge OQ-UBA-1b — console is never the bridge). Console's share is render-only. Sibling: E8-S1/S4/S5.

## 🧪 Testing

`TestFiveInsightSurfacesRenderFromPosthogNotConsoleEmitter` (handler/front). Tiers: front + BFF. CS §5: fake PostHog at the network boundary via golden fixtures; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + React front rules. SE-Arch lenses: compose-not-own, no-spine-publish, honesty. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `Ii9q9kBNms` (§ overview, §6 pipeline), `TYWAtnvliD` (FR-CT-UBA-1/2).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E8; E8-S1/S2.
- [ ] Cross-repo (HARD): `product.*` contracts types (OQ-UBA-1a); the bridge (OQ-UBA-1b, NOT console); billing plan events.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
