## 🎯 Story

**As a** platform operator, **I want** a log-search pane over Loki/LogQL built from Grafana's Apache-2.0 shell with an original LogQL result pane **so that** I get Grafana-grade log search scoped by service/cell/tenant/time without embedding an AGPL Grafana server.

**Definition of Done:** one named test `TestLogSearchRendersScopedLogqlResultsThroughBff` (front/integration — a LogQL query scoped by service/cell/tenant/time returns results rendered in the original result pane via the guardrailed BFF, asserted through the pane's data interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the log pane, When a LogQL query runs scoped by service/cell/tenant/time, Then results render through `/api/obs/logs`. *Assert: scoped query → rendered lines; scope keys from the FR-C18 vocabulary.* [Source: FR-CT1]
- [ ] **AC2** — Given the pane shell, When built, Then it uses `@grafana/ui` + `@grafana/scenes` (Apache-2.0) for layout/controls only. *Assert: no `grafana/grafana` server; shell libs Apache-2.0.* [Source: FR-CT1; D-OBS-UI]
- [ ] **AC3** — Given the LogQL result pane, When rendered, Then it is ORIGINAL console code (not copied Grafana source). *Assert: result-pane code is original; AGPL guard green.* [Source: FR-CT1; NFR-CT4]
- [ ] **AC4** — Given Loki is unreachable, When the pane loads, Then it degrades with a typed error while other panes stay live. *Assert: FR-CT18 typed degradation; no synthetic data.* [Source: FR-CT18 (E1-S6 framework)]

## 🔨 Tasks

- [ ] RED: `TestLogSearchRendersScopedLogqlResultsThroughBff` (AC1). (AC: 1)
- [ ] GREEN: log pane shell (`@grafana/ui`/scenes) + original LogQL result pane. (AC: 1,2,3)
- [ ] Wire scope controls to the FR-C18 vocabulary. (AC: 1)
- [ ] Typed-degradation via the E1-S6 framework. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — BFF log-search over Loki/LogQL is present and tested (scoped query, guardrail 400, typed 502 degrade). MISSING: no web log-search pane exists (web/src has traces/audit/ai-usage/settings only) — the 'Grafana shell + result pane' UI half is unbuilt.

Front tier (React SPA pane) over the E3-S1 BFF proxy. Grafana Apache-2.0 shell only; original result rendering (AGPL boundary). Sibling: E3-S5 correlation pivots consume this pane's trace_id derived-field.

## 🧪 Testing

`TestLogSearchRendersScopedLogqlResultsThroughBff` (front/integration). Tiers: front + BFF. CS §5: fake Loki at the BFF network boundary via golden fixtures; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + React front rules (thin, talks only to own BFF). SE-Arch lenses: AGPL-boundary, honesty. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-PANE, D-OBS-UI), `kldiZu93EC` (FR-CT1, OQ-CT1).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E3; E3-S1, E1-S2/S6.
- [ ] Intra-service: E3-S5 (correlation).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
