## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform operator, **I want** trace lookup over Tempo/TraceQL with an ORIGINAL trace waterfall **so that** I get richness parity with the Grafana Tempo interface without any AGPL Grafana trace-viewer code.

**Definition of Done:** one named test `TestTraceWaterfallRendersSpansOriginalCode` (front/integration — a TraceQL lookup returns a span tree rendered by the original waterfall component via `/api/obs/traces`, asserted through the pane's data interface; no `grafana/grafana` trace import). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a TraceQL lookup, When run, Then a trace + span waterfall renders through `/api/obs/traces`. *Assert: span tree with parent/child + timing.* [Source: FR-CT1]
- [ ] **AC2** — Given the waterfall, When built, Then it is ORIGINAL console code (Grafana trace-viewer is AGPL-only, `@grafana/ui` has zero trace components). *Assert: AGPL guard green; no `grafana/grafana` trace import.* [Source: FR-CT1; NFR-CT4; D-OBS-UI/ENG-1613]
- [ ] **AC3** — Given a span with a workflow_id / trace_id, When clicked, Then pivots to logs and to the workflow (feeds E3-S5). *Assert: pivot hooks present.* [Source: FR-CT2]
- [ ] **AC4** — Given Tempo is unreachable, When the pane loads, Then it degrades with a typed error; logs/metrics stay live. *Assert: FR-CT18 typed degradation.* [Source: FR-CT18]

## 🔨 Tasks

- [ ] RED: `TestTraceWaterfallRendersSpansOriginalCode` (AC1,2). (AC: 1,2)
- [ ] GREEN: original trace-waterfall component over `/api/obs/traces`. (AC: 1,2)
- [ ] trace_id/workflow_id pivot hooks. (AC: 3)
- [ ] Typed degradation. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Trace lookup + ORIGINAL (AGPL-clean) waterfall renderer present full-stack: BFF Tempo proxy shaping OTLP into gen.TraceResult, plus a hand-built React Waterfall pane with license/edge-confinement specs. Tested both tiers.

Front tier — the ORIGINAL trace surface (the AGPL-clean piece D-OBS-UI mandates). Shell chrome may use `@grafana/ui`; the waterfall is original. Sibling: E3-S5 (correlation), E4 (workflow_id pivot lands in Temporal visibility).

## 🧪 Testing

`TestTraceWaterfallRendersSpansOriginalCode` (front/integration). Tiers: front + BFF. CS §5: fake Tempo at the BFF network boundary; do not mock own packages. AGPL guard asserts no Grafana trace import.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + React front rules. SE-Arch lenses: AGPL-boundary (load-bearing here), honesty. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-PANE, D-OBS-UI), `kldiZu93EC` (FR-CT1, NFR-CT4, change log 2026-07-09).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E3; E3-S1, E1-S2/S3/S6.
- [ ] Intra-service: E3-S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
