## 🎯 Story

**As a** platform operator, **I want** a metrics/dashboards pane over Mimir/PromQL built with Grafana Scenes shell primitives **so that** I get Grafana-grade metric dashboards scoped by service/cell/tenant/time through the guardrailed BFF.

**Definition of Done:** one named test `TestMetricsPaneRendersScopedPromqlThroughBff` (front/integration — a PromQL query scoped by service/cell/tenant/time renders panels via `/api/obs/metrics`, asserted through the pane's data interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the metrics pane, When a PromQL query runs scoped by the FR-C18 keys, Then panels render through `/api/obs/metrics`. *Assert: scoped query → rendered panel series.* [Source: FR-CT1]
- [ ] **AC2** — Given the pane, When built, Then it uses `@grafana/scenes` (Apache-2.0) for the panel/query-editor shell. *Assert: Scenes shell; no Grafana server.* [Source: FR-CT1; D-OBS-UI]
- [ ] **AC3** — Given a metric spike, When an exemplar exists, Then a pivot to exemplar traces is offered (feeds E3-S5). *Assert: exemplar → trace pivot present.* [Source: FR-CT2 (correlation seam)]
- [ ] **AC4** — Given Mimir is unreachable, When the pane loads, Then it degrades with a typed error; logs/traces panes stay live. *Assert: FR-CT18 typed degradation.* [Source: FR-CT18]

## 🔨 Tasks

- [ ] RED: `TestMetricsPaneRendersScopedPromqlThroughBff` (AC1). (AC: 1)
- [ ] GREEN: metrics pane on Grafana Scenes shell over `/api/obs/metrics`. (AC: 1,2)
- [ ] Exemplar → trace pivot hook. (AC: 3)
- [ ] Typed degradation. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — BFF metrics over Mimir/PromQL is present and tested (query shaping, step defaulting, cardinality cap, typed degrade). MISSING: no web metrics/dashboards pane / Grafana-Scenes shell in web/src.

Front tier over the E3-S1 proxy. Scenes shell (Apache-2.0). Sibling: E3-S5 (correlation), E3-S7 (health board reuses Mimir SLI reads).

## 🧪 Testing

`TestMetricsPaneRendersScopedPromqlThroughBff` (front/integration). Tiers: front + BFF. CS §5: fake Mimir at the BFF network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + React front rules. SE-Arch lenses: AGPL-boundary, honesty. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-PANE, D-OBS-UI), `kldiZu93EC` (FR-CT1/CT2).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E3; E3-S1, E1-S2/S6.
- [ ] Intra-service: E3-S5, E3-S7.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
