## 🎯 Story

**As a** platform operator, **I want** the workspace/user lifecycle state machine surfaced end-to-end — Clerk webhook → workflow → engine + studio legs — including stuck/failed workflows and reconcile drift reports **so that** "is this org actually deprovisioned everywhere?" is answerable in one screen (FR-CT11).

**Definition of Done:** one named test `TestLifecycleViewShowsStuckWorkflowsAndReconcileDrift` (handler/front — the lifecycle view renders the provision/deprovision state machine for an org and flags a stuck/failed workflow plus a reconcile drift report, sourced via the workflows proxy, asserted through the view's data interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an org, When the lifecycle view loads, Then the provision/deprovision state machine renders end-to-end (Clerk webhook → workflow → engine + studio legs). *Assert: each leg's state shown.* [Source: FR-CT11]
- [ ] **AC2** — Given a stuck/failed lifecycle workflow, When present, Then it is flagged. *Assert: stuck/failed state surfaced distinctly.* [Source: FR-CT11]
- [ ] **AC3** — Given the reconcile drift reports (workflows FR-W4), When present, Then they render. *Assert: drift report shown; "deprovisioned everywhere?" answerable.* [Source: FR-CT11; workflows FR-W4]
- [ ] **AC4** — Given the proxy/workflows source is unreachable, When the view loads, Then it degrades with a typed error. *Assert: FR-CT18 typed degradation.* [Source: FR-CT18]

## 🔨 Tasks

- [ ] RED: `TestLifecycleViewShowsStuckWorkflowsAndReconcileDrift` (AC1,2,3). (AC: 1,2,3)
- [ ] GREEN: lifecycle state-machine view over the E4-S1 proxy client. (AC: 1)
- [ ] Stuck/failed + reconcile-drift rendering. (AC: 2,3)
- [ ] Typed degradation. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — No workspace/user lifecycle state-machine and no reconcile-drift view anywhere in the tree. admin/identity.go is explicitly read-only ('write half of FR-CT7 does not exist yet'); internal/server/reconcile_test.go is the unrelated M8 route-count reconciliation, not workspace reconciliation.

Handler → domain (state-machine shaping, pure) → proxy client. Read-only (visibility). Complements E5-S1 (org registry) — this is the workflow-side lifecycle view; the identity-side lifecycle write half is blocked on OQ-CT9. Sibling: E4-S1, E5-S1.

## 🧪 Testing

`TestLifecycleViewShowsStuckWorkflowsAndReconcileDrift` (handler/front). Tiers: handler + domain + port. CS §5: fake the proxy/workflows source at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: honesty, single-pane. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN), `kldiZu93EC` (FR-CT11), workflows FR-W4.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E4; E4-S1.
- [ ] Cross-repo (HARD): workflows authorizing-proxy + FR-W4 reconcile reports.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
