## 🎯 Story

**As a** platform operator, **I want** an alert-state surface showing currently-firing and recently-resolved alerts from the LGTM alerting components, each linking into the correlated pane **so that** I can jump from an alert straight to its logs/metrics/traces.

**Definition of Done:** one named test `TestAlertStateSurfaceLinksIntoCorrelatedPane` (handler/front — firing + recently-resolved alerts render via `/api/obs/alerts`, each carrying a deep-link into the FR-CT2 correlated pane, asserted through the alert pane's data interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the alert surface, When loaded, Then currently-firing and recently-resolved alerts render from the LGTM alerting components. *Assert: firing + resolved states present.* [Source: FR-CT5]
- [ ] **AC2** — Given an alert, When clicked, Then it links into the FR-CT2 correlated pane. *Assert: deep-link carries the correlation keys.* [Source: FR-CT5/CT2]
- [ ] **AC3** — Given rule authoring/ownership + paging, When considered, Then they are explicitly OUT of scope (OQ-CT7 — display-only floor). *Assert: no rule-authoring surface; documented TBD — pinned by OQ-CT7.* [Source: FR-CT5; OQ-CT7]
- [ ] **AC4** — Given the alerting backend is unreachable, When the surface loads, Then it degrades with a typed error. *Assert: FR-CT18 typed degradation.* [Source: FR-CT18]

## 🔨 Tasks

- [ ] RED: `TestAlertStateSurfaceLinksIntoCorrelatedPane` (AC1,2). (AC: 1,2)
- [ ] GREEN: `/api/obs/alerts` proxy + alert-state pane. (AC: 1)
- [ ] Deep-link into the correlated pane. (AC: 2)
- [ ] Document the OQ-CT7 display-only boundary. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — BFF alert-state surface over Alertmanager v2 (firing/resolved shaping) is present and tested. MISSING: no web alert-state pane and no links from alerts into the correlated pane.

Handler → front. Display-only floor; silences/ack/paging deferred to OQ-CT7. This surface is also where E6-S1's `ai.cap.*` events land (via the alerting pipeline, not a console spine subscription). Sibling: E3-S5, E6-S1.

## 🧪 Testing

`TestAlertStateSurfaceLinksIntoCorrelatedPane` (handler/front). Tiers: handler + front. CS §5: fake the alerting backend at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: honesty, scope-discipline. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-PANE), `kldiZu93EC` (FR-CT5, OQ-CT7).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E3; E3-S1/S5.
- [ ] Intra-service: E6-S1 (cap events surface here).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
