## 🎯 Story

**As a** platform operator, **I want** console inside the day-1 cell-contract ruleset — cell-lint green, cell-guard on every `/api/*` route, and the tenant-move typed step-API stub **so that** console is a compliant registered repo, not a disclosed bootstrap gap.

**Definition of Done:** one named test `TestCellGuardRejectsForeignCellPrincipal` (middleware layer — a request whose `principal.cell != CELL_ID` receives 421 and is never served, asserted through the middleware on an `/api/*` route; `/healthz` remains exempt). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a principal whose cell ≠ `CELL_ID`, When any `/api/*` route is hit, Then 421 and no service. *Assert: status 421, handler body never executed.* [Source: cell rule 3; Arch §3a]
- [ ] **AC2** — Given `/healthz`, When hit with a foreign-cell principal, Then it is exempt and serves. *Assert: 200, no cell-guard applied.* [Source: Arch §3a rule 3]
- [ ] **AC3** — Given the tenant-move requirement, When the repo is inspected, Then a typed step-API 501 stub + `TENANT_MOVE.md` store-inventory exist. *Assert: stub returns 501 typed; `TENANT_MOVE.md` present and coverage-checked (console is stateless → near-empty but required).* [Source: cell rule 10; Arch §3a]
- [ ] **AC4** — Given cell-lint (the reusable contracts workflow), When CI runs, Then it passes. *Assert: cell-lint required-status green; shared principal lib + envelope helper consumed from contracts, not re-implemented.* [Source: Arch §3a; §4 CI gates]

## 🔨 Tasks

- [ ] RED: `TestCellGuardRejectsForeignCellPrincipal` on an `/api/*` route (AC1,2). (AC: 1,2)
- [ ] GREEN: cell-guard middleware wrapping the `/api` router; `/healthz` exempt. (AC: 1,2)
- [ ] Tenant-move typed step-API 501 stub + `TENANT_MOVE.md`. (AC: 3)
- [ ] Wire cell-lint workflow + shared principal/envelope helpers from contracts. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — cell-lint conformance (always-warm replicas>=2, no-HPA/KEDA, no-temporal-egress, secrets-external, /healthz echo) is present and tested; foreign-cell drop exists on the Kafka audit-event sink via events.ConsumeInCell. MISSING: no cell-guard middleware fronting /api/*, no tenant-move 501 stub route, and no TENANT_MOVE.md.

Handler/middleware tier: cell-guard is edge middleware ahead of every domain handler. Seam: consumes the contracts shared principal lib + cell-lint reusable workflow. Sibling: E2-S1 (`lib/auth` router sits alongside cell-guard); OQ-CT4 folds the global/per-cell posture question here (default: per-cell instance).

## 🧪 Testing

`TestCellGuardRejectsForeignCellPrincipal` (middleware integration). Tiers: middleware. CS §5: mock the principal (a value), not own packages; cell-lint runs in CI.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: cell-contract conformance, honesty. Cell-lint `JGAUQRsw2g` rules 3/4/9/10.

## 🔗 References

- `L1NVnmRgpy` (§3a, §4), cell-contract `JGAUQRsw2g`.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E1; E1-S1.
- [ ] Intra-service: E2-S1 (auth router).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
