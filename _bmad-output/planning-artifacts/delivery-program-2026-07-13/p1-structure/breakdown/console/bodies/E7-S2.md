## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform security owner, **I want** break-glass per-use rate limits, out-of-band alerting on every invocation, and an action allow-list structurally bounded to identity-local + console-local recovery **so that** the emergency plane is loud, minimal, and honest about the satellites it cannot reach when both IdPs are down.

**Definition of Done:** one named test `TestBreakGlassAllowListRejectsSatelliteActionsAndAlarmsEveryUse` (handler layer — a break-glass action outside the identity-local/console-local allow-list is rejected, and every invocation raises an out-of-band alert + hits the per-use rate limit, asserted through the break-glass handler). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the action allow-list, When a satellite action (e.g. the workflows FR-W5 grace-cancel) is attempted, Then it is rejected — with both IdPs down no satellite can verify anything console presents. *Assert: only identity-local + console-local recovery allowed; satellite actions rejected.* [Source: FR-CT15; Arch A-BREAKGLASS]
- [ ] **AC2** — Given any break-glass invocation, When it fires, Then an out-of-band alert is raised. *Assert: alarmed on every use.* [Source: FR-CT15]
- [ ] **AC3** — Given per-use rate limits, When exceeded, Then further invocations are limited. *Assert: rate-limit enforced per use.* [Source: FR-CT15]
- [ ] **AC4** — Given the accepted coverage gap, When documented, Then the "no satellite reach" limitation is stated unless OQ-I5 grows offline emergency tokens (pre-distributed emergency JWKS in `lib/auth`). *Assert: gap documented; TBD — pinned by identity OQ-I5.* [Source: FR-CT15; assumptions index]

## 🔨 Tasks

- [ ] RED: `TestBreakGlassAllowListRejectsSatelliteActionsAndAlarmsEveryUse` (AC1,2). (AC: 1,2)
- [ ] GREEN: bounded action allow-list (identity-local + console-local). (AC: 1)
- [ ] Out-of-band alerting + per-use rate limits. (AC: 2,3)
- [ ] Document the coverage gap. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Break-glass rate-limit (Redis bucket, fails closed), out-of-band alarm on every use, and a bounded action allow-list (satellite actions rejected) — all present and tested.

Handler/middleware tier within the break-glass router group (E7-S1). The allow-list is structurally bounded by downstream verifiability. Runbook rehearsal (both IdPs blackholed) is the DoD-level proof at the epic. Sibling: E7-S1.

## 🧪 Testing

`TestBreakGlassAllowListRejectsSatelliteActionsAndAlarmsEveryUse` (handler integration). Tiers: handler + middleware. CS §5: fake the alerting sink + offline-verify at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: concentrated-privilege, honest-coverage-gap, fail-closed. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-BREAKGLASS), `kldiZu93EC` (FR-CT15, assumptions index), identity OQ-I5.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E7; E7-S1.
- [ ] Cross-repo (HARD): identity OQ-I5 (offline emergency tokens would widen the allow-list).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
