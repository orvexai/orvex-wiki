## 🎯 Story

**As a** platform operator, **I want** a satellite health board rendering each sibling's `/healthz` state and named SLIs from Mimir **so that** I can see the whole family's health in one screen (console re-renders SLIs, never redefines them).

**Definition of Done:** one named test `TestHealthBoardRendersSiblingSlisFromMimirWithoutRedefining` (handler/front — the board renders per-satellite health + the SLIs each sibling PRD names, sourced from Mimir via `/api/admin/health`, asserted through the board's data interface; SLI definitions are read as config, not hard-coded). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/admin/health`, When loaded, Then per-satellite `/healthz` + named SLIs render (knowledge FR-K28, workflows FR-W15, identity NFR-I7, wiki-api FR-A19, mcp FR-M18, ai FR-AI25, billing). *Assert: each satellite's SLIs sourced from Mimir.* [Source: FR-CT4]
- [ ] **AC2** — Given console renders SLIs, When a sibling PRD changes an SLI, Then the board tracks it as a docs/config bump. *Assert: SLI list is config, not code; console defines no new SLI semantics.* [Source: FR-CT4; Arch §5]
- [ ] **AC3** — Given a satellite admin API / metrics source is unreachable, When the board loads, Then that card shows `unreachable` (itself a signal); others live. *Assert: FR-CT18 typed degradation, no fabricated health.* [Source: FR-CT18; NFR-CT7]
- [ ] **AC4** — Given console observes itself, When the board loads, Then console appears on its own health board (its own SLIs: fan-out latency, guardrail rejections, audit-write failures, break-glass invocations). *Assert: console self-row present.* [Source: FR-CT19]

## 🔨 Tasks

- [ ] RED: `TestHealthBoardRendersSiblingSlisFromMimirWithoutRedefining` (AC1,2). (AC: 1,2)
- [ ] GREEN: `/api/admin/health` board + Mimir SLI reads. (AC: 1)
- [ ] SLI catalog as short-TTL config in Redis. (AC: 2)
- [ ] Console self-row + typed degradation. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — Satellite health board reachability half (per-sibling /healthz fan-out, isolated so one down sibling never blanks another) is present, live, and tested. MISSING: the FR-CT4 Mimir-sourced per-satellite SLI half is explicitly deferred (see health.go comment); no web health-board pane.

Handler → domain → Mimir client port. SLI catalog is principal-independent metadata cached short-TTL (A-STATE). Also renders the UBA cost SLI (E8-S8) and ai/billing SLIs. Sibling: E3-S3 (Mimir reads), E6, E8-S8.

## 🧪 Testing

`TestHealthBoardRendersSiblingSlisFromMimirWithoutRedefining` (handler/front). Tiers: handler + domain + port. CS §5: fake Mimir + satellite `/healthz` at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: honesty, re-render-not-redefine. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (§1/§5), `kldiZu93EC` (FR-CT4, FR-CT19).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E3; E3-S1/S3, E1-S5.
- [ ] Cross-repo: sibling SLI definitions; E8-S8 cost SLI.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
