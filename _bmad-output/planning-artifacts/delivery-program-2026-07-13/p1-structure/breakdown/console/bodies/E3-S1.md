## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform operator, **I want** the LGTM read plane proxied behind `/api/obs/{logs,metrics,traces,alerts}` with server-side guardrails **so that** console is never the reason Mimir/Loki fall over during the incident I am diagnosing.

**Definition of Done:** one named test `TestUnboundedObsQueryIsRejectedByGuardrail` (handler layer — a query exceeding the time-range/cardinality/volume/concurrency cap is bounded or rejected and is cancellable, asserted through the `/api/obs/*` proxy). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an obs query, When it exceeds the time-range cap, Then it is bounded/rejected server-side. *Assert: over-range query rejected with a typed guardrail error.* [Source: FR-CT6]
- [ ] **AC2** — Given series cardinality / log-line volume caps, When breached, Then the query is bounded. *Assert: high-cardinality/volume query capped, not passed raw to LGTM.* [Source: FR-CT6]
- [ ] **AC3** — Given per-operator concurrent-query limits (Redis rate buckets), When exceeded, Then further queries are rejected until a slot frees. *Assert: N+1 concurrent query rejected; buckets tenant/operator-scoped.* [Source: FR-CT6; FR-CT17]
- [ ] **AC4** — Given a long-running query, When the operator cancels, Then it is cancelled upstream. *Assert: cancellation propagates to the LGTM client (deadline/context).* [Source: FR-CT6; NFR-CT1 overhead budget]

## 🔨 Tasks

- [ ] RED: `TestUnboundedObsQueryIsRejectedByGuardrail` (AC1,2). (AC: 1,2)
- [ ] GREEN: `/api/obs/{logs,metrics,traces,alerts}` proxy handlers. (AC: 1–4)
- [ ] Guardrail evaluation as pure domain functions (`internal/obs/`). (AC: 1,2)
- [ ] Redis rate buckets + cancellable client context. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Guardrailed LGTM proxy /api/obs/{logs,metrics,traces,alerts} with FR-CT6 caps (time-range, log-volume, cardinality/response-size) enforced server-side, plus typed degradation. Fully tested.

Handler → domain (guardrail evaluation, pure) → LGTM client port (confined read-only creds from E2-S3). NFR-CT1 BFF-overhead p95 ≤ 50 ms measured here. Sibling: E3-S2/S3/S4 render on top of this proxy; E1-S6 supplies the isolated client framework.

## 🧪 Testing

`TestUnboundedObsQueryIsRejectedByGuardrail` (handler + domain). Tiers: handler + domain + port. CS §5: fake the LGTM backend at the network boundary via golden fixtures; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp. §6 tiering). SE-Arch lenses: counter-metric/guardrail, honesty. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-PANE), `kldiZu93EC` (FR-CT6, NFR-CT1).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E3; E1-S6, E2-S1/S3.
- [ ] Intra-service: E3-S2/S3/S4/S5/S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
