## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform operator, **I want** Temporal workflow visibility (list/filter/inspect) reached only through the workflows authorizing proxy **so that** I can see workflow status/history/pending-activities/retry/schedule state with console holding no Temporal credential (D-CTRL).

**Definition of Done:** one named test `TestWorkflowReadsGoThroughProxyNeverTemporalFrontend` (client layer — a workflow list/inspect read is issued to the workflows authorizing-proxy client carrying the operator's pass-through token, and no code path constructs a direct `temporal-frontend` client, asserted through the client + a grep gate). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/workflows` + `/api/workflows/:id`, When queried, Then workflows across the family's domains list/inspect with status/history/pending-activities/retry/schedule state. *Assert: read fields render for a real workflow.* [Source: FR-CT3]
- [ ] **AC2** — Given any Temporal read, When issued, Then it goes to the workflows authorizing proxy with the operator's pass-through token (proxy scopes to the per-app namespace). *Assert: request hits the proxy client; no direct `temporal-frontend`.* [Source: FR-CT3; D-CTRL]
- [ ] **AC3** — Given console's codebase, When scanned, Then there is exactly one Temporal binding (the proxy client) and no `temporal-frontend` egress. *Assert: grep gate + NetworkPolicy from E1-S5.* [Source: NFR-CT2; D-CTRL]
- [ ] **AC4** — Given the proxy is unreachable, When the workflow pane loads, Then it degrades with a typed error; LGTM/admin panes unaffected. *Assert: FR-CT18 typed degradation; this panel ships no earlier than the proxy exists.* [Source: FR-CT18; Arch §5]

## 🔨 Tasks

- [ ] RED: `TestWorkflowReadsGoThroughProxyNeverTemporalFrontend` (AC2,3). (AC: 2,3)
- [ ] GREEN: single workflows-proxy client interface + `/api/workflows[/:id]` handlers. (AC: 1,2)
- [ ] grep gate + NetworkPolicy assert. (AC: 3)
- [ ] Typed degradation + workflow_id pivot from E3-S5. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Workflow visibility reads go exclusively through the workflows authorizing proxy with operator-token pass-through; console constructs no Temporal client anywhere (asserted by celllint no-temporal-egress). BFF present and tested. Note: no web workflows-list pane.

Ports/clients tier: one workflows-proxy client, one binding. Handler thin. The workflow_id correlation pivot (E3-S5) lands here. Sibling: E4-S2 (writes reuse the same client), E4-S3 (lifecycle view).

## 🧪 Testing

`TestWorkflowReadsGoThroughProxyNeverTemporalFrontend` (client + grep gate). Tiers: ports + handler. CS §5: fake the proxy at the network boundary via golden fixtures; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: concentrated-privilege (Temporal-by-removal), honesty. Cell-lint `JGAUQRsw2g` rule 13 (no cell-id parse).

## 🔗 References

- `L1NVnmRgpy` (A-EDGE, A-ADMIN, §5), `kldiZu93EC` (FR-CT3, D-CTRL).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E4; E1-S3/S5, E2-S3.
- [ ] Cross-repo (HARD): workflows authorizing-proxy (un-chartered backlog item) — FR-CT3 ships no earlier.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
