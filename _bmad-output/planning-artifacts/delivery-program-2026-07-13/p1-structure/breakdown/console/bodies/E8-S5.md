## 🎯 Story

**As a** staff viewer, **I want** every analytics panel attributed and timestamped, degrading independently, and carrying an explicit `ILLUSTRATIVE — sample data` banner while pre-launch **so that** a sample-data chart never renders as live product truth.

**Definition of Done:** one named test `TestPreLaunchPanelsCarryIllustrativeBannerAndDegradeIndependently` (front/domain layer — every panel renders source + as-of, carries the `ILLUSTRATIVE` banner until wired to a seeded synthetic cohort, and a PostHog-unreachable panel degrades alone with no synthetic fallback, asserted through the panel state interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given any analytics panel, When rendered, Then it shows its source + as-of timestamp. *Assert: source + as-of present on every panel.* [Source: FR-CT-UBA-5; NFR-CT-UBA-4]
- [ ] **AC2** — Given pre-launch, When a panel renders, Then it carries an explicit `ILLUSTRATIVE — sample data` banner until wired to a seeded synthetic cohort (then real tenants). *Assert: banner present pre-launch; a sample-data chart never renders as live truth.* [Source: FR-CT-UBA-5; A-UBA-HONESTY]
- [ ] **AC3** — Given PostHog is unreachable, When a panel loads, Then it degrades with a typed unavailable state; others live; no synthetic fallback. *Assert: NFR-CT-UBA-3 no cross-panel fate-sharing.* [Source: FR-CT-UBA-5; NFR-CT-UBA-3]
- [ ] **AC4** — Given caching, When a cohort is stale, Then console never caches its way into showing a stale cohort as live. *Assert: NFR-CT-UBA-4 honesty; honest empty over fabricated.* [Source: NFR-CT-UBA-4]

## 🔨 Tasks

- [ ] RED: `TestPreLaunchPanelsCarryIllustrativeBannerAndDegradeIndependently` (AC2,3). (AC: 2,3)
- [ ] GREEN: attributed-render + ILLUSTRATIVE banner (reuses E1-S6 framework). (AC: 1,2)
- [ ] Independent typed degradation. (AC: 3)
- [ ] Stale-cohort honesty. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Front + domain (honest-render shaping). Extends A-HONESTY / NFR-CT7 to the analytics plane. Sibling: E1-S6 (framework), E8-S3 (surfaces).

## 🧪 Testing

`TestPreLaunchPanelsCarryIllustrativeBannerAndDegradeIndependently` (front/domain). Tiers: front + domain. CS §5: fake PostHog reachability at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp. §11 ALL-REAL/LIVE, honest empty states). SE-Arch lenses: honesty (the fake-done antibody). Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `Ii9q9kBNms` (§4 A-UBA-HONESTY), `TYWAtnvliD` (FR-CT-UBA-5, NFR-CT-UBA-3/4).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E8; E1-S6, E8-S3.
- [ ] Intra-service: E8-S6/S7 (banner applies to embed panes too).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
