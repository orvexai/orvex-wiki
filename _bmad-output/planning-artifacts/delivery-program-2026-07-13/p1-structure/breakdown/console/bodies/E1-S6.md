## 🎯 Story

**As a** console developer, **I want** a Redis-only state adapter plus a per-backend isolated-client/circuit-breaker framework that renders every panel with source + as-of attribution and degrades independently **so that** console holds no domain state (A-STATE) and never fabricates data for a dead backend (A-HONESTY).

**Definition of Done:** one named test `TestDeadBackendDegradesPanelWithoutCrossPanelFateSharing` (framework layer — a client whose backend is unreachable returns a typed unavailable state; the panel renders `unreachable` with an as-of timestamp while sibling panels stay live, asserted through the render/state interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the store, When any datastore is used, Then only Redis exists (no Postgres/Mongo). *Assert: only a `Cache`/session-store Redis adapter; no other datastore package.* [Source: FR-CT17; A-STATE, D-CT4]
- [ ] **AC2** — Given tenant-derived cache keys, When written, Then every key embeds the tenant and allow/deny is never cached. *Assert: key builder requires tenant; a deny result is never persisted.* [Source: FR-CT17; NFR-CT5]
- [ ] **AC3** — Given a backend is unreachable, When its panel renders, Then it shows a typed unavailable state with source + as-of, and other panels are unaffected. *Assert: no cross-panel cascade; no synthetic fallback data.* [Source: FR-CT18; NFR-CT7]
- [ ] **AC4** — Given Redis loss, When a read misses, Then it is slower, never wrong. *Assert: cold cache = recompute path, no stale allow/deny served.* [Source: FR-CT17; A-STATE]

## 🔨 Tasks

- [ ] RED: `TestDeadBackendDegradesPanelWithoutCrossPanelFateSharing` (AC3). (AC: 3)
- [ ] GREEN: isolated-client wrapper (circuit breaker, deadline, typed unavailable). (AC: 3)
- [ ] Redis adapter behind a `Cache`/session interface; tenant-embedded keys. (AC: 1,2,4)
- [ ] Attributed-render helper (source + as-of). (AC: 3)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — Redis-only stateless-BFF posture (config + smoke tests asserting no Postgres/Mongo) and the isolated-client honest-render framework (typed per-signal/per-card DegradedError, no cross-panel fate-sharing) are present and tested. MISSING: no dedicated circuit-breaker and no generic Redis state-adapter abstraction — Redis is used directly (go-redis) only for the break-glass rate bucket.

Store tier (`internal/store/redis/`) + a cross-cutting ports/clients framework (`pkg/*` clients each a port with a real + test-fake adapter). Domain tier holds the pure honest-render shaping. Sibling: every pane epic (E3/E4/E5/E6/E8) uses this framework; NFR-CT7 honesty is anchored here.

## 🧪 Testing

`TestDeadBackendDegradesPanelWithoutCrossPanelFateSharing` (framework/unit + integration). Tiers: store + ports + domain. CS §5: fake the network client from a golden fixture (network boundary — port justified); do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp. §3.2 ports, §11 honest states). SE-Arch lenses: honesty, isolation. Cell-lint `JGAUQRsw2g` rule 7 (no owned schema).

## 🔗 References

- `L1NVnmRgpy` (A-STATE, A-HONESTY, §6), `kldiZu93EC` (FR-CT17/CT18, NFR-CT7).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E1; E1-S1.
- [ ] Intra-service: consumed by all pane epics.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
