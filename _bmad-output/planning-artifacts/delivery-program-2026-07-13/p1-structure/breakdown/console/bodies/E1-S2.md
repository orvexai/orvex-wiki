## 🎯 Story

**As a** console developer, **I want** the React/shadcn SPA to build to static assets embedded in the Go binary and served from one origin **so that** there is one deploy artifact, no CORS surface, and a strict CSP — the family's first non-pure-Go build.

**Definition of Done:** one named test `TestEmbeddedSpaServedOneOriginWithCsp` (integration — a booted binary serves the embedded `index.html` at `/` with a strict `Content-Security-Policy` header whose `script-src`/`connect-src` are self-only, asserted through the HTTP interface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a built binary, When GET `/`, Then the `go:embed`-ed SPA index is served from the same origin as `/api`. *Assert: 200 `text/html`, same host/port as `/api`.* [Source: Arch A-FRONTEND, D-CT1]
- [ ] **AC2** — Given any SPA response, When headers are read, Then a strict CSP with self-only `script-src`/`connect-src` is present. *Assert: CSP header excludes wildcard sources; no external script host.* [Source: Arch A-FRONTEND; NFR-CT2]
- [ ] **AC3** — Given the front end build, When the AGPL guard runs over `package.json`, Then no `@docmost/*` client dependency is present. *Assert: guard passes; a seeded `@docmost/*` dep fails it.* [Source: NFR-CT4]
- [ ] **AC4** — Given the two-stage build (node → embed → `BP_GO_TARGETS`), When CI builds, Then the image builds without a separate node runtime at serve time. *Assert: runtime image has no node process; assets are embedded.* [Source: Arch §4 build delta]

## 🔨 Tasks

- [ ] RED: `TestEmbeddedSpaServedOneOriginWithCsp` asserting index + CSP (AC1,2). (AC: 1,2)
- [ ] GREEN: SPA scaffold (React/shadcn), `go:embed` mount, CSP middleware. (AC: 1,2)
- [ ] Two-stage build wiring (node build → embed path → Go build). (AC: 4)
- [ ] Front-end AGPL-import check hook (full gate in E1-S3). (AC: 3)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — go:embed SPA pipeline + one-origin catch-all (never swallows /api) present and tested; secret-free cell bootstrap injected. MISSING: no strict CSP header is set anywhere (grep for Content-Security-Policy/default-src/frame-ancestors finds nothing in Go, HTML, or the httproute manifest) — the 'strict CSP' half of the story is unimplemented.

Front tier: React SPA (thin presentational, talks only to its own BFF — D-CT5). Go handler serves embedded assets. Seam: `go:embed` build boundary (first non-pure-Go build — ADR-worthy, filed in E1). Sibling: E1-S3 lands the enforcing AGPL gate; E3/E8 panes build inside this SPA.

## 🧪 Testing

`TestEmbeddedSpaServedOneOriginWithCsp` (integration). Tiers: handler + build. CS §5: no external mocks; do not mock own packages. Build-pipeline asserted in CI, not unit-mocked.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + §9 (A-FRONTEND ADR trigger). SE-Arch lenses: AGPL-boundary, honesty. Cell-lint `JGAUQRsw2g` (host-as-config for the SPA origin).

## 🔗 References

- `L1NVnmRgpy` (A-FRONTEND, §4), `kldiZu93EC` (NFR-CT4).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E1; E1-S1 (binary).
- [ ] Intra-service: E1-S3 (AGPL enforcing gate).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
