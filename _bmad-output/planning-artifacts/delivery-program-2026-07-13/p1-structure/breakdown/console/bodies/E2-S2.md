## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform security owner, **I want** every admin mutation to additionally require fresh, no-grace introspection plus a typed confirmation **so that** the family's highest-privilege operations cannot ride a cached or stale session.

**Definition of Done:** one named test `TestMutationRequiresFreshIntrospectionAndConfirmation` (middleware layer — a mutation with a token that passes cached verification but fails fresh no-grace introspection is rejected, and a mutation lacking the typed confirmation token is rejected, asserted through the mutation middleware). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a mutation route, When invoked, Then `RequireFresh` (lib FR-L5) no-grace introspection runs. *Assert: a token fresh-introspecting as revoked is rejected even if the JWKS cache is valid.* [Source: FR-CT13; Arch A-EDGE]
- [ ] **AC2** — Given a mutation, When the typed confirmation is absent/malformed, Then it is rejected. *Assert: 400/409 without the typed confirmation; present → proceeds.* [Source: FR-CT10/CT13; Arch A-ADMIN]
- [ ] **AC3** — Given a read route, When invoked, Then `RequireFresh` is NOT required (reads = admin-org floor). *Assert: reads succeed without fresh introspection.* [Source: FR-CT13 floor]
- [ ] **AC4** — Given OQ-CT3 RBAC is unresolved, When any operator mutates, Then the accepted floor is admin-org + fresh introspection + confirmation (all operators family-global). *Assert: no finer role check yet; posture documented as TBD — pinned by the Definition Pack (ENG-2106) / OQ-CT3.* [Source: FR-CT13; assumptions index]

## 🔨 Tasks

- [ ] RED: `TestMutationRequiresFreshIntrospectionAndConfirmation` (AC1,2). (AC: 1,2)
- [ ] GREEN: mutation middleware wrapping RequireFresh + typed-confirmation parse. (AC: 1,2)
- [ ] Ensure reads bypass the fresh gate. (AC: 3)
- [ ] Document the OQ-CT3 flat-admin floor. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Fresh no-grace introspection on every mutation route + typed per-target confirmation (echo the workflow/resource id) enforced before any upstream call; fail-closed on stale/errored freshness. Tested end-to-end.

Middleware tier (mutation-only). Depends on E2-S1's router. The A-ADMIN wrap (E2-S4) chains after this gate. RBAC grading (OQ-CT3) is a future refinement; this story lands the floor.

## 🧪 Testing

`TestMutationRequiresFreshIntrospectionAndConfirmation` (middleware). Tiers: middleware + port. CS §5: fake introspection at the network seam via `authtest`; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: concentrated-privilege, fail-closed. Cell-lint `JGAUQRsw2g` rule 3.

## 🔗 References

- `L1NVnmRgpy` (A-EDGE, A-ADMIN), `kldiZu93EC` (FR-CT13, OQ-CT3), lib FR-L5.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E2; E2-S1.
- [ ] Intra-service: E2-S4 (audit wrap), all mutation stories (E4-S2, E5-S2/S3, E6-S2).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
