## 🎯 Story

**As a** platform security owner, **I want** every analytics read verified via `lib/auth` requiring Clerk admin-org membership, with the PostHog credential confined to the analytics handler package and reads audited as sensitive-surface reads **so that** the analytics plane holds the same A-EDGE boundary as every other console route.

**Definition of Done:** one named test `TestAnalyticsCredConfinedAndReadAudited` (gate + handler layer — an admin-mutation package importing the PostHog credential fails the build-tag/lint gate, and an analytics read requires admin-org membership and emits a sensitive-read audit record, asserted through the gate + handler). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an analytics route, When hit without Clerk admin-org membership, Then it is denied (deny-by-default; undeclared route rejected at startup). *Assert: 403 without admin-org; undeclared analytics route rejected.* [Source: FR-CT-UBA-6; A-UBA-EDGE]
- [ ] **AC2** — Given the PostHog credential (project token / personal-API key), When packaged, Then it is confined to the analytics handler package by the same build-tag/lint gate that confines the LGTM creds. *Assert: admin-mutation package importing it fails the gate (extends E1-S3).* [Source: FR-CT-UBA-6; A-UBA-EDGE]
- [ ] **AC3** — Given the embed outcome, When a dashboard mounts, Then the PostHog session is minted server-side from the verified Clerk-admin principal (PostHog never directly routable); under query it is a read-only HogQL credential. *Assert: no browser-reachable PostHog session.* [Source: FR-CT-UBA-6/10]
- [ ] **AC4** — Given an analytics read, When served, Then it is audited as a sensitive-surface read (FR-CT14 read-audit); the only privileged write is the erasure call (E8-S8). *Assert: read-audit record emitted.* [Source: FR-CT-UBA-6; A-UBA-READONLY]

## 🔨 Tasks

- [ ] RED: `TestAnalyticsCredConfinedAndReadAudited` (AC2,4). (AC: 2,4)
- [ ] GREEN: analytics route registration under `lib/auth`; admin-org gate. (AC: 1)
- [ ] Extend the cred-confinement gate to the PostHog credential. (AC: 2)
- [ ] Server-side session mint (embed) / read-only HogQL cred (query) + read-audit. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Middleware + ports tier. Extends E1-S3's confinement gate and E2-S1's router to the analytics package. A-UBA-READONLY: analytics mutates nothing except the erasure cascade (E8-S8). Sibling: E8-S1, E8-S6/S7 (embed session mint).

## 🧪 Testing

`TestAnalyticsCredConfinedAndReadAudited` (gate + handler). Tiers: CI gate + middleware + port. CS §5: fake PostHog session-mint at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: concentrated-privilege, cred-confinement. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `Ii9q9kBNms` (§4 A-UBA-EDGE/A-UBA-READONLY), `TYWAtnvliD` (FR-CT-UBA-6).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E8; E1-S3, E2-S1/S4, E8-S1.
- [ ] Cross-repo: adopt-PostHog ADR (OQ-UBA-1).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
