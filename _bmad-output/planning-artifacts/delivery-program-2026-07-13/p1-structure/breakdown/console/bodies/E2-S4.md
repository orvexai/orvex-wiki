## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform auditor, **I want** every admin mutation wrapped intent-audit → upstream call → outcome-audit, emitted to the durable, ackable family audit stream, where a failure to audit fails the mutation **so that** there are zero unaudited admin writes (G3).

**Definition of Done:** one named test `TestAuditWriteFailureFailsTheMutation` (workflow layer — when the audit emit to the family Kafka topic cannot be acked, the mutation is not performed and the operator receives a failure, asserted through the mutation workflow). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an admin mutation, When executed, Then the sequence is intent-record → upstream call → outcome-record, all emitted to the family audit stream. *Assert: two audit records (intent, outcome) with actor/tenant/target/satellite/action/outcome + upstream correlation id.* [Source: FR-CT14; Arch A-ADMIN]
- [ ] **AC2** — Given the audit emit cannot be durably acked (`acks=all`), When a mutation is attempted, Then the mutation fails (audit-or-fail). *Assert: no upstream mutation call proceeds; operator gets failure.* [Source: FR-CT14; D-CTRL/D-P6]
- [ ] **AC3** — Given audit parameters, When recorded, Then secrets are redacted. *Assert: no secret bytes in the audit record; params secret-redacted.* [Source: FR-CT14; NFR-CT2]
- [ ] **AC4** — Given the durable stream is not yet live, When console audits a **read**, Then structured-logs-into-Loki is acceptable (read-audit only), but a **mutation** is gated on the durable stream. *Assert: mutation blocked until Kafka topic live; TBD — pinned by the Definition Pack (ENG-2106) for the stream build-out.* [Source: FR-CT14; assumptions index]

## 🔨 Tasks

- [ ] RED: `TestAuditWriteFailureFailsTheMutation` (AC2). (AC: 2)
- [ ] GREEN: the A-ADMIN wrap in `internal/workflow/` (liveness only); audit-record construction in `internal/audit/` (pure). (AC: 1)
- [ ] Family audit-stream Kafka writer via the contracts envelope helper (`acks=all`). (AC: 1,2)
- [ ] Secret redaction in record construction. (AC: 3)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Audit-before-respond mutation wrap where a failed audit write fails the mutation; durable CloudEvents Kafka writer to the family audit stream. Comprehensively tested (unit + integration).

Workflow tier (`internal/workflow/`) sequences fresh-introspection + intent-audit + upstream call + outcome-audit across ≥2 domain packages — liveness branching only; record construction is a pure domain function (`internal/audit/`), NOT handler-resident (CS ❌#1). Emits to the family topic via the shared envelope helper; hosts no owned topic (D-CTRL).

## 🧪 Testing

`TestAuditWriteFailureFailsTheMutation` (workflow integration). Tiers: workflow + domain + event port. CS §5: fake the Kafka producer + satellite at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp. §6 tiering, §10 no-secrets-in-logs). SE-Arch lenses: honesty, fail-closed. Cell-lint `JGAUQRsw2g` rules 5/6 (shared envelope helper).

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN, §6), `kldiZu93EC` (FR-CT14, D-CTRL), contracts audit-event type.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E2; E2-S2, E1-S6.
- [ ] Cross-repo (HARD): family audit-stream Kafka build-out (D-P6) — gates mutation durability.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
