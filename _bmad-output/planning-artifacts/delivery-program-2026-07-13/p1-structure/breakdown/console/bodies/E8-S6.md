## 🎯 Story

**As a** sales/product staffer, **I want** the PostHog session-replay player embedded behind the BFF auth-proxy **so that** I can watch real user journeys (the native player, not rebuilt in shadcn) with PostHog never directly routable — capture being client-side and NOT console's concern.

**Definition of Done:** one named test `TestReplayPlayerEmbeddedBehindAuthProxyNeverBrowserRoutable` (handler/front — the replay player mounts via an iframe whose PostHog session is minted server-side from the verified Clerk-admin principal, and PostHog is never reachable directly from the browser, asserted through the embed handler + CSP). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the replay surface, When mounted, Then the native PostHog replay player is embedded (embed outcome preferred — not rebuilt in shadcn) behind the BFF auth-proxy. *Assert: iframe embed; player not reimplemented.* [Source: FR-CT-UBA-11; §7]
- [ ] **AC2** — Given the embed, When served, Then the PostHog session is minted server-side from the verified Clerk-admin principal; a `frame-src` CSP addition scopes it; PostHog is never directly routable. *Assert: no browser→PostHog session; CSP frame-src set.* [Source: FR-CT-UBA-11/10; A-UBA-EDGE]
- [ ] **AC3** — Given capture is client-side (posthog-js in the frontends, consent-FIRST + masked), When console renders, Then console is NOT the capture path and records nothing itself. *Assert: console has no posthog-js capture; capture is OQ-UBA-10 (frontends).* [Source: FR-CT-UBA-11; FR-CT-UBA-8a path 2]
- [ ] **AC4** — Given a recording's erasure, When a person is purged, Then the DELETE-Persons cascade (E8-S8) removes recordings too. *Assert: erasure covers recordings; retention window TBD — pinned by OQ-UBA-9.* [Source: FR-CT-UBA-11; NFR-CT-UBA-1]

## 🔨 Tasks

- [ ] RED: `TestReplayPlayerEmbeddedBehindAuthProxyNeverBrowserRoutable` (AC2). (AC: 2)
- [ ] GREEN: replay embed handler + server-side session mint + frame-src CSP. (AC: 1,2)
- [ ] Assert console holds no capture path. (AC: 3)
- [ ] Wire erasure coverage to E8-S8. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Handler (embed session mint) + front (iframe). Client-side capture (posthog-js in engine React client + studio-ui) is OTHER services (OQ-UBA-10) — console fronts only. Sibling: E8-S2 (session mint/cred), E8-S8 (erasure).

## 🧪 Testing

`TestReplayPlayerEmbeddedBehindAuthProxyNeverBrowserRoutable` (handler/front). Tiers: handler + front. CS §5: fake PostHog session-mint at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + §10 (privacy — most sensitive piece). SE-Arch lenses: never-over-expose, cred-confinement. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `Ii9q9kBNms` (§ Session replay, §7), `TYWAtnvliD` (FR-CT-UBA-11, NFR-CT-UBA-1).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E8; E8-S1/S2/S5.
- [ ] Cross-repo (HARD): posthog-js client-dep ADR (OQ-UBA-10, frontends); consent SoR (OQ-UBA-4); adopt-PostHog ADR.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
