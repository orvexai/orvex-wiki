## 🎯 Story

**As a** platform operator, **I want** a plan/entitlement + quota-usage pane rendering each org's plan and entitlement set from billing alongside live quota usage read back from the engine's F-QUOTA counters, and fronting billing's entitlement-admin surface for the *set* **so that** I can see and (Phase 3) change plan/entitlement/cap without console ever becoming the system of record.

**Definition of Done:** one named test `TestEntitlementPaneReadsBillingAndEngineQuotaStoresNeither` (handler layer — `/api/admin/entitlements` renders plan + entitlement from billing and `/api/admin/quota` renders pages/bytes/files/members from engine F-QUOTA, and console persists neither, asserted through the handlers + a no-store assertion). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/admin/entitlements`, When loaded, Then each org's plan (Free / £7 Personal / Teams / Enterprise) + entitlement set renders from billing (plans/entitlements SoR, D-P3). *Assert: plan + entitlement from billing.* [Source: FR-CT12b; D-P3]
- [ ] **AC2** — Given `/api/admin/quota`, When loaded, Then live usage reads back from engine F-QUOTA (pages/bytes/files/members) — e.g. Free ceiling 200 pages / 1 GiB / 2,000 files / 25 members; £7 Personal 20,000 / 50 GiB / 20,000 / 25. *Assert: usage vs the correct plan ceiling.* [Source: FR-CT12b]
- [ ] **AC3** — Given a plan/entitlement/cap-value *set* (Phase 3), When issued, Then it fronts billing's admin surface under the FR-CT9 wrap (operator token pass-through; billing keeps authz + audit); console enforces nothing. *Assert: set = billing call; engine enforces F-QUOTA at its chokepoint.* [Source: FR-CT12b; A-ADMIN]
- [ ] **AC4** — Given values are billing's + engine's, When rendered, Then console stores neither and shows source + as-of. *Assert: no entitlement/quota persistence; attributed render.* [Source: FR-CT12b; NFR-CT7]

## 🔨 Tasks

- [ ] RED: `TestEntitlementPaneReadsBillingAndEngineQuotaStoresNeither` (AC1,2,4). (AC: 1,2,4)
- [ ] GREEN: `/api/admin/entitlements` (billing) + `/api/admin/quota` (engine F-QUOTA). (AC: 1,2)
- [ ] Phase-3 set fronting billing under the audit wrap. (AC: 3)
- [ ] No-store + attributed render. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — BFF plan/entitlement read+set (billing SoR) and engine F-QUOTA usage read present and gate/audit-tested. MISSING: no web plan/entitlement/quota-usage pane in web/src.

Handler → domain → billing-admin + engine-quota clients (pass-through). Read-only in Phase 2; the set is Phase 3 (behind billing). `billing.entitlement.changed` reaches console's short-TTL caches via the eviction path. Sibling: E6-S1, E2-S4 (set audit wrap).

## 🧪 Testing

`TestEntitlementPaneReadsBillingAndEngineQuotaStoresNeither` (handler integration). Tiers: handler + domain + ports. CS §5: fake billing + engine F-QUOTA at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: compose-not-own (no SoR), honesty. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN, §3), `kldiZu93EC` (FR-CT12b, D-P3, D-S7), billing SoR, engine F-QUOTA.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E6; E1-S6, E2-S1/S3/S4.
- [ ] Cross-repo: billing plans/entitlements SoR + admin surface; engine F-QUOTA read-back.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
