## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform security owner, **I want** satellite admin calls to pass the operator's identity-minted token through, with the only standing service credential being the read-only LGTM one confined to the observability package **so that** a scoped operator can never do through console what they couldn't do against the satellite directly (the wiki-api FR-A16 lesson).

**Definition of Done:** one named test `TestSatelliteAdminCallPassesOperatorTokenNotElevatedCred` (client layer — an admin call to a satellite carries the operator's pass-through token and no elevated/service credential is attached on any mutation path, asserted through the satellite-admin client). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a satellite admin call, When issued, Then it carries the operator's identity token, passed through. *Assert: outgoing `Authorization` = operator token; no elevated credential.* [Source: FR-CT16; D-CT6; Arch A-EDGE]
- [ ] **AC2** — Given the LGTM query APIs (backends that cannot verify identity tokens), When queried, Then a read-only standing credential is used, confined to the observability package. *Assert: creds loaded only by the observability client; ExternalSecrets-sourced, rotatable.* [Source: FR-CT16; NFR-CT2]
- [ ] **AC3** — Given any admin-mutation code path, When it runs, Then it cannot reach the LGTM credential (package boundary). *Assert: E1-S3 gate holds; mutation path has no observability-cred import.* [Source: Arch A-EDGE §4]
- [ ] **AC4** — Given Temporal, When accessed, Then NO credential exists — access is via the workflows proxy only. *Assert: no Temporal client/credential anywhere (D-CTRL).* [Source: FR-CT16; D-CTRL]

## 🔨 Tasks

- [ ] RED: `TestSatelliteAdminCallPassesOperatorTokenNotElevatedCred` (AC1). (AC: 1)
- [ ] GREEN: satellite-admin client that threads the operator token. (AC: 1)
- [ ] Observability client with confined read-only LGTM creds (ExternalSecrets). (AC: 2,3)
- [ ] Assert no Temporal credential exists. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Operator token forwarded byte-equal to each satellite's own operator surface (no elevated console credential on admin paths); the only standing credential is the read-only LGTM token, confined to internal/observability. Tested.

Ports/clients tier: each satellite-admin client + the LGTM client is a port with a real + test-fake adapter. Confinement enforced by E1-S3's gate. Sibling: all admin epics (E5/E6) consume the pass-through client; E3 consumes the confined LGTM client.

## 🧪 Testing

`TestSatelliteAdminCallPassesOperatorTokenNotElevatedCred` (client/unit). Tiers: ports. CS §5: fake the satellite at the network boundary via golden fixtures; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: concentrated-privilege, no-elevated-cred. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-EDGE, D-CT6, D-CTRL), `kldiZu93EC` (FR-CT16, NFR-CT2).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E2; E1-S3, E2-S1.
- [ ] Intra-service: E3 (LGTM client), E5/E6 (pass-through clients), E4 (proxy-only Temporal).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
