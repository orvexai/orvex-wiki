## 🎯 Story

**As a** console developer, **I want** an analytics BFF edge with a `pkg/posthog` port and a guardrailed `/api/analytics/*` surface, computing no metric math itself **so that** console fronts PostHog exactly as it fronts LGTM — funnel/retention/cohort math stays in PostHog, never in the BFF.

**Definition of Done:** one named test `TestAnalyticsBffComputesNoMetricMathAndGuardsHogql` (handler/domain layer — a HogQL read passes through the guardrailed `/api/analytics/*` proxy with bounded range/cardinality/cohort/concurrency, and no funnel/retention/rollup is aggregated in console, asserted through the handler + a no-aggregation assertion on `internal/analytics/`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `internal/analytics/`, When inspected, Then it holds only pure guardrail/response-shaping functions — the metric math (funnel attribution, retention joins, WAU/MAU, cohort aggregation) is computed in PostHog. *Assert: no aggregation in console; a seeded in-BFF rollup fails the gate.* [Source: FR-CT-UBA-9]
- [ ] **AC2** — Given the query outcome (OQ-UBA-2), When a HogQL read runs, Then FR-CT6-style guardrails apply (bounded time range, dimension cardinality, cohort size, concurrent-query caps, cancellable). *Assert: over-budget HogQL rejected.* [Source: FR-CT-UBA-3]
- [ ] **AC3** — Given `pkg/posthog`, When used, Then it is a port at the network seam with a real adapter + golden-fixture fake. *Assert: port faked from fixtures in tests.* [Source: FR-CT-UBA-9]
- [ ] **AC4** — Given the render mechanism is an open embed-vs-query decision (OQ-UBA-2), When built, Then the surface is repo-private (D-CT5) and PostHog is never browser-reachable under either outcome. *Assert: no direct browser→PostHog path; TBD embed-vs-query — pinned by OQ-UBA-2.* [Source: FR-CT-UBA-10]

## 🔨 Tasks

- [ ] RED: `TestAnalyticsBffComputesNoMetricMathAndGuardsHogql` (AC1,2). (AC: 1,2)
- [ ] GREEN: `/api/analytics/*` guardrailed proxy + `pkg/posthog` port. (AC: 2,3)
- [ ] Pure guardrail/shaping funcs in `internal/analytics/`. (AC: 1)
- [ ] Repo-private surface; no browser→PostHog path. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Handler → domain (`internal/analytics/` pure) → `pkg/posthog` port. Reuses the E3 A-PANE guardrail pattern verbatim. Embed-vs-query (OQ-UBA-2) leans embed for replay/web-analytics (E8-S6/S7), query viable for the five charts (E8-S3). Sibling: E8-S2 (authz/cred).

## 🧪 Testing

`TestAnalyticsBffComputesNoMetricMathAndGuardsHogql` (handler + domain). Tiers: handler + domain + port. CS §5: fake PostHog HogQL at the network boundary via golden fixtures; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp. §6 tiering, no BFF creep). SE-Arch lenses: compose-not-compute, guardrail. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `Ii9q9kBNms` (§4 A-UBA-EDGE), `TYWAtnvliD` (FR-CT-UBA-3/9/10).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E8; E1-S6, E2-S1, E3-S1.
- [ ] Cross-repo (HARD): adopt-PostHog-Cloud-EU ADR (OQ-UBA-1) — no UBA story contract-tagged until it lands.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
