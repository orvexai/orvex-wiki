## 🎯 Story

**As a** platform operator, **I want** to browse the org/tenant registry with provisioning history and lifecycle-state inspection, fronted from identity **so that** I can look up any org (orgs-first: a solo user is a personal org) — with lifecycle **writes** gated until identity commits an operator-lifecycle admin surface (OQ-CT9).

**Definition of Done:** one named test `TestOrgRegistryBrowseIsReadOnlyUntilLifecycleSurfaceExists` (handler layer — `/api/admin/tenants` returns the org/tenant/idp/status registry from identity FR-I8 and any operator lifecycle-action attempt returns a typed "not-yet-available" (OQ-CT9), asserted through the handler; console never calls the FR-I14 workflows-only path). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/admin/tenants`, When browsed, Then the org/tenant registry (org/realm, tenant, idp, status — FR-I8) renders, with provisioning history + lifecycle state. *Assert: registry fields render from identity.* [Source: FR-CT7; identity FR-I8]
- [ ] **AC2** — Given an operator lifecycle action, When attempted, Then it returns a typed not-yet-available (write half blocked on OQ-CT9). *Assert: no FR-I14 call; typed "TBD — pinned by OQ-CT9".* [Source: FR-CT7; OQ-CT9]
- [ ] **AC3** — Given console never talks to Clerk/Keycloak directly, When any identity call is made, Then it goes through identity only. *Assert: no direct IdP call; identity is the only IdP-facing party.* [Source: FR-CT7 platform rule]
- [ ] **AC4** — Given identity is unreachable, When the pane loads, Then the tenant card shows unreachable; other panes live. *Assert: FR-CT18 typed degradation.* [Source: FR-CT18]

## 🔨 Tasks

- [ ] RED: `TestOrgRegistryBrowseIsReadOnlyUntilLifecycleSurfaceExists` (AC1,2). (AC: 1,2)
- [ ] GREEN: `/api/admin/tenants` handler over identity FR-I8. (AC: 1)
- [ ] Typed not-yet-available for lifecycle writes. (AC: 2)
- [ ] No direct IdP call; typed degradation. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — Org/tenant registry browse (read half) present at the BFF with admin-org gating and typed degradation. MISSING: no provisioning-history view, and no web org/tenant registry pane. Write half intentionally deferred (matches story's read-half scope).

Handler → domain (shaping) → identity-admin client (operator-token pass-through, E2-S3). Write half explicitly blocked on OQ-CT9 (identity has not committed an operator-lifecycle surface; FR-I14 is workflows-only, not callable). Sibling: E4-S3 (workflow-side lifecycle view), E5-S2.

## 🧪 Testing

`TestOrgRegistryBrowseIsReadOnlyUntilLifecycleSurfaceExists` (handler integration). Tiers: handler + domain + port. CS §5: fake identity admin API at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: compose-not-bypass, honesty, scope-discipline. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN), `kldiZu93EC` (FR-CT7, OQ-CT9), identity FR-I8/I14/I17.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E5; E1-S6, E2-S1/S3.
- [ ] Cross-repo (HARD, write half): identity operator-lifecycle admin surface (OQ-CT9).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
