## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform operator, **I want** to trigger knowledge reindex/rebuild via knowledge's FR-K25 admin surface **so that** I can drive a reindex from console while knowledge — not console — publishes the spine rebuild enqueue and keeps its own authz + audit.

**Definition of Done:** one named test `TestReindexCallsKnowledgeSurfaceConsoleNeverPublishesEnqueue` (workflow layer — a reindex on `/api/admin/reindex` calls knowledge FR-K25 with the operator's token, audit-wrapped, and console emits NO spine CloudEvent itself, asserted through the mutation workflow + an assertion that no publisher is invoked). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/admin/reindex`, When triggered, Then it calls knowledge's FR-K25 admin surface with the operator's token. *Assert: reindex = knowledge call; knowledge then enqueues the spine CloudEvent (FR-W10).* [Source: FR-CT9; knowledge FR-K25]
- [ ] **AC2** — Given the uniform rule, When triggered, Then knowledge keeps its own authz + audit; console adds its gate + audit record on top, never a bypass. *Assert: composition adds a gate; owning satellite authz still runs.* [Source: FR-CT9; Arch A-ADMIN]
- [ ] **AC3** — Given console is never a spine publisher for another domain, When a reindex runs, Then console publishes no CloudEvent. *Assert: no console spine publish; knowledge owns the enqueue (FR-W10).* [Source: FR-CT9; §3 non-goal]
- [ ] **AC4** — Given a mutation, When triggered, Then fresh introspection + typed confirmation + audit-or-fail apply. *Assert: E2-S2/S4 gates.* [Source: FR-CT13/CT14]

## 🔨 Tasks

- [ ] RED: `TestReindexCallsKnowledgeSurfaceConsoleNeverPublishesEnqueue` (AC1,3). (AC: 1,3)
- [ ] GREEN: `/api/admin/reindex` handler → workflow → knowledge client. (AC: 1,2)
- [ ] Assert no console spine publisher exists on this path. (AC: 3)
- [ ] Chain fresh gate + audit wrap. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Knowledge reindex trigger fronted via the FR-K25 wrap: console calls knowledge's own admin surface (knowledge, not console, publishes the spine event), gated + audited + tested. Note: no dedicated web control (would ride a generic action).

Handler → workflow → knowledge-admin client (pass-through). The uniform FR-CT9 wrap other satellites' admin actions grow into. Console never publishes for another domain (the standing BFF-creep failure mode). Sibling: E2-S2/S4.

## 🧪 Testing

`TestReindexCallsKnowledgeSurfaceConsoleNeverPublishesEnqueue` (workflow integration). Tiers: workflow + ports. CS §5: fake knowledge + Kafka + introspection at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: compose-not-bypass, no-spine-publish. Cell-lint `JGAUQRsw2g` rules 5/6.

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN), `kldiZu93EC` (FR-CT9), knowledge FR-K25, workflows FR-W10.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E5; E2-S2/S4.
- [ ] Cross-repo: knowledge FR-K25 admin surface.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
