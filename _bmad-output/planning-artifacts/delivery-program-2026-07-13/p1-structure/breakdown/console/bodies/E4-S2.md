## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform operator, **I want** to retry/replay, cancel/terminate, and signal workflows through the authorizing proxy, each behind fresh introspection + a typed confirmation **so that** the family's most dangerous buttons are audited, gated, and executed workflows-side.

**Definition of Done:** one named test `TestWorkflowWriteRequiresFreshIntrospectionConfirmationAndProxy` (workflow layer — a retry/cancel/signal with a stale token or missing typed confirmation is rejected, and an accepted one is executed only via the proxy with an audit wrap, asserted through the mutation workflow). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/workflows/:id/{retry|cancel|signal}`, When invoked, Then the op executes via the workflows authorizing proxy (never Temporal frontend). *Assert: request hits the proxy client; who-may-signal enforced proxy-side (OQ-W8).* [Source: FR-CT10; D-CTRL]
- [ ] **AC2** — Given a workflow write, When the token is stale or the typed confirmation is missing, Then it is rejected. *Assert: fresh no-grace introspection + typed confirm required (from E2-S2).* [Source: FR-CT10/CT13]
- [ ] **AC3** — Given a successful write, When executed, Then it is wrapped intent-audit → proxy call → outcome-audit (audit-or-fail). *Assert: two audit records; audit failure blocks the write.* [Source: FR-CT14; Arch A-ADMIN]
- [ ] **AC4** — Given the deprovision grace-window cancel (FR-W5), When signalled, Then it routes through the proxy where who-may-signal is enforced. *Assert: grace-cancel signal accepted only if proxy authorizes.* [Source: FR-CT10; OQ-W8]

## 🔨 Tasks

- [ ] RED: `TestWorkflowWriteRequiresFreshIntrospectionConfirmationAndProxy` (AC1,2). (AC: 1,2)
- [ ] GREEN: retry/cancel/signal handlers → workflow wrap → proxy client. (AC: 1,3)
- [ ] Chain E2-S2 fresh gate + typed confirmation. (AC: 2)
- [ ] Audit wrap (E2-S4). (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — Workflow write ops (retry/cancel/signal) via the proxy, each behind gate.Mutate (fresh introspection) + typed confirmation + audit-wrap. Fully tested. Note: no web write UI.

Handler → workflow (`internal/workflow/` liveness sequencing) → proxy client + audit domain. Reuses the E4-S1 single Temporal binding. Phase 3: gated on proxy live + durable audit stream live + OQ-CT3 RBAC + fresh-introspection wiring. Sibling: E2-S2/S4, E4-S1.

## 🧪 Testing

`TestWorkflowWriteRequiresFreshIntrospectionConfirmationAndProxy` (workflow integration). Tiers: workflow + domain + ports. CS §5: fake proxy + introspection + Kafka at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp. §6 tiering — no domain logic in handler). SE-Arch lenses: concentrated-privilege, fail-closed. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN, §5), `kldiZu93EC` (FR-CT10, D-CTRL, rollout Phase 3).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E4; E4-S1, E2-S2/S4.
- [ ] Cross-repo (HARD): workflows authorizing-proxy + who-may-signal (OQ-W8); durable audit stream; OQ-CT3 RBAC.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
