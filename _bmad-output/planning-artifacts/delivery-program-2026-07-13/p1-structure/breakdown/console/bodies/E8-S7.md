## 🎯 Story

**As a** growth/marketing staffer, **I want** the PostHog web-analytics dashboard (pageviews, sessions, acquisition sources, UTM/campaign attribution, conversions, web vitals) embedded behind the BFF auth-proxy **so that** I can run and read a GTM campaign — the native GA4-alternative UI, not rebuilt in shadcn.

**Definition of Done:** one named test `TestWebAnalyticsDashboardEmbeddedBehindAuthProxy` (handler/front — the web-analytics dashboard mounts via an iframe with a server-minted PostHog session behind the auth-proxy, PostHog never directly routable, asserted through the embed handler + CSP). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the web-analytics surface, When mounted, Then pageviews, sessions, acquisition sources, UTM/campaign attribution, conversions, and web vitals render as the native PostHog dashboard (embed — not rebuilt). *Assert: web-analytics fields render; dashboard not reimplemented.* [Source: FR-CT-UBA-12; §7]
- [ ] **AC2** — Given the embed, When served, Then it rides the same auth-proxy/server-minted-session posture as replay; PostHog never directly routable. *Assert: no browser→PostHog; frame-src CSP set.* [Source: FR-CT-UBA-12/10; A-UBA-EDGE]
- [ ] **AC3** — Given capture rides the same client-side posthog-js path (pageviews/autocapture/UTM, consent-first + masked), When console renders, Then console is NOT the capture path. *Assert: console has no client capture (OQ-UBA-10, frontends).* [Source: FR-CT-UBA-12; FR-CT-UBA-8a path 2]
- [ ] **AC4** — Given the audience is internal staff, When served, Then it stays role-scoped Clerk-admin, never tenant-facing. *Assert: no tenant-facing route; sales/marketing notebooks shareable within the staff plane.* [Source: FR-CT-UBA-12; §1 audience]

## 🔨 Tasks

- [ ] RED: `TestWebAnalyticsDashboardEmbeddedBehindAuthProxy` (AC2). (AC: 2)
- [ ] GREEN: web-analytics embed handler + server-minted session + frame-src CSP. (AC: 1,2)
- [ ] Assert console holds no client capture path. (AC: 3)
- [ ] Role-scoped staff-only access. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** absent — User-Behavior Analytics / PostHog surface is entirely unimplemented: no pkg/posthog port, no /api/analytics/* BFF, no analytics/session-replay/web-analytics web panes, and no posthog dependency in web/package.json. Nothing in the tree references PostHog or UBA.

Handler (embed session mint) + front (iframe). Same posture as E8-S6 replay. Client capture (posthog-js) is frontends (OQ-UBA-10). Sibling: E8-S2/S5/S6.

## 🧪 Testing

`TestWebAnalyticsDashboardEmbeddedBehindAuthProxy` (handler/front). Tiers: handler + front. CS §5: fake PostHog session-mint at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: never-over-expose, staff-plane discipline. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `Ii9q9kBNms` (§ Web & marketing analytics, §7), `TYWAtnvliD` (FR-CT-UBA-12).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E8; E8-S1/S2/S5.
- [ ] Cross-repo (HARD): posthog-js client-dep ADR (OQ-UBA-10, frontends); consent SoR (OQ-UBA-4); adopt-PostHog ADR.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
