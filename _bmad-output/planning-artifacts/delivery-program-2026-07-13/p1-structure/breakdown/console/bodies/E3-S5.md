## 🎯 Story

**As a** platform operator, **I want** cross-signal correlation pivots keyed on the contracts FR-C18 label vocabulary **so that** I can follow one incident trace → logs → metric exemplar → workflow history without leaving console (G1).

**Definition of Done:** one named test `TestCorrelationPivotsKeyOnContractsVocabularyNotHardcodedLabels` (domain layer — the pivot resolver maps a trace to its logs / a log's trace_id to the waterfall / a workflow_id to its spans using the FR-C18 vocabulary read as config, asserted through the correlation resolver; a hard-coded satellite label name fails the test). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a trace, When pivoting, Then its logs resolve; from a log's trace_id, the waterfall; from a metric spike, exemplar traces; from a workflow_id, its activity spans. *Assert: all four pivots resolve.* [Source: FR-CT2]
- [ ] **AC2** — Given the pivot keys, When resolved, Then they key on the FR-C18 vocabulary (service, tenant, source_id, contract tag, cell, trace_id, workflow_id) read as config. *Assert: no hard-coded satellite-specific label names.* [Source: FR-CT2; Arch A-PANE]
- [ ] **AC3** — Given a missing correlation key on an event, When pivoting, Then it degrades honestly (no fabricated link). *Assert: absent key → typed "no correlation" state.* [Source: FR-CT2; NFR-CT7]
- [ ] **AC4** — Given the vocabulary appendix updates, When re-read, Then pivots track it (config bump, not code change). *Assert: vocabulary change = config reload, no recompile of hard-coded names.* [Source: FR-CT2; Arch §5]

## 🔨 Tasks

- [ ] RED: `TestCorrelationPivotsKeyOnContractsVocabularyNotHardcodedLabels` (AC2). (AC: 2)
- [ ] GREEN: correlation resolver (pure domain, reads FR-C18 vocab as config). (AC: 1,2)
- [ ] Wire pivots into logs/metrics/traces/workflow panes. (AC: 1)
- [ ] Honest no-correlation state. (AC: 3,4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — Cross-signal correlation keyed on the contracts FR-C18 shared-attribute vocabulary (orvex.tenant/cell, service.name, trace id) is present as a pure mapping and tested. MISSING: no web cross-signal pivot UI wiring a log line into the trace pane.

Domain tier (`internal/obs/` pure functions) — correlation is pure composition over the vocabulary; no I/O. The workflow_id pivot bridges into E4 Temporal visibility. Sibling: E3-S2/S3/S4 panes provide the pivot origins.

## 🧪 Testing

`TestCorrelationPivotsKeyOnContractsVocabularyNotHardcodedLabels` (domain unit). Tiers: domain. CS §5: no mocks — pure functions over a fixture vocabulary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp. §3.5 pure domain). SE-Arch lenses: honesty, contract-conformance. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-PANE), `kldiZu93EC` (FR-CT2), contracts `obs/CONVENTIONS.md` (FR-C18).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E3; E3-S2/S3/S4.
- [ ] Cross-repo: contracts FR-C18 vocabulary; E4-S1 (workflow_id target).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
