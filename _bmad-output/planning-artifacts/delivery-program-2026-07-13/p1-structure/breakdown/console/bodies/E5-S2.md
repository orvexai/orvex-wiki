## 🎯 Story

**As a** platform operator, **I want** to list/inspect/rotate/revoke MCP/agent/api tokens family-wide via identity's FR-I7 lifecycle **so that** a revocation issued in console IS identity's revocation, with its introspection-TTL + push-revocation semantics intact.

**Definition of Done:** one named test `TestTokenRevokeDelegatesToIdentityWithAuditWrap` (workflow layer — a revoke on `/api/admin/tokens` calls identity's FR-I7 revoke with the operator's token, fresh-introspection-gated, audit-wrapped, and console re-implements no revocation logic, asserted through the mutation workflow). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/admin/tokens`, When listed, Then MCP/agent/api tokens render filtered by tenant/subject/scope. *Assert: filter round-trips against identity.* [Source: FR-CT8]
- [ ] **AC2** — Given a revoke, When issued, Then it delegates to identity's FR-I7 revoke (console re-implements nothing). *Assert: revoke = identity call; introspection-TTL + push-revocation semantics apply unchanged.* [Source: FR-CT8; Arch A-ADMIN]
- [ ] **AC3** — Given a revoke is a mutation, When issued, Then fresh introspection + typed confirmation + audit wrap apply. *Assert: E2-S2 gate + E2-S4 audit-or-fail.* [Source: FR-CT13/CT14]
- [ ] **AC4** — Given identity is unreachable, When a revoke is attempted, Then it fails honestly (no fabricated success). *Assert: typed error; token not shown as revoked when it is not.* [Source: FR-CT18; NFR-CT7]

## 🔨 Tasks

- [ ] RED: `TestTokenRevokeDelegatesToIdentityWithAuditWrap` (AC2,3). (AC: 2,3)
- [ ] GREEN: `/api/admin/tokens` list + revoke handler → workflow → identity client. (AC: 1,2)
- [ ] Chain fresh gate + audit wrap. (AC: 3)
- [ ] Honest failure on identity unreachable. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** partial — Token list + revoke (with audit-wrap, operator-surface-not-lifecycle) present and tested. MISSING: no token ROTATE operation, and no web token-management pane (list/inspect/rotate/revoke UI).

Handler → workflow (mutation sequencing) → identity-admin client (pass-through). No revocation logic in console (BFF-creep counter-metric). Sibling: E2-S2/S4, E5-S1.

## 🧪 Testing

`TestTokenRevokeDelegatesToIdentityWithAuditWrap` (workflow integration). Tiers: workflow + ports. CS §5: fake identity + Kafka + introspection at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: compose-not-bypass, fail-closed. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN), `kldiZu93EC` (FR-CT8), identity FR-I7.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E5; E2-S2/S4.
- [ ] Cross-repo: identity FR-I7 token lifecycle.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
