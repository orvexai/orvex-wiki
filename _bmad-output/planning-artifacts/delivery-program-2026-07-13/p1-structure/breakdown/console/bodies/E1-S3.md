## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform security owner, **I want** CI to reject any AGPL import and any admin-mutation package that imports the LGTM credential holder **so that** the AGPL boundary and the concentrated-privilege confinement are enforced by construction, not convention.

**Definition of Done:** one named test `TestCredConfinementGateRejectsAdminImportingLgtmCreds` (build-tag/lint gate level — a fixture admin-mutation package importing the observability credential package fails the gate; a compliant tree passes; asserted through the gate's exit code). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the AGPL-import guard over the Go module + front-end `package.json`, When a `@docmost/*` or AGPL package is present, Then CI fails. *Assert: guard exit≠0 on a seeded AGPL import; exit 0 clean.* [Source: NFR-CT4; Arch §4]
- [ ] **AC2** — Given the LGTM-credential-confinement build-tag/lint gate, When an `internal/admin/*` package imports the observability credential holder, Then the gate fails. *Assert: gate exit≠0 on the fixture; the observability handler package importing it passes.* [Source: Arch A-EDGE §4, SE-Arch F8; NFR-CT2]
- [ ] **AC3** — Given the gate is bespoke (not in the CS §5 catalog), When it is added, Then it is declared as a named CI gate. *Assert: gate named in CI config + docs, not assumed.* [Source: Arch §4 CI gates]
- [ ] **AC4** — Given no Temporal credential exists anywhere, When the tree is scanned, Then no `temporal-frontend` client binding is present. *Assert: grep gate finds zero direct Temporal client construction.* [Source: D-CTRL; NFR-CT2]

## 🔨 Tasks

- [ ] RED: fixture package + `TestCredConfinementGateRejectsAdminImportingLgtmCreds` (AC2). (AC: 2)
- [ ] GREEN: build-tag/lint gate over package import graph. (AC: 2,3)
- [ ] AGPL-import guard over Go module + `package.json`. (AC: 1)
- [ ] `temporal-frontend`-binding grep gate. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — AGPL-import denylist guard (@docmost/*) runs in CI; LGTM read-only credential confined to internal/observability by a package-boundary import test, plus web-side license/edge-confinement specs.

CI/build tier — a governance gate, not runtime code. Enforces the A-EDGE package boundary (admin packages cannot import the credential holder) and NFR-CT4. Seam: static analysis over the module graph. Sibling: E2-S3 (the confined LGTM creds this gate protects); E4 (no-Temporal-cred posture).

## 🧪 Testing

`TestCredConfinementGateRejectsAdminImportingLgtmCreds` (gate/lint level). Tiers: CI gate. CS §5: no service mocks — a static-analysis gate over fixtures; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 + §5 (CI gates) + §9. SE-Arch lenses: AGPL-boundary, concentrated-privilege. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-EDGE §4, §4 CI gates), `kldiZu93EC` (NFR-CT2/CT4), contracts AGPL guard (FR-C21/NFR-C7).

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E1; E1-S1/S2.
- [ ] Intra-service: E2-S3 (confined creds), E4-S1 (proxy-only Temporal).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
