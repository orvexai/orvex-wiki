## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform operator, **I want** an AI usage & enforcement pane over ai's FR-AI15 reporting API **so that** I can see per-tenant spend vs cap, spend by feature, historical usage, cap-denial counts, spend-check staleness and per-caller budgets — with console displaying, never owning, cap policy.

**Definition of Done:** one named test `TestAiUsagePaneDisplaysSpendVsCapWithoutStoringCapValue` (handler layer — `/api/admin/ai-usage` renders spend-vs-cap + enforcement state from ai FR-AI15, and console persists no cap value nor pushes any policy, asserted through the handler + a no-write assertion). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/api/admin/ai-usage`, When loaded, Then per-tenant spend vs cap, spend by feature, and historical usage render from ai FR-AI15. *Assert: usage fields render.* [Source: FR-CT12a; ai FR-AI15]
- [ ] **AC2** — Given ai's enforcement state, When rendered, Then cap-denial counts, spend-check staleness, and per-caller scoped-key budgets display. *Assert: enforcement fields render.* [Source: FR-CT12a; ai FR-AI25]
- [ ] **AC3** — Given `ai.cap.reached`/`ai.cap.warning`, When they fire, Then they surface via the alert-state surface (E3-S6), NOT a console-owned spine subscription. *Assert: cap events reach the alert surface via the alerting pipeline.* [Source: FR-CT12a; Arch §1]
- [ ] **AC4** — Given the cap VALUE is billing's SoR (D-P3), When rendered, Then console stores no cap value and pushes no policy; the cap-value *set* fronts billing (E6-S2), not ai. *Assert: no cap-value persistence; no ai `PUT …/cap`.* [Source: FR-CT12a; D-CAP re-point/C3]

## 🔨 Tasks

- [ ] RED: `TestAiUsagePaneDisplaysSpendVsCapWithoutStoringCapValue` (AC1,4). (AC: 1,4)
- [ ] GREEN: `/api/admin/ai-usage` handler over ai FR-AI15. (AC: 1,2)
- [ ] Wire cap events to the alert surface (E3-S6). (AC: 3)
- [ ] Assert no cap-value store/push. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — AI usage & enforcement pane present full-stack: BFF passes ai's FR-AI15 report through verbatim (console stores no cap value), plus a React spend-vs-cap page with cap-math and a spec. Note: the entrypoint wires TypedStubAiUsageService (honest-empty) pending the real ai client swap — the pane renders, live data is one seam swap away.

Handler → domain (shaping) → ai-reporting client (pass-through). Display-only; cap policy lives in billing (SoR) + ai (enforcement). Sibling: E3-S6 (cap events), E6-S2 (cap-value set fronts billing).

## 🧪 Testing

`TestAiUsagePaneDisplaysSpendVsCapWithoutStoringCapValue` (handler integration). Tiers: handler + domain + port. CS §5: fake ai FR-AI15 at the network boundary; do not mock own packages.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: compose-not-own (no domain state), honesty. Cell-lint `JGAUQRsw2g`.

## 🔗 References

- `L1NVnmRgpy` (A-ADMIN, §5), `kldiZu93EC` (FR-CT12a, D-CAP), ai FR-AI15/AI25.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E6; E1-S6, E2-S1/S3, E3-S6.
- [ ] Cross-repo: ai FR-AI15 reporting API; billing SoR (for the cap value shown).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
