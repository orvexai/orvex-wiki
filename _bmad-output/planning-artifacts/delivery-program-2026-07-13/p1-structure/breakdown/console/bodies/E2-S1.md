## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** platform security owner, **I want** every BFF route registered through the `lib/auth` router with deny-by-default and a Clerk admin-org operator gate **so that** an undeclared route is rejected at startup and only admin-org members reach any surface.

**Definition of Done:** one named test `TestUndeclaredRouteRejectedAndNonAdminOrgDenied` (middleware/router layer — an unregistered path is rejected and a valid token lacking admin-org membership is denied on a declared route, asserted through the router). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2106); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the `lib/auth` router, When a route is not declared, Then it is rejected at startup/registration. *Assert: undeclared route = deny (lib FR-L6).* [Source: FR-CT13; Arch A-EDGE]
- [ ] **AC2** — Given a valid identity token without Clerk admin-org membership, When any `/api/*` read is hit, Then 403. *Assert: 403; admin-org membership required for reads.* [Source: FR-CT13; NFR-CT2]
- [ ] **AC3** — Given the verifier, When a token is checked, Then local JWKS + cached introspection with a deny-by-default scope gate run. *Assert: verifier path exercises JWKS + introspection; unknown scope denied.* [Source: FR-CT13; lib FR-L1–L6]
- [ ] **AC4** — Given an expired/invalid token, When any `/api/*` route is hit, Then denied with a typed auth error (no data leak). *Assert: 401, no backend call made.* [Source: NFR-CT2; error path]

## 🔨 Tasks

- [ ] RED: `TestUndeclaredRouteRejectedAndNonAdminOrgDenied` (AC1,2). (AC: 1,2)
- [ ] GREEN: wire `lib/auth` router; register every route explicitly. (AC: 1,3)
- [ ] Admin-org membership gate on reads. (AC: 2)
- [ ] Typed auth-error responses. (AC: 4)

## 🧠 Context

**🧾 Code audit (origin/dev @ 331ddd7, 2026-07-14):** present — lib/auth edge: deny-by-default + Clerk admin-org gate on every admin/workflows route; verifier delegated to orvex-studio-lib (never re-implemented). Fully tested incl. nil-verifier fail-closed and non-admin-org 403.

Handler/middleware tier: the `lib/auth` router sits alongside cell-guard (E1-S4). Ports: `pkg/auth` (identity verifier). Sibling: E2-S2 adds RequireFresh for mutations; the floor until OQ-CT3 RBAC lands is admin-org for reads.

## 🧪 Testing

`TestUndeclaredRouteRejectedAndNonAdminOrgDenied` (middleware). Tiers: middleware + port. CS §5: use `pkg/auth/authtest` conformance harness (own lib is consumed, faked at the network boundary for introspection); do not mock own packages beyond the network seam.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11. SE-Arch lenses: concentrated-privilege, deny-by-default. Cell-lint `JGAUQRsw2g` rule 3.

## 🔗 References

- `L1NVnmRgpy` (A-EDGE), `kldiZu93EC` (FR-CT13, NFR-CT2), lib FR-L1–L6.

## 🔗 Dependencies

- [ ] Blocked by: ENG-2106 (contract TAG dispatch gate); parent epic E2; E1-S1/S4.
- [ ] Intra-service: E2-S2 (fresh introspection).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
