## 🎯 Story

As a **CLI maintainer**, I want the reference's lossy-era compensating machinery collapsed — `.blocks.json` sidecars, embed-degradation refusals, unbounded conversion caches, and the triplicated canonicalizer — down to a thin post-write receipt check, so that machinery that existed only because reference round-trips were lossy is not carried into the clean-room design.

**Definition of Done:** ONE named test `TestCompensatingMachineryTrimmed` — a content-layer test asserting, through the write path, that no `.blocks.json` sidecar is emitted, no unbounded conversion cache is allocated, and a lossless round-trip collapses to a single post-write receipt check (not an embed-degradation refusal). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a write, When persisted, Then no `.blocks.json` sidecar is written (round-trips are lossless now). *Assertion: no sidecar file emitted.* [Source: FR-CLI13]
- [ ] **AC2** — Given conversion, When it runs, Then no unbounded conversion cache is allocated (bounded/absent). *Assertion: cache is bounded or removed; no unbounded growth.* [Source: FR-CLI13]
- [ ] **AC3** — Given a successful lossless write, When verified, Then a thin post-write receipt check confirms persistence — not an embed-degradation refusal. *Assertion: receipt-check path used; embed-degradation refusal machinery removed.* [Source: FR-CLI13]
- [ ] **AC4** — Given the canonicalizer, When invoked, Then a single implementation exists (triplication removed). *Assertion: one canonicalizer, not three.* [Source: FR-CLI13; evidence — triplicated canonicalizer]
- [ ] **AC5 (regression guard)** — Given the trim, When the golden corpus (E4-S3) runs, Then parity still holds. *Assertion: E4-S3 stays green after the trim.* [Source: FR-CLI21]

## 🔨 Tasks

- [ ] RED: write `TestCompensatingMachineryTrimmed` (AC: 1,2,3,4).
- [ ] GREEN: remove sidecars/unbounded caches/embed-refusals; collapse to receipt check (AC: 1,2,3).
- [ ] Unify the canonicalizer to one implementation (AC: 4).
- [ ] Re-run golden parity as a regression guard (AC: 5).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — The reference's compensating machinery (embed sidecars / embed-degradation / canonicalizer collapse) is absent BY CONSTRUCTION: conversion is a single in-process pkg/dfm bridge with no sidecar/canonicalizer path, and lossy write formats are rejected up front (ValidateWriteFormat). CodeEmbedDegradation remains in the vocabulary but no degradation machinery is wired. There is no explicit test asserting the trim — it is an absence, not a verified removal.

Expected tier placement: `internal/content` write path simplification. Seam crossed: none (internal simplification). Sibling dependency: E4-S3 (parity must stay green — the precondition), E3-S1 (write receipts). Maps to phase C5.

## 🧪 Testing

Named DoD test at content tier. CS §5 mocking: none — real write path + real fixtures; never mock own content package.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. anti-slop: no dead/duplicated code). SE-Arch 8sYi523i4t lenses: Simplicity (primary), Data-integrity. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F5 FR-CLI13; Phasing C5)
- Architecture: orvex-cli — pf10XC2Qjz (Trade-offs: in-process pkg/dfm)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E4**; E4-S3 (golden parity green), E3-S1.
- [ ] Intra-service: after E4-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
