## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **daemon**, I want an SSE consumer that preserves the reference's richer-than-FR-K15 wire contract verbatim — `/events/head` cold-start, a numerically-monotonic epoch-ms cursor, unprefixed event names, thin `page.content_updated` frames, `permission.changed` prune semantics, 1 MiB frame cap + 512-frame queue reconnect-not-drop, <45s heartbeat, control frames, `applied_events` exactly-once dedup — and fixes the `page.status_changed` silent-drop, so that freshness never silently breaks.

**Definition of Done:** ONE named test `TestSseConsumerWireContractAndStatusChangedFix` — an SSE-layer test asserting, through the event dispatcher against a stub events stream, that a thin `page.content_updated` triggers a wiki-api refetch, `permission.changed` prunes, `page.status_changed` is dispatched (not dropped, D3), every non-empty event id is recorded once in `applied_events`, and the cursor is numerically monotonic + epoch-ms (D6). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given cold start, When connecting, Then `/events/head` bootstraps the cursor before streaming. *Assertion: head cursor fetched first.* [Source: FR-CLI16]
- [ ] **AC2** — Given a thin `page.content_updated` (`{pageIds}`, no body), When received, Then the client refetches from wiki-api; `permission.changed` prunes. *Assertion: refetch issued; prune applied.* [Source: FR-CLI16]
- [ ] **AC3** — Given a `page.status_changed` event, When dispatched, Then it is handled (not silently dropped). *Assertion: status change applied to cache (D3 fixed).* [Source: FR-CLI16]
- [ ] **AC4** — Given any non-empty event id, When processed, Then it is recorded once in `applied_events` in the same transaction (exactly-once). *Assertion: duplicate delivery is deduped.* [Source: FR-CLI16]
- [ ] **AC5** — Given the cursor, When advancing, Then it is numerically monotonic + epoch-ms so `advanceCursorTx` never drops a newer smaller-looking cursor; a legacy-format cursor resolves to `stream.gap` re-sync, never a silent stall. *Assertion: monotonic advance; legacy cursor → stream.gap.* [Source: FR-CLI16/NFR-CLI5]
- [ ] **AC6 (limits)** — Given a >1 MiB frame or a full 512-frame queue, When received, Then the consumer reconnects-not-drops with the backoff ladder + <45s heartbeat and honors control frames (`stream.gap/backpressure/terminated`). *Assertion: reconnect-not-drop; control frames drive bounded REST reconcile.* [Source: FR-CLI16]

## 🔨 Tasks

- [ ] RED: write `TestSseConsumerWireContractAndStatusChangedFix` (AC: 1–6).
- [ ] GREEN: implement the SSE consumer + dispatcher (head bootstrap, frames, control frames, dedup) (AC: 1,2,3,4,6).
- [ ] Wire monotonic epoch-ms cursor + legacy→stream.gap; add the `page.status_changed` dispatcher case (AC: 3,5).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — SSE consumer wire contract present: reconcile() parses event:/data: frames, applyFrame handles bootstrap/page.content_updated/permission.changed/heartbeat with monotonic-cursor exactly-once dedup (cursorAdvances). daemon_test.go covers it. Caveat: page.status_changed is NOT a distinctly handled frame type (falls through to the forward-compatible ignore branch) — the DoD's status_changed fix is not explicitly wired.

Expected tier placement: `internal/daemon` SSE consumer + `internal/db` (`applied_events`, cursor). Seam crossed: knowledge events host (SSE). Sibling dependency: E6-S1 (cache), E6-S2 (delta sync uses the same cursor), E3-S1 (wiki-api refetch), E6-S4 (cell-qualification).

## 🧪 Testing

Named DoD test at SSE tier against a stub events stream. CS §5 mocking: stub the events SSE boundary; real SQLite; never mock own daemon/db.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §10 freshness never-false-green). SE-Arch 8sYi523i4t lenses: Reliability (reconnect-not-drop, exactly-once), Data-integrity (cursor monotonicity). Cell-lint JGAUQRsw2g §4.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F6 FR-CLI16; NFR-CLI5; defects D3/D6)
- Architecture: orvex-cli — pf10XC2Qjz (§Data flow: cache freshness)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — SSE wire contract + cursor scheme A6a).
- [ ] Blocked by: parent epic **E6**; E6-S1, E3-S1 (refetch).
- [ ] Intra-service: pairs with E6-S2 (cursor), precedes E6-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI7 (freshness SLO numeric) if a bound is required.
