## 🎯 Story

As a **wiki quality agent**, I want the `verify` suite (`lint/links/orphans/render/space/duplicates/staleness/drift/ia-conformance`) and `spec gate check`, so that I can validate content health and gate compliance — with `verify render` gated to the full binary and `GATE_UNSATISFIED` surfaced at exit 9.

**Definition of Done:** ONE named test `TestVerifySuiteAndSpecGateCheck` — a verify-layer test asserting, through the verify subcommands, that each check runs against its owning service (lint/links/orphans/ia local or wiki-api; duplicates→knowledge; render→full binary), and that `spec gate check` returns `GATE_UNSATISFIED` at exit 9 when the gate is unmet. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given each `verify` check, When run, Then it targets the correct owner (structural/lint local or wiki-api; `duplicates`→knowledge; `drift`→wiki-api). *Assertion: each check routes to its owner; no engine-direct.* [Source: FR-CLI4/FR-CLI10]
- [ ] **AC2** — Given `verify render` on the default binary, When run, Then `REQUIRES_FULL_BINARY`; on `orvex-full` it renders. *Assertion: stub on default, runs on full.* [Source: FR-CLI4/FR-CLI6]
- [ ] **AC3** — Given `spec gate check` with an unmet gate, When run, Then `GATE_UNSATISFIED` at exit 9. *Assertion: exit 9 + GATE_UNSATISFIED.* [Source: FR-CLI4/FR-CLI8]
- [ ] **AC4** — Given `verify duplicates`, When run, Then it queries knowledge (dedup lives there). *Assertion: request targets knowledge host.* [Source: FR-CLI11]

## 🔨 Tasks

- [ ] RED: write `TestVerifySuiteAndSpecGateCheck` (AC: 1,2,3,4).
- [ ] GREEN: implement the verify subcommands + routing per owner (AC: 1,4).
- [ ] Gate `render` to full binary; wire `spec gate check`→GATE_UNSATISFIED exit 9 (AC: 2,3).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — Only `wiki verify drift` is a real handler (verify.go, against the landed wiki-api drift route). lint/links/orphans/render/space/duplicates/staleness/ia-conformance are scaffold stubs (routed NOT_IMPLEMENTED via leaf()/fullLeaf()), by design (verify.go doc: no backend endpoint exists, faking a verdict would be fabricated data). The `spec gate check` leaf is also a stub. verify_test.go covers the drift handler only.

Expected tier placement: `cmd/wiki/verify/*` → `internal/verify` + `internal/client/{wiki,knowledge}`. Seam crossed: wiki-api (drift/space), knowledge (duplicates). Sibling dependency: E2-S4 (full-binary gating for render), E3-S1 (wiki client), E5-S1 (knowledge).

## 🧪 Testing

Named DoD test at verify tier against stub wiki-api + knowledge. CS §5 mocking: stub external HTTP boundaries; never mock own verify package.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance, Honesty (render gating). Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI4; F3 FR-CLI8; F2 FR-CLI6)
- Architecture: orvex-cli — pf10XC2Qjz (Components: Command tree — verify)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E3**; E2-S4, E3-S1, E5-S1.
- [ ] Intra-service: parallel with E3-S2..S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
