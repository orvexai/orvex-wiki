## 🎯 Story

As a **content-authoring agent**, I want block-authoring commands for the 21 retained schema block types plus the `diagram` command (Linear's 8 dropped), including the `image_from_prompt` cross-service block, so that I can compose rich wiki content through wiki-api with unknown/`linear*` blocks preserved as opaque handles rather than dropped or hard-failed.

**Definition of Done:** ONE named test `TestBlockAuthoringRetains21TypesAndPreservesOpaque` — a wiki-authoring test asserting, through the block commands, that the 21 retained types + `diagram` author correctly, that no Linear block type is authorable, and that a pre-existing `linear*` block in fetched content round-trips as opaque-preserve (never dropped/failed). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given each of the 21 retained block types + `diagram`, When authored, Then the block is emitted into wiki content via wiki-api. *Assertion: each type produces valid schema-conformant block content.* [Source: FR-CLI4]
- [ ] **AC2 (negative)** — Given the block command set, When enumerated, Then no Linear block/embed/node type is authorable. *Assertion: 8 Linear block types absent (D-S24).* [Source: FR-CLI9]
- [ ] **AC3** — Given content already containing a `linear*` block, When read/edited/written, Then it round-trips as an opaque-preserve handle, never a hard failure or silent drop. *Assertion: `linear*` block bytes identical after round-trip.* [Source: FR-CLI9/FR-CLI12]
- [ ] **AC4** — Given `image_from_prompt`, When authored, Then the cross-service path (wiki-api↔ai) is exercised and `BUDGET_EXCEEDED`/`MODEL_UNAVAILABLE` surface if raised. *Assertion: image block invokes the defined path; budget/model errors surface typed.* [Source: FR-CLI4; OQ-CLI5 — cross-service path TBD, pinned by ENG-2105]

## 🔨 Tasks

- [ ] RED: write `TestBlockAuthoringRetains21TypesAndPreservesOpaque` (AC: 1,2,3).
- [ ] GREEN: implement the 21 block-authoring commands + `diagram` (AC: 1).
- [ ] Enforce no-Linear-block-authoring + opaque-preserve on read (AC: 2,3).
- [ ] Wire `image_from_prompt` per the resolved cross-service path (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — Diagram/embed authoring via wikiDiagramCmd: builds a PM opaque node, round-trips it through content.PMToDfM/DfMToPM to prove byte-parity, then CAS-updates. image_from_prompt is covered by `ai image` (E5-S2). But there is NO dedicated authoring surface for the '21 retained block types' — opaque nodes are handled generically by pkg/dfm, not enumerated/validated per block type here.

Expected tier placement: `cmd/wiki` block subcommands → `internal/content` (pkg/dfm) + `internal/client/wiki`. Seam crossed: wiki-api; `image_from_prompt` additionally the ai seam (path TBD, OQ-CLI5). Sibling dependency: E4-S1 (opaque encode/reattach), E3-S1 (wiki client), E5-S2 (ai for image).

## 🧪 Testing

Named DoD test at authoring tier against a stub wiki-api (+ stub ai for image). CS §5 mocking: stub wiki-api/ai HTTP boundaries; never mock own content/dfm packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance (block schema), Data-integrity (opaque-preserve). Cell-lint JGAUQRsw2g §5 (write idempotency).

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI4; Non-goals — Linear; OQ-CLI5)
- Architecture: orvex-cli — pf10XC2Qjz (Components: pkg/dfm embedding — opaque nodes)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — block-schema catalog + image cross-service path OQ-CLI5).
- [ ] Blocked by: parent epic **E3**; E3-S1, E4-S1, E5-S2 (image).
- [ ] Intra-service: parallel with E3-S2/S4/S5/S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI5 (image_from_prompt path).
