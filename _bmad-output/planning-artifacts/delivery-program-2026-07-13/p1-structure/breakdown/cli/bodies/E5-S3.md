## 🎯 Story

As an **operator**, I want the `admin` namespace routed per finding F-E — `workspace(info/members/invites/integrations/entitlements)` + `user` to wiki-api/identity, entitlement STATE read from the knowledge FR-K20 projection, and `reindex/reembed/events(settings|connections|log)` to knowledge events-admin — so that admin operations reach the correct owner and billing is correctly absent from the dependency set.

**Definition of Done:** ONE named test `TestAdminNamespaceRoutingPerFindingFE` — an admin-client test asserting, through the admin commands, that workspace/user route to wiki-api/identity, entitlement state reads from the knowledge projection (never a CLI-facing billing host), and `reindex/reembed/events` route to knowledge events-admin. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `admin workspace(info/members/invites/integrations)` + `admin user`, When run, Then they route to wiki-api/identity for workspace + membership state. *Assertion: hosts are wiki-api/identity per verb.* [Source: FR-CLI5/FR-CLI11; finding F-E]
- [ ] **AC2** — Given `admin workspace entitlements`, When run, Then entitlement STATE is read from the knowledge FR-K20 projection (fed by `billing.entitlement.changed`). *Assertion: entitlement read targets knowledge projection, not billing.* [Source: FR-CLI11; finding F-E]
- [ ] **AC3** — Given `reindex`/`reembed`/`events(settings|connections|log)`, When run, Then they route to knowledge events-admin. *Assertion: hosts are knowledge; `reembed` homed under admin (knowledge admin, not ai).* [Source: FR-CLI5/FR-CLI11]
- [ ] **AC4 (negative)** — Given any admin verb, When traced, Then no CLI-facing billing host is contacted. *Assertion: billing absent from dependency set / traces.* [Source: finding F-E]

## 🔨 Tasks

- [ ] RED: write `TestAdminNamespaceRoutingPerFindingFE` (AC: 1,2,3,4).
- [ ] GREEN: implement admin commands over wiki-api/identity/knowledge clients (AC: 1,3).
- [ ] Route entitlement state through the knowledge projection; assert billing absent (AC: 2,4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — admin namespace routing seam is present and tested (workspace/events groups + user/reindex/reembed, with mixed routing to wiki-api/identity/knowledge per finding F-E; entitlements read from the knowledge projection, billing correctly absent). BUT every admin leaf is a routed NOT_IMPLEMENTED stub — no real dispatch. admin_routing_test + admin_family_contract_test assert only the routing/contract seam.

Expected tier placement: `cmd/admin/*` → `internal/client/{wiki,identity,knowledge}`. Seam crossed: wiki-api, identity, knowledge (events-admin + projection). Sibling dependency: E6-S2 (projection for entitlement state), E7 (identity), E3-S1 (wiki client). Billing is deliberately not a dependency (F-E).

## 🧪 Testing

Named DoD test at admin-client tier against stub wiki-api/identity/knowledge. CS §5 mocking: stub each external HTTP boundary; never mock own clients.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §7 seam — billing absence deliberate). SE-Arch 8sYi523i4t lenses: Contract-Conformance, Cost/Resource-governance. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI5; F4 FR-CLI11)
- Architecture: orvex-cli — pf10XC2Qjz (§Data flow: admin routing; §Dependencies — billing not a dependency, F-E)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — FR-K20 projection entitlement fields).
- [ ] Blocked by: parent epic **E5**; E6-S2, E7-S1, E3-S1.
- [ ] Intra-service: after E5-S1 (knowledge client).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
