## 🎯 Story

As a **release engineer**, I want the release chain rebuilt fresh under `orvexai` — two-variant goreleaser, brew formula `orvex` (+ `docmost-cli` alias), cosign keyless + SLSA provenance + reproducible builds for both variants, and a CI assertion that module-owner == release-owner — so that both binaries ship signed with valid provenance and the `orvex-ai`-vs-`orvexai` mismatch (defect D4) can never recur.

**Definition of Done:** ONE named test `TestReleaseChainProvenanceAndOwnerAssertion` — a release/CI-layer test asserting, over a dry-run release, that both variants (`orvex` pure-Go + `orvex-full`) build reproducibly, emit cosign keyless signatures + SLSA provenance, and that the module-owner==release-owner assertion fails a seeded mismatch. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tagged release, When goreleaser runs, Then both `orvex` and `orvex-full` variants build reproducibly under `orvexai`. *Assertion: two reproducible artifacts produced; org is orvexai.* [Source: FR-CLI20/NFR-CLI6]
- [ ] **AC2** — Given each artifact, When released, Then cosign keyless signatures + SLSA provenance are emitted. *Assertion: signature + provenance attestation present and verifiable for both variants.* [Source: FR-CLI20/NFR-CLI6]
- [ ] **AC3** — Given a module-owner ≠ release-owner mismatch, When CI runs, Then the assertion fails (fixes D4). *Assertion: seeded `orvex-ai`-vs-`orvexai` mismatch fails the CI check.* [Source: FR-CLI20]
- [ ] **AC4** — Given the brew formula, When installed, Then `orvex` installs with a `docmost-cli` alias. *Assertion: formula name `orvex`; `docmost-cli` alias resolves.* [Source: FR-CLI20/FR-CLI18]
- [ ] **AC5 (honesty)** — Given the two-variant release, When published, Then no partial binary claims a full-binary capability. *Assertion: default variant advertises only its real capabilities (ties to E2-S4).* [Source: NFR-CLI6]

## 🔨 Tasks

- [ ] RED: write `TestReleaseChainProvenanceAndOwnerAssertion` (dry-run) (AC: 1,2,3).
- [ ] GREEN: author `.goreleaser*` (two variants) + brew formula + cosign/SLSA under orvexai (AC: 1,2,4).
- [ ] Add the module-owner==release-owner CI assertion (AC: 3); wire the variant-honesty check (AC: 5).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — goreleaser.yaml is a scaffold stub: only the single pure-Go `orvex` build (ldflags version/commit/date) is defined; the orvex-full variant, cosign keyless signing, SLSA provenance, Homebrew tap, and the module-owner==release-owner assertion are all explicit TODO(scaffold C0). CI does mint a private-module clone token via OpenBao OIDC and builds+tests both variants, but the release chain itself is not wired.

Expected tier placement: `.goreleaser*`, `.github/workflows`, brew tap — release infra under the orvexai org. Seam crossed: none at runtime. Sibling dependency: E2-S4 (variants), E8-S1 (docmost-cli alias). Preserves the reference's SLSA+cosign+reproducible hygiene.

## 🧪 Testing

Named DoD test at release/CI tier via a goreleaser dry-run + attestation verify. CS §5 mocking: none — real dry-run + signature verify; never mock own release scripts.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Supply-chain/Provenance, Honesty. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F8 FR-CLI20; NFR-CLI6; defect D4)
- Architecture: orvex-cli — pf10XC2Qjz (Components: Release chain)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E8**; E2-S4, E8-S1.
- [ ] Intra-service: last story (ship-blocking gate).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any org/signing-identity ambiguity.
