## 🎯 Story

As a **docmost-cli user at cutover**, I want a `docmost-cli` shim, `DOCMOST_* → ORVEX_*` env aliases (read-old-if-new-unset + warn-once), config-dir `~/.config/docmost-cli → ~/.config/orvex` first-run auto-migration, and `.docmost-cli.yaml → .orvex.yaml` project-file migration, so that my existing scripts and config keep working through one deprecation window.

**Definition of Done:** ONE named test `TestBackCompatShimEnvAndConfigMigration` — a config-layer test asserting, through the binary entry + config loader, that the `docmost-cli` shim warns-once then `exec orvex`, that `DOCMOST_URL`/`DOCMOST_API_TOKEN` resolve when their `ORVEX_*` counterparts are unset (warn-once), that first-run migrates the config dir + project file, and that `DOCMOST_SCHEMAS_*` is dropped (contracts-sourced). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the `docmost-cli` shim invoked, When run, Then it emits a one-time stderr deprecation warning then `exec orvex`. *Assertion: warn-once + exec to orvex.* [Source: FR-CLI18]
- [ ] **AC2** — Given `DOCMOST_URL`/`DOCMOST_API_TOKEN` set and no `ORVEX_*` counterpart, When resolving config, Then the old vars are read with a warn-once. *Assertion: old env honored when new unset; warning emitted once.* [Source: FR-CLI18]
- [ ] **AC3** — Given first run, When started, Then `~/.config/docmost-cli`→`~/.config/orvex` and `.docmost-cli.yaml`→`.orvex.yaml` auto-migrate; hidden namespace aliases survive. *Assertion: config dir + project file migrated once; aliases resolve.* [Source: FR-CLI18/FR-CLI3]
- [ ] **AC4 (negative)** — Given `DOCMOST_SCHEMAS_*`, When resolving, Then it is dropped (schemas are contracts-sourced, not env). *Assertion: `DOCMOST_SCHEMAS_*` ignored; schemas come from the contracts tag.* [Source: FR-CLI18]

## 🔨 Tasks

- [ ] RED: write `TestBackCompatShimEnvAndConfigMigration` (AC: 1,2,3,4).
- [ ] GREEN: implement the shim, env-alias resolution (read-old-if-new-unset + warn-once), config-dir + project-file first-run migration (AC: 1,2,3).
- [ ] Drop `DOCMOST_SCHEMAS_*` (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — DOCMOST_* env aliases are present with warn-once deprecation (config.firstEnv: DOCMOST_URL/DOCMOST_API_TOKEN). Hidden back-compat aliases (page/space/... → wiki) exist. BUT there is no docmost-cli binary shim (only a TODO for a `docmost-cli` brew alias in goreleaser) and NO config-dir first-run migration (nothing reads/moves a legacy ~/.config/docmost dir).

Expected tier placement: `internal/config` + the shim binary entry; extends E1-S2 (config model). Seam crossed: none. Sibling dependency: E1-S2 (endpoint/config model), E2-S1 (hidden aliases), E8-S2 (keychain migration is the credential half).

## 🧪 Testing

Named DoD test at config tier over temp env/config/HOME fixtures. CS §5 mocking: real temp dirs/env; never mock own config package.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Migration-safety, Simplicity. Cell-lint JGAUQRsw2g: n/a. Per no-fallbacks doctrine: the old-env read is a *bounded deprecation window* with a loud warning, not a silent permanent fallback.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F8 FR-CLI18; F2 FR-CLI3)
- Architecture: orvex-cli — pf10XC2Qjz (Components: Endpoint registry — DOCMOST_* deprecated aliases)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — env/config key model).
- [ ] Blocked by: parent epic **E8**; E1-S2, E2-S1.
- [ ] Intra-service: precedes E8-S2 (keychain half).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI6 (config-discovery coupling: `_bmad` key-space vs first-class `default_space`).
