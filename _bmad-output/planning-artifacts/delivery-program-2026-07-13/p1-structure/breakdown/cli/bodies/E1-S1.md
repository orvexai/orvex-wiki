## 🎯 Story

As a **platform maintainer**, I want orvex-cli scaffolded from scratch in an empty repo with an AGPL-import CI guard, so that every capability is re-authored cleanly against the microservice architecture with auditable provenance and no AGPL-derived code ever enters the build.

**Definition of Done:** ONE named test `TestAGPLImportGuardRejectsDocmostPackages` — a CI/unit-layer test that runs the import-guard over a fixture module graph containing an `@docmost/*`/AGPL import and asserts the guard exits non-zero with the offending path, and passes a clean graph — asserting the boundary through the guard's public interface, not its internals. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the empty repo, When the scaffold lands, Then `go.mod` (module `github.com/orvexai/orvex-cli`), `cmd/`, `internal/`, and a CI workflow exist as deliberate commits with no file copied from docmost-cli. *Assertion: `git log` shows no history seeded from docmost-cli and `go build ./...` succeeds.* [Source: FR-CLI1]
- [ ] **AC2** — Given a source tree with an AGPL/`@docmost/*` import, When the AGPL-import CI guard runs, Then it fails with a non-zero exit naming the offending import. *Assertion: guard exit code ≠ 0 and stderr contains the import path.* [Source: NFR-CLI7]
- [ ] **AC3** — Given a clean tree, When the guard runs, Then it passes. *Assertion: guard exit code == 0.* [Source: NFR-CLI7]
- [ ] **AC4 (negative)** — Given a transitive AGPL dependency pulled via `go.sum`, When the guard runs, Then it still fails (not just direct imports). *Assertion: guard walks the full module graph, fails on transitive `@docmost`/AGPL module.* [Source: NFR-CLI7]

## 🔨 Tasks

- [ ] RED: write `TestAGPLImportGuardRejectsDocmostPackages` against fixtures (AC: 2,3,4).
- [ ] GREEN: implement the module-graph import guard + CI wiring (AC: 2,3,4).
- [ ] Scaffold `go.mod`/`cmd/`/`internal/`/CI as deliberate commits (AC: 1).
- [ ] Wire the guard into the CI required-checks set (AC: 2).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — Scaffold fully present (module github.com/orvexai/orvex-cli, cmd/ + internal/ trees, a real CI workflow with gofmt/vet/build/test + private-module OIDC). The AGPL-import guard is only TestContentImportGraphNoAGPLAdjacent (guard_importgraph_test.go) — a package-scoped `go list -deps .` docmost-substring check on internal/content, whose OWN comment admits it is 'NOT wired into any CI'. The DoD's named test TestAGPLImportGuardRejectsDocmostPackages, a fixture-module-graph guard, and the AC4 transitive-dependency detection do not exist. ci.yml has no dedicated AGPL-guard step.

Expected tier placement: non-service Go repo (CS §6 non-service note — no store/event/cache tier). The guard is client-local tooling under `internal/` + a CI workflow. Seam crossed: none at runtime; this is a build-provenance boundary (NFR-CLI7 backing FR-CLI1/FR-CLI13). Sibling dependency: none; foundational for all downstream stories.

## 🧪 Testing

Named DoD test `TestAGPLImportGuardRejectsDocmostPackages` at unit/CI tier over fixture module graphs. CS §5 mocking: use real fixture modules on disk; never mock own packages. No external service mocked (build-time only).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §6 non-service note, §7 seam posture, anti-slop rows). SE-Arch 8sYi523i4t lenses: Provenance/AGPL-boundary, Zero-Trust. Cell-lint JGAUQRsw2g: n/a at build-time.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F1 FR-CLI1, NFR-CLI7)
- Architecture: orvex-cli — pf10XC2Qjz (Overview, ADR triggers)
- Audit & Rebirth Plan — PACF13d3I3 (C0)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (Definition Pack — the contract TAG is the dispatch gate).
- [ ] Blocked by: parent epic **E1**.
- [ ] Intra-service: first story; nothing precedes it.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
