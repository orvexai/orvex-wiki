## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As an **offline author**, I want in-process DfM↔ProseMirror conversion by embedding `orvex-studio-lib/pkg/dfm` (`PMToDfM`/`DfMToPM`/`MarkdownToPM`/`MarkdownToDfM`/`Fidelity` + opaque encode/reattach), so that content converts with no network `/convert` hop and unknown/opaque nodes (diagrams, drawio, excalidraw, embeds, mentions, `linear*`) round-trip byte-identically reattached from the `ifVersion` base.

**Definition of Done:** ONE named test `TestPkgDfmConvertersRoundTripOpaqueByteIdentical` — a content-layer test asserting, through the `internal/content` pkg/dfm embedder, that `MarkdownToPM→PMToDfM` and `DfMToPM→PMToDfM` round-trip a corpus including opaque nodes with byte-identical opaque handles, and that no `/convert` endpoint is called. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given markdown input, When converting, Then `MarkdownToPM`/`MarkdownToDfM` produce PM/DfM in-process with no network call. *Assertion: conversion succeeds offline; no HTTP issued.* [Source: FR-CLI12]
- [ ] **AC2** — Given PM content with opaque nodes, When round-tripped, Then each opaque handle (drawio/excalidraw/embed/mention/`linear*`) is byte-identical, reattached from the `ifVersion` base. *Assertion: opaque node bytes identical pre/post.* [Source: FR-CLI12/FR-CLI9]
- [ ] **AC3** — Given `Fidelity`, When invoked, Then it reports the round-trip fidelity for a document. *Assertion: fidelity metric returned.* [Source: FR-CLI12]
- [ ] **AC4 (negative)** — Given the converter, When it runs, Then it calls no engine/wiki-api `/convert` endpoint. *Assertion: no `/convert` call in any path.* [Source: FR-CLI12]

## 🔨 Tasks

- [ ] RED: write `TestPkgDfmConvertersRoundTripOpaqueByteIdentical` (AC: 1,2,3,4).
- [ ] GREEN: implement the `internal/content` pkg/dfm embedder wiring all six converters + opaque encode/reattach (AC: 1,2,3).
- [ ] Assert no `/convert` code path exists (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — pkg/dfm in-process converters (PMToDfM/DfMToPM/MarkdownToPM/Fidelity) bridge raw PM-JSON to orvex-studio-lib/pkg/dfm — no /convert network hop reachable (imports only pkg/dfm+stdlib+internal/output). Opaque-node byte-identical round-trip is pkg/dfm's own invariant (external pinned lib v0.3.0); dfm_test.go covers the bridge. Some fixtures skip via knownDfmDrift (see E4-S3).

Expected tier placement: `internal/content` embedding `orvex-studio-lib/pkg/dfm`; the CLI owns no DfM semantics (pkg/dfm does). Seam crossed: the pkg/dfm Go API (in-process, not HTTP). Sibling dependency: E4-S2 (AGPL audit gates the swap), E4-S3 (golden parity), consumed by E3 authoring + E4-S5.

## 🧪 Testing

Named DoD test at content tier over a fixture corpus. CS §5 mocking: none — pkg/dfm is a real embedded package, NOT mocked (never mock own/sibling embedded library); use real fixtures.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §7 boundaries — DfM semantics not owned here). SE-Arch 8sYi523i4t lenses: Data-integrity (byte-lossless), Provenance/AGPL-boundary. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F5 FR-CLI12; FR-CLI9 opaque-preserve)
- Architecture: orvex-cli — pf10XC2Qjz (Components: pkg/dfm embedding)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — pkg/dfm Go API + opaque-node handle format).
- [ ] Blocked by: parent epic **E4**; E4-S2 (AGPL audit gate), orvex-studio-lib pkg/dfm.
- [ ] Intra-service: precedes E3 authoring, E4-S3/S4/S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI1 (content_pm parity impacting round-trip).
