## 🎯 Story

As a **program maintainer**, I want the pkg/dfm swap gated on an AGPL-provenance audit of the reference's hand-mirrored Go serializer, with pkg/dfm written clean-room from the documented PM schema + contracts block-schema catalog + golden fixtures, so that nothing AGPL-derived (`@docmost/editor-ext`) enters the serializer path.

**Definition of Done:** ONE named test `TestPkgDfmProvenanceIsCleanRoom` — a provenance/CI-layer test asserting, through the AGPL-import guard + a provenance manifest, that `internal/content`/pkg/dfm import no `@docmost/*` or AGPL package and that the provenance audit record for the serializer swap is present and green. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the pkg/dfm swap, When audited, Then a provenance record documents that the serializer is clean-room (PM schema + block-schema catalog + golden fixtures, never AGPL TS). *Assertion: provenance manifest exists and asserts clean-room sourcing.* [Source: FR-CLI13/NFR-CLI7]
- [ ] **AC2** — Given the content/serializer path, When the AGPL guard runs, Then no `@docmost/*` or AGPL import is present (direct or transitive). *Assertion: guard green over content path.* [Source: NFR-CLI7]
- [ ] **AC3 (gate)** — Given the swap is not yet audit-cleared, When CI runs, Then the swap is blocked (the audit gates FR-CLI12 landing). *Assertion: an un-audited swap fails the gate.* [Source: FR-CLI13]
- [ ] **AC4 (negative)** — Given the reference's hand-mirrored `prosemirror.go`, When building pkg/dfm, Then it is not copied/read as AGPL source. *Assertion: no derivation from the AGPL-mirroring reference file.* [Source: FR-CLI13/FR-CLI1]

## 🔨 Tasks

- [ ] RED: write `TestPkgDfmProvenanceIsCleanRoom` (AC: 1,2,3).
- [ ] GREEN: produce the provenance audit manifest + wire the swap gate to it (AC: 1,3).
- [ ] Extend the AGPL guard to the content/serializer path (AC: 2,4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — Clean-room sourcing is asserted only by TestContentImportGraphNoAGPLAdjacent (`go list -deps` docmost-substring check on internal/content), whose own comment says it is NOT wired into CI as a named required check. pkg/dfm is consumed from orvex-studio-lib (external, clean-room per its own doc). No dedicated AGPL-provenance audit gate/verb exists beyond that one package-scoped test.

Expected tier placement: provenance manifest + `internal/content` guard coverage; build/CI concern. Seam crossed: none at runtime. Sibling dependency: E1-S1 (AGPL guard base), E4-S1 (the swap it gates), orvex-studio-lib (clean-room pkg/dfm authored from contracts).

## 🧪 Testing

Named DoD test at CI/provenance tier. CS §5 mocking: none — real guard over real module graph; never mock own packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Provenance/AGPL-boundary (primary), Legal-posture. Cell-lint JGAUQRsw2g: n/a. Note the AGPL boundary is "not legal advice — confirm with counsel" (family cross-cutting).

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F5 FR-CLI13; NFR-CLI7; F1 FR-CLI1)
- Architecture: orvex-cli — pf10XC2Qjz (ADR triggers: pkg/dfm clean-room boundary)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — block-schema catalog + golden fixtures as the clean-room source).
- [ ] Blocked by: parent epic **E4**; E1-S1.
- [ ] Intra-service: gates E4-S1 (the swap).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any AGPL legal-posture ambiguity (counsel confirmation).
