## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **scripted caller**, I want the output layer to preserve the reference's contract byte-identically — JSON-when-piped / human-on-TTY, `--output json|yaml|human`, snake_case fields, `--fields/--id-only/--compact` token-minimizers, `List` envelopes, `SetEscapeHTML(false)`, stderr-for-progress / stdout-for-data, and cursor pagination — so that my existing parsing keeps working across the repoint.

**Definition of Done:** ONE named test `TestOutputLayerPreservesEnvelopeAndEscaping` — an output-layer test asserting, through the renderer's public interface, that piped output is JSON with snake_case fields and `<`/`>`/`&` un-escaped (SetEscapeHTML(false)), `--fields`/`--id-only`/`--compact` minimize tokens, and List envelopes + cursor pagination match the reference. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given stdout is a pipe, When rendering, Then output is JSON; given a TTY, Then human format. *Assertion: pipe→JSON, TTY→human, overridable by `--output`.* [Source: FR-CLI7]
- [ ] **AC2** — Given content containing `<`, `>`, `&`, When JSON-encoded, Then those characters survive un-escaped. *Assertion: `SetEscapeHTML(false)` — literal `<`/`>`/`&` in output.* [Source: FR-CLI7]
- [ ] **AC3** — Given `--fields a,b`/`--id-only`/`--compact`, When rendering, Then only requested fields / id / minimized whitespace are emitted. *Assertion: token-minimizer flags reduce output as specified.* [Source: FR-CLI7]
- [ ] **AC4** — Given a paginated resource (`spaces`/`sidebar-pages`), When listed, Then a `List` envelope with a cursor is emitted. *Assertion: envelope shape + cursor field match the reference.* [Source: FR-CLI7]
- [ ] **AC5 (channel)** — Given progress vs data, When emitted, Then progress goes to stderr and data to stdout. *Assertion: no progress bytes on stdout.* [Source: FR-CLI7]

## 🔨 Tasks

- [ ] RED: write `TestOutputLayerPreservesEnvelopeAndEscaping` covering AC1–5.
- [ ] GREEN: implement the shared output/renderer package (JSON/YAML/human, field-minimizers, List envelope) (AC: 1,3,4).
- [ ] Configure `SetEscapeHTML(false)` + stdout/stderr channel split (AC: 2,5).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — Full output contract: NewRenderer auto-resolves json-when-piped/human-on-TTY (isTTY), --output/--fields/--id-only/--compact projection, SetEscapeHTML(false), stderr-for-progress/stdout-for-data, deterministic human formatter with sorted keys. contract_test + envelope_test cover it.

Expected tier placement: shared client artifact under `internal/output` reused by every namespace (CS §6 non-service). Seam crossed: none. Sibling dependency: consumed by all command stories; pairs with E2-S3 (error side of the same artifact).

## 🧪 Testing

Named DoD test at output tier over captured stdout/stderr. CS §5 mocking: capture real IO; never mock own output package. Parity oracle is docmost-cli (E2-S5).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance (byte-identical), Simplicity. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F3 FR-CLI7)
- Architecture: orvex-cli — pf10XC2Qjz (§Overview: output/error layer)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E2**; E2-S1 (command tree).
- [ ] Intra-service: pairs with E2-S3; precedes all render-bearing stories.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
