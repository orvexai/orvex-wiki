## 🎯 Story

As a **wiki agent**, I want `history/diff/revert/version`, `drift`, and `audit` dual-write routed to orvex-wiki-api (the C9 non-grammar surfaces), so that version history, drift detection, and audit records — all engine-direct in the reference — now flow through the facade with no engine-direct residual.

**Definition of Done:** ONE named test `TestHistoryDiffDriftAuditRouteToWikiApi` — a wiki-client test asserting, through the history/diff/version/drift/audit commands against a stub wiki-api, that each returns reference-shaped output via wiki-api and that audit dual-write emits an append-only record; no engine host appears in any trace. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `history`/`diff`/`version`/`revert`, When run, Then version data routes through wiki-api with reference envelopes. *Assertion: host is wiki-api; envelope/diff shapes match the reference.* [Source: FR-CLI4/FR-CLI10]
- [ ] **AC2** — Given `drift`, When run, Then it routes to wiki-api (drift lives in wiki-api per D-S8). *Assertion: drift request targets wiki-api.* [Source: FR-CLI10]
- [ ] **AC3** — Given a mutating command, When run, Then the `audit` dual-write emits an append-only record via wiki-api transport. *Assertion: audit record appended; transport is wiki-api (D-S16).* [Source: FR-CLI10; OQ-CLI3 — sink ownership/tamper-evidence TBD, pinned by ENG-2105]
- [ ] **AC4 (negative)** — Given any of these surfaces, When traced, Then no engine-direct call occurs. *Assertion: engine host absent from traces.* [Source: FR-CLI10; D-S16]

## 🔨 Tasks

- [ ] RED: write `TestHistoryDiffDriftAuditRouteToWikiApi` (AC: 1,2,3,4).
- [ ] GREEN: implement history/diff/revert/version + drift commands over wiki-api (AC: 1,2).
- [ ] Wire audit dual-write (append-only; tamper-evidence per OQ-CLI3 resolution) (AC: 3).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — history/diff/revert/version verbs present (client + verbs) and `wiki verify drift` is wired to the real wiki-api drift-verify route (verify.go, with --strict → GATE_UNSATISFIED). BUT audit is only a routing stub: `audit record` is a routed NOT_IMPLEMENTED leaf and `audit log` a client-local stub — there is no audit dual-write implementation (audit.go). audit_routing_test only checks the routing seam.

Expected tier placement: `cmd/wiki/*` → `internal/client/wiki` + `internal/audit`. NestJS analogue: wiki-api facade over engine history/audit. Seam crossed: wiki-api. Sibling dependency: E3-S1 (wiki client); audit sink form pending OQ-CLI3.

## 🧪 Testing

Named DoD test at wiki-client tier against a stub wiki-api. CS §5 mocking: stub wiki-api boundary; never mock own client/audit packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §10 audit/observability). SE-Arch 8sYi523i4t lenses: Contract-Conformance, Auditability/Tamper-evidence, Zero-Trust. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI4; F4 FR-CLI10; OQ-CLI3; Phasing C9)
- Architecture: orvex-cli — pf10XC2Qjz (§Dependencies; ADR triggers)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — audit sink OQ-CLI3).
- [ ] Blocked by: parent epic **E3**; E3-S1.
- [ ] Intra-service: parallel with E3-S2/S3/S4/S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI3 (audit sink ownership / tamper-evidence).
