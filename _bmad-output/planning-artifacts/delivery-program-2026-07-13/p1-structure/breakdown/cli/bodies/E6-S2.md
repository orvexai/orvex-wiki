## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **daemon**, I want cache-sync to be a cursor-delta projection download from the knowledge FR-K20 projection (not a full crawl), ACL-narrowed, with permission-loss tombstones that hard-DELETE cache + FTS + on-disk mirror, so that sync sustains ≥20 pages/s and revoked content never lingers.

**Definition of Done:** ONE named test `TestCacheSyncCursorDeltaHardDeletesOnPermissionLoss` — a sync-layer test asserting, through the projection-sync interface against a stub knowledge projection, that a `sync_cursor_<space>` delta applies server-supplied delta + tombstone frames, that a permission-loss tombstone hard-deletes from cache/FTS/on-disk, and that the projected `content_pm` + lifecycle + verification + presentational fields land in the cache. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a warm cursor, When syncing, Then only a cursor-delta (server-supplied delta + tombstone frames), not a full crawl, is downloaded. *Assertion: request is delta-scoped from `sync_cursor_<space>`; no full crawl.* [Source: FR-CLI15/NFR-CLI2]
- [ ] **AC2** — Given a permission-loss tombstone, When applied, Then the page is hard-DELETED from cache, FTS, and the on-disk mirror (like a purge). *Assertion: no residue in any of the three stores (closes D13).* [Source: FR-CLI15/NFR-CLI4]
- [ ] **AC3** — Given the projection, When applied, Then it carries `content_pm`, full lifecycle (`status/doc_type/supersedes/superseded_by/redirect_from/owner/last_reviewed_at`), verification stamps, presentational fields, and byte-stable markdown for content-hash skip. *Assertion: all listed fields present in the cache row.* [Source: FR-CLI15; OQ-CLI1 content_pm parity — TBD, pinned by ENG-2105]
- [ ] **AC4** — Given throughput, When measured, Then sync sustains ≥20 pages/s (NFR-K2). *Assertion: measured rate ≥20 pages/s on the bench fixture.* [Source: NFR-CLI2]
- [ ] **AC5 (ACL)** — Given the sync, When applied, Then it is ACL-narrowed (never syncs content the principal cannot access). *Assertion: out-of-ACL pages never enter the cache.* [Source: FR-CLI15/NFR-CLI4]

## 🔨 Tasks

- [ ] RED: write `TestCacheSyncCursorDeltaHardDeletesOnPermissionLoss` (AC: 1,2,3,5).
- [ ] GREEN: implement the projection-delta sync over `internal/client/knowledge` + `internal/db` (AC: 1,3,5).
- [ ] Wire hard-delete tombstones across cache/FTS/on-disk (AC: 2).
- [ ] Add a throughput bench asserting ≥20 pages/s (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — Cursor-delta projection + hard-delete tombstones: AdvanceCursor stores a numerically-monotonic epoch-ms cursor (never drops a smaller replay), HardDelete removes record+FTS row with no soft-trash, applyCursoredMutation runs the mutation before advancing the cursor (crash-safe convergence). content_pm is carried as the opaque projection blob (records.data). cache_test.go covers monotonicity + tombstones.

Expected tier placement: `internal/daemon` sync + `internal/db`. Seam crossed: knowledge FR-K20 projection. Sibling dependency: E6-S1 (cache), E6-S3 (SSE triggers), E7 (token for ACL-narrowed sync). content_pm parity is OQ-CLI1 (may degrade `--prosemirror`/hash-skip if excluded).

## 🧪 Testing

Named DoD test at sync tier against a stub knowledge projection + real SQLite/FTS. CS §5 mocking: stub the knowledge projection HTTP boundary; real SQLite; never mock own db/daemon.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Zero-Trust (ACL-narrowed, hard-delete), Efficiency (delta not crawl), Data-integrity. Cell-lint JGAUQRsw2g §4 (cursor cell-qualification — see E6-S4).

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F6 FR-CLI15; NFR-CLI2/NFR-CLI4; OQ-CLI1)
- Architecture: orvex-cli — pf10XC2Qjz (§Data flow: cache-sync delta)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — FR-K20 projection fields + content_pm OQ-CLI1).
- [ ] Blocked by: parent epic **E6**; E6-S1, E7-S1 (token).
- [ ] Intra-service: feeds E5-S1 (`--cached`), E5-S3 (entitlement state).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI1 (content_pm parity).
