## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **platform agent**, I want the full page verb grammar (`get/list/create/update/patch/upsert/replace/edit/scaffold/delete/trash/purge/move/restore/duplicate`) routed to orvex-wiki-api with CAS `ifVersion→409 VERSION_MISMATCH`, `DUPLICATE_CANDIDATE` (exit 8), and `QUOTA_EXCEEDED` verbatim, so that my existing parsing and recovery logic works unchanged and no wiki write ever reaches the engine directly.

**Definition of Done:** ONE named test `TestWikiPageVerbsRouteToWikiApiWithCAS` — a wiki-client test asserting, through the wiki namespace commands against a stub wiki-api, that `update` with a stale `ifVersion` yields `409 VERSION_MISMATCH`, `upsert` surfaces `DUPLICATE_CANDIDATE` at exit 8, `QUOTA_EXCEEDED` passes through verbatim, and every verb targets a wiki-api host (never the engine). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a `create`/`get`/`list`/`update`/`delete` etc., When run, Then the request targets orvex-wiki-api and decodes the `{data}` envelope; the receipt `{url,id,version,persisted}` surfaces on writes. *Assertion: host is wiki-api; envelope/receipt shapes match the reference.* [Source: FR-CLI4/FR-CLI10]
- [ ] **AC2** — Given `update`/`replace` with a stale `ifVersion`, When submitted, Then `409 VERSION_MISMATCH` is surfaced (CAS gate). *Assertion: stale base → VERSION_MISMATCH, no write applied.* [Source: FR-CLI8; OQ-CLI4 — ifVersion representation TBD, pinned by ENG-2105]
- [ ] **AC3** — Given `upsert` matching an existing similar page, When run, Then `DUPLICATE_CANDIDATE` at exit 8 with a runnable `next`. *Assertion: exit 8 + DUPLICATE_CANDIDATE + `next` recovery command.* [Source: FR-CLI8]
- [ ] **AC4** — Given a quota-over write, When submitted, Then `QUOTA_EXCEEDED` (402) propagates verbatim — never cached, never partial-written. *Assertion: passthrough, no cache write, atomic batch not decomposed.* [Source: FR-CLI8]
- [ ] **AC5 (negative)** — Given any wiki verb, When traced, Then no request reaches the engine's raw API. *Assertion: engine host absent from all traces.* [Source: FR-CLI10; D-S16]

## 🔨 Tasks

- [ ] RED: write `TestWikiPageVerbsRouteToWikiApiWithCAS` (AC: 1,2,3,4,5).
- [ ] GREEN: implement the wiki page verb commands over the wiki client (E1-S4) + transport (E1-S3) (AC: 1).
- [ ] Wire CAS ifVersion, DUPLICATE_CANDIDATE exit 8, QUOTA_EXCEEDED verbatim (AC: 2,3,4).
- [ ] Assert atomic batches never decomposed (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — Full page CRUD + verb grammar: get/list/create/update/patch/upsert/delete against wiki-api v1 (POST /v1/wiki, GET /v1/wiki/{loc}, PUT/PATCH /v1/pages, /v1/pages/upsert, bulk delete). CAS ifVersion → typed VERSION_MISMATCH; UpsertPage surfaces DUPLICATE_CANDIDATE (exit 8); LOSSY_WRITE_FORMAT_REJECTED gate before any request. wiki_page_test.go covers them. QUOTA_EXCEEDED is surfaced via the shared decodeServiceError path (in vocab).

Expected tier placement: `cmd/wiki/*` thin handlers → `internal/client/wiki`. NestJS engine analogue: the wiki-api Phase-0 `/api/orvex/*` facade over the engine's internal API (byte-compatible). Seam crossed: orvex-wiki-api contract. Sibling dependency: E1-S3/S4, E4 (pkg/dfm for content bodies), E2-S2/S3.

## 🧪 Testing

Named DoD test at wiki-client tier against a stub wiki-api (httptest). CS §5 mocking: stub the wiki-api HTTP boundary (external seam); never mock own client/transport.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §7 seam, §4 error handling). SE-Arch 8sYi523i4t lenses: Contract-Conformance, Reliability (CAS/idempotency), Zero-Trust. Cell-lint JGAUQRsw2g §5 (idempotency on writes).

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI4, F3 FR-CLI8, F4 FR-CLI10; OQ-CLI4)
- Architecture: orvex-cli — pf10XC2Qjz (§Data flow: wiki write; §Dependencies)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — wiki-api OpenAPI + ifVersion rep OQ-CLI4).
- [ ] Blocked by: parent epic **E3**; E1-S3, E1-S4, E4-S1, E2-S2/S3.
- [ ] Intra-service: precedes E3-S2..S6 (share the wiki client).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI4 if the ifVersion wire representation is unpinned at dispatch.
