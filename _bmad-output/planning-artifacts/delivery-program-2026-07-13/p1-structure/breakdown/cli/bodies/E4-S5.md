## 🎯 Story

As an **offline author**, I want `mirror push/pull/watch` and `update --content markdown` to convert content locally via pkg/dfm, so that authoring never hard-fails on an unreachable conversion endpoint — closing reference defect D1 and delivering the `NFR-CLI3` offline-correctness bar.

**Definition of Done:** ONE named test `TestOfflineAuthoringPathsSucceedWithUpstreamsDown` — a system-layer test asserting, through `mirror push`/`pull` and `update --content markdown` with wiki-api/knowledge/ai unreachable, that local conversion succeeds and only live-only verbs degrade to a typed `*_UNREACHABLE` error (with `--cached` fallback where applicable). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `update --content markdown` with the conversion endpoint unreachable, When run, Then conversion completes locally via pkg/dfm. *Assertion: markdown→PM succeeds offline (D1 closed).* [Source: FR-CLI12/NFR-CLI3]
- [ ] **AC2** — Given `mirror push`/`pull`, When run offline, Then local conversion succeeds and the push queues/reports a typed staleness/`*_UNREACHABLE` only for the network leg. *Assertion: conversion offline-OK; network leg typed-errors, no crash.* [Source: NFR-CLI3]
- [ ] **AC3** — Given `mirror watch`, When a local file changes offline, Then the watch converts locally and defers the network write with a typed indication. *Assertion: watch converts offline; write deferred, not lost.* [Source: FR-CLI12/NFR-CLI3]
- [ ] **AC4 (negative)** — Given a live-only verb (e.g. `ai ask`) offline, When run, Then a typed `*_UNREACHABLE` error is returned, never a false-green. *Assertion: typed error, no fabricated result.* [Source: NFR-CLI3]

## 🔨 Tasks

- [ ] RED: write `TestOfflineAuthoringPathsSucceedWithUpstreamsDown` (AC: 1,2,3,4).
- [ ] GREEN: wire `mirror push/pull/watch` + `update --content markdown` through the pkg/dfm embedder with offline-first conversion (AC: 1,2,3).
- [ ] Ensure live-only verbs degrade to typed `*_UNREACHABLE` (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — mirror pull/push are real (internal/mirror + in-process PM<->DfM, CAS ifVersion on push) and `update --content --format dfm` converts offline via resolvePM. BUT `wiki mirror watch` is a scaffold stub (leaf() → NOT_IMPLEMENTED). mirror_test.go covers pull/push.

Expected tier placement: `cmd/wiki/mirror/*` + `cmd/wiki/page update` → `internal/content` + `internal/client/wiki`. Seam crossed: wiki-api (deferred network write). Sibling dependency: E4-S1 (converters), E3-S1 (wiki write), E6 (daemon for watch/deferred writes).

## 🧪 Testing

Named DoD test at system tier with upstreams stubbed-down. CS §5 mocking: stub the wiki-api boundary as unreachable; never mock own content/dfm packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §10 graceful degradation — never false-green). SE-Arch 8sYi523i4t lenses: Reliability (offline), Honesty. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F5 FR-CLI12; NFR-CLI3; Goals G4 — closes D1)
- Architecture: orvex-cli — pf10XC2Qjz (§Data flow: wiki write offline-capable)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E4**; E4-S1, E3-S1.
- [ ] Intra-service: `mirror watch` deferred writes coordinate with E6 (daemon).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
