## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **migration operator**, I want `migrate import/export/verify/apply` routed to orvex-wiki-api, so that bulk content import/export and verify/apply flow through the facade with reference envelopes and no engine-direct call.

**Definition of Done:** ONE named test `TestMigrateImportExportRouteToWikiApi` — a wiki-client test asserting, through the migrate commands against a stub wiki-api, that `export` produces a reference-shaped bundle, `import`/`apply` are idempotent (Idempotency-Key), `verify` reports drift/conformance, and every call targets wiki-api. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `export`, When run, Then a reference-shaped bundle is produced via wiki-api. *Assertion: export bundle shape matches the reference; host is wiki-api.* [Source: FR-CLI4/FR-CLI10]
- [ ] **AC2** — Given `import`/`apply`, When re-run on a transient failure, Then the write is idempotent (Idempotency-Key) and atomic batches are never decomposed. *Assertion: replay carries a stable Idempotency-Key; no double-apply.* [Source: FR-CLI10; finding F-F]
- [ ] **AC3** — Given `verify`, When run, Then it reports conformance/drift for the migration. *Assertion: verify output enumerates issues per the reference.* [Source: FR-CLI4]
- [ ] **AC4 (negative)** — Given any migrate verb, When traced, Then no engine-direct call occurs. *Assertion: engine host absent.* [Source: FR-CLI10; D-S16]

## 🔨 Tasks

- [ ] RED: write `TestMigrateImportExportRouteToWikiApi` (AC: 1,2,3,4).
- [ ] GREEN: implement migrate import/export/verify/apply over wiki-api (AC: 1,3).
- [ ] Wire Idempotency-Key on import/apply; assert atomic batches (AC: 2).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — Top-level `migrate` namespace (scan/apply/verify) is REAL and in-process: scanTree inventories markdown, apply converts via content.MarkdownToPM and drives wiki-api SavePage writing a manifest, verify re-reads and compares content.Fidelity histograms → GATE_UNSATISFIED on embed loss. migrate_test.go covers it. The `wiki migrate` facade verbs (import/export/verify/apply) are thin client passthroughs to /v1/migrate/* (route existence unverified).

Expected tier placement: `cmd/wiki/migrate/*` → `internal/client/wiki` + `internal/content` (pkg/dfm for bundle bodies). Seam crossed: wiki-api. Sibling dependency: E3-S1 (wiki client), E4 (pkg/dfm for content), E1-S3 (idempotency).

## 🧪 Testing

Named DoD test at wiki-client tier against a stub wiki-api. CS §5 mocking: stub wiki-api boundary; never mock own client/content packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance, Reliability (idempotent bulk), Data-integrity. Cell-lint JGAUQRsw2g §5 (idempotency).

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI4; F4 FR-CLI10; Phasing C9)
- Architecture: orvex-cli — pf10XC2Qjz (§Dependencies)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E3**; E3-S1, E4-S1, E1-S3.
- [ ] Intra-service: parallel with E3-S2..S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
