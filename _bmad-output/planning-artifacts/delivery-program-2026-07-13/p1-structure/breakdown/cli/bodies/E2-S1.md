## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **caller learning the CLI**, I want a namespace-first cobra tree (`wiki`/`search`/`ai`/`auth`/`admin` service namespaces; `daemon`/`cache`/`config`/`doctor`/`audit`/`instructions`/`completion`/`version` client-local groups) with hidden back-compat aliases, so that the surface is organised by service and existing `page|space|comment|label|attach` invocations still resolve for one release window.

**Definition of Done:** ONE named test `TestCommandTreeIsNamespaceFirstWithHiddenAliases` — a command-layer test asserting, through the assembled root command, that service namespaces and client-local groups exist, that `orvex page get` resolves to `orvex wiki page get` as a hidden alias, and that no `linear`/`issue` group exists. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the root command, When assembled, Then `wiki`, `search`, `ai`, `auth`, `admin` are service namespaces and `daemon`, `cache`, `config`, `doctor`, `audit`, `instructions`, `completion`, `version` are client-local groups. *Assertion: each command is registered under the correct parent.* [Source: FR-CLI3]
- [ ] **AC2** — Given a legacy invocation `orvex page get <locator>`, When run, Then it resolves to `wiki page get` via a hidden alias. *Assertion: alias dispatches to the wiki subcommand; alias is `Hidden: true`.* [Source: FR-CLI3]
- [ ] **AC3 (negative)** — Given the tree, When enumerated, Then no `linear` or `issue` group exists. *Assertion: `linear`/`issue` commands are absent (D-S11).* [Source: FR-CLI9 — enforced-by-construction; FR-CLI3]
- [ ] **AC4** — Given `PersistentPreRunE`, When any command runs, Then config/auth/timeout/output options resolve exactly once. *Assertion: pre-run resolves shared context once, not per-subcommand.* [Source: Arch §Overview]

## 🔨 Tasks

- [ ] RED: write `TestCommandTreeIsNamespaceFirstWithHiddenAliases` (AC: 1,2,3).
- [ ] GREEN: build the cobra tree + hidden aliases + shared `PersistentPreRunE` (AC: 1,2,4).
- [ ] Assert no Linear group is ever registered (AC: 3).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — Namespace-first cobra tree (wiki/search/ai/auth/admin/migrate service namespaces + daemon/cache/config/doctor/audit/instructions/version client-local groups). Hidden back-compat aliases (page/space/comment/label/attach) registered in legacy.go, each emitting a typed deprecation pointer with WithNext.

Expected tier placement: `cmd/` command files — thin (parse flags, one internal call, render); all domain logic in `internal/` (CS anti-slop rows 1/11). Seam crossed: none (structure only). Sibling dependency: E1-S2 (config resolved in pre-run); consumed by every namespace story.

## 🧪 Testing

Named DoD test at command tier over the assembled cobra tree. CS §5 mocking: none — inspect real command registration; never mock own packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. anti-slop: thin handlers). SE-Arch 8sYi523i4t lenses: Simplicity, Contract-Conformance. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI3; Non-goals — Linear)
- Architecture: orvex-cli — pf10XC2Qjz (Components: Command tree; §Overview)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E2**; E1-S1 (scaffold), E1-S2 (config for pre-run).
- [ ] Intra-service: precedes all namespace command stories.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
