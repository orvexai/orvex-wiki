## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **program maintainer**, I want a behaviour-parity harness that diffs every retained (non-Linear) command against the `docmost-cli` reference and runs as a CI gate, so that no repoint silently regresses an output envelope, exit code, errorCode string, or ENG-* behaviour.

**Definition of Done:** ONE named test `TestParityHarnessMatchesDocmostCliReference` — a CI-layer harness test asserting, through executing both binaries over the reference contract-test corpus, that orvex-cli's output envelopes, exit codes, errorCode strings, and ENG-* behaviours match the reference for every retained command (Linear cases excluded). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the reference contract-test corpus, When the harness runs a retained command, Then output envelope + exit code + errorCode match the reference. *Assertion: per-case diff is empty for envelope/exit/errorCode.* [Source: FR-CLI2/NFR-CLI1]
- [ ] **AC2** — Given a Linear-touching reference case, When the harness enumerates, Then it is excluded (not failed). *Assertion: Linear cases skipped by construction, not counted as regressions.* [Source: FR-CLI2/FR-CLI9]
- [ ] **AC3** — Given any retained command regresses, When the harness runs in CI, Then the gate fails. *Assertion: a seeded regression fails the CI gate (parity is a gate, not a spot-check).* [Source: NFR-CLI1]
- [ ] **AC4 (coverage)** — Given the retained command set, When the harness reports, Then every retained command has ≥1 parity case. *Assertion: coverage report lists each retained command.* [Source: NFR-CLI1]

## 🔨 Tasks

- [ ] RED: write `TestParityHarnessMatchesDocmostCliReference` skeleton + a seeded-regression case (AC: 1,3).
- [ ] GREEN: build the dual-binary harness over the reference corpus (envelope/exit/errorCode diff) (AC: 1,4).
- [ ] Exclude Linear cases; wire the CI required-check gate (AC: 2,3).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — docmost-cli behaviour-parity harness present: TestByteParityCorpus (PM fixtures lifted verbatim into testdata/corpus/content), TestWikiVerbParity + TestWikiContentCarryingVerbParityCompleteness (command-tree walk fails closed on an uncovered content-carrying verb). All wired as a named CI required check (ci.yml:86).

Expected tier placement: `test/parity` harness + CI workflow; the reference is a spec/oracle, never an import (FR-CLI2). Seam crossed: none at runtime; compares two binaries' observable contracts. Sibling dependency: depends on E2-S2/S3 (output+error), grows as each namespace lands.

## 🧪 Testing

Named DoD test at CI/system tier executing both binaries. CS §5 mocking: no mocks — real binary execution against fixtures; the reference is the oracle, not a mocked own package.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance, Honesty/No-false-green. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F1 FR-CLI2; NFR-CLI1; Goals G2)
- Audit & Rebirth Plan — PACF13d3I3 (reference as behavioural oracle)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E2**; E2-S2, E2-S3.
- [ ] Intra-service: cross-cuts — extended by every namespace story as it lands.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if the reference exhibits behaviour the Definition Pack did not capture.
