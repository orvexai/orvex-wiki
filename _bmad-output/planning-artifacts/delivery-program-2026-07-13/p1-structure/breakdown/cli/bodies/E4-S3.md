## 🎯 Story

As a **content-integrity maintainer**, I want a golden-corpus parity test (FR-CLI21) carrying the ENG-1327/1328/897/898/1261 defences, so that pkg/dfm conversion is proven against the block-schema catalog + golden fixtures — the acceptance gate that lets the reference's lossy-era compensating machinery be dropped.

**Definition of Done:** ONE named test `TestGoldenCorpusParityWithEngDefences` — a content-layer test asserting, over the contracts golden-fixture corpus, node/mark histogram parity (ENG-1328), empty-list-item lint (ENG-1327), lossless inline-escape round-trip (ENG-897), and `EMBED_DEGRADATION`/`LOSSY_WRITE_FORMAT_REJECTED` behaviour (ENG-898/1261: writes accept only `format ∈ {json,dfm}`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the golden-fixture corpus, When converted, Then node/mark histogram parity holds (ENG-1328 `--prosemirror` guard). *Assertion: node/mark counts equal reference for every fixture.* [Source: FR-CLI21/FR-CLI8]
- [ ] **AC2** — Given an empty list item, When linted, Then ENG-1327 fires. *Assertion: empty-list-item lint triggers.* [Source: FR-CLI8]
- [ ] **AC3** — Given inline-escape content, When round-tripped, Then it is lossless (ENG-897). *Assertion: inline escapes survive round-trip byte-for-byte.* [Source: FR-CLI8]
- [ ] **AC4** — Given a write with `format ∉ {json,dfm}`, When submitted, Then `LOSSY_WRITE_FORMAT_REJECTED` (ENG-898/1261); an embed degradation raises `EMBED_DEGRADATION`. *Assertion: rejected formats + embed-degradation errorCodes fire.* [Source: FR-CLI8]
- [ ] **AC5 (corpus adequacy)** — Given the golden corpus, When enumerated, Then it covers embed/opaque-node/table-cell-mark/mermaid pairs (not only the 6 core-GFM pairs flagged inadequate). *Assertion: corpus includes the flagged-missing categories.* [Source: FR-CLI21; evidence — inadequate corpus]

## 🔨 Tasks

- [ ] RED: write `TestGoldenCorpusParityWithEngDefences` over the corpus (AC: 1,2,3,4).
- [ ] GREEN: implement the parity harness + ENG-* defence checks (AC: 1,2,3,4).
- [ ] Expand the golden corpus to cover embed/opaque/table-cell-mark/mermaid (AC: 5).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — Golden-corpus parity test present (TestByteParityCorpus_ContentRoundTrip + embed-manifest test over testdata/corpus/content). BUT knownDfmDrift SKIPS codeblock.json/lists.json/misc.json (real reproduced round-trip failures in the pinned pkg/dfm). The specific ENG-1327/1328/897/898/1261 defences are NOT implemented as such — '1327/1328' appear only in the `wiki verify lint` stub's help string, not as active guards.

Expected tier placement: `internal/content` parity test + fixtures. Seam crossed: consumes the contracts golden fixtures (build-time). Sibling dependency: E4-S1 (converters), E4-S2 (clean-room), E2-S3 (errorCode vocabulary for the ENG defences). This is the gate for E4-S4 (machinery trim).

## 🧪 Testing

Named DoD test at content tier over golden fixtures. CS §5 mocking: none — real converters over real fixtures; never mock own content package.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Data-integrity (parity), Contract-Conformance. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F5 FR-CLI13/FR-CLI21; F3 FR-CLI8)
- Evidence digest — orvexcli.md (§6 inadequate golden-fixture corpus)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — golden fixtures; corpus adequacy).
- [ ] Blocked by: parent epic **E4**; E4-S1, E4-S2, E2-S3.
- [ ] Intra-service: gates E4-S4 (machinery trim).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if the golden corpus remains inadequate at dispatch.
