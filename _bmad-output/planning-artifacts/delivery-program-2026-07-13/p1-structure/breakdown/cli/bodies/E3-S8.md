## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **full-binary user**, I want `screenshot` (chromium) and `code graph` (treesitter) as full-binary verbs, so that page rendering/screenshots and local code-graph tagging work in `orvex-full` while the default binary honestly stubs them.

**Definition of Done:** ONE named test `TestScreenshotAndCodeGraphFullBinaryOnly` — a build-tag test asserting, through the commands under each build tag, that `screenshot`/`code graph` return `REQUIRES_FULL_BINARY` on the default binary and execute under the chromium/treesitter tags, with `code graph` operating locally (no service call). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `screenshot` on `orvex-full` (chromium tag), When run, Then a page screenshot is produced. *Assertion: screenshot output produced under chromium tag.* [Source: FR-CLI4/FR-CLI6]
- [ ] **AC2** — Given `code graph` on the treesitter/cgo build, When run, Then it produces a local code graph with no service call. *Assertion: code-graph output; no outbound HTTP.* [Source: FR-CLI4/FR-CLI6]
- [ ] **AC3 (negative)** — Given the default binary, When invoking either, Then `REQUIRES_FULL_BINARY` and the `code` group is hidden. *Assertion: stub error; `code` group hidden.* [Source: FR-CLI6]
- [ ] **AC4** — Given the daemon platform constraint, When installing, Then daemon install remains Linux/systemd-user only (no launchd/Windows). *Assertion: install path is systemd-user only.* [Source: FR-CLI6; Constraints — platform]

## 🔨 Tasks

- [ ] RED: write `TestScreenshotAndCodeGraphFullBinaryOnly` under both tags (AC: 1,2,3).
- [ ] GREEN: implement chromium screenshot + treesitter code graph under build tags (AC: 1,2).
- [ ] Confirm honest stubs + hidden `code` group on default (AC: 3).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — screenshot (chromedp headless-Chromium, png + svg) and code graph (tree-sitter Go grammar, deterministic sorted symbol/import dep-graph) are real, compiled only under -tags full; stub variants return REQUIRES_FULL_BINARY. build_test.go exercises the real tree-sitter walk over fixtures; screenshot_test + stub/full cmd tests cover the plumbing. ci.yml builds+tests the full variant.

Expected tier placement: `cmd/wiki/{screenshot,code}` under `//go:build chromium`/`treesitter` tags → `internal/render`, `internal/codegraph` (local). Seam crossed: none for code graph (local, treesitter tag); screenshot renders wiki-api-fetched content locally. Sibling dependency: E2-S4 (variant gating), E3-S1 (content fetch for screenshot).

## 🧪 Testing

Named DoD test at command tier compiled under each build tag. CS §5 mocking: none for local code graph; never mock own packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Honesty (no false full-capability), Simplicity. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI4/FR-CLI6; Constraints)
- Architecture: orvex-cli — pf10XC2Qjz (Components: two build variants)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E3**; E2-S4, E3-S1.
- [ ] Intra-service: last wiki story (depends on variant gating).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
