## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **wiki agent**, I want navigation, watchers, permissions, transclusion, and governance verbs routed to orvex-wiki-api, so that tree/backlinks/breadcrumbs, watch state, ACL reads/writes, transclusion, and lifecycle governance (`supersede/status/verify/label`) all work through the facade with the reference's envelopes and no engine-direct call.

**Definition of Done:** ONE named test `TestWikiNavPermGovernanceVerbs` — a wiki-client test asserting, through the nav/permissions/governance commands against a stub wiki-api, that `tree`/`breadcrumbs`/`backlinks`/`recent`/`resolve-slug` return reference-shaped envelopes, permission verbs propagate 401/403/404 verbatim, and `supersede`/`status`/`verify`/`label` apply governance stamps via wiki-api. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `tree`/`breadcrumbs`/`backlinks`/`recent`/`resolve-slug`, When run, Then reference-shaped nav envelopes return from wiki-api. *Assertion: nav envelope fields match the reference.* [Source: FR-CLI4]
- [ ] **AC2** — Given `watchers` add/remove/list, When run, Then watch state changes via wiki-api. *Assertion: watcher mutation reflected in a subsequent list.* [Source: FR-CLI4]
- [ ] **AC3** — Given permission verbs, When the caller lacks access, Then 401/403/404 propagate verbatim with no existence oracle. *Assertion: status/errorCode unchanged; no leak of existence.* [Source: FR-CLI4/FR-CLI10; NFR-CLI4]
- [ ] **AC4** — Given governance `supersede/status/verify/label` + transclusion, When applied, Then lifecycle stamps/transclusion route through wiki-api. *Assertion: governance mutation persists via wiki-api; no engine host in trace.* [Source: FR-CLI4/FR-CLI10]

## 🔨 Tasks

- [ ] RED: write `TestWikiNavPermGovernanceVerbs` (AC: 1,2,3,4).
- [ ] GREEN: implement nav/watchers/permissions/transclusion/governance commands over the wiki client (AC: 1,2,4).
- [ ] Wire verbatim 401/403/404 propagation (AC: 3).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — nav (tree/outline/breadcrumbs/backlinks/recent/resolve-slug/watchers/permissions/transclusion) and governance (supersede/status/verify/label) verbs + WikiClient methods implemented; wiki_verbs_test.go covers them. Caveat: some nav leaf backend routes are flagged known-broken/unverified server-side (wiki.go:270 note) — the CLI side is complete but end-to-end depends on wiki-api routes that may not all exist yet.

Expected tier placement: `cmd/wiki/*` thin handlers → `internal/client/wiki`. NestJS analogue: wiki-api facade over engine CASL/ACL (the CLI makes no ACL decision). Seam crossed: orvex-wiki-api. Sibling dependency: E3-S1 (shared wiki client), E2-S2/S3.

## 🧪 Testing

Named DoD test at wiki-client tier against a stub wiki-api. CS §5 mocking: stub wiki-api HTTP boundary; never mock own client.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §7 seam posture — ACL stays upstream). SE-Arch 8sYi523i4t lenses: Zero-Trust (no existence oracles), Contract-Conformance. Cell-lint JGAUQRsw2g: n/a beyond write-idempotency.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI4; F4 FR-CLI10; NFR-CLI4)
- Architecture: orvex-cli — pf10XC2Qjz (§Boundaries; §Data flow)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — wiki-api OpenAPI).
- [ ] Blocked by: parent epic **E3**; E3-S1.
- [ ] Intra-service: parallel with E3-S3..S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
