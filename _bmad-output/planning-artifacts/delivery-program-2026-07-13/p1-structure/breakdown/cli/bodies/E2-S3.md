## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **script author**, I want the frozen 0–9 exit codes and the errorCode string vocabulary preserved and extended-never-renumbered, exposed as a shared client artifact across all namespaces, so that my recovery logic branches on the stable string (never the number) and keeps working after the repoint.

**Definition of Done:** ONE named test `TestExitCodesAndErrorVocabularyAreFrozen` — an error-layer test asserting, through the error taxonomy's public API, that `DUPLICATE_CANDIDATE`→exit 8, `TITLE_AMBIGUOUS`→exit 4, `GATE_UNSATISFIED`→exit 9, the `{error_code,message,hint?,matches?,request_id?,next?}` envelope shape, and that `LINEAR_NOT_CONNECTED` is absent while all shared codes (AUTH_*/RATE_LIMITED/*_UNREACHABLE/INVALID_ARGS) are present. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given each load-bearing errorCode, When mapped to an exit code, Then `DUPLICATE_CANDIDATE`→8, `TITLE_AMBIGUOUS`→4, `GATE_UNSATISFIED`→9 hold, and numbers are never renumbered (only extended). *Assertion: exit-code map matches the reference exactly.* [Source: FR-CLI8]
- [ ] **AC2** — Given an error, When rendered, Then the `{error_code,message,hint?,matches?,request_id?,next?}` envelope is emitted, with a literal runnable `next` recovery command at high-friction buckets. *Assertion: envelope fields present; `next` is a runnable command string at DUPLICATE/TITLE_AMBIGUOUS/GATE buckets.* [Source: FR-CLI8]
- [ ] **AC3** — Given `QUOTA_EXCEEDED`, When encountered, Then it is propagated verbatim — never cached, never partial-written-around. *Assertion: 402/QUOTA_EXCEEDED passes through unchanged with no cache write.* [Source: FR-CLI8]
- [ ] **AC4 (negative)** — Given the vocabulary, When enumerated, Then `LINEAR_NOT_CONNECTED` is absent and every other shared code (AUTH_*/RATE_LIMITED/*_UNREACHABLE/INVALID_ARGS) is present and reused across namespaces. *Assertion: no Linear code; shared codes reused, not per-namespace duplicated.* [Source: FR-CLI8/FR-CLI9]

## 🔨 Tasks

- [ ] RED: write `TestExitCodesAndErrorVocabularyAreFrozen` (AC: 1,2,3,4).
- [ ] GREEN: implement the shared error taxonomy package (code→exit map, envelope, `next` recovery) (AC: 1,2).
- [ ] Wire QUOTA_EXCEEDED verbatim passthrough + drop LINEAR_NOT_CONNECTED (AC: 3,4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — Frozen 0-9 ExitCode range + shared errorCode vocabulary (ErrorCodeRegistry in declaration order) + exitFor mapping. TestCliContractNoDrift (contract_test.go) parses errors.go via go/parser and byte-matches the const ordering + exitFor against committed goldens; wired as a named CI required check (ci.yml:71).

Expected tier placement: shared client artifact `internal/errs` reused by all namespaces (pairs with E2-S2). Seam crossed: none (contract-preservation). Sibling dependency: E1-S3 (transport surfaces `request_id`); consumed everywhere. Note ENG-* defences (1327/1328/897/898/1261) are exercised in E4-S3 but their errorCodes (`EMBED_DEGRADATION`/`LOSSY_WRITE_FORMAT_REJECTED`) live in this vocabulary.

## 🧪 Testing

Named DoD test at error tier. CS §5 mocking: none — assert the real taxonomy; never mock own error package. Oracle is docmost-cli's frozen codes (E2-S5).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance, Zero-Trust (no existence oracles in messages). Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F3 FR-CLI8; Non-goals — Linear)
- Architecture: orvex-cli — pf10XC2Qjz (§Overview; §Boundaries)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — frozen errorCode vocabulary is a shared contracts artifact).
- [ ] Blocked by: parent epic **E2**; E2-S2 (output layer), E1-S3 (request_id).
- [ ] Intra-service: precedes all error-bearing stories.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE if a new bucket needs a code not in the contracts vocabulary.
