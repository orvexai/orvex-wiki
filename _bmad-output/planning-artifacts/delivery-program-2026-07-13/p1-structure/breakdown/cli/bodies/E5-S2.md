## 🎯 Story

As an **agent needing answers**, I want the `ai` namespace (`ask/chat/inline/image/models/cost`) routed to orvex-studio-ai, returning the cited-ask shape and surfacing `BUDGET_EXCEEDED`/`MODEL_UNAVAILABLE`, so that I get cited answers and cost/model introspection without touching the engine.

**Definition of Done:** ONE named test `TestAiNamespaceRoutesToAiWithBudgetErrors` — an ai-client test asserting, through the ai commands against a stub ai service, that `ask` returns `{answer,citations,unanswered,gapNote,confidence,followups}`, `models`/`cost` introspect, and `BUDGET_EXCEEDED`/`MODEL_UNAVAILABLE` surface as typed errors; no engine host in any trace. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `ai ask`, When run, Then it routes to ai and returns the cited-ask shape `{answer,citations,unanswered,gapNote,confidence,followups}`. *Assertion: response shape matches; host is ai.* [Source: FR-CLI5/FR-CLI11]
- [ ] **AC2** — Given `chat`/`inline`/`image`/`models`/`cost`, When run, Then each routes to ai with typed results. *Assertion: each targets ai; result typed.* [Source: FR-CLI5/FR-CLI11]
- [ ] **AC3** — Given a budget-over or unavailable model, When invoked, Then `BUDGET_EXCEEDED`/`MODEL_UNAVAILABLE` surface as typed errors. *Assertion: typed errorCodes surface.* [Source: FR-CLI5]
- [ ] **AC4 (negative)** — Given ai unreachable, When run, Then a typed `AI_UNREACHABLE`/`*_UNREACHABLE` degradation, never an engine fallback or false answer. *Assertion: typed error; no engine host; no fabricated answer.* [Source: FR-CLI11/NFR-CLI3]

## 🔨 Tasks

- [ ] RED: write `TestAiNamespaceRoutesToAiWithBudgetErrors` (AC: 1,2,3,4).
- [ ] GREEN: implement the ai commands over `internal/client/ai` (AC: 1,2).
- [ ] Surface BUDGET_EXCEEDED/MODEL_UNAVAILABLE + typed unreachable degradation (AC: 3,4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — ask (cited-ask loop), cost, and image dispatch for real to orvex-studio-ai; chat/inline/models are routing-only stubs (leaf → NOT_IMPLEMENTED). BUDGET_EXCEEDED and MODEL_UNAVAILABLE codes are NOT in the frozen vocabulary (only QUOTA_EXCEEDED/RATE_LIMITED exist) — the DoD's specific error codes are absent. ai_test.go covers the wired verbs.

Expected tier placement: `cmd/ai/*` → `internal/client/ai`. Seam crossed: ai query API (host unpinned, OQ-CLI2). The CLI owns no ask/RAG loop (ai does). Sibling dependency: E1-S3/S4; `image` coordinates with E3-S3 (`image_from_prompt` block, OQ-CLI5).

## 🧪 Testing

Named DoD test at ai-client tier against a stub ai service. CS §5 mocking: stub the ai HTTP boundary; never mock own client.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance, Cost/Resource-governance (budget), Honesty (no fabricated answers). Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI5; F4 FR-CLI11; OQ-CLI2/OQ-CLI5)
- Architecture: orvex-cli — pf10XC2Qjz (§Data flow: search and ask)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — ai typed shapes; ai host OQ-CLI2).
- [ ] Blocked by: parent epic **E5**; E1-S3/S4.
- [ ] Intra-service: `image` coordinates with E3-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI2 (ai host) / OQ-CLI5 (image path).
