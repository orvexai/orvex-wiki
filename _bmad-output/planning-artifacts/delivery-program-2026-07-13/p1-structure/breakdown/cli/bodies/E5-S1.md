## 🎯 Story

As an **agent doing retrieval**, I want the `search` namespace (`keyword/semantic/hybrid/related/duplicates/attachment-search`) routed to orvex-studio-knowledge with a `--cached` local-FTS5 fallback, so that I get ranked/faceted results without touching the engine — and still get useful results when knowledge is unreachable.

**Definition of Done:** ONE named test `TestSearchNamespaceRoutesToKnowledgeWithCachedFallback` — a search-client test asserting, through the search commands against a stub knowledge service, that `hybrid`/`semantic`/`related`/`duplicates`/`attachment-search` target knowledge, and that `--cached` falls back to local FTS5 with sanitisation + superseded/archived default-exclusion preserved. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `keyword`/`semantic`/`hybrid`/`related`/`duplicates`, When run, Then requests target knowledge and return ranked/faceted typed results. *Assertion: host is knowledge; result shape is the knowledge typed contract.* [Source: FR-CLI5/FR-CLI11]
- [ ] **AC2** — Given `--cached` with knowledge unreachable, When searching, Then local FTS5 answers with FTS sanitisation applied. *Assertion: FTS5 fallback returns results; query sanitised.* [Source: FR-CLI11/NFR-CLI3]
- [ ] **AC3** — Given cached search results, When returned, Then superseded/archived pages are excluded by default (matching the reference). *Assertion: superseded/archived absent unless explicitly included.* [Source: FR-CLI11]
- [ ] **AC4 (negative)** — Given knowledge unreachable and no `--cached`, When searching, Then a typed `KNOWLEDGE_UNREACHABLE`/`*_UNREACHABLE` error, never engine-direct fallback. *Assertion: typed error; no engine host in trace.* [Source: FR-CLI11/NFR-CLI3]

## 🔨 Tasks

- [ ] RED: write `TestSearchNamespaceRoutesToKnowledgeWithCachedFallback` (AC: 1,2,3,4).
- [ ] GREEN: implement the search commands over `internal/client/knowledge` (AC: 1).
- [ ] Wire `--cached` FTS5 fallback + sanitisation + superseded/archived exclusion (AC: 2,3).
- [ ] Typed `*_UNREACHABLE` on knowledge-down without `--cached` (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** partial — keyword/semantic/hybrid query verbs are real (route through wiki-api's public GET /v1/search which fronts knowledge's query pipeline) and --cached is real (local FTS5 via SQLiteStore.FTSQuery). BUT related/duplicates/attachment-search are routing stubs (leaf → NOT_IMPLEMENTED, targeting the DNS-less knowledge host), and mode-fidelity is a disclosed no-op (all three modes run the same server-side hybrid query). search_test.go covers the real verbs.

Expected tier placement: `cmd/search/*` → `internal/client/knowledge` + `internal/db` (FTS5). Seam crossed: knowledge query API. Sibling dependency: E1-S3/S4 (transport/client), E6 (daemon owns the FTS5 cache). The CLI owns no ranking/retrieval (knowledge does).

## 🧪 Testing

Named DoD test at search-client tier against a stub knowledge + a seeded local FTS5 cache. CS §5 mocking: stub knowledge HTTP boundary; use a real SQLite FTS5 fixture; never mock own client/db.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Contract-Conformance, Reliability (offline fallback), Zero-Trust (ACL-narrowed egress upstream). Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI5; F4 FR-CLI11; NFR-CLI3)
- Architecture: orvex-cli — pf10XC2Qjz (§Data flow: search and ask)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG — knowledge query typed shapes; public knowledge query host OQ-CLI2).
- [ ] Blocked by: parent epic **E5**; E1-S3/S4, E6-S1 (FTS5 cache).
- [ ] Intra-service: precedes E3-S4/S7 (attachment-search/duplicates consumers).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on OQ-CLI2 (public knowledge query host unpinned).
