## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **default-binary user**, I want the two build variants (`orvex` pure-Go and `orvex-full` chromium/treesitter) with honest `REQUIRES_FULL_BINARY` stubs and a hidden `code` group unless compiled in, so that the lightweight binary never falsely claims a full-binary capability.

**Definition of Done:** ONE named test `TestDefaultBinaryStubsFullOnlyVerbs` — a build-tag/command-layer test asserting, through the assembled command set under the default build tag, that `screenshot`/`verify render` and the `code` group return `REQUIRES_FULL_BINARY` (or are hidden), and that under the `full` tag they are present and runnable. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2105); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the default pure-Go binary, When invoking `screenshot`/`verify render`, Then it returns a typed `REQUIRES_FULL_BINARY` error. *Assertion: exit/errorCode == REQUIRES_FULL_BINARY, no partial attempt.* [Source: FR-CLI6]
- [ ] **AC2** — Given the default binary, When enumerating commands, Then the `code graph` group is hidden. *Assertion: `code` group `Hidden: true` unless the treesitter tag is compiled in.* [Source: FR-CLI6]
- [ ] **AC3** — Given `orvex-full` (chromium + treesitter/cgo tags), When invoking the same verbs, Then they are present and execute. *Assertion: under `full` tag the verbs run, not stub.* [Source: FR-CLI6]
- [ ] **AC4 (honesty)** — Given the default binary, When it reports capabilities, Then no partial binary claims a full-binary capability. *Assertion: capability report/`doctor` marks full-only verbs unavailable.* [Source: FR-CLI6; NFR-CLI6]

## 🔨 Tasks

- [ ] RED: write `TestDefaultBinaryStubsFullOnlyVerbs` under both build tags (AC: 1,2,3).
- [ ] GREEN: implement build-tag-gated commands (chromium→`orvex-full` screenshot/render; treesitter/cgo→code graph) with honest stubs (AC: 1,2,3).
- [ ] Wire the capability/`doctor` honesty report (AC: 4).

## 🧠 Context

**🧾 Code audit (origin/dev @ 48329b7, 2026-07-14):** present — Two variants via build tags: fullfeat.Enabled + Require() (stub returns REQUIRES_FULL_BINARY honestly; full is a no-op). Makefile build/build-full; ci.yml compiles AND tests both the default and -tags full variants (with cgo-toolchain assertion for tree-sitter). Stub/full test pairs exist for screenshot.

Expected tier placement: `cmd/` with `//go:build` tags + stub files; the two variants are structural (Constraints). Seam crossed: none. Sibling dependency: E2-S1 (command tree); required by E3-S8 (screenshot/code graph) and E3-S7 (verify render).

## 🧪 Testing

Named DoD test at command tier compiled under each build tag. CS §5 mocking: none for stub assertion; never mock own packages.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t lenses: Honesty/No-false-green, Simplicity. Cell-lint JGAUQRsw2g: n/a.

## 🔗 References

- PRD: orvex-cli — R4AOVBLST7 (F2 FR-CLI6; NFR-CLI6; Constraints — platform)
- Architecture: orvex-cli — pf10XC2Qjz (Components: Command tree, two build variants)

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2105** (contract TAG).
- [ ] Blocked by: parent epic **E2**; E2-S1 (command tree).
- [ ] Intra-service: precedes E3-S7 (render), E3-S8 (screenshot/code graph).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "Closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
