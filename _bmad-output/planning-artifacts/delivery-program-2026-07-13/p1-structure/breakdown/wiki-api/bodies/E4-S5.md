## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent hitting a tenant quota cap**, I want **wiki-api to propagate the engine's HTTP 402 `QUOTA_EXCEEDED` verbatim (never cached, never partial-written-around) with an upgrade/entitlement deep-link, so an atomic multi-op batch that trips the cap fails whole**, so that **the caller can route the human to billing without wiki-api making any entitlement decision (billing owns values, engine enforces, wiki-api relays)**.

**Definition of Done:** ONE named test `TestQuotaExceededPropagatesVerbatimBatchFailsWhole` (integration — an over-cap write returns 402 QUOTA_EXCEEDED unchanged with a deep-link; a batch that trips the cap applies zero ops). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given an over-cap create/duplicate/import/attachment-upload, When applied, Then 402 `QUOTA_EXCEEDED` propagates verbatim. *Assert: `TestQuotaExceededPropagatesVerbatimBatchFailsWhole` — status 402, code unchanged.* [Source: FR-A22]
- [ ] **AC2 (batch atomicity)** — Given a multi-op batch that trips the cap, When applied, Then no op is applied (fails whole). *Assert: zero mutation on quota trip.* [Source: FR-A22, FR-A6]
- [ ] **AC3 (deep-link)** — Given the 402, When returned, Then it carries an upgrade/entitlement deep-link (mirrors `needs_human_*` pattern). *Assert: deep-link present.* [Source: FR-A22]
- [ ] **AC4 (no decision)** — Given the quota, When relayed, Then wiki-api makes no entitlement/cap decision and never caches the rejection. *Assert: no local cap logic; not cached.* [Source: FR-A22]
- [ ] **AC5** — Given `QUOTA_EXCEEDED`, When surfaced, Then it is part of the contracts-authored frozen error vocabulary. *Assert: error code matches the frozen vocabulary (NFR-A6).* [Source: FR-A22, NFR-A6]

## 🔨 Tasks
- [ ] Propagate engine 402 `QUOTA_EXCEEDED` verbatim (RED: `TestQuotaExceededPropagatesVerbatimBatchFailsWhole`) (AC: 1,4)
- [ ] Confirm batch fails whole on quota trip (AC: 2)
- [ ] Attach upgrade/entitlement deep-link (AC: 3)
- [ ] Wire code into the frozen error vocabulary (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — QUOTA_EXCEEDED 402 relayed verbatim with the Studio upgrade deep-link (never masked as 5xx); a batch write fails whole via bulkops. tested.
Tier: `internal/blockpatch` / error-relay in the shaping layer. Engine enforces F-QUOTA on REST + collab paths; wiki-api relays only (FR-A22). Closes CLI contract-gaps G21/G6. Seam: engine quota chokepoint. Sibling dep: E4-S1 (batch atomicity), E5-S3 (error vocabulary).

## 🧪 Testing
`TestQuotaExceededPropagatesVerbatimBatchFailsWhole` (integration). CS §5 mocking: engine mocked at HTTP boundary returning 402; never mock own relay.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Correctness, Billing-Integrity lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A22, FR-A6, NFR-A6); Architecture pG9s9OjqZA (A-VERBS quota passthrough).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the `QUOTA_EXCEEDED` errorCode contract delta is the dispatch gate)
- [ ] Parent epic: E4. Intra: depends on E4-S1

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
