## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent editing a page**, I want **wiki-api to translate DfM/block edits into typed PM-JSON ops and issue exactly ONE atomic `apply-ops` call to the engine (with heading/anchor resolution and opaque reattach), never decomposing a multi-op batch into multiple engine calls**, so that **the shipped cross-op atomicity guarantee is preserved and a batch never silently regresses to partial-batch writes**.

**Definition of Done:** ONE named test `TestBatchIssuesSingleAtomicApplyOps` (integration — a multi-op batch produces exactly one `apply-ops` engine call; injecting a mid-batch failure leaves zero ops applied). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a multi-op batch (string patch, block patch, insert, delete), When applied, Then exactly one `apply-ops` call is issued. *Assert: `TestBatchIssuesSingleAtomicApplyOps` — engine call count == 1.* [Source: FR-A6]
- [ ] **AC2 (atomicity)** — Given a batch that fails partway, When applied, Then zero ops persist (whole-batch atomicity). *Assert: mid-batch failure → no partial write.* [Source: FR-A6]
- [ ] **AC3** — Given section-by-heading/anchor edits, When translated, Then anchor/heading resolution maps to the correct block ops. *Assert: heading resolution correct against the ifVersion base.* [Source: FR-A6]
- [ ] **AC4 (opaque)** — Given opaque nodes in the batch, When applied, Then they reattach byte-identically from the ifVersion base. *Assert: reattach byte-equality (uses E2-S3).* [Source: FR-A2, FR-A6]
- [ ] **AC5 (negative)** — Given any code path, When it would split a batch into N engine calls, Then it does not exist. *Assert: static/behavioural gate — no batch decomposition.* [Source: FR-A6, Arch §5 batch-atomicity invariant]

## 🔨 Tasks
- [ ] DfM/block edits → typed PM-JSON ops translator (RED: `TestBatchIssuesSingleAtomicApplyOps`) (AC: 1,3)
- [ ] Single atomic `apply-ops` engine call for the whole batch (AC: 1,2)
- [ ] Heading/anchor resolution against the ifVersion base (AC: 3)
- [ ] Opaque reattach via pkg/dfm (AC: 4)
- [ ] No-batch-decomposition gate (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — DfM->apply-ops translation with anchor/heading resolution and a single atomic apply-ops call; tested against recorded engine fixtures.
Tier: `internal/verb` write path → `internal/blockpatch` domain (DfM→ops translation) → engine `apply-ops` primitive. The `apply-ops` primitive is the ONE thing that must NOT relocate (Arch §5, A-VERBS). DfM→apply-ops translation is a §3.7 design-it-twice seam (Arch B1). Sibling dep: E2-S3 (reattach), E4-S2 (CAS/receipt).

## 🧪 Testing
`TestBatchIssuesSingleAtomicApplyOps` (integration). CS §5 mocking: engine `apply-ops` mocked at HTTP boundary asserting single call; pkg/dfm real (own-family); never mock own translator.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Correctness, Concurrency lens — the batch-atomicity invariant); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A6, FR-A2); Architecture pG9s9OjqZA (A-VERBS write-primitive line, §5 batch atomicity, B1).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the `apply-ops` engine-primitives spec is the dispatch gate)
- [ ] Parent epic: E4. Intra: depends on E2-S3, E3-S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
