## 🎯 Story
As an **agent referencing a page by URL, slug, title, or natural language**, I want **wiki-api to resolve the locator to an id by composing the engine's resolver primitives (never re-implementing them)**, so that **every verb accepts flexible locators while resolution authority stays in the engine**.

**Definition of Done:** ONE named test `TestLocatorResolutionComposesEngineResolvers` (integration — url/slug/title/NL locators each resolve to the correct id via the engine resolver client, and wiki-api contains no independent resolution logic). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a url/slug/title locator, When resolved, Then it maps to the correct id via the engine resolver. *Assert: `TestLocatorResolutionComposesEngineResolvers` — engine resolver invoked, correct id.* [Source: FR-A4]
- [ ] **AC2** — Given an NL locator, When resolved, Then it composes the engine's NL resolver primitive. *Assert: NL path delegates, no local NL logic.* [Source: FR-A4]
- [ ] **AC3 (no re-implementation)** — Given the source, When inspected, Then wiki-api contains no independent locator-resolution algorithm. *Assert: resolution is delegation-only (static review).* [Source: FR-A4, Arch A-VERBS]
- [ ] **AC4 (negative)** — Given an unresolvable locator, When resolved, Then the engine's 404 semantics propagate unchanged (no existence oracle). *Assert: 404 relayed verbatim.* [Source: FR-A17]

## 🔨 Tasks
- [ ] Locator-resolution adapter over the engine resolver client (RED: `TestLocatorResolutionComposesEngineResolvers`) (AC: 1,2)
- [ ] Wire into verb dispatch (E3-S1) (AC: 1)
- [ ] Confirm no local resolution logic (AC: 3)
- [ ] Propagate 404 verbatim (AC: 4); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — Locator resolution composes engine resolvers for id|slug|auto|url. title/NL dialects are PERMANENTLY refused with a typed error (no engine resolver primitive exists) -- so the story's url/slug/title/NL set is only partially served, by design.
Tier: `internal/verb` → engine resolver client. Composes engine primitives per A-VERBS. Seam: resolver boundary. Sibling dep: E3-S1 (dispatch), E3-S3/S4 (reads use resolution), E4 (writes use resolution).

## 🧪 Testing
`TestLocatorResolutionComposesEngineResolvers` (integration). CS §5 mocking: engine resolver mocked at HTTP boundary; never mock own adapter.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Correctness, Security lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A4, FR-A17); Architecture pG9s9OjqZA (A-VERBS).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — locator/resolver contract is the dispatch gate)
- [ ] Parent epic: E3. Intra: depends on E3-S1

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
