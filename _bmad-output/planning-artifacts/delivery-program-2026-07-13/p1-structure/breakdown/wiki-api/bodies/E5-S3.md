## 🎯 Story
As a **consumer codegen-ing a typed client**, I want **the agent contract authored in orvex-studio-contracts (`openapi/wiki-api.yaml` + verb-grammar + receipt/error schemas), with wiki-api registry-generating a served `/v1/openapi.json` that CI-diffs against the authored contract and fails loud on unknown routes/fields**, so that **MCP/CLI codegen from the contracts tag (never wiki-api's served spec), keeping AGPL-originated code out of the closed codegen path, and additive-only changes preserve compatibility (NFR-A6)**.

**Definition of Done:** ONE named test `TestServedDescriptorConformsToAuthoredContract` (CI-gate — diffs the registry-generated `/v1/openapi.json` against the authored `openapi/wiki-api.yaml` and fails on any drift; asserts unknown routes/fields are rejected at runtime). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the authored `openapi/wiki-api.yaml`, When wiki-api's served descriptor is generated, Then CI diffs the two and fails on drift. *Assert: `TestServedDescriptorConformsToAuthoredContract` — 0 drift or CI fails.* [Source: FR-A14, NFR-A6]
- [ ] **AC2 (fail-loud)** — Given an unknown route or unknown body field, When requested, Then it is rejected, never dropped on the floor. *Assert: unknown route/field → typed 4xx.* [Source: FR-A14]
- [ ] **AC3 (codegen source)** — Given consumers, When they codegen, Then they use the contracts tag, not the served `/v1/openapi.json`. *Assert: served spec is a conformance input only (documented + CI-enforced).* [Source: FR-A14, NFR-A7]
- [ ] **AC4 (versioning)** — Given a breaking change, When proposed, Then it requires a new version; additive-only within a version. *Assert: additive-compat check on the authored contract.* [Source: NFR-A6]
- [ ] **AC5 (frozen vocabulary)** — Given the error vocabulary, When served, Then `VERSION_MISMATCH`, `needs_human_*`, `QUOTA_EXCEEDED`, `RATIFY_*`/`GATE_UNSATISFIED` are frozen in contracts. *Assert: error codes match the frozen set.* [Source: NFR-A6, FR-A14]

## 🔨 Tasks
- [ ] Registry-generate `/v1/openapi.json` from routes/DTOs (RED: `TestServedDescriptorConformsToAuthoredContract`) (AC: 1)
- [ ] CI drift-gate: served vs authored contract (AC: 1)
- [ ] Fail-loud unknown route/field rejection (AC: 2)
- [ ] Additive-compat + frozen error vocabulary checks (AC: 4,5)
- [ ] Document served spec is conformance-only, contracts tag is codegen source (AC: 3); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — A served /v1/openapi.json descriptor + write-contract conformance tests + an in-repo route-table drift gate are present. Full conformance against the contracts-authored external OpenAPI (orvex-studio-contracts/openapi/wiki-api.yaml) is a DISCLOSED cross-repo follow-up (CI lacks clone access) -- the descriptor is self-authored here, not yet diffed against the authored contract.
Tier: descriptor-generation + CI drift-gate (A-DESCRIPTOR). The authored contract must be FILED as a contracts delta (new FR + SEAMS.md entry) — wiki-api is absent from the contracts seam inventory today, and contracts NFR-C4 blocks cutover until pinned. This is the named Phase-0 exit blocker (2). Seam: contracts repo. Sibling dep: E4 (receipt/error schemas), all verb surfaces.

## 🧪 Testing
`TestServedDescriptorConformsToAuthoredContract` (CI-gate). CS §5 mocking: none — operates on real generated + authored specs; never mock own generator.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (§9 authored-not-generated); SE-Arch 8sYi523i4t (API-Design, Contract-Stability lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A14, NFR-A6, NFR-A7); Architecture pG9s9OjqZA (A-DESCRIPTOR, §4 contracts deltas).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the authored `openapi/wiki-api.yaml` tag + SEAMS.md entry are the dispatch gate; contracts NFR-C4 gates cutover)
- [ ] Parent epic: E5. Intra: depends on E1-S3, E4-S2; consumes all verb surfaces

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
