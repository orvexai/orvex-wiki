## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As **orvex-studio-ai executing an inline-edit from its ask loop**, I want **to land writes through wiki-api's standard block-patch chokepoint carrying the caller's delegated token — no special path, no privilege — with wiki-api owning no cited-ask and making no calls to ai**, so that **the ask topology stays acyclic (MCP→ai, ai→knowledge, ai→wiki-api, wiki-api→{engine,knowledge}) and ai's writes get no more privilege than the caller has (D-A12)**.

**Definition of Done:** ONE named test `TestAiWritePathUsesStandardChokepointNoPrivilege` (integration — an ai-origin write goes through the same FR-A6/A7 chokepoint with the caller's delegated token; wiki-api exposes no ask verb and makes no outbound call to ai). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given an ai-origin inline-edit write, When applied, Then it uses the standard block-patch chokepoint (FR-A6/A7), no special path. *Assert: `TestAiWritePathUsesStandardChokepointNoPrivilege` — same code path as agent writes.* [Source: FR-A9]
- [ ] **AC2 (no privilege)** — Given the ai write, When applied, Then it carries the caller's delegated token and gets no elevated scope. *Assert: token pass-through; a scope the caller lacks is denied.* [Source: FR-A9, FR-A16]
- [ ] **AC3 (no ask here)** — Given wiki-api, When inspected, Then it exposes no `ask` verb, assembles no cited answer, and makes no call to ai or knowledge-for-ask. *Assert: no wiki-api→ai edge; no ask assembly.* [Source: FR-A9, FR-A10, D-A12]
- [ ] **AC4 (acyclic)** — Given the dependency graph, When analyzed, Then it is acyclic with no wiki-api→ai / wiki-api→knowledge-for-ask edge. *Assert: graph check confirms acyclicity.* [Source: FR-A9, Arch A-ASK §D preserve]
- [ ] **AC5 (retired proxy)** — Given the interim engine-cited-ask proxy, When checked, Then it is absent from wiki-api (retired with the ask verb). *Assert: no ask-proxy code.* [Source: FR-A10]

## 🔨 Tasks
- [ ] Confirm ai-origin writes reuse the E4-S1/S2 chokepoint unchanged (RED: `TestAiWritePathUsesStandardChokepointNoPrivilege`) (AC: 1,2)
- [ ] Static gate: no ask verb, no wiki-api→ai edge, no ask assembly (AC: 3,4,5)
- [ ] Graph acyclicity check in CI (AC: 4); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — ai-origin writes have NO privileged path -- they traverse the same /v1 block-patch chokepoint with the caller's delegated token; the token-passthrough/no-amplification property is proven by test.
Tier: reuses `internal/blockpatch` — this story is largely a set of negative/topology assertions, not new write code. A-ASK is a load-bearing preserve-on-ratify item (Arch §D). Seam: ai is a consumer, not an upstream. Sibling dep: E4-S1/S2 (the chokepoint), E1-S4 (token pass-through).

## 🧪 Testing
`TestAiWritePathUsesStandardChokepointNoPrivilege` (integration) + graph acyclicity static check. CS §5 mocking: engine mocked at HTTP boundary; never mock own chokepoint. ai is not called — assert its absence.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Architecture-Integrity, Security lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A9, FR-A10, D-A12); Architecture pG9s9OjqZA (A-ASK, §D preserve acyclic topology).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the ask-topology decision D-A12 is the dispatch gate)
- [ ] Parent epic: E4. Intra: depends on E4-S1, E4-S2, E1-S4

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
