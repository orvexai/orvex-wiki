## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **consumer preparing to migrate onto the verb grammar**, I want **wiki-api to serve the draft `/v1` verb-grammar surface alongside the byte-compatible proxy, marked draft until contracts freezes it**, so that **the (a)→(b) migration is one deliberate client regeneration from the contracts tag, with the external contract fixed at the `/v1` freeze**.

**Definition of Done:** ONE named test `TestV1SurfaceServedAndDraftMarked` (integration — asserts `/v1` routes are mounted alongside `/api/orvex/*`, respond, and carry the draft marker until the contracts-freeze flag flips). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given both surfaces mounted, When a `/v1/*` route is requested, Then it responds and both surfaces coexist. *Assert: `/v1` + `/api/orvex/*` both routable in one process.* [Source: FR-A15, D-A9]
- [ ] **AC2** — Given the contracts freeze has not landed, When `/v1` is served, Then responses/descriptor carry the draft marker. *Assert: draft flag present until freeze-config flips.* [Source: FR-A15]
- [ ] **AC3** — Given there is no `ask` verb, When `/v1/ask` is requested, Then 404. *Assert: no ask route exists on `/v1` (D-A12).* [Source: FR-A4, D-A12]
- [ ] **AC4** — Given `search/related/retrieve` on `/v1`, When served, Then they delegate to knowledge on both surfaces. *Assert: fan-out target is the knowledge client.* [Source: FR-A15]
- [ ] **AC5 (negative)** — Given an unknown `/v1` route, When requested, Then fail-loud 404 (never dropped silently). *Assert: unknown route rejected.* [Source: FR-A14]

## 🔨 Tasks
- [ ] Mount `/v1` router alongside proxy (RED: `TestV1SurfaceServedAndDraftMarked`) (AC: 1)
- [ ] Draft-marker gated on a contracts-freeze config flag (AC: 2)
- [ ] Assert no `/v1/ask` route; wire search/related to knowledge client (AC: 3,4)
- [ ] Fail-loud unknown-route 404 (AC: 5)
- [ ] GREEN (AC: 1,2)

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — The /v1 verb-grammar surface is mounted alongside /api/*; any not-yet-composed verb answers a typed 501 X-Stub envelope (draft-marked), never a 404.
Tier: thin handler → verb-dispatch domain package (fleshed out in E3/E4). Phase-0 surface (b) per Arch A-STRANGLER / §3. Seam: this is where the fixed external contract will crystallize at the `/v1` freeze. Sibling dep: E3 fills the verbs, E5-S3 the descriptor.

## 🧪 Testing
`TestV1SurfaceServedAndDraftMarked` (integration). CS §5 mocking: knowledge is external → mock at HTTP boundary; never mock own router.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (API-Design lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A15, FR-A4, D-A12); Architecture pG9s9OjqZA (§3 surface b, A-STRANGLER).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the `/v1` grammar artifact tag is the dispatch gate)
- [ ] Parent epic: E1. Intra: depends on E1-S1

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
