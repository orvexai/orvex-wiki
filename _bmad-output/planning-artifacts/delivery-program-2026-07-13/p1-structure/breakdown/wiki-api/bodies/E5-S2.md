## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent reading a large page**, I want **body-offload: large bodies returned as handles / `resource_link` envelopes plus token estimates instead of inline content, with the handle dereferencing through the same ACL-enforced read path**, so that **the ≤ ~1k-token budget holds on big pages and offload introduces no ACL bypass or existence oracle**.

**Definition of Done:** ONE named test `TestBodyOffloadHandleDerefsThroughAcl` (integration — a large body returns a `resource_link` + token estimate; dereferencing the handle calls the engine ACL-enforced read and a caller without access is denied identically). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a large body, When read, Then a `resource_link` handle + token estimate return instead of inline content. *Assert: `TestBodyOffloadHandleDerefsThroughAcl` — envelope + estimate, no inline body.* [Source: FR-A13]
- [ ] **AC2 (ACL-enforced deref)** — Given a handle, When dereferenced, Then it rides the same ACL-enforced engine read path. *Assert: deref invokes engine read honoring ACL.* [Source: FR-A13, FR-A17]
- [ ] **AC3 (no oracle)** — Given a handle for a resource the caller cannot access, When dereferenced, Then the same denial as a direct read (no existence oracle). *Assert: forbidden == nonexistent shape.* [Source: FR-A17]
- [ ] **AC4 (budget)** — Given a big page, When read with offload, Then the response stays within the ≤ ~1k-token budget. *Assert: token estimate + envelope size within budget.* [Source: FR-A13, G5]
- [ ] **AC5 (negative)** — Given an expired/invalid handle, When dereferenced, Then a typed error, never a raw content leak. *Assert: invalid handle → typed 4xx.* [Source: FR-A13]

## 🔨 Tasks
- [ ] Body-offload envelope + token-estimate emission over a size threshold (RED: `TestBodyOffloadHandleDerefsThroughAcl`) (AC: 1,4)
- [ ] Handle deref through the engine ACL-enforced read (AC: 2,3)
- [ ] Typed error on invalid handle (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Body-offload emits resource_link handles + token estimates above a budget; the offloaded Blocks read derefs through the ACL-gated read route. tested.
Tier: shaping layer (E5-S1) + `internal/verb` read path. Body-offload is FR-A13; the ACL-enforced deref is the security-critical part (interacts with E1-S5 no-existence-oracle). Seam: engine read boundary. Sibling dep: E5-S1 (shaping), E3-S3 (read ladder token_estimate).

## 🧪 Testing
`TestBodyOffloadHandleDerefsThroughAcl` (integration). CS §5 mocking: engine mocked at HTTP boundary returning ACL 403/content; never mock own offload logic.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Security, Performance lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A13, FR-A17); Architecture pG9s9OjqZA (A-SHAPE, §D preserve FR-A17).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — `resource_link` envelope schema is the dispatch gate)
- [ ] Parent epic: E5. Intra: depends on E5-S1, E3-S3; interacts with E1-S5

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
