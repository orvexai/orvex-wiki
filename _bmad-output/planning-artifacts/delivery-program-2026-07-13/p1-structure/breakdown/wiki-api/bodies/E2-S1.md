## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **wiki-api / CLI embedder**, I want **a clean-room Go `DfM → ProseMirror-JSON` converter in `orvex-studio-lib/pkg/dfm`, written from the documented PM schema + the engine's block-schema catalog + contracts golden fixtures**, so that **agents author in DfM and the result is structurally valid ProseMirror without importing or porting the AGPL `@docmost/editor-ext`**.

**Definition of Done:** ONE named test `TestDfmToPmMatchesGoldenFixtures` (package unit — converts each contracts golden DfM fixture and asserts the emitted PM-JSON equals the fixture's expected PM tree). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a golden DfM fixture, When converted, Then the PM-JSON equals the expected tree. *Assert: `TestDfmToPmMatchesGoldenFixtures` deep-equals over the fixture corpus.* [Source: FR-A1]
- [ ] **AC2 (clean-room)** — Given the package source, When the no-AGPL guard runs, Then it imports no `@docmost/*` and was built from schema/catalog. *Assert: no-AGPL-import guard green; provenance note references PM schema + `GET /api/schemas/blocks`.* [Source: FR-A1, NFR-A7, D-A11]
- [ ] **AC3** — Given every block type in the engine catalog, When converted, Then each has a handler (no fall-through). *Assert: catalog-coverage test — 0 uncovered block types.* [Source: FR-A1, NFR-A4]
- [ ] **AC4 (negative)** — Given malformed/unknown DfM, When converted, Then a typed error, never silent drop. *Assert: unknown directive → typed error.* [Source: FR-A14 fail-loud]
- [ ] **AC5** — Given the package, When embedded, Then it exposes a pure in-process API (no HTTP). *Assert: no network calls in the convert path.* [Source: FR-A3]

## 🔨 Tasks
- [ ] Parse DfM (markdown + `:::` colon-directives) into an AST (RED: `TestDfmToPmMatchesGoldenFixtures`) (AC: 1,4)
- [ ] Map AST → PM nodes per the block-schema catalog; handler per block type (AC: 1,3)
- [ ] Typed errors on unknown directives (AC: 4)
- [ ] Provenance note + no-AGPL guard clean (AC: 2); pure in-process API (AC: 5)
- [ ] GREEN over the golden corpus (AC: 1,3)

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — DfM->PM conversion composed in-process from orvex-studio-lib/pkg/dfm (the clean-room serializer lives in the lib, re-exported here as type aliases + New()); wiki-api never reimplements or mocks it.
Tier: external shared library `orvex-studio-lib/pkg/dfm` (not an internal wiki-api tier — Arch B1 names the serializer as the external dependency). Clean-room per D-A11 / A-SERIALIZER. Seam: this is the closed-side single implementation embedded by wiki-api + CLI. Sibling dep: E2-S2 (reverse), E2-S3 (opaque), E2-S5 (parity gate).

## 🧪 Testing
`TestDfmToPmMatchesGoldenFixtures` (package unit) + catalog-coverage test. CS §5 mocking: the block-schema catalog is fetched at build time into fixtures — tests run against fixtures, no live engine; never mock own package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Correctness, Supply-Chain/AGPL lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A1, FR-A3, NFR-A7, D-A11); Architecture pG9s9OjqZA (A-SERIALIZER, B1, §5 double-impl-drift).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — golden fixtures + block-schema catalog version are the dispatch gate)
- [ ] Parent epic: E2. Intra: none (first serializer story)

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
