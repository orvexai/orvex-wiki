## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent editing a page containing diagrams and embeds**, I want **opaque nodes (Excalidraw, drawio, mentions, embeds) rendered as typed handles `{block_id, type, summary}` and reattached byte-identically from the `ifVersion` base on write**, so that **non-textual content is never serialized to editable text and survives round-trips losslessly (G3)**.

**Definition of Done:** ONE named test `TestOpaqueNodeReattachByteIdentical` (package unit — converts a page with opaque nodes to DfM handles, edits surrounding text, reattaches from the ifVersion base, asserts the opaque node bytes are unchanged). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given an opaque node, When converted to DfM, Then it renders only as a typed handle `:::dfm-opaque type=<t> id=<uuid>:::`, never editable text. *Assert: no opaque payload in the DfM text stream.* [Source: FR-A2]
- [ ] **AC2 (reattach)** — Given a write with an ifVersion base, When applied, Then opaque nodes are reattached byte-identically from that base. *Assert: `TestOpaqueNodeReattachByteIdentical` byte-equality on opaque payloads.* [Source: FR-A2, FR-A6]
- [ ] **AC3 (no raw PM)** — Given any boundary crossing, When data flows either direction, Then raw ProseMirror JSON never crosses the agent boundary. *Assert: write formats limited to `dfm | json` (structured block form); `format:"markdown"` rejected.* [Source: FR-A2]
- [ ] **AC4 (negative)** — Given a handle whose block_id is absent from the ifVersion base, When reattach is attempted, Then typed error, never fabricated content. *Assert: missing-handle → typed error.* [Source: FR-A2, FR-A7]
- [ ] **AC5** — Given all opaque node types in the corpus, When round-tripped, Then all reattach identically. *Assert: golden-corpus opaque coverage 100%.* [Source: NFR-A4]

## 🔨 Tasks
- [ ] Handle-emit for opaque node types (RED: `TestOpaqueNodeReattachByteIdentical`) (AC: 1)
- [ ] Reattach-from-ifVersion-base transform (AC: 2,4)
- [ ] Reject raw markdown write path; allow dfm|json only (AC: 3)
- [ ] Golden-corpus opaque coverage (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Opaque-node typed handles round-trip byte-identically (excalidraw exemplar tested); reattach-from-base logic lives in orvex-studio-lib/pkg/dfm and is exercised via the fidelity tests.
Tier: external `orvex-studio-lib/pkg/dfm` (opaque discipline) + used by the E4 write chokepoint for reattach. Reattach done in wiki-api is read-base→transform→write across the network; CAS makes the widened race window safe (E4-S1/S2). Seam: serializer ↔ write chokepoint.

## 🧪 Testing
`TestOpaqueNodeReattachByteIdentical` (package unit) + corpus coverage. CS §5 mocking: fixtures only; never mock own package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Data-Fidelity, Correctness lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A2, FR-A6, FR-A7, NFR-A4); Architecture pG9s9OjqZA (A-SERIALIZER, A-VERBS).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — opaque-node handle schema is the dispatch gate)
- [ ] Parent epic: E2. Intra: depends on E2-S1, E2-S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
