## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **release engineer**, I want **blocking CI bootstrap gates — `go test` + `gofmt` + `golangci-lint`, the handler-thinness / store-import-restriction / projection-determinism static gates, the no-AGPL-import guard, and the engine-vs-wiki-api + golden-corpus parity job**, so that **a convenience `@docmost/*` import can never land and the Tekton pipeline stops building/pushing images without running tests**.

**Definition of Done:** ONE named test `TestNoAgplImportGuardFailsOnDocmostImport` (CI-gate layer — a fixture introducing a `@docmost/*`/AGPL import causes the guard to exit non-zero). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the pipeline, When it runs, Then `go test`, `gofmt`, and `golangci-lint` are CI-pinned blocking steps. *Assert: pipeline fails if any fails; not build-and-push-only.* [Source: Arch B3, CS §0/§4]
- [ ] **AC2 (security)** — Given a fixture importing `@docmost/*` or AGPL code, When the no-AGPL guard runs, Then CI fails. *Assert: `TestNoAgplImportGuardFailsOnDocmostImport` exits non-zero on the fixture, 0 on clean tree.* [Source: NFR-A7, Arch B3]
- [ ] **AC3** — Given the static gates, When run, Then handler-thinness, store-import-restriction, and projection-determinism are enforced. *Assert: each static gate present and blocking.* [Source: Arch B3, CS §6]
- [ ] **AC4** — Given the parity job, When run, Then engine-vs-wiki-api black-box + golden-corpus parity are blocking gates. *Assert: parity job wired as a required check.* [Source: NFR-A4, Arch B3]
- [ ] **AC5 (negative)** — Given a red test, When the pipeline runs, Then no image is pushed. *Assert: push step gated behind green test stage.* [Source: Arch B3]

## 🔨 Tasks
- [ ] Add go test/gofmt/golangci-lint blocking stages to the Tekton pipeline (AC: 1,5)
- [ ] Implement no-AGPL-import guard (RED: `TestNoAgplImportGuardFailsOnDocmostImport`) (AC: 2)
- [ ] Wire handler-thinness / store-import / projection-determinism static gates (AC: 3)
- [ ] Wire engine-vs-wiki-api + golden-corpus parity job as required check (AC: 4)
- [ ] GREEN; verify push is gated behind green tests (AC: 5)

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Blocking CI gates present: gofmt/golangci/gosec/govulncheck/test + a real no-AGPL-import guard + handler-thinness + route-table drift. The DfM golden-corpus 'parity job' is the one gate not in this repo (see E2-S5).
Tier: CI/CD (build pipeline), not runtime. Fixes the honest-state gap that `tekton/wikiapi-build-pipeline.yaml` builds+pushes without tests (Arch B3, C). The no-AGPL guard is filed as a contracts family NFR (NFR-A7) but enforced here per-repo. Sibling dep: E2-S5 supplies the golden-corpus job content.

## 🧪 Testing
`TestNoAgplImportGuardFailsOnDocmostImport` (CI-gate layer, fixture-driven). CS §5 mocking: none — operates on the real source tree. Never mock own packages.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (bootstrap gates §0/§4, six-tier static gates §6); SE-Arch 8sYi523i4t (Operational-Excellence, Supply-Chain lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (NFR-A7, NFR-A4); Architecture pG9s9OjqZA (B3, C honest-state, §5 risks).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the family AGPL-import guard spec + parity-corpus tag are the dispatch gate)
- [ ] Parent epic: E1. Intra: depends on E1-S1; parity content from E2-S5

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
