## 🎯 Story
As a **performance owner**, I want **benchmarks asserting the filed latency budgets — facade overhead p95 ≤ 20 ms in-cell (NFR-A1), bulk per-item overhead ≤ 20 ms (NFR-A1a), warm-cache artifact read ≤ 20 ms (NFR-A1b), rebuild-on-miss ≤ 150 ms (NFR-A2a) — plus a content-type-aware deadline bounding a hung non-SSE upstream body**, so that **the NFR AC-citations assert against real filed thresholds and a stalled engine cannot pin connections**.

**Definition of Done:** ONE named test `TestLatencyBudgetsBenchmarked` (benchmark/integration — asserts facade p95 ≤ 20 ms, bulk per-item p95 ≤ 20 ms, warm-cache read p95 ≤ 20 ms, rebuild-on-miss p95 ≤ 150 ms, and a non-SSE hung upstream body is bounded by the idle-read deadline). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given proxy paths, When benchmarked, Then facade overhead p95 ≤ 20 ms in-cell. *Assert: `TestLatencyBudgetsBenchmarked` — facade p95 within budget (NFR-A1).* [Source: NFR-A1]
- [ ] **AC2** — Given the bulk fan-out (`POST /v1/pages/bulk`), When benchmarked, Then per-item composition overhead p95 ≤ 20 ms. *Assert: `internal/bulkops` per-item p95 within budget (NFR-A1a, DRAFT/pending-ratify).* [Source: NFR-A1a]
- [ ] **AC3** — Given a warm-cache artifact read, When benchmarked, Then p95 ≤ 20 ms. *Assert: warm read within budget (NFR-A1b, DRAFT/pending-ratify — provisional pending live-Redis benchmark).* [Source: NFR-A1b]
- [ ] **AC4** — Given a cold rebuild-on-miss (deterministic build, no engine hop), When benchmarked, Then p95 ≤ 150 ms and a cache miss/loss is a pure performance event. *Assert: rebuild p95 within budget (NFR-A2a).* [Source: NFR-A2a, NFR-A2]
- [ ] **AC5 (deadline)** — Given a hung non-SSE upstream body, When read, Then a content-type-aware idle-read deadline bounds it (SSE exempt). *Assert: non-SSE deadline fires; connection not pinned (review B4).* [Source: Arch B4, CS §10]

## 🔨 Tasks
- [ ] Facade-overhead benchmark asserting NFR-A1 (RED: `TestLatencyBudgetsBenchmarked`) (AC: 1)
- [ ] `internal/bulkops` per-item benchmark asserting NFR-A1a (AC: 2)
- [ ] Warm-cache read + rebuild-on-miss benchmarks asserting NFR-A1b/NFR-A2a (AC: 3,4)
- [ ] Content-type-aware idle-read deadline for non-SSE bodies (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — Bulk per-item latency budget + the non-SSE upstream 30s deadline are present and asserted. The full facade/warm-cache/rebuild benchmark suite the story names is not fully evidenced (no warm-cache/rebuild benchmark).
Tier: benchmark tests across `internal/pageblocks`, `internal/bulkops`, `internal/cache`, `internal/onboard` + router deadline (`cmd/wikiapi/router.go`). NFR-A1a/A1b/A2a are DRAFT amendments (pending-ratify) — cite the filed ids, do not invent numbers; the values are provisional until live-Redis benchmarks land. Seam: request deadline vs SSE streaming (review B4). Sibling dep: E7-S2 (SLIs), E7-S1 (cache).

## 🧪 Testing
`TestLatencyBudgetsBenchmarked` (Go benchmarks + integration). CS §5 mocking: upstreams/Redis via in-cell fakes for the facade-overhead delta; never mock own benchmarked packages.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (§10 timeouts); SE-Arch 8sYi523i4t (Performance, Reliability lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (NFR-A1, NFR-A1a, NFR-A1b, NFR-A2, NFR-A2a); Architecture pG9s9OjqZA (B4, §5 extra-hop).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — ratification of the DRAFT NFR thresholds is the dispatch gate)
- [ ] Parent epic: E7. Intra: depends on E1-S2 (proxy), E7-S1 (cache), E7-S2 (SLIs)

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
