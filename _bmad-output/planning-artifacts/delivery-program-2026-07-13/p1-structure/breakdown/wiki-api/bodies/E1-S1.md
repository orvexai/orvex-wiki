## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **platform operator standing up the wiki's agent tier**, I want **a Go service skeleton laid out on the CS §6 six-tier model with config (CELL_ID, CLUSTER_NAME, upstream base URLs) and a `/healthz` endpoint**, so that **every later composition seam has a thin-handler → workflow → domain → event → cache spine to slot into and the pod is kill-any-time safe**.

**Definition of Done:** ONE named test `TestHealthzEchoesCellAndCluster` (integration layer — boots the router, GETs `/healthz`, asserts the JSON body echoes both `cell` (CELL_ID) and `cluster` (CLUSTER_NAME) as distinct fields and returns 200). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the binary boots with CELL_ID and CLUSTER_NAME set, When `GET /healthz`, Then it returns 200 with both fields. *Assert: `resp.cell == $CELL_ID && resp.cluster == $CLUSTER_NAME`.* [Source: Arch A3 review finding + FR-A19]
- [ ] **AC2** — Given the repo tree, When the tier-layout lint runs, Then `cmd/wikiapi` holds only a thin handler and no domain logic imports the store tier (there is none — stateless). *Assert: handler-thinness static gate exits 0; no `internal/*store*` package exists.* [Source: Arch B1 + CS §6]
- [ ] **AC3** — Given upstream base URLs are fixed config (engine/knowledge/identity), When any is caller-supplied, Then it is ignored. *Assert: config loads targets from env only; no request field influences fetch target.* [Source: NFR-A3]
- [ ] **AC4 (negative)** — Given CLUSTER_NAME is unset, When the binary boots, Then it fails loud at startup (no silent default). *Assert: process exits non-zero with a named missing-config error.* [Source: Arch A3 + Memory "no fallbacks — hard cuts"]
- [ ] **AC5** — Given any pod is killed mid-request, When traffic re-routes, Then no correctness impact. *Assert: no in-process durable state; `TestStatelessKillAny` holds.* [Source: NFR-A2]

## 🔨 Tasks
- [ ] Scaffold `cmd/wikiapi` thin handler + `internal/config` (RED: `TestHealthzEchoesCellAndCluster`) (AC: 1,4)
- [ ] Add CLUSTER_NAME + CELL_ID config fields, fail-loud on missing (AC: 1,4)
- [ ] Lay down empty `internal/{workflow,verb,cache,event}` packages per six-tier map (AC: 2)
- [ ] Fixed-config upstream base URLs; reject caller-influenced targets (AC: 3)
- [ ] GREEN + statelessness assertion (AC: 5)

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Skeleton, six-tier internal/* layout, config load, and /healthz echoing cell+cluster are present and tested. GAP: AC4 fail-loud on missing CLUSTER_NAME is NOT implemented (config.Load defaults ClusterName to ""); router.go's own /healthz echoes only cell (server.Handler()'s echoes both).
Tier placement: thin handler in `cmd/wikiapi`; `internal/workflow` liveness-only sequencing; pure DI'd domain packages below; `internal/cache` (Redis) and `internal/event` (evict consumer) added in E7. No store tier — stateless (Arch B1). This is the foundation every other epic imports. Seam crossed: process boundary / config.

## 🧪 Testing
`TestHealthzEchoesCellAndCluster` (integration) + `TestStatelessKillAny` (unit). CS §5 mocking: mock nothing own; config from env fixtures. No upstream calls in this story.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (six-tier §6, fail-loud config §0/§10, honest-state §11); SE-Arch 8sYi523i4t Operational-Excellence lens; cell-lint JGAUQRsw2g (CELL_ID + CLUSTER_NAME day-1 contract).

## 🔗 References
PRD 8jAiCBifDW (FR-A19, NFR-A2); Architecture pG9s9OjqZA (§1 topology, A3, B1).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the contract TAG is the dispatch gate)
- [ ] Parent epic: E1. Intra: none (first story; all E1/E2/E3 stories depend on this)

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
