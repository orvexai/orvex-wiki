## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **security reviewer**, I want **wiki-api to make no authorization decision — never evaluate, cache, or override an allow/deny, only propagate upstream 401/403/404 semantics unchanged**, so that **ACL enforcement stays the engine's CASL primitive and knowledge's egress chokepoint, and response shaping introduces no existence oracles**.

**Definition of Done:** ONE named test `TestNoExistenceOracleFromShaping` (integration — a page the caller cannot see returns the same 404/403 shape whether or not it exists; shaping/projection never leaks existence). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given upstream returns 401/403/404, When wiki-api relays, Then the status/semantics are unchanged. *Assert: no remap of upstream auth statuses.* [Source: FR-A17]
- [ ] **AC2 (security)** — Given a resource the caller cannot access, When requested via any verb, Then the response does not distinguish "forbidden" from "nonexistent" beyond what the engine already exposes. *Assert: `TestNoExistenceOracleFromShaping` — no new oracle from projection/handles.* [Source: FR-A17, Arch A-TOKEN]
- [ ] **AC3 (negative)** — Given any handler, When it would cache or compute an allow/deny locally, Then it does not exist. *Assert: no local ACL evaluation; grep/static gate clean.* [Source: FR-A17, D-A10]
- [ ] **AC4** — Given a body-offload handle, When dereferenced, Then it rides the same ACL-enforced engine read path (no bypass). *Assert: handle deref calls the engine read, honoring its ACL.* [Source: FR-A13, FR-A17]

## 🔨 Tasks
- [ ] Status-passthrough policy for 401/403/404 across all verbs (RED: `TestNoExistenceOracleFromShaping`) (AC: 1,2)
- [ ] Static gate: no local ACL evaluation/caching (AC: 3)
- [ ] Handle-deref routes through engine ACL-enforced read (AC: 4)
- [ ] GREEN with an unauthorized-access oracle probe (AC: 2)

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — 401/403/404 propagate verbatim from engine/knowledge via a typed mapping; no existence-oracle laundering into a generic 5xx. tested.
Tier: cross-cutting policy in the handler/shaping layer. This is a load-bearing security invariant (Arch §D preserve-on-ratify: FR-A17). Sibling dep: interacts with E5 shaping/body-offload and E1-S4 token pass-through.

## 🧪 Testing
`TestNoExistenceOracleFromShaping` (integration). CS §5 mocking: engine/knowledge mocked at HTTP boundary returning real 403/404 shapes; never mock own policy layer.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Security lens — existence oracles, never-cache-allow/deny); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A17, FR-A13, D-A10); Architecture pG9s9OjqZA (A-TOKEN, A-STATE, §D preserve).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — error/status vocabulary is the dispatch gate)
- [ ] Parent epic: E1. Intra: depends on E1-S1; interacts with E5-S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
