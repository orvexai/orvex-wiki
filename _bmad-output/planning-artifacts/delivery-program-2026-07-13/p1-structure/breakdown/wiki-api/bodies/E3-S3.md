## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **context-budget-conscious agent**, I want **the read ladder `info → outline (with token_estimate, no body) → blocks/:id?format=dfm` plus ranged sub-page DfM reads**, so that **I can navigate a page cheaply and pull only the blocks I need, keeping the ≤ ~1k-token budget (G5)**.

**Definition of Done:** ONE named test `TestReadLadderInfoOutlineBlocks` (integration — `info` returns metadata, `outline` returns structure with token_estimate and no body, `blocks/:id?format=dfm` returns DfM, and a ranged read returns only the requested range). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a page, When `info` is requested, Then metadata (incl. `meta.version`) returns without body. *Assert: `TestReadLadderInfoOutlineBlocks` — info has no body.* [Source: FR-A5]
- [ ] **AC2** — Given `outline`, When requested, Then structure + `token_estimate` returns with no body. *Assert: outline carries token_estimate, no block content.* [Source: FR-A5, FR-A13]
- [ ] **AC3** — Given `blocks/:id?format=dfm`, When requested, Then DfM for that block returns via pkg/dfm. *Assert: block DfM equals serializer output.* [Source: FR-A5, FR-A1]
- [ ] **AC4** — Given a ranged sub-page read (R2), When requested, Then only the requested range returns. *Assert: range bounds honored.* [Source: FR-A5]
- [ ] **AC5 (negative)** — Given a block id absent on the page, When requested, Then engine 404 semantics propagate. *Assert: 404 relayed.* [Source: FR-A17]

## 🔨 Tasks
- [ ] `info`/`outline`/`blocks/:id` read-ladder handlers over engine primitives + pkg/dfm (RED: `TestReadLadderInfoOutlineBlocks`) (AC: 1,2,3)
- [ ] token_estimate on outline (AC: 2)
- [ ] Ranged sub-page DfM reads (AC: 4)
- [ ] 404 propagation (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — info->outline(token_estimate)->blocks/{id}?format=dfm read ladder plus ranged block reads, all real+tested.
Tier: `internal/verb` read-ladder assembly → engine primitives + pkg/dfm. Read-ladder assembly is composed here (A-VERBS). Seam: engine read primitives. Sibling dep: E2 (pkg/dfm), E3-S2 (locator), E5 (shaping/token estimates).

## 🧪 Testing
`TestReadLadderInfoOutlineBlocks` (integration). CS §5 mocking: engine mocked at HTTP boundary; pkg/dfm called real (own-family); never mock own assembly.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (API-Design, Performance lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A5, FR-A1, FR-A13, FR-A17); Architecture pG9s9OjqZA (A-VERBS §3).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — read-ladder response schemas are the dispatch gate)
- [ ] Parent epic: E3. Intra: depends on E3-S1, E3-S2, E2-S1/S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
