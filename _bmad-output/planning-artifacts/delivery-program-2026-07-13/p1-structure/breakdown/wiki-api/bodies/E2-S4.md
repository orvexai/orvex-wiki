## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **future agent or ad-hoc tool that doesn't embed pkg/dfm**, I want **wiki-api to serve `/v1/convert/dfm-to-json`, `/v1/convert/json-to-dfm`, and `/v1/convert/fidelity-check` backed in-process by pkg/dfm**, so that **conversion is available over HTTP for non-embedders while wiki-api and the CLI convert in-process with no serializer HTTP hop on the hot path (NFR-A1)**.

**Definition of Done:** ONE named test `TestConvertEndpointsBackedInProcess` (integration — POST DfM→JSON and JSON→DfM return correct conversions and the handler calls pkg/dfm in-process, not an engine `/convert` hop). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `POST /v1/convert/dfm-to-json`, When called, Then it returns valid PM-JSON via in-process pkg/dfm. *Assert: `TestConvertEndpointsBackedInProcess` — result matches E2-S1 conversion; no outbound convert HTTP.* [Source: FR-A3]
- [ ] **AC2** — Given `POST /v1/convert/json-to-dfm`, When called, Then it returns DfM via the shipped-renderer-parity path. *Assert: output matches E2-S2.* [Source: FR-A3]
- [ ] **AC3** — Given `POST /v1/convert/fidelity-check`, When called, Then it reports round-trip fidelity. *Assert: fidelity result reflects the golden-corpus round-trip.* [Source: FR-A3, NFR-A4]
- [ ] **AC4 (latency)** — Given a typical page, When converted via the endpoint, Then conversion p95 < 150 ms and no serializer HTTP hop appears on the embedder edit hot path. *Assert: benchmark within NFR-A1 conversion budget.* [Source: NFR-A1, FR-A3]
- [ ] **AC5 (negative)** — Given malformed input, When posted, Then a typed 4xx error, never a partial conversion. *Assert: fail-loud on bad input.* [Source: FR-A14]

## 🔨 Tasks
- [ ] `/v1/convert/*` handlers calling pkg/dfm in-process (RED: `TestConvertEndpointsBackedInProcess`) (AC: 1,2)
- [ ] `/v1/convert/fidelity-check` over the round-trip harness (AC: 3)
- [ ] Confirm embedders (wiki-api, CLI) convert in-process (no `/convert` hop on hot path) (AC: 4)
- [ ] Typed 4xx on malformed input (AC: 5); conversion benchmark (AC: 4); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — /v1/convert/to-prosemirror|to-dfm|fidelity are real in-process handlers backed by the embedded serializer + tests (the older /v1/convert/dfm-to-json draft paths remain typed 501 stubs).
Tier: thin handler → in-process pkg/dfm call. Re-homes the semantics of the engine's `orvex/markdown` fidelity/convert surface. D-A6's server-endpoint delegation is superseded by D-A11 (in-process). Seam: HTTP convert surface for non-embedders only.

## 🧪 Testing
`TestConvertEndpointsBackedInProcess` (integration) + conversion benchmark. CS §5 mocking: pkg/dfm is own-family — do NOT mock it; call it real. No engine dependency on this path.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (API-Design, Performance lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A3, NFR-A1, NFR-A4); Architecture pG9s9OjqZA (A-SERIALIZER §3 convert endpoints).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — convert-endpoint DTO schemas are the dispatch gate)
- [ ] Parent epic: E2. Intra: depends on E2-S1, E2-S2, E2-S3

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
