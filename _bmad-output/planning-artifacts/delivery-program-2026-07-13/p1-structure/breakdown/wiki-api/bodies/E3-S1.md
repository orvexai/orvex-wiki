## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent client**, I want **the resource-agnostic verb grammar `search / get / save / edit / list` over `{resource_type, locator}` dispatched inside wiki-api (`search/related`→knowledge, everything else→engine primitives), with no `ask` verb**, so that **I integrate against one stable surface and ask orvex-studio-ai directly (D-A12)**.

**Definition of Done:** ONE named test `TestVerbDispatchRoutesByResourceAndVerb` (integration — each verb routes to the correct upstream; `resource_type:"wiki"` works, a second resource_type is accepted-but-unbuilt, and there is no ask route). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `get/save/edit/list` on `resource_type:"wiki"`, When dispatched, Then they route to engine primitives. *Assert: `TestVerbDispatchRoutesByResourceAndVerb` — engine client invoked.* [Source: FR-A4]
- [ ] **AC2** — Given `search`, When dispatched, Then it routes to knowledge. *Assert: knowledge client invoked for search.* [Source: FR-A4, FR-A11]
- [ ] **AC3 (no ask)** — Given any request, When it targets an `ask` verb, Then 404/unsupported. *Assert: no ask verb exists (D-A12).* [Source: FR-A4, D-A12]
- [ ] **AC4** — Given `resource_type` other than `"wiki"`, When requested, Then a typed "unsupported resource_type" error (grammar keeps shape, only wiki built). *Assert: second resource_type → typed 4xx, not a crash.* [Source: FR-A4, D-A5]
- [ ] **AC5 (negative)** — Given an unknown verb, When dispatched, Then fail-loud 404. *Assert: unknown verb rejected.* [Source: FR-A14]

## 🔨 Tasks
- [ ] Verb-dispatch domain package keyed on `{resource_type, verb}` (RED: `TestVerbDispatchRoutesByResourceAndVerb`) (AC: 1,2)
- [ ] Route search/related→knowledge, rest→engine (AC: 1,2)
- [ ] Assert no ask verb; typed error for non-wiki resource_type (AC: 3,4)
- [ ] Fail-loud unknown verb (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Verb dispatch over {resource_type,locator} for search/get/save/edit/list is real+tested; no ask verb (by design).
Tier: thin handler → `internal/verb` domain dispatch → engine/knowledge clients. Grammar artifact authored in contracts (E5-S3). Seam: verb dispatch is the §3.7 design-it-twice seam (Arch B1). Sibling dep: E3-S2 (locator resolution), E4 (write verbs), E5 (shaping).

## 🧪 Testing
`TestVerbDispatchRoutesByResourceAndVerb` (integration). CS §5 mocking: engine + knowledge mocked at HTTP boundary; never mock own dispatch package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (API-Design, Correctness lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A4, FR-A11, D-A5, D-A12); Architecture pG9s9OjqZA (A-VERBS, B1).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the verb-grammar artifact tag is the dispatch gate)
- [ ] Parent epic: E3. Intra: depends on E1-S1/S3

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
