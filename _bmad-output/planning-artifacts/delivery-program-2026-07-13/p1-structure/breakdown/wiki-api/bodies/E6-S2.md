## 🎯 Story
As a **wiki-first spec-gate consumer (CLI/MCP)**, I want **wiki-api to own the spec-gate check returning `{satisfied, token, reason}` and transport engine-minted CONFIRM/RATIFY tokens verbatim, with the work-item trigger de-Linearized (the `contentHasLinearIssue` PM-node scan deleted, only the text-only story-id path kept)**, so that **wiki-first enforcement re-homes from the fork (D-S8) without minting here and without the dropped Linear machinery (D-S11)**.

**Definition of Done:** ONE named test `TestSpecGateCheckDeLinearizedTextOnlyStoryId` (integration — the spec-gate check returns `{satisfied,token,reason}`, transports the engine token verbatim, triggers ONLY on a text-only story-id match, and contains zero `contentHasLinearIssue`/linear_* code). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a page, When the spec-gate check runs, Then it returns `{satisfied, token, reason}`. *Assert: `TestSpecGateCheckDeLinearizedTextOnlyStoryId` — shape correct.* [Source: FR-A21]
- [ ] **AC2 (transport-only)** — Given a CONFIRM/RATIFY token, When surfaced, Then it is the engine-minted token transported verbatim (minting + enforcement stay engine-side). *Assert: no minting in wiki-api.* [Source: FR-A21, D-A8]
- [ ] **AC3 (de-Linearized)** — Given the trigger, When it fires, Then it uses ONLY the text-only story-id confirmation path; the `contentHasLinearIssue` scan is deleted. *Assert: zero linear_* / contentHasLinearIssue code (CI grep-gate).* [Source: FR-A21, D-S11]
- [ ] **AC4 (no silent drop)** — Given a page that previously used the Linear trigger, When re-homed, Then confirmation is not silently dropped — the text-only story-id path is the sole trigger. *Assert: story-id path fires on such pages.* [Source: FR-A21, Arch §5 de-Linearize risk]
- [ ] **AC5 (force surfaces)** — Given `ratify-gate`/`ratify-token`/`confirm-token`, When called, Then they are served verbatim (shared with E4-S3), closing CLI gap G5. *Assert: force codes propagate.* [Source: FR-A21]

## 🔨 Tasks
- [ ] Clean-room re-home spec-gate check `{satisfied,token,reason}` (RED: `TestSpecGateCheckDeLinearizedTextOnlyStoryId`) (AC: 1)
- [ ] Verbatim CONFIRM/RATIFY transport (AC: 2,5)
- [ ] Delete contentHasLinearIssue scan; text-only story-id path only + CI grep-gate (AC: 3,4)
- [ ] GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — The spec-gate package, wiring, route, and the {satisfied,token,reason} result shape exist, but the core is an explicit stub: Check()/GateState() return ErrNotImplemented and v1SpecGate/v1RatifyGate answer a typed 501. The de-Linearized text-only story-id gate is a documented TODO, not implemented.
Tier: `internal/specgate` domain → engine ratify chokepoint (transport-only). Re-homed clean-room per A-GOVERNANCE / D-S8 (Phase-2), de-Linearized per D-S11. Shares token transport with E4-S3. The doc-spec-gate skill chain is the current ad-hoc equivalent. Seam: engine ratify chokepoint.

## 🧪 Testing
`TestSpecGateCheckDeLinearizedTextOnlyStoryId` (integration) + zero-linear grep-gate. CS §5 mocking: engine mocked at HTTP boundary emitting tokens/gate codes; never mock own spec-gate package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Security, Correctness lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A21, D-S8, D-S11); Architecture pG9s9OjqZA (A-GOVERNANCE, §5 de-Linearize risk).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the D-S8 spec-gate contract delta is the dispatch gate)
- [ ] Parent epic: E6. Intra: depends on E4-S3 (token transport); Phase-2 re-homing

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
