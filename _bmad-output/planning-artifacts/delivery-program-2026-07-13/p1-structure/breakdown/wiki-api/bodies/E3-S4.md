## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent navigating the wiki structure**, I want **structural nav fronted through the grammar — space tree, backlinks, breadcrumbs — all delegating to engine primitives**, so that **I can traverse relationships and hierarchy through the same stable surface as reads and writes**.

**Definition of Done:** ONE named test `TestStructuralNavDelegatesToEngine` (integration — `/tree`, `/backlinks`, `/breadcrumbs` each return the engine's structural data, shaped through the grammar). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a space, When `/tree` is requested, Then the space tree returns via the engine nav primitive. *Assert: `TestStructuralNavDelegatesToEngine` — engine nav invoked.* [Source: FR-A5]
- [ ] **AC2** — Given a page, When `/backlinks` is requested, Then inbound links return. *Assert: backlinks list matches engine.* [Source: FR-A5]
- [ ] **AC3** — Given a page, When `/breadcrumbs` is requested, Then the ancestor path returns. *Assert: breadcrumb chain matches engine.* [Source: FR-A5]
- [ ] **AC4 (shaping)** — Given nav results, When returned, Then the shared shaping layer applies (projection/pagination). *Assert: cursor pagination on tree/backlinks lists.* [Source: FR-A12]
- [ ] **AC5 (negative)** — Given an unauthorized space/page, When nav is requested, Then engine 403/404 propagates unchanged. *Assert: no existence oracle.* [Source: FR-A17]

## 🔨 Tasks
- [ ] `/tree`, `/backlinks`, `/breadcrumbs` handlers over engine nav primitives (RED: `TestStructuralNavDelegatesToEngine`) (AC: 1,2,3)
- [ ] Apply shared shaping (pagination) (AC: 4)
- [ ] Propagate 403/404 verbatim (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Space tree, backlinks, breadcrumbs delegate to engine primitives (cursor-paginated); tested.
Tier: `internal/verb` nav handlers → engine nav primitives. Fronted through the grammar (FR-A5). Seam: engine nav boundary. Sibling dep: E3-S2 (locator), E5-S1 (shaping).

## 🧪 Testing
`TestStructuralNavDelegatesToEngine` (integration). CS §5 mocking: engine nav mocked at HTTP boundary; never mock own handlers.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (API-Design lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A5, FR-A12, FR-A17); Architecture pG9s9OjqZA (A-VERBS §3 nav).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — nav response schemas are the dispatch gate)
- [ ] Parent epic: E3. Intra: depends on E3-S1, E3-S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
