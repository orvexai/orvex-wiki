## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **delivery-program author restoring milestone dashboards**, I want **wiki-api to author milestone-scoped status-distribution dashboard graphs as the generic `orvex_dashboard` composition block through the block-patch chokepoint (FR-A6) and the per-type block-handler registry (ENG-1465)**, so that **the 16 dashboard pages get their graphs back WITHOUT resurrecting `linear_graph` or any `linear_*` handler (permanently dropped, D-S11 / PO ruling 10)**.

**Definition of Done:** ONE named test `TestOrvexDashboardBlockAuthorsWithReceipt` (integration — authoring a milestone-filtered `orvex_dashboard` block returns the standard `{url,id,version,persisted:true}` receipt, carries the M-label filter as typed block data, and is not a `linear_graph`/`resource_type='linear'` node). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

> Sources this story from the DRAFT amendment FR-A24 (appended 2026-07-09, awaits ratify) — do not treat as canonical until ratified.

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a milestone (M-label) filter, When an `orvex_dashboard` block is authored, Then it writes through FR-A6 and returns the standard receipt. *Assert: `TestOrvexDashboardBlockAuthorsWithReceipt` — receipt `{url,id,version,persisted:true}`.* [Source: FR-A24, FR-A7]
- [ ] **AC2 (no linear)** — Given the block, When authored, Then it is a generic `orvex_dashboard` block, NOT `linear_graph` and NOT `resource_type='linear'`. *Assert: zero linear_* nodes; CI grep-gate clean.* [Source: FR-A24, D-S11]
- [ ] **AC3 (no 500 regression)** — Given a milestone-filtered dashboard-graph write, When applied in the clean-room Go tier, Then it must not regress to a server 500 (the legacy-fork ENG-1582 defect does not carry over). *Assert: authoring regression test green.* [Source: FR-A24]
- [ ] **AC4 (registry-gated)** — Given the per-type block-handler registry (ENG-1465), When absent, Then this block cannot land (sequencing gate). *Assert: FR-A24 blocked-by ENG-1465.* [Source: FR-A24 sequencing]
- [ ] **AC5** — Given the 16 dashboard pages, When the block ships, Then the graphs are re-added and a composition-tier authoring regression test travels with it. *Assert: 16-page re-add + regression test present.* [Source: FR-A24]

## 🔨 Tasks
- [ ] `orvex_dashboard` block handler in the per-type registry (RED: `TestOrvexDashboardBlockAuthorsWithReceipt`) (AC: 1,4)
- [ ] M-label filter as typed block data; standard receipt (AC: 1)
- [ ] Zero-linear_* CI grep-gate (AC: 2)
- [ ] Authoring regression test (no 500) + 16-page re-add (AC: 3,5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Generic orvex_dashboard block is registered + validated (dashboardId), decoupled from any linear_* type, and authored through the block chokepoint with a receipt. Scope is a generic dashboard embed, not specifically a milestone-scoped graph builder.
Tier: `internal/blockpatch` per-type block-handler registry. Sourced from DRAFT amendment FR-A24; gated behind ENG-1465 (the registry that owns the `orvex_dashboard` builder). Seam: block-handler registry. This is the KEEP disposition already in ENG-1465's supported-type set — never a linear_graph revival (PO ruling 10 stands unamended).

## 🧪 Testing
`TestOrvexDashboardBlockAuthorsWithReceipt` (integration) + zero-linear grep-gate. CS §5 mocking: engine `apply-ops` mocked at HTTP boundary; never mock own registry.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Correctness, Regression lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A24 DRAFT, FR-A6, FR-A7, D-S11); ENG-1465 (block-handler registry), ENG-1582 (traceability).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — FR-A24 ratification + the block schema are the dispatch gate) + **ENG-1465** (per-type block-handler registry)
- [ ] Parent epic: E4. Intra: depends on E4-S1, E4-S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
