## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **external agent recording an audit event**, I want **wiki-api to serve the external-agent audit-write endpoint (`POST /v1/audit`, `WriteAuditDto`) re-homed from `@orvex/extensions`, shaping and forwarding the write to the engine WORM sink**, so that **the write endpoint composes here while the audit sink stays engine-side (WORM, purge-exempt, never `NoopAuditService`) and wiki-api makes no audit-retention decision**.

**Definition of Done:** ONE named test `TestAuditWriteForwardsToEngineWormSink` (integration — a `WriteAuditDto` posted to `/v1/audit` is shaped and forwarded to the engine sink; wiki-api stores nothing and makes no retention decision). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a `WriteAuditDto`, When posted to `/v1/audit`, Then it is shaped and forwarded to the engine sink. *Assert: `TestAuditWriteForwardsToEngineWormSink` — engine sink call made.* [Source: FR-A23]
- [ ] **AC2 (sink stays engine)** — Given the write, When forwarded, Then the sink is engine-side (WORM, purge-exempt, bound to `ORVEX_AUDIT_SERVICE`, never `NoopAuditService`). *Assert: no local persistence; sink target is the engine WORM sink.* [Source: FR-A23]
- [ ] **AC3 (no retention decision)** — Given the audit write, When processed, Then wiki-api makes no audit-retention decision. *Assert: no retention logic in wiki-api.* [Source: FR-A23]
- [ ] **AC4 (negative)** — Given a malformed `WriteAuditDto`, When posted, Then a typed 4xx, never a dropped/silent audit. *Assert: fail-loud on bad DTO.* [Source: FR-A23, FR-A14]
- [ ] **AC5 (sink-ownership TBD)** — Given the sink target, When the Definition Pack pins OQ-A13 (engine vs shared admin/audit service), Then the forward target follows. *Assert: target matches the pinned contract — TBD, pinned by the Definition Pack (ENG-2104).* [Source: OQ-A13]

## 🔨 Tasks
- [ ] `POST /v1/audit` handler shaping + forwarding `WriteAuditDto` (RED: `TestAuditWriteForwardsToEngineWormSink`) (AC: 1)
- [ ] Forward to engine WORM sink; no local persistence (AC: 2,3)
- [ ] Fail-loud on malformed DTO (AC: 4)
- [ ] Wire sink target to the pinned OQ-A13 decision (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — External-agent POST /v1/audit shapes + forwards to the engine WORM sink via AuditForwardClient, with a verified caller-identity seam + correlated request-id + full tests.
Tier: thin handler → engine audit sink client (transport/shape only). Re-homed from `@orvex/extensions` (§6.2) per FR-A23. Audit-sink ownership is OQ-A13/OD-7 (Daniel) — the sink may re-target. Seam: engine audit sink. Sibling dep: E1 (surface/auth), E5-S1 (shaping).

## 🧪 Testing
`TestAuditWriteForwardsToEngineWormSink` (integration). CS §5 mocking: engine sink mocked at HTTP boundary asserting the forward; never mock own handler.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Security, Auditability lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A23, OQ-A13); Architecture pG9s9OjqZA (A-GOVERNANCE §3 audit-write, OD-7).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the audit-write contract delta + OQ-A13 sink decision are the dispatch gate)
- [ ] Parent epic: E6. Intra: depends on E1

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
