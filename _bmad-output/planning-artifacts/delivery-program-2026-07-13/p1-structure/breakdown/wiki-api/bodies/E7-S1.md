## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **platform operator**, I want **an event-evicted Redis cache holding ONLY principal-independent artifacts (descriptor, block-schema catalog, authoring guide), evicted by a Trigger consuming `wiki.page.*` off the studio-spine Kafka topics (fed by the engine transactional-outbox relay — no Redis→Kafka bridge), with the consumer failing closed on a cell mismatch**, so that **no ACL-gated bytes are ever cached (the engine is the cache) and cross-cell events are dropped, not acted on**.

**Definition of Done:** ONE named test `TestArtifactCacheEvictsAndFailsClosedOnCellMismatch` (integration — an artifact is served warm then evicted on a `wiki.page.*` CloudEvent; a CloudEvent whose `orvexcell` extension mismatches this cell is dropped). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the cache, When populated, Then it holds only principal-independent artifacts (descriptor, catalog, authoring guide). *Assert: `TestArtifactCacheEvictsAndFailsClosedOnCellMismatch` — no tenant-derived content bytes cached.* [Source: FR-A18, D-A10]
- [ ] **AC2 (evict)** — Given a `wiki.page.*` CloudEvent off the spine, When consumed, Then the evict runs BEFORE any reprocessing. *Assert: evict-before-reprocess ordering.* [Source: FR-A18]
- [ ] **AC3 (cell-fail-closed)** — Given a CloudEvent with a mismatched `orvexcell` extension, When consumed, Then it is dropped, not acted on. *Assert: cross-cell event dropped (review A6).* [Source: Arch A6, cell-lint JGAUQRsw2g]
- [ ] **AC4 (no body cache)** — Given any code path, When it would cache a `{tenant,scope-hash,resource,version}` body, Then it does not exist (a cached allow decision by proxy is forbidden). *Assert: static gate — no shaped-read body cache.* [Source: FR-A18, D-A10, FR-A17]
- [ ] **AC5 (purge probe)** — Given the purge zero-residual probe, When run, Then it passes trivially (no tenant content bytes in Redis). *Assert: purge probe green.* [Source: FR-A18]
- [ ] **AC6 (mechanism)** — Given the event feed, When described, Then it is the engine transactional-outbox relay → studio-spine, NOT a Redis→Kafka bridge. *Assert: no Redis→Kafka bridge referenced (review A1).* [Source: Arch A1]

## 🔨 Tasks
- [ ] `internal/cache` Redis artifact cache (principal-independent keys only) (RED: `TestArtifactCacheEvictsAndFailsClosedOnCellMismatch`) (AC: 1,4)
- [ ] `internal/event` evict consumer + `POST /internal/events/evict` handler (currently missing → 404) (AC: 2)
- [ ] Cell-fail-closed guard on `orvexcell` extension (AC: 3)
- [ ] Purge zero-residual probe (AC: 5); correct mechanism wording (AC: 6); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Event-evicted Redis artifact cache + evict consumer with a fail-closed cross-cell guard; tested. The Kafka evict Trigger is Phase-0-deferred at the deploy layer (kafka-trigger-evict.yaml commented out); the handler is wired ahead of it.
Tier: `internal/cache` (Redis) + `internal/event` (CloudEvent-validating evict consumer). Honest-state gaps to close: the evict Trigger manifest is commented out and `/internal/events/evict` has no handler (Arch C). A-STATE preserve-on-ratify: no shaped-read body cache. Seam: studio-spine Kafka boundary. External prereq: engine outbox relay + shared studio-events broker (Phase-0 launch prerequisite for eviction).

## 🧪 Testing
`TestArtifactCacheEvictsAndFailsClosedOnCellMismatch` (integration). CS §5 mocking: Redis via testcontainer/real; CloudEvents injected as fixtures; engine mocked at HTTP boundary; never mock own cache/event packages.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Security, Reliability lens); cell-lint JGAUQRsw2g (orvexcell fail-closed).

## 🔗 References
PRD 8jAiCBifDW (FR-A18, D-A10, FR-A17); Architecture pG9s9OjqZA (A-STATE, A1, A6, C honest-state).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — CloudEvent envelope + `orvexcell` extension spec are the dispatch gate) + external: engine outbox relay + studio-events broker
- [ ] Parent epic: E7. Intra: depends on E1-S1; caches E5-S3 descriptor + E2 catalog

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
