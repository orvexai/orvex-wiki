## 🎯 Story
As an **operator diagnosing a degraded upstream**, I want **liveness kept engine-independent (the proxy stays in rotation, returns 502 on upstream failure) AND a separate dependency-health surface that round-trips engine/knowledge/identity reachability, with `/healthz` echoing both CELL_ID and CLUSTER_NAME**, so that **a degraded upstream is observable via console and never reads green-by-omission (the shipped single local-only `/healthz` collapses liveness and readiness today)**.

**Definition of Done:** ONE named test `TestHealthSplitLivenessVsDependencyRoundTrip` (integration — liveness returns healthy without upstream round-trips and stays in rotation on upstream failure; the dependency-health surface reports a degraded upstream instead of green; `/healthz` echoes CELL_ID + CLUSTER_NAME). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given liveness/readiness, When probed, Then readiness is engine-independent (proxy stays in rotation, 502 on upstream failure), not collapsed with a dependency check. *Assert: `TestHealthSplitLivenessVsDependencyRoundTrip` — readiness green while an upstream is down.* [Source: Arch B2]
- [ ] **AC2 (dependency health)** — Given the dependency-health surface, When an upstream is degraded, Then it round-trips engine/knowledge/identity and reports the degradation. *Assert: degraded upstream is not green-by-omission.* [Source: Arch B2, FR-A19]
- [ ] **AC3 (cell contract)** — Given `/healthz`, When probed, Then it echoes both CELL_ID and CLUSTER_NAME as distinct fields. *Assert: both fields present (review A3, day-1 cell contract).* [Source: Arch A3, cell-lint JGAUQRsw2g]
- [ ] **AC4 (negative)** — Given a hung upstream, When the dependency-health round-trip runs, Then it times out and reports degraded, never hangs the probe. *Assert: bounded dependency probe.* [Source: Arch B2, B4]

## 🔨 Tasks
- [ ] Split liveness/readiness from a dependency-health surface (RED: `TestHealthSplitLivenessVsDependencyRoundTrip`) (AC: 1,2)
- [ ] Add CLUSTER_NAME config field + echo it alongside CELL_ID in `/healthz` (AC: 3)
- [ ] Bounded dependency round-trip with timeout (AC: 4); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — Health-vs-liveness split is real (/healthz never round-trips upstreams; /health reports upstream state + echoes CLUSTER_NAME). GAP: /health reports only whether each upstream is CONFIGURED (Configured() booleans), NOT a real dependency round-trip probe.
Tier: health handlers + `internal/config` (CLUSTER_NAME). Fixes the collapsed `/healthz` honest-state gap (Arch B2) and the missing CLUSTER_NAME (Arch A3 — `internal/config/config.go` has no ClusterName field, `cmd/wikiapi` healthResponse omits it). Seam: upstream reachability probes. Sibling dep: E1-S1 (config), E7-S2 (obs).

## 🧪 Testing
`TestHealthSplitLivenessVsDependencyRoundTrip` (integration). CS §5 mocking: upstreams mocked at HTTP boundary (healthy/degraded/hung); never mock own health handlers.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (§10 operability, §11 honest-state); SE-Arch 8sYi523i4t (Operational-Excellence lens); cell-lint JGAUQRsw2g (CELL_ID + CLUSTER_NAME).

## 🔗 References
PRD 8jAiCBifDW (FR-A19); Architecture pG9s9OjqZA (A3, B2, B4).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the day-1 cell contract fields are the dispatch gate)
- [ ] Parent epic: E7. Intra: depends on E1-S1

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
