## 🎯 Story
As an **agent attempting a publish/confirm-gated action**, I want **wiki-api to return `needs_human_publish` / `needs_human_confirm` plus a Studio deep-link and transport server-minted RATIFY/CONFIRM tokens verbatim**, so that **human-gated promotion is surfaced to the caller while minting and enforcement stay at the engine chokepoint (D-A8) — wiki-api never mints, never promotes draft→canonical**.

**Definition of Done:** ONE named test `TestHumanGatingTransportsTokensVerbatim` (integration — a gated action returns `needs_human_*` + a deep-link and the engine-minted token unchanged; wiki-api performs no minting and no promotion). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a human-gated action, When attempted, Then `needs_human_publish`/`needs_human_confirm` + a Studio deep-link returns. *Assert: `TestHumanGatingTransportsTokensVerbatim` — gate code + deep-link present.* [Source: FR-A8]
- [ ] **AC2 (transport-only)** — Given an engine-minted RATIFY/CONFIRM token, When surfaced, Then it is transported byte-for-byte unchanged. *Assert: token forwarded verbatim.* [Source: FR-A8, D-A8]
- [ ] **AC3 (no minting)** — Given any code path, When it would mint a token or promote draft→canonical, Then it does not exist. *Assert: static gate — no minting/promotion in wiki-api.* [Source: FR-A8, D-A8]
- [ ] **AC4 (force surfaces)** — Given `ratify-gate`/`ratify-token`/`confirm-token` surfaces, When called, Then they are served verbatim with codes `RATIFY_FORCE_NOT_ALLOWED`/`GATE_UNSATISFIED`/`FORCE_TOKEN_REQUIRED` (closes CLI gap G5). *Assert: force-token codes propagate.* [Source: FR-A21]
- [ ] **AC5 (negative)** — Given an invalid/expired token from the engine, When transported, Then the engine's rejection propagates unchanged. *Assert: no local token validation.* [Source: FR-A8]

## 🔨 Tasks
- [ ] `needs_human_*` + deep-link response path (RED: `TestHumanGatingTransportsTokensVerbatim`) (AC: 1)
- [ ] Verbatim token transport (AC: 2)
- [ ] Static gate: no minting/promotion (AC: 3)
- [ ] Serve ratify-gate/ratify-token/confirm-token verbatim with force codes (AC: 4)
- [ ] Propagate engine token rejections (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — Typed needs_human_publish/needs_human_confirm codes + the DeepLink envelope are DEFINED, but no live path emits them: specgate.Check (which would transport the engine-minted RATIFY/CONFIRM token verbatim) returns ErrNotImplemented, and blockpatch human-gating is comment-only. Human-gated transport is scaffolded, not wired.
Tier: `internal/verb` write path → engine lifecycle/ratify chokepoint (transport-only). Minting stays engine-side (A-TOKEN/D-A8). Shared with E6-S2 (spec-gate confirm-token transport). Seam: engine ratify chokepoint. Preserve-on-ratify security posture (Arch §D).

## 🧪 Testing
`TestHumanGatingTransportsTokensVerbatim` (integration). CS §5 mocking: engine mocked at HTTP boundary emitting gate codes/tokens; never mock own transport.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Security lens — no privilege minting); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A8, FR-A21, D-A8); Architecture pG9s9OjqZA (A-TOKEN, A-GOVERNANCE).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the RATIFY_*/GATE_UNSATISFIED error vocabulary is the dispatch gate)
- [ ] Parent epic: E4. Intra: depends on E4-S1; shared with E6-S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
