## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent performing concurrent-safe writes**, I want **CAS on every write via required `ifVersion` → `409 VERSION_MISMATCH` on stale, and every write (including per-type block-authoring) returning the verified read-after-write receipt `{url, id, version, persisted:true}`**, so that **the widened read-base→transform→write race window is safe and block-authoring writes stop under-populating `version` (closing contract-gap G4)**.

**Definition of Done:** ONE named test `TestWriteReturnsVerifiedReceiptAndCasOn409` (integration — a fresh-base write returns the full receipt with a real version; a stale `ifVersion` returns 409 VERSION_MISMATCH; a block-authoring write returns the full `{url,id,version,persisted:true}`, not `{block_id,status}`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a write with the current `ifVersion`, When applied, Then it returns `{url,id,version,persisted:true}` (a verified read-after-write receipt, never a re-rendered body). *Assert: `TestWriteReturnsVerifiedReceiptAndCasOn409` — receipt shape + persisted true.* [Source: FR-A7]
- [ ] **AC2 (CAS)** — Given a stale `ifVersion`, When applied, Then `409 VERSION_MISMATCH`, no write. *Assert: 409 + zero mutation.* [Source: FR-A7]
- [ ] **AC3 (G4)** — Given a per-type block-authoring write, When applied, Then it returns the full receipt with a populated `version`, not the legacy `{block_id,status}`. *Assert: block-authoring receipt == full shape.* [Source: FR-A7]
- [ ] **AC4 (required)** — Given a write missing `ifVersion`, When applied, Then it is rejected (CAS required). *Assert: missing ifVersion → typed 4xx.* [Source: FR-A7]
- [ ] **AC5 (representation)** — Given the `ifVersion` representation (OQ-A9: timestamp vs int vs etag; header vs body), When pinned by the Definition Pack, Then the receipt `version` type follows. *Assert: representation matches the pinned contract — TBD, pinned by the Definition Pack (ENG-2104).* [Source: OQ-A9]

## 🔨 Tasks
- [ ] CAS `ifVersion` enforcement + 409 on stale (RED: `TestWriteReturnsVerifiedReceiptAndCasOn409`) (AC: 2,4)
- [ ] Verified read-after-write receipt assembly (AC: 1)
- [ ] Full receipt for block-authoring writes (close G4) (AC: 3)
- [ ] Align `version` type to the pinned ifVersion representation (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — CAS ifVersion->409 VERSION_MISMATCH plus a verified read-after-write receipt {url,id,version,persisted:true}, incl per-type block-authoring; tested.
Tier: `internal/blockpatch` receipt/CAS handling → engine `apply-ops`. CAS makes the read-base→transform→write window safe (A-VERBS). ifVersion representation is OQ-A9/OD-5 (contracts + Daniel). Sibling dep: E4-S1 (single apply-ops), E5-S3 (receipt schema in contracts).

## 🧪 Testing
`TestWriteReturnsVerifiedReceiptAndCasOn409` (integration). CS §5 mocking: engine mocked at HTTP boundary returning version/409; never mock own receipt logic.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Concurrency, Correctness lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A7, OQ-A9); Architecture pG9s9OjqZA (A-VERBS, OD-5).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — receipt schema + ifVersion representation OQ-A9 are the dispatch gate)
- [ ] Parent epic: E4. Intra: depends on E4-S1

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
