## 🎯 Story
As a **security reviewer of the strangler relocation**, I want **a black-box redaction parity test proving secret-redaction (R8c) executes below the relocation line, plus verified standalone degradation (knowledge/ai absent → typed errors while core verbs work)**, so that **Phases 2–3 relocating/deleting the engine's composition layer cannot resurrect unredacted read paths, and a standalone wiki degrades cleanly (NFR-A5)**.

**Definition of Done:** ONE named test `TestRedactionParityBelowRelocationLineAndStandalone` (integration — a secret-bearing read is redacted identically before and after the relocation line; with knowledge/ai unconfigured, `search/related` and ai-writes return typed errors while `get/save/edit/list` and drift/spec-gate SHA-mode keep working). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1 (redaction parity)** — Given a secret-bearing read, When served across the relocation line, Then redaction (R8c) executes below the line (primitive/export path) and output is identical. *Assert: `TestRedactionParityBelowRelocationLineAndStandalone` — no unredacted path resurfaces.* [Source: NFR-A3]
- [ ] **AC2** — Given Phases 2–3 relocate/delete the composition layer, When the parity test runs at each gate, Then it is green (redaction did not live only in the deleted layer). *Assert: Phase-2/3 exit gate runs this test.* [Source: NFR-A3]
- [ ] **AC3 (standalone: knowledge down)** — Given knowledge unconfigured, When `search/related` is called, Then a typed error while `get/save/edit/list` keep working. *Assert: typed degradation; core verbs unaffected.* [Source: NFR-A5, A-STANDALONE]
- [ ] **AC4 (standalone: ai down)** — Given ai unconfigured/down, When wiki-api serves, Then it is unaffected (ai is a consumer, not an upstream). *Assert: ai-down has no effect on wiki-api.* [Source: NFR-A5]
- [ ] **AC5 (standalone: engine down)** — Given engine down, When wiki verbs are called, Then they fail (expected; engine is source of truth) while identity-bundled edge verification still works. *Assert: engine-down → wiki verbs fail cleanly, edge auth intact.* [Source: NFR-A5, A-STANDALONE]

## 🔨 Tasks
- [ ] Black-box redaction parity test (secret-bearing read, before/after relocation line) (RED: `TestRedactionParityBelowRelocationLineAndStandalone`) (AC: 1,2)
- [ ] Wire the parity test into the Phase-2/3 exit gates (AC: 2)
- [ ] Standalone degradation matrix: knowledge/ai/engine absent (AC: 3,4,5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — Standalone degradation posture (typed 502 on an unconfigured/unreachable engine) is present everywhere. The redaction-below-relocation-line PARITY TEST (NFR-A3) is ABSENT -- no redaction logic exists in the tree.
Tier: black-box test harness + standalone config matrix. R8c redaction stays engine-side (NFR-A3); the risk is Phases 2–3 deleting the layer where it lives (Arch §5). A-STANDALONE: every upstream is a configured endpoint that may be absent; a fully standalone wiki bundles identity (D-S3). Seam: relocation line + upstream config. Sibling dep: E3-S5 (search degradation), E6-S1 (drift SHA-mode).

## 🧪 Testing
`TestRedactionParityBelowRelocationLineAndStandalone` (integration/black-box). CS §5 mocking: engine returns secret-bearing content real; upstreams toggled absent via config; never mock own redaction path — assert it via black-box output.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Security, Reliability lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (NFR-A3, NFR-A5, A-STANDALONE); Architecture pG9s9OjqZA (A-STANDALONE, §5 R8c relocation risk).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the redaction parity fixtures + standalone contract are the dispatch gate)
- [ ] Parent epic: E7. Intra: depends on E3-S5, E6-S1; gates Phases 2–3

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
