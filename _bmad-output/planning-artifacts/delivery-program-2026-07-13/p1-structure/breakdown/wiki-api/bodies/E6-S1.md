## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **living-wiki curator (via CLI/MCP)**, I want **wiki-api to own drift verification (`verifyPage` / `getDrift` / force-new-token) re-homed from the fork's `orvex/drift`, computing a real drift metric and writing the `orvex_page_meta` stamp fields through the engine's lifecycle-stamp primitive**, so that **I can tell whether a page drifted from its verified source, with the engine keeping only the stamp fields (D-S8) and no product column added to `pages`**.

**Definition of Done:** ONE named test `TestDriftVerifyComputesRealMetricAndStamps` (integration — `verifyPage` computes a real drift metric [not the constant behindBy=1], `getDrift` returns status, and stamps are written via the engine lifecycle-stamp primitive, not a second write path). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a page and its verified source, When `verifyPage` runs, Then a real drift metric is computed. *Assert: `TestDriftVerifyComputesRealMetricAndStamps` — metric varies with actual drift, not a constant.* [Source: FR-A20]
- [ ] **AC2** — Given a page, When `getDrift` is called, Then drift status the CLI/MCP consume returns. *Assert: status shape matches the re-homed contract.* [Source: FR-A20]
- [ ] **AC3 (stamp path)** — Given a verification, When stamps (`verified_against`/`verified_at`/`spec_confirmed`) are written, Then they go through the engine lifecycle-stamp primitive (single write path), no new `pages` column. *Assert: stamp write == lifecycle-stamp primitive call.* [Source: FR-A20, D-S8]
- [ ] **AC4 (standalone)** — Given knowledge absent, When drift is computed, Then it degrades to the SHA/stamp comparison the engine still supports. *Assert: SHA/stamp fallback works with knowledge unconfigured.* [Source: FR-A20, A-STANDALONE, NFR-A5]
- [ ] **AC5 (force-new-token)** — Given a force-new-token request, When issued, Then the engine-minted token is transported verbatim. *Assert: no minting in wiki-api.* [Source: FR-A20, D-A8]

## 🔨 Tasks
- [ ] Clean-room re-home `verifyPage`/`getDrift` with a real drift metric (RED: `TestDriftVerifyComputesRealMetricAndStamps`) (AC: 1,2)
- [ ] Stamp writes via engine lifecycle-stamp primitive (AC: 3)
- [ ] SHA/stamp standalone degradation (AC: 4)
- [ ] force-new-token verbatim transport (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Living-wiki drift verification (VerifyPage/GetDrift/force-new-token) with a real content-hash drift metric + quarantine; real HTTP handlers and a full per-file test suite.
Tier: `internal/drift` domain package → engine lifecycle-stamp primitive + knowledge (content-similarity). Re-homed clean-room per A-GOVERNANCE / D-S8 (Phase-2 work), never a port of the AGPL module (NFR-A7). Fixes the meaningless behindBy=1 signal. The real drift metric is a §3.7 design-it-twice seam (Arch B1). Seam: engine stamp primitive + knowledge similarity.

## 🧪 Testing
`TestDriftVerifyComputesRealMetricAndStamps` (integration). CS §5 mocking: engine + knowledge mocked at HTTP boundary; never mock own drift package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Correctness, Data-Integrity lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A20, D-S8, NFR-A5); Architecture pG9s9OjqZA (A-GOVERNANCE, A-STANDALONE, B1).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the D-S8 drift contract delta is the dispatch gate)
- [ ] Parent epic: E6. Intra: depends on E1 (surface/auth); Phase-2 re-homing

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
