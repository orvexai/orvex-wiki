## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **operator of the deployed MCP client (322-path typed client)**, I want **wiki-api to reverse-proxy the engine's `/api/orvex/*` + page-blocks + nav paths byte-compatibly**, so that **MCP repoints from engine→wiki-api with a genuine base-URL change — no regeneration, no route remap — and the engine-vs-wiki-api black-box parity gate diffs like-for-like**.

**Definition of Done:** ONE named test `TestProxyBytesIdenticalToEngine` (integration — for a corpus of `/api/orvex/*` requests, asserts wiki-api's response bytes + status + headers are identical to the engine's direct response). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a request to a proxied `/api/orvex/*` path, When wiki-api serves it, Then response bytes/status/headers match the engine verbatim. *Assert: `TestProxyBytesIdenticalToEngine` byte-equality over the parity corpus.* [Source: FR-A15, D-A9]
- [ ] **AC2** — Given page-blocks and nav paths, When proxied, Then they too are path-identical. *Assert: proxy route table covers page-blocks + nav; unlisted paths 404.* [Source: FR-A15]
- [ ] **AC3** — Given an upstream engine 5xx, When proxying, Then wiki-api returns 502 and stays in rotation (readiness engine-independent). *Assert: upstream-fail → 502, not a crash or 503-drop.* [Source: Arch B2]
- [ ] **AC4 (error-path)** — Given a non-streaming `/api` call to a hung engine body, When the idle-read timeout elapses, Then the connection is bounded, not pinned. *Assert: content-type-aware deadline fires; SSE paths exempt.* [Source: Arch B4]
- [ ] **AC5 (negative)** — Given a path not in the engine surface, When requested, Then 404 (no silent pass-through of arbitrary hosts). *Assert: only fixed-config engine base URL is targeted.* [Source: NFR-A3]

## 🔨 Tasks
- [ ] Reverse-proxy handler over fixed engine base URL (RED: `TestProxyBytesIdenticalToEngine`) (AC: 1,2,5)
- [ ] Byte-preserving copy of status/headers/body; streaming-safe (AC: 1,4)
- [ ] 502-on-upstream-fail, readiness stays engine-independent (AC: 3)
- [ ] Content-type-aware idle-read deadline for non-SSE bodies (AC: 4)
- [ ] GREEN via parity corpus (AC: 1)

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — Real httputil reverse proxy under /api/*; facade routes return typed 502 when engine unconfigured (contract_trace_test). Byte-identical parity is a stdlib ReverseProxy property, not asserted against a live engine here.
Tier: thin handler → `internal/proxy` domain package → engine client. Phase-0 surface (a) per Arch A-STRANGLER. This is the meaningful parity oracle for all later relocation phases. Seam crossed: wiki-api→engine network boundary (REST).

## 🧪 Testing
`TestProxyBytesIdenticalToEngine` (integration, engine as real upstream or golden-tape) + upstream-fail unit test. CS §5 mocking: engine is an external dependency → mock at HTTP boundary with recorded golden tapes; never mock own proxy package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Operational-Excellence, Reliability lenses); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A15); Architecture pG9s9OjqZA (§3 surface a, A-STRANGLER, B2, B4).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the contract TAG is the dispatch gate)
- [ ] Parent epic: E1. Intra: depends on E1-S1 (skeleton)

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
