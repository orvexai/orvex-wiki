## 🗺️ Epic — Clean-room Go DfM↔ProseMirror serializer (pkg/dfm)

The K1 keystone: bidirectional, lossless DfM↔ProseMirror conversion, delivered as a **clean-room Go package** at `orvex-studio-lib/pkg/dfm` (D-A11) — written from the documented ProseMirror schema, the engine's published block-schema catalog (`GET /api/schemas/blocks`), and the contracts golden fixtures, **never** ported from or importing `@docmost/editor-ext` (AGPL, NFR-A7). One closed-side implementation, embedded by wiki-api **and** the Go CLI, replacing the CLI's contamination-risky hand-mirrored Go copy at Phase-1 exit [Source: FR-A1, FR-A3, Arch A-SERIALIZER].

Why it exists: conversion must be in-process for both first-party consumers (removes a `/convert` HTTP hop from the edit hot path, NFR-A1) and offline for the CLI; the reverse (PM→DfM) parity target is the engine's **shipped DfM read-path renderer**, not the legacy turndown export path [Source: FR-A1]. Losslessness is preserved *by construction*: opaque nodes never become editable text, and the golden corpus gates every phase (G3, NFR-A4). Double-implementation drift (engine AGPL TS vs closed Go) is permanent by design and kept honest by a continuous CI parity test [Source: Arch §5].

### Stories
- [ ] E2-S1 — pkg/dfm DfM→ProseMirror-JSON conversion (clean-room)
- [ ] E2-S2 — pkg/dfm ProseMirror→DfM reverse conversion vs shipped read-path renderer
- [ ] E2-S3 — Opaque-node typed handles + byte-identical reattach from ifVersion base
- [ ] E2-S4 — In-process embedding + /v1/convert/* + fidelity-check endpoints
- [ ] E2-S5 — Golden-corpus CI parity gate (Go round-trip vs engine catalog + fixtures)

### FR ids owned
FR-A1 (DfM↔PM serialization), FR-A2 (opaque-node discipline), FR-A3 (in-process conversion), NFR-A4 (fidelity/golden corpus), NFR-A7 (clean-room satisfies AGPL boundary).

### DoD
All five stories Done ∧ golden corpus (byte-identical opaque round-trip, all node types) green as a CI gate on the package ∧ engine-vs-pkg/dfm black-box parity green ∧ CLI hand-mirrored serializer replaced by pkg/dfm ∧ no engine `format=dfm` read path still falls through to turndown (surfaced at Phase-1 gate) ∧ family-E2E share green ∧ SDD lines evidenced. *Exact contract tag/golden-fixture versions pinned at pack certification (ENG-2104).*

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — golden fixtures + block-schema catalog version are the dispatch gate)
- [ ] Intra: E2-S3 depends on E2-S1/S2; E2-S4 depends on E2-S1/S2/S3; E2-S5 gates all

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
