## 🎯 Story
As a **maintainer of two deliberate serializer implementations (engine AGPL TS vs closed Go pkg/dfm)**, I want **a CI parity gate: Go round-trip vs the engine block-schema catalog + contracts golden fixtures, plus the black-box engine-vs-wiki-api corpus diff running continuously**, so that **the permitted double-implementation never drifts and byte-identical opaque round-trips are guaranteed before any relocation phase cuts over**.

**Definition of Done:** ONE named test `TestGoldenCorpusParityGate` (CI-gate layer — runs the full golden corpus through pkg/dfm and asserts byte-identical opaque round-trip for all node types AND diffs pkg/dfm vs the engine catalog with zero mismatches). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given the golden corpus, When the gate runs, Then all node types round-trip byte-identically. *Assert: `TestGoldenCorpusParityGate` 0 regressions across the corpus.* [Source: NFR-A4]
- [ ] **AC2** — Given the engine block-schema catalog, When diffed against pkg/dfm, Then zero mismatches. *Assert: catalog-vs-Go parity 0 diffs.* [Source: NFR-A4, FR-A1]
- [ ] **AC3 (continuous)** — Given a phase cutover, When it is proposed, Then the black-box engine-vs-wiki-api corpus diff must be green (runs continuously, not just at gates). *Assert: parity job is a required check on every merge.* [Source: Arch §5 double-impl-drift, NFR-A4]
- [ ] **AC4 (negative)** — Given a fixture regression introduced, When the gate runs, Then CI fails. *Assert: seeded regression → non-zero exit.* [Source: NFR-A4]

## 🔨 Tasks
- [ ] Golden-corpus round-trip harness (RED: `TestGoldenCorpusParityGate`) (AC: 1,4)
- [ ] Engine-catalog-vs-Go diff step (AC: 2)
- [ ] Wire black-box engine-vs-wiki-api corpus diff as a required, continuously-run check (AC: 3)
- [ ] Seed a regression fixture to prove the gate bites (AC: 4); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — Round-trip fidelity tests exist, but there is NO golden-corpus CI parity gate in THIS repo against the engine catalog+fixtures (that corpus lives in orvex-studio-lib); the in-repo contract gate is a route-table snapshot only, and a full external-contract diff is a disclosed cross-repo follow-up.
Tier: CI/CD gate feeding E1-S6's parity job. The corpus travels with pkg/dfm as a package CI gate AND is the black-box engine-vs-wiki-api gate that must pass before any relocation phase (NFR-A4). Seam: the drift-honesty mechanism for the permanent double implementation.

## 🧪 Testing
`TestGoldenCorpusParityGate` (CI-gate). CS §5 mocking: none — runs pkg/dfm real and the engine as a golden-tape/black-box oracle; never mock own package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Data-Fidelity, Operational-Excellence lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (NFR-A4, FR-A1, G3); Architecture pG9s9OjqZA (A-SERIALIZER, §5 double-impl-drift, B3).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the golden-fixture corpus tag is the dispatch gate)
- [ ] Parent epic: E2. Intra: depends on E2-S1..S3; feeds E1-S6

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
