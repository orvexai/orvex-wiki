## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As an **agent with a tight context budget**, I want **uniform response shaping applied in one shared layer — field projection (`fields=`), `concise|detailed` verbosity, and cursor pagination on every list**, so that **every read surface behaves consistently and the ≤ ~1k-token KPI holds, without shaping being re-implemented per route**.

**Definition of Done:** ONE named test `TestShapingAppliedOnceAcrossVerbs` (integration — `fields=` projects, `concise|detailed` toggles verbosity, and every list paginates by cursor, all via one middleware/interceptor applied to multiple verbs). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given `fields=a,b`, When any read is requested, Then only those fields return. *Assert: `TestShapingAppliedOnceAcrossVerbs` — projection honored across verbs.* [Source: FR-A12]
- [ ] **AC2** — Given `concise|detailed`, When requested, Then verbosity toggles consistently. *Assert: verbosity applied by the shared layer.* [Source: FR-A12]
- [ ] **AC3** — Given any list surface, When requested, Then cursor pagination applies uniformly. *Assert: every list returns a cursor.* [Source: FR-A12]
- [ ] **AC4 (one layer)** — Given the source, When inspected, Then shaping lives in one middleware, not per-route. *Assert: projection-determinism static gate — no per-route shaping.* [Source: FR-A12, Arch A-SHAPE, B1]
- [ ] **AC5 (determinism)** — Given identical inputs, When shaped, Then output is deterministic (projection-determinism gate). *Assert: stable field ordering/selection.* [Source: Arch B3 projection-determinism gate]

## 🔨 Tasks
- [ ] Shaping middleware: projection + verbosity + cursor pagination (RED: `TestShapingAppliedOnceAcrossVerbs`) (AC: 1,2,3)
- [ ] Apply to all read/list verbs via one interceptor (AC: 4)
- [ ] Projection-determinism static gate (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — One shaping layer: field projection (?fields=), concise|detailed verbosity, and cursor pagination -- wired through every read rung via projectJSON; tested.
Tier: shared shaping interceptor (handler-adjacent, applied to every verb). A-SHAPE names this as one layer; the single shaping interceptor is a §3.7 design-it-twice seam (Arch B1). This layer defends the ≤ ~1k-token KPI. Sibling dep: E3/E4 surfaces, E5-S2 (body-offload).

## 🧪 Testing
`TestShapingAppliedOnceAcrossVerbs` (integration). CS §5 mocking: upstreams mocked at HTTP boundary; never mock own shaping layer.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (API-Design, Consistency lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A12); Architecture pG9s9OjqZA (A-SHAPE, B1, B3).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — shaping-parameter contract is the dispatch gate)
- [ ] Parent epic: E5. Intra: applies across E3/E4; feeds E5-S2

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
