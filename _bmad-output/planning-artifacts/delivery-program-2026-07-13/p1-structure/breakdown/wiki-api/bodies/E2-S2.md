## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **reader fetching a page as DfM**, I want **a clean-room Go `ProseMirror-JSON → DfM` converter whose behavioral parity target is the engine's shipped DfM read-path renderer (not the legacy turndown export path)**, so that **read→edit→write round-trips are byte-identical and no node type silently falls through to turndown**.

**Definition of Done:** ONE named test `TestPmToDfmMatchesShippedRenderer` (package unit — for the golden corpus, asserts pkg/dfm's PM→DfM output equals the engine's shipped read-path renderer output byte-for-byte). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a PM tree, When converted to DfM, Then output equals the shipped renderer byte-for-byte. *Assert: `TestPmToDfmMatchesShippedRenderer` byte-equality.* [Source: FR-A1]
- [ ] **AC2 (round-trip)** — Given DfM→PM→DfM, When run over the corpus, Then the DfM is byte-identical to the input. *Assert: round-trip idempotence over all node types.* [Source: FR-A1, NFR-A4]
- [ ] **AC3 (scope-hole detection)** — Given any engine node type that still falls through to the legacy turndown path on `format=dfm`, When detected, Then it is surfaced as a Phase-1 scope hole. *Assert: parity oracle is the real renderer; turndown fall-through fails the gate loudly.* [Source: FR-A1]
- [ ] **AC4 (negative)** — Given an unrepresentable PM node, When converted, Then typed error, never lossy silent output. *Assert: unknown node → typed error.* [Source: FR-A14 fail-loud]

## 🔨 Tasks
- [ ] Implement PM→DfM emitter per node type (RED: `TestPmToDfmMatchesShippedRenderer`) (AC: 1)
- [ ] Round-trip idempotence harness DfM→PM→DfM (AC: 2)
- [ ] Turndown-fall-through detector against the real renderer oracle (AC: 3)
- [ ] Typed error on unrepresentable nodes (AC: 4); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** present — PM->DfM reverse conversion + fidelity check via the lib serializer; opaque nodes are fenced (:::dfm-opaque:::) not dropped. tested. Reverse logic lives in the lib.
Tier: external `orvex-studio-lib/pkg/dfm`. The FR-A1 reconciliation is explicit: reverse parity target = shipped read-path renderer, superseding the brief's "K1-reverse = XL keystone gap" claim. Seam: engine-vs-pkg/dfm parity. Sibling dep: E2-S1 (forward), E2-S3 (opaque reattach), E2-S5.

## 🧪 Testing
`TestPmToDfmMatchesShippedRenderer` + round-trip idempotence (package unit). CS §5 mocking: shipped-renderer output captured into golden fixtures; never mock own package.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t (Correctness, Data-Fidelity lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A1, NFR-A4); Architecture pG9s9OjqZA (A-SERIALIZER, §5).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — shipped-renderer golden fixtures are the dispatch gate)
- [ ] Parent epic: E2. Intra: depends on E2-S1

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
