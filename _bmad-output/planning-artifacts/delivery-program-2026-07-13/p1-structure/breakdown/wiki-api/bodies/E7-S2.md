## 🎯 Story
As an **operator watching the agent tier via orvex-studio-console**, I want **OTel traces + Prometheus metrics per the house pattern, with per-verb latency/error SLIs plus a facade-overhead SLI**, so that **the LGTM stack (Loki/Mimir/Tempo) shows per-verb health and the extra facade hop is observable, without duplicating the engine's agent-write audit/provenance stamping**.

**Definition of Done:** ONE named test `TestFacadeOverheadAndPerVerbSlisEmitted` (integration — a request emits an OTel trace and Prometheus per-verb latency/error metrics plus a facade-overhead SLI = wiki-api time minus upstream time). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2104); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria
- [ ] **AC1** — Given a request, When served, Then an OTel trace is emitted to Tempo per the house pattern. *Assert: `TestFacadeOverheadAndPerVerbSlisEmitted` — trace span present.* [Source: FR-A19]
- [ ] **AC2** — Given a request, When served, Then per-verb latency + error Prometheus SLIs are emitted. *Assert: per-verb metric labels present.* [Source: FR-A19]
- [ ] **AC3 (facade SLI)** — Given a proxied/composed request, When served, Then a facade-overhead SLI (wiki-api time minus upstream time) is emitted. *Assert: facade-overhead metric computed.* [Source: FR-A19, NFR-A1]
- [ ] **AC4 (no duplication)** — Given agent-write audit + provenance stamping (`ai_produced`/`ai_edited`), When a write occurs, Then they remain engine-side and are exercised, not duplicated. *Assert: no provenance stamping in wiki-api.* [Source: FR-A19]
- [ ] **AC5 (console name)** — Given observability wiring, When referenced, Then it names orvex-studio-console (not the renamed control). *Assert: console naming (review A4).* [Source: Arch A4]

## 🔨 Tasks
- [ ] OTel trace wiring to Tempo (RED: `TestFacadeOverheadAndPerVerbSlisEmitted`) (AC: 1)
- [ ] Prometheus per-verb latency/error SLIs (AC: 2)
- [ ] Facade-overhead SLI (wiki-api minus upstream) (AC: 3)
- [ ] Confirm provenance/audit stays engine-side (AC: 4); console naming (AC: 5); GREEN

## 🧠 Context

**🧾 Code audit (origin/dev @ 44bc54a, 2026-07-14):** partial — Per-verb + facade-overhead SLIs are real (in-process ring, p95, exposed on /health), but there are NO OTel traces and NO Prometheus /metrics endpoint -- both explicitly deferred (sli.go: orvex-studio-lib/pkg/metrics is a closed name-set owned elsewhere). Observability is only partially met.
Tier: cross-cutting observability middleware. Feeds LGTM consumed by orvex-studio-console (renamed from control, review A4). The facade-overhead SLI backs NFR-A1's ≤ 20 ms budget (E7-S4). Seam: telemetry export. Sibling dep: E7-S4 (latency benchmarks assert against these SLIs).

## 🧪 Testing
`TestFacadeOverheadAndPerVerbSlisEmitted` (integration). CS §5 mocking: OTel/Prometheus exporters via in-memory collectors; upstreams mocked at HTTP boundary; never mock own middleware.

## 📏 Guidance
CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (§10 operability); SE-Arch 8sYi523i4t (Operational-Excellence lens); cell-lint JGAUQRsw2g.

## 🔗 References
PRD 8jAiCBifDW (FR-A19, NFR-A1); Architecture pG9s9OjqZA (§4 obs wiring, A4 console rename).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2104** (Definition Pack — the house telemetry contract is the dispatch gate)
- [ ] Parent epic: E7. Intra: depends on E1-S1; feeds E7-S4

## 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
