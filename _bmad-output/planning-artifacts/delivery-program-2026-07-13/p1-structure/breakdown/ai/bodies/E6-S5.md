## 🎯 Story

As the platform, I want the durable/interactive boundary enforced (interactive in-request; long generations, tools-retry escalation, and maintenance schedules as central workflows), the A-RELAY durable-loop progress mechanism (poll-only fallback), and NO in-service Temporal worker — `cmd/temporal-worker` deleted with a CI grep guard — so that ai runs no workflow worker (FR-AI22, NFR-AI8, D-WF-1).

**Definition of Done:** one named test `TestNoInServiceTemporalWorker` (CI/static + integration) asserts no Temporal worker bootstrap import exists (grep guard passes, `cmd/temporal-worker` absent) and that a durable job's progress is observable via the poll-only job-status endpoint. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the codebase, When the CI grep runs, Then no Temporal worker bootstrap import exists and `cmd/temporal-worker` is deleted. *Assert: grep guard passes; binary absent.* [Source: NFR-AI8]
- [ ] **AC2** — Given the interactive/durable boundary, When a turn is long/expensive, Then it escalates to a central workflow rather than running unbounded in-request. *Assert: escalation to workflows path.* [Source: FR-AI22]
- [ ] **AC3** — Given maintenance work (history TTL sweep, reconciler, cost backfill, spend reconcile), When scheduled, Then it runs as central Temporal schedules calling step-APIs (no in-service cron/BullMQ). *Assert: no in-service scheduler.* [Source: FR-AI22]
- [ ] **AC4** (progress/negative) — Given a durable generation, When a client polls, Then progress is observable via the poll-only job-status endpoint (A-RELAY streaming is a sketch, OQ-AI5). *Assert: poll returns job status; no dependency on the unresolved relay.* [Source: FR-AI22]

## 🔨 Tasks

- [ ] RED: `TestNoInServiceTemporalWorker` (AC1, AC4)
- [ ] GREEN: delete `cmd/temporal-worker` + CI grep guard (AC1)
- [ ] GREEN: durable-escalation boundary + job-status poll endpoint (AC2, AC4)
- [ ] GREEN: wire maintenance as workflows schedules (AC3)

## 🧠 Context

Ops/CI + HTTP tier. Engine analogue: a scheduler externalized to workflows. Seams: workflows (schedules/dispatcher), A-RELAY (poll fallback; OQ-AI5 streaming TBD). Nothing else depends on which relay lands.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — maintenance schedules (retention + orphan_sweep) and the A-OUTBOX/A-RELAY drain loop (event.KafkaRelay) are real. MISSING: the durable step boundary for central Temporal, A-RELAY progress-poll fallback, and the no-in-service-worker guard — internal/workflow is scaffold (RunChatTurn/RunInlineTurn return ErrNotImplemented) and no step-ledger exists.

## 🧪 Testing

`TestNoInServiceTemporalWorker` (CI static grep + integration for poll endpoint). CS §5: no mocks for the grep; mock workflows caller boundary for the poll endpoint.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — no in-service worker (lib FR-L18). SE-Arch 8sYi523i4t: worker-sprawl lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F8 FR-AI22, §5 NFR-AI8; Arch Cb0XBkNezd §1, A-LOOP, A-RELAY, §4 deploy deltas.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; OQ-AI5 relay TBD)
- [ ] Parent epic: E6
- [ ] Intra-service: after E6-S1..S4

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
