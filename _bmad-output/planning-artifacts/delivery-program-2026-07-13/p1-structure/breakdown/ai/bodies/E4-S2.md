## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the call-site ceiling (D-S5/D-P5), I want per-caller scoped LiteLLM keys + budgets (cost-center = call-site × principal), routing every model call through the caller's scoped key, so that a runaway loop, compromised token, or buggy call-site is capped at *that* call-site, not merely the tenant aggregate (FR-AI12, layer 1a).

**Definition of Done:** one named test `TestPerCallerScopedKeyCeiling` (integration) provisions a scoped key per call-site × principal and asserts a runaway at one call-site is capped there while other call-sites in the same tenant remain unaffected. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a call-site × principal, When first used, Then a scoped key with a derived budget is provisioned and every model call routes through it. *Assert: scoped key used per cost-center.* [Source: FR-AI12]
- [ ] **AC2** — Given a runaway at call-site A, When it exhausts A's budget, Then A is capped while call-site B (same tenant) is unaffected. *Assert: A denied; B still allowed.* [Source: FR-AI12]
- [ ] **AC3** — Given scoped keys, When budgets are derived, Then they come from the tenant's entitlement (E4-S3) and sit beneath the per-tenant `max_budget` backstop. *Assert: scoped budget ≤ tenant budget.* [Source: FR-AI12]
- [ ] **AC4** (negative) — Given a call with no resolvable cost-center, When routed, Then it fails closed rather than falling back to the raw tenant key. *Assert: no unscoped model call.* [Source: FR-AI12, NFR-AI4]

## 🔨 Tasks

- [ ] RED: `TestPerCallerScopedKeyCeiling` (AC1, AC2)
- [ ] GREEN: scoped-key provisioning keyed on call-site × principal (AC1)
- [ ] GREEN: budget derivation under the tenant backstop (AC3)
- [ ] RED→GREEN: fail-closed on unresolvable cost-center (AC4)

## 🧠 Context

Adapter/domain tiers. Engine analogue: a scoped-credential manager. Seams: LiteLLM key-admin, E4-S1 backstop, E4-S3 entitlement.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — per-caller scoped keys are real — budget.CostCenter (tenant x call-site x principal) plus litellm.Provider.BuildTags/Resolve build the per-workspace,per-feature scoped provider + cost-center tags (provider.go:23,32); provider_test.go covers the tag grammar.

## 🧪 Testing

`TestPerCallerScopedKeyCeiling` (integration against LiteLLM stub). CS §5: mock LiteLLM key-admin; never mock ai's own provisioner.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — layered enforcement. SE-Arch 8sYi523i4t: blast-radius (per call-site) lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F5 FR-AI12 (D-S5/D-P5); Arch Cb0XBkNezd A-METER layer 1a.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate)
- [ ] Parent epic: E4
- [ ] Intra-service: after E4-S1 (backstop), E4-S3 (entitlement)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
