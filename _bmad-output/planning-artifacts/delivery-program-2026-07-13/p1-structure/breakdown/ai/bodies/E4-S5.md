## 🎯 Story

As a Free-tier tenant, I want the pre-flight gate to be a model-class allowlist keyed on billing's entitlement (D-AI12) — zero-cost models pass uncounted, frontier-model calls are denied 402 (paid-only), image generation excluded — so that Free = the ~zero-cost AI set free forever, with no lifetime action counter (FR-AI13, FR-AI12).

**Definition of Done:** one named test `TestFreeTierModelClassAllowlist` (integration) asserts a Free tenant's zero-cost-model call passes uncounted while a frontier-model call is denied 402 (paid-only), and that a failed allowlist/entitlement read denies frontier (fail-closed) — with the retired `ai:calls:{tenant}` lifetime counter absent. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Free tenant, When calling a zero-cost/allowlisted model, Then the call passes uncounted (no lifetime counter). *Assert: zero-cost call allowed; no action count decremented.* [Source: FR-AI13, FR-AI12]
- [ ] **AC2** — Given a Free tenant, When calling a frontier model, Then it is denied 402 (paid-only). *Assert: frontier ⇒ 402 paid-only.* [Source: FR-AI13, FR-AI12]
- [ ] **AC3** — Given image generation on Free, When requested, Then it is excluded (paid-only). *Assert: Free image-gen ⇒ 402.* [Source: FR-AI12, FR-AI16]
- [ ] **AC4** (fail-closed/negative) — Given an allowlist/entitlement read failure, When gating a frontier call, Then it is denied (fail-closed), never read as under-limit. *Assert: read failure ⇒ frontier denied.* [Source: FR-AI13, NFR-AI4]

## 🔨 Tasks

- [ ] RED: `TestFreeTierModelClassAllowlist` (AC1, AC2, AC4)
- [ ] GREEN: pre-flight model-class allowlist keyed on entitlement (AC1, AC2)
- [ ] GREEN: image-gen paid-only exclusion (AC3)
- [ ] RED→GREEN: fail-closed on read failure; assert no lifetime counter (AC4)

## 🧠 Context

Domain/middleware tier. Engine analogue: a policy gate. Seams: entitlement cache (E4-S3), gateway model names (E5). D-AI12 supersedes the Free 10-lifetime-action split; `ai:calls:{tenant}` counter is retired.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — no Free-tier model-class allowlist / frontier-denied-402 gate exists — grep across internal/ finds no model-class allowlist, frontier-deny, or zero-cost-uncounted logic. Only the Free-tier action-count cap (budget.ActionCounter) is present, which is a different lever (E4-S4/E4-S2).

## 🧪 Testing

`TestFreeTierModelClassAllowlist` (integration). CS §5: mock entitlement + LiteLLM model routing; never mock ai's own allowlist.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — fail-closed policy. SE-Arch 8sYi523i4t: free-tier-abuse + fail-closed lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F5 FR-AI13 (D-AI12), §9 D-AI12; Brief rgBOQh31p3 free-tier cost doctrine.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; allowlist model-class taxonomy + frontier-taster redesign TBD)
- [ ] Parent epic: E4
- [ ] Intra-service: after E4-S3 (entitlement), E5-S1 (gateway)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
