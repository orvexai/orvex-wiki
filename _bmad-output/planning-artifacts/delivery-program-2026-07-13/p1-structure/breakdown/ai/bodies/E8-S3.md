## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the edge, I want `orvex-studio-lib/auth` verification (identity JWKS + cached introspection, deny-by-default per-route scopes, `RequireFresh` on settings/admin/cap mutations) and caller-token pass-through to knowledge and wiki-api on every upstream call, launching token-first (identity ships first; browser paths stay on the fork until identity is live), so that authz is enforced consistently and delegated upstream (FR-AI24).

**Definition of Done:** one named test `TestEdgeAuthDenyByDefaultAndPassThrough` (integration) asserts an undeclared-scope route denies by default, `RequireFresh` gates a settings mutation, and an upstream knowledge/wiki-api call forwards the caller's token (not a service credential). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a bearer/workload token, When it hits an edge route, Then lib/auth verifies via identity JWKS + cached introspection with deny-by-default per-route scopes. *Assert: undeclared scope ⇒ deny.* [Source: FR-AI24]
- [ ] **AC2** — Given a settings/admin mutation, When submitted, Then `RequireFresh` is enforced. *Assert: stale session ⇒ refresh required.* [Source: FR-AI24]
- [ ] **AC3** — Given an upstream call to knowledge/wiki-api, When made, Then the caller's token is forwarded (enforcement happens at their chokepoints). *Assert: upstream Authorization = caller token.* [Source: FR-AI24]
- [ ] **AC4** (sequencing/negative) — Given identity is not yet live, When a browser path is attempted, Then ai stays token-first (browser paths on the fork); the retired engine RS256 enabler is not a fallback. *Assert: token-first; no RS256 fallback.* [Source: FR-AI24]

## 🔨 Tasks

- [ ] RED: `TestEdgeAuthDenyByDefaultAndPassThrough` (AC1, AC3)
- [ ] GREEN: lib/auth edge middleware (JWKS + introspection + scopes) (AC1)
- [ ] GREEN: RequireFresh on mutations + upstream token pass-through (AC2, AC3)
- [ ] RED→GREEN: token-first sequencing guard (AC4)

## 🧠 Context

Edge/middleware tier. Engine analogue: an auth guard importing the shared verifier. Seams: identity JWKS/introspection (lib/auth — blocked on lib FR-L1–L8 real verifier), knowledge/wiki-api upstreams. The `/internal/*` routes use `WorkloadIdentityOnly` (E6).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — edge token verification is real — auth.Verifier verifies two bearer shapes (JWT via JWKS + identity /v1/exchange), RS* alg allowlist, fail-closed to ErrUnauthorized (verifier.go), with upstream token pass-through via clients/session_exchange.go; verifier_test.go covers it. NOTE: a distinct 'RequireFresh' max_age knob is not separately named (freshness comes from the /v1/exchange token).

## 🧪 Testing

`TestEdgeAuthDenyByDefaultAndPassThrough` (integration). CS §5: mock identity JWKS/introspection; never mock ai's own lib/auth wiring (import the real lib).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — deny-by-default, RequireFresh, delegated enforcement. SE-Arch 8sYi523i4t: authz-ceiling lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F9 FR-AI24; Arch Cb0XBkNezd A-AUTH; lib FR-L1–L8/L6.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; scope declarations + lib/auth contract freeze there); lib FR-L1–L8 real verifier (deny-by-default stub blocks M7)
- [ ] Parent epic: E8
- [ ] Intra-service: foundational for all authenticated routes

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
