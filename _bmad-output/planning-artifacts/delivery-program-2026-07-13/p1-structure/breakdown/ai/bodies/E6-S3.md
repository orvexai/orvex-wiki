## 🎯 Story

As a workflows activity, I want `POST /internal/v1/steps/result-sink` to persist the final artifact (chat message, draft, page-write receipt) and emit the terminal event, plus `cmd/server` to publish the durable-job enqueue `ai.job.*` events via the outbox that workflows' dispatcher consumes, so that ai exposes no start-workflow API and durable jobs start Kafka-first (FR-AI21, FR-AI22, A-LOOP).

**Definition of Done:** one named test `TestResultSinkTerminalEventAndJobEnqueue` (integration) runs result-sink, asserts the artifact is persisted idempotently and a terminal event emitted, and asserts an `ai.job.*` enqueue is published via the outbox (not directly). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a completed loop, When `steps/result-sink` runs, Then the final artifact is persisted (message/draft/receipt) and a terminal event emitted. *Assert: artifact persisted + terminal event.* [Source: FR-AI21]
- [ ] **AC2** — Given a durable job to enqueue, When `cmd/server` publishes, Then `ai.job.*` is emitted via the outbox+relay (not directly) for workflows' dispatcher. *Assert: enqueue rides the outbox; ai exposes no start-workflow API.* [Source: FR-AI22, FR-AI21]
- [ ] **AC3** — Given a replay, When result-sink is called again with the same idempotency key, Then it is a no-op. *Assert: replay ⇒ no duplicate artifact/event.* [Source: FR-AI21]
- [ ] **AC4** (negative) — Given a bearer token, When presented, Then it is rejected (`WorkloadIdentityOnly`). *Assert: bearer ⇒ 401/403.* [Source: FR-AI21]

## 🔨 Tasks

- [ ] RED: `TestResultSinkTerminalEventAndJobEnqueue` (AC1, AC2)
- [ ] GREEN: result-sink persistence + terminal event (AC1)
- [ ] GREEN: `ai.job.*` outbox enqueue (AC2)
- [ ] RED→GREEN: idempotent replay + WorkloadIdentityOnly (AC3, AC4)

## 🧠 Context

Internal HTTP + store + event tier. Engine analogue: an internal step + event producer. Seams: outbox (E8-S4), workflows dispatcher (FR-W10), Postgres. ai exposes no start-workflow API.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — the durable result-sink step is an honest 501 stub — server.go:351 mux.HandleFunc('/internal/v1/steps/result-sink', todo('persist artifact + terminal event (FR-AI21)')); no ai.job.* enqueue exists.

## 🧪 Testing

`TestResultSinkTerminalEventAndJobEnqueue` (integration with Kafka + Postgres). CS §5: real Kafka/Postgres ephemeral; never mock ai's own sink/outbox.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — outbox, idempotency, Kafka-first enqueue. SE-Arch 8sYi523i4t: loss-tolerance + replay lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F8 FR-AI21/22; Arch Cb0XBkNezd A-LOOP, A-OUTBOX.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; step-API + `ai.job.*` types freeze there)
- [ ] Parent epic: E6
- [ ] Intra-service: after E8-S4 (outbox), E6-S1/S2

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
