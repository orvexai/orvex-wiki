## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a caller whose write hits the engine quota, I want ai to surface the engine's 402 `QUOTA_EXCEEDED` rejection verbatim as a typed error frame carrying the upgrade/entitlement deep-link, so that the write door is honest — never retried-around, cached, partial-written, or papered over by the model (FR-AI9, NFR-AI5).

**Definition of Done:** one named test `TestQuotaExceededSurfacedVerbatim` (integration) forces a wiki-api chokepoint 402 `QUOTA_EXCEEDED` and asserts ai returns a typed error frame with the verbatim code + deep-link and performs no retry and no partial write. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a chokepoint write returns 402 `QUOTA_EXCEEDED`, When ai handles it, Then a typed error frame carries the verbatim code + upgrade/entitlement deep-link. *Assert: frame.code=="QUOTA_EXCEEDED" && frame.deepLink present.* [Source: FR-AI9]
- [ ] **AC2** — Given the 402, When handled, Then ai performs no retry-around and no partial write. *Assert: exactly one chokepoint attempt; no residual partial state.* [Source: FR-AI9, NFR-AI5]
- [ ] **AC3** — Given a multi-op batch, When one op hits quota, Then the batch is not partially applied. *Assert: batch atomicity preserved (all-or-nothing).* [Source: FR-AI9]
- [ ] **AC4** (negative) — Given the model attempts to reframe the quota error as success, When streamed, Then ai emits the typed error, never a fabricated success. *Assert: no success frame follows the 402.* [Source: NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestQuotaExceededSurfacedVerbatim` (AC1, AC2)
- [ ] GREEN: 402 mapping → typed error frame with deep-link (AC1)
- [ ] GREEN: no-retry / no-partial-write enforcement (AC2, AC3)
- [ ] RED→GREEN: model-paper-over guard (AC4)

## 🧠 Context

Adapter/orchestration tier. Engine analogue: outbound-error mapping. Seams: wiki-api chokepoint (E2-S2), billing entitlement deep-link. This is the honest-write-door invariant (F-QUOTA).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the F-QUOTA 402 QUOTA_EXCEEDED path is real — budget.DeniedError wraps ErrBudgetExceeded carrying the full Decision (budget.go:41-59), surfaced as a typed error frame (inline/errors.go, frame.go) and asserted by server/quota_preflight_test.go.

## 🧪 Testing

`TestQuotaExceededSurfacedVerbatim` (integration). CS §5: mock wiki-api chokepoint to return 402; never mock ai's own mapping.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — typed errors, no silent success. SE-Arch 8sYi523i4t: honesty/no-confabulation lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F3 FR-AI9, §5 NFR-AI5; Arch Cb0XBkNezd A-TOOLS.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; `QUOTA_EXCEEDED` code + deep-link shape freeze there)
- [ ] Parent epic: E2
- [ ] Intra-service: after E2-S2 (chokepoint client)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
