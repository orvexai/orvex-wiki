## 🎯 Story

As a Prompt Composer user, I want an Improve-with-AI backend that takes a draft prompt and returns an improved version (structure, clarity, portability), running as an LLM call in ai over the same metering + PII + gateway spine, so that "the most amazing prompt editor in the world" has server-side AI without any model call leaving ai (Brief §Prompt Composer, D-AI3/FR-AI18).

**Definition of Done:** one named test `TestImproveWithAIMeteredRedacted` (integration) submits a draft prompt, asserts an improved prompt is returned, and asserts the call is metered (E4) and PII-redacted (E5) — with no model call originating outside ai. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.* Note: exact Improve-with-AI API surface is TBD — pinned by the Definition Pack (ENG-2097) against contracts + the api/ui seam.

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a draft prompt, When Improve-with-AI runs, Then an improved prompt is returned via the LiteLLM gateway. *Assert: improved prompt returned; gateway used.* [Source: Brief §Prompt Composer; FR-AI18]
- [ ] **AC2** — Given the call, When executed, Then it is metered and enforced under the tenant's entitlement (frontier gated per E4-S5). *Assert: usage recorded; entitlement enforced.* [Source: Brief; FR-AI12]
- [ ] **AC3** — Given the outbound payload, When sent, Then PII/secret redaction runs (E5-S4). *Assert: PII middleware ran.* [Source: Brief; FR-AI20]
- [ ] **AC4** (negative) — Given any attempt to run the LLM call outside ai, When reviewed, Then it does not exist — all LLM logic is confined to ai. *Assert: no model call outside ai (grep/architecture check).* [Source: Map; FR-AI18]

## 🔨 Tasks

- [ ] RED: `TestImproveWithAIMeteredRedacted` (AC1, AC2)
- [ ] GREEN: Improve-with-AI backend over the gateway (AC1)
- [ ] GREEN: metering + PII wiring (AC2, AC3)
- [ ] RED→GREEN: LLM-confinement assertion (AC4)

## 🧠 Context

Domain tier (ai's share of a product feature). Engine analogue: an AI-transform service. Seams: gateway (E5-S1), metering (E4), PII (E5-S4), api/ui (the editor UX — out of scope here). Only the LLM share lives in ai.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — no Improve-with-AI backend exists — 'improve_with_ai' appears only as a billing GatedFeature enum name (clients.go:692), with no handler, service, or route implementing prompt-improvement over the metering/PII spine.

## 🧪 Testing

`TestImproveWithAIMeteredRedacted` (integration). CS §5: mock LiteLLM; never mock ai's own composer backend.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — LLM confinement, altitude (ai's share only). SE-Arch 8sYi523i4t: scope-creep lens (no marketplace/UI logic here). cell-lint JGAUQRsw2g.

## 🔗 References

Brief rgBOQh31p3 §Prompt Composer (Improve-with-AI, RULED 2026-07-13); current-state-map §2 (ai owns Composer backend); PRD pbKI3BpQmY FR-AI18.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; Composer backend API surface + api/ui seam freeze there)
- [ ] Parent epic: E9
- [ ] Intra-service: after E4 (metering), E5 (gateway/PII)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
