## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the reporting layer, I want every completed model call to emit `ai.usage.recorded` onto the Kafka `studio-spine`, a `cmd/metering` Kafka consumer to journal it to a range-partitioned Postgres append table and maintain Redis fast counters, and `ai.cap.reached`/`ai.cap.warning` converted to Alertmanager/Mimir alert state — so that usage is journaled Kafka-first and reporting never gates enforcement (FR-AI14, D-S12/D-S13).

**Definition of Done:** one named test `TestUsageJournalKafkaConsumer` (integration) publishes an `ai.usage.recorded`, asserts `cmd/metering` upserts it to the Postgres append table and increments Redis counters idempotently, and asserts a `ai.cap.reached` becomes alert state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a completed model call, When it finishes, Then `ai.usage.recorded` (tenant, principal, feature, model, tokens, cost, latency) is published to the Kafka spine. *Assert: event published with the fields.* [Source: FR-AI14]
- [ ] **AC2** — Given the event, When `cmd/metering` consumes it, Then it journals to the range-partitioned Postgres append table (no Mongo) and updates Redis counters. *Assert: journal row + counter incremented.* [Source: FR-AI14]
- [ ] **AC3** — Given cap events, When consumed, Then `ai.cap.reached`/`ai.cap.warning` are converted to Alertmanager/Mimir alert state for console's LGTM reads. *Assert: alert state materialized.* [Source: FR-AI14]
- [ ] **AC4** (negative) — Given a journal-write failure, When it occurs, Then enforcement is unaffected (journal is reporting-only) and events buffer via spine retry/DLQ. *Assert: enforcement path unchanged; DLQ on failure.* [Source: FR-AI14, NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestUsageJournalKafkaConsumer` (AC2)
- [ ] GREEN: `ai.usage.recorded` producer + `cmd/metering` consumer group (AC1, AC2)
- [ ] GREEN: Postgres append-table journal + Redis counters (AC2)
- [ ] RED→GREEN: cap-event → alert state; DLQ on journal failure (AC3, AC4)

## 🧠 Context

Event producer/consumer + store tiers. Engine analogue: an event-sourced metering worker. Seams: Kafka spine (outbox-backed producer — E8-S4), Postgres append tables (E8-S2), Alertmanager/Mimir. `cmd/metering` is a plain-Deployment consumer group (NO-KEDA).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the usage journal is real — cmd/metering runs a real Kafka consumer group feeding metering.Journal.Record (upsert-on-ce-id idempotent) into the range-partitioned ai_usage_journal (migration 0003) with Redis spend counters (cache/spend_cache.go); metering_test.go covers it. NOTE: Journal.HandleCapEvent -> Alertmanager cap alerts is still a TODO stub.

## 🧪 Testing

`TestUsageJournalKafkaConsumer` (integration with Kafka + Postgres + Redis). CS §5: real Kafka/Postgres ephemeral; never mock ai's own consumer.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — CloudEvents, reporting≠enforcement. SE-Arch 8sYi523i4t: loss-tolerance lens. cell-lint JGAUQRsw2g: `orvexcell` fail-closed (E8-S4).

## 🔗 References

PRD pbKI3BpQmY §4 F5 FR-AI14 (D-S12/D-S13); Arch Cb0XBkNezd §1 topology, A-METER layer 3, A-OUTBOX.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; `ai.usage.*`/`ai.cap.*` topic types freeze there)
- [ ] Parent epic: E4
- [ ] Intra-service: after E8-S2 (append tables), E8-S4 (outbox)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
