## 🎯 Story

As a tenant admin, I want per-tenant model configuration (the `workspace_ai_credentials` successor minus embeddings): base URL, encrypted-at-rest virtual key, chat/completion/tool/image model names, applied budgets (read-only, derived from billing's entitlement), PII policy — over the preserved settings wire contract, so that model config survives cutover unchanged (FR-AI19).

**Definition of Done:** one named test `TestTenantModelConfigCrud` (integration) writes and reads `tenant_ai_config`, asserts the virtual key is encrypted at rest, budgets render read-only (derived from entitlement, no stored cap value), and the embedding model/dimension half is absent (split to knowledge, OQ-AI3). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a settings write, When persisted, Then model names + PII policy are stored and the virtual key is encrypted at rest. *Assert: ciphertext at rest; plaintext never persisted.* [Source: FR-AI19]
- [ ] **AC2** — Given the settings read, When returned, Then applied budgets are derived from billing's entitlement and render read-only (no stored cap value). *Assert: budget is read-only + entitlement-derived.* [Source: FR-AI19, FR-AI12]
- [ ] **AC3** — Given the config row, When inspected, Then the embedding model/dimension half is absent (split to knowledge). *Assert: no embedding config column in ai.* [Source: FR-AI19]
- [ ] **AC4** (negative) — Given the preserved wire contract, When a settings request arrives, Then the DTO shape matches the shipped contract (G3, except the FR-AI19a mask in E5-S3). *Assert: DTO parity.* [Source: FR-AI19]

## 🔨 Tasks

- [ ] RED: `TestTenantModelConfigCrud` (AC1, AC2)
- [ ] GREEN: `tenant_ai_config` store (encrypted key, KEK via OpenBao) + settings API (AC1)
- [ ] GREEN: entitlement-derived read-only budgets (AC2)
- [ ] RED→GREEN: assert embedding-half absent; wire parity (AC3, AC4)

## 🧠 Context

Store + HTTP tiers. Engine analogue: a config service with encrypted secrets. Seams: ExternalSecrets/OpenBao (KEK), entitlement (E4-S3), knowledge (embedding-config split, OQ-AI3 TBD).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — the per-tenant model-config store is real — tenant_ai_config table (migration 0004) with an encrypted key column (litellm_key_ciphertext) via store/postgres/ai_credentials_repo.go + litellm/decrypt.go. MISSING: the settings HTTP API to read/write tenant model config — no route or handler exists.

## 🧪 Testing

`TestTenantModelConfigCrud` (integration against ephemeral Postgres). CS §5: mock OpenBao KEK; never mock ai's own config store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — encryption at rest, no stored cap value. SE-Arch 8sYi523i4t: secret-at-rest lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F7 FR-AI19 (OQ-AI3); Arch Cb0XBkNezd A-DATA.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; settings DTO + OQ-AI3 embedding-config split freeze there)
- [ ] Parent epic: E5
- [ ] Intra-service: after E8-S1 (RLS), before E5-S3 (mask)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
