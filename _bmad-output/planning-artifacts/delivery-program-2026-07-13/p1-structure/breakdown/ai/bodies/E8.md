## 🗺️ Feature area

The runtime spine every other epic stands on: Postgres (CNPG, tenant-RLS, repository-layer tenant GUC) for chat/prompt/memory/config state + range-partitioned append journals (Mongo struck, D-S12); Redis for spend/entitlement caches, counters, idempotency and relay buffers (all keys tenant-embedded); `orvex-studio-lib/auth` at the edge with `RequireFresh` and upstream token pass-through (token-first sequencing, identity ships first); the A-OUTBOX producer outbox+relay with an idempotent, cell-guarded metering consumer; and A-OPS liveness/readiness split with the full cell-contract health echo and OTel SLIs feeding LGTM for console.

**Why it exists:** tenant isolation, the event substrate, fail-closed cell guarding, and honest health are cross-cutting invariants (FR-AI23/24/25, NFR-AI1/3/6/7; Arch A-DATA/A-AUTH/A-OUTBOX/A-OPS; cell-lint JGAUQRsw2g). This epic also carries the SE-Arch audit's build-time corrections (A-DRIFT: F1/F5/F-JOURNAL/F9/F10).

## 📋 Story roster

- [ ] E8-S1 — [ai] Postgres schema + tenant-RLS + repository-layer tenant GUC
- [ ] E8-S2 — [ai] Range-partitioned usage/journal + step-ledger append tables + partition cronjob + Redis caches
- [ ] E8-S3 — [ai] lib/auth edge verification + RequireFresh + upstream token pass-through (token-first sequencing)
- [ ] E8-S4 — [ai] A-OUTBOX producer outbox+relay + metering ce-id idempotent + orvexcell fail-closed
- [ ] E8-S5 — [ai] A-OPS liveness/readiness split + cell-contract echo + OTel SLIs → LGTM + audit events
- [ ] E8-S6 — [ai] No-AGPL-import CI guard + latency-budget verification (NFR-AI1)

## 🎯 FR ids owned

FR-AI23, FR-AI24, FR-AI25, NFR-AI1, NFR-AI3, NFR-AI6, NFR-AI7. Seams: identity JWKS/introspection (lib/auth), Kafka spine, LGTM/Alertmanager, ExternalSecrets/OpenBao. F6 LLM-confinement carve-out + F8 break-glass are OPEN governance ADRs (TBD — ENG-2097).

## ✅ Epic DoD

- [ ] All 6 stories Done (reviewer ≠ implementer).
- [ ] Cross-tenant RLS probe CI green; cell-mismatch event fails closed; `/healthz` echoes CELL_ID + CLUSTER_NAME, `/readyz` gates on real upstream round-trips.
- [ ] SDD lines evidenced: no `@docmost/*`/AGPL import; every Redis key tenant-embedded; pre-flight ≤5 ms cache-hit; no Temporal worker bootstrap import.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; CloudEvent catalog, cell contract, lib/auth scope declarations freeze there). Blocked-by lib FR-L1–L8/L18/L19 real (deny-by-default verifier).
- [ ] Intra-epic order: E8-S1 → E8-S2 → E8-S3/E8-S4 → E8-S5/E8-S6. Foundational to all other epics.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on blocker.
