## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the in-editor AI affordances (Cmd+K/Cmd+J palette, bubble-menu actions, `/ai` slash, AI-diagram), I want all logic server-side in ai — including the mermaid→drawio/excalidraw conversion and the bake-payload/excalidraw bake pipeline — so that the client is thin UI carrying zero AI logic (D-S4, FR-AI26, FR-AI17).

**Definition of Done:** one named test `TestServerSideDiagramBake` (integration) submits an `emit-excalidraw`/mermaid payload and asserts ai performs the mermaid→drawio/excalidraw conversion server-side and stores an attachment-backed preview under the caller's token — with no conversion logic on any client path. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a mermaid diagram, When emitted, Then ai converts it to drawio/excalidraw server-side and streams the converted result. *Assert: conversion runs in ai; client receives converted frames.* [Source: FR-AI26, FR-AI17]
- [ ] **AC2** — Given a bake-payload request, When processed, Then the excalidraw scene renders to an attachment-backed preview via the engine attachment API under the caller's token. *Assert: attachment stored with caller token.* [Source: FR-AI17]
- [ ] **AC3** — Given the in-editor palette/bubble/slash entries, When invoked, Then they call `/api/ai/*` over ingress and carry no prompt composition or model call client-side. *Assert: client sends request only; no client-side prompt/model.* [Source: FR-AI26]
- [ ] **AC4** (negative) — Given a malformed diagram payload, When converted, Then a typed error is returned, never a partial/corrupt attachment. *Assert: no attachment written on failure.* [Source: FR-AI17, NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestServerSideDiagramBake` (AC1, AC2)
- [ ] GREEN: server-side mermaid→drawio/excalidraw converter (AC1)
- [ ] GREEN: bake-payload pipeline → attachment API under caller token (AC2)
- [ ] RED→GREEN: malformed-payload typed error (AC4)

## 🧠 Context

Domain + adapter tiers. Engine analogue: a server-side rendering service. Seams: engine attachment API (caller token), E2-S1 registry. The client-side AI-diagram affordance is the named D-S4 thin-UI carve-out.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the visual-bake thin-UI carve-out is real — internal/diagram (13 files: convert/render/scene/chokepoint) does server-side mermaid->drawio/excalidraw conversion, mounted at POST /api/ai/diagrams/bake (server.go:345) behind edge-auth; bake_test.go + convert_test.go + diagram_route_test.go cover it.

## 🧪 Testing

`TestServerSideDiagramBake` (integration). CS §5: mock the engine attachment API; never mock ai's own converter.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — server-side logic, thin-UI boundary. SE-Arch 8sYi523i4t: thin-UI-carve-out lens (no AI logic client-side). cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F6 FR-AI17, F10 FR-AI26 (D-S4); Arch Cb0XBkNezd A-TOOLS.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; bake-payload DTO + attachment seam freeze there)
- [ ] Parent epic: E2
- [ ] Intra-service: after E2-S1 (registry)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
