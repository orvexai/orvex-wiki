## 🎯 Story

As the Librarian, I want the memory extraction/proposal pipeline to run asynchronously off the write path (never inline) and propose memory candidates (from the beads stream, save-this-thread, and imports) at a measurable quality bar — proposal precision ≥ 0.75 and recall ≥ 0.60 against a labeled golden set — so that confirming keepers is a light touch and the instant feel is preserved (FR-X1, FR-X5).

**Definition of Done:** one named test `TestExtractionQualityBarOffWritePath` (integration + eval) asserts extraction runs async off the write path and that proposals against the golden set meet precision ≥ 0.75 / recall ≥ 0.60. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.* [ASSUMPTION baseline: precision/recall thresholds — pinned by the eval set at pack certification.]

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a capture (beads/save-thread/import), When extraction runs, Then it runs asynchronously off the write path (never inline). *Assert: write path latency unaffected; extraction async.* [Source: FR-X5]
- [ ] **AC2** — Given the golden set, When proposals are generated, Then precision ≥ 0.75 and recall ≥ 0.60. *Assert: precision ≥ 0.75 && recall ≥ 0.60.* [Source: FR-X1]
- [ ] **AC3** — Given extraction compute, When it calls a model, Then it rides the separate nested extraction budget (AD-10, D-S15), never the user-action cap. *Assert: extraction spend ≠ user cap line.* [Source: FR-X1, FR-X5]
- [ ] **AC4** (negative) — Given the model is unavailable, When extraction fails, Then it retries async and never blocks or fails the originating write. *Assert: originating write unaffected on extraction failure.* [Source: FR-X5]

## 🔨 Tasks

- [ ] RED: `TestExtractionQualityBarOffWritePath` (AC1, AC2)
- [ ] GREEN: async extraction pipeline (off write path) (AC1)
- [ ] GREEN: proposal generation + golden-set eval harness (AC2)
- [ ] RED→GREEN: nested extraction budget + async retry (AC3, AC4)

## 🧠 Context

Domain + async worker tier (via step-APIs/schedules). Engine analogue: an async extraction service. Seams: beads/save-thread/import sources, budget (E4, D-S15), golden set. Owned slice F2 (proposal quality) per PRD g9vWbSYplh.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — no async memory-extraction pipeline exists — grep across internal/ + cmd/ finds no extraction/proposal-generation code. Only manual memory CRUD (E7-S2) is present.

## 🧪 Testing

`TestExtractionQualityBarOffWritePath` (integration + a labeled eval fixture). CS §5: mock LiteLLM extraction model; never mock ai's own pipeline.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — async off write path, budget isolation. SE-Arch 8sYi523i4t: quality-bar + instant-feel lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY memory gap-closure FR-X1/FR-X5; source-of-record g9vWbSYplh + spine iiCcKhGptV + map vBvVDFklZo; AD-10.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; proposal shape + eval-set baseline freeze there)
- [ ] Parent epic: E7
- [ ] Intra-service: after E7-S2 (memory store), E4 (budget)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
