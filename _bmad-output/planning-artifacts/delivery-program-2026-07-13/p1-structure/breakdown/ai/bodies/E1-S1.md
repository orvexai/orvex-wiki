## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the docmost React client (and Studio surfaces), I want `POST /api/ai/chat` to stream an AI-SDK data-stream SSE response byte-compatible with the shipped in-fork contract, so that the chat panel works at cutover with zero client change (G3).

**Definition of Done:** one named test `TestChatSSEDataStreamParity` (integration, HTTP layer) replays a recorded fixture prompt against `cmd/server` and asserts the emitted frame sequence — text deltas, tool frames, the citations frame, error frames, finish metadata — matches the recorded fork tape by frame type/order/DTO shape (modulo model-nondeterministic token text). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a fixture chat request, When `/api/ai/chat` is called, Then the response is `text/event-stream` with headers-first hijack and the first byte within the shipped first-byte timeout. *Assert: `Content-Type: text/event-stream` and first frame ≤ timeout.* [Source: FR-AI1]
- [ ] **AC2** — Given a turn with tool calls + citations, When streamed, Then frame types + ordering + DTO shapes equal the recorded tape modulo token text. *Assert: `diffFrames(tape, live).structuralDiff == 0`.* [Source: FR-AI1]
- [ ] **AC3** — Given a mid-stream model error, When it occurs, Then an error frame is emitted (never a hung stream) and finish metadata closes the stream. *Assert: stream ends with an error frame + finish event.* [Source: FR-AI1, NFR-AI5]
- [ ] **AC4** (negative) — Given a request whose DTO diverges from the frozen contract, When validated, Then a typed 4xx is returned before any model call. *Assert: `status==400 && body.code=="INVALID_REQUEST"`.* [Source: FR-AI1]

## 🔨 Tasks

- [ ] RED: write `TestChatSSEDataStreamParity` against a recorded tape fixture (AC1, AC2)
- [ ] GREEN: implement the SSE data-stream encoder (text/tool/citations/error/finish frames) (AC1–AC3)
- [ ] RED→GREEN: request DTO validation + typed rejection (AC4)
- [ ] Wire first-byte timeout + headers-first hijack (AC1)

## 🧠 Context

`cmd/server` HTTP handler tier (NestJS-engine analogue: a streaming controller). Seam crossed: none inward on this story (context assembly is E1-S4). Sibling deps: E1-S2 (session state to persist the turn), E4 metering (per-call), E8 auth. This is the always-warm streaming path — Knative never fronts it (NFR-AI2).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the AI-SDK SSE data-stream chat spine is real — chat.Service.RunChat (internal/chat/service.go:88) hijacks the response, streams text/tool/error/finish frames via internal/stream/sse.go, mounted at /v1/chat + /api/ai/chat (server.go:255,262) behind edge-auth; covered by chat_route_test.go + service_test.go + sse_test.go.

## 🧪 Testing

`TestChatSSEDataStreamParity` (integration) + unit tests for the frame encoder. CS §5 mocking: mock LiteLLM (external gateway) and the model response; never mock ai's own packages. Recorded-tape parity gate is the CI artifact (Arch A-STREAM).

## 📏 Guidance

CS 6aMAzsYeQb: §0 altitude, §3 typed errors (no hung streams), §4 naming, §5 mocking (LiteLLM is the external), §6 tiers/ports, §7 events, §10 cell contract, §11 CI gates. SE-Arch 8sYi523i4t: wire-drift lens (structural diff, not text). cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F1 FR-AI1, G3; Arch Cb0XBkNezd A-STREAM.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — the contract **TAG** is the dispatch gate; SSE frame catalog + `/api/ai/chat` DTO freeze there)
- [ ] Parent epic: E1
- [ ] Intra-service: after E1-S2 (persist turn), E8-S3 (auth)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
