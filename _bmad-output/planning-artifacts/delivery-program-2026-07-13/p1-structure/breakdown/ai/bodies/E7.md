## 🗺️ Feature area

ai owns the prompt library, the ephemeral chat-recall memory store (`ai_memories` — distinct from the product Memory SoR, which is orvex-studio-api per AD-1), and its owned slice of the platform memory gap-closure: **F2 proposal/extraction quality** (FR-X1–6), **F5 proposal-quality eval**, and **F8 token-cost** (FR-C1–3), plus inbound import (FR-O2). Extraction is compute ai runs; the product Memory SoR stays api. Memory LLM calls ride the inherited budget spine on a separate nested extraction budget (AD-10, D-S15).

**Why it exists:** the Librarian must propose keepers at a real precision/recall bar so confirming is a light touch (FR-X1), off the write path to preserve the instant feel (FR-X5), defended against memory-poisoning (FR-X6), and token-budgeted so injection costs ≥50% less than naive full-context (FR-C1). Governing SoR: PRD g9vWbSYplh + spine iiCcKhGptV + map vBvVDFklZo.

## 📋 Story roster

- [ ] E7-S1 — [ai] Prompt library CRUD + system-prompt rendering, admin-gated
- [ ] E7-S2 — [ai] Memories CRUD + chat-recall + GDPR opt-in + space-scoped visibility
- [ ] E7-S3 — [ai] Async extraction pipeline off the write path at the proposal quality bar
- [ ] E7-S4 — [ai] beads→staging capture edge as the reliable capture path
- [ ] E7-S5 — [ai] Proposal dedup/reconcile + memory-poisoning / prompt-injection defense
- [ ] E7-S6 — [ai] Continuous proposal-quality eval (accept/edit/discard) + regression gate
- [ ] E7-S7 — [ai] Token-budgeted memory injection ≥50% + measure/surface + warm-cache + inbound import

## 🎯 FR ids owned

FR-AI10, FR-AI11, FR-X1, FR-X2, FR-X3, FR-X4, FR-X5, FR-X6, FR-C1, FR-C2, FR-C3, FR-O2. Seams: staging (capture edge — workgraph/staging pre-build; TBD), knowledge (Memory retrieval under delegated principal, I-4), api (product Memory SoR, AD-1), Personal-Data Guard.

## ✅ Epic DoD

- [ ] All 7 stories Done (reviewer ≠ implementer).
- [ ] Proposal precision ≥0.75 / recall ≥0.60 against the golden set (FR-X1); ≥50% token reduction at no answer-correctness loss (FR-C1).
- [ ] SDD lines evidenced: extraction runs async off the write path; GDPR opt-in default off ⇒ recall empty + writes rejected; poisoning defense (human-confirm gate + provenance + sensitivity screen).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; memory/proposal shapes + capture-edge contract freeze there). Baselines FR-X1/X3/C1 are [ASSUMPTION] pending the eval set.
- [ ] Intra-epic order: E7-S1/E7-S2 → E7-S3 → E7-S4/E7-S5 → E7-S6/E7-S7.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on blocker.
