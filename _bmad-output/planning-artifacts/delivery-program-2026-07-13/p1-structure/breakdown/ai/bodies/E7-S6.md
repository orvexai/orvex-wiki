## 🎯 Story

As the platform, I want proposal quality continuously measured (accept / edit / discard rates) with regressions gated, so that the memory-extraction pipeline's quality is a monitored, non-regressing invariant (FR-X4).

**Definition of Done:** one named test `TestProposalQualityRegressionGate` (integration) records accept/edit/discard rates across a fixture run and asserts a regression below the FR-X1 quality bar fails the gate. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given proposal outcomes, When observed, Then accept / edit / discard rates are continuously measured and recorded. *Assert: the three rates are recorded per run.* [Source: FR-X4]
- [ ] **AC2** — Given a measured run, When quality falls below the FR-X1 bar, Then the regression gate fails. *Assert: below-bar ⇒ gate fails.* [Source: FR-X4, FR-X1]
- [ ] **AC3** — Given a passing run, When quality holds, Then the gate passes and the metrics are surfaced for monitoring. *Assert: at-bar ⇒ gate passes; metrics exported.* [Source: FR-X4]
- [ ] **AC4** (negative) — Given missing/insufficient labeled data, When gating, Then it fails safe (does not silently pass on absent evidence). *Assert: insufficient data ⇒ gate does not pass.* [Source: FR-X4]

## 🔨 Tasks

- [ ] RED: `TestProposalQualityRegressionGate` (AC2, AC4)
- [ ] GREEN: accept/edit/discard measurement + metric export (AC1, AC3)
- [ ] GREEN: regression gate vs FR-X1 bar (AC2)
- [ ] RED→GREEN: fail-safe on insufficient data (AC4)

## 🧠 Context

Ops/eval + metrics tier. Engine analogue: a quality-gate harness. Seams: extraction pipeline (E7-S3), OTel/LGTM metrics (E8-S5), golden set. 

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — no proposal-quality eval (accept/edit/discard) or regression gate exists anywhere in the tree — depends on the unbuilt extraction/proposal pipeline (E7-S3/S5).

## 🧪 Testing

`TestProposalQualityRegressionGate` (integration with eval fixtures). CS §5: no external mocks; never mock ai's own eval harness.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — measured quality, fail-safe gates. SE-Arch 8sYi523i4t: regression-gate lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY memory gap-closure FR-X4 (F5 proposal-quality eval); map vBvVDFklZo.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; eval-set + gate thresholds freeze there)
- [ ] Parent epic: E7
- [ ] Intra-service: after E7-S3/S5, E8-S5 (metrics)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
