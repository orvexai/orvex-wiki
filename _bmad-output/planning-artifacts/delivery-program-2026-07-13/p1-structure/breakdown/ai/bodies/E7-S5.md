## 🎯 Story

As the proposal layer, I want proposals de-duplicated and reconciled against existing Memory before display (≤ 5% near-duplicate rate), with the pipeline defended against memory-poisoning / prompt-injection carried in captured content — the human-confirm gate as the structural defense, backed by provenance capture and sensitivity screening — so that a malicious source cannot silently write canon (FR-X3, FR-X6).

**Definition of Done:** one named test `TestProposalDedupAndPoisoningDefense` (integration) asserts surfaced proposals have ≤ 5% near-duplicate rate against the confirmed set, and that an injection-laden captured source cannot write memory without passing the human-confirm gate + provenance + sensitivity screen. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.* [ASSUMPTION: ≤5% near-dup rate — pinned by the eval set.]

## ✅ Acceptance Criteria

- [ ] **AC1** — Given new proposals, When reconciled against confirmed Memory, Then near-duplicates are collapsed to ≤ 5% surfaced near-dup rate. *Assert: near-dup rate ≤ 5%.* [Source: FR-X3]
- [ ] **AC2** — Given captured content with injected instructions, When proposed, Then it cannot write memory without passing the human-confirm gate. *Assert: no auto-write; human-confirm required.* [Source: FR-X6]
- [ ] **AC3** — Given a proposal, When surfaced, Then provenance is captured and sensitivity screening applied. *Assert: provenance + sensitivity flags present.* [Source: FR-X6]
- [ ] **AC4** (negative) — Given a malicious source attempting to bypass the gate, When processed, Then it is blocked (structural defense holds). *Assert: bypass attempt ⇒ blocked.* [Source: FR-X6]

## 🔨 Tasks

- [ ] RED: `TestProposalDedupAndPoisoningDefense` (AC1, AC2)
- [ ] GREEN: dedup/reconcile against confirmed Memory (AC1)
- [ ] GREEN: human-confirm gate + provenance capture + sensitivity screen (AC2, AC3)
- [ ] RED→GREEN: bypass-attempt block (AC4)

## 🧠 Context

Domain tier over E7-S3. Engine analogue: a reconciliation + safety-screen service. Seams: confirmed Memory (api SoR, AD-1), sensitivity/PII screen (E5-S4 reuse). FR-X6 cross-refs FR-S6.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — no proposal dedup/reconcile or memory-poisoning / prompt-injection defense exists — the only injection-related bound is the memory render payload cap (E7-S7 slice), not a poisoning defense. No proposal pipeline to dedup.

## 🧪 Testing

`TestProposalDedupAndPoisoningDefense` (integration + injection fixtures). CS §5: mock the api Memory SoR read; never mock ai's own dedup/screen.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — human-in-the-loop structural defense. SE-Arch 8sYi523i4t: prompt-injection / poisoning lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY memory gap-closure FR-X3/FR-X6; map vBvVDFklZo.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; dedup threshold + Memory-SoR read seam freeze there)
- [ ] Parent epic: E7
- [ ] Intra-service: after E7-S3 (extraction)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
