## 🎯 Story

As the settings UI and console, I want `GET /api/ai/usage` (period spend/actions vs the billing-owned cap, by feature, last-12-months, counts), the separate nested embedding budget provisioned as an ai-provisioned sibling scoped key for knowledge (D-S15), and the capped/uncapped/Free-frontier black-box CI gate that closes FR-20, so that usage is reported and cost-protection is proven (FR-AI15, FR-AI12, NFR-AI4, FR-20).

**Definition of Done:** one named test `TestFR20BlackBoxCapGate` (CI/black-box) proves a capped tenant is refused, an uncapped tenant is not, a Free tenant is denied frontier (402) while zero-cost passes uncounted, and a re-point-the-key bypass attempt fails; plus asserts embedding spend rides the sibling budget and never draws the user-action cap. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a period, When `GET /api/ai/usage` is called, Then it returns current spend/actions vs the billing-owned cap, spend by feature, last-12-months, counts. *Assert: reporting DTO matches contract.* [Source: FR-AI15]
- [ ] **AC2** — Given knowledge's embedding calls, When provisioned, Then they ride a sibling scoped LiteLLM key against the separate embedding budget and never draw the user-action cap (content-hash skip mandatory). *Assert: embedding spend ≠ user-action cap line.* [Source: FR-AI12, FR-AI15]
- [ ] **AC3** — Given the black-box gate, When run in CI, Then a capped tenant is refused, an uncapped tenant is not, and a Free tenant is denied frontier while zero-cost passes uncounted. *Assert: all four gate assertions pass.* [Source: NFR-AI4, FR-20]
- [ ] **AC4** (bypass/negative) — Given a re-point-the-key attempt via settings, When made, Then it fails (FR-AI19a mask holds). *Assert: bypass attempt ⇒ typed rejection; spend still capped.* [Source: NFR-AI4, FR-AI19a]

## 🔨 Tasks

- [ ] RED: `TestFR20BlackBoxCapGate` (AC3, AC4)
- [ ] GREEN: `GET /api/ai/usage` reporting API (AC1)
- [ ] GREEN: sibling embedding-budget key provisioning for knowledge (AC2)
- [ ] RED→GREEN: black-box CI gate incl. bypass attempt (AC3, AC4)

## 🧠 Context

HTTP + adapter + CI tiers. Engine analogue: a reporting controller + a CI black-box harness. Seams: journal (E4-S6), knowledge embedding key (D-S15, F6 ADR open), FR-AI19a mask (E5-S3).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — PARTIAL/mostly-absent — the usage reporting API is an honest 501 stub (server.go:312 mux.HandleFunc('/api/ai/usage', todo(...))); the separate nested embedding budget (budget.EmbeddingKey) is scaffold only; no capped/uncapped black-box CI gate found.

## 🧪 Testing

`TestFR20BlackBoxCapGate` (black-box CI) + integration for the reporting API. CS §5: mock billing entitlement + LiteLLM; never mock ai's own reporting/journal.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — CI gates, separate embedding budget. SE-Arch 8sYi523i4t: FR-20 proof + bypass lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F5 FR-AI15, §5 NFR-AI4 (D-S15); Arch Cb0XBkNezd A-METER, §4 CI gates.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; usage DTO + embedding-budget seam + F6 LLM-confinement ADR freeze there)
- [ ] Parent epic: E4
- [ ] Intra-service: after E4-S1..S6, E5-S3 (mask)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
