## 🎯 Story

As the capture layer, I want the beads→staging capture edge (today flagged "not yet in canon") specified and delivered as the reliable capture path, so that keepers from the beads stream reliably reach the propose-and-confirm pipeline (FR-X2).

**Definition of Done:** one named test `TestBeadsToStagingCaptureEdge` (integration) emits a bead, asserts it is reliably captured onto the staging capture edge (at-least-once, idempotent), and reaches the extraction pipeline. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.* Note: staging is pre-build; the concrete capture-edge contract is TBD — pinned by the Definition Pack (ENG-2097).

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a bead in the beads stream, When captured, Then it is delivered onto the staging capture edge as the reliable capture path. *Assert: bead reaches the capture edge.* [Source: FR-X2]
- [ ] **AC2** — Given at-least-once delivery, When a bead is redelivered, Then capture is idempotent (no duplicate proposal). *Assert: redelivery ⇒ no duplicate.* [Source: FR-X2]
- [ ] **AC3** — Given a captured bead, When it lands, Then it feeds the extraction/proposal pipeline (E7-S3). *Assert: captured bead ⇒ extraction input.* [Source: FR-X2]
- [ ] **AC4** (negative) — Given the capture edge is unavailable, When a bead is emitted, Then it is buffered/retried and never silently dropped. *Assert: no silent drop; buffered.* [Source: FR-X2]

## 🔨 Tasks

- [ ] RED: `TestBeadsToStagingCaptureEdge` (AC1, AC2)
- [ ] GREEN: beads→staging capture edge (reliable, idempotent) (AC1, AC2)
- [ ] GREEN: hand-off into the extraction pipeline (AC3)
- [ ] RED→GREEN: buffer/retry on edge unavailable (AC4)

## 🧠 Context

Event/capture tier. Engine analogue: an ingestion edge. Seams: beads stream, orvex-studio-staging (pre-build — contract TBD), extraction (E7-S3). The Librarian is productized in staging; today it's the ad-hoc doc-librarian skill chain.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — no beads->staging capture edge exists — no beads capture, staging queue, or reliable-capture path anywhere in the tree.

## 🧪 Testing

`TestBeadsToStagingCaptureEdge` (integration). CS §5: mock the staging capture-edge boundary via its contract; never mock ai's own capture logic.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — at-least-once, idempotent, no silent drop. SE-Arch 8sYi523i4t: capture-reliability lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY memory gap-closure FR-X2; map vBvVDFklZo; Map §2 (Librarian loop → staging).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; beads→staging capture-edge contract freeze there — staging pre-build)
- [ ] Parent epic: E7
- [ ] Intra-service: feeds E7-S3

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
