## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the cost-protection backstop, I want a per-tenant LiteLLM virtual key (`/key/generate`, alias = tenant) provisioned with a hard `max_budget`, so that the gateway stops spend at the tenant cap even if every line of ai's own metering is wrong (FR-AI12, layer 1b — authoritative).

**Definition of Done:** one named test `TestTenantVirtualKeyBackstopEnforced` (integration against a LiteLLM stub) provisions a per-tenant key with `max_budget` and asserts an over-cap call is refused by the gateway independent of ai's pre-flight, with key↔tenant binding asserted on every call. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tenant, When provisioned, Then a virtual key aliased to the tenant is created with a hard `max_budget` derived from the entitlement. *Assert: key exists; max_budget set.* [Source: FR-AI12]
- [ ] **AC2** — Given ai's pre-flight is bypassed/buggy, When an over-cap call is made, Then the gateway refuses it. *Assert: over-cap call ⇒ gateway 4xx.* [Source: FR-AI12, NFR-AI4]
- [ ] **AC3** — Given any model call, When routed, Then key↔tenant binding is asserted (a tenant's key can never bill another tenant). *Assert: binding checked at call time.* [Source: FR-AI12, NFR-AI3]
- [ ] **AC4** (negative) — Given a missing entitlement on provisioning, When it fails to read, Then provisioning fails closed (deny the gated capability), never an unbounded key. *Assert: no key without a budget.* [Source: FR-AI12, NFR-AI4]

## 🔨 Tasks

- [ ] RED: `TestTenantVirtualKeyBackstopEnforced` (AC1, AC2)
- [ ] GREEN: LiteLLM key-admin client (`/key/generate`) + max_budget (AC1)
- [ ] GREEN: key↔tenant binding assertion (AC3)
- [ ] RED→GREEN: fail-closed on missing entitlement (AC4)

## 🧠 Context

Adapter tier (LiteLLM external port, CS §5 ADR trigger). Engine analogue: an outbound key-admin client. Seams: LiteLLM key-admin API, billing entitlement (E4-S3). This is the authoritative layer below ai.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — per-tenant virtual-key provisioning is real — provisioning.Provisioner.Provision get-or-creates the tenant_ai_config row and mints a LiteLLM key via litellm.Admin.CreateVirtualKey (admin.go:108, master-key auth); driven by cmd/ai-provisioner; provisioning_test.go + main_test.go cover it.

## 🧪 Testing

`TestTenantVirtualKeyBackstopEnforced` (integration against a LiteLLM stub). CS §5: mock LiteLLM key-admin; never mock ai's own provisioner.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — fail-closed, ports/adapters. SE-Arch 8sYi523i4t: cost-runaway + cross-tenant-billing lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F5 FR-AI12 (D-S5/D-AI11); Arch Cb0XBkNezd A-METER.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; LiteLLM external-dependency ADR + entitlement seam freeze there)
- [ ] Parent epic: E4
- [ ] Intra-service: foundational; before E4-S2..S7

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
