## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the family, I want a CI no-AGPL-import guard (no `@docmost/*`/AGPL package — clean-room Go against wire contracts and observed behavior) and a latency-budget verification harness (chat first-token p95 ≤ in-fork baseline + 150 ms, ask-loop overhead ≤ 50 ms/turn, pre-flight spend check ≤ 5 ms cache-hit), so that the AGPL boundary holds and NFR-AI1 is measured at cutover (NFR-AI7, NFR-AI1).

**Definition of Done:** one named test `TestNoAGPLImportAndLatencyBudgets` (CI/static + perf) asserts the codebase imports no `@docmost/*`/AGPL package (grep guard) and that the measured first-token p95, ask-loop overhead, and pre-flight cache-hit are within the NFR-AI1 budgets. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.* [NFR-AI1 budgets are targets pending cutover measurement.]

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the codebase, When the CI guard runs, Then no `@docmost/*`/AGPL import exists (clean-room Go). *Assert: grep guard passes.* [Source: NFR-AI7]
- [ ] **AC2** — Given chat, When first-token latency is measured, Then p95 ≤ in-fork baseline + 150 ms. *Assert: first-token p95 within budget.* [Source: NFR-AI1]
- [ ] **AC3** — Given the ask loop, When measured (excluding model + retrieval time), Then p95 overhead ≤ 50 ms/turn; pre-flight spend check ≤ 5 ms cache-hit. *Assert: ask overhead + pre-flight within budgets.* [Source: NFR-AI1]
- [ ] **AC4** (negative) — Given an added AGPL import, When CI runs, Then the guard fails the build. *Assert: introduced AGPL import ⇒ CI red.* [Source: NFR-AI7]

## 🔨 Tasks

- [ ] RED: `TestNoAGPLImportAndLatencyBudgets` (AC1, AC4)
- [ ] GREEN: no-AGPL-import CI grep guard (AC1)
- [ ] GREEN: latency-budget perf harness (first-token, ask overhead, pre-flight) (AC2, AC3)
- [ ] RED→GREEN: negative AGPL-import case (AC4)

## 🧠 Context

CI/perf tier. Engine analogue: a build guard + perf gate. Seams: CI pipeline (Tekton→Harbor), perf fixtures. NFR-AI1 budgets measured at cutover against the in-fork baseline.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the guardrails are real — ci.yml runs a static AGPL-import scan (AC7, ci.yml:275) and internal/nfr pins per-op latency budgets with a fail-closed EnforcedOps conformance gate (nfr.go); nfr_test.go + contract_test.go cover the budget table.

## 🧪 Testing

`TestNoAGPLImportAndLatencyBudgets` (CI static grep + perf harness). CS §5: perf harness stubs the model to isolate ai's overhead; never mock ai's own hot path logic.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — §11 CI gates, AGPL boundary. SE-Arch 8sYi523i4t: clean-room + latency lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §5 NFR-AI7, NFR-AI1; Arch Cb0XBkNezd A-RUNTIME, §4 CI gates.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; recorded in-fork baseline pinned there)
- [ ] Parent epic: E8
- [ ] Intra-service: spans all epics (final gate)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
