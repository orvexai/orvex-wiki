## 🎯 Story

As a workflows activity, I want `POST /internal/v1/steps/model-call` — one bounded, non-streaming model invocation (messages+tools in, completion/tool-calls+usage out) — idempotent (lib FR-L19), `WorkloadIdentityOnly` (lib FR-L6), and metered like any request, so that durable loops call ai steps without bypassing FR-20 (FR-AI21).

**Definition of Done:** one named test `TestStepModelCallIdempotentMetered` (integration) calls the step twice with the same idempotency key and asserts a single metered model call + identical replayed result, and that a bearer token is rejected (workload identity only). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given messages+tools, When `steps/model-call` is called, Then it returns completion/tool-calls + usage in a single bounded non-streaming invocation. *Assert: response carries completion + usage.* [Source: FR-AI21]
- [ ] **AC2** — Given a replayed idempotency key, When called again, Then the prior result is returned and no second model call is billed. *Assert: replay ⇒ same result; one metered call.* [Source: FR-AI21]
- [ ] **AC3** — Given a bearer token on this route, When presented, Then it is rejected (`WorkloadIdentityOnly`). *Assert: bearer ⇒ 401/403.* [Source: FR-AI21]
- [ ] **AC4** (negative) — Given a step over its per-step budget, When re-checked server-side, Then it is refused (belt-and-braces vs the workflow budget). *Assert: over-budget step ⇒ typed refusal.* [Source: FR-AI21, FR-20]

## 🔨 Tasks

- [ ] RED: `TestStepModelCallIdempotentMetered` (AC2, AC3)
- [ ] GREEN: step handler over the gateway (E5-S1) + metering (E4) (AC1)
- [ ] GREEN: lib FR-L19 idempotency-key replay + typed error envelope (AC2)
- [ ] RED→GREEN: WorkloadIdentityOnly + per-step budget re-check (AC3, AC4)

## 🧠 Context

Internal HTTP + domain tier. Engine analogue: an internal step controller. Seams: workflows (caller), lib/auth (workload identity, E8-S3), gateway (E5-S1), metering (E4). Authors workflows OQ-W4.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — the durable model-call step is an honest 501 stub — server.go:349 mux.HandleFunc('/internal/v1/steps/model-call', todo('bounded model invocation (FR-AI21)')); no idempotent/workload-identity step handler exists.

## 🧪 Testing

`TestStepModelCallIdempotentMetered` (integration). CS §5: mock LiteLLM; never mock ai's own step handler or idempotency store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — idempotency, workload identity, no metering bypass. SE-Arch 8sYi523i4t: replay + budget-bypass lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F8 FR-AI21; Arch Cb0XBkNezd A-LOOP.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; step-API shapes + OQ-W4 co-authored with workflows freeze there)
- [ ] Parent epic: E6
- [ ] Intra-service: after E4 (metering), E5-S1 (gateway), E8-S3 (auth)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
