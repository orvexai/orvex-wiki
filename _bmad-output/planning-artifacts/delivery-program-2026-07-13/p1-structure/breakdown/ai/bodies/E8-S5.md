## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As operations, I want each binary to separate liveness (`GET /healthz`, always-200, echoing CELL_ID + CLUSTER_NAME per the cell contract) from readiness (`GET /readyz`, real dependency round-trips — LiteLLM, knowledge, wiki-api, Postgres, Redis), plus OTel/Prometheus SLIs (first-token latency, tool-loop step count/duration, LiteLLM error rate, spend-check staleness, cap-denial count, usage-event lag) feeding LGTM, and audit events for cap hits / settings changes / memory-prompt admin ops (FR-AI25, NFR-AI1, A-OPS).

**Definition of Done:** one named test `TestHealthSplitAndCellEchoAndSLIs` (integration) asserts `/healthz` returns 200 echoing CELL_ID + CLUSTER_NAME (unset ⇒ startup fails), `/readyz` gates on a dead upstream, and the named SLIs + audit events are emitted. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the liveness probe, When `/healthz` is called, Then it returns 200 and echoes both CELL_ID and CLUSTER_NAME; an unset value fails startup, never renders empty. *Assert: echo present; unset ⇒ startup failure.* [Source: FR-AI25, A-OPS]
- [ ] **AC2** — Given a dead upstream, When `/readyz` runs its round-trips, Then it gates traffic (never reads green with a dead dependency). *Assert: dead upstream ⇒ not ready.* [Source: FR-AI25]
- [ ] **AC3** — Given operations, When SLIs emit, Then first-token latency, tool-loop step count/duration, LiteLLM error rate, spend-check staleness, cap-denial count, and usage-event lag feed LGTM. *Assert: the six SLIs exported.* [Source: FR-AI25, NFR-AI1]
- [ ] **AC4** (audit/negative) — Given a cap hit / settings change / memory-prompt admin op, When it happens, Then an audit event is emitted (content-free for secrets). *Assert: audit event present; no secret content.* [Source: FR-AI25]

## 🔨 Tasks

- [ ] RED: `TestHealthSplitAndCellEchoAndSLIs` (AC1, AC2)
- [ ] GREEN: liveness/readiness split + cell-contract echo + config.go required fields (AC1, AC2)
- [ ] GREEN: OTel/Prometheus SLI wiring → LGTM (AC3)
- [ ] RED→GREEN: audit events for cap/settings/admin ops (AC4)

## 🧠 Context

Ops/health tier (both binaries; `cmd/metering` exposes liveness + consumer-lag readiness). Engine analogue: health controllers + OTel instrumentation. Seams: LGTM/Alertmanager, all upstreams. Corrects audit F9/A-OPS.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the A-OPS surface is real — /healthz always-200 liveness echoing BOTH CELL_ID and CLUSTER_NAME, /readyz readiness gate, /metrics prometheus, backed by internal/health + nfr SLO budgets (server.go:124-163); health service_test.go + integration_test.go cover it. NOTE: full OTel-SLI-to-LGTM export + audit-event emission is partial.

## 🧪 Testing

`TestHealthSplitAndCellEchoAndSLIs` (integration). CS §5: mock upstreams for readyz round-trips; never mock ai's own health handlers.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — §10 cell contract, health split. SE-Arch 8sYi523i4t: honest-health lens (dead upstream never green). cell-lint JGAUQRsw2g: CELL_ID + CLUSTER_NAME echo.

## 🔗 References

PRD pbKI3BpQmY §4 F9 FR-AI25, §5 NFR-AI1; Arch Cb0XBkNezd A-OPS.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; cell contract JGAUQRsw2g + SLI catalog freeze there)
- [ ] Parent epic: E8
- [ ] Intra-service: after E8-S1..S4

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
