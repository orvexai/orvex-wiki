## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As an editor user, I want inline AI (`POST /api/ai/inline`) for selection/cursor completions and transforms with the shipped DTO, workspace-gated by the inline feature flag and riding the same metering + PII pipeline as chat, so that in-editor completions work at cutover unchanged (FR-AI7).

**Definition of Done:** one named test `TestInlineCompletionMeteredAndRedacted` (integration) runs an inline transform and asserts the shipped DTO is honored, the feature flag gates access, and the call is metered (E4) and PII-redacted (E5) before reaching LiteLLM. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a selection/cursor request, When `/api/ai/inline` is called, Then it returns completions/transforms per the shipped DTO. *Assert: DTO matches contract.* [Source: FR-AI7]
- [ ] **AC2** — Given the inline feature flag is off for a workspace, When called, Then it is gated (typed refusal). *Assert: flag off ⇒ 403/typed refusal.* [Source: FR-AI7]
- [ ] **AC3** — Given an inline call, When executed, Then it is metered and PII-redacted identically to chat. *Assert: usage recorded; PII middleware ran.* [Source: FR-AI7, FR-AI20]
- [ ] **AC4** (negative) — Given an inline edit that writes, When applied, Then it goes through the wiki-api chokepoint with the caller's token. *Assert: chokepoint call with caller token.* [Source: FR-AI7, FR-AI9]

## 🔨 Tasks

- [ ] RED: `TestInlineCompletionMeteredAndRedacted` (AC1, AC3)
- [ ] GREEN: `/api/ai/inline` handler + shipped DTO (AC1)
- [ ] GREEN: feature-flag gate (AC2)
- [ ] RED→GREEN: metering + PII + chokepoint write (AC3, AC4)

## 🧠 Context

HTTP + domain tiers. Engine analogue: an inline-completion controller. Seams: E4 metering, E5 PII, E2-S2 write door.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — inline AI is real — internal/inline (12 files: prompt shaping, SSE stream, draft slot) serves POST /api/ai/inline + /{draftId}/accept (server.go:310-311) behind edge-auth; inline_route_test.go + stream_test.go cover the selection/cursor completion + transform path.

## 🧪 Testing

`TestInlineCompletionMeteredAndRedacted` (integration). CS §5: mock LiteLLM + wiki-api; never mock ai's own inline service.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: metering-bypass lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F2 FR-AI7; Arch Cb0XBkNezd §3.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; inline DTO freeze there)
- [ ] Parent epic: E3
- [ ] Intra-service: after E4 metering, E5-S4 PII, E2-S2 write door

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
