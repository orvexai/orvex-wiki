## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As every metered request, I want a pre-flight spend check (cached `/key/info` vs cap) that is fast-fail UX with a soft-cap warning, where a spend-lookup failure keeps last-known spend and leans on the backstop — never reading as zero spend — so that the cap can never silently turn off (FR-AI13, NFR-AI4; the named in-fork regression).

**Definition of Done:** one named test `TestSpendCheckNeverZeroOnFailure` (integration) forces a `/key/info` auth/transport failure and asserts the pre-flight keeps last-known spend (does not read 0), leans on layer 1, and raises an operational alert on repeated failure. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a metered request, When pre-flight runs, Then cached spend (short-TTL Redis) is compared to the cap; hard-cap reached ⇒ typed 402-class refusal before any model call. *Assert: over-cap ⇒ 402 pre-model.* [Source: FR-AI13]
- [ ] **AC2** — Given soft-cap percent reached, When checked, Then the request proceeds and a deduplicated `ai.cap.warning` is emitted. *Assert: proceeds + dedup warning.* [Source: FR-AI13]
- [ ] **AC3** — Given a spend-lookup failure, When it occurs, Then last-known spend is kept (never 0) and the request leans on the backstop. *Assert: failed lookup ≠ under-limit.* [Source: FR-AI13, NFR-AI4]
- [ ] **AC4** (alert/negative) — Given repeated lookup failure, When observed, Then an operational alert is raised. *Assert: alert fired after threshold.* [Source: FR-AI13, NFR-AI4]

## 🔨 Tasks

- [ ] RED: `TestSpendCheckNeverZeroOnFailure` (AC3, AC4)
- [ ] GREEN: pre-flight spend check (cached, ≤5ms hit) (AC1)
- [ ] GREEN: soft-cap dedup warning (AC2)
- [ ] RED→GREEN: last-known-spend + alert on repeated failure (AC3, AC4)

## 🧠 Context

Domain/middleware tier on the hot path (NFR-AI1: ≤5ms cache-hit, never a blocking LiteLLM round-trip). Engine analogue: a request guard. Seams: Redis spend cache, LiteLLM `/key/info`, E4-S1/S2 keys.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the pre-flight spend gate is real and fail-closed — budget.Preflight returns a Decision (Quota/Usage/Percent on allow AND deny), a spend/entitlement read failure fails the turn closed (nfr.HonestSpendRead, nfr.go:110), never a fabricated zero; budget_test.go + quota_preflight_test.go cover it.

## 🧪 Testing

`TestSpendCheckNeverZeroOnFailure` (integration with Redis). CS §5: mock LiteLLM `/key/info` (incl. 401/timeout); never mock ai's own guard.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — the zero-on-failure trap is designed out. SE-Arch 8sYi523i4t: silent-cap-off P0 lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F5 FR-AI13, §5 NFR-AI4; Arch Cb0XBkNezd A-METER layer 2, §5 risks.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; 402 code + soft-cap event freeze there)
- [ ] Parent epic: E4
- [ ] Intra-service: after E4-S1/S2/S3

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
