## 🗺️ Feature area

The tool registry is how the model acts on the world, and there is exactly one write door: orvex-wiki-api's block chokepoint, called with the caller's token (FR-AI9, A-TOOLS). ai holds no elevated write credential and never emits raw ProseMirror across a boundary. This epic covers the data-driven registry (block-emission + page-edit tools + web search), output validation, honest quota surfacing, the tools-retry surface, the server-side diagram bake, and the in-editor thin-UI carve-out (D-S4, FR-AI26) where all AI logic lives server-side.

**Why it exists:** a scoped caller must never write through ai what it couldn't write directly, and an engine F-QUOTA 402 must surface verbatim, never be retried-around or papered over by the model (PRD §4 F3; Arch A-TOOLS; NFR-AI5).

## 📋 Story roster

- [ ] E2-S1 — [ai] Data-driven tool registry + bounded in-request loop + tool-output validation
- [ ] E2-S2 — [ai] Page-edit tools via orvex-wiki-api block chokepoint with the caller's token
- [ ] E2-S3 — [ai] F-QUOTA 402 QUOTA_EXCEEDED surfaced verbatim as a typed tool-error frame
- [ ] E2-S4 — [ai] tools-retry surface + SearXNG web-search helper (SSRF-guarded)
- [ ] E2-S5 — [ai] Visual bake + server-side mermaid→drawio/excalidraw conversion; in-editor thin-UI

## 🎯 FR ids owned

FR-AI4, FR-AI9, FR-AI17, FR-AI26, NFR-AI5 (write/degradation path). Seams: wiki-api chokepoint (CAS/receipts/provenance), engine block-schema catalog, engine attachment API, SearXNG. No Linear tool set (D-S11).

## ✅ Epic DoD

- [ ] All 5 stories Done (reviewer ≠ implementer).
- [ ] Family-E2E share green: an AI page edit lands through wiki-api with the caller's token and a read-after-write receipt.
- [ ] SDD lines evidenced: a QUOTA_EXCEEDED write is surfaced verbatim (never partial, never retried-around); no `@docmost/*` import; no raw ProseMirror crosses a boundary.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; QUOTA_EXCEEDED code + block-op contract + attachment seam freeze there).
- [ ] Intra-epic order: E2-S1 → E2-S2 → E2-S3/E2-S4/E2-S5. Cross-epic: consumes E4 metering, E8 auth.

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on blocker.
