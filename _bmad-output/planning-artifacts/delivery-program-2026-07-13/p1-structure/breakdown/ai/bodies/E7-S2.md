## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a chat user, I want per-user + per-space memory CRUD with recency-capped recall into the chat system prompt, opt-in per workspace (GDPR gate — default off ⇒ recall empty, writes rejected), space-scoped visibility following space membership, and recall failures degrading to empty (never breaking a turn), so that chat-recall works as shipped and privacy-safe (FR-AI11).

**Definition of Done:** one named test `TestMemoryRecallGdprGatedAndDegrades` (integration) asserts default-off ⇒ recall empty + writes rejected, that opt-in enables recency-capped recall, that space-scoped visibility follows membership (checked against the engine/knowledge primitive, not a local copy), and that a recall failure degrades to empty without breaking the turn. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the GDPR gate default off, When recall/write is attempted, Then recall returns empty and writes are rejected. *Assert: disabled ⇒ empty recall + write rejection.* [Source: FR-AI11]
- [ ] **AC2** — Given opt-in enabled, When a turn recalls, Then per-user + per-space memories are recalled recency-capped into the system prompt. *Assert: ≤ recency cap; merged into prompt.* [Source: FR-AI11]
- [ ] **AC3** — Given space-scoped memory, When visibility is checked, Then it follows space membership via the engine/knowledge primitive (not a local copy). *Assert: membership checked at source.* [Source: FR-AI11]
- [ ] **AC4** (degrade/negative) — Given a recall failure, When it occurs, Then it degrades to empty and never breaks the chat turn. *Assert: failed recall ⇒ turn continues.* [Source: FR-AI11, NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestMemoryRecallGdprGatedAndDegrades` (AC1, AC4)
- [ ] GREEN: memory CRUD (`ai_memories` chat-recall store) + GDPR opt-in (AC1, AC2)
- [ ] GREEN: space-membership visibility via primitive (AC3)
- [ ] RED→GREEN: recall-failure degradation (AC4)

## 🧠 Context

Store + domain tiers. Engine analogue: a recall service. Seams: engine/knowledge membership primitive, context assembly (E1-S4). This is the ephemeral chat-recall store — distinct from the product Memory SoR (api, AD-1).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — memories are real — memory.Service.Recall (opt-in via ai_memory_settings, space-scoped isolation, degrade-to-empty) + RenderSection, over store/postgres/memory_repo.go and ai_memories (migration 0002); service_test.go + memory_integration_test.go cover recall + opt-in + isolation.

## 🧪 Testing

`TestMemoryRecallGdprGatedAndDegrades` (integration). CS §5: mock the membership primitive; never mock ai's own memory store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — GDPR gate, degrade-to-empty. SE-Arch 8sYi523i4t: privacy-default + degradation lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F4 FR-AI11; Arch Cb0XBkNezd A-DATA, Memory ownership boundary (AD-1).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; memory DTO + membership-primitive seam freeze there)
- [ ] Parent epic: E7
- [ ] Intra-service: after E8-S1 (RLS); feeds E1-S4

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
