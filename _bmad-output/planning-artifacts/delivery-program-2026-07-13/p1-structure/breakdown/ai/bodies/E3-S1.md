## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the ask surface, I want an agentic cited-ask core (`internal/ask`) where retrieval is a model-callable tool run multi-hop (query refinement, follow-up fetches, cross-checking), every hop calling knowledge `retrieve` with the caller's delegated principal, so that the loop moving to ai causes no ACL regression (D-ASK, FR-AI5).

**Definition of Done:** one named test `TestAgenticAskRetrievalDelegated` (integration) runs a multi-hop ask and asserts each knowledge `retrieve` hop carries the caller's delegated principal (not a service credential) and the loop is bounded by the A-TOOLS discipline. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an ask, When the loop runs, Then retrieval is invoked as a tool the model calls (multi-hop), not a fixed retrieve-then-stuff pipeline. *Assert: ≥1 model-driven retrieve hop possible; loop is agentic.* [Source: FR-AI5]
- [ ] **AC2** — Given each retrieval hop, When it calls knowledge, Then the caller's delegated principal is forwarded and ACL ∩ token-scope is enforced at knowledge's chokepoint. *Assert: retrieve called with caller principal.* [Source: FR-AI5]
- [ ] **AC3** — Given ranked chunks from knowledge, When consumed, Then ai adds only prompt-side logic (no re-rank — ranking is knowledge's, D-S14). *Assert: no re-ranking step in ai.* [Source: FR-AI5]
- [ ] **AC4** (negative) — Given knowledge returns an error, When the hop fails, Then the loop degrades to a typed error / un-grounded refusal, never a fabricated cited answer. *Assert: no fabricated citation.* [Source: FR-AI5, NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestAgenticAskRetrievalDelegated` (AC1, AC2)
- [ ] GREEN: `internal/ask` agentic loop with retrieval-as-tool (AC1)
- [ ] GREEN: delegated-principal forwarding to knowledge (AC2)
- [ ] RED→GREEN: knowledge-down degradation (AC4)

## 🧠 Context

Domain tier — the single `internal/ask` package (one core, both front doors). Engine analogue: a RAG orchestration service. Seams: knowledge `retrieve` (delegated), E2-S1 bounded loop, E3-S2 synthesis.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the cited-ask core is real — ask.Service.Ask (ask.go:179) drives retrieval (RetrieveCitations, clients.Knowledge.Retrieve under the caller's principal), similarity-band splitting and the K5 envelope; ask_test.go covers it. NOTE: retrieval is a single fused pass rather than a fully agentic multi-hop tool loop.

## 🧪 Testing

`TestAgenticAskRetrievalDelegated` (integration). CS §5: mock knowledge (external sibling) via contract; never mock ai's own ask package.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: ACL-regression + no-confabulation lens; single-owner (no door-private grounding). cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F2 FR-AI5 (D-ASK, D-S14); Arch Cb0XBkNezd A-ASK.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; knowledge retrieve seam freeze there)
- [ ] Parent epic: E3
- [ ] Intra-service: after E2-S1 (bounded loop), before E3-S2/S3

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
