## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As an image consumer, I want image generation via LiteLLM `/images/generations` with the tenant's configured image model — one helper shared by the REST endpoint (`POST /api/ai/images/generate`), the chat `emit-image-from-prompt` tool, and the MCP `generate_image` tool — storing images via the engine attachment API under the caller's token, gated as a paid-only entitlement (FR-AI16).

**Definition of Done:** one named test `TestImageGenPaidGatedOneHelper` (integration) asserts the three call-sites share one helper, images store via the attachment API under the caller's token, and a Free-tier tenant's image-gen request is denied (paid-only). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an image request, When generated, Then it routes through LiteLLM `/images/generations` with the tenant's image model via the shared helper. *Assert: one helper serves REST + tool + MCP.* [Source: FR-AI16]
- [ ] **AC2** — Given a generated image, When stored, Then it uses the engine attachment API under the caller's token. *Assert: attachment stored with caller token.* [Source: FR-AI16]
- [ ] **AC3** — Given a Free-tier tenant, When requesting image-gen, Then it is denied (paid-only entitlement, billing-gated). *Assert: Free image-gen ⇒ 402 paid-only.* [Source: FR-AI16, FR-AI12]
- [ ] **AC4** (negative) — Given a generation failure, When it occurs, Then a typed error is returned and no partial attachment is written. *Assert: no attachment on failure.* [Source: FR-AI16, NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestImageGenPaidGatedOneHelper` (AC1, AC3)
- [ ] GREEN: shared image helper (REST + chat tool + MCP tool) (AC1)
- [ ] GREEN: attachment storage under caller token (AC2)
- [ ] RED→GREEN: paid-only gate + typed failure (AC3, AC4)

## 🧠 Context

Domain + adapter tiers. Engine analogue: an image-generation service. Seams: LiteLLM images, engine attachment API, entitlement gate (E4-S5). Metered + PII-redacted like any model call.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — image generation is real — internal/image (service/handler/decode) does text-to-image via LiteLLM, exposed at POST /api/ai/images/generate (server.go:332) paid-gated through Billing, with the emit_image_from_prompt block default; handler_test.go + service_test.go cover it. NOTE: the MCP generate_image tool wiring lives in the sibling orvex-studio-mcp repo (handler.go:26-28).

## 🧪 Testing

`TestImageGenPaidGatedOneHelper` (integration). CS §5: mock LiteLLM images + attachment API + entitlement; never mock ai's own helper.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — one helper (DRY), paid-gate. SE-Arch 8sYi523i4t: entitlement-gate lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F6 FR-AI16; Arch Cb0XBkNezd §3.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; images DTO + attachment seam freeze there)
- [ ] Parent epic: E5
- [ ] Intra-service: after E5-S1 (gateway), E4-S5 (paid-gate)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
