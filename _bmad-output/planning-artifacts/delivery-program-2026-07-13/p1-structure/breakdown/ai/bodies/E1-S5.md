## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the platform, I want SSE turns to survive pod restarts of other replicas with no sticky-session correctness dependency, and a severed stream to leave persisted state consistent, so that any pod is killable mid-turn (NFR-AI2, NFR-AI6).

**Definition of Done:** one named test `TestSeveredStreamHealsConsistent` (integration) starts a streaming turn, kills the stream mid-flight, runs the reconciler, and asserts the persisted turn is a consistent state with no orphaned partial message. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an in-flight turn, When the client stream is severed, Then the turn ledger is persisted incrementally so no state is lost. *Assert: ledger rows written per frame batch.* [Source: NFR-AI2]
- [ ] **AC2** — Given a half-written turn, When the reconciler runs, Then the turn is healed to a consistent terminal or reverted state. *Assert: no partial message remains post-reconcile.* [Source: NFR-AI2]
- [ ] **AC3** — Given a turn served by replica A, When replica A dies and the client reconnects to replica B, Then correctness holds without sticky sessions. *Assert: no sticky-session assumption in routing.* [Source: NFR-AI2]
- [ ] **AC4** (negative) — Given Redis loss during a turn, When it occurs, Then it is a performance/staleness event within NFR-AI4 bounds, never a correctness event. *Assert: durable state in Postgres remains authoritative.* [Source: NFR-AI6]

## 🔨 Tasks

- [ ] RED: `TestSeveredStreamHealsConsistent` (AC1, AC2)
- [ ] GREEN: incremental turn-ledger persistence (AC1)
- [ ] GREEN: reconciler heal logic (AC2)
- [ ] RED→GREEN: Redis-loss tolerance assertion (AC4)

## 🧠 Context

Store + streaming tiers; house rule — plain Deployment, Knative never fronts streaming. Engine analogue: a resumable stream service. Seams: Postgres (durable), Redis (buffers). Sibling deps: E1-S1/S3, E8-S2 (Redis).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the incremental turn ledger (ai_chat_messages append via ChatRepo.SaveMessage) plus the reconciler that heals severed turns are real and exercised by chat_reconcile_integration_test.go.

## 🧪 Testing

`TestSeveredStreamHealsConsistent` (integration with real ephemeral Postgres + Redis). CS §5: no external model mock needed beyond a stubbed frame source; never mock ai's own store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — statelessness of serving tier. SE-Arch 8sYi523i4t: crash-consistency + blast-radius lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §5 NFR-AI2, NFR-AI6; Arch Cb0XBkNezd A-STREAM, §1.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate)
- [ ] Parent epic: E1
- [ ] Intra-service: after E1-S1, E1-S3 (reconciler), E8-S2 (Redis)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
