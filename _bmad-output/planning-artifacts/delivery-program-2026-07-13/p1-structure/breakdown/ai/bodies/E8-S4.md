## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a producer and consumer on the event spine, I want ai's own producer path outbox-backed (each state change + its CloudEvent commit in one Postgres transaction via `internal/event/`, relay marks published only after broker ack) and the `cmd/metering` consumer idempotent (upsert-on-`ce-id`, counter application keyed on `ce-id`) with `orvexcell` checked and cell mismatch failing closed, so that no event is lost, double-counted, or applied cross-cell (A-OUTBOX, FR-AI14, canon P2, CS §7).

**Definition of Done:** one named test `TestOutboxIdempotentCellGuarded` (integration) asserts a state change + CloudEvent commit atomically, the relay publishes only after ack, a redelivered `ce-id` is a no-op (no double-count), and a foreign-`orvexcell` event is rejected. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a state change with a CloudEvent, When committed, Then both land in one Postgres transaction via the outbox table and the relay marks published only after broker ack. *Assert: atomic commit; published-after-ack.* [Source: FR-AI14]
- [ ] **AC2** — Given `ai.job.*` and `ai.cap.reached` (not loss-tolerant), When produced, Then they ride the outbox. *Assert: these events go through the relay, never direct publish.* [Source: FR-AI14]
- [ ] **AC3** — Given a redelivered event, When `cmd/metering` consumes it, Then upsert-on-`ce-id` + counter-by-`ce-id` make it a no-op (no double-count). *Assert: redelivery ⇒ counters unchanged.* [Source: FR-AI14]
- [ ] **AC4** (cell/negative) — Given a foreign-`orvexcell` event, When consumed, Then it is rejected (fail closed). *Assert: foreign cell ⇒ not applied.* [Source: FR-AI14, NFR-AI3]

## 🔨 Tasks

- [ ] RED: `TestOutboxIdempotentCellGuarded` (AC1, AC3, AC4)
- [ ] GREEN: `internal/event/` outbox table + relay (publish-after-ack) (AC1, AC2)
- [ ] GREEN: consumer upsert-on-ce-id + counter-by-ce-id (AC3)
- [ ] RED→GREEN: orvexcell check fails closed (AC4)

## 🧠 Context

Event tier — every producer outbox-backed (canon P2, not only the engine's). Engine analogue: transactional-outbox + idempotent consumer. Seams: Kafka spine, Postgres outbox. `ce-id` dedup-window duration is an OPEN DECISION (audit page) — TBD by the pack.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the A-OUTBOX spine is real — event.Publisher writes to ai_event_outbox in-txn (migration 0003), event.KafkaRelay drains to the spine (startOutboxRelay, cmd/server/main.go:451), metering is upsert-on-ce-id idempotent, and Handlers fail closed on orvexcell CELL_ID mismatch; event_test.go + kafka_consumer_test.go cover it.

## 🧪 Testing

`TestOutboxIdempotentCellGuarded` (integration with Kafka + Postgres). CS §5: real Kafka/Postgres ephemeral; never mock ai's own outbox/consumer.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — §7 outbox/CloudEvents, §10 cell contract. SE-Arch 8sYi523i4t: loss/double-count/cross-cell lens. cell-lint JGAUQRsw2g: `orvexcell` fail-closed.

## 🔗 References

PRD pbKI3BpQmY §4 F5 FR-AI14; Arch Cb0XBkNezd A-OUTBOX; audit oaD5XUHqfb (F2/F3).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; CloudEvent catalog + ce-id dedup-window freeze there)
- [ ] Parent epic: E8
- [ ] Intra-service: after E8-S2; underpins E4-S6, E6-S3

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
