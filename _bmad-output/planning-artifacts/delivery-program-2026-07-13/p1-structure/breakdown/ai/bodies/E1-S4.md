## 🎯 Story

As a chat turn, I want context assembled from the prompt library, recalled memories, page/selection context (as DfM via wiki-api reads), and RAG citations from knowledge `retrieve` under the caller's delegated principal, so that answers are grounded without any ACL regression (FR-AI3).

**Definition of Done:** one named test `TestTurnContextAssembly` (integration) builds a turn context and asserts it composes system prompt + capped memories + DfM page context + knowledge citations, with the knowledge call carrying the caller's delegated principal (never a service credential). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a turn with a page reference, When context is assembled, Then page context is fetched as DfM via a wiki-api read (never raw ProseMirror, never a direct engine DB path). *Assert: wiki-api read called; no ProseMirror in the assembled context.* [Source: FR-AI3]
- [ ] **AC2** — Given RAG is needed, When knowledge `retrieve` is called, Then the caller's delegated principal is forwarded and ACL ∩ token-scope is enforced at knowledge's chokepoint. *Assert: retrieve called with caller principal; not a service key.* [Source: FR-AI3]
- [ ] **AC3** — Given memories are recalled, When composed, Then recall is recency-capped and merged into the system prompt. *Assert: ≤ cap memories injected.* [Source: FR-AI3, FR-AI11]
- [ ] **AC4** (negative) — Given knowledge is down, When retrieval fails, Then the turn degrades to a typed error/un-grounded path, never a fabricated citation. *Assert: no citations frame with fabricated sources.* [Source: FR-AI3, NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestTurnContextAssembly` (AC1, AC2)
- [ ] GREEN: context assembler composing prompt+memories+DfM+citations (AC1–AC3)
- [ ] RED→GREEN: knowledge-down degradation path (AC4)
- [ ] Wire delegated-principal forwarding (AC2)

## 🧠 Context

Domain/orchestration tier feeding the E1-S1 stream. Seams: wiki-api DfM reads, knowledge `retrieve` (delegated), prompt library (E7-S1), memories (E7-S2). Engine analogue: a context-composition service.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — per-turn memory recall is wired into chat (chat.Service.MemoryProvider seam, service.go:46-62, backed by memory.Service.Recall/RenderSection). MISSING: DfM page-context injection and RAG citation composition on the chat context-assembly path (RAG/citations live only in the separate ask core, internal/ask).

## 🧪 Testing

`TestTurnContextAssembly` (integration). CS §5: mock knowledge + wiki-api (external siblings) via their contracts; never mock ai's own assembler or store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: ACL-regression lens (delegated principal), no-confabulation lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F1 FR-AI3, F4 FR-AI11; Arch Cb0XBkNezd A-ASK, A-AUTH.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; knowledge retrieve seam + wiki-api DfM read seam freeze there)
- [ ] Parent epic: E1
- [ ] Intra-service: after E7-S1/S2 (prompt+memory), E8-S3 (auth pass-through)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
