## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a workspace admin, I want workspace-scoped prompt CRUD (`ai_prompts` semantics), referenced by chat/inline turns and rendered into the system prompt, with admin-gated management, so that the prompt library survives cutover unchanged (FR-AI10).

**Definition of Done:** one named test `TestPromptLibraryRenderedIntoSystemPrompt` (integration) creates a workspace prompt, asserts admin-gated management, and asserts a chat turn renders the referenced prompt into its composed system prompt. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given prompt CRUD, When called, Then prompts are workspace-scoped and persisted per the shipped semantics. *Assert: workspace-scoped rows; DTO parity.* [Source: FR-AI10]
- [ ] **AC2** — Given a referenced prompt, When a chat/inline turn runs, Then it is rendered into the composed system prompt. *Assert: rendered content present in system prompt.* [Source: FR-AI10]
- [ ] **AC3** — Given prompt management, When mutated, Then it is admin-gated. *Assert: non-admin ⇒ typed refusal.* [Source: FR-AI10]
- [ ] **AC4** (negative) — Given another workspace's prompt, When referenced, Then it is not visible. *Assert: cross-workspace isolation.* [Source: FR-AI10, NFR-AI3]

## 🔨 Tasks

- [ ] RED: `TestPromptLibraryRenderedIntoSystemPrompt` (AC1, AC2)
- [ ] GREEN: prompt CRUD store + admin gate (AC1, AC3)
- [ ] GREEN: render into system-prompt composition (AC2)
- [ ] RED→GREEN: cross-workspace isolation (AC4)

## 🧠 Context

Store + HTTP tiers, feeding context assembly (E1-S4). Engine analogue: a prompt-library service. Seams: Postgres (E8-S1), lib/auth admin gate (E8-S3).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the prompt library is real — library.PromptService Create/List/Update/Delete/Apply (workspace-scoped) over store/postgres/prompt_repo.go and the ai_prompt_library table (migration 0002); library_test.go covers CRUD + apply/render.

## 🧪 Testing

`TestPromptLibraryRenderedIntoSystemPrompt` (integration against ephemeral Postgres). CS §5: no external mocks; never mock ai's own store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — workspace scoping, admin gate. SE-Arch 8sYi523i4t: isolation lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F4 FR-AI10; Arch Cb0XBkNezd A-DATA.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; prompts DTO freeze there)
- [ ] Parent epic: E7
- [ ] Intra-service: after E8-S1 (RLS)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
