## 🎯 Story

As the Prompt Composer, I want a backend that composes a marketplace skill + plain-language tweaks + the user's Memory + wiki knowledge (retrieved via RAG over the ask core, under the caller's delegated principal) into a prompt that works in any AI, so that consumption — the payoff — is server-side in ai (Brief §Prompt Composer, D-ASK/FR-AI5).

**Definition of Done:** one named test `TestComposeSkillMemoryRag` (integration) composes a prompt from a skill + Memory + a wiki-RAG retrieval and asserts the retrieval runs through the ask core with the caller's delegated principal (no ACL regression) and the output is a portable prompt. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.* Note: composition API surface + the api Memory-SoR read seam are TBD — pinned by the Definition Pack (ENG-2097).

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a skill + tweaks + Memory + wiki knowledge, When composed, Then a single portable prompt is produced. *Assert: composed prompt includes all inputs; portable form.* [Source: Brief §Prompt Composer]
- [ ] **AC2** — Given the wiki-knowledge draw, When retrieved, Then it goes through the ask/RAG core (E3) with the caller's delegated principal. *Assert: retrieval via knowledge under caller principal; no ACL regression.* [Source: FR-AI5; Brief]
- [ ] **AC3** — Given the user's Memory, When read, Then it comes from the api Memory SoR (AD-1) / chat-recall as applicable, not a re-implemented store. *Assert: Memory sourced from SoR, not duplicated.* [Source: Map; AD-1]
- [ ] **AC4** (negative) — Given a corpus the caller cannot access, When composing, Then it is never included (ACL enforced at knowledge's chokepoint). *Assert: inaccessible content excluded.* [Source: FR-AI5]

## 🔨 Tasks

- [ ] RED: `TestComposeSkillMemoryRag` (AC1, AC2)
- [ ] GREEN: composition backend (skill + tweaks + Memory + RAG) (AC1)
- [ ] GREEN: RAG via the ask core with delegated principal (AC2)
- [ ] RED→GREEN: Memory-SoR read + ACL exclusion (AC3, AC4)

## 🧠 Context

Domain tier (ai's share). Engine analogue: a composition service. Seams: ask core (E3), knowledge retrieval (delegated), api Memory SoR (AD-1). The marketplace/skill catalog + editor UX live in api/ui.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — no Prompt Composer backend exists — 'composer' appears only as a billing GatedFeature enum name (clients.go:691); nothing composes skill + Memory + wiki RAG into a portable prompt.

## 🧪 Testing

`TestComposeSkillMemoryRag` (integration). CS §5: mock knowledge + api Memory SoR + LiteLLM; never mock ai's own composition backend.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — reuse ask core, no store duplication. SE-Arch 8sYi523i4t: ACL-regression + scope lens. cell-lint JGAUQRsw2g.

## 🔗 References

Brief rgBOQh31p3 §Prompt Composer; current-state-map §2 (Composer backend = ai; Memory SoR = api); PRD pbKI3BpQmY FR-AI5.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; composition API + api Memory-SoR read seam freeze there)
- [ ] Parent epic: E9
- [ ] Intra-service: after E3 (ask core), E7-S2 (recall)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
