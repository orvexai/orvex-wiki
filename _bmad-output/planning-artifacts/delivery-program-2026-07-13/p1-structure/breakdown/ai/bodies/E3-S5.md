## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As an AI draft consumer, I want a per `{workspace, user, page}` ephemeral draft slot with server-controlled TTL (the `ai_drafts` semantics — save/read/clear), so that expired drafts become invisible and the draft slot behaves exactly as shipped (FR-AI8).

**Definition of Done:** one named test `TestAIDraftSlotTTL` (integration) saves a draft, reads it back, advances past TTL, and asserts the expired draft is invisible on read and clear is idempotent. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a save to the draft slot, When `POST /api/ai/draft` is called, Then the draft is stored keyed by `{workspace,user,page}` with a server-controlled TTL. *Assert: row keyed correctly; TTL set server-side.* [Source: FR-AI8]
- [ ] **AC2** — Given a stored draft, When `GET /api/ai/draft` is called before TTL, Then it is returned. *Assert: read returns the draft.* [Source: FR-AI8]
- [ ] **AC3** — Given a draft past TTL, When read, Then it is invisible. *Assert: expired ⇒ empty/404.* [Source: FR-AI8]
- [ ] **AC4** (negative) — Given another user's draft slot, When read, Then it is not visible. *Assert: cross-user isolation (per {workspace,user,page}).* [Source: FR-AI8, NFR-AI3]

## 🔨 Tasks

- [ ] RED: `TestAIDraftSlotTTL` (AC1, AC3)
- [ ] GREEN: draft store keyed by {workspace,user,page} + server TTL (AC1, AC2)
- [ ] GREEN: expiry invisibility + clear (AC3)
- [ ] RED→GREEN: cross-user isolation (AC4)

## 🧠 Context

Store + HTTP tiers. Engine analogue: an ephemeral slot service. Seams: Postgres (E8-S1). TTL is server-controlled (never client).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the AI draft slot is real — inline.Draft lifecycle (accept/discard/partial, keyed by {workspace,user,page}), persisted via store/postgres/draft_repo.go over migration 0002_ai_inline_drafts, with TTL cleanup via the orphan/retention sweeps; draft_repo_integration_test.go covers it.

## 🧪 Testing

`TestAIDraftSlotTTL` (integration against ephemeral Postgres). CS §5: no external mocks; never mock ai's own store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — server-controlled TTL, tenant/user isolation. SE-Arch 8sYi523i4t: isolation lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F2 FR-AI8; Arch Cb0XBkNezd A-DATA.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; draft DTO freeze there)
- [ ] Parent epic: E3
- [ ] Intra-service: after E8-S1 (RLS repository)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
