## 🎯 Story

As a workflows activity, I want `POST /internal/v1/steps/tool-exec` — one validated tool execution, including chokepoint writes on the caller's behalf — idempotent, `WorkloadIdentityOnly`, and metered, so that durable loops execute tools through the same validation + one-write-door discipline as interactive turns (FR-AI21).

**Definition of Done:** one named test `TestStepToolExecChokepointIdempotent` (integration) runs a tool-exec step that performs a chokepoint write, asserts the write carries the resolved caller authority (not a raw token), validation ran, and a replay is a no-op. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tool-exec step, When run, Then the tool output is validated before any write or return. *Assert: invalid output ⇒ typed terminal error.* [Source: FR-AI21]
- [ ] **AC2** — Given a chokepoint write, When performed, Then it goes through wiki-api with the resolved caller authority (E6-S4 delegation), never an elevated ai credential. *Assert: write carries delegated caller authority.* [Source: FR-AI21, FR-AI9]
- [ ] **AC3** — Given a replayed idempotency key, When called again, Then the step is a no-op returning the prior receipt. *Assert: replay ⇒ no duplicate write.* [Source: FR-AI21]
- [ ] **AC4** (negative) — Given a bearer token, When presented on this route, Then it is rejected (`WorkloadIdentityOnly`). *Assert: bearer ⇒ 401/403.* [Source: FR-AI21]

## 🔨 Tasks

- [ ] RED: `TestStepToolExecChokepointIdempotent` (AC2, AC3)
- [ ] GREEN: tool-exec step over the registry (E2-S1) + chokepoint (E2-S2) (AC1, AC2)
- [ ] GREEN: idempotency-key replay (AC3)
- [ ] RED→GREEN: WorkloadIdentityOnly (AC4)

## 🧠 Context

Internal HTTP + domain/adapter tier. Engine analogue: an internal step controller. Seams: registry (E2-S1), chokepoint (E2-S2), delegation resolution (E6-S4), metering (E4).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** absent — the durable tool-exec step is an honest 501 stub — server.go:350 mux.HandleFunc('/internal/v1/steps/tool-exec', todo('validated tool execution (FR-AI21)')); no step handler exists.

## 🧪 Testing

`TestStepToolExecChokepointIdempotent` (integration). CS §5: mock wiki-api chokepoint; never mock ai's own step/registry.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — validation, one write door, idempotency. SE-Arch 8sYi523i4t: privilege + replay lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F8 FR-AI21, F3 FR-AI9; Arch Cb0XBkNezd A-LOOP, A-TOOLS.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; step-API + delegation-token shapes freeze there)
- [ ] Parent epic: E6
- [ ] Intra-service: after E2-S1/S2, E6-S4 (delegation), E4 (metering)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
