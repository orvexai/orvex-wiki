## 🎯 Story

As the journal + cache tier, I want the AI usage/event journal (`ai_usage_journal`) and durable-loop `step_ledger` as range-partitioned Postgres append tables (retention = DROP PARTITION, Mongo struck D-S12) with a partition-management cronjob, plus Redis for spend/entitlement caches, counters, idempotency keys, and relay buffers — every key tenant-embedded, allow/deny never cached — so that journaling and fast state scale without Mongo (FR-AI23).

**Definition of Done:** one named test `TestPartitionedJournalAndTenantEmbeddedRedis` (integration) asserts the append journal writes to the correct time partition, the partition cronjob creates/drops partitions per retention, and every Redis key is tenant-embedded with no allow/deny cached. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a usage event, When journaled, Then it appends to the correct range partition of `ai_usage_journal` (no Mongo). *Assert: row in the time partition.* [Source: FR-AI23]
- [ ] **AC2** — Given retention, When the partition cronjob runs, Then old partitions are dropped and new ones created. *Assert: DROP PARTITION per retention; future partition exists.* [Source: FR-AI23]
- [ ] **AC3** — Given Redis usage, When keys are written, Then every key is tenant-embedded and allow/deny is never cached. *Assert: keys carry tenant; no allow/deny key.* [Source: FR-AI23, NFR-AI3]
- [ ] **AC4** (negative) — Given Redis loss, When it occurs, Then it is a performance/staleness event within NFR-AI4 bounds, never a correctness event. *Assert: durable Postgres state remains authoritative.* [Source: FR-AI23, NFR-AI6]

## 🔨 Tasks

- [ ] RED: `TestPartitionedJournalAndTenantEmbeddedRedis` (AC1, AC3)
- [ ] GREEN: range-partitioned append tables + step_ledger + RLS (AC1)
- [ ] GREEN: partition-management cronjob (AC2)
- [ ] GREEN: Redis caches/counters/idempotency/relay, tenant-embedded (AC3, AC4)

## 🧠 Context

Store + ops tier. Engine analogue: partitioned append tables + a Redis cache layer. Seams: Postgres (partitions), Redis. Feeds metering (E4-S6) and step-ledger (E6).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — the range-partitioned usage journal (ai_usage_journal PARTITION BY RANGE, migration 0003) and the Redis caches (internal/cache) are real. MISSING: the step-ledger append tables (no such migration) and the ahead-of-need partition cronjob — migration 0003 notes the monthly-partition job is 'a future ticket' and ships only a DEFAULT partition.

## 🧪 Testing

`TestPartitionedJournalAndTenantEmbeddedRedis` (integration against ephemeral Postgres + Redis). CS §5: no external mocks; never mock ai's own store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — partitioned append, Mongo struck, tenant-embedded keys. SE-Arch 8sYi523i4t: retention + isolation lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F9 FR-AI23 (D-S12); Arch Cb0XBkNezd A-DATA, A-DRIFT (F-JOURNAL).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate)
- [ ] Parent epic: E8
- [ ] Intra-service: after E8-S1; feeds E4-S6, E6

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
