## 🎯 Story

As the token-cost owner (F8), I want memory injection token-budgeted (only the most relevant memories enter a prompt) with ≥ 50% token reduction vs naive full-context at no measurable loss of answer-correctness, measured/optionally-surfaced cost reduction, warm-cached user memory profiles, and a first-class inbound import path (ChatGPT/Claude/Gemini/Grok exports screened by the Personal-Data Guard through the propose-and-confirm gate), so that memory is cheap, fast, and importable (FR-C1, FR-C2, FR-C3, FR-O2).

**Definition of Done:** one named test `TestTokenBudgetedInjectionAndImport` (integration + eval) asserts ≥ 50% token reduction vs naive injection at no correctness loss on the eval set, and that an uploaded provider export is screened and routed through propose-and-confirm. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.* [ASSUMPTION: ≥50% reduction + async/lossy provider export — pinned by the eval set / import contract.]

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a prompt needing memory, When injected, Then only the most relevant memories enter, achieving ≥ 50% token reduction vs naive full-context at no measurable correctness loss. *Assert: reduction ≥ 50% && correctness unchanged.* [Source: FR-C1]
- [ ] **AC2** — Given injection, When measured, Then token/cost reduction is measured and optionally surfaced. *Assert: reduction metric recorded/surfaceable.* [Source: FR-C2]
- [ ] **AC3** — Given user memory profiles, When accessed, Then they are precomputed / warm-cached to hold latency and cost. *Assert: profile served from warm cache.* [Source: FR-C3]
- [ ] **AC4** — Given a sanctioned provider export upload, When imported, Then it is screened by the Personal-Data Guard and routed through the propose-and-confirm gate; the async/lossy "request → come back → re-request" UX is handled. *Assert: import screened + gated; async states handled.* [Source: FR-O2]

## 🔨 Tasks

- [ ] RED: `TestTokenBudgetedInjectionAndImport` (AC1, AC4)
- [ ] GREEN: relevance-ranked token-budgeted injection + reduction eval (AC1, AC2)
- [ ] GREEN: warm-cached user memory profiles (AC3)
- [ ] GREEN: inbound import path (upload-archive) + PII Guard + propose-confirm (AC4)

## 🧠 Context

Domain + cache + import tiers. Engine analogue: a context-budgeting + import service. Seams: Personal-Data Guard, propose-and-confirm (E7-S5), knowledge indexing (import indexing lands in knowledge; ETL split with api Curator). Owned slice F8 (token-cost) + F0 import.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — token-budgeted memory injection is partially real — RenderSection applies per-memory + total-payload caps (service.go:79, types.go). MISSING: the >=50% injection-ratio measurement/surfacing, the warm-cache path, and inbound memory import — none exist.

## 🧪 Testing

`TestTokenBudgetedInjectionAndImport` (integration + eval fixtures). CS §5: mock provider-export fixtures + PII Guard; never mock ai's own budgeting/import.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — token budgeting, screened import. SE-Arch 8sYi523i4t: cost + import-safety lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY memory gap-closure FR-C1/FR-C2/FR-C3/FR-O2; source-of-record g9vWbSYplh; map vBvVDFklZo; Map §2 (import ETL split api/knowledge).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; import contract + reduction baseline + PII Guard seam freeze there)
- [ ] Parent epic: E7
- [ ] Intra-service: after E7-S2/S3/S5

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
