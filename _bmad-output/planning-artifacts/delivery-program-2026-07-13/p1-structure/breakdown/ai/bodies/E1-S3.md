## 🎯 Story

As the platform, I want async auto-titling, history TTL expiry, and the message-reconciler + cost-backfill maintenance jobs to run as central Temporal schedules calling ai step-APIs, so that chat housekeeping never runs an in-service worker (D-WF-1, FR-AI2).

**Definition of Done:** one named test `TestChatMaintenanceStepAPIs` (integration) drives the reconciler and cost-backfill step-APIs against a half-written turn + an unbilled message and asserts the turn is healed and the cost row backfilled idempotently. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a new session's first turn, When titling runs async, Then a model-generated title is set without blocking the turn. *Assert: title populated post-turn; turn latency unaffected.* [Source: FR-AI2]
- [ ] **AC2** — Given a session past its history TTL, When the expiry schedule runs, Then expired history is removed per the TTL policy. *Assert: expired rows gone; within-TTL rows retained.* [Source: FR-AI2]
- [ ] **AC3** — Given a half-written turn, When the reconciler step runs, Then the turn is healed to a consistent state. *Assert: no orphaned partial message remains.* [Source: FR-AI2, NFR-AI2]
- [ ] **AC4** (idempotency/negative) — Given the cost-backfill step re-runs, When replayed with the same idempotency key, Then it is a no-op. *Assert: cost row count unchanged on replay.* [Source: FR-AI2]

## 🔨 Tasks

- [ ] RED: `TestChatMaintenanceStepAPIs` (AC3, AC4)
- [ ] GREEN: reconciler + cost-backfill + TTL-sweep step handlers (idempotent) (AC2–AC4)
- [ ] GREEN: async auto-titling worker call via step-API (AC1)
- [ ] Wire schedules as workflows inputs (no in-service cron) (AC1–AC3)

## 🧠 Context

Step-API + domain tier; schedules live in orvex-studio-workflows (E6). Engine analogue: a scheduled maintenance service, but externalized. Seam: workflows dispatcher, lib FR-L19 idempotency. Sibling deps: E1-S1/S2, E6 step-APIs.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — history TTL expiry (retention.Run + ChatRepo.DeleteBefore) and the message reconciler (chat_reconcile_integration_test.go) exist. MISSING: async auto-titling and the cost-backfill maintenance pass — no titling or backfill code anywhere in internal/.

## 🧪 Testing

`TestChatMaintenanceStepAPIs` (integration) + unit on TTL policy. CS §5: mock the workflows caller boundary via the step-API contract; never mock ai's own store.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — idempotency (§3/§7), no in-service worker. SE-Arch 8sYi523i4t: durable-vs-interactive lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F1 FR-AI2, §8 F8 FR-AI22; Arch Cb0XBkNezd §1, A-LOOP.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; step-API shapes freeze there)
- [ ] Parent epic: E1
- [ ] Intra-service: after E6-S1/S3 (step-APIs), E1-S2 (state)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
