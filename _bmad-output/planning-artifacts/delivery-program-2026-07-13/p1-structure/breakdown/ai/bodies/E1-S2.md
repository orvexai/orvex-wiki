## 🎯 Story

As a chat user, I want session CRUD, message lifecycle, fork-at-message and branch listing (the shipped migration-005/008 semantics) in ai's own Postgres, so that my conversation history and branches survive cutover unchanged (FR-AI2).

**Definition of Done:** one named test `TestChatBranchTopologyRoundTrip` (integration, store+HTTP) creates a session, appends messages, forks at a message, and asserts the branch listing reproduces the exact parent/child topology after a store round-trip. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a create request, When `/api/ai/chats/create` is called, Then a tenant-scoped session row is written and returned with the shipped DTO. *Assert: row exists under tenant RLS; response DTO matches contract.* [Source: FR-AI2]
- [ ] **AC2** — Given a message thread, When `POST /api/ai/chats/:id/fork` at message M, Then a new branch is created rooted at M and `GET /api/ai/chats/:id/branches` lists it with correct parent links. *Assert: branch topology equals expected DAG.* [Source: FR-AI2]
- [ ] **AC3** — Given chats/{info,update,delete,search}, When each is called, Then it behaves per the shipped contract and is tenant-isolated. *Assert: cross-tenant read returns empty/404.* [Source: FR-AI2, NFR-AI3]
- [ ] **AC4** (negative) — Given a fork at a message from another tenant's session, When attempted, Then it is denied. *Assert: `status==404` (no existence leak).* [Source: FR-AI2, NFR-AI3]

## 🔨 Tasks

- [ ] RED: `TestChatBranchTopologyRoundTrip` (AC2)
- [ ] GREEN: `ai_chats`/`ai_chat_messages` store + repository (tenant GUC) (AC1–AC3)
- [ ] GREEN: fork + branch-listing endpoints (AC2)
- [ ] RED→GREEN: cross-tenant deny (AC4)

## 🧠 Context

Store + domain + HTTP tiers (engine analogue: a repository-backed service). Seam: Postgres via repository (E8-S1). Sibling deps: E1-S1 (turn persistence), E1-S3 (TTL/reconciler). Branch topology is user-visible state (OQ-AI7 ETL is a separate cutover concern).

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — fork-at-message + branch listing + message lifecycle are real (chat/branch.go Fork; ChatRepo.MessageBranch/CopyBranchUpTo/SetActiveBranch/BranchHistory, chat_repo.go:170-210; branch_test.go + chat_repo_integration_test.go). MISSING: session-level CRUD (list/get/delete chats) and search — no SearchChats/ListChats method exists (only ListChatRefs for the orphan sweep).

## 🧪 Testing

`TestChatBranchTopologyRoundTrip` (integration against ephemeral Postgres) + unit tests on topology math. CS §5: never mock ai's own store; use a real ephemeral Postgres. No external mocks needed.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — tiers, RLS repository, typed errors. SE-Arch 8sYi523i4t: tenant-isolation lens (no existence leak). cell-lint JGAUQRsw2g: tenant-embedded keys.

## 🔗 References

PRD pbKI3BpQmY §4 F1 FR-AI2; Arch Cb0XBkNezd A-DATA.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; chats DTO + branch semantics freeze there)
- [ ] Parent epic: E1
- [ ] Intra-service: after E8-S1 (RLS repository)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
