## 🎯 Story

As every model path in the family, I want LiteLLM to be the only gateway (chat/completion/tool-round/synthesis/image) via the tenant's virtual key, with the fast tool-model swap preserved as per-tenant config and `GET /api/ai/models` served, so that no in-fork model path remains and FR-39 closes (FR-AI18, FR-39, D-AI3).

**Definition of Done:** one named test `TestLiteLLMSoleGateway` (integration) asserts every model invocation routes through the LiteLLM port (no direct provider SDK), the `AI_CHAT_TOOL_MODEL` swap is honored per-tenant, and `GET /api/ai/models` returns the tenant's configured models. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given any model call, When routed, Then it goes through the LiteLLM gateway via the tenant's virtual key — no direct provider SDK, no second gateway. *Assert: only the LiteLLM port is used.* [Source: FR-AI18, FR-39]
- [ ] **AC2** — Given a per-tenant tool model config, When a tool round runs, Then the fast tool-model swap is applied. *Assert: tool round uses the configured tool model.* [Source: FR-AI18]
- [ ] **AC3** — Given a tenant, When `GET /api/ai/models` is called, Then its configured models are returned per the wire contract. *Assert: DTO matches contract.* [Source: FR-AI18]
- [ ] **AC4** (negative) — Given LiteLLM is down, When a model call is attempted, Then it fails with a typed error — never a direct-provider fallback. *Assert: no provider fallback path exists.* [Source: FR-AI18, NFR-AI5]

## 🔨 Tasks

- [ ] RED: `TestLiteLLMSoleGateway` (AC1, AC4)
- [ ] GREEN: LiteLLM port/adapter (chat/completion/tool/synthesis/image) (AC1)
- [ ] GREEN: per-tenant tool-model swap + `GET /api/ai/models` (AC2, AC3)
- [ ] RED→GREEN: typed fail-fast on LiteLLM down (AC4)

## 🧠 Context

Adapter tier (LiteLLM = external true-dependency, port+adapter, CS §5 ADR trigger). Engine analogue: an outbound model-gateway client. Seams: LiteLLM, tenant config (E5-S2). A CI grep guards no direct provider SDK.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** partial — the LiteLLM sole-gateway port/adapter is real — internal/litellm (domain) over internal/client/litellm (http/stream/chat/image transport), the only model path (FR-39), with per-feature tool-model swap via provider tags. MISSING: the GET /api/ai/models discovery endpoint — no /api/ai/models route or ListModels handler exists.

## 🧪 Testing

`TestLiteLLMSoleGateway` (integration against LiteLLM stub). CS §5: mock LiteLLM; never mock ai's own gateway adapter.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 — single gateway, ports/adapters, no fallback. SE-Arch 8sYi523i4t: single-point + no-fallback lens. cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F7 FR-AI18, G1/FR-39; Arch Cb0XBkNezd §3, A-GOV (ADR trigger a).

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; LiteLLM external-dependency ADR + models DTO freeze there)
- [ ] Parent epic: E5
- [ ] Intra-service: foundational; before E4-S1 (keys ride this) and E5-S2..S5

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
