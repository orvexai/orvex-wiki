## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As both the React client and orvex-studio-mcp, I want two front doors — `POST /api/ai/ask` (wire-compatible, draft-quarantine) and MCP's AI tools calling ai directly with the forwarded bearer — running the identical FR-AI5 core, so that answer quality can never fork and ai becomes MCP's third upstream (FR-AI6, G4).

**Definition of Done:** one named test `TestAskFrontDoorsShareOneCore` (integration) drives both `/api/ai/ask` and the MCP-direct ask path against the same fixture and asserts an identical K5 shape from the same `internal/ask` core, with draft-quarantine honored (drafts excluded by default; caller's own drafts opt-in via `isDraftCitation`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2097); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a client ask, When `/api/ai/ask` is called, Then it returns the wire-compatible `{answer, citations[], unanswered, gapNote, confidence, followups}`. *Assert: DTO matches contract.* [Source: FR-AI6]
- [ ] **AC2** — Given an MCP AI tool ask, When it calls ai directly with the forwarded bearer, Then ai serves it over the same core (a third MCP upstream, no new route class). *Assert: same core; bearer forwarded.* [Source: FR-AI6]
- [ ] **AC3** — Given draft-quarantine, When retrieving, Then drafts are excluded by default and only the caller's own drafts opt-in via `isDraftCitation`. *Assert: other users' drafts never cited.* [Source: FR-AI6]
- [ ] **AC4** (negative) — Given the same fixture, When both doors run, Then neither owns private grounding logic (no drift). *Assert: structural K5 equality across doors.* [Source: FR-AI6, G4]

## 🔨 Tasks

- [ ] RED: `TestAskFrontDoorsShareOneCore` (AC2, AC4)
- [ ] GREEN: `/api/ai/ask` handler over `internal/ask` (AC1)
- [ ] GREEN: MCP-direct bearer-forwarded path (AC2)
- [ ] RED→GREEN: draft-quarantine + `isDraftCitation` (AC3)

## 🧠 Context

HTTP tier, both doors over one domain core (E3-S1/S2). Engine analogue: two controllers, one service. Seams: MCP (forwarded bearer, closes MCP OQ-M7), knowledge draft-quarantine semantics.

**🧾 Code audit (origin/main @ 6c761ba, 2026-07-14):** present — the /api/ai/ask front door is real — GET /api/ai/ask (server.go:272) behind edge-auth calls orchestrator.RunAskTurn (workflow.go:126: real pre-flight budget gate + scoped-key resolve + ask.Ask); ask_discovery_test.go covers it. NOTE: draft-quarantine of answers and MCP-direct ask/chat/inline/generate wiring live in the sibling orvex-studio-mcp repo (out of this tree).

## 🧪 Testing

`TestAskFrontDoorsShareOneCore` (integration). CS §5: mock knowledge + LiteLLM; never mock ai's own ask core.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: door-drift lens (one core). cell-lint JGAUQRsw2g.

## 🔗 References

PRD pbKI3BpQmY §4 F2 FR-AI6 (D-ASK, G4); Arch Cb0XBkNezd A-ASK.

## 🔗 Dependencies
- [ ] Blocked by: **ENG-2097** (Definition Pack — TAG gate; MCP→ai tool surface + ask DTO freeze there)
- [ ] Parent epic: E3
- [ ] Intra-service: after E3-S1/S2 (core), E8-S3 (auth)

## 📡 Protocol
CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
