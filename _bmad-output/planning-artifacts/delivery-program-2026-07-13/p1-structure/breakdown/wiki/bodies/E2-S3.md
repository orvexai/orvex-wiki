## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **retrieval consumer (knowledge, ai, wiki-api)** I want **the FR-13 ACL evaluation primitive (accessible-space-IDs + `filterAccessiblePageIds`) exposed as the load-bearing read narrow, with the `evalPage` page-level-restriction bug fixed and the changed-pages-since delta pull routed through it** so that **every external read narrows through one ACL and no retrieval path (including the AI delta pull) leaks cross-tenant or restricted pages** (FR-W2, G2).

**Definition of Done:** ONE named test `TestAclDeltaPullNarrowsPageLevel` (integration layer — asserts through the ACL primitive that a page-level-restricted page is excluded from both `filterAccessiblePageIds` and the changed-pages-since delta, closing REM-2). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a caller's principal, When `accessible-space-IDs` + `filterAccessiblePageIds` run, Then only pages the principal may read are returned. *Assert: restricted pages absent.* [Source: FR-W2, FR-13]
- [ ] **AC2** — Given a page-level restriction (not just space-level), When `evalPage` runs, Then it honours the page-level action (the current bug: `evalPage` returns space-level actions and ignores page-level restrictions). *Assert: page-level deny wins over space-level allow.* [Source: FR-W2]
- [ ] **AC3** — Given the changed-pages-since delta pull, When it runs, Then it routes through FR-13, not around it. *Assert: delta excludes restricted pages (REM-2 closed).* [Source: FR-W2]
- [ ] **AC4 (negative)** — Given a cross-tenant page id, When any ACL call runs, Then it is never returned. *Assert: cross-tenant bytes = 0.* [Source: G2]
- [ ] **AC5 (error-path)** — Given a caller with no accessible spaces, When `filterAccessiblePageIds` runs, Then it returns empty, not all. *Assert: empty set (fail-closed).* [Source: FR-W2]

## 🔨 Tasks

- [ ] RED: `TestAclDeltaPullNarrowsPageLevel` with a page-level-restricted fixture (AC2, AC3)
- [ ] GREEN: fix `evalPage` page-level handling; route the delta pull through FR-13 (AC1–AC5)
- [ ] Add a cross-tenant probe to CI (bytes = 0)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — FR-13 ACL primitive: evalPage page-level eval (orvex-permissions.service.ts:101) + batch delta-pull ACL intersection (internal-api filterAccessiblePages); tested.

Tier: engine service/domain (ACL evaluation). Seam crossed: every external retrieval calls this. Sibling dependency: E2-S4 (export scopes by ACL), knowledge (delta pull consumer).

## 🧪 Testing

Named DoD test `TestAclDeltaPullNarrowsPageLevel` at integration tier against real Postgres with RLS. CS §5: real DB; never mock own ACL service.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (authz-correctness, IDOR); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W2 (FR-13); Architecture twQ3BBzpTE §2 A-THIN

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E2**; E2-S1
- Blocks: E2-S4 (export authz), knowledge delta pull, E7-S2 (RLS backstop under this ACL)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
