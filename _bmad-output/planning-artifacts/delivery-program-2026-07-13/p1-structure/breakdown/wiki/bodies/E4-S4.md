## 🎯 Story

As a **platform operator** I want **an O(changed) background reconciliation sweep that corrects Redis fast-counters from truth, with Redis-loss failing CLOSED for storage bytes/files and fail-OPEN for cheap resources, and a `ORVEX_QUOTAS_ENFORCE=warn` calibration rollout with a 200%-of-cap absolute hard stop** so that **counter drift is corrected without O(N-tenants) scans and a Redis reset cannot open free-tier abuse** (FR-W12, A-QUOTA-HARDENING).

**Definition of Done:** ONE named test `TestQuotaFailsClosedForStorageOnRedisLoss` (integration layer — asserts through the quota service that with Redis unavailable, a storage-byte write is rejected (fail-closed) while a cheap page-count write is permitted (fail-open) until the sweep reconciles). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given drifted counters, When the reconciliation sweep runs, Then it corrects them in O(changed), never O(N-tenants). *Assert: sweep touches only changed tenants; counters == truth after.* [Source: FR-W12]
- [ ] **AC2** — Given Redis loss, When a storage-bytes/files write arrives, Then it fails CLOSED (rejected) until reconciled. *Assert: storage write blocked on Redis loss.* [Source: A-QUOTA-HARDENING F6]
- [ ] **AC3** — Given Redis loss, When a cheap-resource (page/member count) write arrives, Then it fails OPEN (permitted). *Assert: page write permitted on Redis loss.* [Source: A-QUOTA-HARDENING F6]
- [ ] **AC4** — Given `ORVEX_QUOTAS_ENFORCE=warn`, When a write exceeds cap during calibration, Then it warns but permits — except a 200%-of-cap absolute hard stop still blocks even in warn mode. *Assert: warn passes ≤200%; blocks at 200%.* [Source: FR-W13]
- [ ] **AC5 (error-path)** — Given the fail-open tradeoff (CS §9 triggers met), When the design lands, Then it is ratified in a filed ADR (A-QUOTA-HARDENING F6). *Assert: ADR-NNNN draft filed (handoff to E9-S2); TBD — pinned by the Definition Pack (ENG-2103) for the registry parent.* [Source: A-QUOTA-HARDENING]

## 🔨 Tasks

- [ ] RED: `TestQuotaFailsClosedForStorageOnRedisLoss` (AC2, AC3)
- [ ] GREEN: implement the O(changed) sweep (worker role) + fail-mode branching + warn-mode + 200% hard stop (AC1, AC4)
- [ ] File the fail-open ADR draft (AC5, coordinate with E9-S2)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** absent — No Redis fast-counter exists, so no O(changed) counter-reconciliation sweep and no fail-closed-storage/fail-open-cheap Redis-loss counter path; the entitlement cache fail-closed is a different seam.

Tier: engine worker (reconciliation sweep) + quota service fail-mode. Seam crossed: Redis counters ↔ Postgres truth. Sibling dependency: E4-S2 (counters), E4-S3 (verdict), E9-S2 (ADR filing).

## 🧪 Testing

Named DoD test `TestQuotaFailsClosedForStorageOnRedisLoss` at integration tier with real Redis toggled off. CS §5: real Redis + Postgres; never mock own sweep/quota service.

## 📏 Guidance

CS §§0/3/4/5/6/7/9/10/11 (esp §9 the ADR trigger); SE-Arch lenses (availability-vs-cost, SPOF); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W12; Architecture twQ3BBzpTE §2 A-QUOTA-HARDENING; ADR-0003 (Nr3WIs0Zpt)

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E4**; E4-S2, E4-S3
- Blocks: quota durability under Redis SPOF

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
