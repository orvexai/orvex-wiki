## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **collaborating user** I want **Yjs/Hocuspocus collab whose server-schema registers every additive node/mark (or Yjs strips them) MINUS all Linear NodeViews (never folded in, D-S11), with the React client rendering ProseMirror natively on the engine API** so that **real-time co-editing preserves all custom content and the client is a first-class consumer of the engine primitives** (FR-W9).

**Definition of Done:** ONE named test `TestCollabSchemaRegistersAdditiveNodesNoLinear` (integration layer — asserts through the Hocuspocus server schema that an additive custom node survives a collab round-trip while a Linear node type is absent from the registry). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a doc with additive custom nodes/marks, When edited over collab, Then the server schema registers each so Yjs does not strip them. *Assert: additive node survives the collab round-trip.* [Source: FR-W9]
- [ ] **AC2** — Given the fork's Linear editor NodeViews, When the fold-in lands, Then they are never registered (D-S11). *Assert: no Linear node type in the server schema.* [Source: FR-W9]
- [ ] **AC3** — Given the React client, When it renders, Then it renders ProseMirror natively on the engine API (not behind wiki-api). *Assert: client talks to the engine API directly for editing.* [Source: FR-W9]
- [ ] **AC4** — Given a collab write, When persisted, Then it funnels through the same block chokepoint + quota check as REST. *Assert: collab persistence hits the chokepoint (cross-check E2-S2/E4-S2).* [Source: FR-W9]
- [ ] **AC5 (error-path)** — Given an unregistered node arriving over collab, When persisted, Then it round-trips via the DfM opaque fence rather than being dropped. *Assert: unknown node preserved via `:::dfm-opaque` (cross-check E3-S1).* [Source: FR-W9, FR-W18]

## 🔨 Tasks

- [ ] RED: `TestCollabSchemaRegistersAdditiveNodesNoLinear` (AC1, AC2)
- [ ] GREEN: register additive nodes/marks in the collab server schema; exclude Linear NodeViews; wire the client to the engine API (AC3)
- [ ] Route collab persistence through the chokepoint + quota (AC4, AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — Yjs/Hocuspocus server schema built from the shared tiptap extensions (yjs.util.ts:22, collaboration.util.ts:144); collab handler + persistence specs; React client ProseMirror editors present under apps/client/src/ee.

Tier: engine collab (`collaboration/*` files, 13-row allow-list) + React client. Seam crossed: Yjs persistence ↔ chokepoint. Sibling dependency: E2-S2 (chokepoint), E4-S2 (quota on collab path), E3-S1 (DfM), E6-S2 (client session).

## 🧪 Testing

Named DoD test `TestCollabSchemaRegistersAdditiveNodesNoLinear` at integration tier against a real Hocuspocus + Postgres. CS §5: real collab server; never mock own schema/chokepoint.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (schema-completeness, chokepoint-funnel); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W9; Architecture twQ3BBzpTE §1 (collab role)

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E8**; E2-S2, E6-S2
- Blocks: E4-S2 (collab quota path), E8-S2 (client affordances)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
