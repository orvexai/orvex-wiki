## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **studio-spine consumer** I want **a worker-role relay that drains the `orvex_outbox` and publishes each row straight to Kafka as a CloudEvent (`partitionkey = workspaceId`), using the engine's OWN small TypeScript envelope builder that conforms to the contracts golden fixtures** so that **the transactional outbox + relay IS the emission mechanism (no Redis→Kafka bridge) and the AGPL engine never imports the Apache-2.0 contracts helper across the license boundary** (FR-W5, A-EVENTS, A-SEAMS).

**Definition of Done:** ONE named test `TestRelayEnvelopeConformsToGoldenFixtures` (contract+integration layer — asserts through the relay that a drained outbox row publishes a CloudEvent whose envelope matches the contracts golden fixtures byte-for-byte, with `partitionkey = workspaceId`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given committed outbox rows, When the worker-role relay runs, Then each is published to Kafka as a CloudEvent with `partitionkey = workspaceId`. *Assert: message on the spine; partition key == workspaceId.* [Source: FR-W5]
- [ ] **AC2** — Given the engine is AGPL, When it builds the envelope, Then it uses its OWN `orvex/*` TS envelope builder (not the Apache-2.0 contracts helper) conforming to the frozen wire-names. *Assert: no cross-boundary import; envelope matches golden fixtures.* [Source: A-SEAMS, A-CELL rule #2]
- [ ] **AC3** — Given a `wiki.page.*` event (created/updated/moved/deleted, provenance), When published, Then it is a typed entry in the contracts CloudEvent catalog. *Assert: event name ∈ catalog; round-trips against fixtures.* [Source: A-SEAMS F12]
- [ ] **AC4 (negative)** — Given the retired Redis→Kafka bridge, When the relay lands, Then no bridge and no Redis-Streams stage exists. *Assert: no bridge component; direct outbox→Kafka only (D-S13).* [Source: A-EVENTS]
- [ ] **AC5 (error-path)** — Given Kafka unavailable, When the relay drains, Then rows stay unpublished and are retried (at-least-once), never lost. *Assert: unpublished rows persist + retry; cursor not advanced past unacked.* [Source: FR-W5]

## 🔨 Tasks

- [ ] RED: `TestRelayEnvelopeConformsToGoldenFixtures` (AC1, AC2)
- [ ] GREEN: implement the worker relay + own envelope builder; wire to Kafka direct; catalog the `wiki.page.*` events (AC3)
- [ ] Retry/at-least-once on Kafka loss (AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — Worker relay drains outbox to a real CloudEvents 1.0 structured-mode envelope on Kafka, exactly-once with crash-retry idempotency (spec AC3/AC4), envelope conforms to pinned _envelope.json.

Tier: engine worker (relay) + envelope builder in `orvex/*`. Seam crossed: outbox → Kafka spine. Sibling dependency: E5-S1 (outbox rows), E3 (serialized content), E5-S3 (cell attrs/topics).

## 🧪 Testing

Named DoD test `TestRelayEnvelopeConformsToGoldenFixtures` at contract+integration tier against real Kafka. CS §5: Kafka is an external boundary (real container); golden fixtures pin the envelope; never mock own relay.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp §7 fixtures); SE-Arch lenses (AGPL-boundary, at-least-once); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W5; Architecture twQ3BBzpTE §2 A-EVENTS/A-SEAMS; ADR-0001 (5uf23v6wIW)

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (CloudEvent catalog + golden-fixtures tag is the gate); parent epic **E5**; E5-S1, E3-S1
- Blocks: knowledge indexing, billing/quota fan-out

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
