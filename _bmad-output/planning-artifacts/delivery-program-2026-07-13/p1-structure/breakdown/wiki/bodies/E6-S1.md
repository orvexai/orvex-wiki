## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **licensing owner** I want **a clean-room AGPL api-key implementation under `orvex/api-key` with behavioural parity, and `jwt.strategy.ts:83` repointed off `ee/api-key`** so that **the runtime-required api-key path is no longer EE-derived and the AGPL source can be published without a high-licensing-risk gate open** (FR-W7, FR-11).

**Definition of Done:** ONE named test `TestOrvexApiKeyBehaviouralParity` (integration layer — asserts through `jwt.strategy.ts` that api-key authentication behaves identically to the prior EE path across create/verify/scope, while importing only from `orvex/api-key`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an api-key request, When authenticated, Then `jwt.strategy.ts:83` resolves via `orvex/api-key`, not `ee/api-key`. *Assert: no `ee/api-key` import remains; auth succeeds.* [Source: FR-W7]
- [ ] **AC2** — Given the prior EE behaviour, When compared, Then create/verify/scope/tenant-binding behave identically (parity). *Assert: parity suite green.* [Source: FR-W7]
- [ ] **AC3** — Given the rebuild, When reviewed, Then it is a true clean-room rebuild — a copy of the EE code is NOT acceptable. *Assert: no EE-copied source; provenance clean.* [Source: FR-W7]
- [ ] **AC4 (negative)** — Given `packages/orvex-api-key` was an empty placeholder, When the fold-in lands, Then it is the real implementation. *Assert: placeholder replaced.* [Source: FR-W7]
- [ ] **AC5 (error-path)** — Given an invalid/expired api-key, When presented, Then auth fails closed. *Assert: 401, no session minted.* [Source: FR-W7]

## 🔨 Tasks

- [ ] RED: `TestOrvexApiKeyBehaviouralParity` incl the invalid-key path (AC2, AC5)
- [ ] GREEN: clean-room implement `orvex/api-key`; repoint `jwt.strategy.ts:83`; remove the `ee/api-key` dependency (AC1, AC3, AC4)
- [ ] Assert no `ee/*` import via the E1-S1 guard

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — Clean-room api-key under core/api-key with hashed keys + scopes migration + bearer guard; jwt.strategy no longer imports ee/api-key; contract spec.

Tier: engine auth (`jwt.strategy.ts` + `orvex/api-key`). Seam crossed: api-key auth. Sibling dependency: E1-S1 (no-ee guard), E6-S2 (session mint), satellites (authenticate via api-key).

## 🧪 Testing

Named DoD test `TestOrvexApiKeyBehaviouralParity` at integration tier against real Postgres. CS §5: real DB; never mock own auth; identity is an external boundary faked from fixtures where needed.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (AGPL-cleanliness, clean-room-provenance); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W7 (FR-11); Architecture twQ3BBzpTE §2 A-AUTH/A-BOUNDARY

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E6**; E1-S1
- Blocks: satellite authentication, source publication

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
