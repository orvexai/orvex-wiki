## 🎯 Story

As an **AGPL-compliance owner** I want **the upstream Stripe seat-sync severed from AGPL core — `STRIPE_SEATS_SYNC`/`BILLING_QUEUE` neutralized to emit a `billing.*` event, the in-engine billing migration retired, and NO Stripe secret in the engine's secret set** so that **publishing AGPL source is clean and the engine enforces entitlements but never talks to Stripe** (FR-W21, A-BOUNDARY).

**Definition of Done:** ONE named test `TestSeatSyncEmitsBillingEventNoStripe` (integration layer — asserts through the workspace-invitation path that a seat change emits a `billing.*` CloudEvent consumed by orvex-studio-billing and that no Stripe client/secret is referenced anywhere in the engine). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a seat change, When `workspace-invitation.service` runs, Then instead of the `STRIPE_SEATS_SYNC` → `BILLING_QUEUE` path it emits a `billing.*` event consumed by billing. *Assert: `billing.*` event emitted via the outbox; no `BILLING_QUEUE` enqueue.* [Source: FR-W21]
- [ ] **AC2** — Given the in-engine billing migration, When the fold-in lands, Then it is reconciled/retired. *Assert: no live in-engine billing migration.* [Source: FR-W21]
- [ ] **AC3** — Given the engine secret set, When inspected, Then no Stripe secret is present. *Assert: grep finds no Stripe secret; ExternalSecrets set has Stripe removed.* [Source: FR-W21]
- [ ] **AC4 (negative)** — Given entitlement enforcement, When it runs, Then the engine enforces but never makes a live Stripe call. *Assert: no Stripe SDK/network call in the engine.* [Source: FR-W21]
- [ ] **AC5 (error-path)** — Given the `billing.*` event, When emitted, Then it is a typed contracts CloudEvent entry, round-tripping golden fixtures. *Assert: event ∈ catalog; fixtures pass (via E5-S2 envelope).* [Source: A-SEAMS]

## 🔨 Tasks

- [ ] RED: `TestSeatSyncEmitsBillingEventNoStripe` (AC1, AC3)
- [ ] GREEN: neutralize the Stripe seat-sync → emit `billing.*` via outbox; retire the migration; strip the Stripe secret (AC2, AC4)
- [ ] Catalog the `billing.*` event + fixture round-trip (AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — Billing read moved to the HTTP entitlement port, but upstream Stripe seat-sync is NOT severed (STRIPE_SEATS_SYNC queue at queue.constants.ts:30, STRIPE_SECRET_KEY still read at environment.service.ts:249) and the in-engine billing migration is NOT retired.

Tier: engine service (`workspace-invitation.service`) + outbox emit. Seam crossed: engine → billing (`billing.*` events). Sibling dependency: E5-S1/S2 (outbox + relay carry the event), billing (consumer), E6-S3 (AGPL-cleanliness sibling).

## 🧪 Testing

Named DoD test `TestSeatSyncEmitsBillingEventNoStripe` at integration tier. CS §5: billing is an external boundary (fake from fixtures); never mock own emit path.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (AGPL-cleanliness, no-secret-in-engine); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W21; Architecture twQ3BBzpTE §2 A-BOUNDARY/A-SEAMS

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (`billing.*` CloudEvent contract tag is the gate); parent epic **E7**; E5-S1, E5-S2
- Blocks: AGPL source publication, billing seat SoR

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
