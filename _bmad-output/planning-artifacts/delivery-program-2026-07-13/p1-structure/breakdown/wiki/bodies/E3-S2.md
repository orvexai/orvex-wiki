## 🎯 Story

As a **licensing-conscious platform** I want **an import-boundary guard that lets ONLY the AGPL engine import `@orvex/dfm`, plus a golden-fixture conformance suite proving the TS and Go (`orvex-studio-lib/pkg/dfm`) twins agree** so that **importing the AGPL TS package into a closed satellite (which would relicense it) is mechanically impossible and the wire format never drifts between engine, CLI, and satellites** (FR-W18, FR-W18b, A-SEAMS finding F8).

**Definition of Done:** ONE named test `TestDfmTwinsConformToGoldenFixtures` (contract layer — asserts through the golden-fixtures interface that TS `@orvex/dfm` and the Go `pkg/dfm` produce byte-identical output for every fixture, and that a lint guard reds any closed-repo import of the AGPL TS package). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the contracts golden fixtures, When both twins serialize/round-trip each fixture, Then output is byte-identical. *Assert: TS output == Go output for all fixtures.* [Source: FR-W18, A-SEAMS]
- [ ] **AC2** — Given a closed satellite (mcp/wiki-api/ai/llms), When it attempts to import `@orvex/dfm`, Then the CS import-guard reds it (P10). *Assert: lint exit ≠ 0 on the import.* [Source: A-SEAMS F8]
- [ ] **AC3** — Given the superseded PRD phrasing ("ai/mcp/wiki-api/llms import @orvex/dfm"), When the correction is applied, Then satellites reach DfM via the Go twin or a wiki-api network call. *Assert: no closed repo imports the TS package; correction noted.* [Source: A-SEAMS F8]
- [ ] **AC4 (negative)** — Given the AGPL engine importing `@orvex/dfm`, When lint runs, Then it passes (the engine is the permitted importer). *Assert: lint exit 0 for the engine.* [Source: A-SEAMS]
- [ ] **AC5 (error-path)** — Given a fixture where the twins disagree, When conformance runs, Then it fails loudly naming the fixture. *Assert: `TestDfmTwinsConformToGoldenFixtures` fails with the fixture id.* [Source: FR-W18]

## 🔨 Tasks

- [ ] RED: `TestDfmTwinsConformToGoldenFixtures` with an intentionally-divergent fixture (AC5)
- [ ] GREEN: wire the golden-fixtures conformance harness (TS side) + the closed-repo import guard (AC1–AC4)
- [ ] Note the PRD FR-W18 phrasing correction owed (AC3)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — Local golden fixtures exist but no cross-repo Go-twin conformance (no orvex-studio-contracts fixture wiring); no dedicated DfM import-boundary guard script found (only the general engine-only-import-guard).

Tier: contract/fixture conformance + lint boundary. Seam crossed: TS engine ↔ Go twin ↔ closed satellites. Sibling dependency: E3-S1 (the package), contracts (golden fixtures owner).

## 🧪 Testing

Named DoD test `TestDfmTwinsConformToGoldenFixtures` at contract tier. CS §5: real fixtures + real twin invocations; the Go twin is a sibling artifact exercised via fixtures, never mocked-away.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp §7 golden fixtures); SE-Arch lenses (AGPL-boundary, wire-drift); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W18/FR-W18b; Architecture twQ3BBzpTE §2 A-SEAMS

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (golden-fixtures tag is the gate); parent epic **E3**; E3-S1
- Blocks: closed-satellite DfM consumption

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
