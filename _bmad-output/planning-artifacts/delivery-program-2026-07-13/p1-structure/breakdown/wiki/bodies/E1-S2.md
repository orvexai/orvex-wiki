## 🎯 Story

As a **wiki engine maintainer** I want **an FR-30 divergence-gate CI job that reds the build if any upstream file NOT on the frozen 13-row allow-list is modified, budgeted on weighted hot-file conflict-hunks** so that **the fork's divergence from upstream stays small and rebase-cheap over time** (G1, FR-W23/FR-30).

**Definition of Done:** ONE named test `TestFr30DivergenceGate` (CI-integration layer — asserts through the gate's CLI interface that a planted edit to an off-allow-list upstream file exits non-zero, and that the 13 permitted rows plus the A-HARDENING class exit zero). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the committed 13-row allow-list ledger, When the gate diffs the tree against the pinned v0.95.0 base, Then only allow-listed upstream files may differ. *Assert: gate exit 0 on a clean fold-in.* [Source: FR-W23, A-THIN]
- [ ] **AC2** — Given an edit to an off-allow-list upstream file (e.g. `space.service.ts`), When the gate runs, Then it reds. *Assert: gate exit ≠ 0, offending path reported.* [Source: FR-30]
- [ ] **AC3** — Given the weighting table, When `page.service.ts`/`app.module.ts`/`jwt.strategy.ts` are touched, Then their conflict-hunks count weighted-high in the budget. *Assert: budget report shows weight > 1 for the hot files.* [Source: FR-W23]
- [ ] **AC4 (negative)** — Given a change confined to `orvex/*`/`@orvex/*`, When the gate runs, Then it does not count against the divergence budget. *Assert: budget delta = 0.* [Source: A-THIN]
- [ ] **AC5 (error-path)** — Given an A-HARDENING item edited within its tagged upstream anchor, When the gate runs, Then it is not flagged as un-permitted divergence. *Assert: gate exit 0 with the item recognised via its ledger anchor.* [Source: FR-W24]

## 🔨 Tasks

- [ ] RED: `TestFr30DivergenceGate` with a planted off-allow-list edit fixture (AC2)
- [ ] GREEN: author the allow-list ledger (13 rows + weights) + the gate script; wire into CI (AC1–AC5)
- [ ] Emit a machine-readable budget report artifact for trend tracking (counter-metric: hunks must fall over time)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — patches-drift + frozen inline-edit-allowlist CI gate present (self-test fixtures), but it is a fuzzy patch-context matcher, not the FR-30 weighted hot-file conflict-hunk model; the allow-list ledger is EMPTY (entries:[]), not the 13-row ledger.

Tier: engine tooling/CI. Seam crossed: upstream↔fork divergence surface. Sibling dependency: E1-S1 (module boundary) feeds the allow-list; E1-S3 tags the A-HARDENING class the gate must recognise.

## 🧪 Testing

Named DoD test `TestFr30DivergenceGate` at CI-integration tier over fixture diffs. CS §5: real gate invocation over real git fixtures; never mock our own gate.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (rebase-cost, drift-detection); cell-lint JGAUQRsw2g (the gate is a sibling CI check to cell-lint).

## 🔗 References

- Architecture twQ3BBzpTE §2 A-THIN, A-IMPORT, A-HARDENING
- PRD EPsdD7uK8e §4 FR-W23, FR-W24

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E1**; E1-S1
- Blocks: every overlay-rebase import (E1-S3 runbook invokes this gate)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
