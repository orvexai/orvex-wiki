## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **wiki user** I want **the in-editor AI/MCP/API/api-key affordances kept in the AGPL client bundle as pure thin UI (bubble-menu quick-actions, Cmd+K/Cmd+J palette, `/ai` slash, Ask-search box, AI-diagram affordances, MCP + API settings pages) whose SSE handlers are `ReadableStream` readers over a caller-supplied stream with ZERO AI logic** so that **the client keeps its rich affordances while the engine server carries no AI/MCP/API logic (D-S4 named exception)** (FR-W18c).

**Definition of Done:** ONE named test `TestThinUiSseReaderHasNoServerAiLogic` (component/integration layer — asserts through the client's AI affordance that its SSE handler only reads a stream the satellite supplies, and a static check confirms the engine server contains no AI/prompting/mermaid-conversion/MCP-config code). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the client bundle, When it renders, Then the D-S4 affordances (bubble-menu, Cmd+K/Cmd+J, `/ai` slash, Ask-search, AI-diagram, MCP + API/api-key settings) are present as thin UI. *Assert: affordances render; call the satellites over the ingress.* [Source: FR-W18c]
- [ ] **AC2** — Given an in-editor AI action, When invoked, Then its SSE handler is a `ReadableStream` reader over a stream the caller supplies — no prompting logic client-side. *Assert: handler contains no AI logic.* [Source: FR-W18c]
- [ ] **AC3** — Given the engine SERVER, When inspected, Then it carries zero AI/MCP/API logic — all prompting, mermaid→drawio/excalidraw conversion, and MCP config live in ai/mcp. *Assert: no AI/MCP logic in the engine server.* [Source: FR-W18c, Non-goals]
- [ ] **AC4 (negative)** — Given the D-S4 carve-out, When the FR-30 gate runs, Then these client affordances are recognised as the allow-listed exception, not a violation. *Assert: gate does not red the thin-UI exception.* [Source: FR-W18c]
- [ ] **AC5 (error-path)** — Given a satellite (ai/mcp) outage, When an affordance is used, Then it degrades cleanly (the affordance errors gracefully; core editing unaffected). *Assert: graceful degrade; page editing still works.* [Source: G5, FR-W16]

## 🔨 Tasks

- [ ] RED: `TestThinUiSseReaderHasNoServerAiLogic` (AC2, AC3)
- [ ] GREEN: retain the thin-UI affordances as SSE readers calling satellites; assert zero server AI logic (AC1)
- [ ] Register the D-S4 exception in the allow-list; add graceful-degrade (AC4, AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — Thin AI/MCP client affordances (ee/ai-chat) + engine llms surface; client-ai-thinness CI gate enforces zero server-side AI logic (D-S4 exception); controller spec.

Tier: React client (thin UI) — engine server carries none of it. Seam crossed: client → ai/mcp over ingress. Sibling dependency: E8-S1 (editor), E8-S3 (configured endpoints + degrade), E1-S2 (gate recognises the exception).

## 🧪 Testing

Named DoD test `TestThinUiSseReaderHasNoServerAiLogic` at component+integration tier. CS §5: ai/mcp are external boundaries (fake SSE from fixtures); never mock own client components.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (thin-UI-carve-out, no-server-AI); cell-lint JGAUQRsw2g. React front rules for the ui-adjacent client.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W18c (D-S4); Architecture twQ3BBzpTE §2 A-APIBOUNDARY

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (ai/mcp SSE contract tags are the gate); parent epic **E8**; E8-S1
- Blocks: consumer-grade editor UX

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
