## 🎯 Story

As a **tenant hitting a cap** I want **the over-quota rejection to be a clear, actionable `402 QUOTA_EXCEEDED` computed by a domain function (not embedded in the controller), with an upgrade deep-link, a largest-files list for storage, and SSO/SCIM JIT allowed to 110%** so that **the frozen error contract holds, the highest-intent conversion moment is handled gracefully, and no data is ever deleted** (FR-W13, A-QUOTA-HARDENING).

**Definition of Done:** ONE named test `TestQuotaVerdictIsDomainFunction` (unit+integration layer — asserts through the `orvex/quota` verdict function that the entitlement-vs-usage comparison lives in the service and `page.controller.ts`/the collab hook only marshal a `402 QUOTA_EXCEEDED`, never 429/delete). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an over-quota write, When rejected, Then the response is `402 QUOTA_EXCEEDED` (never 429, never a delete) with an upgrade/entitlement deep-link. *Assert: HTTP 402; body carries the deep-link.* [Source: FR-W13]
- [ ] **AC2** — Given the verdict, When computed, Then it lives in a domain function in `orvex/quota`; `page.controller.ts` and the collab persistence hook only marshal it. *Assert: no entitlement-vs-usage comparison in the handler (CS §6).* [Source: A-QUOTA-HARDENING F9]
- [ ] **AC3** — Given a storage over-quota, When rejected, Then a one-click largest-files list is included. *Assert: largest-files present.* [Source: FR-W13]
- [ ] **AC4** — Given SSO/SCIM JIT member provisioning, When the 26th member logs in, Then JIT is allowed to 110% so first login never breaks; new manual invites are blocked at the cap. *Assert: JIT ≤110% allowed; manual invite blocked at cap.* [Source: FR-W13]
- [ ] **AC5 (negative)** — Given a download/export/delete at cap, When performed, Then it succeeds and nothing is auto-deleted. *Assert: no auto-delete; read paths unaffected.* [Source: FR-W13]

## 🔨 Tasks

- [ ] RED: `TestQuotaVerdictIsDomainFunction` asserting handlers only marshal (AC2)
- [ ] GREEN: implement the verdict fn + 402 contract + deep-link + largest-files + 110% JIT rule (AC1, AC3, AC4)
- [ ] Assert no auto-delete anywhere on the quota path (AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — 402 QUOTA_EXCEEDED domain exception (QuotaExceededException) present and thrown/marshalled at the chokepoints; hardening extras (deep-link/largest-files payload, members 110% JIT) not evidenced.

Tier: engine domain (`orvex/quota` verdict) marshalled by controller/collab hook. Seam crossed: frozen `QUOTA_EXCEEDED` contract (contracts). Sibling dependency: E4-S1 (entitlement), E4-S2 (counters), E4-S4 (fail modes).

## 🧪 Testing

Named DoD test `TestQuotaVerdictIsDomainFunction` at unit tier (verdict) + integration (402 marshalling). CS §5: never mock own quota domain; contracts fixtures pin the error code.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp §6 domain-vs-handler); SE-Arch lenses (contract-freeze, conversion-UX); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W13; Architecture twQ3BBzpTE §2 A-QUOTA-HARDENING

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (frozen `QUOTA_EXCEEDED` contract tag is the gate); parent epic **E4**; E4-S1, E4-S2
- Blocks: client usage-meter UX

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
