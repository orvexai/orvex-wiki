## 🎯 Story

As a **wiki engine maintainer** I want **an executable overlay-rebase-per-tag import runbook plus the 15-item correctness-hardening allow-list class tagged with upstream anchors** so that **importing a new upstream Docmost tag is a routine, gated procedure and the hardening fixes are not mistaken for un-permitted divergence** (FR-W23, FR-W24, A-IMPORT, A-HARDENING).

**Definition of Done:** ONE named test `TestHardeningAllowlistClassCoverage` (unit layer — asserts through the ledger-reader interface that all 15 hardening items resolve to a live upstream anchor and that the FR-30 gate recognises each). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the 15-item hardening list, When the ledger is validated, Then each item (ifVersion CAS, unique-slug retry, copy-title dedup, move-cycle guard, attachment-copy resilience, unique-title-per-parent index, real-IP handling, user-throttler key fix, authMethod classification, attachment delete-authz, filename sanitising, postgres-URL normaliser, streaming-response bypass, FK/query-path indexes, slug-rewrite-on-title-change) carries an upstream anchor. *Assert: ledger has 15 entries, each with a resolvable anchor.* [Source: FR-W24]
- [ ] **AC2** — Given the runbook, When executed against a new tag, Then it performs: pull tag → replay 13-row patch set → regenerate `db.d.ts` verbatim → append orvex migrations after the new tip → run seam contract tests + DfM golden fixtures → run FR-30 gate → deploy. *Assert: runbook steps present and ordered; a dry-run completes each stage.* [Source: FR-W23]
- [ ] **AC3** — Given cosmetics (Swagger tags, log-level tuning, micro-fixes), When the import runs, Then they are NOT re-applied. *Assert: cosmetics list is explicitly excluded from the patch set.* [Source: FR-W24]
- [ ] **AC4 (negative)** — Given a hardening item edited within its anchor, When the FR-30 gate runs, Then it is not flagged. *Assert: gate exit 0 (cross-check with E1-S2).* [Source: FR-W24]
- [ ] **AC5 (error-path)** — Given a hardening item whose anchor no longer resolves after a tag bump, When the ledger validates, Then it fails loudly. *Assert: `TestHardeningAllowlistClassCoverage` fails with the unresolved item named.* [Source: A-IMPORT]

## 🔨 Tasks

- [ ] RED: `TestHardeningAllowlistClassCoverage` over a ledger fixture with one unresolved anchor (AC5)
- [ ] GREEN: author the 15-item ledger with anchors; write the overlay-rebase runbook + dry-run harness (AC1–AC3)
- [ ] Cross-wire recognition into the FR-30 gate (AC4)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** absent — No overlay-rebase import runbook doc (docs/ holds only delivery-checklist.md + design/patches-drift-ci-design.md); no 15-item correctness-hardening allow-list class evidenced.

Tier: engine tooling/runbook + migration ledger. Seam crossed: upstream import boundary. Sibling dependency: E1-S2 (gate must recognise the class); E1-S4 (migration append rules).

## 🧪 Testing

Named DoD test `TestHardeningAllowlistClassCoverage` at unit tier over ledger fixtures + a runbook dry-run. CS §5: real ledger reader; never mock own code.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (drift, provenance); cell-lint JGAUQRsw2g.

## 🔗 References

- Architecture twQ3BBzpTE §2 A-IMPORT, A-HARDENING
- PRD EPsdD7uK8e §4 FR-W23, FR-W24

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E1**; E1-S2 (gate)
- Blocks: sustainable upstream imports

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
