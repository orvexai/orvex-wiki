## 🎯 Story

As the **sole studio-spine producer** I want **the cell-contract producer duties on every emitted event — the `orvexcell` extension attribute, `{domain}-events.{cell}` single-partition topics asserted at boot, an end-to-end correlation-id threaded from ingress into the CloudEvent, and a worker relay liveness + lag heartbeat** so that **consumers fail closed on a cell mismatch, topics are correct day one, requests are traceable across the outbox, and a dead relay never reads green** (A-CELL, A-OBSERVE).

**Definition of Done:** ONE named test `TestProducedEventCarriesOrvexcellAndCorrelation` (contract+integration layer — asserts through the relay that every published CloudEvent carries `orvexcell = CELL_ID`, is on a `{domain}-events.{cell}` single-partition topic, and carries the correlation id threaded from ingress). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given any emitted CloudEvent, When published, Then it carries the `orvexcell` extension attribute (value = `CELL_ID`); consumers fail closed on a mismatch. *Assert: `orvexcell == CELL_ID` on every event (rule #6).* [Source: A-CELL]
- [ ] **AC2** — Given topics, When asserted at boot, Then they are `{domain}-events.{cell}` (e.g. `wiki-events.eu-central-1a`), single-partition (partitions:1), named by the internal cell id. *Assert: topic name + partitions:1 asserted (rule #5).* [Source: A-CELL]
- [ ] **AC3** — Given an ingress request, When it mutates and emits, Then a request/correlation id threads ingress → outbox CloudEvent (standard attribute) → consumer. *Assert: correlation id present end-to-end (F11).* [Source: A-OBSERVE]
- [ ] **AC4** — Given the worker relay, When it runs, Then it exposes a relay-liveness + lag heartbeat (age of last-drained row / count of unpublished rows) as a probe AND an OTLP metric. *Assert: probe + metric present; a killed relay flips the probe red (F5).* [Source: A-OBSERVE]
- [ ] **AC5 (error-path)** — Given the `solo` sentinel (no registry configured), When events emit, Then cell enforcement no-ops so standalone boots still work. *Assert: `solo` skips the cell assertion.* [Source: A-CELL]

## 🔨 Tasks

- [ ] RED: `TestProducedEventCarriesOrvexcellAndCorrelation` (AC1, AC3)
- [ ] GREEN: add `orvexcell` attr, boot topic assertion, correlation threading, relay heartbeat probe + OTLP metric (AC2, AC4)
- [ ] `solo` no-op path (AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — orvexcell + orvextenant extension attrs + correlation_id present on the emitted envelope; but the relay publishes to a single getKafkaOutboxTopic(), not per-domain {domain}-events.{cell} topics, and there is no dedicated relay liveness/lag heartbeat probe.

Tier: engine worker (relay) + observability. Seam crossed: cell contract (JGAUQRsw2g §5) + OTel to Mimir. Sibling dependency: E5-S2 (relay), E9-S2 (health echo + collab probe), E7-S1 (solo sentinel).

## 🧪 Testing

Named DoD test `TestProducedEventCarriesOrvexcellAndCorrelation` at contract+integration tier against real Kafka. CS §5: real spine; never mock own relay; OTLP export verified via a real collector or fixture.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11 (esp §10 observability); SE-Arch lenses (cell-contract, dead-component-never-green); cell-lint JGAUQRsw2g §5.

## 🔗 References

- Architecture twQ3BBzpTE §2 A-CELL/A-OBSERVE; cell-lint JGAUQRsw2g

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (cell contract JGAUQRsw2g §5 tag is the gate); parent epic **E5**; E5-S2
- Blocks: consumer cell-guard, console observability

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
