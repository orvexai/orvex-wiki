## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **consumer of the engine (ai, collab, REST writers)** I want **the atomic AI-provenance stamp — the `aiAuthored` mark + `provenanceChangedById` written in the SAME transaction as the content mutation, via the `updatePageContent({markAiAuthored})` write path on BOTH the REST and collab seams** so that **an AI-authored edit can never be persisted without its provenance (no torn write, no after-the-fact stamp), and downstream trust/attribution reads are authoritative** (FR-12).

**Definition of Done:** ONE named test `TestAiProvenanceStampedAtomicRestAndCollab` (integration layer — asserts through the write path that a `markAiAuthored` write persists content + `aiAuthored` mark + `provenanceChangedById` in one tx such that a mid-tx failure leaves neither, and that the same atomicity holds on the Yjs collab persistence path). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an AI-authored write with `markAiAuthored`, When it persists via `updatePageContent`, Then content, the `aiAuthored` mark, and `provenanceChangedById` land in the SAME DB transaction. *Assert: all three present together; version bumped once.* [Source: FR-12]
- [ ] **AC2** — Given a failure mid-write, When the tx rolls back, Then neither content nor provenance is stamped (no torn / after-the-fact stamp). *Assert: page unchanged; no orphan provenance row.* [Source: FR-12]
- [ ] **AC3** — Given the collab (Yjs/Hocuspocus) persistence path, When an AI-authored change is debounce-flushed, Then the provenance stamp is applied atomically there too, not only on REST. *Assert: collab-persisted AI edit carries `provenanceChangedById`.* [Source: FR-12]
- [ ] **AC4** — Given the create/update/upsert page DTOs, When `markAiAuthored` is supplied, Then it is validated in-request (the one DTO field that cannot be declaration-merged — it must be validated with the write, not merged post-hoc). *Assert: DTO validation rejects a malformed provenance field.* [Source: FR-12]
- [ ] **AC5 (negative)** — Given AI authorship inference/attribution *composition* (who/why summaries, model metadata enrichment), When invoked, Then it lives in the ai satellite, NOT the engine — the engine writes only the atomic mark + id. *Assert: no provenance composition logic in the engine.* [Source: FR-12, Non-goals]

## 🔨 Tasks

- [ ] RED: `TestAiProvenanceStampedAtomicRestAndCollab` covering the mid-tx rollback + the collab-path stamp (AC2, AC3)
- [ ] GREEN: implement `updatePageContent({markAiAuthored})` in the `page.service` chokepoint; write the `aiAuthored` mark + `provenanceChangedById` in the mutation tx; add the in-request `orvex` DTO field (AC1, AC4)
- [ ] Apply the same atomic stamp on the collab persistence extension path (handoff to E8-S1)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — aiAuthored provenance stamp on REST + collab (updatePageContent markAiAuthored) with provenance trio in orvex_page_meta; service + collab handler specs.

Tier: engine service/domain (`page.service.ts` `updatePageContent` chokepoint + `page.controller.ts` REST forwarding + `collaboration/persistence.extension.ts`). Seam crossed: REST + Yjs both stamp here. The atomic-with-write provenance field is the DTO exception (`create/update/upsert-page.dto.ts` `orvex` field) that must be validated in-request rather than declaration-merged. Sibling dependency: E2-S2 (the shared write chokepoint), E2-S1 (`orvex_page_meta` / `provenanceChangedById` placement), E8-S1 (collab path funnels here).

## 🧪 Testing

Named DoD test `TestAiProvenanceStampedAtomicRestAndCollab` at integration tier against real Postgres + a real Hocuspocus persistence path. CS §5: real DB; never mock the own write path — assert atomicity through the interface.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (atomicity/torn-write, provenance-integrity, chokepoint-funnel); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 (F1 primitives); Plan-of-record qJojHbWJni §5 (engine-resident survivors: REST + collab provenance, atomic AI-write path `updatePageContent({markAiAuthored})`, the `orvex` in-request DTO field); Architecture twQ3BBzpTE §2 A-THIN

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E2**; E2-S2 (chokepoint), E2-S1 (meta/provenance column)
- Blocks: E8-S1 (collab stamp path), ai satellite (provenance attribution reads)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
