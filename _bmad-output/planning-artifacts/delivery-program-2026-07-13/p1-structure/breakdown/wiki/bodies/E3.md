## 🗺️ Feature area

DfM (the shared serializer) is **not** a pure-AI concern that can be deleted — the engine's own write path imports it (`page.service.ts` imports `dfmToJson`/`reattachOpaqueRefs`; `collaboration.util.ts` imports `pmToDfm`). This epic extracts `@orvex/dfm` out of the upstream `editor-ext` barrel into a standalone AGPL TypeScript package (`pmToDfm`/`dfmToJson`/`registry`/`common-nodes`/`inline-serializer` + the `:::dfm-opaque` fence machinery, ~17k LOC incl. round-trip tests), makes the engine seam import it, and enforces the **DfM import boundary** (A-SEAMS / finding F8): the AGPL TS package is imported **only** by the AGPL engine; every closed satellite reaches DfM through the Go twin `orvex-studio-lib/pkg/dfm` or a network call — importing the AGPL TS package would relicense a closed repo (canon P10 + the CS import-guard). Both twins conform to the same **contracts golden fixtures** so the wire format never drifts.

Why it exists: FR-W18 — the write path structurally depends on lossless serialization; unknown nodes must round-trip through the `:::dfm-opaque` fence and `reattachOpaqueRefs` must **throw `DFM_OPAQUE_UNKNOWN_REF`, never silently drop**.

## 📋 Story roster

- [ ] E3-S1 — Extract @orvex/dfm standalone package + write-path imports + opaque-fence round-trip / DFM_OPAQUE_UNKNOWN_REF throw
- [ ] E3-S2 — DfM import-boundary guard (AGPL TS in-engine only) + golden-fixture conformance vs Go twin

## 🎯 FR ownership

FR-W18, FR-W18b, A-DFM, A-SEAMS (DfM leg). Note: PRD FR-W18's "ai/mcp/wiki-api/llms import @orvex/dfm" phrasing is **superseded** by A-SEAMS finding F8. [Source: PRD EPsdD7uK8e §4 FR-W18; Architecture twQ3BBzpTE §2 A-DFM/A-SEAMS]

## ✅ DoD

- [ ] Both stories Done
- [ ] Family-E2E share green: engine write path serializes/round-trips a doc containing an unknown node type without loss
- [ ] SDD lines evidenced: no closed satellite imports `@orvex/dfm`; TS + Go twins both pass the contracts golden fixtures; `reattachOpaqueRefs` throw contract asserted
- [ ] *Final H1–H17 elaboration + exact contract tag/versions pinned at pack certification (ENG-2103).*

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (golden-fixtures tag is the dispatch gate); E2-S2 (apply-ops write path that imports dfm)
- **Blocks:** E5 (content events carry serialized repr); wiki-api/CLI convert seams

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
