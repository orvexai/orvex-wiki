## 🎯 Story

As a **platform operator** I want **the cross-cell tenant-move typed step-API (quiesce/export/import/activate, mandatory `Idempotency-Key`) with a versioned `TenantMoveManifest{schema_version, stores, s3_prefixes, cursors}` covering all four engine-owned stores, plus a `TENANT_MOVE.md` coverage-checked against declared datastores** so that **moving a tenant across cells is a non-retrofittable typed contract present day one (a 501 body is acceptable, the typed contract is not)** (A-MOVE, cell rule #10).

**Definition of Done:** ONE named test `TestTenantMoveManifestCoversAllStores` (contract+integration layer — asserts through the step-API that the emitted `TenantMoveManifest` enumerates Postgres rows (incl `orvex_page_meta`), S3 attachment prefixes, Redis quota counters, and the outbox cursor, and that `TENANT_MOVE.md`'s inventory matches the engine's declared datastores). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the tenant-move step-API, When defined, Then it exposes typed quiesce/export/import/activate steps each requiring an `Idempotency-Key` (rule #11). *Assert: 4 typed steps; missing key rejected.* [Source: A-MOVE]
- [ ] **AC2** — Given the manifest, When produced, Then `TenantMoveManifest{schema_version, stores, s3_prefixes, cursors}` covers all four stores: Postgres rows (upstream `pages` + `orvex_page_meta` + comments/history/attachment metadata), S3 prefixes (`{tenant_id}/…`), Redis `quota:*:{tenant}`, outbox cursor. *Assert: all four stores present.* [Source: A-MOVE]
- [ ] **AC3** — Given `TENANT_MOVE.md`, When the rule #10 CI check runs, Then its store inventory matches the engine's declared datastores. *Assert: coverage check passes; a missing store reds it.* [Source: A-MOVE]
- [ ] **AC4 (negative)** — Given day-1 scope, When export/import bodies are 501 stubs, Then that is acceptable — but the typed contract is present and complete. *Assert: 501 bodies allowed; contract typed + versioned.* [Source: A-MOVE]
- [ ] **AC5 (open-decision)** — Given the manifest's exact store list depends on `orvex_page_meta` + `version`/`content_hash` placement (FR-W3), When those settle, Then the manifest reflects the decision. *Assert: manifest aligned with E2-S1's placement decision; TBD — pinned by the Definition Pack (ENG-2103) if unresolved.* [Source: A-MOVE, FR-W3]

## 🔨 Tasks

- [ ] RED: `TestTenantMoveManifestCoversAllStores` with a deliberately-missing store fixture (AC3)
- [ ] GREEN: define the typed step-API + manifest schema + `TENANT_MOVE.md` + the rule #10 coverage check (AC1, AC2)
- [ ] 501 export/import bodies; align with E2-S1 placement (AC4, AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — Typed step-API (quiesce/export/import/activate) + TenantMoveManifest DTO + mandatory Idempotency-Key header present; moveCell delegates to identity Registry.Move (real, spec'd), but the four content-pipeline steps are deliberate 501 stubs and no coverage-checked TENANT_MOVE.md exists.

Tier: engine step-API (contract) + ops doc. Seam crossed: cross-cell move (cell contract JGAUQRsw2g §5). Sibling dependency: E2-S1 (page_meta store), E5-S2 (outbox cursor), E4-S2 (quota counters).

## 🧪 Testing

Named DoD test `TestTenantMoveManifestCoversAllStores` at contract+integration tier. CS §5: real store enumeration; never mock own manifest; the coverage check runs against real declared datastores.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (non-retrofittable-contract, coverage-completeness); cell-lint JGAUQRsw2g §5 (rule #10/#11).

## 🔗 References

- Architecture twQ3BBzpTE §2 A-MOVE; cell-lint JGAUQRsw2g

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (cell contract step-API tag is the gate); parent epic **E9**; E2-S1, E5-S2, E4-S2
- Blocks: cross-cell operations

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
