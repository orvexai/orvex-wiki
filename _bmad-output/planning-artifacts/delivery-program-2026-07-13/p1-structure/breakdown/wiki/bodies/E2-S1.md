## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **wiki engine maintainer** I want **the 18 product columns physically injected on upstream `pages` moved into an indexed `orvex_page_meta` side table + join, with the ~10 read sites rewritten** so that **upstream `pages` stays pristine, every upstream migration touching `pages` stops forcing a coordinated orvex change, and the FR-30 gate can forbid new product columns** (FR-W3 — the schema-enemy move, built FIRST).

**Definition of Done:** ONE named test `TestPageMetaSideTableReadParity` (integration layer — asserts through `page.repo.ts` that every `baseFields` value previously read off `pages` now resolves identically via the `orvex_page_meta` join, with upstream `pages` carrying zero product columns). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the 18 injected columns (15 lifecycle/provenance/verification + `version` + `content_hash` + `external_id`), When the migration runs, Then all move to `orvex_page_meta` (indexed) and upstream `pages` reverts to stock. *Assert: `pages` column set == v0.95 stock; `db.d.ts` reverts via declaration-merge.* [Source: FR-W3]
- [ ] **AC2** — Given the ~10 direct read sites + `page.repo.ts baseFields` (16 surfaced), When rewritten, Then reads resolve through the join with identical values. *Assert: read parity for every field pre/post migration.* [Source: FR-W3]
- [ ] **AC3** — Given the D-S8 stamp fields (`verified_against`, `verified_at`, `spec_confirmed`), When the engine stores them, Then only the stamp lives here (the drift/spec-gate *services* live in wiki-api). *Assert: engine writes stamps; no drift-verify logic in engine.* [Source: FR-W3, Non-goals]
- [ ] **AC4 (open-decision)** — Given `version`/`content_hash` are arguably core concurrency columns (FR-W3 decides keep-in-`pages` during build), When the decision is recorded, Then the A-MOVE manifest store list (E9-S1) reflects it. *Assert: decision documented; TBD — pinned by the Definition Pack (ENG-2103) if unresolved.* [Source: FR-W3, OQ FR-W3]
- [ ] **AC5 (error-path)** — Given a new attempt to add a product column onto `pages` after this move, When the FR-30 gate runs, Then it reds. *Assert: gate exit ≠ 0 (cross-check E1-S2).* [Source: FR-W3]

## 🔨 Tasks

- [ ] RED: `TestPageMetaSideTableReadParity` seeded with the 18 legacy columns (AC1–AC2)
- [ ] GREEN: author the `orvex_page_meta` migration + join; rewrite `baseFields` + read sites; revert `db.d.ts` (AC1–AC3)
- [ ] Record the `version`/`content_hash` placement decision for E9-S1 (AC4)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — orvex_page_meta side table carries governance/lifecycle/provenance + version/content_hash; provenance trio migrated off pages via INSERT..SELECT; upstream pages kept stock (fresh port, not a column move); read-parity covered by migration + service specs.

Tier: engine data/repo layer (`page.repo.ts`, migrations). Seam crossed: upstream `pages` ↔ orvex side table. Sibling dependency: E1-S4 (migration ledger); E9-S1 (manifest store list); every read primitive downstream.

## 🧪 Testing

Named DoD test `TestPageMetaSideTableReadParity` at integration tier against real Postgres. CS §5: real DB; never mock own repo.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (data-model hygiene, rebase-cost); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W3; Architecture twQ3BBzpTE §2 A-THIN

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E2**; E1-S4 (migration ledger)
- Blocks: E2-S2/S3/S4 (primitives read meta), E9-S1 (store list)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
