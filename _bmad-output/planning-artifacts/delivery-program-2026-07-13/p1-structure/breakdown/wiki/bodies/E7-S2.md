## 🎯 Story

As a **security owner** I want **transaction-scoped fail-closed RLS (`set_config('app.workspace_id',$1,true)` inside an explicit transaction) as the backstop beneath the FR-13 app-layer ACL, with cross-tenant + intra-tenant-restricted CI probes** so that **tenant isolation holds even under pgBouncer transaction-pooling (a session-level `SET` leaks across tenants) and cross-tenant bytes = 0** (FR-W8, G2).

**Definition of Done:** ONE named test `TestRlsTransactionScopedNoLeakUnderPooling` (integration layer — asserts through a pgBouncer transaction-pooled connection that tenant A's GUC never leaks into tenant B's pooled transaction and cross-tenant reads return zero rows). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a request principal, When a transaction begins, Then `set_config('app.workspace_id',$1,true)` sets the GUC transaction-scoped. *Assert: GUC scoped to the txn; reset at commit/rollback.* [Source: FR-W8]
- [ ] **AC2** — Given pgBouncer transaction-pooling, When two tenants share a pooled backend, Then A's GUC never leaks into B's transaction. *Assert: no cross-tenant GUC bleed (a session-level `SET` would leak — must not be used).* [Source: FR-W8]
- [ ] **AC3** — Given RLS policies (zero exist today — a launch gate), When applied, Then every tenant-scoped table enforces them fail-closed. *Assert: policies present; deny-by-default.* [Source: FR-W8]
- [ ] **AC4 (negative)** — Given a cross-tenant read attempt bypassing the app ACL, When RLS evaluates, Then it returns zero rows. *Assert: cross-tenant + intra-tenant-restricted probes = 0 bytes.* [Source: FR-W8, G2]
- [ ] **AC5 (error-path)** — Given a request with no principal set, When a query runs, Then RLS denies (fail-closed), not defaults-open. *Assert: no principal → no rows.* [Source: FR-W8]

## 🔨 Tasks

- [ ] RED: `TestRlsTransactionScopedNoLeakUnderPooling` over a real pgBouncer pool (AC2)
- [ ] GREEN: add the transaction-scoped GUC hook (`database.module.ts` RLS GUC hook, 13-row allow-list) + RLS policies (AC1, AC3)
- [ ] Add cross-tenant + intra-tenant-restricted CI probes (AC4, AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** absent — No transaction-scoped fail-closed RLS: no set_config tenant binding, no CREATE POLICY / ENABLE ROW LEVEL SECURITY migration, no cross-tenant bytes=0 probe.

Tier: engine data (RLS GUC hook in `database.module.ts`) + Postgres policies. Seam crossed: app-ACL (FR-13) ↔ RLS backstop. Sibling dependency: E2-S3 (ACL above), E7-S1 (resolved principal), E7-S3 (workspace_id as RLS key).

## 🧪 Testing

Named DoD test `TestRlsTransactionScopedNoLeakUnderPooling` at integration tier against real Postgres + pgBouncer. CS §5: real DB + pooler; never mock own data layer.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (isolation, fail-closed, pooling-hazard); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W8; Architecture twQ3BBzpTE §2 A-TENANCY

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E7**; E7-S1, E2-S3
- Blocks: prod cutover (isolation gate)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
