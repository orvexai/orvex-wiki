## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **wiki reader/editor** I want **mark-superseded / archive / status-control as a first-class engine capability — with the show-superseded tree filter, the superseded banner, and supersession link-rewrite — all reading and writing status through the `orvex_page_meta` seam (never a column on upstream `pages`)** so that **page lifecycle is a supported product feature and the status/docType filters scattered inline today stop diverging the tightest-rebase surface** (FR-40, D-LIFECYCLE-1).

**Definition of Done:** ONE named test `TestPageLifecycleSupersedeRidesMetaSeam` (integration layer — asserts through the lifecycle mutation path that marking a page superseded flips status in `orvex_page_meta` while upstream `pages` stays untouched, the tree query hides superseded pages unless the show-superseded toggle is set, and the successor link-rewrite resolves to the live successor). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a page marked superseded or archived, When the status change commits, Then it writes to `orvex_page_meta` (the E2-S1 seam), never a status column on upstream `pages`. *Assert: status persisted in `orvex_page_meta`; upstream `pages` column set unchanged.* [Source: FR-40]
- [ ] **AC2** — Given the page tree, When the show-superseded filter is off then on, Then superseded pages are hidden by default and shown when toggled. *Assert: tree query excludes `status=superseded` unless show-superseded is set.* [Source: FR-40]
- [ ] **AC3** — Given a superseded page is opened, When it renders, Then the superseded banner + successor link are returned to the client and the link-rewrite points at the live successor. *Assert: response carries superseded banner + resolved successor pageId.* [Source: FR-40]
- [ ] **AC4 (constraint)** — Given the status/docType filters scattered across `page.service`/`page.controller`/`search.service` + 4 DTOs, When folded in, Then they route through the `orvex_page_meta` seam (FR-42/FR-W3), not the upstream files. *Assert: FR-30 gate green — no new status/docType branches on upstream page files (cross-check E1-S2).* [Source: FR-40, FR-W3]
- [ ] **AC5 (error-path)** — Given a mark-superseded pointing at a missing or cross-tenant successor, When applied, Then it is rejected with no dangling or cross-tenant link-rewrite. *Assert: reject on missing/cross-tenant successor; zero orphan supersession links.* [Source: FR-40]

## 🔨 Tasks

- [ ] RED: `TestPageLifecycleSupersedeRidesMetaSeam` seeded with a page + successor (AC1, AC4)
- [ ] GREEN: implement mark-superseded/archive/status-control on the `orvex_page_meta` seam; wire the tree filter + banner + link-rewrite (AC1–AC3)
- [ ] Route the scattered status/docType filters through the meta seam; keep upstream page files pristine + successor-integrity guard (AC4, AC5)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — FR-40 lifecycle (supersede/promote/status/archive) + superseded-tree discovery filter + ws lifecycle broadcaster riding orvex_page_meta; integration specs.

Tier: engine service/repo (`page.service`, `page.controller`, `search.service` + 4 DTOs) over `orvex_page_meta`. Seam crossed: lifecycle status ↔ `orvex_page_meta` (not upstream `pages`). Sibling dependency: E2-S1 (the side table this rides), E2-S3 (ACL narrows the tree read), E1-S2 (the FR-30 gate that must stay green).

## 🧪 Testing

Named DoD test `TestPageLifecycleSupersedeRidesMetaSeam` at integration tier against real Postgres. CS §5: real DB; never mock own repo/meta seam.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (rebase-cost, data-model hygiene); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §1/§4 (FR-1..47 folded fine-grained source) + FR-W3 (`orvex_page_meta` seam); Core Wiki Engine PRD aleSxNZCb1 §3 FR-40 (D-LIFECYCLE-1) + FR-42 (meta-seam constraint); Architecture twQ3BBzpTE §2 A-THIN

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E2**; E2-S1 (`orvex_page_meta` side table)
- Blocks: wiki-api lifecycle grammar; knowledge status-aware indexing

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
