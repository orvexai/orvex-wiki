## 🗺️ Feature area

Quota bypass is **THE failure** the engine guards against (G3 — bounded cost per tenant; the loss-leader stays bounded). Because pages and attachments are creatable via the engine's own REST API *and* the Hocuspocus collab persistence path (`persistence.extension.ts` stores unbounded ydoc bytes on a 10s debounce with zero accounting), enforcement cannot live in wiki-api — the **engine write chokepoint is the only leak-proof point**, the same reasoning as ACL. This epic builds: the **entitlement-reader interface** (FR-W11 — engine hard-codes no plan numbers; reads billing-owned entitlements, cached short-TTL, evicted on `billing.entitlement.changed`; interim hardcode-Free behind the interface), the **pre-flight O(1) Redis fast-counters** at page-create/attachment-upload/member-add over both write paths (FR-W10/FR-W12), the **frozen `402 QUOTA_EXCEEDED` verdict computed by a domain function** (FR-W13 / A-QUOTA-HARDENING — controllers only marshal), the **reconciliation sweep + fail-closed-storage/fail-open-cheap** durability behaviour (FR-W12 / A-QUOTA-HARDENING — needs its own ADR), and the **singleton history-prune cron + quota-state query** (FR-W14/FR-W15).

Why it exists: A-QUOTA is the entitlement→enforcement sibling of the AI spend-cap; a Redis reset must never open free-tier abuse (G3).

## 📋 Story roster

- [ ] E4-S1 — Entitlement-reader interface + billing cache + interim hardcode-Free
- [ ] E4-S2 — Redis fast-counter pre-flight O(1) check over REST + collab paths
- [ ] E4-S3 — 402 QUOTA_EXCEEDED verdict as domain fn + marshal + deep-link/largest-files + members 110% JIT
- [ ] E4-S4 — Reconciliation sweep + fail-closed-storage/fail-open-cheap on Redis loss + warn-mode rollout
- [ ] E4-S5 — History-retention prune cron (singleton) + quota-state query endpoint

## 🎯 FR ownership

FR-W10, FR-W11, FR-W12, FR-W13, FR-W14, FR-W15, A-QUOTA, A-QUOTA-HARDENING. [Source: PRD EPsdD7uK8e §4 F-QUOTA; Architecture twQ3BBzpTE §2 A-QUOTA]

## ✅ DoD

- [ ] All 5 stories Done
- [ ] Family-E2E share green: a tenant at cap gets 402 on both the REST *and* collab write paths; reads/exports/deletes still succeed
- [ ] SDD lines evidenced: no plan numbers in engine code (all via entitlement-reader); 402 verdict lives in `orvex/quota`, controllers marshal only; Redis-loss fails closed for storage bytes
- [ ] *Final H1–H17 elaboration + exact contract tag/versions pinned at pack certification (ENG-2103).*

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (billing↔engine entitlement contract shape OQ-W2 is the gate); E2-S2 (chokepoint); E8-S1 (collab persistence path)
- **Blocks:** paid-plan launch (interim-hardcode→SoR swap must precede any paid plan sold)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
