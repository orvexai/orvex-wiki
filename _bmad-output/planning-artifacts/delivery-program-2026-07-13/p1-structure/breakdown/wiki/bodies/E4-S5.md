## 🎯 Story

As a **tenant and console operator** I want **a singleton worker-role history-prune cron enforcing `min(N versions, D days)` per entitlement, plus a quota-state query returning current counts vs caps** so that **page history is the only unbounded-in-time term stays bounded and the client usage-meter + console admin pane can show usage** (FR-W14, FR-W15).

**Definition of Done:** ONE named test `TestHistoryPruneCronIsSingleton` (integration layer — asserts through the cron interface that history is pruned to `min(N,D)` per the tenant entitlement and that the cron does not double-run under `replicas:2`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tenant with `history_retention=min(10,180d)` (Free), When the prune cron runs, Then versions beyond 10 or older than 180d are pruned. *Assert: retained set == min(N,D) per entitlement.* [Source: FR-W14]
- [ ] **AC2** — Given `replicas:2` on the worker role, When the cron fires, Then it runs exactly once (singleton). *Assert: no double-run; single execution observed.* [Source: FR-W14]
- [ ] **AC3** — Given a tenant, When quota-state is queried, Then current counts vs caps (pages/bytes/files/members) are returned from the fast-counters. *Assert: counts served from Redis counters, not a scan.* [Source: FR-W15]
- [ ] **AC4 (negative)** — Given Personal entitlement `min(100,730d)`, When the cron runs, Then it uses the per-tenant entitlement, not a hardcoded number. *Assert: retention read via the entitlement-reader.* [Source: FR-W14]
- [ ] **AC5 (error-path)** — Given a prune that would delete the only remaining version, When it runs, Then it never deletes the current page content (history-only prune). *Assert: current version preserved.* [Source: FR-W13, FR-W14]

## 🔨 Tasks

- [ ] RED: `TestHistoryPruneCronIsSingleton` under simulated replicas (AC2)
- [ ] GREEN: implement the prune cron (pattern: existing chat-history-ttl processor) with singleton guard; add the quota-state query (AC1, AC3)
- [ ] Read retention from the entitlement-reader (AC4)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — Quota-state query endpoint present (GET /orvex/quota); no history-retention prune cron (no @Cron/ScheduleModule singleton for history pruning).

Tier: engine worker (cron) + query endpoint. Seam crossed: worker singleton + client/console read. Sibling dependency: E4-S1 (entitlement), E4-S2 (counters for the query), E9-S2 (worker liveness).

## 🧪 Testing

Named DoD test `TestHistoryPruneCronIsSingleton` at integration tier against real Postgres + Redis. CS §5: real stores; never mock own cron.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (singleton-crons, bounded-retention); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W14/FR-W15; Architecture twQ3BBzpTE §1 (worker role)

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E4**; E4-S1, E4-S2
- Blocks: client usage-meter, console admin pane

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
