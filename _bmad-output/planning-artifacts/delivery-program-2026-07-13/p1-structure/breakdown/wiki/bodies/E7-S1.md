## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **cell operator** I want **a CLOUD-clean boot that does not `process.exit(1)` when `CLOUD=true` and the absent `ee/ee.module` fails to load, the existing `domain.middleware.ts` label-0 tenant resolver enabled, and a SOFT label-2 cell assertion that no-ops under the `solo` sentinel** so that **multi-tenant hosting turns on without reviving `ee/`, and self-hosted/standalone Docmost still boots** (FR-W20).

**Definition of Done:** ONE named test `TestCloudBootCleanWithoutEeModule` (integration layer — asserts through the app boot that `CLOUD=true` with `ee/ee.module` absent boots successfully, the `split('.')[0]` tenant resolver resolves label-0, and a `solo`-sentinel boot skips the label-2 cell assertion). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `CLOUD=true` and a deliberately-absent `ee/ee.module`, When the app boots, Then it does NOT `process.exit(1)` and comes up clean. *Assert: boot succeeds; no exit.* [Source: FR-W20]
- [ ] **AC2** — Given a request to `{tenant}.wiki.{cell}.orvex.ai`, When resolved, Then `isCloud()` → `split('.')[0]` → `workspaceRepo.findByHostname` resolves label-0 as the tenant (unchanged under D-S27). *Assert: tenant == host label 0.* [Source: FR-W20]
- [ ] **AC3** — Given a wrong-cell request, When the soft label-2 assertion runs, Then it rejects (defence-in-depth), but is claim-inferior to the edge/principal-vs-`CELL_ID` check. *Assert: wrong-cell soft-rejected; edge remains authoritative.* [Source: FR-W20, A-TENANCY]
- [ ] **AC4 (negative)** — Given nothing on the multi-tenant hot path may depend on `ee/`, When grepped, Then the hot path has no `ee/` dependency. *Assert: no `ee/` on the hot path.* [Source: FR-W20]
- [ ] **AC5 (error-path)** — Given the `solo` sentinel (no registry endpoint configured), When the app boots, Then the label-2 cell assertion no-ops. *Assert: `solo` boot skips cell enforcement.* [Source: A-CELL, A-TENANCY]

## 🔨 Tasks

- [ ] RED: `TestCloudBootCleanWithoutEeModule` (AC1, AC5)
- [ ] GREEN: decouple the `ee.module` boot gate; enable the resolver; add the soft label-2 assertion + `solo` no-op (AC2, AC3)
- [ ] Grep-guard the hot path for `ee/` (AC4)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — CLOUD-mode boot + hostname->workspace resolver (domain.middleware, spec'd) + CLOUD-mode session-mint exemptions in main.ts; #117 enabled CLOUD multi-tenant on the prod engine.

Tier: engine boot (`app.module.ts`) + middleware (`domain.middleware.ts`). Seam crossed: CLOUD boot + tenant/cell resolution. Sibling dependency: E7-S2 (RLS uses resolved tenant), E5-S3 (`solo` sentinel), E1 (CLOUD-clean boot overlaps A-BOUNDARY).

## 🧪 Testing

Named DoD test `TestCloudBootCleanWithoutEeModule` at integration tier. CS §5: real boot; never mock own boot/middleware.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (AGPL-boundary, edge-authoritative-cell-guard); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W20; Architecture twQ3BBzpTE §2 A-TENANCY/A-BOUNDARY

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E7**
- Blocks: E7-S2, E7-S3, prod cutover

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
