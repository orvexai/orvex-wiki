## 🎯 Story

As a **wiki engine write path** I want **`@orvex/dfm` extracted from the upstream `editor-ext` barrel into a standalone AGPL package that `page.service.ts` and `collaboration.util.ts` import, with lossless opaque-fence round-tripping** so that **the engine's write path has a stable serializer dependency and unknown node types never silently drop** (FR-W18, A-DFM).

**Definition of Done:** ONE named test `TestDfmOpaqueRoundTripThrows` (unit layer — asserts through `@orvex/dfm`'s `pmToDfm`/`dfmToJson`/`reattachOpaqueRefs` that an unknown node round-trips through a `:::dfm-opaque` fence and that a missing base ref throws `DFM_OPAQUE_UNKNOWN_REF`, never returns partial). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the upstream `editor-ext` barrel, When the fold-in runs, Then `@orvex/dfm` is a standalone package (`pmToDfm`/`dfmToJson`/`registry`/`common-nodes`/`inline-serializer` + fence machinery) that `page.service.ts` imports (`dfmToJson`/`reattachOpaqueRefs`) and `collaboration.util.ts` imports (`pmToDfm`). *Assert: both engine seams import from `@orvex/dfm`.* [Source: FR-W18]
- [ ] **AC2** — Given a doc with an unknown node type, When serialized, Then it becomes a lossless `:::dfm-opaque type=… id=…` fence. *Assert: round-trip byte-equality of the opaque body.* [Source: FR-W18, A-DFM]
- [ ] **AC3** — Given a `reattachOpaqueRefs` call whose base page lacks the referenced opaque body, When it runs, Then it throws `DFM_OPAQUE_UNKNOWN_REF`. *Assert: throw, never silent drop or partial.* [Source: FR-W18]
- [ ] **AC4 (negative)** — Given the DfM *verb grammar* (block-patch grammar over DfM), When packaged, Then it is NOT in `@orvex/dfm` — that lives in wiki-api; only the serializer library is here. *Assert: no verb-grammar in the package.* [Source: FR-W18, Non-goals]
- [ ] **AC5 (error-path)** — Given the ~17k LOC round-trip test suite, When run, Then it passes on the extracted package. *Assert: suite green post-extraction.* [Source: FR-W18]

## 🔨 Tasks

- [ ] RED: `TestDfmOpaqueRoundTripThrows` for the missing-ref throw + unknown-node fence (AC2, AC3)
- [ ] GREEN: extract the package; repoint engine imports; port the round-trip suite (AC1, AC5)
- [ ] Confirm no verb-grammar leaked into the package (AC4)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — @orvex/dfm package extracted (pmToDfm/dfmToJson/registry/errors) with real round-trip for the covered paragraph subset + honesty-contract throws for uncovered types; opaque-fence round-trip is a TYPED SIGNATURE that throws DFM_NOT_IMPLEMENTED (pm-to-dfm.ts:84); engine write-path does not yet import it (only llms + one import test do).

Tier: shared package (`packages/@orvex/dfm`) consumed by engine service + collab util. Seam crossed: engine write path ↔ serializer. Sibling dependency: E2-S2 (apply-ops imports it), E3-S2 (import-boundary + golden fixtures).

## 🧪 Testing

Named DoD test `TestDfmOpaqueRoundTripThrows` at unit tier (pure serialization). CS §5: never mock our own `@orvex/dfm`; tests exercise the real functions.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (lossless-fidelity, throw-never-drop); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W18; Architecture twQ3BBzpTE §2 A-DFM

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (DfM golden-fixtures tag is the gate); parent epic **E3**; E2-S2
- Blocks: E3-S2, E5-S2 (events carry serialized repr)

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
