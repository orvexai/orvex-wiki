## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **wiki engine maintainer** I want **an additive `apps/server/src/orvex/*` module tree loaded behind a single `OrvexRootModule` import plus lint guards that forbid `@docmost/*` and `ee/*` imports from any orvex package** so that **the fork's coupling to upstream is a process boundary, not tangled imports, and a rebase never conflicts on orvex code** (A-THIN).

**Definition of Done:** ONE named test `TestOrvexModuleBoundaryImportGuard` (unit/lint-integration layer — asserts through the ESLint rule interface that an `orvex/*` file importing `@docmost/*` or `ee/*` fails CI, and that the app boots with exactly one `OrvexRootModule` import in `app.module.ts`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a clean v0.95.0 tree, When the fold-in scaffolds `apps/server/src/orvex/*` and `packages/@orvex/*`, Then `app.module.ts` gains exactly one import (`OrvexRootModule`). *Assert: `grep -c "OrvexRootModule" app.module.ts == 1` and the app boots.* [Source: A-THIN]
- [ ] **AC2** — Given an `orvex/*` file that imports `@docmost/*`, When lint runs, Then CI fails with the no-`@docmost` rule. *Assert: eslint exit code ≠ 0, rule id `no-restricted-imports/docmost` reported.* [Source: A-THIN, A-BOUNDARY type-hygiene]
- [ ] **AC3** — Given an `orvex/*` file that imports `ee/*`, When lint runs, Then CI fails. *Assert: eslint exit ≠ 0 for the `ee` pattern.* [Source: A-BOUNDARY]
- [ ] **AC4 (negative)** — Given an orvex package importing only `@orvex/*` and framework packages, When lint runs, Then it passes. *Assert: eslint exit 0.* [Source: A-THIN]
- [ ] **AC5 (error-path)** — Given a second `OrvexRootModule`-equivalent import sneaks into an upstream file off the allow-list, When the FR-30 gate (E1-S2) runs, Then the build reds. *Assert: gate exit ≠ 0.* [Source: A-THIN]

## 🔨 Tasks

- [ ] RED: write `TestOrvexModuleBoundaryImportGuard` with a fixture orvex file importing `@docmost/*` (AC2)
- [ ] GREEN: scaffold `orvex/` + `@orvex/*`, add the ESLint `no-restricted-imports` guards, single `OrvexRootModule` import (AC1–AC4)
- [ ] Wire the guard into the CI lint gate; document the boundary in `project-context.md` (AC5 handoff to E1-S2)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — orvex/* tree mounted via the single OrvexRootModule.register() import (app.module.ts:137); engine-only-import-guard.sh + eslint no-@docmost/no-ee boundary rules; VANILLA byte-parity gate on ORVEX_MODULES_ENABLED.

Tier: NestJS engine — this is a *module-composition + tooling* concern (root module import + lint config), not domain logic. Seam crossed: the upstream↔orvex import boundary. Sibling dependency: E1-S2 consumes this boundary to define the FR-30 allow-list.

## 🧪 Testing

Named DoD test `TestOrvexModuleBoundaryImportGuard` at the lint-integration tier. CS §5 mocking: mock nothing of our own packages; the ESLint runner is a real invocation over fixtures (never mock own packages).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11; SE-Arch 8sYi523i4t lenses (boundary-integrity, rebase-cost); cell-lint JGAUQRsw2g for CI-gate shape.

## 🔗 References

- Architecture twQ3BBzpTE §2 A-THIN, A-BOUNDARY
- PRD EPsdD7uK8e §4 F4 (FR-W23/FR-30)

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (Definition Pack — the contract TAG is the dispatch gate); parent epic **E1**
- Intra-service order: first story of E1; E1-S2 depends on it

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
