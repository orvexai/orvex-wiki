## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **wiki engine operator** I want **the boot-migration path guarded by a `pg_advisory_xact_lock`, the two renamed upstream migrations dropped, `db.d.ts` taken verbatim from v0.95, and genuinely-additive orvex migrations appended in a separate `orvex_migrations` ledger** so that **concurrent api-role replica boots don't race the migration chain and the renamed-migration duplicate-object trap never crashloops the cell** (FR-W22, A-BOUNDARY).

**Definition of Done:** ONE named test `TestBootMigrateAdvisoryLockSerialises` (integration layer — asserts through the migrator interface that two concurrent `migrateToLatest` invocations serialise via the advisory lock and neither re-runs a v0.95-applied `CREATE TABLE`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given two api replicas booting simultaneously, When each runs `migrateToLatest`, Then a `pg_advisory_xact_lock` serialises them (or migration runs as an ArgoCD PreSync Job). *Assert: no duplicate-apply; second boot no-ops.* [Source: FR-W22]
- [ ] **AC2** — Given the fork's two renamed upstream migrations (`page-transclusions`, `labels`), When the fold-in migration chain is assembled, Then both renames are dropped (byte-identical to v0.95-applied originals; Kysely tracks by filename). *Assert: neither rename present; boot does not re-run their `CREATE TABLE`.* [Source: FR-W22]
- [ ] **AC3** — Given v0.95's `db.d.ts`, When the fold-in runs, Then it is taken verbatim (restoring SCIM EE columns + `is_base`/`base_schema_version`); additive columns route through one declaration-merge file. *Assert: `db.d.ts` diff vs v0.95 = 0; additive types live in the merge file.* [Source: A-BOUNDARY type-hygiene]
- [ ] **AC4 (negative)** — Given only genuinely-additive orvex migrations, When they run, Then they are appended after the v0.95 tip in the separate `orvex_migrations` ledger, idempotently. *Assert: re-run is a no-op; `kysely_migration` untouched by orvex migrations.* [Source: FR-W22]
- [ ] **AC5 (error-path)** — Given the dead `r5-pages-tsv-keyword-fastlane` no-op, When the chain is assembled, Then it is dropped. *Assert: absent from the chain.* [Source: FR-W22]

## 🔨 Tasks

- [ ] RED: `TestBootMigrateAdvisoryLockSerialises` with two concurrent migrator goroutines/processes (AC1)
- [ ] GREEN: add the advisory-lock guard; strip the two renames + the dead no-op; take `db.d.ts` verbatim; stand up `orvex_migrations` (AC2–AC5)
- [ ] Add a cross-tenant-safe CI check that re-running boot migrate is idempotent

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — pg_advisory_xact_lock boot-migrate (service.ts:70/82) + orvex_migrations ledger table (service.ts:113) + renamed-migration duplicate trap tested in orvex-migrator.spec.ts; concurrency spec covers the lock.

Tier: engine data/migration layer (`database.module`). Seam crossed: upstream migration chain vs the orvex ledger. Sibling dependency: E1-S3 (import runbook appends migrations here); E2-S1 (first orvex migration lands the page_meta table).

## 🧪 Testing

Named DoD test `TestBootMigrateAdvisoryLockSerialises` at integration tier against a real Postgres. CS §5: real DB (no mocking own migrator); Postgres is an external boundary that may use a real container.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (operational-safety, idempotency); cell-lint JGAUQRsw2g.

## 🔗 References

- Architecture twQ3BBzpTE §2 A-IMPORT, A-BOUNDARY
- PRD EPsdD7uK8e §4 FR-W22

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E1**
- Blocks: E2-S1 (page_meta migration), all multi-replica cell boots

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
