## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **knowledge indexer / export consumer** I want **the per-page export primitive (markdown FR-18 + embed-resolved `text_repr` FR-38) scoped to the api-key's tenant + space-ACLs and bound to a real audit service** so that **indexing and downloads get lossless content with no cross-tenant/cross-space bytes (REM-1 IDOR fix) and every export is WORM-audited** (FR-W4).

**Definition of Done:** ONE named test `TestExportScopedToTenantAndAudited` (integration layer — asserts through the export interface that an export for tenant A cannot return tenant B / out-of-space pages and that the audit record lands via a real `ORVEX_AUDIT_SERVICE`, never `NoopAuditService`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a page, When exported, Then per-page markdown (FR-18) + embed-resolved `text_repr` (FR-38) are produced. *Assert: markdown + text_repr both present; embeds resolved.* [Source: FR-W4, FR-18, FR-38]
- [ ] **AC2** — Given an api-key for tenant A, When it requests a tenant B / out-of-space page, Then the export is denied (REM-1 IDOR). *Assert: no cross-tenant/cross-space bytes returned.* [Source: FR-W4]
- [ ] **AC3** — Given any export, When it runs, Then it binds to a real `ORVEX_AUDIT_SERVICE` (WORM, purge-exempt), never `NoopAuditService`. *Assert: audit record written; noop path unreachable in prod config.* [Source: FR-W4]
- [ ] **AC4 (open correctness)** — Given the ancestry-vs-`pageAccess.spaceId` short-circuit bug, When export authz evaluates, Then ancestry does not bypass space-ACL. *Assert: short-circuit closed; TBD — pinned by the Definition Pack (ENG-2103) if the exact fix shape is still open.* [Source: FR-W4]
- [ ] **AC5 (negative)** — Given the bulk tar builder, When invoked, Then it is NOT in the engine (relocated to knowledge FR-19); the engine feeds it per-page. *Assert: no bulk-tar code in engine.* [Source: FR-W4, Non-goals]

## 🔨 Tasks

- [ ] RED: `TestExportScopedToTenantAndAudited` with a cross-tenant request fixture (AC2, AC3)
- [ ] GREEN: implement the per-page export scoped by ACL (uses E2-S3); bind the real audit service (AC1–AC4)
- [ ] Assert the noop audit path is unreachable under prod config

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** present — exportPage markdown/text_repr (FR-18/38) with a tenant-isolation IDOR guard before ExportService fetch (internal-api.service.ts:137); real audit-service present; tested.

Tier: engine service/domain (export) + audit boundary. Seam crossed: export → knowledge indexing. Sibling dependency: E2-S3 (ACL scope), E3-S1 (text_repr uses DfM).

## 🧪 Testing

Named DoD test `TestExportScopedToTenantAndAudited` at integration tier against real Postgres. CS §5: real DB + real audit sink (external boundary); never mock own export/ACL.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (IDOR, audit-integrity); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W4 (FR-18/38); Architecture twQ3BBzpTE §2 A-THIN

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (export contract tag is the gate); parent epic **E2**; E2-S3 (ACL)
- Blocks: knowledge indexing/export

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
