## 🎯 Story

As an **operator** I want **each of the three engine roles to carry its own liveness signal (worker relay-liveness + lag heartbeat; collab Hocuspocus WS probe), `/api/health` (or `/healthz`) to echo `CELL_ID` + `CLUSTER_NAME`, and the load-bearing rulings filed as draft ADR-NNNN** so that **a dead component never reads green (a dead relay silently stops all emission while api `/api/health` stays green), the cell params are observable, and the CS §9 decisions are ratifiable** (A-OBSERVE, A-CELL rule #4, A-ADR).

**Definition of Done:** ONE named test `TestPerRoleLivenessAndHealthEchoesCell` (integration layer — asserts through the probes that killing the worker relay flips its liveness/lag probe red while the api role stays independent, and `/api/health` echoes both `CELL_ID` and `CLUSTER_NAME`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the worker role, When it runs, Then it exposes a relay-liveness + lag heartbeat (age of last-drained outbox row / count of unpublished rows) as a probe AND an OTLP metric. *Assert: a killed relay flips the probe red (F5); metric exported to Mimir.* [Source: A-OBSERVE]
- [ ] **AC2** — Given the collab role, When it runs, Then it exposes a Hocuspocus WS liveness probe. *Assert: collab probe defined in the deploy manifest.* [Source: A-OBSERVE]
- [ ] **AC3** — Given `/api/health` (or `/healthz`), When called, Then it echoes both `CELL_ID` and `CLUSTER_NAME` (distinct params). *Assert: both echoed (F10, rule #4).* [Source: A-CELL]
- [ ] **AC4** — Given the load-bearing rulings (D-S13, D-S17, D-S3, the `402 QUOTA_EXCEEDED` contract, the A-QUOTA fail-open tradeoff, D-S27), When filed, Then each is a draft ADR-NNNN (masthead Status·Date·Deciders; body Context·Decision·Consequences·References). *Assert: ≥6 ADR-NNNN drafts filed. TBD — the Studio Decision-Records parent + `0001` numbering registry is pinned by the Definition Pack (ENG-2103).* [Source: A-ADR]
- [ ] **AC5 (negative)** — Given the deploy manifest currently defines only the api role probe, When this lands, Then worker + collab probes are added (not left implicit). *Assert: manifest defines all three role probes.* [Source: A-OBSERVE]

## 🔨 Tasks

- [ ] RED: `TestPerRoleLivenessAndHealthEchoesCell` killing the relay (AC1, AC3)
- [ ] GREEN: add worker relay heartbeat probe + OTLP metric, collab WS probe, health cell-echo (AC2, AC5)
- [ ] File the ADR-NNNN drafts once the registry parent exists (AC4)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — Per-dependency liveness health (HTTP 200 with per-dep status body) present + e2e/service specs; explicit CELL_ID/CLUSTER_NAME echo, dedicated per-role relay/collab-WS probes, and load-bearing ADR-NNNN draft files not evidenced.

Tier: engine observability (per-role probes) + ops (deploy manifest) + ADR authoring. Seam crossed: OTel → Mimir/LGTM (console) + cell contract. Sibling dependency: E5-S3 (relay heartbeat + correlation), E4-S4 (fail-open ADR), E7-S1 (CELL_ID param).

## 🧪 Testing

Named DoD test `TestPerRoleLivenessAndHealthEchoesCell` at integration tier. CS §5: real probes; OTLP verified via a real collector or fixture; never mock own health/relay.

## 📏 Guidance

CS §§0/3/4/5/6/7/9/10/11 (esp §9 ADR trigger, §10 observability); SE-Arch lenses (dead-component-never-green, decision-traceability); cell-lint JGAUQRsw2g §5 (rule #4).

## 🔗 References

- Architecture twQ3BBzpTE §2 A-OBSERVE/A-CELL/A-ADR; Decision Records R5OC7z3v3y; cell-lint JGAUQRsw2g

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (Studio ADR registry parent + cell contract tags are the gates); parent epic **E9**; E5-S3, E4-S4
- Blocks: console observability panes, ADR ratification

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
