## 🎯 Story

As an **AGPL-compliance owner** I want **one canonical `@Public` §13 source-offer endpoint returning `{sha, sourceRepo}` from `ORVEX_GIT_SHA`/`ORVEX_SOURCE_REPO`, a public git mirror at the advertised URL, a UI source link, and `LICENSE-COMPLIANCE.md`, reconciling the fork's two conflicting endpoints to one value** so that **the deployed engine actually satisfies AGPL §13 source availability (today it has none) — a cheap, do-it-now launch gate** (FR-W19).

**Definition of Done:** ONE named test `TestSourceOfferReturnsCanonicalShaRepo` (integration layer — asserts through the `@Public` endpoint that it returns one canonical `{sha, sourceRepo}` with no auth required, and that the two prior conflicting endpoints / URL inconsistencies are collapsed to a single value). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2103); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the engine, When the source-offer endpoint is called, Then it returns `{sha, sourceRepo}` from `ORVEX_GIT_SHA`/`ORVEX_SOURCE_REPO` and is `@Public` (no auth gate). *Assert: 200 unauthenticated; fields present.* [Source: FR-W19]
- [ ] **AC2** — Given the fork's two conflicting endpoints with inconsistent sha fallbacks and org/repo URLs (`orvexai/docmost` vs `orvex-ai/orvex`), When reconciled, Then exactly one canonical endpoint + value remains. *Assert: one endpoint; consistent URL.* [Source: FR-W19]
- [ ] **AC3** — Given the advertised URL, When followed, Then a public git mirror + a UI source link + `LICENSE-COMPLIANCE.md` exist. *Assert: mirror reachable; UI link present; compliance file committed.* [Source: FR-W19]
- [ ] **AC4 (negative)** — Given upstream's auth-gated `/version` pointing at `github.com/docmost/docmost`, When the fold-in lands, Then it no longer serves as the §13 mechanism. *Assert: §13 answered by the canonical `@Public` endpoint, not upstream `/version`.* [Source: FR-W19]
- [ ] **AC5 (error-path)** — Given `ORVEX_GIT_SHA` unset, When the endpoint is called, Then it fails loudly/deterministically rather than serving a wrong sha. *Assert: no silent wrong-sha; deterministic behaviour.* [Source: FR-W19]

## 🔨 Tasks

- [ ] RED: `TestSourceOfferReturnsCanonicalShaRepo` incl the unset-sha path (AC1, AC5)
- [ ] GREEN: implement the single `@Public` endpoint; reconcile the two endpoints; add mirror + UI link + compliance file (AC2, AC3)
- [ ] Remove upstream `/version` as the §13 answer (AC4)

## 🧠 Context

**🧾 Code audit (origin/main @ 84e4f3dd, 2026-07-14):** partial — AGPL §13 source-offer @Public endpoint returning {sha,sourceRepo} present (typed defect if unset, never fabricated); LICENSE-COMPLIANCE doc + git source-mirror not found in the tree.

Tier: engine controller (`@Public` endpoint) + ops (mirror). Seam crossed: AGPL §13 public surface. Sibling dependency: E7-S4 (AGPL boundary siblings), E8-S3 (UI source link).

## 🧪 Testing

Named DoD test `TestSourceOfferReturnsCanonicalShaRepo` at integration tier. CS §5: real endpoint invocation; never mock own controller.

## 📏 Guidance

CS §§0/3/4/5/6/7/10/11; SE-Arch lenses (AGPL-§13, single-source-of-truth); cell-lint JGAUQRsw2g.

## 🔗 References

- PRD EPsdD7uK8e §4 FR-W19; Architecture twQ3BBzpTE §2 A-BOUNDARY

## 🔗 Dependencies

- **Blocked by:** ENG-2103 (contract TAG dispatch gate); parent epic **E6**
- Blocks: AGPL source publication

## 📡 Protocol

CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
