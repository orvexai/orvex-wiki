## 🎯 Story

As a **user**, I want **shareable permalinks, prompt-via-us launch into a chosen engine, and the "Enrich this prompt" tick (default-on for entitled users, silent, never persisted)** so that **I can share and launch my skills, and enrichment happens invisibly without ever storing enriched text** (FR-UI17, FR-UI18).

**Definition of Done:** ONE named test `TestEnrichTickSilentAndNeverPersisted` — an integration test asserting the Enrich control is ticked by default for entitled users, enrichment is silent (no context preview), the tick renders visually off when search degradation ships an unenriched prompt (FR-57), and the Library/share/publish always store the clean prompt (enrichment never persisted), verified through the render + stored artifact. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a skill, When shared, Then a `/app/s/:skillId` permalink mints (ULID-stable through title edits); an anon recipient opens the live permalink with generic render + "Make it mine →" fork at the sign-up boundary. *Assertion: stable permalink; anon fork boundary.* [Source: FR-UI17]
- [ ] **AC2 (error)** — Given a missing/deprecated target, When opened, Then it falls back gracefully to Discover — never a 404 dead-end. *Assertion: no 404; Discover fallback.* [Source: FR-UI17, FR-UI29]
- [ ] **AC3** — Given any Skill, When launched, Then the composed prompt launches into a chosen engine (deep-link prefill where supported, copy-and-open fallback). *Assertion: launch prefill or fallback.* [Source: FR-UI18]
- [ ] **AC4** — Given every Prompt-Use Surface, When rendered for an entitled user, Then the "Enrich this prompt" control is ticked by default and silent (no context preview); it renders visually off when unenriched (FR-57); the Private-Memory ask is identical across all four surfaces. *Assertion: default-on + silent; off on degrade; identical ask.* [Source: FR-UI18, FR-57]
- [ ] **AC5 (negative)** — Given Free users, When they see the control, Then it renders the honest £7 upsell and the Phase-1 deterministic weave stays live unchanged; enrichment never persists — the clean prompt is always what stores. *Assertion: `TestEnrichTickSilentAndNeverPersisted` — stored artifact is clean.* [Source: FR-UI18]

## 🔨 Tasks

- [ ] RED: `TestEnrichTickSilentAndNeverPersisted` (AC4/AC5).
- [ ] GREEN: permalink mint + anon fork (AC1); dead-end fallback (AC2); launch (AC3); Enrich tick across surfaces (AC4); Free upsell + clean-store rule (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no share permalinks, no prompt-via-us launch, no Enrich-this-prompt tick.

React-front outbound + entitlement (CS §6). Seam: BFF share/launch + entitlement verdict (E1-S6). Sibling dependency: E1-S6 (entitlement), E3-S4 (consent ask), E5-S3 (export surface).

## 🧪 Testing

`TestEnrichTickSilentAndNeverPersisted` (integration) + unit tests. CS §5 mocking: BFF share/launch/entitlement fixtures; never mock own control.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest (no dead-ends, no persisted enrichment) · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: honesty + entitlement-cosmetic-gating lenses.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI17, FR-UI18, FR-57) · MB1 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E5.
- [ ] **Intra-service order:** after E1-S6, E5-S3, E3-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
