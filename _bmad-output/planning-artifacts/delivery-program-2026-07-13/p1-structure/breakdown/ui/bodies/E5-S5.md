## 🎯 Story

As a **user**, I want **the Library and Collections — saved Skills and Your-versions with the Delta intact, create/rename/add/remove collections, bulk export, and cross-device reload** so that **my work is organised and reopens exactly as I left it on any device** (FR-UI19).

**Definition of Done:** ONE named test `TestLibraryReopensDeltaIntactCrossDevice` — an integration test asserting a saved Your-version reopens with the Delta fully intact, Collections support create/rename/add/remove, bulk export reuses the MB1 adapters, and cross-device reload hydrates from Postgres via the BFF, verified through the reopened state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given saved Skills and Your-versions, When listed, Then they reopen with the Delta fully intact. *Assertion: `TestLibraryReopensDeltaIntactCrossDevice` — Delta preserved.* [Source: FR-UI19]
- [ ] **AC2** — Given Collections, When managed, Then create/rename/add/remove work. *Assertion: all four operations persist.* [Source: FR-UI19]
- [ ] **AC3** — Given a selection, When bulk-exported, Then it reuses the MB1 adapters. *Assertion: export via E5-S3 adapters.* [Source: FR-UI19]
- [ ] **AC4** — Given another device, When reloaded, Then state hydrates from Postgres via the BFF. *Assertion: cross-device state matches.* [Source: FR-UI19]
- [ ] **AC5 (negative)** — Given Library/Collections, When loading/empty/error, Then the shared skeleton/empty/error trio renders. *Assertion: trio present on both surfaces.* [Source: FR-UI19, FR-UI29]

## 🔨 Tasks

- [ ] RED: `TestLibraryReopensDeltaIntactCrossDevice` (AC1/AC4).
- [ ] GREEN: Library list + Delta-intact reopen (AC1); Collections CRUD (AC2); bulk export (AC3); cross-device hydrate (AC4); honest trio (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — a thin Library list pane over `/library` + a `LibraryItemPane` draft/commit demo exist & tested (`src/routes/Library.tsx`, `productPanes.test.tsx`). REMAINS: no Collections, no Delta-intact reopen, no bulk export, no cross-device sync.

React-front library (CS §6). Seam: BFF library/collections + Postgres via BFF. Sibling dependency: E4-S3 (Delta), E5-S3 (export), E1-S5 (honest states).

## 🧪 Testing

`TestLibraryReopensDeltaIntactCrossDevice` (integration) + unit tests. CS §5 mocking: BFF library fixtures; never mock own list.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest trio · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: no-data-loss lens (Delta intact cross-device).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI19) · MB3 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E5.
- [ ] **Intra-service order:** after E4-S3, E5-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
