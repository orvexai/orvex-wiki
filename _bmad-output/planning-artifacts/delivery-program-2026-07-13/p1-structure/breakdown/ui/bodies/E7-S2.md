## 🎯 Story

As a **user**, I want **Connections & MCP setup — token issuance that displays the one-time secret (never stored), per-connection write-mode switching, and guided setup for the Five AIs** so that **I can connect my AI agents to Orvex without the SPA ever handling token-minting material** (FR-UI25).

**Definition of Done:** ONE named test `TestOneTimeSecretDisplayedNeverStored` — an integration test asserting token issuance (BFF→identity mints) displays the one-time secret and the SPA never stores it, per-connection write-mode visibility/switching works (mode changes apply to subsequent content only, stated in UI), and the capture feed status renders, verified through the render + store state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given token issuance, When requested, Then the BFF→identity mints and the SPA displays the one-time secret and never stores it. *Assertion: `TestOneTimeSecretDisplayedNeverStored` — secret absent from any store.* [Source: FR-UI25, A-STATELESS]
- [ ] **AC2** — Given a connection, When viewed, Then per-connection write-mode visibility + switching works (Free direct / £7 direct / £7 curated-default per FR-53); mode changes apply to subsequent content only, stated in the UI. *Assertion: write-mode switch + "subsequent only" statement.* [Source: FR-UI25, FR-53]
- [ ] **AC3** — Given the Five AIs, When set up, Then guided setup UX (Beta) for their MCP clients renders. *Assertion: guided setup present.* [Source: FR-UI25]
- [ ] **AC4** — Given credentials, When displayed, Then per-credential trust/lifecycle display + capture feed status render. *Assertion: trust/lifecycle + feed status.* [Source: FR-UI25]
- [ ] **AC5 (negative)** — Given the SPA, When handling tokens, Then it never mints tokens and never stores token-minting material beyond the one-time secret display. *Assertion: no mint; no persisted secret.* [Source: FR-UI25, NFR-UI4]

## 🔨 Tasks

- [ ] RED: `TestOneTimeSecretDisplayedNeverStored` (AC1/AC5).
- [ ] GREEN: token issuance UI (AC1); write-mode switch (AC2); guided setup (AC3); trust/lifecycle + feed (AC4); no-mint guarantee (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Connections & MCP setup (one-time secret, write-mode switch, guided setup).

React-front connections surface (CS §6; A-STATELESS). Seam: BFF→identity mint (the SPA never mints/verifies). Sibling dependency: E1-S2 (client), E1-S6 (entitlement for write-mode tiers).

## 🧪 Testing

`TestOneTimeSecretDisplayedNeverStored` (integration) + unit tests on the secret-display lifecycle. CS §5 mocking: BFF mint fixtures; never mock own connections UI.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · NFR-UI4 no secrets stored · §11 honest · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: security lens (no token minting/persistence in the SPA).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI25, FR-53, NFR-UI4) · Phase-2 PRD `gh6tVUtrwr`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the token-issuance shapes are the gate).
- [ ] **Parent epic:** E7.
- [ ] **Intra-service order:** after E1-S2/S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
