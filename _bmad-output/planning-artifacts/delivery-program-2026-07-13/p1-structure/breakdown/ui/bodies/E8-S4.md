## 🎯 Story

As a **build agent**, I want **the product-metric SM funnel instrumentation emitted to the BFF metrics ingest, first-party only** so that **the success-metric funnel is measurable and no third-party tracker ever ships** (FR-UI30).

**Definition of Done:** ONE named test `TestSmFunnelEmitsFirstPartyOnly` — an integration test asserting the SM funnel events (first-generic-copy clock, onboarding beats, graduation, import start/complete, enrichment latency samples, white-screen detector) emit to the BFF's metrics ingest with no third-party tracker and no PII in the payload, verified through the captured emissions. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the funnel, When events fire, Then first-generic-copy clock, onboarding beats, graduation, import start/complete, enrichment latency samples, and the white-screen detector all emit to the BFF metrics ingest. *Assertion: `TestSmFunnelEmitsFirstPartyOnly` — all funnel events reach the ingest.* [Source: FR-UI30]
- [ ] **AC2** — Given emission, When it happens, Then it is first-party only — no third-party trackers, no ads. *Assertion: no non-`/api` telemetry origin (ties to NFR-UI5 guard).* [Source: FR-UI30, NFR-UI9]
- [ ] **AC3** — Given the white-screen detector, When a surface renders blank, Then it emits a signal (SM-2 observability). *Assertion: blank render triggers the detector.* [Source: FR-UI30]
- [ ] **AC4 (negative)** — Given a payload, When emitted, Then no PII/token material is included. *Assertion: payload scrubbed; no PII/token.* [Source: FR-UI30, NFR-UI4]
- [ ] **AC5** — Given enrichment, When run, Then latency samples are captured for the budget dashboards. *Assertion: enrichment latency sample emitted.* [Source: FR-UI30]

## 🔨 Tasks

- [ ] RED: `TestSmFunnelEmitsFirstPartyOnly` (AC1/AC2).
- [ ] GREEN: funnel event emitters (AC1); first-party transport (AC2); white-screen detector (AC3); PII scrub (AC4); enrichment latency sampling (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no product-metric instrumentation (SM funnel to BFF ingest); no analytics/telemetry/instrumentation code anywhere in `src/` or `worker/`.

Cross-cutting instrumentation (CS §6). Seam: BFF metrics ingest. Sibling dependency: E1-S2 (client), E2-S2 (onboarding beats), E5-S4 (enrichment), E6-S2 (import). No third-party trackers is a non-goal.

## 🧪 Testing

`TestSmFunnelEmitsFirstPartyOnly` (integration) + unit tests on the emitter + scrub. CS §5 mocking: BFF ingest fixture; never mock own emitter.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest (white-screen detector) · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: privacy lens (no PII in metrics); first-party-only.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI30, NFR-UI9) · Architecture `DmJsnB5Z9Y` §7.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the metrics-ingest shape is the gate).
- [ ] **Parent epic:** E8.
- [ ] **Intra-service order:** after E2-S2; taps enrichment (E5-S4) + import (E6-S2) events.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
