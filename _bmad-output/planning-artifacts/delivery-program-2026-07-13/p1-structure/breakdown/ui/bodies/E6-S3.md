## 🎯 Story

As a **user**, I want **the Chat History surface — browse by provider/date, instant search, faithful conversation render, and retention controls** so that **my imported/captured conversations are a first-class, searchable, deletable corpus** (FR-UI22).

**Definition of Done:** ONE named test `TestConversationRendersFaithfullyWithProvenance` — an integration test asserting a Conversation renders faithfully (turns ordered, code blocks intact) with provenance (provider, date, import-vs-capture) and cross-links to the wiki/Memory items the Curator distilled from it, and instant search returns within the surfaced ≤300ms p95 warm budget, verified through rendered output + timings. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the surface, When browsed, Then it is free and first-class: browse by provider/date with instant search (backed by orvex-studio-knowledge via the BFF, ≤300ms p95 warm surfaced). *Assertion: browse + search within budget.* [Source: FR-UI22]
- [ ] **AC2** — Given a Conversation, When opened, Then it renders faithfully (turns ordered, code blocks intact) with provenance (provider, date, import-vs-capture). *Assertion: `TestConversationRendersFaithfullyWithProvenance` — faithful + provenance.* [Source: FR-UI22]
- [ ] **AC3** — Given a distilled item, When shown, Then cross-links to the wiki/Memory items the Curator distilled from it render. *Assertion: cross-links present.* [Source: FR-UI22]
- [ ] **AC4** — Given retention controls, When used, Then per-user Archive auto-delete setting + delete all/per-provider/per-conversation work with honest confirmation of what hard-deletes. *Assertion: retention ops + honest hard-delete confirm.* [Source: FR-UI22]
- [ ] **AC5 (negative)** — Given remote conversation content, When rendered, Then it is XSS-sanitized before DOM insertion. *Assertion: sanitized; no script execution.* [Source: FR-UI22, NFR-UI4]

## 🔨 Tasks

- [ ] RED: `TestConversationRendersFaithfullyWithProvenance` (AC2).
- [ ] GREEN: browse + instant search (AC1); faithful render + provenance (AC2); cross-links (AC3); retention controls (AC4); sanitization (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Chat History surface (browse, search, conversation render, retention controls).

React-front history surface (CS §6 — knowledge search is the BFF's). Seam: BFF chat-history + knowledge search. Sibling dependency: E6-S2 (import), E7-S1 (wiki cross-links). Search is one knowledge corpus.

## 🧪 Testing

`TestConversationRendersFaithfullyWithProvenance` (integration) + unit tests + sanitization test. CS §5 mocking: BFF history/search fixtures; never mock own render.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest hard-delete confirm · NFR-UI4 sanitize · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: fidelity + security (XSS) lenses.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI22, NFR-UI4) · Phase-2 PRD `gh6tVUtrwr`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the chat-history + search shapes are the gate).
- [ ] **Parent epic:** E6.
- [ ] **Intra-service order:** after E6-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
