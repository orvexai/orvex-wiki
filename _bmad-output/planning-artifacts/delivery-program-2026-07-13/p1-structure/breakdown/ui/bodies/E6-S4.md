## 🎯 Story

As a **user or curator**, I want **the Curator desk + Curation Queue — grouped proposals, per-group approve/reject, provenance, and diff-style previews** so that **I can review the Librarian's proposals efficiently and nothing lands without my recorded approval** (FR-UI23, G3).

**Definition of Done:** ONE named test `TestQueueNothingLandsWithoutRecordedApproval` — an integration test asserting proposals arrive grouped (topic/conversation cluster), approve/reject works singly or per-group only (no global approve-all), every proposal shows provenance + a diff-style preview, and nothing renders as landed without the recorded approval event, verified through the render + state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the Curator desk, When opened, Then it is distinct from Your Wiki (PO boundary) and shows pending proposals, recent activity, feed status (MCP capture, imports). *Assertion: desk distinct from wiki; feed status present.* [Source: FR-UI23]
- [ ] **AC2** — Given proposals, When they arrive, Then they are grouped (topic/conversation cluster); approve/reject is single or per-group only — no global approve-all; bulk flows nudge a spot-check of samples. *Assertion: no global approve-all; spot-check nudge.* [Source: FR-UI23]
- [ ] **AC3** — Given a proposal, When shown, Then it displays provenance (source conversations) and a diff-style preview for updates; structure proposals (FR-52a) render before/after previews. *Assertion: provenance + diff preview; before/after for structure.* [Source: FR-UI23, FR-52a]
- [ ] **AC4** — Given nothing approved, When rendered, Then nothing renders as landed without the recorded approval event; over-budget processing renders the honest "still processing" status. *Assertion: `TestQueueNothingLandsWithoutRecordedApproval` — no land pre-approval.* [Source: FR-UI23, G3]
- [ ] **AC5 (scale)** — Given 100+ pending, When interacting, Then it stays efficient (FR-UI10 ≤200ms p95 budgets apply). *Assertion: interaction budget met at 100+.* [Source: FR-UI23, FR-UI10]

## 🔨 Tasks

- [ ] RED: `TestQueueNothingLandsWithoutRecordedApproval` (AC4).
- [ ] GREEN: Curator desk (AC1); grouped proposals + per-group approve (AC2); provenance + diff previews (AC3); recorded-approval gating + still-processing (AC4); scale (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Curator desk / Curation Queue.

React-front curation surface (CS §6; A-DRAFT). Seam: BFF curator queue. Sibling dependency: E1-S4 (proposal layer), E3-S5 (scale/virtualization), E6-S2 (import feed). Shares virtualization with Memory wall.

## 🧪 Testing

`TestQueueNothingLandsWithoutRecordedApproval` (integration) + scale test at 100+. CS §5 mocking: BFF queue fixtures; never mock own queue/proposal layer.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest (no land pre-approval) · §4 perf · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: draft-vs-committed + scale lenses.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI23, FR-52a, G3) · Phase-2 PRD `gh6tVUtrwr`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the curator queue shapes are the gate).
- [ ] **Parent epic:** E6.
- [ ] **Intra-service order:** after E1-S4, E3-S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
