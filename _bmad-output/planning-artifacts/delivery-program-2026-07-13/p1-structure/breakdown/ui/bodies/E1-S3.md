## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **build agent**, I want **the `orvex-ds.css` design-system consumed exact-match (literal tokens, both theme sets, both skins) with theme set on `<html data-theme>` before first paint** so that **every surface renders the adopted look with zero theme-flash and token-only markup** — the DS is a ship-gate, not a suggestion (A-DS).

**Definition of Done:** ONE named test `TestThemeAppliedBeforeFirstPaint` — a Playwright test asserting `document.documentElement` carries the resolved `data-theme` (from `localStorage orvex-theme`, default light) before the first meaningful paint, with no flash-of-wrong-theme frame, verified through the rendered DOM/screenshot. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the DS file, When markup renders, Then only token references are used (no raw hex outside token definitions). *Assertion: token-fidelity lint (E1-S1) green across the tree.* [Source: NFR-UI3, A-DS]
- [ ] **AC2** — Given a stored theme, When the app boots, Then `data-theme` is set pre-paint and `color-scheme` follows. *Assertion: no flash frame in the reduced-motion screenshot diff; `data-theme` present before hydration.* [Source: NFR-UI3]
- [ ] **AC3** — Given both themes, When a surface renders, Then the two blessed AA deviations (`--cta`, `--faint`) honor their constraints (`.btn-cta` ≥1.1875rem/700; `--faint` fine-print only). *Assertion: usage-constraint lint green.* [Source: NFR-UI3]
- [ ] **AC4** — Given the default (expressive) and Professional skins, When a surface renders, Then both resolve from the same token contract. *Assertion: skin switch changes token set with no untokened value.* [Source: NFR-UI3, FR-UI10]
- [ ] **AC5 (negative)** — Given glass surfaces, When placed over imagery, Then the build/lint flags it (glass only on gradient/solid). *Assertion: lint fails on glass-over-image.* [Source: NFR-UI3]

## 🔨 Tasks

- [ ] RED: `TestThemeAppliedBeforeFirstPaint` (AC2).
- [ ] GREEN: import `orvex-ds.css` as-is; inline pre-paint theme bootstrap; wire dual-skin token switch; constraint lints (AC1/AC3/AC4/AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** present — full dual-theme/dual-skin substrate + pre-first-paint inline script + `orvex-ds.css` token layer, all unit-tested (`src/lib/theme.ts`,`skin.ts`,`useTheme.ts`,`useSkin.ts` + `.test`s, `index.html`). Caveat: 'exact-match' token fidelity to canonical DS is only hex-ban-linted, not token-diffed — verify at hardening.

React-front presentational layer (CS §6). Seam: DS wiki node is visual truth (`C9ufBCuXsy`/`j8ZO0NAB2A`) — consumed as-is per OQ-UI7 posture (no Tailwind port). Sibling dependency: E1-S1 (lint host). Underpins every surface's look-good bar.

## 🧪 Testing

`TestThemeAppliedBeforeFirstPaint` (Playwright) + unit lint-rule tests. CS §5 mocking: none of our own; render real DS tokens. No external mock.

## 📏 Guidance

- CS `6aMAzsYeQb`: §3 naming · §6 shallow view · §11 honest states (no theme-flash) · §5 exact-pin.
- SE-Arch `8sYi523i4t`: DS-fidelity lens — token drift is a defect.
- Cell-lint `JGAUQRsw2g`: N/A runtime; CI gate only.

## 🔗 References

PRD `xsRMrju3D1` (NFR-UI3, D-UI5) · Architecture `DmJsnB5Z9Y` §2 (A-DS) · DS `C9ufBCuXsy` / tokens `j8ZO0NAB2A`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E1.
- [ ] **Intra-service order:** after E1-S1; pairs with E8-S1 (a11y).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
