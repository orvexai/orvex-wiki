## 🎯 Story

As a **new user**, I want **the user-initiated form→Home morph choreography with a reduced-motion instant swap** so that **finishing my Memory form feels like a reward, not a jump-cut — and it degrades to an instant end-state swap for those who need it** (FR-UI6).

**Definition of Done:** ONE named test `TestFormToHomeMorphAndReducedMotion` — a Playwright test asserting the "See what this gets me →" transition runs the four-beat choreography (form recedes ≤380ms, zones stagger-reveal, one "For your role" pulse, ≤2.8s to first shelf), fires exactly one polite aria-live announcement, moves focus to SEARCH at settle, and collapses to an instant end-state-identical swap under `prefers-reduced-motion`, verified through the rendered timeline + a11y tree. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a completed form, When the user taps "See what this gets me →" (never automatic), Then the four-beat choreography runs (recede ≤380ms, stagger-reveal, one pulse ring, ≤2.8s to first shelf). *Assertion: user-initiated; timing budgets met.* [Source: FR-UI6]
- [ ] **AC2** — Given the morph, When it settles, Then exactly one polite aria-live announcement fires and focus moves to the SEARCH input. *Assertion: single announcement; focus at SEARCH.* [Source: FR-UI6, NFR-UI2]
- [ ] **AC3** — Given `prefers-reduced-motion`, When the morph triggers, Then it collapses to an instant, end-state-identical swap. *Assertion: no animation frames; identical end state.* [Source: FR-UI6, NFR-UI2]
- [ ] **AC4 (error)** — Given a persist failure, When it occurs, Then the user is never bounced back — a non-blocking retry toast shows over settled Home. *Assertion: no back-navigation; retry toast present.* [Source: FR-UI6]
- [ ] **AC5 (negative)** — Given the morph, When it runs, Then no CLS occurs (reserved space). *Assertion: zero layout shift through the morph.* [Source: NFR-UI1]

## 🔨 Tasks

- [ ] RED: `TestFormToHomeMorphAndReducedMotion` (AC1/AC3).
- [ ] GREEN: choreography (AC1); aria-live + focus (AC2); reduced-motion swap (AC3); non-blocking retry (AC4); reserved-space layout (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Form->Home morph choreography and no reduced-motion instant-swap (no onboarding form exists to morph from).

React-front animated transition (CS §6). Seam: BFF persist for the form. Sibling dependency: E3-S1 (form), E2-S4 (Home zones). Motion is a NFR-UI2 gate item.

## 🧪 Testing

`TestFormToHomeMorphAndReducedMotion` (Playwright) + reduced-motion snapshot. CS §5 mocking: BFF persist fixture; never mock own choreography.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest (no bounce-back) · §3 naming.
- SE-Arch `8sYi523i4t`: a11y-as-gate lens (reduced-motion, single announcement).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI6, NFR-UI1/UI2) · First-Run spec `KeREQUiGjA` §11.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E3.
- [ ] **Intra-service order:** after E3-S1 and E2-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
