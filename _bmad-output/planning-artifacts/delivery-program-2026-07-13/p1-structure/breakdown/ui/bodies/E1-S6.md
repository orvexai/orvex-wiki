## 🎯 Story

As a **build agent**, I want **entitlement rendering fed by the single BFF SSE entitlement channel, plus the over-quota 402 state** so that **gate states render from one principal-keyed {user|org} verdict shape, the paywall dissolves <60s without reload, and wiki-touching writes render the engine's `QUOTA_EXCEEDED` honestly** (FR-UI28, NFR-UI10, A-AUTH).

**Definition of Done:** ONE named test `TestEntitlementFlipDissolvesPaywallUnder60s` — an integration test that opens the SSE entitlement channel, pushes a `billing.entitlement.changed`-driven verdict, and asserts the gated feature unlocks in-session without reload within the 60,000ms ceiling, verified through the rendered gate state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a BFF verdict, When a gated action renders, Then it reads one principal-keyed {user|org} verdict shape and shows locked + honest upsell naming exactly what £7 adds. *Assertion: gate state derives from the verdict; hiding is cosmetic — the action still round-trips.* [Source: FR-UI28, A-AUTH, D-S17]
- [ ] **AC2** — Given the SSE channel, When an entitlement flip arrives, Then the paywall dissolves in-session ≤60,000ms with no reload/gesture. *Assertion: `nfr-budget.json` `entitlement-freshness` ceiling met (ENG-1570).* [Source: NFR-UI10, D-S20]
- [ ] **AC3** — Given the stream drops, When it is severed, Then refetch-on-focus/navigation + bounded TTL (`FALLBACK_POLL_MS = 60_000`) is the fallback floor. *Assertion: verdict re-fetched on focus; freshness still ≤ ceiling.* [Source: NFR-UI10]
- [ ] **AC4 (error)** — Given a wiki-touching write, When the engine returns 402 `QUOTA_EXCEEDED`, Then the SPA renders a distinct honest quota-reached state with the upgrade path, states nothing was deleted, and never gates reads/downloads/exports. *Assertion: 402 → quota state; reads/exports remain available.* [Source: FR-UI28, F-QUOTA engine `EPsdD7uK8e`]
- [ ] **AC5 (negative)** — Given downgrade/lapse, When entitlement drops, Then the "nothing deleted, manual product fully usable" truth renders. *Assertion: no data-loss messaging; manual features stay usable.* [Source: FR-UI28, D-S19]

## 🔨 Tasks

- [ ] RED: `TestEntitlementFlipDissolvesPaywallUnder60s` (AC2).
- [ ] GREEN: `useEntitlementStream` + `src/lib/entitlements.ts`; verdict-shape reader + gate components (AC1); fallback floor (AC3); 402 quota state (AC4); lapse messaging (AC5); wire `nfr-budget.json`.

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — single SSE entitlement channel + tier-locked banner present & tested (`src/lib/entitlements.ts`, `entitlements.test.tsx`, `Shell.tsx`, `Settings.tsx`). REMAINS: no over-quota 402 upgrade STATE (apiFetch throws generic ApiError); degraded fallback refetch is a TODO no-op — BFF endpoint is scaffold-only.

React-front entitlement layer (CS §6). Seam: BFF SSE entitlement channel + REST verdict (remote-but-owned); engine 402 surfaced via BFF. Sibling dependency: E1-S2 (client), E1-S5 (honest states). Consumed by E3-S5, E5-S4, E6, E7-S3.

## 🧪 Testing

`TestEntitlementFlipDissolvesPaywallUnder60s` (integration) + unit tests on the verdict reader + 402 mapping. CS §5 mocking: fixture the BFF SSE + verdict (sibling); never mock our own entitlement lib. Machine-check via `nfr-budget.json`.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view (no client entitlement math) · §7 contract types · §11 honest states · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: server-verdict-is-truth lens — client gating is cosmetic only.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI28, NFR-UI10, D-S19/D-S20) · Architecture `DmJsnB5Z9Y` §2 (A-AUTH-UI) · engine PRD `EPsdD7uK8e` (F-QUOTA).

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the entitlement verdict shape + SSE channel is the gate).
- [ ] **Parent epic:** E1.
- [ ] **Intra-service order:** after E1-S2/E1-S5; blocks E7-S3 and all gated surfaces.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
