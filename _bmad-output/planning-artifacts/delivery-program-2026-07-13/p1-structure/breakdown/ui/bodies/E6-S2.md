## 🎯 Story

As a **user**, I want **the Chat-History Import wizard — per-provider guided export flows with content-managed instructions, honest trap-naming, resumable multi-GB upload, and a time-window picker** so that **I can bring my ChatGPT/Claude/Gemini/Grok/Copilot history in without hitting a stuck job or a silent failure** (FR-UI21).

**Definition of Done:** ONE named test `TestImportResumesAndNamesFailures` — an integration test asserting pending-import state survives sign-out and prompts resumption, the multi-GB Archive upload is resumable with loud size warnings, async parse shows visible progress + completion notification, and failures render the named human-readable reason (never a stuck job), verified through the state transitions. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a provider, When starting import, Then guided export-request flows render with content-managed instructions (updatable without deploy) and honest trap-naming (24h-expiring links, Gemini history-off emptiness, Copilot truncation). *Assertion: content-managed instructions; traps named.* [Source: FR-UI21]
- [ ] **AC2** — Given an in-progress import, When the user signs out, Then pending-import state survives and prompts resumption. *Assertion: `TestImportResumesAndNamesFailures` — resumes after sign-out.* [Source: FR-UI21]
- [ ] **AC3** — Given a multi-GB archive, When uploaded, Then upload is resumable with loud size warnings; the Time Window picker (3/6/12 months/everything, default 6) shows imported-vs-skipped counts. *Assertion: resumable upload; window default 6; counts shown.* [Source: FR-UI21]
- [ ] **AC4 (error)** — Given a parse failure, When it occurs, Then the named human-readable reason renders — never a stuck job. *Assertion: named reason; no stuck job.* [Source: FR-UI21]
- [ ] **AC5** — Given completion, When done, Then Free-tier shows the honest Curator upsell; £7/trial links to the resulting Curation Queue proposals; the paste-prompt quick import (FR-47a, Beta) shares the surface with provenance labeling. *Assertion: tiered completion; paste-prompt provenance.* [Source: FR-UI21, FR-47a]

## 🔨 Tasks

- [ ] RED: `TestImportResumesAndNamesFailures` (AC2/AC4).
- [ ] GREEN: per-provider instructions + traps (AC1); resumable pending state (AC2); resumable upload + window picker (AC3); named failures (AC4); tiered completion + paste-prompt (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Chat-History Import wizard.

React-front import wizard (CS §6). Seam: BFF import jobs + content-managed instructions. Sibling dependency: E1-S6 (entitlement), E1-S5 (honest states). Feeds E6-S3/S4.

## 🧪 Testing

`TestImportResumesAndNamesFailures` (integration) + unit tests. CS §5 mocking: BFF import-job fixtures incl. failure frames; never mock own wizard.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest (named failures, no stuck jobs) · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: honesty + resumability lenses.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI21, FR-47a) · Phase-2 PRD `gh6tVUtrwr`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the import job shapes are the gate).
- [ ] **Parent epic:** E6.
- [ ] **Intra-service order:** after E1-S5/S6; blocks E6-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
