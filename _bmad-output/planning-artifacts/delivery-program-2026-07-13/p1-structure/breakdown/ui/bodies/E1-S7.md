## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **build agent**, I want **the serving topology moved to Cloudflare Workers Static Assets with grey-cloud DNS-only cell origins, a region-agnostic single bundle, and runtime `window.CONFIG` injection** so that **one bundle ships worldwide, the Worker routes `/api` by the `cell` claim, and no secret is build-baked** (A-GLOBAL, NFR-UI1) — the scaffold's orange-proxied nginx origin contradicts canon.

**Definition of Done:** ONE named test `TestBundleIsRegionAgnostic` — a build/deploy-integration test asserting the produced bundle contains no region/cell literal and no baked secret, serves `/healthz` with build metadata, and the Worker resolves `/api` by cell claim, verified through the built artifact + Worker request. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the Vite build, When it produces assets, Then they ship as Workers Static Assets (canon end-state) with the nginx origin marked a sanctioned Alpha interim only (OQ-UI4). *Assertion: deploy manifest targets Workers Static Assets; interim is labelled, not the end-state.* [Source: A-GLOBAL, Architecture §1/§6]
- [ ] **AC2** — Given multi-region, When the bundle is built, Then it contains no region/cell in any URL and no per-cell deploy. *Assertion: `TestBundleIsRegionAgnostic` — grep finds no cell literal; one bundle worldwide.* [Source: A-GLOBAL, D-UI10]
- [ ] **AC3** — Given service origins, When DNS is configured, Then per-cell origins are grey-cloud DNS-only (LE cert, no Worker route), never orange-proxied. *Assertion: HTTPRoute/DNS posture is grey-cloud; no `cloudflare-proxied: "true"` on service origins.* [Source: A-GLOBAL, Architecture §6]
- [ ] **AC4** — Given per-env config, When the app boots, Then Clerk publishable key + `/api` base are runtime-injected via `window.CONFIG`, never build-baked. *Assertion: no secret/key in the bundle; config read at runtime.* [Source: A-STATELESS, NFR-UI4]
- [ ] **AC5 (negative)** — Given `/healthz`, When probed, Then it returns liveness "ok" + build metadata (commit, version) and never a dependency round-trip. *Assertion: static liveness only (CS §10); metadata present (NFR-UI9).* [Source: NFR-UI9, Architecture §7]

## 🔨 Tasks

- [ ] RED: `TestBundleIsRegionAgnostic` (AC2/AC4).
- [ ] GREEN: Workers Static Assets deploy path + Worker `/api` cell routing (AC1/AC3); `window.CONFIG` inject (AC4); `/healthz` build metadata (AC5); env→URL hosts (`orvex.ai`/`orvex.dev`/`crew-{name}.orvex.dev`); prod-sourcemaps off.

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** present — Workers Static-Assets serving path fully implemented & tested (`worker/index.ts`, `worker/__tests__/*`, `wrangler.toml`, `src/lib/config.ts`, `scripts/cutover-worker.sh`): cache policy, runtime `/config.js` injection, same-origin `/api` reverse-proxy with prefix-strip + edge-secret + fail-loud 502s.

Deploy/topology for a cell-free static SPA (CS §6/§10; Architecture §1/§6). Seam: Cloudflare Worker ↔ cell origins (cell contract `JGAUQRsw2g` §2). Sibling dependency: E1-S2 (token cell claim). Serving-posture drift is an explicit audit risk.

## 🧪 Testing

`TestBundleIsRegionAgnostic` (build/deploy-integration) + unit tests on the config injector. CS §5 mocking: none of our own; assert against the real built artifact and a faked Worker request.

## 📏 Guidance

- CS `6aMAzsYeQb`: §5 exact-pin, no baked secrets · §10 liveness/health split · §11 honest build-state.
- SE-Arch `8sYi523i4t`: serving-topology ADR trigger — record it; do not orange-proxy service origins.
- Cell-lint `JGAUQRsw2g` §2/§5: cell-claim edge routing; no host literals in HTTPRoute; enrol required status.

## 🔗 References

PRD `xsRMrju3D1` (D-UI10, NFR-UI1/UI9, Rollout) · Architecture `DmJsnB5Z9Y` §1/§5/§6 · cell contract `JGAUQRsw2g`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E1.
- [ ] **Intra-service order:** parallel with E1-S2 (needs the cell-token shape).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
