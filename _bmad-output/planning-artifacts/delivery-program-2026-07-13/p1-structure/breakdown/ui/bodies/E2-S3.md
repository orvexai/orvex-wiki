## 🎯 Story

As a **visitor**, I want **a public marketing front door and the full route map, with a signed-in `/` → `/app` client redirect and deep-link resolution** so that **I land the right place from any URL and a live session never sees marketing** (FR-UI3).

**Definition of Done:** ONE named test `TestFrontDoorRoutesAndRedirects` — a router-integration test asserting `/` renders public marketing, "Browse skills" reaches the wall-free `/app`, a live session at `/` client-redirects to `/app`, and the canon sub-routes resolve, verified through the router state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/`, When anon visits, Then the public marketing page renders; "Get started free" → sign-up → onboarding and "Browse skills" → wall-free `/app`. *Assertion: routes resolve as specified.* [Source: FR-UI3]
- [ ] **AC2** — Given a live session, When it hits `/`, Then the SPA client-redirects to `/app` (edge 307 is an infra upgrade, OQ-UI2). *Assertion: signed-in `/` → `/app`.* [Source: FR-UI3, OQ-UI2]
- [ ] **AC3** — Given the route map, When sub-routes are hit, Then `/app/s/:skillId`, `/app/s/:skillId/use`, `/app/s/:skillId/make`, `/app/create/:skillId`, `/app/account`, `/discover/search?q=` all resolve. *Assertion: each route mounts its surface.* [Source: FR-UI3]
- [ ] **AC4** — Given `/app/account` deep-link, When resolved, Then it opens into the footer-menu surface. *Assertion: account deep-link mounts the account surface.* [Source: FR-UI3]
- [ ] **AC5 (negative)** — Given an unknown route, When hit, Then an honest not-found renders that routes back into the product (never a white screen). *Assertion: no white screen; recovery path present.* [Source: FR-UI3, FR-UI29]

## 🔨 Tasks

- [ ] RED: `TestFrontDoorRoutesAndRedirects` (AC1/AC2/AC3).
- [ ] GREEN: marketing `/` + route map (AC1/AC3); signed-in client redirect (AC2); account deep-link (AC4); not-found (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — data-driven routing map + honest 404 + signed-out gate present & tested (`src/routes/index.tsx`, `index.test.tsx`, `NotFound.tsx`, `SignInAffordance.tsx`). REMAINS: no front-door landing, no post-auth deep-link restore, no client session-redirect flow.

React-front routing (CS §6). Seam: Clerk session read for redirect; BFF for surface data. Sibling dependency: E2-S1/S2. OQ-UI3 (SEO prerender) is deferred.

## 🧪 Testing

`TestFrontDoorRoutesAndRedirects` (router-integration) + unit tests on the redirect guard. CS §5 mocking: Clerk (true-external); BFF fixtures.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest not-found · §3 naming.
- SE-Arch `8sYi523i4t`: honesty lens (no dead-ends).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI3, OQ-UI2/UI3) · First-Run spec `KeREQUiGjA`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E2.
- [ ] **Intra-service order:** after E2-S1/S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
