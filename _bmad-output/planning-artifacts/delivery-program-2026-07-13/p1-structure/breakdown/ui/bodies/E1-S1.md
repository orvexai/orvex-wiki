## 🎯 Story

As a **build agent**, I want **a CI ship-gate harness (vitest + Playwright + eslint + axe + visual-regression + DS token-fidelity lint) enrolled in the org-level `cell-lint` required status** so that **every subsequent surface is provable and no red build can merge** — the audit flags all of these as unlanded despite being PRD hard gates.

**Definition of Done:** ONE named test `TestCiHarnessBlocksRedBuild` — a CI-layer test that runs the harness against a deliberately-failing fixture (a raw-hex token, a missing focus-ring, a non-`/api` egress) and asserts the pipeline exits non-zero on each, verified through the CI runner interface, not by inspecting config. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a fresh clone, When CI runs, Then vitest + Playwright + `tsc -b` + eslint (`no-explicit-any`) execute as required checks. *Assertion: `.github/workflows` job matrix contains all five and the org `cell-lint` required-status includes this repo.* [Source: NFR-UI2, Architecture §7]
- [ ] **AC2** — Given a component with a raw hex value outside token definitions, When the token-fidelity lint runs, Then the build fails. *Assertion: lint exit code ≠ 0 with a rule-id naming the offending line.* [Source: NFR-UI3, A-DS]
- [ ] **AC3** — Given an axe scan on a fixture with a contrast violation, When the a11y gate runs, Then the build fails. *Assertion: axe results `violations.length > 0` fails the job.* [Source: NFR-UI2]
- [ ] **AC4** — Given a hardcoded non-`/api`, non-Clerk, non-Stripe origin, When the egress guard runs, Then the build fails. *Assertion: guard exit ≠ 0 naming the origin.* [Source: NFR-UI5]
- [ ] **AC5 (negative)** — Given a clean commit, When CI runs, Then all gates pass and the merge is allowed. *Assertion: pipeline exit 0; required-status green.* [Source: NFR-UI2]

## 🔨 Tasks

- [ ] RED: add `TestCiHarnessBlocksRedBuild` with three failing fixtures (AC1–4).
- [ ] GREEN: wire vitest/Playwright/eslint/`tsc -b`, axe runner, visual-regression, token-lint, egress guard; enrol in `cell-lint` required status (AC1–5).
- [ ] Add prod-sourcemap-off + exact-pin dep check (audit debt) (AC1).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — most gates present in `.github/workflows/ci.yml` (vitest+coverage, Playwright e2e, tsc app+worker, eslint A-SHELL boundary, axe, pnpm audit, NFR-budget, worker gate, actionlint, FAIL-never-SKIP + `scripts/check-no-hardcoded-hex.sh`). REMAINS: no visual-regression gate and no org cell-lint required-status enrolment; no fixture-driven `TestCiHarnessBlocksRedBuild`.

Tooling layer for a React front (CS §6 — presentational adapter; no domain logic here, only gates). Seam crossed: CI ↔ repo required-status (`cell-lint` `JGAUQRsw2g` §5). Sibling dependency: none — this is Wave-0 root; every other story's DoD test runs on this harness.

## 🧪 Testing

`TestCiHarnessBlocksRedBuild` (CI-integration layer) + unit tests for the token-lint and egress-guard rules. CS §5 mocking: mock nothing of our own; the guard/lint operate on real source fixtures. Do not mock the CI runner.

## 📏 Guidance

- CS `6aMAzsYeQb`: §0 adversarial-review gate · §3 naming · §4 `tsc -b` + eslint `no-explicit-any` (❌#12) · §5 exact-pin deps · §10 liveness/health · §11 honest states (no fake-done).
- SE-Arch `8sYi523i4t`: Done-gate lens — a gate that cannot fail a red build is not a gate.
- Cell-lint `JGAUQRsw2g` §5: enrol this repo in the org-level required `cell-lint` status.

## 🔗 References

PRD `xsRMrju3D1` (NFR-UI2/UI3/UI5) · Architecture `DmJsnB5Z9Y` §7 · cell contract `JGAUQRsw2g` · DS `C9ufBCuXsy` / tokens `j8ZO0NAB2A`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E1.
- [ ] **Intra-service order:** none — root story; unblocks all others.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
