## 🎯 Story

As a **new user**, I want **the AI-declared Memory FormSpec to stream in place over SSE, with a byte-identical deterministic fallback when the stream is slow** so that **I get the wow of a role-shaped form appearing — the AI builds the FORM, I fill the VALUES, never AI-invented facts** (FR-UI5, A-STREAM).

**Definition of Done:** ONE named test `TestFormSpecStreamsWithByteIdenticalFallback` — an integration test that consumes the `scaffold_proposed → scaffold_committed → section* → done` SSE grammar rendering empty typed sections, then forces a slow/offline upstream and asserts the deterministic baseline form renders instantly with a structurally byte-identical shape, verified through the rendered form structure. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given GENERATE submit, When the SSE stream runs, Then ~8 role-appropriate empty sections snap in with typed widgets and no AI-generated facts (values are empty). *Assertion: R2 — sections empty; no fabricated values.* [Source: FR-UI5]
- [ ] **AC2** — Given the stream, When it renders, Then budgets hold: TTFT ≤2.5s, first blocks <3s, full <12s. *Assertion: SM-9 budgets met (or honest degrade).* [Source: FR-UI5, G2]
- [ ] **AC3** — Given a slow/offline upstream, When the stream stalls, Then the deterministic baseline form renders instantly, byte-identical in structure, and the AI version replaces it if it returns. *Assertion: `TestFormSpecStreamsWithByteIdenticalFallback` — fallback structure == streamed structure.* [Source: FR-UI5, FR-1b, NFR-UI6]
- [ ] **AC4** — Given the `done` event, When it arrives, Then its category tag (or the deterministic keyword fallback) lights "For your role". *Assertion: category tag drives the shelf.* [Source: FR-UI5]
- [ ] **AC5 (negative/escape)** — Given the persistent "Skip — just browse skills →" (≥48×48, first paint), When tapped, Then it emits the keyword tag and lands on populated Home; partial forms persist and reopen where left. *Assertion: skip works from first paint; partial persists.* [Source: FR-UI5]

## 🔨 Tasks

- [ ] RED: `TestFormSpecStreamsWithByteIdenticalFallback` (AC1/AC3).
- [ ] GREEN: SSE consumer for the FormSpec grammar (AC1/AC2); deterministic fallback (AC3); category-tag wiring (AC4); Skip escape + partial persistence (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no streaming FormSpec onboarding (SSE) and no deterministic fallback; `src/routes/Memory.tsx` is a thin read-only list adapter only.

React-front streaming surface (CS §6/§7; A-STREAM). Seam: BFF SSE (remote-but-owned; grammar pinned in contracts). Sibling dependency: E1-S2 (client), E1-S4 (proposal layer — values as proposals). Feeds E3-S2 morph.

## 🧪 Testing

`TestFormSpecStreamsWithByteIdenticalFallback` (integration) + unit tests on the fallback generator. CS §5 mocking: fixture the BFF SSE from the pinned grammar (sibling); contract-test the fallback stays in lockstep; never mock own consumer.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §7 front types match the pinned SSE grammar exactly · §11 honest degrade · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: streaming-fallback lens — fallback must not diverge from the streamed shape.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI5, G2, SM-9) · Architecture `DmJsnB5Z9Y` §2 (A-STREAM) · First-Run/MA1 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the SSE FormSpec grammar tag is the gate).
- [ ] **Parent epic:** E3.
- [ ] **Intra-service order:** after E1-S2/S4; blocks E3-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
