## 🎯 Story

As a **user who just improved a prompt**, I want **the capture-back seam — a quiet "Save to Memory" CTA after Apply** so that **a useful learning can stick, but only my own value saves, never an AI-invented fact, and sensitive captures stay consent-gated** (FR-UI15).

**Definition of Done:** ONE named test `TestCaptureBackSavesUserValueNotAiFact` — an integration test asserting after Apply the single quiet CTA hands off to the M4.3 flow where the AI suggests the question/section but the user's own value is what saves (never an AI-invented fact), sensitive captures are consent-gated and never auto-woven, and Decline persists nothing, verified through the persisted payload + render. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a completed Apply, When capture-back offers, Then a single quiet CTA renders ("Want this to stick? → Save to Memory"). *Assertion: one quiet CTA present.* [Source: FR-UI15]
- [ ] **AC2** — Given the M4.3 flow, When it runs, Then the AI suggests the question/section but the user's own value is what saves — never an AI-invented fact. *Assertion: `TestCaptureBackSavesUserValueNotAiFact` — persisted value is the user's.* [Source: FR-UI15, R2]
- [ ] **AC3** — Given a sensitive capture, When offered, Then it is consent-gated and never auto-woven. *Assertion: consent required (via E3-S4); no auto-weave.* [Source: FR-UI15, FR-UI8]
- [ ] **AC4 (negative)** — Given Decline, When chosen, Then nothing persists and the Delta stands alone. *Assertion: no persist on decline.* [Source: FR-UI15]
- [ ] **AC5** — Given the promotion, When it renders, Then it shows only after the recorded accept (draft-vs-committed at its sharpest). *Assertion: promotion renders post-accept only.* [Source: FR-UI15, FR-UI27]

## 🔨 Tasks

- [ ] RED: `TestCaptureBackSavesUserValueNotAiFact` (AC2/AC4).
- [ ] GREEN: post-Apply CTA (AC1); M4.3 handoff, user-value save (AC2); sensitive consent gate (AC3); decline no-op (AC4); post-accept promotion (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no capture-back seam (Save to Memory after Apply, consent-gated).

React-front capture seam (CS §6; A-DRAFT). Seam: BFF memory capture. Sibling dependency: E5-S1 (Apply), E3-S4 (consent), E1-S4 (proposal layer).

## 🧪 Testing

`TestCaptureBackSavesUserValueNotAiFact` (integration) + unit tests. CS §5 mocking: BFF memory fixtures + consent; never mock own capture.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: draft-vs-committed + privacy lenses.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI15, FR-UI27) · Improve/MB2 + M4.3 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E5.
- [ ] **Intra-service order:** after E5-S1, E3-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
