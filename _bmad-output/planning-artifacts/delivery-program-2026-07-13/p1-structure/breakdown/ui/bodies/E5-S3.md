## 🎯 Story

As a **user**, I want **five-AI export with correctly-formatted artifacts and a Tier-1 clipboard-blocked fallback** so that **I can take my prompt to ChatGPT, Claude, Gemini, Grok, or Copilot, and Copy never fails** (FR-UI16).

**Definition of Done:** ONE named test `TestFiveAiExportAndClipboardFallback` — an integration test asserting export renders correctly-formatted artifacts for all five engines with "Open {engine} →" affordances (deterministic BFF formatters, no keys/OAuth/wizard), and the clipboard-blocked fallback shows a selectable pre-focused read-only field + "Select all" (≥48×48) + aria-live microcopy, verified through rendered output. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a skill, When exported, Then correctly-formatted artifacts render for ChatGPT, Claude, Gemini, Grok, Copilot (extends the shipped Claude/ChatGPT pair) with "Open {engine} →". *Assertion: five formats + affordances.* [Source: FR-UI16, FR-63]
- [ ] **AC2** — Given formatting, When it runs, Then it uses deterministic formatters via the BFF with no keys/OAuth/wizard. *Assertion: no client keys; BFF formatters.* [Source: FR-UI16]
- [ ] **AC3 (fallback)** — Given clipboard is blocked, When Copy is attempted, Then a selectable pre-focused read-only field + "Select all" (≥48×48) + aria-live microcopy renders. *Assertion: `TestFiveAiExportAndClipboardFallback` — fallback path renders + accessible.* [Source: FR-UI16, NFR-UI2]
- [ ] **AC4 (negative)** — Given any degradation/auth/entitlement state, When Copy is used, Then it never degrades (Tier-1). *Assertion: Copy works in anon + degraded + offline.* [Source: FR-UI16, FR-UI29]
- [ ] **AC5** — Given export, When rendered, Then enrichment is never persisted into the exported artifact (clean prompt only unless per-use enrich, per FR-UI18). *Assertion: stored/exported artifact is the clean prompt.* [Source: FR-UI16, FR-UI18]

## 🔨 Tasks

- [ ] RED: `TestFiveAiExportAndClipboardFallback` (AC1/AC3).
- [ ] GREEN: five-engine export UI + affordances (AC1/AC2); clipboard fallback (AC3); Tier-1 Copy (AC4); clean-artifact rule (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no five-AI export and no Tier-1 clipboard-blocked fallback.

React-front outbound surface (CS §6 — formatters are BFF's). Seam: BFF export formatters + engine deep-links. Sibling dependency: E1-S5 (Tier-1 Copy). Shares with E5-S4 launch.

## 🧪 Testing

`TestFiveAiExportAndClipboardFallback` (integration) + unit tests. CS §5 mocking: BFF formatter fixtures; simulate clipboard-blocked; never mock own export UI.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest (Copy never degrades) · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: Tier-1-never-degrades lens.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI16, FR-63) · MB1 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the export formatter shapes are the gate).
- [ ] **Parent epic:** E5.
- [ ] **Intra-service order:** after E1-S5; shares with E5-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
