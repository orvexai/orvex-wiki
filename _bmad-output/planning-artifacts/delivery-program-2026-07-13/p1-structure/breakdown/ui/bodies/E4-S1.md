## 🎯 Story

As a **visitor or user**, I want **the marketplace home with skeleton→outcome-first cards and results-as-you-type search plus composable facets** so that **I can find a skill fast, and never see a blank screen even on a typo or no-result** (FR-UI11).

**Definition of Done:** ONE named test `TestSearchAsYouTypeNeverBlank` — an integration test asserting first-paint transitions skeleton→outcome-first cards (no flash of blank), free-text search renders results-as-you-type within the surfaced ≤300ms p95 warm budget (debounce, optimistic skeletons, no spinner-wall), and typo/no-result renders guidance + suggestions (never blank), verified through rendered states + timings. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given first paint, When the marketplace loads, Then skeleton → outcome-first Skill cards render with no flash of blank. *Assertion: no blank frame; SM-2 = 0.* [Source: FR-UI11, NFR-UI1]
- [ ] **AC2** — Given typing, When the user searches, Then results render as-you-type within the surfaced ≤300ms p95 warm budget (BFF/knowledge owns retrieval; the SPA owns perceived latency via debounce + optimistic skeletons, no spinner-walls). *Assertion: perceived latency budget met; no spinner-wall.* [Source: FR-UI11, G2]
- [ ] **AC3** — Given facets, When applied, Then category (primary) + kind (co-equal secondary) compose with search and show result counts. *Assertion: facets compose; counts shown.* [Source: FR-UI11]
- [ ] **AC4 (error)** — Given a typo/no-result, When it happens, Then guidance + suggestions render — never a blank screen. *Assertion: `TestSearchAsYouTypeNeverBlank` — no-result path shows guidance.* [Source: FR-UI11, FR-UI29]
- [ ] **AC5 (negative)** — Given anon, When browsing, Then the same surface renders wall-free with generic Copy always live. *Assertion: anon variant wall-free; Copy works.* [Source: FR-UI11, R5]

## 🔨 Tasks

- [ ] RED: `TestSearchAsYouTypeNeverBlank` (AC1/AC4).
- [ ] GREEN: skeleton→card render (AC1); debounced search-as-you-type (AC2); facets + counts (AC3); no-result guidance (AC4); anon variant (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — a thin Discover list pane over `/marketplace` with honest-empty exists & tested (`src/routes/Discover.tsx`, `productPanes.test.tsx`). REMAINS: no search-as-you-type, no facets, no anon wall-free variant.

React-front search surface (CS §6 — retrieval is the BFF/knowledge's; the SPA owns perceived latency only). Seam: BFF search. Sibling dependency: E1-S2 (client), E1-S5 (honest states). Feeds E4-S2 detail.

## 🧪 Testing

`TestSearchAsYouTypeNeverBlank` (integration) + unit tests on the debounce. CS §5 mocking: BFF search fixtures; never mock own debounce/render.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view (no client ranking) · §11 honest no-result · §3 naming.
- SE-Arch `8sYi523i4t`: perceived-latency lens; honesty (no blank screens).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI11, G2) · MA2 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E4.
- [ ] **Intra-service order:** after E1-S2/S5; blocks E4-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
