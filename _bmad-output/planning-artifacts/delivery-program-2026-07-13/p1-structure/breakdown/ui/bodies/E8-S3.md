## 🎯 Story

As a **build agent**, I want **security + BFF-only egress hardening + observability enforced** so that **no secret ships in the bundle, product I/O goes only to `/api`, remote content is XSS-sanitized, and web-vitals/errors/build-metadata reach the BFF ingest** (NFR-UI4, NFR-UI5, NFR-UI9).

**Definition of Done:** ONE named test `TestEgressGuardAndCspAndSanitize` — a CI/integration test asserting the build fails on any hardcoded non-`/api` origin (Clerk SDK + Stripe redirect allowlisted), the CSP restricts `connect-src` to self+Clerk and `frame-src` to Stripe/Clerk, rendered remote content (wiki pane, conversations, AI output) is sanitized before DOM insertion, and no PII/token material appears in logs/errors/metrics, verified through the gate + rendered output. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the bundle, When built, Then no provider/model keys, no satellite credentials, no secrets appear (build or runtime). *Assertion: secret scan clean; config runtime-injected only.* [Source: NFR-UI4]
- [ ] **AC2** — Given product network I/O, When it runs, Then it goes to `/api` exclusively; a CI guard fails the build on any other hardcoded origin (Clerk SDK + Stripe redirect allowlisted). *Assertion: `TestEgressGuardAndCspAndSanitize` — egress guard fails a seeded rogue origin.* [Source: NFR-UI5, A-SHELL]
- [ ] **AC3** — Given the CSP, When set, Then `connect-src` = self+Clerk and `frame-src`/redirect = Stripe/Clerk only; no `@docmost/*`/AGPL import (family guard). *Assertion: CSP + AGPL-import guard green.* [Source: NFR-UI4, NFR-UI5]
- [ ] **AC4** — Given rendered remote content, When inserted, Then it is sanitized against XSS before DOM insertion; no PII/token in console logs, error reports, or metric events. *Assertion: sanitized; no PII/token leakage.* [Source: NFR-UI4]
- [ ] **AC5 (observability)** — Given the app, When it runs, Then web-vitals + JS error reporting go to the BFF ingest (first-party), and build metadata (commit, version) is surfaced at the `/healthz`-style endpoint + in error reports. *Assertion: first-party telemetry; build metadata present.* [Source: NFR-UI9]

## 🔨 Tasks

- [ ] RED: `TestEgressGuardAndCspAndSanitize` with a seeded rogue origin + XSS payload (AC2/AC4).
- [ ] GREEN: secret scan (AC1); egress guard (AC2); CSP + AGPL guard (AC3); sanitizer + log scrub (AC4); web-vitals/error/build-metadata reporting (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — BFF-only egress hardening strong & tested (`eslint.config.js` import-boundary, `apiClient.ts` assertBffBase SSRF guard, `oneBffUpstream.contract.test.ts`, `cellBearingToken.test.ts`, `worker/index.ts` edge-secret). REMAINS: no observability seam (ErrorBoundary just console.errors; no OTel/metrics/error-reporting).

Cross-cutting security/observability (CS §5/§10/§11; A-SHELL/A-STATELESS). Seam: CI + BFF ingest. Sibling dependency: E1-S1 (harness), E1-S2 (client egress), E1-S7 (build metadata). One-upstream invariant is load-bearing (audit risk).

## 🧪 Testing

`TestEgressGuardAndCspAndSanitize` (CI/integration) + unit tests on the sanitizer + log scrub. CS §5 mocking: none of our own; run guards against real source + rendered content.

## 📏 Guidance

- CS `6aMAzsYeQb`: §5 no secrets/AGPL imports · §10 liveness/health metadata · §11 honest · §0 adversarial review.
- SE-Arch `8sYi523i4t`: security lens (one-upstream, CSP, XSS); external-dep boundary.
- Cell-lint `JGAUQRsw2g` §5: required-status enrolment; no host literals.

## 🔗 References

PRD `xsRMrju3D1` (NFR-UI4/UI5/UI9) · Architecture `DmJsnB5Z9Y` §4 (seams) §7.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the metrics-ingest shape is the gate).
- [ ] **Parent epic:** E8.
- [ ] **Intra-service order:** after E1-S1/S2/S7; runs continuously.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
