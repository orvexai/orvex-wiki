## 🎯 Story

As a **user**, I want **the first-run state machine to resolve ANON / NEW-AUTH / RETURNING and mount the right chrome, with the Task Box morphing GENERATE↔SEARCH in place** so that **I get the honest experience for my state and never a cold box or a route change on morph** (FR-UI2).

**Definition of Done:** ONE named test `TestFirstRunResolvesStateAndMorphsInPlace` — an integration test that drives the three states (from Clerk session + BFF `memoryState`) and asserts anon mounts minimal chrome with no Task Box/memory/state, RETURNING resumes (never the cold box), and GENERATE↔SEARCH morphs inside `/app` with no route change, verified through the rendered mount + router state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given no session, When the shell resolves ANON, Then it mounts minimal chrome (wordmark, recessive Sign-in / "Get started free"), a wall-free public marketplace at `/app`, and no Task Box, no memory, no saved state. *Assertion: R5 — anon holds no state.* [Source: FR-UI2, D-UI7]
- [ ] **AC2** — Given a fresh sign-in, When NEW-AUTH resolves, Then it mounts the full shell + honest-empty dashboard + GENERATE nudge. *Assertion: NEW-AUTH mount with honest-empty zones.* [Source: FR-UI2]
- [ ] **AC3** — Given a returning user with `memoryState`, When RETURNING resolves, Then a resume-first dashboard mounts, never the cold box. *Assertion: R4 — no cold box for RETURNING.* [Source: FR-UI2]
- [ ] **AC4** — Given the Task Box, When it toggles GENERATE↔SEARCH, Then it morphs in place inside `/app` with no route change ever. *Assertion: R3 — router path unchanged across morph.* [Source: FR-UI2]
- [ ] **AC5 (negative)** — Given a deep-link, When the user is signed in at a boundary, Then the shell mounts *around* the requested surface and restores the exact surface after boundary sign-in. *Assertion: deep-link wins over dashboard.* [Source: FR-UI2]

## 🔨 Tasks

- [ ] RED: `TestFirstRunResolvesStateAndMorphsInPlace` (AC1/AC3/AC4).
- [ ] GREEN: state resolver from Clerk session + BFF `memoryState`; three mounts (AC1–3); Task Box two-mode morph (AC4); deep-link restore (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no first-run state machine (ANON/NEW-AUTH/RETURNING) and no Task Box two-mode morph; `Shell.tsx` only distinguishes isLoading/needsSignIn/children.

React-front state machine (CS §6 — state derives from `/api` + Clerk). Seam: Clerk session (true-external) + BFF `memoryState` read. Sibling dependency: E2-S1 (shell), E1-S2 (client). Feeds E2-S4 dashboard.

## 🧪 Testing

`TestFirstRunResolvesStateAndMorphsInPlace` (integration) + unit tests on the resolver. CS §5 mocking: Clerk (true-external) + BFF `memoryState` fixtures; never mock own resolver.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest states · §3 naming.
- SE-Arch `8sYi523i4t`: invariant lens (R3/R4/R5 are canon, not options).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI2, D-UI7) · First-Run spec `KeREQUiGjA` · Architecture `DmJsnB5Z9Y` §2.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E2.
- [ ] **Intra-service order:** after E2-S1; pairs with E2-S3/S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
