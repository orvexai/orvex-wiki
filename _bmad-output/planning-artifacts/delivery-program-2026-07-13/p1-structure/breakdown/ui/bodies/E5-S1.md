## 🎯 Story

As a **user on the Builder**, I want **the Improve panel with its five result states and non-optimistic Apply** so that **the AI proposes an improvement, I see honestly why, and nothing applies until the Delta PATCH succeeds** (FR-UI14, A-STREAM/A-DRAFT).

**Definition of Done:** ONE named test `TestImproveApplyNotOptimistic` — an integration test asserting Apply is not optimistic (the Delta PATCH must succeed before "✓ Applied" and the re-render; a failure keeps the diff in view with inline retry), and the cached-exemplar fallback renders labelled "(example)" with Apply disabled (`aria-disabled` + tooltip), verified through the render + state transitions. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/app/s/:id/make`, When Improve opens, Then the focus-trapped overlay (right slide-in desktop / full sheet mobile) shows the two optional inputs (describe the problem / paste the Miss) and the honest "p95 < 6s" label. *Assertion: panel + inputs + label; focus trapped.* [Source: FR-UI14]
- [ ] **AC2** — Given a result, When it returns, Then the five states render: idle · loading (aria-live polite) · diff (names the block + why; old struck on `--discuss-t`, new `--ink` on `--edited-t`, sr-only "Removing:/Adding:") · honest no-good-fix (`fix: null` → info state, recovery paths, never a fabricated diff) · cached-exemplar fallback. *Assertion: all five states; no fabricated diff on null.* [Source: FR-UI14]
- [ ] **AC3** — Given Apply, When clicked, Then it is not optimistic — the Delta PATCH must succeed before "✓ Applied" + re-render; failure keeps the diff with inline retry. *Assertion: `TestImproveApplyNotOptimistic` — no apply before PATCH ok.* [Source: FR-UI14, A-DRAFT]
- [ ] **AC4 (fallback)** — Given a timeout/error, When the cached exemplar shows, Then it is clearly labelled "(example)" with Apply disabled (`aria-disabled` + tooltip). *Assertion: labelled + Apply disabled.* [Source: FR-UI14, NFR-UI6]
- [ ] **AC5 (negative)** — Given anon, When Improve is reached, Then the disabled button + sign-in nudge render. *Assertion: anon sees disabled + nudge.* [Source: FR-UI14, R5]

## 🔨 Tasks

- [ ] RED: `TestImproveApplyNotOptimistic` (AC3/AC4).
- [ ] GREEN: panel + inputs (AC1); five states (AC2); non-optimistic Apply (AC3); cached-exemplar (AC4); anon (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Improve-with-AI panel (five result states, non-optimistic apply, cached-exemplar fallback).

React-front streaming surface (CS §6/§7; A-STREAM/A-DRAFT). Seam: BFF Improve (SSE/JSON) + Delta PATCH. Sibling dependency: E4-S3 (Builder host), E1-S4 (proposal layer). Feeds E5-S2 capture-back.

## 🧪 Testing

`TestImproveApplyNotOptimistic` (integration) + unit tests on the state machine. CS §5 mocking: fixture BFF Improve responses incl. `fix: null` + timeout (sibling); never mock own panel.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §7 contract types · §11 honest (no fabricated diff) · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: draft-vs-committed lens (Apply non-optimistic); streaming-fallback lens.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI14) · Improve spec `DRP67l1a6q` · Architecture `DmJsnB5Z9Y` §2.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the improve response shape is the gate).
- [ ] **Parent epic:** E5.
- [ ] **Intra-service order:** after E4-S3 and E1-S4; blocks E5-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
