## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **build agent**, I want **the WCAG 2.1 AA ship-gate enforced across both themes AND both skins** so that **accessibility is a CI gate baked in from M0, not slipped to the end of the train** (NFR-UI2).

**Definition of Done:** ONE named test `TestWcagAaBothThemesBothSkins` — a CI a11y-gate test running axe + token-pair contrast checks + a reduced-motion snapshot across all surfaces in light+dark and default(expressive)+Professional skins, asserting zero violations and that a seeded violation fails the build, verified through the gate result. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given all surfaces, When the a11y gate runs, Then WCAG 2.1 AA passes in both themes and both skins. *Assertion: `TestWcagAaBothThemesBothSkins` — axe violations = 0 across the matrix.* [Source: NFR-UI2]
- [ ] **AC2** — Given interactives, When rendered, Then ≥48×48px targets everywhere (no exceptions), the shared `--focus-ring` on all interactives, status by text+shape never color-alone. *Assertion: target-size + focus-ring + text+shape checks green.* [Source: NFR-UI2]
- [ ] **AC3** — Given animations, When `prefers-reduced-motion` is set, Then it is honored on every animation (incl. the FN.2 morph + streaming reveals). *Assertion: reduced-motion snapshot has no animation.* [Source: NFR-UI2, FR-UI6]
- [ ] **AC4** — Given regions, When rendered, Then correct aria-live (polite for progress, assertive for no-fix/error), focus-trapped modals/palette/drawer/panels with focus return, keyboard-complete flows, `nav` landmark + skip-link. *Assertion: aria-live + focus-trap + landmark checks green.* [Source: NFR-UI2]
- [ ] **AC5 (negative)** — Given a seeded contrast/focus violation, When the gate runs, Then the build fails. *Assertion: seeded violation → non-zero exit.* [Source: NFR-UI2]

## 🔨 Tasks

- [ ] RED: `TestWcagAaBothThemesBothSkins` with a seeded violation (AC5).
- [ ] GREEN: axe + contrast + reduced-motion matrix runner (AC1/AC3); target-size/focus-ring/text+shape rules (AC2); aria-live/focus-trap/landmark audits (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** present — WCAG 2.1 AA ship-gate implemented & wired (`e2e/a11y.axe.spec.ts`, `playwright.config.ts`, CI e2e job): axe scan over THEMES x SKINS x SURFACES against the real built bundle + keyboard operability.

Cross-cutting a11y gate (CS §6 view + §11; A-DS). Seam: CI (E1-S1 harness). Sibling dependency: E1-S1 (harness), E1-S3 (skins), every feature surface to audit. A11y-as-gate is an explicit audit warning.

## 🧪 Testing

`TestWcagAaBothThemesBothSkins` (CI a11y-gate) + per-component axe unit runs. CS §5 mocking: none of our own; run against real rendered surfaces.

## 📏 Guidance

- CS `6aMAzsYeQb`: §11 honest states · §0 adversarial review · §4 gate discipline.
- SE-Arch `8sYi523i4t`: a11y-as-gate lens — bake into CI from M0.
- Cell-lint `JGAUQRsw2g` §5: part of the required-status suite.

## 🔗 References

PRD `xsRMrju3D1` (NFR-UI2, G5) · Architecture `DmJsnB5Z9Y` §2 (A-DS) §7 · DS `C9ufBCuXsy`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E8.
- [ ] **Intra-service order:** after E1-S1/S3; runs continuously against every feature epic.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
