## 🎯 Story

As a **user**, I want **the private Memory wall with fill/edit/reorder and full FR-31 provenance** so that **I can see and shape what Orvex knows about me, and which Memory was applied to each render — with zero sharing/reputation chrome** (FR-UI7, R1).

**Definition of Done:** ONE named test `TestMemoryWallPrivateWithProvenance` — a component-integration test asserting the wall renders the FormSpec with fill/edit/reorder, every skill render shows full FR-31 provenance ("which Memory was applied") plus a "Held back (sensitive)" label for sensitive-but-not-invoked fields, and no sharing/suggestions/reputation chrome appears, verified through rendered output. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the Memory wall, When it renders, Then it is PRIVATE: no sharing, no suggestions inbox, no reputation chip on any Memory surface. *Assertion: R1 — none of those elements present.* [Source: FR-UI7]
- [ ] **AC2** — Given the FormSpec, When on the wall, Then fields support fill/edit/reorder. *Assertion: edit + reorder work; changes persist via BFF.* [Source: FR-UI7]
- [ ] **AC3** — Given a skill render, When Memory is applied, Then full FR-31 provenance shows "which Memory was applied" per render. *Assertion: per-render provenance present.* [Source: FR-UI7]
- [ ] **AC4** — Given a sensitive-but-not-invoked field, When rendered, Then it shows a "Held back (sensitive)" label. *Assertion: held-back label present.* [Source: FR-UI7]
- [ ] **AC5 (negative)** — Given progress, When shown, Then it is honest filled-vs-empty (no fake %). *Assertion: no % meter.* [Source: FR-UI7, FR-UI4]

## 🔨 Tasks

- [ ] RED: `TestMemoryWallPrivateWithProvenance` (AC1/AC3/AC4).
- [ ] GREEN: wall render + fill/edit/reorder (AC2); FR-31 provenance (AC3); held-back label (AC4); honest progress (AC5); private guarantees (AC1).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — a thin Memory list pane over `/v1/memory` with honest-empty exists & tested (`src/routes/Memory.tsx`, `productPanes.test.tsx`). REMAINS: no Memory wall UI and no FR-31 per-entry provenance.

React-front Memory surface (CS §6). Seam: BFF memory read/write. Sibling dependency: E3-S1 (FormSpec), E1-S5 (honest states). Pairs with E3-S4 consent.

## 🧪 Testing

`TestMemoryWallPrivateWithProvenance` (component-integration) + unit tests. CS §5 mocking: BFF memory fixtures; never mock own wall.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest states · §3 naming.
- SE-Arch `8sYi523i4t`: privacy lens (SM-C3 = 0 leaks); provenance completeness.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI7, R1, FR-31) · Architecture `DmJsnB5Z9Y` §2.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E3.
- [ ] **Intra-service order:** after E3-S1; pairs with E3-S4/S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
