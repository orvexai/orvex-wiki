## 🎯 Story

As a **signed-in user**, I want **a dashboard with honest RETURNING/NEW-AUTH zones that fill live on state transitions** so that **I resume my work with real counts and never a fabricated value or a reload** (FR-UI4).

**Definition of Done:** ONE named test `TestDashboardZonesHonestAndLiveFill` — a component-integration test asserting the RETURNING zones render with a real `memoryHealth.sectionsWithValues` count (never a % meter/live woven line), "For your role" is absent (not empty) without a seed, and zones fill live on a state transition without reload, verified through rendered output. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given RETURNING, When the dashboard mounts, Then zones render top-to-bottom: Task Box (SEARCH) + moat line with a real `memoryHealth.sectionsWithValues` count; "Continue where you left off" (3–5 deep-restored preview cards, honest top-of-artifact fallback, labeled-absent when empty). *Assertion: real count; deep state restored.* [Source: FR-UI4]
- [ ] **AC2** — Given "Your stuff", When rendered, Then it shows Your Skills count, a Memory at-a-glance summary (never a live woven line, never a % meter), Your Wiki compact zone, Review count. *Assertion: no % meter; no woven line.* [Source: FR-UI4]
- [ ] **AC3** — Given no role seed, When "For your role" would render, Then it is absent (not empty) and relabels "Popular starters" when sparse; Discover shows Trending + the never-empty Starter shelf. *Assertion: absent-not-empty; Starter floor always present.* [Source: FR-UI4]
- [ ] **AC4** — Given NEW-AUTH, When the dashboard mounts, Then a GENERATE-nudge card shows, zones are gracefully absent/honest-empty, Starter floor present. *Assertion: NEW-AUTH honest-empty.* [Source: FR-UI4]
- [ ] **AC5 (negative)** — Given a state transition, When state changes, Then zones fill live with no reload. *Assertion: no page reload on transition.* [Source: FR-UI4]

## 🔨 Tasks

- [ ] RED: `TestDashboardZonesHonestAndLiveFill` (AC1/AC2/AC5).
- [ ] GREEN: RETURNING zones (AC1/AC2); role/Starter logic (AC3); NEW-AUTH variant (AC4); live-fill wiring (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — a generic honest-empty Home list pane exists & tested (`src/routes/Home.tsx`, `productPanes.test.tsx`). REMAINS the story core: no RETURNING vs NEW-AUTH dashboard ZONES and no live-fill-on-transition.

React-front dashboard (CS §6). Seam: BFF dashboard data. Sibling dependency: E2-S2 (state machine), E1-S5 (honest states). Consumes many surfaces' summary shapes.

## 🧪 Testing

`TestDashboardZonesHonestAndLiveFill` (component-integration) + unit tests. CS §5 mocking: BFF fixtures (sibling); never mock own zones.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest states (real counts, no % meter) · §3 naming.
- SE-Arch `8sYi523i4t`: honesty lens (absent-not-empty).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI4, G1) · First-Run spec `KeREQUiGjA`.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E2.
- [ ] **Intra-service order:** after E2-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
