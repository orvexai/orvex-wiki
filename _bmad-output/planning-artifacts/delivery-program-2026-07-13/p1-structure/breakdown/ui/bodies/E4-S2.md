## 🎯 Story

As a **user**, I want **the Skill Viewer to preview a skill as labelled typed Items in plain language, with per-block provenance, clean injection slots, and a raw fallback** so that **I understand exactly what a prompt does before I use it** (FR-UI12).

**Definition of Done:** ONE named test `TestViewerRendersTypedItemsAndSlots` — a component-integration test asserting a skill previews as labelled typed Items in plain language (no Delta), per-block provenance renders, every declared injection slot mounts empty/clean, and the raw-fallback path renders the raw original with "view source" disclosure editable as a single Text block, verified through rendered output. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a skill, When viewed, Then it previews as labelled typed Items in plain language (no Delta) with the sticky Copy. *Assertion: typed items in plain language; Copy present.* [Source: FR-UI12]
- [ ] **AC2** — Given a block, When rendered, Then per-block provenance shows. *Assertion: provenance per block.* [Source: FR-UI12]
- [ ] **AC3** — Given the base skill-detail page, When it mounts, Then every declared injection slot renders empty/clean (slot content owned by its feature's surface). *Assertion: slots present + empty.* [Source: FR-UI12]
- [ ] **AC4 (fallback)** — Given a raw-fallback skill, When viewed, Then the raw original renders with "view source" disclosure, editable as a single Text block. *Assertion: `TestViewerRendersTypedItemsAndSlots` raw path renders + editable.* [Source: FR-UI12]
- [ ] **AC5 (negative)** — Given Copy, When invoked, Then it emits exact plain text ungated. *Assertion: Copy Tier-1 byte-faithful.* [Source: FR-UI12, FR-UI16]

## 🔨 Tasks

- [ ] RED: `TestViewerRendersTypedItemsAndSlots` (AC1/AC3/AC4).
- [ ] GREEN: typed-item render + sticky Copy (AC1/AC5); provenance (AC2); injection slots (AC3); raw fallback (AC4).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Skill Viewer (typed items, per-block provenance, injection slots, raw fallback).

React-front viewer (CS §6). Seam: BFF skill read. Sibling dependency: E4-S1 (search), E1-S5 (honest states). Base for E4-S3 builder + feature slots.

## 🧪 Testing

`TestViewerRendersTypedItemsAndSlots` (component-integration) + unit tests. CS §5 mocking: BFF skill fixtures; never mock own viewer.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest · §3 naming.
- SE-Arch `8sYi523i4t`: honesty lens (raw fallback disclosed, not hidden).
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI12) · MA3 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E4.
- [ ] **Intra-service order:** after E4-S1; blocks E4-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
