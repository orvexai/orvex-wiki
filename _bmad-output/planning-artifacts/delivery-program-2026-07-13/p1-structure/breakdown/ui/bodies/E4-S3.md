## 🎯 Story

As a **user**, I want **the Base+Delta Builder — fork/edit/add/disable/move blocks with typed fields, Base immutability, an Original⇄Your-version toggle, debounced autosave, a <50ms offline deterministic weave, and last-write-wins concurrency** so that **I can craft my own version without ever mutating the base or losing unsaved work** (FR-UI13).

**Definition of Done:** ONE named test `TestBuilderBaseImmutableDeltaWeaveUnder50ms` — an integration test asserting editing never mutates Base (a Delta is minted on fork/edit), the deterministic weave/render completes client-side <50ms with no spinner and works offline, and a Delta whose Base advanced shows the non-destructive "updated to vN — reload" banner without clobbering unsaved Delta, verified through the render + timing + state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a fork, When editing/adding/disabling/moving blocks with the typed-field toolbar, Then Base immutability holds in the UI (a Delta is minted; Base is never mutated). *Assertion: Base bytes unchanged; Delta reflects edits.* [Source: FR-UI13]
- [ ] **AC2** — Given the Original⇄Your-version toggle, When toggled, Then both render correctly. *Assertion: toggle swaps views.* [Source: FR-UI13]
- [ ] **AC3** — Given an edit, When it settles, Then debounced autosave shows Saving…→Saved with retry-on-failure; Copy emits exact plain text byte-faithful. *Assertion: pill states; retry on fail; byte-faithful Copy.* [Source: FR-UI13]
- [ ] **AC4** — Given the weave, When it renders, Then it completes client-side <50ms with no spinner and works offline (no-LLM). *Assertion: `TestBuilderBaseImmutableDeltaWeaveUnder50ms` — <50ms offline.* [Source: FR-UI13, NFR-UI7]
- [ ] **AC5 (concurrency)** — Given two devices, When both edit, Then last-write-wins per MB3; a Delta whose Base advanced shows the non-destructive "updated to vN — reload" banner (unsaved Delta never clobbered). *Assertion: LWW; unsaved Delta preserved.* [Source: FR-UI13]

## 🔨 Tasks

- [ ] RED: `TestBuilderBaseImmutableDeltaWeaveUnder50ms` (AC1/AC4/AC5).
- [ ] GREEN: typed-field toolbar + Delta minting (AC1); toggle (AC2); autosave pill + Copy (AC3); offline deterministic weave (AC4); concurrency banner (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no Skill Builder (Base+Delta fork/edit, immutability, toggle, autosave, <50ms weave, concurrency); `useDraftState` is unrelated substrate.

React-front editor (CS §6 — the deterministic weave is presentational, no-LLM). Seam: BFF Delta persist. Sibling dependency: E4-S2 (viewer host), E1-S5 (honest states). Hosts E5-S1 Improve panel.

## 🧪 Testing

`TestBuilderBaseImmutableDeltaWeaveUnder50ms` (integration) + unit tests on the weave + concurrency. CS §5 mocking: BFF Delta fixtures; never mock own weave.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest (non-destructive banner) · §4 perf · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: immutability + no-data-loss lenses.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI13, NFR-UI7) · MA3/MB3 canon.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E4.
- [ ] **Intra-service order:** after E4-S2; hosts E5-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
