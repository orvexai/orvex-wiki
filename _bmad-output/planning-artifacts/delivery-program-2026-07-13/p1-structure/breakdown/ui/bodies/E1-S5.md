## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **build agent**, I want **honest-states primitives — a shared skeleton/empty/error trio, "—" while loading, Tier-1 Copy, and soft-fail-within-itself** so that **every surface ships the canon honesty rules once and no surface can render a white screen, a fabricated count, or a global read-only degrade** (FR-UI29, CS §11).

**Definition of Done:** ONE named test `TestHonestStatesNeverWhiteOrFabricated` — a component test that drives each primitive through loading/empty/error and asserts counts show "—" (never "0"), "Used by N" only when real, failures name the reason with inline retry, and Copy is never gated, verified through rendered output. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a loading surface, When data is pending, Then a skeleton renders and counts show "—" (never "0", never white). *Assertion: SM-2 white-screen count = 0; count text is "—".* [Source: FR-UI29, NFR-UI1]
- [ ] **AC2** — Given an empty result, When there is no data, Then the honest empty state renders ("New" not "Used by 0"; labeled-absent). *Assertion: no fabricated value; "Used by N" only when real.* [Source: FR-UI29, G1]
- [ ] **AC3** — Given a write failure, When it errors, Then it fails soft within itself (inline error naming the reason + retry) with no global read-only degrade. *Assertion: RC-3 — sibling surfaces stay interactive.* [Source: FR-UI29]
- [ ] **AC4** — Given any path, When Copy is invoked, Then it works ungated by auth/entitlement/degradation. *Assertion: Copy Tier-1 succeeds in anon + degraded + offline.* [Source: FR-UI29, FR-UI16]
- [ ] **AC5 (error)** — Given a stream/network error, When it surfaces, Then it renders a typed honest error frame, never a stuck spinner. *Assertion: error frame with reason; no infinite spinner.* [Source: FR-UI29, NFR-UI6]

## 🔨 Tasks

- [ ] RED: `TestHonestStatesNeverWhiteOrFabricated` across the trio (AC1–5).
- [ ] GREEN: build skeleton/empty/error components + "—" count helper + Tier-1 Copy util + soft-fail wrapper (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** present — honest-states trio present & tested (`src/components/PaneState.tsx` loading/error+retry/honest-empty + `usePaneData`, `productPanes.test.tsx`), `ErrorBoundary.tsx` soft-fail, '—' honest-loading in `Shell.tsx`.

React-front shared primitives (CS §6/§11). Seam: none external. Sibling dependency: E1-S3 (tokens for skeleton). Consumed by every feature surface.

## 🧪 Testing

`TestHonestStatesNeverWhiteOrFabricated` (component) + unit tests. CS §5 mocking: none of our own; test the primitives directly.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest states (no fake-done, no fabricated values) · §3 naming.
- SE-Arch `8sYi523i4t`: honesty lens — no black-box scores, no white screens.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI29, G1, NFR-UI1) · Architecture `DmJsnB5Z9Y` §2 (A-DS honest states) §7.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E1.
- [ ] **Intra-service order:** after E1-S3; consumed by all feature epics.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
