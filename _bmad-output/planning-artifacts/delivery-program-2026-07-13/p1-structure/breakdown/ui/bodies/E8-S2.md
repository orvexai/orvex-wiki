## 🎯 Story

As a **build agent**, I want **the performance, scale, streaming-resilience, and offline posture enforced** so that **the SPA stays fast at scale, tolerates dropped streams, and degrades honestly offline** (NFR-UI1, NFR-UI6, NFR-UI7, NFR-UI8).

**Definition of Done:** ONE named test `TestPerfScaleAndStreamResilience` — a Playwright/CI test asserting first-paint is skeleton (never white), the anon marketplace first-load JS stays within budget, interaction stays ≤200ms p95 at the 1k/10k fixtures (virtualized), a severed SSE stream never leaves a half-committed visual state, and the no-LLM weave + cached Copy work offline, verified through timings + rendered states. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given first paint, When any route loads, Then the skeleton renders from static markup (never white, SM-2 = 0), code-split per sidebar destination, with reserved-space rendering (no CLS through streams/morphs/shelf loads). *Assertion: no white; no CLS; code-split.* [Source: NFR-UI1]
- [ ] **AC2** — Given the anon marketplace path, When measured, Then first-load JS stays within budget (≤250KB gz assumption, tuned at extraction). *Assertion: bundle budget met or the assumption re-pinned in `nfr-budget.json`.* [Source: NFR-UI1]
- [ ] **AC3** — Given the 1k/10k fixtures, When interacting, Then interaction stays ≤200ms p95 (virtualized walls); hover cards ≤150ms; the deterministic weave/render <50ms. *Assertion: `TestPerfScaleAndStreamResilience` — budgets met.* [Source: NFR-UI1]
- [ ] **AC4 (resilience)** — Given a severed SSE stream, When it drops, Then onboarding falls to the byte-identical deterministic form, Improve to the cached exemplar, chat-like streams surface typed error frames, and the UI never shows a half-committed state (pending stays pending). *Assertion: no half-commit; typed error frames.* [Source: NFR-UI6]
- [ ] **AC5 (offline)** — Given offline/degraded, When used, Then the no-LLM weave/render + cached Copy work; reads degrade to cached recents with honest labels; writes fail clearly, never silent-queue; the mobile shell is first-class. *Assertion: offline weave + Copy work; no silent-queue; mobile shell present.* [Source: NFR-UI7, NFR-UI8]

## 🔨 Tasks

- [ ] RED: `TestPerfScaleAndStreamResilience` (AC3/AC4).
- [ ] GREEN: skeleton + code-split + reserved space (AC1); bundle budget check (AC2); virtualization budgets (AC3); SSE resilience (AC4); offline posture + mobile shell (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — a real NFR bundle-size DoD gate exists & runs in CI (`nfr-budget.json`, `scripts/checkNfrBudget.ts`, `nfrBudget.test.ts`). REMAINS: no scale-load, no streaming-resilience harness, no offline posture; first-paint + availability budgets explicitly open/unratified.

Cross-cutting perf/resilience (CS §6/§4). Seam: CI + BFF SSE fixtures. Sibling dependency: E1-S1 (harness), E1-S6/E3-S1/E5-S1 (streams), E3-S5/E6-S4 (scale). Ties the streaming stories together at the gate level.

## 🧪 Testing

`TestPerfScaleAndStreamResilience` (Playwright/CI) + unit tests. CS §5 mocking: BFF SSE fixtures incl. mid-stream disconnect; never mock own resilience code.

## 📏 Guidance

- CS `6aMAzsYeQb`: §4 perf discipline · §11 honest degrade (no silent-queue) · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: scale-as-gate + streaming-resilience lenses.
- Cell-lint `JGAUQRsw2g` §5: part of the required-status suite.

## 🔗 References

PRD `xsRMrju3D1` (NFR-UI1/UI6/UI7/UI8, SM-2) · Architecture `DmJsnB5Z9Y` §9.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E8.
- [ ] **Intra-service order:** after E1-S1; runs against streaming + scale surfaces.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
