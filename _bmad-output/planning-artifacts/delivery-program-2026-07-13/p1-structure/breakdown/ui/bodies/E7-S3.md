## 🎯 Story

As a **user**, I want **the pricing page, billing state, trial UX, and upgrade moments — GBP-only VAT-inclusive, Stripe Checkout/Portal redirects, the SSE entitlement flip, the card-required trial, and the dunning truth** so that **I can subscribe, manage, and understand my plan honestly with no data-loss surprises** (FR-UI26).

**Definition of Done:** ONE named test `TestBillingStateFlipsWithoutReload` — an integration test asserting in-app billing state renders from BFF entitlement data and flips without re-login on a webhook-driven change (via the single SSE entitlement channel, <60s), and additionally re-fetches on Checkout/Portal return, navigation, and window focus as the fallback floor, verified through the rendered state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the public pricing page, When rendered, Then it shows Free / £7 Personal (monthly, £70 annual "2 months free") / Teams coming-soon card (interest capture, no purchase path) / no Enterprise anywhere; prices display VAT-inclusive, GBP-only (Stripe Tax data via the BFF). *Assertion: GBP-only VAT-inclusive; no Enterprise; no purchase path on Teams.* [Source: FR-UI26, D-S19]
- [ ] **AC2** — Given subscribe/manage, When chosen, Then Subscribe → Stripe Checkout redirect and manage/cancel → Customer Portal redirect (no stripe.js in the bundle). *Assertion: full-page redirects; no stripe.js.* [Source: FR-UI26, OQ-UI6]
- [ ] **AC3** — Given a webhook-driven change, When it arrives, Then billing state flips without re-login via the single SSE entitlement channel (<60s), with refetch on Checkout/Portal return + navigation + focus as the fallback floor. *Assertion: `TestBillingStateFlipsWithoutReload` — flip <60s + fallback.* [Source: FR-UI26, D-S20, NFR-UI10]
- [ ] **AC4** — Given the trial, When rendered, Then the 7-day card-required Personal trial (a card IS captured up front), the clock-from-Graduation notice, the ~100-actions/week fair-use advisory, and expiry states (AI locks like lapse; staged proposals visibly persist + reactivate on upgrade) show. *Assertion: card-required trial + expiry states.* [Source: FR-UI26, D-S23]
- [ ] **AC5 (negative)** — Given lapse, When it occurs, Then the D-S19 dunning truth renders (14-day grace → auto-downgrade to Free; never data loss, nothing deleted); upgrade moments (Beta) render only at catalogued after-value points, dismissible, never interrupting an in-progress action. *Assertion: dunning truth; upgrade moments non-interrupting.* [Source: FR-UI26, D-S19, FR-70]

## 🔨 Tasks

- [ ] RED: `TestBillingStateFlipsWithoutReload` (AC3).
- [ ] GREEN: pricing page (AC1); Stripe redirects (AC2); SSE flip + fallback (AC3); trial UX (AC4); dunning + upgrade moments (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** partial — a thin Settings pane over `/billing` + entitlement-driven tier-locked banner exist & tested (`src/routes/Settings.tsx`, `productPanes.test.tsx`). REMAINS: no pricing surface, no GBP-only display, no Stripe Checkout/Portal redirect, no trial/upgrade moments, no dunning truth.

React-front billing surface (CS §6 — no custom billing UI; redirect-only). Seam: BFF entitlement/billing state + Stripe hosted redirect (true-external) + Clerk. Sibling dependency: E1-S6 (entitlement channel), E1-S5 (honest states).

## 🧪 Testing

`TestBillingStateFlipsWithoutReload` (integration) + unit tests on the fallback floor. CS §5 mocking: BFF billing/entitlement fixtures + Stripe redirect (true-external); never mock own billing UI.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view (no client entitlement math) · §11 honest (no data loss) · §5 fixture the sibling + true-external Stripe.
- SE-Arch `8sYi523i4t`: external-dep ADR trigger (Stripe); honesty (dunning truth); no-interrupt upsell lens.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI26, D-S19/S20/S23, OQ-UI6, FR-70, NFR-UI10) · Architecture `DmJsnB5Z9Y` §2 (A-AUTH-UI).

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the entitlement + billing-state + Stripe-session shapes are the gate).
- [ ] **Parent epic:** E7.
- [ ] **Intra-service order:** after E1-S6.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
