## 🎯 Story

As a **privacy-conscious user**, I want **sensitive-field consent UX with session-only pre-consent buffering, an Enrichment Policy setting, and prominent hard-delete** so that **a sensitive value is never in the store before I consent and never enriches a prompt without my opt-in** (FR-UI8).

**Definition of Done:** ONE named test `TestSensitiveValueAbsentUntilConsent` — an integration test asserting a sensitive value is session-only buffered and verifiably absent from the store until a consent event exists, the consent ask names the specific item, and hard-delete is the prominent action, verified through the store state + rendered consent flow. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2109); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a sensitive value, When entered, Then it is session-only buffered and verifiably absent from the store until a consent event exists. *Assertion: no persist call before consent; store lacks the value.* [Source: FR-UI8]
- [ ] **AC2** — Given the consent ask, When shown, Then it names the specific item. *Assertion: item name present in the ask (proposal via E1-S4 layer).* [Source: FR-UI8, FR-UI27]
- [ ] **AC3** — Given consent withdrawal, When triggered, Then the render re-weaves against the bumped Memory version. *Assertion: re-weave fires on the version bump.* [Source: FR-UI8]
- [ ] **AC4** — Given a sensitive field, When on Memory or Account surfaces, Then hard-delete is the prominent action. *Assertion: prominent hard-delete present on both.* [Source: FR-UI8]
- [ ] **AC5** — Given the Enrichment Policy setting (ask-each-time default / never-use / always-allow), When set, Then it governs Private Memory in enrichment (FR-56) and renders in Settings. *Assertion: policy default is ask-each-time; setting drives enrichment.* [Source: FR-UI8, FR-56]

## 🔨 Tasks

- [ ] RED: `TestSensitiveValueAbsentUntilConsent` (AC1).
- [ ] GREEN: session-only buffer (AC1); named consent ask via proposal layer (AC2); withdrawal re-weave (AC3); prominent hard-delete (AC4); Enrichment Policy setting (AC5).

## 🧠 Context

**🧾 Code audit (origin/main @ 74c2a39, 2026-07-14):** absent — no sensitive-field consent UX, no Enrichment Policy setting, no hard-delete affordance.

React-front consent surface (CS §6; A-DRAFT). Seam: BFF consent + memory versioning. Sibling dependency: E1-S4 (proposal layer), E3-S3 (wall). Consumed by enrichment surfaces (E5-S4, E6).

## 🧪 Testing

`TestSensitiveValueAbsentUntilConsent` (integration) + unit tests on the buffer. CS §5 mocking: BFF fixtures; never mock own buffer/consent.

## 📏 Guidance

- CS `6aMAzsYeQb`: §6 shallow view · §11 honest · §3 naming · §5 fixture the sibling.
- SE-Arch `8sYi523i4t`: privacy/consent lens — no store-before-consent; SM-C3 = 0.
- Cell-lint `JGAUQRsw2g`: N/A runtime.

## 🔗 References

PRD `xsRMrju3D1` (FR-UI8, FR-56) · Architecture `DmJsnB5Z9Y` §2.

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2109 (Definition Pack — the contract TAG is the dispatch gate).
- [ ] **Parent epic:** E3.
- [ ] **Intra-service order:** after E3-S3 and E1-S4.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
