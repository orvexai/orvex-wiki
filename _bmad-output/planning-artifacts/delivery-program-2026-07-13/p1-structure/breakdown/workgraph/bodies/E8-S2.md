### 🎯 Story
As a **fleet operations lead** I want **a console fleet view rendered from knowledge projections of `studio.workgraph.*` events, with exactly three anomaly detectors and tenant-fact pin/unpin** so that **I can see what my agents are doing, blocked on, and dropped — without reading transcripts or touching workgraph's Postgres**.

**Definition of Done:** one named test `TestConsoleReadsProjectionsThreeAnomalies` (projection+console integration layer) asserts the fleet view is built ONLY from knowledge projections (never workgraph Postgres) and surfaces exactly the three v1 detectors — stale claim, re-open loop (≥2 re-opens after rejection), orphaned blocker — no more. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the console fleet view, When rendered, Then it shows in-progress with lease freshness, ready, blocked-with-blockers, and the closed trail, all from knowledge projections. *assert: view fields populated from projections; no workgraph Postgres read.* [Source: FR-MEM17, AD-7]
- [ ] **AC2** — Given anomaly detection, When computed, Then exactly three detectors fire — stale claim (lease expired without close), re-open loop (≥2 re-opens after rejection), orphaned blocker (`blocks` edge → closed/missing item) — and no further classes. *assert: the three detectors present; a fourth class is absent (deferred).* [Source: FR-MEM17, AD-7]
- [ ] **AC3** — Given tenant facts, When an admin pins/unpins, Then the change carries to every agent's next `prime`. *assert: pinned fact appears in subsequent prime; unpinned drops out.* [Source: FR-MEM17]
- [ ] **AC4 (negative)** — Given the console, When it tries to read workgraph Postgres directly, Then it is forbidden (topology/import proof). *assert: console → workgraph Postgres edge does not exist.* [Source: AD-7]
- [ ] **AC5 (error-path)** — Given knowledge projections are lagging, When the view renders, Then it shows a staleness indicator, never fabricated state. *assert: projection lag → staleness surfaced.* [Source: AD-3, AD-13]

### 🔨 Tasks
- [ ] RED: `TestConsoleReadsProjectionsThreeAnomalies` incl. no-Postgres + exactly-three cases (AC1, AC2, AC4).
- [ ] GREEN: projection consumers feeding the console fleet view (AC1).
- [ ] Three anomaly detectors; pin/unpin carrying to prime (AC2, AC3).
- [ ] Staleness indicator on projection lag (AC5).

### 🧠 Context
Tier: `internal/event` projection consumers + `orvex-studio-console` BFF/UI. Seam: knowledge projections (E8-S1); console reads projections only. Sibling dependency: `orvex-studio-knowledge` (past 501 scaffold — the view is dead until then), `orvex-studio-console` (empty scaffold per current-state map). UJ-3.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No console fleet-view code, no knowledge projections, no anomaly detectors, no fact pin/unpin.
### 🧪 Testing
DoD `TestConsoleReadsProjectionsThreeAnomalies` (projection+console integration). CS §5: fake knowledge projections from contracts fixtures; never mock own projection code.

### 📏 Guidance
CS §§5/6/7 (read-side via projections); SE-Arch lenses (console-never-reads-store, bounded anomaly set); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-7, AD-3); PRD `Kgp6JT3IOR` §4.4 FR-MEM17, UJ-3.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E8**; **E8-S1** (event schemas/projections).
- Upstream: `orvex-studio-knowledge`, `orvex-studio-console`.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
