### 🎯 Story
As a **coordinating agent** I want **hybrid `search`/`recall` retrieval that fuses the knowledge service's semantic leg with keyword tsvector and graph adjacency** so that **I find relevant items/notes/facts with no LLM in the hot path and no grant bypass**.

**Definition of Done:** one named test `TestHybridRankingReFiltersGrants` (domain layer) asserts `ranking` calls knowledge-query with the caller's principal, re-filters authoritatively against AD-9 grants (egress-narrower wins), fuses semantic × tsvector × adjacency × recency/importance, and calls NO LLM on the path. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given a `search`, When run, Then `ranking` retrieves semantic candidates via the knowledge query client (principal-scoped) and fuses them with tsvector keyword + graph adjacency + recency/importance. *assert: fused ranking composite matches the spec formula.* [Source: FR-MEM10, AD-3]
- [ ] **AC2** — Given knowledge narrows via workgraph's `acl_primitive`, When results return, Then `ranking` re-filters against AD-9 grants and the narrower egress wins. *assert: an item allowed by knowledge but denied by grants is excluded.* [Source: AD-3, AD-9]
- [ ] **AC3** — Given the hot path, When retrieval runs, Then zero LLM calls occur (Zep lesson). *assert: no `aiclient` call in the retrieval path.* [Source: FR-MEM10, NFR-MEM1]
- [ ] **AC4** — Given content newer than the projection lag, When searched, Then the Postgres legs alone return correct (if less semantic) results and staleness is bannered. *assert: fresh-but-unindexed content found via Postgres; `{kind: staleness}` banner.* [Source: AD-3]
- [ ] **AC5 (negative/error)** — Given knowledge is unavailable, When search runs, Then it degrades to the Postgres legs with a `staleness` banner (never an empty response). *assert: knowledge-down → Postgres results + staleness banner.* [Source: AD-3, AD-13, NFR-MEM6]

### 🔨 Tasks
- [ ] RED: `TestHybridRankingReFiltersGrants` incl. grant-narrowing + no-LLM + knowledge-down cases (AC2, AC3, AC5).
- [ ] GREEN: `internal/ranking` fusion; principal-scoped knowledge query client (AC1).
- [ ] Authoritative grant re-filter; staleness banner (AC2, AC4).
- [ ] Postgres-legs fallback path (AC5).

### 🧠 Context
Tier: `internal/ranking/` + `internal/clients` (knowledge). Seam: knowledge query client (principal-scoped, acl-narrowing) — pinned in contracts (ADR-0013 OD-7 kept platform-service clients). Sibling dependency: `orvex-studio-knowledge` past its 501 scaffold (the AD-3 semantic leg is dead until then).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — internal/ranking/doc.go is a doc comment only. GET /v1/search is a 501 stub. knowledgeclient is constructed (clients.go:41) but no hybrid retrieval/ranking logic uses it.
### 🧪 Testing
DoD `TestHybridRankingReFiltersGrants` (domain integration, testcontainers). CS §5: fake knowledge from contracts fixtures; use real `authz` (own package — not mocked).

### 📏 Guidance
CS §§4/5/6/7 (egress narrowing, no LLM in hot path); SE-Arch lenses (double-sided authz, staleness-not-silence); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-3, AD-9, AD-13); PRD `Kgp6JT3IOR` §4.2 FR-MEM10.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — knowledge query client contract); parent epic **E4**; **E4-S1**, **E6-S1** (grants), **E8-S1** (source-adapter/projection).
- Upstream: `orvex-studio-knowledge`.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
