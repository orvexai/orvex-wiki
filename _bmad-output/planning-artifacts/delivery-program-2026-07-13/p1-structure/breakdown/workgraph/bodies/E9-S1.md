### 🎯 Story
As a **platform owner** I want **a versioned Prompt Pack store (ChatGPT-portal blocks, Claude project instructions, CLI snippets) with admin-approved revisions and server-side reinforcement** so that **the beads rituals ride chat platforms that have no hooks, without silent prompt drift**.

**Definition of Done:** one named test `TestPackRevisionAdminApprovedReinforced` (domain+MCP integration layer) asserts pack revisions activate ONLY via admin approval, per-tenant overrides layer on top, and server-side reinforcement ships (compact prime via MCP `instructions` at connect, tool-description micro-prompts, stale-claim nudges in tool results). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given prompt packs, When stored, Then they are versioned artifacts in the workgraph store and carry the beads ritual content verbatim (file-before-work, claim-before-work, shadow-tracking ban, the `COMPLETED/IN PROGRESS/NEXT/KEY DECISION/BLOCKER` template, the resumability test, prime's truncation-defense first line). *assert: pack content matches the canon ritual set.* [Source: FR-MEM18, AD-11]
- [ ] **AC2** — Given a revision, When proposed, Then it activates ONLY via admin approval (no silent behavior shift on chat platforms). *assert: unapproved revision is inactive; approval activates it.* [Source: FR-MEM18, AD-11]
- [ ] **AC3** — Given a per-tenant override, When set, Then it layers on top of the base pack (the hosted `PRIME.md`/`customize.toml` analog). *assert: override composes with the base, not replaces silently.* [Source: FR-MEM18, AD-11]
- [ ] **AC4** — Given server-side reinforcement, When an agent connects, Then compact prime is delivered via MCP server `instructions`, tool descriptions carry the ritual micro-prompts, and a stale claim triggers a close-protocol nudge in tool results. *assert: instructions at connect; nudge fires on a going-stale claim.* [Source: FR-MEM18, AD-11]
- [ ] **AC5 (negative)** — Given a pack, When it renders any error/nudge, Then it never contains a copy-pasteable destructive command (beads ADR-0002). *assert: no destructive command text in packs/nudges.* [Source: Agent-safety]

### 🔨 Tasks
- [ ] RED: `TestPackRevisionAdminApprovedReinforced` incl. admin-gate + override + nudge cases (AC2–AC4).
- [ ] GREEN: pack store + revision model (versioned artifacts) (AC1, AC2).
- [ ] Per-tenant override composition; server-side reinforcement wiring (AC3, AC4).
- [ ] Agent-safety review of pack text (AC5).

### 🧠 Context
Tier: `internal/graph` (pack store) + MCP server reinforcement (E7-S1's `instructions`/tool descriptions). Seam: MCP server; the feedback/adherence event envelope authored once in contracts (shared with staging). Sibling dependency: studio-mcp. Learning machinery stays separate from staging's Librarian in v1 (convergence deferred, OQ7).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No prompt-pack store: no table, no versioned revisions, no server-side reinforcement.
### 🧪 Testing
DoD `TestPackRevisionAdminApprovedReinforced` (domain+MCP integration, testcontainers). CS §5: fake MCP connect from contracts fixtures; never mock own pack store.

### 📏 Guidance
CS §§5/6/7 (versioned artifacts, no silent drift); SE-Arch lenses (admin-gated activation, agent-safety); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-11, Agent-safety); PRD `Kgp6JT3IOR` §4.5 FR-MEM18; Addendum `2KrwsvY5zL` A4.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — feedback/adherence envelope); parent epic **E9**; **E7-S1** (MCP instructions surface), **E4-S2** (compact prime).
- Blocks: E9-S2 (adherence telemetry).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
