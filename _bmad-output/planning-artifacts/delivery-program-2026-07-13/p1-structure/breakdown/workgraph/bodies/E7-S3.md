### 🎯 Story
As a **host/tool expecting the de-facto MCP knowledge-graph shape** I want **a KG interop adapter (entities/relations/observations, read + write) mapping onto workgraph facts/edges/items** so that **tooling built against the reference memory server can ground against and write into the Orvex workgraph**.

**Definition of Done:** one named test `TestKgInteropMappingGoldenFixtured` (adapter integration layer) asserts the entities/relations/observations ↔ facts/edges/items mapping (pinned to `@modelcontextprotocol/server-memory` 2026.7.4) round-trips against a golden fixture, and writes route through the same direct-write path with namespace scoping + grants. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the KG reference shape (2026.7.4), When entities/relations/observations are read, Then they map to workgraph items/edges/facts per the golden-fixtured mapping table. *assert: read mapping matches the golden fixture.* [Source: FR-MEM16, AD-10]
- [ ] **AC2** — Given a write of entities/observations/relations, When applied, Then observations→Remembered Facts and relations→edges route through the SAME direct-write path (never a bypass), with namespace scoping and grants. *assert: writes go through published verbs + `authz`.* [Source: FR-MEM16, AD-10, AD-9]
- [ ] **AC3** — Given the mapping table, When exercised, Then it is golden-fixtured at build and every parser agrees on short-id resolution. *assert: golden fixture in CI; shared grammar used.* [Source: AD-10, AD-6]
- [ ] **AC4 (negative)** — Given a write that would cross a namespace, When submitted through the adapter, Then `authz` denies it (adapter cannot bypass isolation). *assert: cross-namespace interop write denied.* [Source: AD-9, AD-10]

### 🔨 Tasks
- [ ] RED: `TestKgInteropMappingGoldenFixtured` incl. write-routing + cross-namespace-denied cases (AC1, AC2, AC4).
- [ ] GREEN: interop adapter module; read mapping (entities/relations/observations ↔ items/edges/facts) (AC1).
- [ ] Write path through published verbs + `authz`; golden fixtures (AC2, AC3).

### 🧠 Context
Tier: adapter module (thin translator). Seam: published verbs + `authz`; short-id grammar (E1-S5). Sibling dependency: pinned to `@modelcontextprotocol/server-memory` 2026.7.4. Mapping-table detail is OQ6 — golden-fixtured here; AD-10 fixes direction + authz.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No KG interop adapter (entities/relations/observations), no golden fixtures.
### 🧪 Testing
DoD `TestKgInteropMappingGoldenFixtured` (adapter integration). CS §5: golden fixtures from contracts; never mock own adapter/authz.

### 📏 Guidance
CS §§4/5/6/7 (adapter-through-verbs, golden fixtures); SE-Arch lenses (interop-shape fidelity, no bypass); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-10, AD-9, AD-6, Stack — KG interop shape); PRD `Kgp6JT3IOR` §4.4 FR-MEM16, §11 OQ6.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E7**; **E7-S1** (verbs), **E6-S1** (authz), **E1-S5** (short ids).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
