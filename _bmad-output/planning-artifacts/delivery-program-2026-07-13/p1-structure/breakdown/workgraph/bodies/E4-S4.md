### 🎯 Story
As a **platform owner** I want **a retrieval spike that measures the real AD-3 topology and publishes the SLO contract, plus a fast-path index-scan tier** so that **latency budgets are a measured contract and exact-key recall / pinned-only prime hit ≤100ms**.

**Definition of Done:** one named test `TestFastPathPlansIndexScan` (benchmark+EXPLAIN layer) asserts exact-key recall and pinned-only prime-compact plan as pure index scans (no vector search) and the spike publishes per-op-class SLOs (create/claim/prime-compact/hybrid-recall/fast-path) on the real topology. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the AD-3 topology (knowledge semantic leg + tsvector + adjacency fusion) at the target envelope, When the spike runs, Then it measures and publishes per-op-class p95 SLOs. *assert: published SLO artifact for create/claim/prime-compact/hybrid-recall.* [Source: NFR-MEM1]
- [ ] **AC2** — Given fast-path reads (exact-key recall, pinned-only prime-compact), When executed, Then they plan as index scans with no vector search and target ≤100ms p95. *assert: EXPLAIN shows index scan; measured p95 ≤100ms.* [Source: NFR-MEM1]
- [ ] **AC3** — Given the committed invariant, When any hot-path op runs, Then zero LLM calls occur. *assert: spike confirms no LLM on any measured hot path.* [Source: NFR-MEM1]
- [ ] **AC4 (negative)** — Given the spike fails a budget, When results land, Then the remedy is an ADR-0014 carve-out ADR (revisit), NEVER a quiet local index. *assert: a failing budget triggers the ADR path, not a silent pgvector addition.* [Source: AD-3, NFR-MEM1]

### 🔨 Tasks
- [ ] RED: `TestFastPathPlansIndexScan` with EXPLAIN assertions (AC2).
- [ ] GREEN: fast-path read routing for exact-key recall + pinned-only prime (AC2).
- [ ] Spike harness measuring all op-classes on the AD-3 topology; publish SLOs (AC1, AC3).
- [ ] Encode the ADR-0014-carve-out-not-local-index gate (AC4).

### 🧠 Context
Tier: benchmark harness + `internal/facts`/`internal/prime` fast-path routing. Seam: knowledge query client. Sibling dependency: `orvex-studio-knowledge` past 501 scaffold + the platform→knowledge query-client contract confirmed in contracts. Sequencing gate, not a deferral.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No retrieval spike, no published SLOs, no fast-path index-scan tier.
### 🧪 Testing
DoD `TestFastPathPlansIndexScan` (benchmark tier + EXPLAIN). CS §5: real store + real knowledge on dev cell; never mock own packages. SLO artifact is an SDD evidence line.

### 📏 Guidance
CS §§5/6/10 (measured NFRs); SE-Arch lenses (measured-not-asserted, ADR-carve-out discipline, fake-done antibody); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-3, Deferred — numeric SLOs); PRD `Kgp6JT3IOR` §7 NFR-MEM1.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E4**; **E4-S1**, **E4-S2**, **E4-S3**.
- Upstream: `orvex-studio-knowledge`.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
