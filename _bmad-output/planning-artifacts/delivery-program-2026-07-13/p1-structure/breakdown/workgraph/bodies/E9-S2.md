### 🎯 Story
As a **platform owner** I want **adherence telemetry (prime/claim-before-work/close/stale-claim/discovered-from rates) over the observable denominator, plus a pre-GA viability spike that sets the floor and the GA gate** so that **prompt-pack effectiveness is measured honestly and GA is gated on real adherence, not a hope**.

**Definition of Done:** one named test `TestAdherenceMetricsObservableDenominator` (telemetry layer) asserts the metrics derive from tool-call events using the observable denominator (workgraph-active sessions ≥1 workgraph tool call), label the proxy, and the spike output sets the published viability floor. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given tool-call events, When metrics compute, Then prime-rate, claim-before-work, close-rate, stale-claim, and discovered-from usage are derived per tenant. *assert: each metric computed from the event stream.* [Source: FR-MEM19, AD-11]
- [ ] **AC2** — Given the denominator, When rates are computed, Then it is the observable proxy — workgraph-active sessions (≥1 workgraph tool call), "prime-first" = first workgraph call is prime — and the proxy is labeled (it undercounts fully non-adherent sessions). *assert: denominator = active sessions; proxy explicitly labeled.* [Source: FR-MEM19, SM-3]
- [ ] **AC3** — Given the pre-GA adherence spike on real ChatGPT-portal + Claude tenants, When run, Then it measures actual prime/close rates with the shipped pack and sets the published viability floor. *assert: spike publishes measured rates + the floor.* [Source: FR-MEM19]
- [ ] **AC4** — Given the viability floor, When adherence is below it, Then server-side auto-injection (FR-MEM18) becomes the DEFAULT product mode rather than an assist. *assert: below-floor → auto-injection default flips on.* [Source: FR-MEM19]
- [ ] **AC5 (negative)** — Given a regression in a metric, When detected, Then it flags a prompt-pack revision (admin-proposed), never a silent pack change. *assert: regression → admin-facing revision proposal.* [Source: FR-MEM19, AD-11]
- [ ] **AC6 (gate)** — Given GA readiness, When evaluated, Then GA is gated on the spike (not a reported pass). *assert: GA gate ties to the observed spike result.* [Source: FR-MEM19, fake-done antibody]

### 🔨 Tasks
- [ ] RED: `TestAdherenceMetricsObservableDenominator` incl. proxy-label + below-floor cases (AC1, AC2, AC4).
- [ ] GREEN: adherence telemetry derivation from tool-call events (AC1, AC2).
- [ ] Spike harness on real tenants; floor publication + GA gate (AC3, AC6).
- [ ] Regression → admin revision; below-floor auto-injection flip (AC4, AC5).

### 🧠 Context
Tier: `internal/event` telemetry derivation. Seam: the feedback/adherence event envelope authored once in contracts (shared with staging so the later learning-loop merge is additive). Sibling dependency: staging (envelope shared); SM-1 cannot use live host-side token telemetry (lab A/B harness proxy). This is a GA sequencing gate.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No adherence telemetry, no pre-GA viability spike, no GA gate.
### 🧪 Testing
DoD `TestAdherenceMetricsObservableDenominator` (telemetry integration). CS §5: fake event stream from fixtures; never mock own telemetry. Spike is an evidenced SDD line.

### 📏 Guidance
CS §§5/6/10 (measured adherence, honest denominators); SE-Arch lenses (proxy-labeling, GA-gate-on-observed, fake-done antibody); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-11); PRD `Kgp6JT3IOR` §4.5 FR-MEM19, §8 SM-3.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — feedback/adherence envelope); parent epic **E9**; **E9-S1** (packs), **E3** (stale-claim signals), **E4-S2** (prime).
- Upstream: `orvex-studio-staging` (shared envelope).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
