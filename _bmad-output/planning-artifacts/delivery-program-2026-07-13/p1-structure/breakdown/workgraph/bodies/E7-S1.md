### 🎯 Story
As a **chat-based agent** I want **a `workgraph_*` MCP section on the Studio MCP server with context-budget levers, plus disambiguation pointers on the `studio_memory_*` tools** so that **I coordinate through the workgraph via MCP cheaply, and agent-state usage is routed away from the user-memory product**.

**Definition of Done:** one named test `TestMcpWorkgraphSectionLeanAndRouted` (MCP-server integration layer) asserts the `workgraph_*` tools translate to the published verbs through `authz` (never the store), stay lean (lazy schema discovery, `brief=true`, result compaction), and the `studio_memory_get/save` descriptions carry the agent-state disambiguation pointer. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the Studio MCP server, When the section registers, Then the `workgraph_*` tools exist (`prime, ready, claim, heartbeat, create, update, close (with claim_next), dep_add, note, handoff, remember, recall, search, stats, batch`) in a hidden `workgraph` category, registered before `installToolVisibility`. *assert: all listed tools present; category hidden.* [Source: FR-MEM14, AD-10]
- [ ] **AC2** — Given context-budget levers, When tools respond, Then lazy schema discovery, `brief=true` minimal returns, and result compaction above threshold (preview+hint) apply. *assert: eager-schema token cost avoided; `brief=true` shrinks the return.* [Source: FR-MEM14, AD-10]
- [ ] **AC3** — Given every tool call, When executed, Then it routes through the published workgraph verbs + `authz` — never reads/writes the store directly. *assert: adapter → verbs; import-lint forbids `store/`.* [Source: AD-10]
- [ ] **AC4** — Given `studio_memory_get/save`, When their descriptions render, Then they carry the disambiguation pointer directing agent-state usage to `workgraph_*`; the user-memory tools are NOT stubbed or removed. *assert: pointer present; user-memory tools untouched.* [Source: FR-MEM22, FR-MEM21, AD-1]
- [ ] **AC5 (negative)** — Given `close --claim_next`, When the follow-on claim fails, Then the close is NOT rolled back (best-effort chaining). *assert: failed next-claim leaves the close committed.* [Source: AD-2]

### 🔨 Tasks
- [ ] RED: `TestMcpWorkgraphSectionLeanAndRouted` incl. routing + disambiguation + claim_next cases (AC1, AC3–AC5).
- [ ] GREEN: `workgraph-tools.ts` section (all tools) on the studio-mcp server (AC1).
- [ ] Context-budget levers; through-verbs-only routing (AC2, AC3).
- [ ] `studio_memory_*` description repoints (AC4).

### 🧠 Context
Tier: React/TS is UI; here the MCP server is the existing studio-mcp TS repo (`@modelcontextprotocol/sdk ^1.29`, zod 4). Seam: published workgraph verbs (codegen'd) + `authz`. Sibling dependency: studio-mcp repo; the user-memory tools stay product-side (naming firewall — `memory` ≠ this service).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No MCP server/adapter code in the repo. No workgraph_* tool section, no studio_memory disambiguation pointers.
### 🧪 Testing
DoD `TestMcpWorkgraphSectionLeanAndRouted` (MCP-server integration). CS §5: fake the workgraph API from contracts fixtures; never mock own MCP section. Token-budget measured.

### 📏 Guidance
CS §§4/5/6/7 (adapter-through-verbs, no store access); SE-Arch lenses (context-budget discipline, naming firewall); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-10, AD-1, AD-2, Consistency Conventions); PRD `Kgp6JT3IOR` §4.4 FR-MEM14, §4.6 FR-MEM21/22.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — verb schemas); parent epic **E7**; **E2**, **E3**, **E4** (verbs), **E6-S1** (authz).
- Upstream: `orvex-studio-mcp` server repo.
- Blocks: E7-S2..S4 (share the verb surface), E9-S1 (MCP `instructions`).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
