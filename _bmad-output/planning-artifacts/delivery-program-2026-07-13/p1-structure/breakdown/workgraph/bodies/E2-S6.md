### 🎯 Story
As a **platform owner** I want **a pre-GA load test that exercises the tenant envelope and publishes a measured degradation curve** so that **the scale claim is a measured contract, not an asserted-flat latency**.

**Definition of Done:** one named test `TestScaleEnvelopeDegradationCurve` (load/benchmark layer) drives the `[ASSUMPTION: 100k items / 500k edges]` envelope and emits a published degradation curve (p50/p95/p99 per op-class vs. graph size), validating or lowering the envelope. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given a tenant loaded toward 100k items / 500k edges, When ops run, Then a per-op-class latency curve is measured and published (never asserted flat). *assert: curve artifact produced with p50/p95/p99 vs. size.* [Source: NFR-MEM2]
- [ ] **AC2** — Given 100-item batch writes, When they run under load, Then `recomputeBlocked` stays incremental (cost scales with affected subgraph, not graph size). *assert: recompute cost curve is sub-linear in graph size.* [Source: NFR-MEM2, AD-4]
- [ ] **AC3** — Given per-tenant rate limits, When a noisy tenant bursts, Then `quota` admission control caps it (data partition does not isolate compute → cell pinning + rate limits are the controls). *assert: burst tenant throttled; neighbor unaffected.* [Source: NFR-MEM2, AD-4]
- [ ] **AC4 (negative)** — Given the envelope fails the target, When measured, Then the envelope is LOWERED and republished (not silently accepted). *assert: a failing run produces a lowered, documented envelope, not a green rubber-stamp.* [Source: NFR-MEM2, fake-done antibody]

### 🔨 Tasks
- [ ] RED: `TestScaleEnvelopeDegradationCurve` harness seeding the envelope (AC1).
- [ ] GREEN: measure per-op-class curves; assert incremental recompute (AC1, AC2).
- [ ] Exercise rate-limit admission under burst (AC3).
- [ ] Publish the curve; encode the validate-or-lower gate (AC4).

### 🧠 Context
Tier: load/benchmark harness against a real testcontainers/dev-cell Postgres. Seam: `quota` admission (E6-S3). Sibling dependency: none — this validates E2/E4 hot paths. This is a sequencing gate, not a deferral.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No load tests, no benchmark harness, no published degradation curve anywhere in the tree.
### 🧪 Testing
DoD `TestScaleEnvelopeDegradationCurve` (load tier). CS §5: real store at scale; never mock own packages. Curve is an evidenced artifact (SDD line).

### 📏 Guidance
CS §§5/6/10 (measured NFRs); SE-Arch lenses (measured-not-asserted, fake-done antibody); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-4, Deferred — NFR-MEM2 load test); PRD `Kgp6JT3IOR` §7 NFR-MEM2.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E2**; **E2-S1..S5**, **E6-S3** (rate limits).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
