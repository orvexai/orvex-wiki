### 🎯 Story
As a **coordinating agent** I want **marking an item/note wiki-worthy to package distilled content and submit a Staging Proposal via `stagingclient` (never the wiki directly)** so that **mature working memory flows into the customer's wiki through the Librarian, with cross-links and loud cycle rejection**.

**Definition of Done:** one named test `TestWikiWorthySubmitsViaStagingRejectsCycle` (domain+client integration layer) asserts wiki-worthy packages distilled content and submits a staging Proposal through `stagingclient` (never wiki/wiki-api), persists cross-links both sides, and rejects resubmission of unchanged content as a cycle via the shared sha256 content-hash. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given an item/note marked wiki-worthy, When submitted, Then distilled content is packaged and sent as a Staging Proposal via the lib `stagingclient` — never the wiki, never wiki-api. *assert: submission goes through `stagingclient`; no direct wiki write path exists.* [Source: FR-MEM20, AD-12]
- [ ] **AC2** — Given a submitted Proposal, When accepted, Then cross-links persist on both sides (workgraph item ↔ applied staging Proposal/page). *assert: bidirectional cross-links stored.* [Source: FR-MEM20, AD-12]
- [ ] **AC3** — Given a Proposal that staging diverts back (`divert-to-workgraph`), When it carries the originating item id, Then resubmission of unchanged content is rejected as a cycle, loudly. *assert: unchanged resubmission → loud cycle rejection.* [Source: AD-12]
- [ ] **AC4** — Given "unchanged", When decided, Then it uses the sha256 content-hash defined once in the `stagingclient` contract, computed identically on both sides. *assert: same hash on both sides; changed content is NOT a cycle.* [Source: AD-12]
- [ ] **AC5 (negative)** — Given the workgraph, When it would write the wiki directly, Then no such path exists (a second agent write path is forbidden). *assert: import/topology proof — no workgraph→wiki edge.* [Source: AD-12]
- [ ] **AC6 (maturation)** — Given a wiki-worthy item lands in the wiki, When compaction runs, Then its bulky notes become compaction-eligible and it links to the applied page. *assert: compaction-eligibility flips after application (ties E5-S2).* [Source: FR-MEM20, UJ-4]

### 🔨 Tasks
- [ ] RED: `TestWikiWorthySubmitsViaStagingRejectsCycle` incl. cycle + no-direct-wiki cases (AC1, AC3, AC5).
- [ ] GREEN: wiki-worthy packaging + `stagingclient` submission (AC1).
- [ ] Bidirectional cross-links; shared sha256 content-hash cycle guard (AC2, AC3, AC4).
- [ ] Compaction-eligibility on application (AC6).

### 🧠 Context
Tier: `internal/graph` + `internal/clients` (`stagingclient`). Seam: `stagingclient` (lib) → `orvex-studio-staging`; the sha256 content-hash is defined once in that contract. Sibling dependency: `orvex-studio-staging` (Proposal ingest, `divert-to-workgraph`) — pre-build per current-state map. UJ-4.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No wiki-worthy flow. stagingclient is explicitly noted as pending its lib typed client (clients.go comment) and is intentionally absent — no package/submit-Proposal path exists.
### 🧪 Testing
DoD `TestWikiWorthySubmitsViaStagingRejectsCycle` (domain+client integration, testcontainers). CS §5: fake staging from contracts fixtures; never mock own `graph`/`clients`.

### 📏 Guidance
CS §§4/5/6/7 (single write path, no direct wiki); SE-Arch lenses (no-second-write-path, cycle-safety); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-12); PRD `Kgp6JT3IOR` §4.5 FR-MEM20, UJ-4; staging FR-STG8/FR-STG1.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — `stagingclient` contract + content-hash); parent epic **E9**; **E2-S1** (items/notes), **E5-S2** (compaction-eligibility).
- Upstream: `orvex-studio-staging`.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
