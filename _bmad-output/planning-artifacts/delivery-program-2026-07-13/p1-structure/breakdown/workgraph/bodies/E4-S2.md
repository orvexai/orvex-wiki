### 🎯 Story
As a **chat-agent starting a new session** I want **`prime` in compact and full modes with a fixed content contract and a ≤300-token compact budget** so that **my session's first useful action happens within one tool call and no semantic retrieval runs on the compact path**.

**Definition of Done:** one named test `TestPrimeCompactContractAndBudget` (domain layer) asserts compact prime carries exactly {caller's own active claims, close-protocol reminder, capped pinned tenant facts, ready/handoff counts + single top item with fetch hint}, runs zero semantic retrieval, and stays ≤300 tokens p95. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given `prime --compact`, When called, Then it returns the caller's own active claims, the close-protocol reminder, and capped pinned tenant facts; ready work + pending handoffs appear as counts plus the single top item with a fetch hint. *assert: content matches the fixed contract exactly.* [Source: FR-MEM9, AD-13]
- [ ] **AC2** — Given compact mode, When assembled, Then NO semantic retrieval runs (pinned-only); the top item orders by the priority/age sub-order of `ready` (agrees with the ready surface). *assert: no knowledge call in compact path; top item = ready's top.* [Source: FR-MEM9, AD-3, AD-13]
- [ ] **AC3** — Given the ≤300-token budget, When compact exceeds it, Then content is elided with an explicit banner (never silently). *assert: budget ≤300 p95; over-budget → elision banner.* [Source: NFR-MEM3, AD-13]
- [ ] **AC4** — Given `prime --full`, When called, Then it carries the lists (~1–2k tokens) incl. retrieved relevant facts. *assert: full mode includes semantic results.* [Source: FR-MEM9]
- [ ] **AC5** — Given a per-tenant template override, When set, Then prime uses it (the hosted `PRIME.md` escape hatch) with a truncation-defense first line. *assert: override applied; first line is the truncation defense.* [Source: FR-MEM9, AD-11]
- [ ] **AC6 (negative)** — Given the store is degraded, When prime runs, Then it returns honest-empty-with-status, never fabricated content. *assert: degraded → empty+status.* [Source: NFR-MEM6]

### 🔨 Tasks
- [ ] RED: `TestPrimeCompactContractAndBudget` incl. no-semantic + budget + degraded cases (AC1–AC3, AC6).
- [ ] GREEN: `internal/prime` compact assembly with the fixed contract (AC1, AC2).
- [ ] Full mode with semantic leg; template override + truncation-defense line (AC4, AC5).
- [ ] Token counting + elision banner (AC3).

### 🧠 Context
Tier: `internal/prime/`. Seam: reads `claim`/`graph`/`facts`; full mode calls `ranking` (E4-S3); token budget counted server-side. Sibling dependency: knowledge only in full mode.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — internal/prime/doc.go is a doc comment only. GET /v1/prime is a 501 stub. No prime compact/full, no content contract, no token budget.
### 🧪 Testing
DoD `TestPrimeCompactContractAndBudget` (domain integration, testcontainers). CS §5: fake knowledge from contracts fixtures for full mode; never mock own `prime`.

### 📏 Guidance
CS §§5/6/7/10 (fixed contract, budget discipline); SE-Arch lenses (compact-vs-full parity, no-semantic-in-compact); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-13, AD-3, AD-11); PRD `Kgp6JT3IOR` §4.2 FR-MEM9, §7 NFR-MEM3.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E4**; **E4-S1**, **E2-S3** (ready), **E3-S1** (claims).
- Blocks: E4-S4 (fast-path), E7 (surfaces), E9 (server-side prime injection).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
