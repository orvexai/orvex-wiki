### 🎯 Story
As a **coordinating agent** I want **typed edges between work items (`blocks`, `related`, `parent-child`, `discovered-from`) with cycle rejection on `blocks`** so that **dependencies and side-quest provenance are first-class and the ready-work graph never corrupts**.

**Definition of Done:** one named test `TestBlocksEdgeRejectsCycle` (domain+store layer) asserts a `blocks` edge that would form a cycle is rejected at write, and the three other edge types persist without cycle checks. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given two items, When a `blocks` edge is added, Then it persists and triggers `recomputeBlocked(affectedSet)` for the affected subgraph. *assert: edge row + blocked flag recompute invoked with the correct affected set.* [Source: FR-MEM2, AD-4]
- [ ] **AC2** — Given an edge that would close a `blocks` cycle, When added, Then it is rejected loudly at write time. *assert: cycle write → error; graph unchanged.* [Source: FR-MEM2, AD-4]
- [ ] **AC3** — Given a side-quest item created while holding a claim, When filed with a `discovered-from` edge, Then the provenance edge persists (the mandated side-quest ritual). *assert: `discovered-from` edge present; SM-4 signal derivable.* [Source: FR-MEM2, SM-4]
- [ ] **AC4 (negative)** — Given a `related` or `parent-child` edge forming a "cycle", When added, Then it is allowed (only `blocks` is cycle-checked). *assert: non-blocks cycles persist.* [Source: FR-MEM2]
- [ ] **AC5 (error-path)** — Given an edge to a missing item, When added, Then it is rejected (or flagged orphaned per edge type). *assert: dangling `blocks` target → rejected; orphan-blocker signal for closed targets.* [Source: AD-7]

### 🔨 Tasks
- [ ] RED: `TestBlocksEdgeRejectsCycle` + `discovered-from` + non-blocks-cycle cases (AC2, AC3, AC4).
- [ ] GREEN: edge repository + `blocks` cycle detection at write (AC1, AC2).
- [ ] Trigger `recomputeBlocked(affectedSet)` on edge writes (AC1, ties E2-S3).
- [ ] Orphan/dangling-target handling + events (AC5).

### 🧠 Context
Tier: `internal/graph/`. Seam: feeds the single `recomputeBlocked` primitive (E2-S3). Sibling dependency: none. Bi-temporal columns from AD-6 present on edges but full as-of engine deferred.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No edges table (0001_baseline migration explicitly defers edges), graph/doc.go empty, POST /v1/edges is a 501 stub. No typed edges, no cycle rejection.
### 🧪 Testing
DoD `TestBlocksEdgeRejectsCycle` (domain+store integration, testcontainers). CS §5: real store; never mock own `graph`.

### 📏 Guidance
CS §§5/6/7 (domain purity); SE-Arch lenses (cycle-safety, ready-graph integrity); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-4, AD-6); PRD `Kgp6JT3IOR` §4.1 FR-MEM2.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E2**; **E2-S1**.
- Blocks: E2-S3 (ready computation), E2-S5.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
