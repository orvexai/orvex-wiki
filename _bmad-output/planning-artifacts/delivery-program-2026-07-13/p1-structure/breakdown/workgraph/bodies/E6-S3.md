### 🎯 Story
As a **platform owner** I want **the `quota` domain package to compute item/injection-cap verdicts against billing-owned entitlements and return the frozen 402 `QUOTA_EXCEEDED` shape** so that **cap logic never lives in handlers and prime creep is never "fixed" by raising budgets**.

**Definition of Done:** one named test `TestQuotaVerdictFrozen402` (domain layer) asserts a capped write surface returns the frozen 402 `QUOTA_EXCEEDED` (never 429/destructive), computed by `quota` against `billingclient` entitlements, and enrolled in `errors/vocabulary.yaml surfaces`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given a write at the item cap, When submitted, Then `quota` returns the frozen 402 `QUOTA_EXCEEDED` shape and the write is refused. *assert: 402 matches the contract; item not created.* [Source: FR-MEM1 caps, AD-13, ADR-0003]
- [ ] **AC2** — Given entitlements, When a verdict is computed, Then it reads billing-owned dimensions via `billingclient` (dimensions are billing's; the verdict is domain's). *assert: verdict uses billing entitlements, not a local constant.* [Source: AD-13]
- [ ] **AC3** — Given prime/recall injection caps (max-count/max-chars, ≤300-token compact), When enforced, Then truncation is announced via the elision banner (never silent) and recall returns at least one memory or explicit empty. *assert: elision bannered; never silent.* [Source: AD-13, NFR-MEM3]
- [ ] **AC4** — Given a new capped surface, When it returns 402, Then it enrolls in `errors/vocabulary.yaml surfaces` via the ADR + human-ratify lane. *assert: surface enrolled through the ratify lane (ADR-0003 D5, ADR-0008 fail-safe).* [Source: AD-13]
- [ ] **AC5 (negative)** — Given a quota breach, When handled, Then the response is NEVER 429 and NEVER destructive. *assert: no 429; no data deleted on quota breach.* [Source: ADR-0003]

### 🔨 Tasks
- [ ] RED: `TestQuotaVerdictFrozen402` incl. never-429 + injection-cap cases (AC1, AC3, AC5).
- [ ] GREEN: `internal/quota` verdict against `billingclient` (AC1, AC2).
- [ ] Injection-cap enforcement + elision banner wiring (AC3).
- [ ] Vocabulary enrollment via the ratify lane (AC4).

### 🧠 Context
Tier: `internal/quota/`. Seam: `billingclient` (entitlements); banner envelope (contracts). Sibling dependency: `orvex-studio-billing` SoR (quota dimensions OQ2 open — verdict seam is dimension-agnostic). Per-tenant rate limits enforced here at admission (E2-S6).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — internal/quota/doc.go is a doc comment only. The frozen CodeQuotaExceeded=QUOTA_EXCEEDED constant exists (gen/notimpl.go:14) but no 402 verdict logic and no billing-entitlement check. billingclient is constructed but unused for quota.
### 🧪 Testing
DoD `TestQuotaVerdictFrozen402` (domain integration, testcontainers). CS §5: fake billing from contracts fixtures; never mock own `quota`.

### 📏 Guidance
CS §§5/6/7 (quota out of handlers); SE-Arch lenses (frozen error posture, no-destructive-on-quota); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-13, ADR-0003); PRD `Kgp6JT3IOR` §4.1 FR-MEM1, §7 NFR-MEM3.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — frozen 402 shape); parent epic **E6**; **E2-S1** (item writes), **E4-S1/S2** (injection caps).
- Upstream: `orvex-studio-billing`.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
