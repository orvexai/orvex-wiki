### 🎯 Story
As a **platform owner** I want **async consolidation (ADD/UPDATE/DELETE/NOOP over top-k similar, preceded by cheap 0.98-cosine intra-batch dedup)** so that **the fact/closed-item store stays lean without ever touching live coordination state**.

**Definition of Done:** one named test `TestConsolidationNeverTouchesLiveState` (janitor layer) asserts consolidation operates ONLY on Remembered Facts and *closed* items, soft-deletes contradictions with a trail (never silent overwrite), and never mutates open/in-progress/claimed items or pending handoffs. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given a post-write trigger, When consolidation runs, Then a cheap non-LLM 0.98-cosine intra-batch dedup runs first, then top-k similar → ADD/UPDATE/DELETE/NOOP via `orvex-studio-ai`. *assert: dedup precedes the LLM decide; decision recorded.* [Source: FR-MEM11, AD-5]
- [ ] **AC2** — Given a contradiction, When decided, Then it soft-deletes (invalidate with trail), never a silent overwrite. *assert: invalidation carries a decision trail (NFR-MEM5).* [Source: FR-MEM11, NFR-MEM5]
- [ ] **AC3** — Given open/in-progress/claimed items or pending handoffs, When consolidation runs, Then they are UNTOUCHABLE. *assert: property test — no mutation of live state under any input.* [Source: FR-MEM11, AD-5]
- [ ] **AC4** — Given the hot path, When a write occurs, Then consolidation runs OFF it (sleep-time pattern, async). *assert: write latency excludes consolidation; it runs via the step-API.* [Source: FR-MEM11, AD-2]
- [ ] **AC5 (error-path)** — Given the ai call fails, When consolidation runs, Then it fails safe (no partial mutation, retry via workflow), never corrupts the store. *assert: ai failure → no store change, retriable.* [Source: no-fallbacks]

### 🔨 Tasks
- [ ] RED: `TestConsolidationNeverTouchesLiveState` property test (AC3) + contradiction-trail (AC2).
- [ ] GREEN: `internal/janitor` consolidate loop (dedup → ai-decide → apply) (AC1).
- [ ] Soft-delete with trail; live-state guard (AC2, AC3).
- [ ] Off-hot-path via step-API; ai-failure safety (AC4, AC5).

### 🧠 Context
Tier: `internal/janitor/`. Seam: `aiclient` (no-train config, E5-S4); knowledge top-k (same AD-3 path, async). Sibling dependency: `orvex-studio-ai`; runs under the E5-S4 step-API. The janitor owns the whole loop internally; workflows only triggers/checkpoints.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — internal/janitor/doc.go is a doc comment only. POST /internal/consolidate is a 501 stub. No consolidation ADD/UPDATE/DELETE/NOOP, no cosine dedup.
### 🧪 Testing
DoD `TestConsolidationNeverTouchesLiveState` (janitor integration, testcontainers). CS §5: fake ai + knowledge from contracts fixtures; never mock own `janitor`.

### 📏 Guidance
CS §§4/5/6/7 (async janitor, no live mutation); SE-Arch lenses (scope-invariant enforcement, fail-safe); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-5, AD-2); PRD `Kgp6JT3IOR` §4.3 FR-MEM11; Addendum `2KrwsvY5zL` A2.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E5**; **E2-S1** (closed items), **E4-S1** (facts), **E5-S4** (step-API), **E4-S3** (top-k path).
- Upstream: `orvex-studio-ai`, `orvex-studio-knowledge`.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
