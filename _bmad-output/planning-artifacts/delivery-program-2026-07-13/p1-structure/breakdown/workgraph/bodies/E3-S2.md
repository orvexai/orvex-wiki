### 🎯 Story
As a **coordinating agent whose peer crashed** I want **a reaper that returns stale-leased items to ready via a conditional CAS** so that **nothing is lost when an agent dies mid-work and slow-but-alive agents are never wrongly reaped**.

**Definition of Done:** one named test `TestReaperRevertsOnlyStaleLeases` (domain/`cmd/reaper` layer) asserts an item whose lease expired is reverted to ready with a recovery event, while an item heartbeated within the grace window is untouched — the staleness predicate living inside the mutating statement (no read-then-revert-by-id). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given an item with an expired lease, When the reaper ticks, Then it reverts to ready (claim released, `in-progress`→`open`) and emits a recovery event. *assert: reverted item + `studio.workgraph.*` recovery event.* [Source: FR-MEM4, AD-5]
- [ ] **AC2** — Given the revert, When executed, Then the staleness predicate is in the mutating CAS statement (conditional revert), never read-then-revert-by-id. *assert: a lease renewed between read and write is NOT reverted.* [Source: AD-5]
- [ ] **AC3** — Given ≥95% of expired-lease items, When the reaper runs within the grace window, Then they return to ready (SM-2). *assert: recovery rate ≥95% in the harness.* [Source: SM-2]
- [ ] **AC4 (negative)** — Given a slow agent that heartbeats in time, When the reaper ticks, Then its item is NOT reaped. *assert: heartbeated item untouched.* [Source: FR-MEM4, AD-5]
- [ ] **AC5 (error-path)** — Given the reaper runs twice concurrently, When both tick, Then reverts are idempotent (no double-revert corruption). *assert: idempotent under concurrent ticks.* [Source: no-fallbacks]

### 🔨 Tasks
- [ ] RED: `TestReaperRevertsOnlyStaleLeases` incl. heartbeat-in-time + concurrent-tick cases (AC1, AC2, AC4, AC5).
- [ ] GREEN: `cmd/reaper` in-service tick; conditional-CAS revert with recovery event (AC1, AC2).
- [ ] Recovery-rate measurement (AC3).
- [ ] Idempotency under concurrent ticks (AC5).

### 🧠 Context
Tier: `cmd/reaper` thin main + `internal/claim`. Seam: revert triggers `recomputeBlocked`; emits events. Sibling dependency: `system` principal in `authz` (E6-S1) authorizes reaper revert. Workload shape open (in-service tick vs workflows cron) — default in-service Deployment (billing `cmd/orphansweep` precedent).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — cmd/reaper is a health-only binary scaffold with an explicit TODO(delivery) — no reaper tick, no conditional-CAS stale-lease revert. claim/doc.go empty. Binary scaffold + Deployment manifest exist only.
### 🧪 Testing
DoD `TestReaperRevertsOnlyStaleLeases` (domain integration, testcontainers). CS §5: real store; never mock own `claim`.

### 📏 Guidance
CS §§5/6/7 (conditional CAS, idempotency); SE-Arch lenses (crash-recovery correctness, no read-then-write); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-5, AD-9 system principal); PRD `Kgp6JT3IOR` §4.1 FR-MEM4, §8 SM-2.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E3**; **E3-S1**.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
