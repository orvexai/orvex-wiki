### 🎯 Story
As a **coordinating agent** I want **to create, update, close, and reopen Work Items and append notes/comments** so that **durable context is captured so nobody has to read the transcript**.

**Definition of Done:** one named test `TestItemCrudEmitsEvents` (domain+event integration layer) asserts each mutation persists correctly and emits the matching `studio.workgraph.<resource>.<past-tense>` event through the outbox. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given a create request, When persisted, Then the item carries type (`task|bug|decision|note|handoff|epic`), title, description, `status ∈ {open, in-progress, closed}`, priority, producer, and a short id, and emits `studio.workgraph.item.created`. *assert: row + event match; create p95 measured (provisional NFR-MEM1 ≤150ms).* [Source: FR-MEM1, AD-4]
- [ ] **AC2** — Given an update/close/reopen, When applied, Then status transitions are single-valued (`blocked`/`deferred`/`claimed` are NOT status members) and each emits its event. *assert: illegal enum member rejected; events emitted.* [Source: FR-MEM1, AD-4]
- [ ] **AC3** — Given notes/comments, When appended, Then they are append-only (no in-place edit) and carry the resumability template fields when supplied. *assert: attempt to mutate an existing note is rejected.* [Source: FR-MEM1]
- [ ] **AC4 (negative)** — Given a reopen after a `rejected` resolution, When applied, Then the per-item re-open counter increments (anomaly signal). *assert: counter increments on the event payload.* [Source: FR-MEM1, AD-7]
- [ ] **AC5 (error-path)** — Given a create for a tenant at its item cap, When submitted, Then it returns the frozen 402 `QUOTA_EXCEEDED` (delegated to `quota`). *assert: 402 shape, item not created.* [Source: FR-MEM1, AD-13]

### 🔨 Tasks
- [ ] RED: `TestItemCrudEmitsEvents` covering create/update/close/reopen + note append (AC1–AC3).
- [ ] GREEN: `internal/graph` item repository + mutations; wire outbox events (AC1, AC2).
- [ ] Append-only notes; re-open counter on the event (AC3, AC4).
- [ ] Delegate cap verdict to `quota` (AC5, ties E6-S3).

### 🧠 Context
Tier: `internal/graph/` domain + `store/postgres` + `event`. Seam: outbox events (E1-S4); `quota` (E6-S3). Sibling dependency: none in the hot path (AD-2 zero-fanout).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — internal/graph/doc.go is a doc comment only. POST/PATCH /v1/items are 501 stubs (server.go:50). No item CRUD, no notes/comments, no events emitted.
### 🧪 Testing
DoD `TestItemCrudEmitsEvents` (domain+event integration, testcontainers). CS §5: real store + fake spine sink; never mock own `graph`/`event` packages.

### 📏 Guidance
CS §§5/6/7/10 (domain purity, event discipline); SE-Arch lenses (single-valued lifecycle, no orthogonal-dimension-as-enum); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-2, AD-4, AD-7); PRD `Kgp6JT3IOR` §4.1 FR-MEM1.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E2**; **E1** (store, short ids, outbox).
- Blocks: E2-S2..S6, E3, E4, E5.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
