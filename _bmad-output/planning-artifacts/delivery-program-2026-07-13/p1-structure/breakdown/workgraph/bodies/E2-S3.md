### 🎯 Story
As a **coordinating agent** I want **`ready`/`blocked`/`stats` queries backed by a single denormalized `blocked` flag with incremental subgraph recompute, plus defer/undefer** so that **I get correct unblocked work fast, even under batch writes, without full-graph rebuilds**.

**Definition of Done:** one named test `TestReadyReflectsIncrementalRecompute` (domain+store layer) asserts `ready ≝ status=open ∧ ¬blocked ∧ ¬deferred ∧ ¬claimed` holds after edge/claim/defer transitions and after the idempotent repair job, with recompute scoped to the affected subgraph only. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given items with edges/claims/defers, When `ready` runs, Then it returns exactly open ∧ ¬blocked ∧ ¬deferred ∧ ¬claimed items ranked by priority/age/hybrid. *assert: result set matches ground truth for a seeded graph.* [Source: FR-MEM3, AD-4]
- [ ] **AC2** — Given a `blocks` resolution or claim/close transition, When it fires, Then `recomputeBlocked(affectedSet)` updates only the affected subgraph via ONE serialized row-locked path (never parallel bespoke recomputes). *assert: recompute touches only affected rows; concurrent triggers serialize.* [Source: AD-4, NFR-MEM2]
- [ ] **AC3** — Given `blocked`, When called on an item, Then it explains its named blockers; `stats` summarizes counts by class. *assert: blockers list + stats totals correct.* [Source: FR-MEM3]
- [ ] **AC4** — Given defer/undefer, When applied, Then a deferred item is hidden from `ready` until due, distinct from blocked. *assert: deferred item absent from ready; reappears after undefer/due.* [Source: FR-MEM7]
- [ ] **AC5 (negative)** — Given a 100-item batch write, When it commits, Then recompute stays incremental (no full-graph rebuild). *assert: recompute cost scales with affected set, not graph size — measured.* [Source: NFR-MEM2]

### 🔨 Tasks
- [ ] RED: `TestReadyReflectsIncrementalRecompute` incl. batch + defer cases (AC1, AC2, AC4, AC5).
- [ ] GREEN: denormalized `blocked` flag + single `recomputeBlocked(affectedSet)` primitive (AC2).
- [ ] `ready`/`blocked`/`stats` queries + ranking; defer/undefer dimension (AC1, AC3, AC4).
- [ ] Idempotent repair job (AC2).

### 🧠 Context
Tier: `internal/graph/`. Seam: recompute is triggered by edge writes (E2-S2), claim/close (E3), gate resolutions (E2-S5). Sibling dependency: none — pure Postgres. Compact prime's top item orders by this ready sub-order (E4-S2).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — Only schema affordances exist: denormalized blocked + deferred columns and the idx_work_item_ready partial index (0001_baseline.sql:33,41). GET /v1/ready and /v1/stats are 501 stubs; no ready/blocked/stats queries, no defer/undefer, no incremental recompute.
### 🧪 Testing
DoD `TestReadyReflectsIncrementalRecompute` (domain+store integration, testcontainers). CS §5: real store; never mock own `graph`. Concurrency under `-race`.

### 📏 Guidance
CS §§5/6/7/10 (domain purity, no parallel bespoke recomputes); SE-Arch lenses (denormalization-repair correctness); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-4, NFR-MEM2); PRD `Kgp6JT3IOR` §4.1 FR-MEM3, FR-MEM7.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E2**; **E2-S1**, **E2-S2**.
- Blocks: E2-S6 (load test), E4-S2 (prime), E3 (transitions trigger recompute).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
