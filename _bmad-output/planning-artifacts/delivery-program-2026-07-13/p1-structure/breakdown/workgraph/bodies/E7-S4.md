### 🎯 Story
As a **Claude API agent** I want **a memory-tool backend that serves Anthropic's `memory_20250818` `/memories` file protocol from the workgraph store** so that **I get native Claude memory backed by Orvex with the same namespaces, grants, and audit**.

**Definition of Done:** one named test `TestClaudeMemoryToolBackendFileOps` (adapter integration layer) asserts the `view/create/str_replace/insert/delete/rename` file ops over `/memories` map onto workgraph Remembered Facts through the published verbs + `authz`, honoring namespaces, grants, and audit. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the `memory_20250818` protocol, When a Claude agent issues `/memories` file ops, Then `view/create/str_replace/insert/delete/rename` are served from the workgraph store via published verbs. *assert: each file op maps to a workgraph verb; no direct store access.* [Source: FR-MEM27, AD-10]
- [ ] **AC2** — Given the backend, When it serves an op, Then namespaces, grants, and audit apply identically to other surfaces (through `authz`). *assert: a grant-denied memory is invisible via the file protocol; op audited.* [Source: FR-MEM27, AD-9, AD-10]
- [ ] **AC3** — Given a `delete`, When issued, Then it soft-deletes with a trail (hard erasure remains FR-MEM13). *assert: delete leaves a trail; not an immediate hard purge unless erasure.* [Source: FR-MEM27, NFR-MEM5]
- [ ] **AC4 (negative)** — Given ChatGPT-native memory, When considered, Then it is out of scope — platform-blocked (OpenAI exposes no memory API), recorded as an external constraint. *assert: no ChatGPT-native bridge shipped; documented as external constraint.* [Source: FR-MEM27, PRD §5]
- [ ] **AC5 (error-path)** — Given a file op that would cross a namespace, When issued, Then `authz` denies it. *assert: cross-namespace file op denied.* [Source: AD-9]

### 🔨 Tasks
- [ ] RED: `TestClaudeMemoryToolBackendFileOps` incl. grant-visibility + cross-namespace cases (AC1, AC2, AC5).
- [ ] GREEN: `memory_20250818` `/memories` file-op handlers mapping to workgraph verbs (AC1).
- [ ] Namespace/grant/audit enforcement; soft-delete trail (AC2, AC3).
- [ ] Document the ChatGPT-native external constraint (AC4).

### 🧠 Context
Tier: adapter module (thin translator). Seam: published verbs + `authz`; protocol pinned `memory_20250818`. Sibling dependency: served for platform-run Claude agents. Naming firewall: this is agent-state, distinct from the user-memory product.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No Claude memory-tool backend (memory_20250818 /memories file ops).
### 🧪 Testing
DoD `TestClaudeMemoryToolBackendFileOps` (adapter integration). CS §5: fake the workgraph API from contracts fixtures; never mock own adapter/authz.

### 📏 Guidance
CS §§4/5/6/7 (adapter-through-verbs); SE-Arch lenses (protocol fidelity, no bypass); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-10, AD-9, Stack — `memory_20250818`); PRD `Kgp6JT3IOR` §4.7 FR-MEM27, §5.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E7**; **E7-S1** (verbs), **E6-S1** (authz), **E4-S1** (facts).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
