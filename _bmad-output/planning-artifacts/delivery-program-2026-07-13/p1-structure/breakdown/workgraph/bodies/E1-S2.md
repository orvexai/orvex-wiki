### 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.
As a **workgraph build agent** I want **the Postgres store baseline — UUIDv7 keys, tenant-keyed tables, fail-closed RLS, and the embedded-FS forward-only migration applier** so that **every domain writes through one driver with tenant isolation enforced at the row level**.

**Definition of Done:** one named test `TestTenantRLSFailClosed` (store/integration layer, testcontainers) asserts that a query without the `app.tenant` GUC set returns zero rows and a query set to tenant A never returns tenant B's rows. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the baseline migration `0001`, When it applies, Then RLS policies exist on every tenant-keyed table and the `app.tenant` GUC gates every read. *assert: `SELECT` with no GUC → 0 rows; with GUC=A → only A's rows.* [Source: NFR-MEM4, AD-9]
- [ ] **AC2** — Given tables, When created, Then PKs are UUIDv7, the tenant column is `tenant`, and no table carries a `cell_id` column. *assert: schema introspection matches; cell-lint 5/7 pass.* [Source: AD-6, cell-lint JGAUQRsw2g]
- [ ] **AC3** — Given the embedded-FS applier, When migrations run, Then they are forward-only, idempotent, and taken under `pg_advisory_xact_lock` (no external framework). *assert: re-running the applier is a no-op; concurrent applier instances serialize.* [Source: Architecture Migrations & tests]
- [ ] **AC4 (negative)** — Given a query for tenant B while GUC=A, When executed, Then it returns 0 rows (never leaks). *assert: cross-tenant read = 0 rows in `TestTenantRLSFailClosed`.* [Source: NFR-MEM4]
- [ ] **AC5 (error-path)** — Given a migration that fails midway, When re-run, Then it resumes idempotently without partial-state corruption. *assert: injected failure → clean re-apply.* [Source: no-fallbacks]

### 🔨 Tasks
- [ ] RED: `TestTenantRLSFailClosed` against a testcontainers Postgres (AC1, AC4).
- [ ] GREEN: author `0001_baseline.sql` with RLS policies + UUIDv7 defaults + tenant columns (AC1, AC2).
- [ ] Build the embedded-FS advisory-lock applier (AC3, AC5).
- [ ] Wire `internal/store/postgres` as the sole `pgx` importer (AC2).

### 🧠 Context
Tier: `internal/store/postgres/` is the ONLY package importing the driver. Seam: none external. Sibling dependency: none — pure local floor. CNPG Postgres 17, no pgvector (ADR-0014).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** present — Fully implemented with meaningful testcontainers tests. postgres.go has Open + EnsureSchema (embedded-FS forward-only applier under pg_advisory_xact_lock:107). 0001_baseline.sql: UUIDv7 PKs (uuidv7()), tenant column, FORCE RLS fail-closed policies on work_item + outbox. Tests: TestEnsureSchemaBaselineApplies (idempotent re-apply, table existence) + TestRLSFailsClosedWithoutTenantGUC (non-superuser role, 0 rows without GUC, 1 with GUC). NOTE: DoD names the test TestTenantRLSFailClosed; the repo test is TestRLSFailsClosedWithoutTenantGUC and covers only single-tenant GUC gating, not the AC4 cross-tenant B-while-GUC=A leak assertion (harden this). Migration deliberately minimal: only work_item+outbox tables exist yet.
### 🧪 Testing
DoD `TestTenantRLSFailClosed` (store/integration, testcontainers in-package). CS §5: never mock own packages; use a real container, not a fake DB. Add cell-lint static checks.

### 📏 Guidance
CS §§5/6/7/10/11 (no driver leakage, migrations discipline, RLS); SE-Arch lenses (isolation-probe rigor); cell-lint `JGAUQRsw2g` 5/7/10 (tenant-keyed, UUIDv7, TenantMoveManifest coverage).

### 🔗 References
- Architecture `bnbRUdCm6R` (Inherited Invariants P5/D-S12/ADR-0014, AD-9, Consistency Conventions); PRD `Kgp6JT3IOR` §7.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E1**; **E1-S1** (scaffold).
- Blocks: E1-S4, E1-S5, and all domain epics.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
