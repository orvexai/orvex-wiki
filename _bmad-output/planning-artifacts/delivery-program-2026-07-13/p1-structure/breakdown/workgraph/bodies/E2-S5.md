### 🎯 Story
As a **fleet operator / planning agent** I want **beads-style data-plane coordination primitives — gate items, work templates, and epic dispatch views** so that **I can express wait conditions, instantiate parameterized multi-item work, and see the ready-front of an epic — without a workflow engine**.

**Definition of Done:** one named test `TestGateBlocksDependentsUntilResolved` (domain layer) asserts a gate item blocks its dependents through the same `blocked`-flag machinery until resolved, a work template instantiates its multi-item set with pre-wired edges in one transaction, and a dispatch view computes the ready-front live from item state. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given a gate item (human/timer/external-check), When unresolved, Then dependents are blocked via the same `recomputeBlocked` flag; on resolution they become ready. *assert: dependent blocked then ready after gate resolve.* [Source: FR-MEM26, AD-4]
- [ ] **AC2** — Given a work template, When instantiated with params, Then its multi-item set with pre-wired edges is created in one transaction, cycle-rejected at instantiation like any edge write. *assert: atomic instantiation; a template forming a cycle is rejected.* [Source: FR-MEM26, AD-4]
- [ ] **AC3** — Given an epic subtree, When a dispatch view is requested, Then ready-front / parallelism is computed live from item state (never stored separately). *assert: view recomputes after a state change; no persisted view row.* [Source: FR-MEM26, AD-4]
- [ ] **AC4 (negative)** — Given a request for scheduling/execution/retries, When made, Then it is out of scope (data plane only — no workflow engine, no agent mail). *assert: no execution primitive exists; boundary documented.* [Source: PRD §5, FR-MEM26]
- [ ] **AC5 (error-path)** — Given a gate resolved twice, When the second resolve arrives, Then it is idempotent (no double-unblock corruption). *assert: idempotent gate resolution.* [Source: no-fallbacks]

### 🔨 Tasks
- [ ] RED: `TestGateBlocksDependentsUntilResolved` incl. template + dispatch-view + idempotent-resolve cases (AC1–AC3, AC5).
- [ ] GREEN: gate items routed through `recomputeBlocked`; template instantiation in one tx (AC1, AC2).
- [ ] Live-computed dispatch views over epic subtrees (AC3).
- [ ] Enforce the data-plane-only boundary (AC4).

### 🧠 Context
Tier: `internal/graph/`. Seam: `recomputeBlocked` (E2-S3). Sibling dependency: scheduling/execution stays in `orvex-studio-workflows` (§5 boundary) — this is data plane only.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No coordination-primitive code: no gate items, work templates, or epic dispatch views. graph/doc.go is a doc comment only.
### 🧪 Testing
DoD `TestGateBlocksDependentsUntilResolved` (domain integration, testcontainers). CS §5: real store; never mock own `graph`.

### 📏 Guidance
CS §§5/6/7 (domain purity); SE-Arch lenses (charter-boundary — beads deleted `bd mail`); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-4); PRD `Kgp6JT3IOR` §4.7 FR-MEM26, §5 Non-Goals.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E2**; **E2-S2**, **E2-S3**.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
