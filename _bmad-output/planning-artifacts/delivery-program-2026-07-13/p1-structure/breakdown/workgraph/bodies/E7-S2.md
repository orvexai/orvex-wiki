### 🎯 Story
As a **CI / terminal agent** I want **an `orvex-cli workgraph` section with verb parity and `--json` throughout** so that **scripted agents coordinate through the same workgraph verbs as the MCP surface**.

**Definition of Done:** one named test `TestCliWorkgraphParityJson` (CLI integration layer) asserts the CLI verbs (`prime, ready, claim, close, note, dep, handoff, remember, recall, search, stats, batch`) map onto the same published workgraph API verbs through `authz`, and every command supports `--json`. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given `orvex-cli workgraph <verb>`, When run, Then each parity verb calls the corresponding published API verb (no store access). *assert: verb → API mapping; import-lint forbids `store/`.* [Source: FR-MEM15, AD-10]
- [ ] **AC2** — Given `--json`, When any command runs, Then it emits machine-parseable JSON for scripted agents. *assert: `--json` output validates against the response schema.* [Source: FR-MEM15]
- [ ] **AC3** — Given the same principal token, When CLI and MCP both act, Then `authz` evaluates identically (one authz path). *assert: a grant-denied item is denied on both surfaces.* [Source: AD-9, AD-10]
- [ ] **AC4 (negative)** — Given a degraded store, When a CLI command runs, Then it surfaces the honest-empty/status banner, never fabricated output, and never a weaponizable error. *assert: degraded → status banner; no destructive command text.* [Source: NFR-MEM6, Agent-safety]

### 🔨 Tasks
- [ ] RED: `TestCliWorkgraphParityJson` incl. authz-parity + degraded cases (AC1, AC3, AC4).
- [ ] GREEN: `orvex-cli workgraph` namespace + parity verbs via the API client (AC1).
- [ ] `--json` on every command (AC2).
- [ ] Shared authz + degradation surfacing (AC3, AC4).

### 🧠 Context
Tier: `orvex-cli` repo (codegen from contracts tag). Seam: published workgraph verbs + `authz`. Sibling dependency: `orvex-cli` (bare scaffold per current-state map — this section is one of its build phases). Naming: `orvex-cli workgraph <verb>` (always full word).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — No CLI code in this repo (orvex-cli is a separate repo). No workgraph CLI section present here.
### 🧪 Testing
DoD `TestCliWorkgraphParityJson` (CLI integration). CS §5: fake the workgraph API from contracts fixtures; never mock own CLI code.

### 📏 Guidance
CS §§4/5/6/7 (adapter-through-verbs); SE-Arch lenses (surface parity, agent-safety); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-10, AD-9); PRD `Kgp6JT3IOR` §4.4 FR-MEM15.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E7**; **E7-S1** (verb surface), **E6-S1** (authz).
- Upstream: `orvex-cli` repo.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
