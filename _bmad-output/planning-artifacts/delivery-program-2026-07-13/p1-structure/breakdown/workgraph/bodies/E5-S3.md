### 🎯 Story
As a **data subject exercising right-to-erasure** I want **hard cascading delete that purges an item/fact and all its derived artifacts immediately, leaving only a content-free tombstone** so that **GDPR erasure is real — no TTL-floor soft-expiry, no derived residue**.

**Definition of Done:** one named test `TestHardDeletePurgesDerivedAndTombstones` (domain+janitor+event layer) asserts erasure purges rows, history, summaries, embeddings and consolidation traces, emits a knowledge purge event, and leaves only a content-free tombstone event — overriding FR-MEM12's restore promise and the reference-aware guard. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given an erasure request, When executed, Then the item/fact AND its derived artifacts (summaries, embeddings, consolidation traces) are purged immediately (no TTL floor). *assert: zero rows + zero derived artifacts remain.* [Source: FR-MEM13]
- [ ] **AC2** — Given the embeddings live in knowledge, When erasure runs, Then a knowledge purge event is emitted per the source-adapter contract. *assert: purge event shaped + emitted; knowledge drops the vectors.* [Source: FR-MEM13, AD-3]
- [ ] **AC3** — Given erasure precedence, When it conflicts with FR-MEM12 restore or the reference-aware guard, Then erasure WINS. *assert: erased content is not restorable and not protected by a citation.* [Source: FR-MEM13, AD-5]
- [ ] **AC4** — Given the audit trail, When erasure completes, Then only a content-free tombstone event remains (records that a deletion occurred, no content). *assert: tombstone carries no erased content.* [Source: FR-MEM13, NFR-MEM7]
- [ ] **AC5 (negative)** — Given a per-principal purge, When run, Then no cross-tenant or cross-principal data is affected. *assert: only the target principal's data purged.* [Source: NFR-MEM7]
- [ ] **AC6 (error-path)** — Given the knowledge purge event fails, When erasure runs, Then it retries until the derived artifacts are confirmed gone (no silent half-erasure). *assert: purge is eventually-complete, not fire-and-forget.* [Source: no-fallbacks]

### 🔨 Tasks
- [ ] RED: `TestHardDeletePurgesDerivedAndTombstones` incl. precedence + tombstone + cross-principal cases (AC1, AC3–AC5).
- [ ] GREEN: cascading purge across rows/history/summaries/traces (AC1).
- [ ] Knowledge purge event; content-free tombstone (AC2, AC4).
- [ ] Erasure-wins precedence + retry-until-confirmed (AC3, AC6).

### 🧠 Context
Tier: `internal/graph`/`internal/facts` + `internal/janitor` + `internal/event`. Seam: knowledge purge event (E8-S1 shape); source-adapter contract. Sibling dependency: `orvex-studio-knowledge` (drops vectors). NFR-MEM7 compliance posture — day-one.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — internal/janitor/doc.go is a doc comment only. POST /internal/purge is a 501 stub. No cascading hard delete, no tombstone, no knowledge purge.
### 🧪 Testing
DoD `TestHardDeletePurgesDerivedAndTombstones` (domain+event integration, testcontainers). CS §5: fake knowledge sink from contracts fixtures; never mock own packages.

### 📏 Guidance
CS §§5/6/7/11 (cascading purge, no soft-expiry floor); SE-Arch lenses (erasure-wins precedence, no residue); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-5, AD-3, AD-7); PRD `Kgp6JT3IOR` §4.3 FR-MEM13, §7 NFR-MEM7.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — purge event shape); parent epic **E5**; **E8-S1** (purge event), **E5-S1**/**E5-S2** (derived artifacts to purge).
- Upstream: `orvex-studio-knowledge`.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
