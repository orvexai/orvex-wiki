### 🎯 Story
As a **platform owner** I want **janitor `/internal/*` step-APIs scheduled by `orvex-studio-workflows`, running the LLM path under a no-train / EU-resident / zero-retention `orvex-studio-ai` config** so that **the service owns no Temporal worker and NFR-MEM7's compliance clause is contractually backed**.

**Definition of Done:** one named test `TestJanitorStepApiIdempotentUnderReplay` (step-API integration layer) asserts the pinned `/internal/*` step-APIs honor `Idempotency-Key` (safe under workflow retry), and the ai calls are configured against the named no-train/EU-resident/zero-retention provider from the contracts seam. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the janitor step-APIs, When defined, Then their bodies + schemas are pinned in the contracts tag and honor `Idempotency-Key` (idempotent under replay). *assert: replay with the same key applies once.* [Source: AD-5, NFR-MEM5]
- [ ] **AC2** — Given schedules, When they run, Then they live in `orvex-studio-workflows` calling the idempotent `/internal/*` step-APIs; the service runs NO own Temporal worker. *assert: no Temporal SDK worker in this repo; workflows triggers via step-API.* [Source: P6/D-WF-1, AD-5]
- [ ] **AC3** — Given the orchestration boundary, When consolidation/compaction runs, Then the janitor owns the whole loop internally (top-k → ai-decide → apply); workflows only triggers/checkpoints. *assert: workflows never sees intermediate loop state.* [Source: AD-5]
- [ ] **AC4** — Given the ai path, When invoked, Then it runs under the contractual no-train / EU-resident / zero-retention provider configuration named in the contracts seam. *assert: config asserts the named provider posture; NFR-MEM7 conditional dependency satisfied.* [Source: NFR-MEM7]
- [ ] **AC5 (negative)** — Given the no-train config is absent, When the janitor would call ai, Then it refuses to run (NFR-MEM7 otherwise unbacked). *assert: missing no-train contract → janitor halts loudly.* [Source: NFR-MEM7, no-fallbacks]
- [ ] **AC6 (error-path)** — Given burst applies, When large payloads flow, Then workflows checkpoints are event-count-capped and store-by-reference (Temporal cliff). *assert: large payload stored by reference, not inlined into the workflow.* [Source: NFR-MEM2]

### 🔨 Tasks
- [ ] RED: `TestJanitorStepApiIdempotentUnderReplay` (AC1) + no-train-refusal case (AC5).
- [ ] GREEN: `/internal/*` step-API handlers (Idempotency-Key) for consolidate/compact triggers (AC1, AC2).
- [ ] Wire workflows schedules (trigger/checkpoint only); no own worker (AC2, AC3).
- [ ] Assert the no-train provider config; store-by-reference burst handling (AC4, AC5, AC6).

### 🧠 Context
Tier: `internal/janitor` + `internal/workflow` (request-scoped only) + step-API handlers on `cmd/api`. Seam: `orvex-studio-workflows` schedules; `aiclient` no-train config named in contracts. Sibling dependency: workflows + ai. Reaper workload shape (E3-S2) is the one genuinely-open form.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — The three /internal/{consolidate,compact,purge} step-API routes are declared as 501 stubs with an Idempotency-Key comment (server.go:71) — a route seam only. No janitor step-API contract implementation, no orvex-studio-workflows schedules, no no-train EU-resident provider config.
### 🧪 Testing
DoD `TestJanitorStepApiIdempotentUnderReplay` (step-API integration, testcontainers). CS §5: fake workflows + ai from contracts fixtures; never mock own `janitor`.

### 📏 Guidance
CS §§5/6/7/11 (step-API idempotency, no own scheduler); SE-Arch lenses (compliance-clause enforcement, Temporal-cliff); cell-lint `JGAUQRsw2g` (Idempotency-Key on `/internal/*`).

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-5, Inherited P6/D-WF-1); PRD `Kgp6JT3IOR` §7 NFR-MEM7, NFR-MEM5.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — step-API + no-train provider named); parent epic **E5**; **E1-S4** (events).
- Upstream: `orvex-studio-workflows`, `orvex-studio-ai`.
- Blocks: E5-S1, E5-S2 (run under these step-APIs).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
