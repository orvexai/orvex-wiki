### 🎯 Story
As a **coordinating agent** I want **to atomically claim a ready item with a TTL lease and renew it via owner-only heartbeat** so that **two concurrent agents never double-claim and my in-progress work is not reaped while I'm active**.

**Definition of Done:** one named test `TestConcurrentClaimNoDoubleClaim` (domain/store layer, `go test -race`) asserts that under N concurrent claimers exactly one wins each item (< 1% double-claims, SM-2) and heartbeat renews only for the lease owner. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given a ready item, When `claim` is called, Then assignee + `in-progress` status are set in ONE CAS statement with a TTL lease. *assert: single CAS; item now claimed with lease expiry.* [Source: FR-MEM4, AD-2]
- [ ] **AC2** — Given two concurrent claimers, When both target one item, Then exactly one succeeds and the other CAS-fails. *assert: `-race` harness → 1 winner; SM-2 < 1% double-claims.* [Source: FR-MEM4, SM-2]
- [ ] **AC3** — Given a lease owner, When `heartbeat` is called, Then the lease TTL renews; a non-owner heartbeat is rejected. *assert: owner renews; non-owner → deny.* [Source: FR-MEM4, AD-2]
- [ ] **AC4 (negative)** — Given an already-claimed item, When another agent claims it, Then it fails without side effects. *assert: second claim → conflict, no state change.* [Source: FR-MEM4]
- [ ] **AC5 (error-path)** — Given a degraded prime (stale view), When an agent blind-claims, Then the server-side CAS still fails on conflict (no double-claim from stale reads). *assert: stale-blind claim CAS-fails.* [Source: NFR-MEM6, AD-2]

### 🔨 Tasks
- [ ] RED: `TestConcurrentClaimNoDoubleClaim` under `-race` (AC1, AC2, AC4).
- [ ] GREEN: single-CAS `claim` (assignee + status + lease) in `internal/claim` (AC1).
- [ ] Owner-only `heartbeat` renewal (AC3).
- [ ] Prove stale-blind CAS-fail (AC5).

### 🧠 Context
Tier: `internal/claim/`. Seam: claim/close transitions trigger `recomputeBlocked` (E2-S3); `authz` guards ownership (E6-S1). Sibling dependency: none in the hot path (AD-2). Lease timing is one coherent per-tenant config (`reaper_grace > heartbeat_interval`).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** absent — internal/claim/doc.go is a doc comment only. No claims/leases table in the migration. POST /v1/items/{id}/claim and /heartbeat are 501 stubs. No CAS claim, no lease TTL, no heartbeat.
### 🧪 Testing
DoD `TestConcurrentClaimNoDoubleClaim` (domain/store integration, testcontainers, `-race`). CS §5: real store; never mock own `claim` package.

### 📏 Guidance
CS §§5/6/7 (CAS discipline, no read-then-write races); SE-Arch lenses (concurrency correctness, degraded-mode CAS); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-2, AD-5); PRD `Kgp6JT3IOR` §4.1 FR-MEM4, §8 SM-2.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG); parent epic **E3**; **E2-S1**, **E2-S3**.
- Blocks: E3-S2 (reaper), E3-S3 (handoff claim-CAS).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
