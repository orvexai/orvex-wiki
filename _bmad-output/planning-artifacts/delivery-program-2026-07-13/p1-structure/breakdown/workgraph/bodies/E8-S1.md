### 🎯 Story
As a **platform integrator** I want **`studio.workgraph.*` event schemas plus the P9 source-adapter (`sources/workgraph.yaml`) with a fail-closed `acl_primitive`, purge-event shape, anomaly signals, and OTel wiring** so that **knowledge can index workgraph content safely and the console/search read side has a governed feed**.

**Definition of Done:** one named test `TestSourceAdapterAclPrimitiveFailClosed` (event/adapter integration layer) asserts the `acl_primitive` (`{principal, candidate_ids[]} → allowed_ids[]`) is fail-closed, the rich-event-inline indexing path is authoritative, and no item/note/fact content appears in telemetry. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given `studio.workgraph.<resource>.<past-tense>` events, When authored, Then the types, rich payload schemas, and the `sources/workgraph.yaml` source-adapter entry are pinned in contracts (additive lane; producer-model extension registers via P9). *assert: schemas + source-adapter in the contracts tag; drift-gate lane settled at authoring.* [Source: AD-7, AD-3]
- [ ] **AC2** — Given the `acl_primitive`, When knowledge narrows candidates, Then it is shaped `{principal, candidate_ids[]} → allowed_ids[]` and fail-closed (unknown → denied). *assert: unresolvable candidate → excluded from allowed_ids.* [Source: AD-7, AD-3, P8]
- [ ] **AC3** — Given the source-adapter, When indexing, Then rich-event-inline is authoritative (payloads carry indexable content); the content resolver serves full-reindex/repair only. *assert: index built from event payload, not a live pull.* [Source: AD-3, AD-7]
- [ ] **AC4** — Given anomaly detection needs, When events emit, Then they carry the input signals: a close `resolution` enum (incl. `rejected`), a per-item re-open counter, and edge-target status on edge events; the purge event is shaped alongside. *assert: signals present in payloads; purge event schema exists.* [Source: AD-7, FR-MEM13]
- [ ] **AC5** — Given OTel via `pkg/obs`, When events flow, Then DT extension attrs cross the outbox seam, consumers LINK, and the PII deny-list keeps item titles/notes/fact text OUT of telemetry. *assert: telemetry carries no content; consumers LINK spans.* [Source: ADR-0016]
- [ ] **AC6 (negative)** — Given the cross-tenant isolation probe, When run against the knowledge-indexed corpus, Then bytes=0 across tenants. *assert: cross-tenant bytes=0 probe green in CI + post-deploy.* [Source: AD-3, NFR-MEM4]

### 🔨 Tasks
- [ ] RED: `TestSourceAdapterAclPrimitiveFailClosed` incl. fail-closed + PII-deny cases (AC2, AC5).
- [ ] GREEN: author event + source-adapter schemas (contracts); rich-event-inline indexing (AC1, AC3).
- [ ] Anomaly signals + purge event shape; OTel + PII deny-list (AC4, AC5).
- [ ] Cross-tenant isolation probe (AC6).

### 🧠 Context
Tier: `internal/event/` + `pkg/obs` wiring. Seam: contracts source-adapter (P9); knowledge consumes. Sibling dependency: `orvex-studio-knowledge` (indexer); OTel `pkg/obs` is stub today — wiring is early scope. If drift-gate reads the producer-model extension as a reshape it fail-safes to the ADR lane.

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** partial — OTel is wired via orvex-studio-lib pkg/obs.Setup in every cmd main, and event.go builds studio.workgraph.<resource>.<pastTense> ADR-0007 envelopes (EventType:31, NewEvent:40). But the per-resource event schemas in gen/ are absent (gen carries only Error/Banner), the sources/workgraph.yaml source-adapter with acl_primitive does not exist, and no events are actually emitted (no store writes). No tests.
### 🧪 Testing
DoD `TestSourceAdapterAclPrimitiveFailClosed` (event/adapter integration, testcontainers). CS §5: fake knowledge indexer from contracts fixtures; never mock own `event`.

### 📏 Guidance
CS §§4/5/6/7/11 (event/source-adapter discipline, PII deny-list); SE-Arch lenses (fail-closed acl, telemetry-scoped-not-payload PII rule); cell-lint `JGAUQRsw2g`.

### 🔗 References
- Architecture `bnbRUdCm6R` (AD-7, AD-3, ADR-0016); PRD `Kgp6JT3IOR` §4.4 FR-MEM17, §9.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (contract TAG — event + source-adapter schemas); parent epic **E8**; **E1-S4** (outbox/relay), **E2**-**E5** (producers).
- Upstream: `orvex-studio-knowledge`.
- Blocks: E8-S2 (projections), E4-S3 (semantic leg), E5-S3 (purge event).

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
