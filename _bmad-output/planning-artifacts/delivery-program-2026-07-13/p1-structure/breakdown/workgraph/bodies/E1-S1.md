### 🎯 Story
As a **workgraph build agent** I want **the greenfield repo scaffolded in the six-tier family shape with `gen/` codegen'd from the pinned contracts tag** so that **every later story compiles against generated verb/event/step-API types, never hand-authored prose**.

**Definition of Done:** one named test `TestCodegenReproducibleFromTag` (build/CI layer) asserts that regenerating `gen/` from the pinned `orvex-studio-contracts` tag produces a byte-identical tree (no manual edits) and `go build ./...` succeeds. *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2095); this story is dispatch-blocked until that tag exists.*

### ✅ Acceptance Criteria
- [ ] **AC1** — Given the repo, When `go build ./...` runs, Then it compiles with the six-tier layout (`cmd/{api,relay,reaper}`, `internal/{graph,claim,handoff,facts,prime,ranking,janitor,authz,quota,workflow,store/postgres,event,cache,clients,config,server}`, `gen/`, `migrations/`). *assert: build exit 0; `go vet ./...` clean.* [Source: AD-1]
- [ ] **AC2** — Given the pinned contracts tag, When codegen runs, Then `gen/` regenerates byte-identically. *assert: `git diff --exit-code gen/` after regen returns 0.* [Source: AD-7]
- [ ] **AC3** — Given a tier-discipline lint, When an adapter package imports `store/`, Then the lint fails. *assert: import-lint returns non-zero on a seeded violation.* [Source: Architecture Consistency Conventions]
- [ ] **AC4 (negative)** — Given a hand-edit inside `gen/`, When CI runs, Then `TestCodegenReproducibleFromTag` fails loudly. *assert: seeded manual edit → test red.* [Source: AD-7, no-fallbacks]

### 🔨 Tasks
- [ ] RED: add `TestCodegenReproducibleFromTag` expecting a clean regen (AC2, AC4).
- [ ] GREEN: scaffold six-tier dirs + thin `cmd/*` mains; wire the codegen target to the pinned tag (AC1, AC2).
- [ ] Add the adapter→`store/` import-lint (AC3).
- [ ] CI: `go build/vet/test` + codegen-drift gate (AC2, AC4).

### 🧠 Context
Tier placement: `cmd/*` thin mains; all domain under `internal/<context>/`; `gen/` codegen only. NestJS-engine analogue: not applicable (Go satellite). Seam crossed: contracts tag → `gen/`. Sibling dependency: `orvex-studio-contracts` must have the tag filed (ENG-2095).

**🧾 Code audit (origin/dev @ 5554d3f, 2026-07-14):** partial — Six-tier layout (cmd/{api,relay,reaper} + internal/* + gen/ + deploy/ + CI/Makefile/Dockerfile) and the 501 route table (server.go:38) are real and deliberate. But contracts-tag CODEGEN is NOT wired: gen/ carries only hand-written Error/Banner/ErrorCode envelope shapes (gen/notimpl.go) with a comment that 'the full verb bodies land with the contracts codegen wiring'. No test exercises the route table.
### 🧪 Testing
DoD `TestCodegenReproducibleFromTag` (build/CI tier). CS §5 mocking: no siblings involved; never mock own packages. Add `go vet` + import-lint as static tiers.

### 📏 Guidance
CS `6aMAzsYeQb` §§0/3/4/6/7 (tier discipline, contracts-first, no type-laundering); SE-Arch `8sYi523i4t` lenses (scaffold-honesty, no fake-done); cell-lint `JGAUQRsw2g` (no host literals, UUIDv7 PKs) applied from the first migration.

### 🔗 References
- PRD `Kgp6JT3IOR`; Architecture `bnbRUdCm6R` (Design Paradigm, Structural Seed, Stack); Addendum `2KrwsvY5zL`.

### 🔗 Dependencies
- Blocked by: **ENG-2095** (Definition Pack — the contract TAG is the dispatch gate); parent epic **E1**.
- Blocks: all other E1 stories and every downstream epic.

### 📡 Protocol
CLAIM→PLAN→PROGRESS→COMMITS ("Part of ENG-NNN", never closes)→HANDOFF→REVIEW (reviewer ≠ implementer)→TICK→DONE (orchestrator-only)→ESCALATE.
