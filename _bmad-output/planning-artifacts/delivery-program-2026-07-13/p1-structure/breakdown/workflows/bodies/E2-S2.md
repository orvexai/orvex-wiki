## 🎯 Story

**As a** workflows platform engineer
**I want** the step-API OpenAPI for every domain callee (wiki, studio/identity, knowledge rebuild, billing create-Free/revoke, ai) plus each step's AUTHORITATIVE|BEST-EFFORT retry-class annotation authored in contracts
**so that** activities call a pinned, typed, idempotent contract with a declared retry class — not hand-shaped prose.

**Definition of Done:** one named test `TestStepApiContractConformance` — contract layer: a conformance probe drives each step-API against its OpenAPI + asserts the declared retry-class annotation is present and idempotent (replay N → same end state)

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given every domain's callees, When contracts is authored, Then each has an `/internal/v1/*` OpenAPI entry — *OpenAPI paths exist for wiki provision/deprovision, knowledge rebuild/*, billing create-Free/revoke, studio {scope,id,mode}* [Source: FR-W16 / A-W3]
- [ ] **AC2** — Given each step, When annotated, Then its retry class (AUTHORITATIVE|BEST-EFFORT) is declared — *every step carries exactly one retry-class annotation* [Source: A-W3(7) / NFR-W5]
- [ ] **AC3** — Given an idempotency probe, When a step is called twice, Then the end state is identical (negative: no double-effect) — *double-call yields one effect (natural-key idempotency, not client nonce)* [Source: NFR-W2 / A-W3(1)]
- [ ] **AC4** — Given a 4xx from a step, When observed, Then it is typed non-retryable (no 200-with-error-body) — *contract forbids 200+error-body; 4xx=non-retryable, 5xx/timeout=retryable* [Source: A-W3(4)]

## 🔨 Tasks

- [ ] RED: TestStepApiContractConformance driving each step-API against its OpenAPI
- [ ] Author OpenAPI per domain callee incl. billing create-Free/revoke (AC1)
- [ ] Annotate every step with its retry class (AC2)
- [ ] Add idempotency + typed-error conformance probes (AC3, AC4)
- [ ] GREEN: conformance green against golden-fixture fakes

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — No per-domain step-API OpenAPI documents and no retry-class annotations authored anywhere in the repo. Absent.

**Tier:** contract tier. **Seams:** contracts + every satellite callee (faked from golden fixtures). **Sibling deps:** billing step-API shape is OQ-W10 (TBD — pinned by ENG-2107); ai shape is OQ-W4 (blocked on ai PRD). **Cross-service:** only this service's consumer view of the contracts.

## 🧪 Testing

- [ ] Named DoD test `TestStepApiContractConformance` (contract)
- [ ] Conformance probes run against committed golden-fixture fakes; real satellite integration deferred to the family-E2E share
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-contracts
- [ ] orvex-studio-billing (OQ-W10)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E2 — [EPIC] workflows: Contract seam & shared-lib client integration

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
