## Feature area

The live domain, hardened and generalized. Operates the shipped Clerk pipeline (FR-W1) and extends it: the FR-W2 org-liveness guard (closes the cross-type ordering race), D-S17 user-keyed personal-tenant provisioning (`user:<clerkUserId>`, no Clerk org), Free-plan entitlement create on provision (FR-W18, AUTHORITATIVE billing step-API), the FR-W3 deprovision disable→grace→purge with entitlement revoke, the FR-W4 corrective reconcile (today log-only), and the FR-W5 grace-cancel signal + purge-confirmation gate.

**Why it exists:** PRD F1 (FR-W1–W6, FR-W18) + Arch domain map row 1 + §5 risks (ordering race, over-grant). This is the production domain; every change ships behind GetVersion + a recorded replay (30-day sleeps).

## Story roster

- [ ] E5-S1 — [workflows] Operate + verify the shipped Clerk pipeline: Svix 401/502/200 + 1MB cap (FR-W1)
- [ ] E5-S2 — [workflows] ProvisionWorkflow org-liveness guard — non-retryable stop on deleted org (FR-W2)
- [ ] E5-S3 — [workflows] User-keyed personal-tenant provisioning + polymorphic workflow-ID (FR-W2/FR-W17, D-S17)
- [ ] E5-S4 — [workflows] Free-plan entitlement create on provision — AUTHORITATIVE billing step-API (FR-W18)
- [ ] E5-S5 — [workflows] DeprovisionWorkflow disable→grace→purge + entitlement revoke (FR-W3/FR-W18)
- [ ] E5-S6 — [workflows] ReconcileWorkflow corrective remediation — dry-run first, bounded (FR-W4)
- [ ] E5-S7 — [workflows] Grace-window cancel signal + purge confirmation gate (FR-W5)

## FR/NFR ids this epic owns

FR-W1, FR-W2, FR-W3, FR-W4, FR-W5, FR-W18, FR-W17, NFR-W6, NFR-W7

## Definition of Done

- [ ] All child stories Done (orchestrator-verified)
- [ ] The family six-surface E2E share for this area is **green on the dev cell, human-verified** (not reported) — the fake-done antibody
- [ ] SDD lines evidenced: named tests green, contract fixtures round-trip, SE-Arch lenses signed off

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E5 — [EPIC] workflows: Tenant-lifecycle domain — provision/deprovision/reconcile + polymorphic tenancy + entitlements

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
