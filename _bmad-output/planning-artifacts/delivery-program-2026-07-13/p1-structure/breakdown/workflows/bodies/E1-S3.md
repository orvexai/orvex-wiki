## 🎯 Story

**As a** workflows platform engineer
**I want** a real CI pipeline (go vet/build/test -race + the CS ❌-table lints + the org-level cell-lint required-status ruleset) as the first enforced merge gate
**so that** every 'CI-checkable' claim in the arch stops being aspirational and gates merges for real.

**Definition of Done:** one named test `TestCIGateBlocksNonCompliantPR` — CI-harness layer: a fixture PR that violates a CS lint (e.g. `time.Now()` in a projection) is rejected by the pipeline; a clean PR passes

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a PR, When CI runs, Then go vet + build + `go test -race ./...` all execute and gate merge — *required status checks present + blocking on the branch ruleset* [Source: F-CI / Arch §7]
- [ ] **AC2** — Given a PR introducing `time.Now()` in workflow/projection code, When CI runs, Then it fails the CS determinism lint (negative) — *lint job exit != 0 with the offending file cited* [Source: F-CI / NFR-W1]
- [ ] **AC3** — Given the org cell-lint required status (JGAUQRsw2g §5), When configured, Then it is a blocking check — *cell-lint status is `required` on the ruleset* [Source: cell-lint JGAUQRsw2g]

## 🔨 Tasks

- [ ] RED: TestCIGateBlocksNonCompliantPR with a deliberately non-compliant fixture PR
- [ ] Add `.github/workflows/ci.yml`: go vet/build/test -race (AC1)
- [ ] Wire CS ❌-table lints (handler-thinness, driver-confinement, no time.Now in projection) (AC2)
- [ ] Add cell-lint required-status ruleset (AC3)
- [ ] GREEN: clean PR passes, non-compliant PR blocked

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** PARTIAL — ci.yml runs go vet / go build / go test -race plus a workflow-determinism denylist grep, and .golangci.yml exists. Missing: no golangci-lint step is actually invoked in CI, and there is no cell-lint required status. Partial CI bootstrap.

**Tier:** CI/enforcement. **Seams:** GitHub branch ruleset, cell-lint. **Sibling deps:** prerequisite for E1-S4 (replay gate rides this pipeline) and the A-W9 'Proof' checklist. Per §7 this is 'the single highest-leverage next step'.

## 🧪 Testing

- [ ] Named DoD test `TestCIGateBlocksNonCompliantPR` (ci-harness)
- [ ] Verify a genuinely RED fixture (fake-done antibody — the gate must actually fail, not merely be present)
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] cell-lint contract — `JGAUQRsw2g`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E1 — [EPIC] workflows: Rename, namespace & CI/replay foundation

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
