## 🎯 Story

**As a** workflows platform engineer
**I want** the five `clerk.*` CloudEvent types + `ProvisionTarget`/`DeprovisionTarget` payload schemas defined in `orvex-studio-contracts` and validated by a committed golden-fixture round-trip
**so that** the CE envelope is enforced against the catalog, not asserted in prose — closing F-SEAM and contracts OQ-C6.

**Definition of Done:** one named test `TestClerkCEGoldenFixtureRoundTrip` — contract layer: each of the five `clerk.*` golden fixtures decodes to the contracts-generated type and re-encodes byte-stable

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the five FR-W1 event types, When defined in contracts, Then all five land as `clerk.*` catalog entries — *catalog contains exactly {organization.created, organizationMembership.created, organization.deleted, organizationMembership.deleted, user.deleted}* [Source: FR-W16 / FR-W1 (OQ-C6)]
- [ ] **AC2** — Given a committed golden fixture per type, When round-tripped, Then decode→encode is byte-stable against the contracts type — *`bytes.Equal(fixture, reencode(decode(fixture)))` for each* [Source: F-SEAM / A-W3]
- [ ] **AC3** — Given `internal/events` local constants, When migrated, Then they import the contracts dep (negative: no local drift) — *no locally-defined `clerk.*` constant survives outside the contracts import* [Source: F-SEAM]

## 🔨 Tasks

- [ ] RED: TestClerkCEGoldenFixtureRoundTrip over committed fixtures
- [ ] Author the five clerk.* CE types + ProvisionTarget/DeprovisionTarget schemas in contracts (AC1)
- [ ] Commit golden fixtures + round-trip test (AC2)
- [ ] Replace `internal/events` local constants with the contracts import (AC3)
- [ ] GREEN: round-trip byte-stable

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — clerk.* CE type constants live as local Go consts in internal/events/events.go — there is no orvex-studio-contracts catalog, no payload JSON schemas, and no golden-fixture round-trip test. Contract seam not landed.

**Tier:** event/contract tier (CS §7). **Seams:** orvex-studio-contracts (remote-but-owned → golden fixtures). **Sibling deps:** contracts repo (ENG-2107 pack tag); consumed by E4-S3 (ID-only payload), E4-S4 (ce-id), all lifecycle stories. **Cross-service share:** only THIS service's `clerk.*` publisher entries.

## 🧪 Testing

- [ ] Named DoD test `TestClerkCEGoldenFixtureRoundTrip` (contract)
- [ ] Golden fixtures committed in-repo; CS §5 remote-but-owned — never mock the contracts package
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-contracts (OQ-C6)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E2 — [EPIC] workflows: Contract seam & shared-lib client integration

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
