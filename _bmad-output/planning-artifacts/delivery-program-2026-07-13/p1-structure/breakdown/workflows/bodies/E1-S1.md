## 🎯 Story

**As a** workflows platform engineer
**I want** the Go module path, on-disk dir, ArgoCD repoURL, kpack git URLs, Harbor image paths, and OpenBao secret paths moved off the legacy `orvex-workflows` name onto `orvex-studio-workflows`
**so that** new-domain deploys are never built on stale names masked only by GitHub redirects.

**Definition of Done:** one named test `TestRenameNoLegacyReferences` — repo-lint layer: greps the built tree + rendered kustomize and asserts zero `orvex-workflows` (module/image/secret-path) references survive except sanctioned redirect notes

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the repo, When `go build ./...` runs, Then the module path is `github.com/orvexai/orvex-studio-workflows/backend` — *`go list -m` == the new path; no import cites the old path* [Source: OQ-W1 / PRD §7]
- [ ] **AC2** — Given rendered kustomize, When manifests are built, Then no Harbor/image or ArgoCD repoURL names `orvex-workflows` — *`kustomize build` output grep `orvex-workflows` count == 0* [Source: Arch §4 rename migration]
- [ ] **AC3** — Given ExternalSecrets, When reconciled, Then OpenBao paths read `orvex-studio-workflows/*` — *each ExternalSecret `remoteRef.key` prefix == new path* [Source: OQ-W1]
- [ ] **AC4** — Given a dangling legacy `kpack-image-dispatcher.yaml` pointing at a nonexistent repo, When hygiene runs, Then it is deleted (negative) — *file absent; `kustomization.yaml` has no reference to it* [Source: Arch §4]

## 🔨 Tasks

- [ ] RED: TestRenameNoLegacyReferences asserting zero legacy refs (fails on current tree)
- [ ] Rename Go module path + on-disk dir; fix all imports (AC1)
- [ ] Update ArgoCD Application repoURL, kpack Image git URLs, Harbor project + image paths (AC2)
- [ ] Migrate OpenBao secret paths + ExternalSecret remoteRefs (AC3)
- [ ] Delete vestigial legacy kpack trio + stale `Dockerfile.api` (AC4)
- [ ] GREEN: lint passes; smoke-deploy on dev cell verifies image pulls + secrets resolve

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — Module is still `github.com/orvexai/orvex-workflows/backend`, README still titled `orvex-workflows`, k8s namespace still `studio-clerk-webhook-dev` — no repo/module/image/OpenBao-path rename to orvex-studio-workflows on main. The rename lands on branch origin/dev (commit 1cc8b7b `redirect repo self-references orvex-workflows → orvex-studio-workflows (OQ-W1/W2)`).

**Tier:** infra/build tier only — no workflow-logic change (so no replay concern here, but see E1-S2 for in-flight sleeps). **Seams crossed:** ArgoCD, kpack, Harbor, OpenBao. **Sibling deps:** must precede E7 (knowledge-rebuild) per rollout step 1 — 'do not land it on the old names'. Coordinate with `my-idp-apps` registration.

## 🧪 Testing

- [ ] Named DoD test `TestRenameNoLegacyReferences` (repo-lint)
- [ ] Deploy a dev-cell smoke after rename; confirm the live `reconcile` Schedule + in-flight workflows survive (no namespace change in this story — that is E1-S2)
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E1 — [EPIC] workflows: Rename, namespace & CI/replay foundation
- [ ] Blocks: E1-S2 (namespace), E7-* (knowledge-rebuild first extension)

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
