## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** workflows platform engineer
**I want** all task-queue / schedule / workflow-ID strings held as constants in `internal/temporal` (never literals) and `schedule-init` generalized to idempotently create each domain's Schedule
**so that** adding a domain is 'workflow code + a worker + Triggers/Schedules', never a new infra pattern (G4).

**Definition of Done:** one named test `TestScheduleInitIdempotent` — integration layer against a real Temporal server: running `schedule-init` twice creates each Schedule exactly once and re-runs are no-ops

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `internal/temporal`, When code is scanned, Then no queue/schedule/workflow-ID literal appears outside the constants file — *lint: string literals for those names == 0 outside `internal/temporal`* [Source: FR-W14]
- [ ] **AC2** — Given `schedule-init` run twice, When the second run executes, Then no duplicate Schedule is created — *Schedule count per domain == 1 after N runs* [Source: FR-W14]
- [ ] **AC3** — Given a new domain's schedule entry, When schedule-init runs, Then it is created idempotently alongside existing ones (negative: no clobber of the reconcile Schedule) — *reconcile Schedule untouched; new Schedule present* [Source: FR-W14 / A-W9]

## 🔨 Tasks

- [ ] RED: TestScheduleInitIdempotent double-running schedule-init
- [ ] Centralize all queue/schedule/workflow-ID strings as constants (AC1)
- [ ] Make schedule-init upsert-idempotent per domain (AC2, AC3)
- [ ] GREEN: double-run yields one Schedule each

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** PRESENT — temporal/signals.go centralizes TaskQueue + both schedule-ID constants (no literals). schedule-init generalizes ensureSchedule() to idempotently Create each domain's Schedule, distinguishing serviceerror.AlreadyExists via errors.As (not substring). Meaningful tests: TestEnsureSchedulesIdempotent, TestEnsureSchedulesFirstCreateSucceeds, TestEnsureSchedulesRejectsNonAlreadyExistsErrorMentioningAlready, TestEnsureSchedulesPropagatesOtherErrors.

**Tier:** driver (`cmd/schedule-init` Job) + constants module. **Seams:** Temporal Schedules. **Sibling deps:** the A-W9 checklist depends on this; downstream domains (E7 rebuild, E8) add Schedule entries here.

## 🧪 Testing

- [ ] Named DoD test `TestScheduleInitIdempotent` (integration)
- [ ] Real Temporal test env; assert no clobber of the live reconcile Schedule
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E1 — [EPIC] workflows: Rename, namespace & CI/replay foundation

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
