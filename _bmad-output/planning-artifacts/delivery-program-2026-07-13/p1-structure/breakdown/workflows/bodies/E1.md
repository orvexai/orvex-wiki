## Feature area

The load-bearing prerequisite wave. Before any new domain lands, the half-done `orvex-workflows`→`orvex-studio-workflows` rename (OQ-W1/WS-20), the domain-flavoured namespace consolidation (OQ-W2), and — most critically — the **NOT-YET-BUILT CI + WorkflowReplayer + `workflow.GetVersion` merge gate** (A-W5/F-CI) must exist. A durable-workflow service running 720-hour grace sleeps has *no automated guard today* against a deploy replaying a live history; §7 of the arch names this the single highest-leverage next step.

**Why it exists:** Arch §7 build-state honesty: 'there is no `.github/` and no Tekton pipeline… presenting [the replay gate] as a live merge gate would be fake-done (CS §11)'. Rollout step 1 mandates the rename 'before FR-W8 lands'. Everything else in this pack builds on these names + gates.

## Story roster

- [ ] E1-S1 — [workflows] Repo/module/image/OpenBao-path rename migration (OQ-W1/WS-20)
- [ ] E1-S2 — [workflows] Namespace + task-queue consolidation carrying in-flight grace sleeps (OQ-W2)
- [ ] E1-S3 — [workflows] CI bootstrap: go vet/build/test-race + CS lints + cell-lint required status (F-CI)
- [ ] E1-S4 — [workflows] WorkflowReplayer harness + recorded histories + workflow.GetVersion gate (A-W5/NFR-W1)
- [ ] E1-S5 — [workflows] GitOps hygiene: vestigial manifests, httproute, dead Temporal secret wiring, README (F-HTTPROUTE/F-SECRETS)
- [ ] E1-S6 — [workflows] Temporal constants + idempotent schedule-init generalization (FR-W14)

## FR/NFR ids this epic owns

OQ-W1, OQ-W2, NFR-W1, NFR-W3, FR-W14, F-CI, F-SECRETS, F-HTTPROUTE, A-W5

## Definition of Done

- [ ] All child stories Done (orchestrator-verified)
- [ ] The family six-surface E2E share for this area is **green on the dev cell, human-verified** (not reported) — the fake-done antibody
- [ ] SDD lines evidenced: named tests green, contract fixtures round-trip, SE-Arch lenses signed off

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E1 — [EPIC] workflows: Rename, namespace & CI/replay foundation

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
