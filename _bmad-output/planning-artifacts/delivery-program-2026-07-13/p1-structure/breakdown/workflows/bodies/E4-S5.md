## 🎯 Story

**As a** workflows platform engineer
**I want** a single strictly read-only, admin-authenticated Temporal workflow-view endpoint for orvex-studio-console — no write/signal/start surface, ever — importing `orvex-studio-lib/auth` (`RequireFresh` + declared admin scope, deny-by-default)
**so that** the console gets its Temporal window (resolves console OQ-CT5) without breaking the 'only Svix inbound' posture.

**Definition of Done:** one named test `TestConsoleViewEndpointReadOnlyAndAuthz` — handler layer: the endpoint returns workflow status read-only; any write/signal/start verb is absent; an unscoped/stale token is denied

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an admin-scoped fresh token, When the view endpoint is called, Then it returns workflow status read-only — *200 with read-only view; RequireFresh + admin scope enforced* [Source: NFR-W6 amendment]
- [ ] **AC2** — Given the endpoint surface, When enumerated, Then no write/signal/start verb exists (negative) — *only GET/read verbs; no mutation route* [Source: NFR-W6 ('no write/signal/start surface, ever')]
- [ ] **AC3** — Given a stale or unscoped token, When it calls the endpoint, Then it is denied deny-by-default — *401/403 on missing freshness or admin scope* [Source: NFR-W6 / lib FR-L5/L6]

## 🔨 Tasks

- [ ] RED: TestConsoleViewEndpointReadOnlyAndAuthz
- [ ] Add the read-only view handler importing lib/auth RequireFresh + admin scope (AC1)
- [ ] Assert no mutation verb exists (AC2)
- [ ] Enforce deny-by-default on stale/unscoped tokens (AC3)
- [ ] GREEN: read-only + authz verified

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — No read-only admin Temporal-view endpoint exists on main. The /v1/workflows authorizing proxy server lands on origin/dev (commit 4da078d, ENG-2009).

**Tier:** handler (thin) — the ONE additional inbound surface sanctioned by the NFR-W6 amendment. **Seams:** orvex-studio-lib/auth (real, not mocked — CS §5 own-family); console. **Sibling deps:** build only when console ships; depends on lib/auth being real (M7).

## 🧪 Testing

- [ ] Named DoD test `TestConsoleViewEndpointReadOnlyAndAuthz` (integration)
- [ ] Real lib/auth verifier; deny-by-default negative tests; build gated on console readiness
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-lib/auth (FR-L5/L6)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E4 — [EPIC] workflows: Observability, cell contract & residency posture

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
