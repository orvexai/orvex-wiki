## Feature area

The identity-alignment wave. When orvex-studio-identity's lifecycle mirror ships, `STUDIO_API_URL` re-points at it, the `{scope,id,mode}` calls adopt workload-identity (mTLS/SPIFFE + NetworkPolicy) instead of the static bearer, the shipped '404=idempotent success' carve-out dies (identity serves constant-200 with no existence oracle), Keycloak-backed tenants get provision/deprovision/purge through the SAME workflows via identity-emitted lifecycle CloudEvents, and provision/deprovision activity targets become cell-routed via the identity tenant→cell registry.

**Why it exists:** PRD FR-W7 + FR-W17 + Arch A-W7/§4/§6 (F-CELL c). Without FR-W17 a Keycloak tenant could never be deprovisioned (D-WF-1 forbids identity orchestrating it); F-IDP may additionally move Clerk ingestion here.

## Story roster

- [ ] E6-S1 — [workflows] Studio-side cutover: re-point at identity mirror + workload identity + retire 404 carve-out (FR-W7)
- [ ] E6-S2 — [workflows] Keycloak-tenant lifecycle via identity-emitted CloudEvents on the spine (FR-W17)
- [ ] E6-S3 — [workflows] Cell-routed activity targets via the identity tenant→cell registry (F-CELL c)

## FR/NFR ids this epic owns

FR-W7, FR-W17, F-CELL, F-IDP

## Definition of Done

- [ ] All child stories Done (orchestrator-verified)
- [ ] The family six-surface E2E share for this area is **green on the dev cell, human-verified** (not reported) — the fake-done antibody
- [ ] SDD lines evidenced: named tests green, contract fixtures round-trip, SE-Arch lenses signed off

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E6 — [EPIC] workflows: IdP-agnostic lifecycle & identity cutover

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
