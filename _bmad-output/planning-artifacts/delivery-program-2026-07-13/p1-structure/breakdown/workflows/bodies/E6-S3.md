## 🎯 Story

**As a** workflows platform engineer
**I want** provision/deprovision activities resolving the target cell's wiki-api / studio-api / billing origin through the identity tenant→cell registry instead of the single static `WIKI_API_URL`/`STUDIO_API_URL`
**so that** once cell #2 exists, lifecycle flows address the right cell — the single-static-origin assumption is removed.

**Definition of Done:** one named test `TestActivityTargetsCellRouted` — integration layer: an activity for a tenant resolves its wiki/studio/billing origin from the tenant→cell registry, not a static env origin

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tenant in cell B, When a provision activity runs, Then it targets cell B's wiki/studio/billing origin from the registry — *origin resolved via registry == cell B; not the static WIKI_API_URL* [Source: F-CELL(c)]
- [ ] **AC2** — Given a static single origin in code, When scanned, Then no lifecycle activity hard-codes it (negative) — *no static WIKI_API_URL/STUDIO_API_URL used for routing* [Source: F-CELL(c)]
- [ ] **AC3** — Given an unknown tenant→cell mapping, When resolution fails, Then the activity fails closed (does not target a default cell) — *unresolved mapping → error, no default-cell fallback* [Source: F-CELL(c) / no-fallbacks]

## 🔨 Tasks

- [ ] RED: TestActivityTargetsCellRouted
- [ ] Resolve activity origins via the identity tenant→cell registry (AC1)
- [ ] Remove static single-origin routing (AC2)
- [ ] Fail closed on unresolved mapping — no default-cell fallback (AC3)
- [ ] GREEN: cell-routed

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — CallIdentityRehome talks to identity's registry for tenant-move only; there is no cell-routed activity-target resolution via a tenant→cell registry for general activities. Absent.

**Tier:** activity (driver). **Seams:** identity tenant→cell registry (the cell-routing model is an OPEN DECISION §8 — TBD pinned by ENG-2107). **Sibling deps:** depends on E4-S2 (cell config) + identity registry; the last F-CELL piece (a/b in E4-S2).

## 🧪 Testing

- [ ] Named DoD test `TestActivityTargetsCellRouted` (integration)
- [ ] Integration with a two-cell registry fixture; assert no default-cell fallback (no-fallbacks doctrine)
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E6 — [EPIC] workflows: IdP-agnostic lifecycle & identity cutover
- [ ] Blocked by: E4-S2, E6-S1

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
