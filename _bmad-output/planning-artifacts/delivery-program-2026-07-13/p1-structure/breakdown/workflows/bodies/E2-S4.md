## 🎯 Story

**As a** workflows platform engineer
**I want** the rebuild-domain activity clients generated from lib FR-L17 codegen and the billing entitlement client riding lib's fail-closed `pkg/billingclient`, replacing hand-rolled types
**so that** this service and its callees pin the same step-API shapes from one source — no drift between caller and server.

**Definition of Done:** one named test `TestActivityClientsFromLibCodegen` — integration layer: the rebuild + billing activity clients are the lib-generated types (not local structs) and round-trip against the contracts golden fixtures

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the rebuild activities, When built, Then their clients come from lib FR-L17 codegen — *no hand-rolled rebuild step-API struct in `internal/activities/knowledge`* [Source: deps table / lib OQ-L5]
- [ ] **AC2** — Given the billing entitlement call, When built, Then it uses lib `pkg/billingclient` fail-closed typing — *billing client type == lib's; fail-closed default on decode error* [Source: FR-W18 / deps table]
- [ ] **AC3** — Given the shipped hand-rolled `studio_activities.go`, When reviewed, Then it stays for the live clerk domain until the FR-W7 cutover (negative: not prematurely migrated) — *studio_activities.go retained; migration deferred to E6-S1* [Source: deps table]

## 🔨 Tasks

- [ ] RED: TestActivityClientsFromLibCodegen
- [ ] Wire lib FR-L17 codegen for rebuild clients (AC1)
- [ ] Adopt lib pkg/billingclient fail-closed for the entitlement call (AC2)
- [ ] Leave studio_activities.go for the clerk domain pending E6 cutover (AC3)
- [ ] GREEN: clients round-trip against golden fixtures

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — All activity clients are hand-rolled net/http (CallWikiProvision, CallWikiDeprovision, CallStudioProvision, CallIdentityRehome…). No lib-codegen clients. The step-API client scaffolding lands on origin/dev (commit efe0010).

**Tier:** driver (activity clients). **Seams:** orvex-studio-lib (own-family shared lib — CS §5: do NOT mock, use the real generated types). **Sibling deps:** lib FR-L17/L18 (ENG-2107 pack); knowledge rebuild (E7), billing entitlement (E5-S4).

## 🧪 Testing

- [ ] Named DoD test `TestActivityClientsFromLibCodegen` (integration)
- [ ] Use the real lib-generated clients; golden-fixture fakes stand in for the remote servers only
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-lib (OQ-L5)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E2 — [EPIC] workflows: Contract seam & shared-lib client integration
- [ ] Blocked by: E2-S2 (step-API OpenAPI)

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
