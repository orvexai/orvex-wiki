## 🎯 Story

**As a** workflows platform engineer
**I want** `ProvisionTarget.OrgName` (a tenant display name) dropped from both the CloudEvent `data` and the `ProvisionWorkflow` input — the wiki-provision step resolves the display name from the org id — with an ID-only CI gate
**so that** Temporal payloads carry references not tenant bytes, keeping a single global Temporal residency-legal (Principle 6 / cell §4).

**Definition of Done:** one named test `TestPayloadsAreIdOnly` — contract + CI-gate layer: no Temporal payload or seeding CE carries tenant display bytes; OrgName is resolved downstream from the id

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given ProvisionTarget, When serialized, Then it carries no `OrgName` display bytes — *payload schema has no display-name field; only ids* [Source: F-PAYLOAD / cell §4]
- [ ] **AC2** — Given the wiki-provision step, When it needs the display name, Then it resolves it from the org id — *display name fetched via the sanctioned path, not carried* [Source: F-PAYLOAD]
- [ ] **AC3** — Given a PR reintroducing a display-name payload field, When CI runs, Then the ID-only gate fails (negative) — *ID-only CI lint exit != 0 on the offending field* [Source: F-PAYLOAD / Arch §7]

## 🔨 Tasks

- [ ] RED: TestPayloadsAreIdOnly
- [ ] Drop OrgName from the CE data + workflow input (AC1)
- [ ] Resolve display name from org id in the wiki-provision step (AC2)
- [ ] Add the ID-only CI gate (AC3)
- [ ] Land behind workflow.GetVersion + replay history (E1-S4)
- [ ] GREEN: ID-only enforced

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — OrgName is still a field on ProvisionTarget and is forwarded into the wiki provision request body (wikiProvisionRequest.OrgName). Not dropped — ID-only payload not enforced. Absent.

**Tier:** contract + workflow input. **Seams:** CE catalog (E2-S1), cell-lint. **Sibling deps:** MUST land behind GetVersion + replay (E1-S4); ties to E4-S2 envelope helper. **Note:** alternative is an explicit ADR exception for the single low-sensitivity field — default is to drop it.

## 🧪 Testing

- [ ] Named DoD test `TestPayloadsAreIdOnly` (contract)
- [ ] ID-only CI gate lands with the E1-S3 CI bootstrap; replay-safety via E1-S4
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E4 — [EPIC] workflows: Observability, cell contract & residency posture
- [ ] Blocked by: E1-S4, E2-S1

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
