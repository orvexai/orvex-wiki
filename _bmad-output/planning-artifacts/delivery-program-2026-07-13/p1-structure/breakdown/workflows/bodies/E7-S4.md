## 🎯 Story

**As a** workflows platform engineer
**I want** the rebuild-domain worker's fixed replica count derived from a measured SHADOW rebuild against knowledge's RTO (tenant <15min, cell <24h), with `MaxConcurrentActivityExecutionSize` bounds and a manual scale-up DR runbook — NO KEDA
**so that** the rebuild RTO is met by measured fixed-replica sizing (not KEDA, not assumed), and the four stale KEDA sibling texts get reconciled to NO-KEDA.

**Definition of Done:** one named test `TestRebuildWorkerMeetsRtoBudget` — load/measurement layer: a shadow rebuild at the sized replica count completes within the RTO budget; scaling is fixed-replica Deployment, not KEDA

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the sized worker, When a shadow tenant rebuild runs, Then it completes < 15 min — *measured tenant rebuild p95 < 15 min at the fixed replica count* [Source: NFR-W4 / OQ-W7]
- [ ] **AC2** — Given a cell shadow rebuild, When run, Then it completes < 24 h at the sized replicas — *measured cell rebuild < 24 h* [Source: NFR-W4 / FR-W9]
- [ ] **AC3** — Given the worker Deployment, When inspected, Then it is fixed-replica (no KEDA) (negative) — *no KEDA ScaledObject; plain Deployment + MaxConcurrentActivityExecutionSize bound* [Source: NFR-W4 / A-W6]
- [ ] **AC4** — Given a DR need, When operators must scale, Then a manual scale-up runbook exists — *runbook documents the manual replica bump* [Source: OQ-W7]

## 🔨 Tasks

- [ ] RED: TestRebuildWorkerMeetsRtoBudget as a measured shadow run
- [ ] Run a shadow tenant + cell rebuild; measure throughput (AC1, AC2)
- [ ] Set fixed replica count + MaxConcurrentActivityExecutionSize; NO KEDA (AC3)
- [ ] Author the manual scale-up DR runbook (AC4)
- [ ] Reconcile the four stale KEDA texts to NO-KEDA
- [ ] GREEN: RTO met, fixed-replica

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — No worker fixed-replica-vs-RTO shadow-measurement artifacts and no scale-up runbook exist. Absent.

**Tier:** deploy sizing + ops runbook. **Seams:** knowledge RTO (NFR-K4). **Sibling deps:** sizing input is E7-S2 (RebuildCell throughput); reconciles knowledge arch A8 / PRD FR-K24 / deps row / split page KEDA lines.

## 🧪 Testing

- [ ] Named DoD test `TestRebuildWorkerMeetsRtoBudget` (load-measurement)
- [ ] Measured shadow rebuild on the dev cell; assert fixed-replica + RTO; no KEDA ScaledObject
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-knowledge (NFR-K4, FR-K24)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E7 — [EPIC] workflows: Knowledge index rebuild/backfill domain
- [ ] Blocked by: E7-S2

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
