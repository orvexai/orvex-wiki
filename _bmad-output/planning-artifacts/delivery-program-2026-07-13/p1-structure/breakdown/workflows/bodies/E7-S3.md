## 🎯 Story

**As a** workflows platform engineer
**I want** rebuild enqueues (knowledge admin reindex FR-K25 + the DR runbook) entering as CloudEvents on the spine, consumed by a Trigger + the dispatcher's type→workflow registry — no inbound start-workflow HTTP API
**so that** the enqueue auth burden stays on the publishing satellite's surface and this service's only authenticated HTTP ingress remains the Svix webhook (A-W10).

**Definition of Done:** one named test `TestRebuildEnqueueViaSpineNoHttpApi` — integration layer: a rebuild CE on the spine starts the workflow via the dispatcher registry; there is no start-workflow HTTP endpoint on this service

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a rebuild CE on the spine, When a Trigger routes it, Then the dispatcher starts RebuildTenant/RebuildCell via its registry — *CE → dispatcher registry → ExecuteWorkflow; deterministic ID* [Source: FR-W10 / A-W10]
- [ ] **AC2** — Given this service, When its HTTP surface is enumerated, Then there is no start-workflow API (negative) — *only the Svix webhook (+ the read-only console view) inbound; no start API* [Source: A-W10 / NFR-W6]
- [ ] **AC3** — Given a duplicate enqueue, When received, Then it dedups on the workflow ID — *duplicate CE → AlreadyStarted ACK; one rebuild* [Source: FR-W10 / NFR-W2]

## 🔨 Tasks

- [ ] RED: TestRebuildEnqueueViaSpineNoHttpApi
- [ ] Add the rebuild CE type + Trigger + dispatcher registry entry (AC1)
- [ ] Assert no start-workflow HTTP API is added (AC2)
- [ ] Dedup on workflow ID (AC3)
- [ ] GREEN: CE-on-spine enqueue works

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — No CE-on-spine rebuild-enqueue Trigger and no dispatcher registry entry for a rebuild event type — dispatcher.handle switches only on clerk.* types. Absent.

**Tier:** ingest (spine Trigger) + dispatch (registry). **Seams:** spine broker; knowledge publishing surface owns the admin-scope authz (deny-by-default RequireFresh). **Sibling deps:** dispatcher registry generalizes the shipped switch (A-W2); depends on E3-S2 (already-started ACK).

## 🧪 Testing

- [ ] Named DoD test `TestRebuildEnqueueViaSpineNoHttpApi` (integration)
- [ ] Integration against a Kafka-backed broker; assert the no-HTTP-start-API posture holds
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E7 — [EPIC] workflows: Knowledge index rebuild/backfill domain
- [ ] Blocked by: E3-S2, E7-S1

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
