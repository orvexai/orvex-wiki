## 🎯 Story

**As a** workflows platform engineer
**I want** the k8s + Temporal logical namespace moved from the domain-flavoured `studio-clerk-webhook-dev` to a per-app name with a task queue per domain, without losing in-flight 30-day grace sleeps or the hourly reconcile Schedule
**so that** multi-domain naming is correct and per-app canon holds before any second domain lands.

**Definition of Done:** one named test `TestNamespaceMigrationPreservesInFlight` — integration layer against a real Temporal test server: seeds an in-flight grace-sleep workflow + the reconcile Schedule, runs the migration, asserts both resume/fire on the new namespace

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the per-app namespace `orvex-studio-workflows(-dev)`, When namespace-init Job runs, Then the namespace registers on the shared cluster — *`tctl namespace describe` returns the per-app name; queue-per-domain registered* [Source: NFR-W3 / OQ-W2]
- [ ] **AC2** — Given an in-flight 720h grace DeprovisionWorkflow, When migration completes, Then the sleep survives and the purge still fires at its original deadline — *seeded workflow's `WorkflowExecutionTimeout`/timer intact post-migration; purge activity invoked once* [Source: OQ-W2 / Arch §4]
- [ ] **AC3** — Given the hourly `reconcile` Schedule, When migrated, Then it continues firing on the new namespace — *Schedule present + `nextActionTimes` non-empty on new ns* [Source: OQ-W2]
- [ ] **AC4** — Given the per-org `TemporalControlPlaneClaim`, When reviewed, Then it stays disabled (negative — MUST NOT re-enable) — *no crossplane claim manifest is applied; namespace-init Job path is used* [Source: NFR-W3]

## 🔨 Tasks

- [ ] RED: TestNamespaceMigrationPreservesInFlight seeding grace-sleep + Schedule
- [ ] Add per-app namespace-init Job + queue-per-domain constants (AC1)
- [ ] Author the migration runbook for in-flight sleeps + Schedule carry-over (AC2, AC3)
- [ ] Assert the per-org claim stays disabled (AC4)
- [ ] GREEN: dev-cell dry-run migration; verify live workflows resume

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** PARTIAL — Task-queue is ALREADY a single shared constant `studio-clerk-webhook` (temporal.TaskQueue) used by worker/dispatcher/schedule-init, and the deprovision grace-sleep is carried. But there is no namespace consolidation into a workflows-wide namespace (still per-service); the OQ-W2 consolidation completes on origin/dev.

**Tier:** infra + Temporal topology. **Seams:** Temporal shared cluster (rule-14 sanctioned singleton). **Sibling deps:** depends on E1-S1 (rename) — sequence names before topology. Feeds A-W9 'add a domain' checklist. **Race note:** carry the FR-W3 grace sleeps determinism-safe.

## 🧪 Testing

- [ ] Named DoD test `TestNamespaceMigrationPreservesInFlight` (integration)
- [ ] Real Temporal test env (temporalite/dev-server) — do NOT mock the Temporal client for this tier
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E1 — [EPIC] workflows: Rename, namespace & CI/replay foundation
- [ ] Blocked by: E1-S1

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
