## Feature area

The F-SEAM fix. Today the CloudEvent type constants live locally in `internal/events` with **no `orvex-studio-contracts` dependency and no golden-fixture round-trip** — the contract seam is asserted in prose, not enforced. This epic lands the `clerk.*` CE catalog + payload schemas, the per-domain step-API OpenAPI (incl. the billing create-Free/revoke step-API), the retry-class annotations, versioned append-only workflow types, and swaps hand-rolled activity clients for lib FR-L17/L18 codegen.

**Why it exists:** Arch A-W3 tightening (F-SEAM) + PRD FR-W16: CS §7 event-tier + CS §5 remote-but-owned require the envelope validated against contracts and siblings faked from committed golden fixtures. Resolves contracts OQ-C6 (the five clerk.* types) and lib OQ-L5.

## Story roster

- [ ] E2-S1 — [workflows] Land clerk.* CE catalog + payload schemas in contracts with golden-fixture round-trip (F-SEAM)
- [ ] E2-S2 — [workflows] Per-domain step-API OpenAPI + retry-class annotations authored in contracts (A-W3)
- [ ] E2-S3 — [workflows] Versioned append-only workflow input/result types (FR-W16/NFR-W1)
- [ ] E2-S4 — [workflows] Lib-codegen activity clients replacing hand-rolled (rebuild + billing) (deps table/lib OQ-L5)

## FR/NFR ids this epic owns

FR-W16, NFR-W2, A-W3, F-SEAM

## Definition of Done

- [ ] All child stories Done (orchestrator-verified)
- [ ] The family six-surface E2E share for this area is **green on the dev cell, human-verified** (not reported) — the fake-done antibody
- [ ] SDD lines evidenced: named tests green, contract fixtures round-trip, SE-Arch lenses signed off

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E2 — [EPIC] workflows: Contract seam & shared-lib client integration

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
