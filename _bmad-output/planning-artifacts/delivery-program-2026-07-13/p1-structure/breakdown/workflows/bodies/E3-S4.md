## 🎯 Story

**As a** workflows platform engineer
**I want** the dispatcher routing malformed payloads to the DLQ (disposition `malformed`) and the ingress publishing Svix-verified-but-unsupported types to the DLQ (disposition `unsupported`), replacing the shipped ACK-and-drop
**so that** nothing verified is ACKed-and-dropped: Temporal history + the DLQ together are this service's record (satisfies identity A-DATA without a datastore).

**Definition of Done:** one named test `TestNoVerifiedEventIsDropped` — integration layer: a malformed payload and an unsupported-but-verified type each land in the DLQ with the correct disposition — neither is silently 200-and-dropped

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a malformed dispatched payload, When processed, Then it routes to the DLQ with disposition `malformed` — *DLQ record present, disposition==malformed (not ACK-and-drop)* [Source: FR-W6 / A-W1]
- [ ] **AC2** — Given a Svix-verified but unsupported event type, When received at ingress, Then it publishes to the DLQ disposition `unsupported` — *DLQ record present, disposition==unsupported (not silent 200)* [Source: FR-W6]
- [ ] **AC3** — Given any verified event, When processed, Then it either starts a workflow (history) or lands in the DLQ (negative: no ACKed-and-nowhere event) — *for a corpus of verified events, started+DLQ == total; dropped == 0* [Source: A-W8 / identity A-DATA]

## 🔨 Tasks

- [ ] RED: TestNoVerifiedEventIsDropped over malformed + unsupported fixtures
- [ ] Route malformed dispatch payloads → DLQ disposition=malformed (AC1)
- [ ] Route ingress unsupported-but-verified types → DLQ disposition=unsupported (AC2)
- [ ] Assert the started+DLQ==total invariant (AC3)
- [ ] GREEN: zero dropped

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — Malformed / unsupported dispositions are still ACK-and-drop (return nil with a log) — there is no DLQ-as-record. Absent.

**Tier:** ingest + dispatch drivers. **Seams:** DLQ topic (E3-S3). **Sibling deps:** depends on E3-S3 (DLQ exists); this is the A-W8 'DLQ = the record' obligation — reconciles identity A-DATA wording.

## 🧪 Testing

- [ ] Named DoD test `TestNoVerifiedEventIsDropped` (integration)
- [ ] Integration against the DLQ topic; assert the loss-boundedness invariant holds for a mixed corpus
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E3 — [EPIC] workflows: Retry taxonomy & delivery hardening (G2 loss-window fixes)
- [ ] Blocked by: E3-S3

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
