## 🎯 Story

**As a** workflows platform engineer
**I want** `ReconcileWorkflow` (from the hourly Schedule) extended from log-only to comparing live Clerk topology against wiki/studio state and starting bounded corrective Provision/Deprovision workflows for detected drift, dry-run mode first, every corrective start logged with evidence
**so that** corrective reconcile — the only recovery for events lost before the ingress ACK (past Clerk's retry horizon) — makes the G2 guarantee real and backstops the FR-W2 race.

**Definition of Done:** one named test `TestReconcileStartsBoundedCorrections` — workflow-test-env layer: seeded drift triggers a bounded set of corrective workflows in dry-run (logs only) then live (starts corrections), each logged with evidence

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given drift between Clerk and wiki/studio, When ReconcileWorkflow runs live, Then it starts corrective Provision/Deprovision workflows — *corrective workflow started per drift item with evidence log* [Source: FR-W4]
- [ ] **AC2** — Given dry-run mode, When reconcile runs, Then it logs intended corrections without starting them (negative) — *dry-run → zero corrective starts, full evidence log* [Source: FR-W4]
- [ ] **AC3** — Given many drift items, When reconcile runs, Then corrections are bounded per pass — *corrective starts <= per-pass bound* [Source: FR-W4]
- [ ] **AC4** — Given a resurrection from the FR-W2 race, When reconcile runs, Then it is corrected (backstop) — *reconcile deprovisions the re-activated principal* [Source: FR-W4 / §5]

## 🔨 Tasks

- [ ] RED: TestReconcileStartsBoundedCorrections
- [ ] Add live Clerk topology fetch (BEST-EFFORT) + compare to wiki/studio (AC1)
- [ ] Dry-run mode first, then bounded live corrections with evidence logs (AC2, AC3)
- [ ] Verify it backstops the FR-W2 race (AC4)
- [ ] Land behind GetVersion + replay history (E1-S4)
- [ ] GREEN: bounded corrections verified

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** PARTIAL — ReconcileWorkflow exists and is scheduled hourly, but it only fetches Clerk org/membership counts and logs them for drift observation — it performs NO corrective remediation, has no dry-run mode, and explicitly has 'no wiki tenant-list dependency'. The FR-W4 remediation behaviour is not implemented; only the observe backstop skeleton is. Partial.

**Tier:** workflow + activities (Clerk fetch BEST-EFFORT). **Seams:** Clerk Management API; engine/studio state read. **Sibling deps:** backstops E5-S2 (race); rides the hourly Schedule (E1-S6).

## 🧪 Testing

- [ ] Named DoD test `TestReconcileStartsBoundedCorrections` (workflow-test-env)
- [ ] Temporal testsuite; seed drift; assert bounded + dry-run-first
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E5 — [EPIC] workflows: Tenant-lifecycle domain — provision/deprovision/reconcile + polymorphic tenancy + entitlements
- [ ] Blocked by: E1-S4

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
