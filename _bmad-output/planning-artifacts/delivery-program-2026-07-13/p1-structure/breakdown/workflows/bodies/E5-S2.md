## 🎯 Story

**As a** workflows platform engineer
**I want** an AUTHORITATIVE org-liveness check against the Clerk Management API before any activation in `ProvisionWorkflow`, treating a deleted org as a non-retryable stop
**so that** the cross-type ordering race is closed — a late `organizationMembership.created` after `organization.deleted` cannot re-activate a deprovisioned principal.

**Definition of Done:** one named test `TestProvisionLivenessGuardStopsDeletedOrg` — workflow-test-env layer: a provision for a Clerk-deleted org halts with a non-retryable stop before any wiki/studio activation

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a deleted org, When ProvisionWorkflow runs, Then the liveness check stops it non-retryably before activation — *no wiki/studio activate activity invoked; workflow ends non-retryable* [Source: FR-W2 / A-W7]
- [ ] **AC2** — Given a live org, When ProvisionWorkflow runs, Then activation proceeds on wiki (authoritative) + studio (best-effort) in parallel — *wiki activate AUTHORITATIVE; studio BEST-EFFORT; both dispatched parallel* [Source: FR-W2]
- [ ] **AC3** — Given a late membership.created after org.deleted, When processed, Then the principal is NOT re-activated (negative — the race) — *liveness stop fires; no reactivation* [Source: FR-W2 / §5 ordering race]

## 🔨 Tasks

- [ ] RED: TestProvisionLivenessGuardStopsDeletedOrg
- [ ] Add the AUTHORITATIVE Clerk-Management liveness activity before activation (AC1)
- [ ] Non-retryable stop on deleted org (AC1, AC3)
- [ ] Keep wiki=authoritative / studio=best-effort parallel activation (AC2)
- [ ] Land behind workflow.GetVersion + replay history (E1-S4)
- [ ] GREEN: race closed

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — ProvisionWorkflow has no org-liveness guard — it never checks whether the Clerk org still exists and never emits a non-retryable stop on a deleted org. Absent.

**Tier:** workflow (pure decision) + activity (Clerk Mgmt liveness IO). **Seams:** Clerk Management API (F-IDP open-decision note — direct Clerk contact). **Sibling deps:** MUST land behind GetVersion (E1-S4); backstopped by FR-W4 reconcile (E5-S6). **Note:** F-IDP may later move Clerk contact to identity.

## 🧪 Testing

- [ ] Named DoD test `TestProvisionLivenessGuardStopsDeletedOrg` (workflow-test-env)
- [ ] Temporal testsuite; simulate the deleted-org + late-membership race; GetVersion-gated replay
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E5 — [EPIC] workflows: Tenant-lifecycle domain — provision/deprovision/reconcile + polymorphic tenancy + entitlements
- [ ] Blocked by: E1-S4, E3-S1

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
