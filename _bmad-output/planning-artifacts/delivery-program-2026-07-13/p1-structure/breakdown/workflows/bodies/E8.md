## Feature area

The two remaining planned domains. The **personal→Teams upgrade pass** (D-S17 named workstream): mint the Clerk org at upgrade, re-key the user-keyed tenant to the org in place (`user:<id>`→`org:<id>`), carry data + entitlements, start seat counting — orchestrated across identity + engine + billing, durable, resumable, and **compensating** (rolls back to the pre-upgrade user-keyed tenant on any authoritative-step failure). And the **AI tool-loop** (charter-level): loop state in the workflow, every model call + tool exec as one activity so a crash resumes, hard caps ride the input, `ContinueAsNew` bounds history — blocked on the ai PRD's step-API shape (OQ-W4).

**Why it exists:** PRD F3 (FR-W11) + F4 (FR-W13) + Arch domain map rows 3–4 + A-W10. Org-conversion follows identity FR-I8; AI stays charter-level until its PRD (OQ-W4). NFR-W8: idle domains cost ~one idle pod.

## Story roster

- [ ] E8-S1 — [workflows] OrgUpgrade compensating workflow: mint org, re-key tenant in place, carry data+entitlements (FR-W13)
- [ ] E8-S2 — [workflows] AI tool-loop durable workflow: loop-state, activity-per-call, caps, ContinueAsNew (FR-W11)

## FR/NFR ids this epic owns

FR-W13, FR-W11, NFR-W8, A-W10

## Definition of Done

- [ ] All child stories Done (orchestrator-verified)
- [ ] The family six-surface E2E share for this area is **green on the dev cell, human-verified** (not reported) — the fake-done antibody
- [ ] SDD lines evidenced: named tests green, contract fixtures round-trip, SE-Arch lenses signed off

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E8 — [EPIC] workflows: Org-conversion & AI tool-loop domains

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
