## 🎯 Story

**As a** workflows platform engineer
**I want** `STUDIO_API_URL` re-pointed from the Studio app to identity's lifecycle mirror, the `{scope,id,mode}` calls adopting workload identity (mTLS/SPIFFE + NetworkPolicy) instead of the static `STUDIO_API_TOKEN`, and the '404=idempotent success' carve-out retired (identity serves constant-200, no existence oracle)
**so that** the studio leg runs on identity's auth posture and conformance tests stop encoding the 404 convention past the Studio-app era.

**Definition of Done:** one named test `TestStudioCutoverAuthAndNo404Oracle` — integration layer: the studio activity uses workload identity (no static bearer) and treats identity's constant-200 correctly — no 404 existence-oracle path survives

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given identity's mirror, When the studio leg calls it, Then it uses workload identity (mTLS/SPIFFE), not the static bearer — *no STUDIO_API_TOKEN on the call; mTLS/SPIFFE identity used* [Source: FR-W7 / identity FR-I14]
- [ ] **AC2** — Given identity's constant-200 responses, When the studio leg processes them, Then no 404 existence-oracle branch exists (negative) — *conformance test has no 404=success path against identity* [Source: FR-W7]
- [ ] **AC3** — Given the migrated studio client, When the FR-W7 cutover completes, Then `studio_activities.go` types move to lib's identity client — *studio client == lib identity codegen (E2-S4 deferred migration)* [Source: FR-W7 / deps table]

## 🔨 Tasks

- [ ] RED: TestStudioCutoverAuthAndNo404Oracle
- [ ] Re-point STUDIO_API_URL at identity's mirror (AC1)
- [ ] Adopt workload identity (mTLS/SPIFFE) + NetworkPolicy; drop the static bearer (AC1)
- [ ] Retire the 404 carve-out; align to constant-200 (AC2)
- [ ] Migrate studio_activities.go to lib identity client (AC3)
- [ ] Land behind GetVersion + replay history (E1-S4)
- [ ] GREEN: cutover verified

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — CallWikiProvision still targets the wiki engine directly (a.WikiURL + /api/clerk/provision) — no re-point at an identity mirror, no workload-identity, no 404-carve-out retirement. The repoint at identity's FR-I14 callee lands on origin/dev (commit 7cdfbee).

**Tier:** activity (studio client) + deploy. **Seams:** orvex-studio-identity mirror (FR-I14); workload identity. **Sibling deps:** gated on identity's mirror shipping; completes the E2-S4 deferred client migration; may promote studio leg to authoritative (decision recorded with identity, not here).

## 🧪 Testing

- [ ] Named DoD test `TestStudioCutoverAuthAndNo404Oracle` (integration)
- [ ] Integration against identity's mirror (faked from golden fixtures until it ships); GetVersion-gated replay
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-identity (FR-I14)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E6 — [EPIC] workflows: IdP-agnostic lifecycle & identity cutover
- [ ] Blocked by: E1-S4, E2-S4

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
