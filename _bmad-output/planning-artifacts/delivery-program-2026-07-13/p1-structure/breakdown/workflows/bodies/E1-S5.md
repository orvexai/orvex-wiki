## 🎯 Story

**As a** workflows platform engineer
**I want** the dangling `httproute-ingress.yaml` reference resolved, the dead `-temporal-conn` SecretStore/Role and unused `CLERK_JWKS_URL` removed, and the stale README refreshed
**so that** the GitOps source of truth matches reality and no misleading dead wiring survives.

**Definition of Done:** one named test `TestManifestsSelfConsistent` — manifest-lint layer: asserts every referenced file exists in `kustomization.yaml`, no ExternalSecret reads a nonexistent Crossplane secret, and no env var is materialized without a `config.go` field

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `knative-event-ingress.yaml` claims reachability via an httproute, When linted, Then the HTTPRoute is either in kustomize or the misleading comment is deleted with reachability confirmed — *no reference to a file absent from `kustomization.yaml`* [Source: F-HTTPROUTE / Arch §4]
- [ ] **AC2** — Given `external-secrets.yaml` builds a SecretStore for the disabled claim's `-temporal-conn`, When cleaned, Then that dead wiring is removed (negative) — *no ExternalSecret targets `-temporal-conn`; `-temporal-credentials` remains the source* [Source: F-SECRETS]
- [ ] **AC3** — Given `CLERK_JWKS_URL` is materialized with no config field, When cleaned, Then it is removed — *env var absent from manifests; no orphan config keys* [Source: F-SECRETS]
- [ ] **AC4** — Given the README naming pre-rename `clerk-deprovision` namespace/binaries + the disabled claim, When refreshed, Then it describes current state — *README grep `clerk-deprovision`/`per-org claim` count == 0* [Source: Arch §4]

## 🔨 Tasks

- [ ] RED: TestManifestsSelfConsistent over the current tree
- [ ] Resolve httproute: add to kustomize OR delete comment + confirm reachability (AC1)
- [ ] Remove dead `-temporal-conn` SecretStore/Role + `CLERK_JWKS_URL` (AC2, AC3)
- [ ] Refresh README off pre-rename names (AC4)
- [ ] GREEN: manifest-lint clean

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — GitOps hygiene not done on main: knative-event-ingress.yaml references an httproute-ingress.yaml that is NOT present in the tree (vestigial reference), per-image kpack manifests (kpack-image-*.yaml) still duplicated, README still clerk-deprovision. The cleanup (kpack consolidation, external-secrets rework) lands on origin/dev.

**Tier:** infra/GitOps hygiene. **Seams:** Knative HTTPRoute, ExternalSecrets/OpenBao. **Sibling deps:** pairs with E1-S1 rename; feeds the F-HOST resolution tracked in E6/E4.

## 🧪 Testing

- [ ] Named DoD test `TestManifestsSelfConsistent` (manifest-lint)
- [ ] dev-cell apply confirms the webhook is genuinely reachable via the declared route
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E1 — [EPIC] workflows: Rename, namespace & CI/replay foundation

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
