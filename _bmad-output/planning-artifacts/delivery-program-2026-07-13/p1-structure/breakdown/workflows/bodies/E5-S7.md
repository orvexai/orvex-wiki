## 🎯 Story

**As a** workflows platform engineer
**I want** the purge leg of `DeprovisionWorkflow` gaining a cancel signal (org restored / operator abort) and gating the irreversible `delete` step on an explicit confirmation step rather than timer-expiry alone (aligned with identity FR-I14's two-phase contract)
**so that** an org restored or an operator abort during the grace window stops the irreversible purge, and delete never fires on timer-expiry alone.

**Definition of Done:** one named test `TestGraceCancelSignalHaltsPurge` — workflow-test-env layer: a cancel signal during the grace sleep halts the purge before delete; without confirmation the delete step does not execute on expiry

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a grace sleep in progress, When a cancel signal arrives, Then the purge halts before hard delete — *signal received → workflow exits grace without delete* [Source: FR-W5]
- [ ] **AC2** — Given the grace timer expires, When no confirmation is present, Then delete does not fire on expiry alone (negative) — *delete gated on explicit confirmation step, not timer-only* [Source: FR-W5 / identity FR-I14]
- [ ] **AC3** — Given a confirmed purge after expiry, When confirmation is present, Then the delete proceeds — *confirmation → delete executes* [Source: FR-W5]

## 🔨 Tasks

- [ ] RED: TestGraceCancelSignalHaltsPurge
- [ ] Add the cancel signal handler to the purge leg (AC1)
- [ ] Gate delete on an explicit confirmation step (AC2, AC3)
- [ ] Land behind GetVersion + replay a grace-sleep history (E1-S4)
- [ ] GREEN: cancel + confirmation gate verified

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — The grace window is a plain workflow.Sleep with no cancel-signal handler (temporal/signals.go holds only string constants; no GetSignalChannel / Selector anywhere) and no purge-confirmation gate. Absent.

**Tier:** workflow (signal handling — replay-sensitive). **Seams:** signaller (who may signal + Clerk-restore detection is OQ-W8 — TBD pinned by ENG-2107). **Sibling deps:** extends E5-S5; MUST land behind GetVersion + a recorded grace-sleep replay history.

## 🧪 Testing

- [ ] Named DoD test `TestGraceCancelSignalHaltsPurge` (workflow-test-env)
- [ ] Temporal testsuite; signal during time-skipped grace; grace-sleep replay history recorded
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E5 — [EPIC] workflows: Tenant-lifecycle domain — provision/deprovision/reconcile + polymorphic tenancy + entitlements
- [ ] Blocked by: E1-S4, E5-S5

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
