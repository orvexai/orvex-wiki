## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

**As a** workflows platform engineer
**I want** the shipped `POST /webhooks/clerk` pipeline verified end-to-end: Svix verify against `CLERK_WEBHOOK_SECRET`, 1 MB body cap, 401 bad-signature, 502 publish-failure (Clerk retries), 200 unsupported, five event types mapped to `clerk.*` CEs and routed per-type to the dispatcher's deterministic workflow ID
**so that** the live domain's contract semantics are pinned by tests before any extension lands on top.

**Definition of Done:** one named test `TestClerkWebhookPipelineSemantics` — integration layer: drives the ingress with signed/unsigned/oversized/unsupported payloads and asserts the exact 401/502/200 + publish behaviour and deterministic workflow-ID mapping

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Svix-signed supported event, When posted, Then it maps to the right `clerk.*` CE and starts the matching workflow with the deterministic ID — *workflow ID == `provision-org-<org>` / `deprov-<type>-<org>` scheme* [Source: FR-W1]
- [ ] **AC2** — Given a bad signature, When posted, Then ingress returns 401 (negative) — *401; no CE published* [Source: FR-W1 / NFR-W6]
- [ ] **AC3** — Given a publish failure, When posting a valid event, Then ingress returns 502 so Clerk retries — *502 on broker publish error* [Source: FR-W1 / NFR-W7]
- [ ] **AC4** — Given a >1MB body or unsupported type, When posted, Then the cap rejects / 200-unsupported applies — *oversized rejected at 1MB; unsupported→200 (or DLQ once E3-S4 lands)* [Source: FR-W1]

## 🔨 Tasks

- [ ] RED: TestClerkWebhookPipelineSemantics with signed/unsigned/oversized/unsupported fixtures
- [ ] Assert the five-type mapping + deterministic workflow-ID scheme (AC1)
- [ ] Verify 401/502/200 + 1MB cap (AC2, AC3, AC4)
- [ ] GREEN: pipeline semantics pinned

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** PRESENT — The shipped Clerk pipeline is operable and verified: ClerkWebhookHandler.Handle Svix-verifies → 401 on bad signature, 502 (BadGateway) on publish failure so Clerk retries, 200 otherwise, and bounds the body at MaxWebhookBodySize=1<<20 (1MB) via http.MaxBytesReader. Meaningful tests: TestClerkWebhookHandler_InvalidSignature (401), _PublishFailureReturns502 / _OrgCreated_PublishFailureReturns502 (502), _OrgDeleted/_UserDeleted/_OrgMembershipDeleted_PublishesEvent (200 + forward), _UnsupportedEventType, _MissingSecret.

**Tier:** ingest (handler: Svix verify + CE map only) → dispatch. **Seams:** Svix, Kafka broker, Temporal. **Sibling deps:** foundation for all E5 extensions; unsupported-type disposition changes when E3-S4 (DLQ-as-record) lands.

## 🧪 Testing

- [ ] Named DoD test `TestClerkWebhookPipelineSemantics` (integration)
- [ ] Integration against a real broker + Temporal test env; signed-payload fixtures
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E5 — [EPIC] workflows: Tenant-lifecycle domain — provision/deprovision/reconcile + polymorphic tenancy + entitlements

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
