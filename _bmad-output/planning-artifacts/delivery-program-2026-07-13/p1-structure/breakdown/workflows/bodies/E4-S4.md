## 🎯 Story

**As a** workflows platform engineer
**I want** the CloudEvent id derived from the Svix message id (a natural key, not a fresh random UUID) and an `Idempotency-Key` propagated on webhook→broker forwards and every `/internal/*` step-API call
**so that** a redelivery carries a stable id and the mandated dedup layer exists above the deterministic-workflow-ID backstop (cell rule 11).

**Definition of Done:** one named test `TestCeIdDerivedAndIdempotencyKeyPropagated` — integration layer: a redelivered Svix message yields the same ce-id; each step-API call carries a stable Idempotency-Key

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Svix message, When mapped to a CE, Then the ce-id derives from the Svix message id — *ce-id == deterministic function of Svix id (not random UUID)* [Source: F-DEDUP / cell rule 11]
- [ ] **AC2** — Given a redelivered webhook, When re-mapped, Then it carries the same ce-id — *redelivery ce-id equals original* [Source: F-DEDUP / NFR-W2]
- [ ] **AC3** — Given each step-API call, When issued, Then it carries an Idempotency-Key — *Idempotency-Key header present + stable across replays* [Source: F-DEDUP / NFR-W2]
- [ ] **AC4** — Given a random-UUID ce-id in code, When scanned, Then none survives (negative) — *no `uuid.New()` for ce-id in `events.PublishData`* [Source: F-DEDUP]

## 🔨 Tasks

- [ ] RED: TestCeIdDerivedAndIdempotencyKeyPropagated
- [ ] Derive ce-id from the Svix message id (AC1, AC2)
- [ ] Propagate Idempotency-Key on webhook forward + step-API calls (AC3)
- [ ] Remove the random-UUID ce-id mint (AC4)
- [ ] GREEN: stable ids + keys

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — Deprovision/provision CE ids are freshly random (PublishData → uuid.NewString()); the Svix message id is used only to verify the signature, never propagated as the ce-id, and no Idempotency-Key is derived from it. (TenantMove separately uses a deterministic MoveID-derived id, but that is not the F-DEDUP Svix path.) Absent.

**Tier:** ingest + activity drivers. **Seams:** Svix, CE envelope helper, step-APIs. **Sibling deps:** rides E4-S2 helper; complements the deterministic workflow-ID dedup (the honest current backstop).

## 🧪 Testing

- [ ] Named DoD test `TestCeIdDerivedAndIdempotencyKeyPropagated` (integration)
- [ ] Integration with a redelivered Svix fixture; assert stable ce-id + Idempotency-Key on step calls
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] cell-lint — `JGAUQRsw2g`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E4 — [EPIC] workflows: Observability, cell contract & residency posture

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
