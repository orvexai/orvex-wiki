## Feature area

The first new domain — instantiated, not invented. `RebuildTenant` implements knowledge A8 as a checkpointed pipeline (fresh namespace → cursor walk → delta re-pull → atomic alias-swap → retire), `RebuildCell` fans out children with bounded parallelism + `ContinueAsNew`, both enqueued as CloudEvents on the spine (no start-workflow HTTP API, A-W10), with the worker fixed-replica-sized against knowledge's RTO (tenant <15min, cell <24h) WITHOUT KEDA. This is the forcing function for OQ-W1/W2/W7 — it must not land on old names or an unsized worker.

**Why it exists:** PRD F2 (FR-W8–W10) + NFR-W4 + Arch domain map row 2 + A-W6/A-W10. Gates knowledge FR-K23/K24 GA + its DR posture. Reconciles the four stale KEDA texts to NO-KEDA.

## Story roster

- [ ] E7-S1 — [workflows] RebuildTenant checkpointed workflow: namespace→walk→delta-repull→alias-swap→retire (FR-W8)
- [ ] E7-S2 — [workflows] RebuildCell parent fan-out + ContinueAsNew + progress query (FR-W9)
- [ ] E7-S3 — [workflows] CE-on-spine rebuild enqueue Trigger + dispatcher registry (FR-W10/A-W10)
- [ ] E7-S4 — [workflows] Worker fixed-replica sizing vs RTO — shadow measure + scale-up runbook, NO-KEDA (NFR-W4/OQ-W7)

## FR/NFR ids this epic owns

FR-W8, FR-W9, FR-W10, NFR-W4, NFR-W8, A-W10

## Definition of Done

- [ ] All child stories Done (orchestrator-verified)
- [ ] The family six-surface E2E share for this area is **green on the dev cell, human-verified** (not reported) — the fake-done antibody
- [ ] SDD lines evidenced: named tests green, contract fixtures round-trip, SE-Arch lenses signed off

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E7 — [EPIC] workflows: Knowledge index rebuild/backfill domain

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
