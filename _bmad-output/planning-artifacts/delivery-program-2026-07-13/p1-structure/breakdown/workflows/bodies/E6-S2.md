## 🎯 Story

**As a** workflows platform engineer
**I want** Keycloak-backed (SCIM-originated) tenants getting provision / deprovision-with-grace / purge through the SAME workflows, triggered by identity-emitted lifecycle CloudEvents arriving as spine Triggers, with the workflow-ID scheme generalized to registry tenant IDs
**so that** a Keycloak tenant can be deprovisioned/purged at all — since D-WF-1/identity FR-I15 forbid identity running that orchestration itself.

**Definition of Done:** one named test `TestKeycloakTenantLifecycleViaSpineCE` — workflow-test-env layer: an identity lifecycle CE (no Svix path) drives the same Provision/Deprovision/Reconcile workflows keyed by the registry tenant ID

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an identity-emitted lifecycle CE on the spine, When a Trigger routes it, Then the same Provision/Deprovision workflow runs — *no new machinery; same workflows, ingest via spine Trigger* [Source: FR-W17 / A-W7]
- [ ] **AC2** — Given a Keycloak tenant, When deprovisioned, Then it gets the same grace+purge path — *deprovision-with-grace + purge run for the registry tenant ID* [Source: FR-W17]
- [ ] **AC3** — Given the workflow-ID scheme, When a registry tenant ID is used, Then it generalizes beyond Clerk org IDs (negative: not Clerk-specific) — *workflow ID accepts registry tenant id form* [Source: FR-W17]

## 🔨 Tasks

- [ ] RED: TestKeycloakTenantLifecycleViaSpineCE
- [ ] Add the identity-lifecycle-CE Trigger as a second ingest source (AC1)
- [ ] Route to the same workflows keyed by registry tenant ID (AC1, AC3)
- [ ] Verify grace+purge coverage for Keycloak tenants (AC2)
- [ ] Land behind GetVersion + replay history (E1-S4)
- [ ] GREEN: Keycloak path covered

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — No Keycloak-tenant lifecycle and no consumption of identity-emitted CloudEvents on the spine. Only clerk.* + orvex.tenant.move.* CE types exist. Absent.

**Tier:** ingest (spine Trigger) + workflow. **Seams:** orvex-studio-identity FR-I15 CE catalog (exact CE types + tenant-ID scheme are OQ-W9 — TBD pinned by ENG-2107, gated on identity FR-I15 freeze). **Sibling deps:** reuses the polymorphic workflow-ID (E5-S3); depends on the spine (E3-S3/E4-S2).

## 🧪 Testing

- [ ] Named DoD test `TestKeycloakTenantLifecycleViaSpineCE` (workflow-test-env)
- [ ] Temporal testsuite; identity CE faked from golden fixtures; GetVersion-gated replay
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-identity (FR-I15, OQ-W9)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E6 — [EPIC] workflows: IdP-agnostic lifecycle & identity cutover
- [ ] Blocked by: E1-S4, E5-S3

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
