## 🎯 Story

**As a** workflows platform engineer
**I want** `RebuildTenant` implementing knowledge A8 — create fresh namespace → checkpointed cursor `walk` (cursor in workflow state, each step one bounded activity) → record watermark → `delta-repull` of events landed during the walk → atomic `alias-swap` → `retire` old namespace — calling knowledge's `/internal/v1/rebuild/*` step-APIs and nothing else, resumable from its last checkpoint
**so that** a tenant reindex is durable, resumable after worker restarts, and enforces one concurrent rebuild per tenant via workflow ID `rebuild-tenant-<tenant>`.

**Definition of Done:** one named test `TestRebuildTenantCheckpointResume` — workflow-test-env layer: RebuildTenant progresses through the five steps carrying the cursor in state; killing/restarting the worker resumes from the last checkpoint, not the start

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a rebuild, When RebuildTenant runs, Then it executes namespace→walk→delta-repull→alias-swap→retire via knowledge step-APIs only — *only `/internal/v1/rebuild/*` activities called; correct order* [Source: FR-W8]
- [ ] **AC2** — Given a worker restart mid-walk, When it resumes, Then it continues from the checkpoint cursor (not restart) — *resume cursor == last watermark; no re-walk from zero* [Source: FR-W8]
- [ ] **AC3** — Given a second rebuild for the same tenant, When enqueued, Then the workflow ID dedups to one concurrent rebuild (negative) — *`rebuild-tenant-<tenant>` → AlreadyStarted ACK; one rebuild* [Source: FR-W8 / NFR-W2]
- [ ] **AC4** — Given all steps, When run, Then each uses the AUTHORITATIVE retry class — *every rebuild activity AUTHORITATIVE (MaximumAttempts==0)* [Source: FR-W8 / NFR-W5]

## 🔨 Tasks

- [ ] RED: TestRebuildTenantCheckpointResume
- [ ] Implement the five-step checkpointed workflow with cursor-in-state (AC1, AC2)
- [ ] Enforce one-concurrent-rebuild-per-tenant via workflow ID (AC3)
- [ ] Apply AUTHORITATIVE retry to all steps (AC4)
- [ ] Use lib-codegen rebuild clients (E2-S4)
- [ ] Land behind GetVersion + replay history (E1-S4)
- [ ] GREEN: resume-from-checkpoint verified

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** ABSENT — No RebuildTenant workflow (no namespace→walk→delta-repull→alias-swap→retire checkpointed workflow) on main. The knowledge-rebuild pipeline lands on origin/dev (commit 291f176).

**Tier:** workflow (cursor decision) + activities (knowledge step-APIs, only-IO). **Seams:** knowledge `/internal/v1/rebuild/*` (CAS/epoch semantics are knowledge's, behind the API). **Sibling deps:** rebuild clients from lib (E2-S4); enqueue via E7-S3; MUST land on renamed/consolidated names (E1-S1/S2) + GetVersion (E1-S4).

## 🧪 Testing

- [ ] Named DoD test `TestRebuildTenantCheckpointResume` (workflow-test-env)
- [ ] Temporal testsuite with worker-restart mid-walk; knowledge faked from golden fixtures
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`
- [ ] orvex-studio-knowledge (A8, FR-K23/K24)

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E7 — [EPIC] workflows: Knowledge index rebuild/backfill domain
- [ ] Blocked by: E1-S1, E1-S2, E1-S4, E2-S4

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
