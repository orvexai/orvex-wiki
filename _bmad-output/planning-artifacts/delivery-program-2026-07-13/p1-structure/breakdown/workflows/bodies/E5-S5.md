## 🎯 Story

**As a** workflows platform engineer
**I want** `DeprovisionWorkflow`: step 1 disable NOW on wiki(`deactivate`)+studio(`disable`) in parallel; step 2 for `organization.deleted` only — `workflow.Sleep(GraceHours)` (default 720h from the workflow input) then hard delete on both + revoke the tenant's billing entitlements as an ordered purge step; membership/user.deleted deactivate-only
**so that** deprovision is lossless and reversible during grace, and purge is a complete ordered teardown incl. entitlement revoke — with studio 404 treated as idempotent success (until FR-W7).

**Definition of Done:** one named test `TestDeprovisionGraceAndPurgeOrder` — workflow-test-env layer: org.deleted disables now, sleeps the grace window (input-carried), then purges wiki+studio and revokes entitlements in order; membership/user.deleted deactivate only

Note: *final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2107); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `organization.deleted`, When DeprovisionWorkflow runs, Then it disables both targets now, sleeps GraceHours, then hard-deletes both — *disable→Sleep(720h from input)→delete order; GraceHours from input not env* [Source: FR-W3 / NFR-W1]
- [ ] **AC2** — Given the purge leg, When it runs, Then entitlements are revoked as an ordered step before hard delete — *billing revoke called before/within purge; AUTHORITATIVE* [Source: FR-W18 / FR-W3]
- [ ] **AC3** — Given `organizationMembership.deleted`/`user.deleted`, When processed, Then it deactivates only — no grace, no purge (negative) — *no Sleep, no delete, no revoke on these types* [Source: FR-W3]
- [ ] **AC4** — Given studio returns 404 on disable/delete, When observed, Then it is idempotent success (until FR-W7 cutover) — *404→success; convention flagged to die at FR-W7* [Source: FR-W3 / NFR-W5]

## 🔨 Tasks

- [ ] RED: TestDeprovisionGraceAndPurgeOrder
- [ ] Implement disable-now parallel on wiki+studio (AC1)
- [ ] org.deleted grace Sleep(input GraceHours) → hard delete + entitlement revoke ordered (AC1, AC2)
- [ ] membership/user.deleted deactivate-only (AC3)
- [ ] Studio 404=idempotent-success carve-out (AC4)
- [ ] Land behind GetVersion + record a grace-sleep replay history (E1-S4)
- [ ] GREEN: ordering + revoke verified

## 🧠 Context

**🧾 Code audit (origin/main @ 6e56015, 2026-07-14):** PARTIAL — DeprovisionWorkflow implements disable-now → workflow.Sleep(GraceHours) → purge on BOTH wiki (authoritative) and studio (best-effort), tests in deprovision_workflow_test.go. Missing: no entitlement revoke step (FR-W18 half). Partial.

**Tier:** workflow (Sleep timer) + activities. **Seams:** engine, studio, billing revoke. **Sibling deps:** the #1 replay risk (720h sleep) — MUST have a recorded grace-sleep history (E1-S4); revoke uses billing client (E2-S4); 404 carve-out dies at E6-S1.

## 🧪 Testing

- [ ] Named DoD test `TestDeprovisionGraceAndPurgeOrder` (workflow-test-env)
- [ ] Temporal testsuite with time-skipping over the grace window; grace-sleep replay history recorded
- [ ] CS §5 mock categories: fake siblings from committed **golden fixtures** (remote-but-owned); real Temporal test env for workflow/replay tiers; **never mock own packages** (`internal/*`)

## 📏 Guidance

- [ ] **CS `6aMAzsYeQb`** §§0/3/4/5/6/7/10/11 — tier placement (§6: ingest=handler, dispatch=thin driver, workflow=pure decision, activity=only-IO driver); seam + mock boundaries (§5 remote-but-owned = golden fixtures, never mock own packages; §7 event-tier); operability SLIs (§10); ALL-REAL honesty, no fake-done (§11)
- [ ] **SE-Arch `8sYi523i4t`** lenses — determinism/replay-safety, loss-boundedness (G2), dedup-exactly-once-effect, fake-done antibody, ID-only-payload residency
- [ ] **cell-lint `JGAUQRsw2g`** — ID-only Temporal payloads, `orvexcell` extension, `CELL_ID` distinct from `CLUSTER_NAME`, global-Temporal singleton is the sanctioned rule-14 exception

## 🔗 References

- [ ] PRD: orvex-studio-workflows — `4IF3xjIdAs`
- [ ] Architecture: orvex-studio-workflows — `JiIyp0RLLJ`
- [ ] Architecture Audit — SE-Arch review (2026-07-05) — `7LGGFR5tGE`

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E5 — [EPIC] workflows: Tenant-lifecycle domain — provision/deprovision/reconcile + polymorphic tenancy + entitlements
- [ ] Blocked by: E1-S4, E2-S4, E3-S1

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
