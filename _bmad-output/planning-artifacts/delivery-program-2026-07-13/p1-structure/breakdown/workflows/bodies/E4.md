## Feature area

The CS §10 operability + cell-contract wave. It lands the per-domain SLIs (webhook→start latency, failure rate, DLQ depth, rebuild throughput vs RTO), the owed cell-contract pieces (`CELL_ID`/`CLUSTER_NAME` + `/healthz` echo, the `orvexcell` CloudEvent extension stamped on every publish), the ID-only Temporal payload invariant (drop `OrgName`), the ce-id/Idempotency-Key dedup layer (derive ce-id from the Svix message id), and the single sanctioned read-only admin Temporal-view endpoint for orvex-studio-console.

**Why it exists:** Arch §6 cell posture (F-CELL a/b, F-PAYLOAD, F-DEDUP) + PRD FR-W15 + NFR-W6 amendment. Cell §4/Principle 6 make ID-only payloads a residency invariant that keeps a single global Temporal legal.

## Story roster

- [ ] E4-S1 — [workflows] Per-domain SLIs + metrics + OTel spans feeding LGTM (FR-W15)
- [ ] E4-S2 — [workflows] Cell-contract compliance: CELL_ID/CLUSTER_NAME/healthz + orvexcell stamping (F-CELL a/b)
- [ ] E4-S3 — [workflows] ID-only Temporal payloads: drop OrgName (F-PAYLOAD)
- [ ] E4-S4 — [workflows] ce-id from Svix message id + Idempotency-Key propagation (F-DEDUP/NFR-W2)
- [ ] E4-S5 — [workflows] Read-only admin Temporal-view endpoint for console (NFR-W6 amendment)

## FR/NFR ids this epic owns

FR-W15, NFR-W2, NFR-W6, F-CELL, F-PAYLOAD, F-DEDUP

## Definition of Done

- [ ] All child stories Done (orchestrator-verified)
- [ ] The family six-surface E2E share for this area is **green on the dev cell, human-verified** (not reported) — the fake-done antibody
- [ ] SDD lines evidenced: named tests green, contract fixtures round-trip, SE-Arch lenses signed off

## 🔗 Dependencies

- [ ] **Blocked by:** ENG-2107 (Definition Pack — the contract **TAG** is the dispatch gate; this story is dispatch-blocked until that tag exists)
- [ ] **Parent epic:** E4 — [EPIC] workflows: Observability, cell contract & residency posture

## 📡 Protocol

- [ ] **CLAIM** — comment `claiming <this story-id>` on the issue → move to In Progress (reviewer ≠ implementer reserved)
- [ ] **PLAN** — post approach + the named DoD test signature *before* code
- [ ] **PROGRESS** — RED→GREEN notes as tracer bullets land
- [ ] **COMMITS** — every commit `Part of ENG-NNN` (never `Closes`/`Fixes`)
- [ ] **HANDOFF** — PR links the named test green + evidence it exercised the interface, not a mock
- [ ] **REVIEW** — a second agent (reviewer ≠ implementer) runs the SE-Arch lenses
- [ ] **TICK** — check an AC only when its literal machine assertion passed
- [ ] **DONE** — orchestrator-only transition after review + green family-E2E share
- [ ] **ESCALATE** — a missing/ambiguous contract tag → comment on ENG-2107, do not guess
