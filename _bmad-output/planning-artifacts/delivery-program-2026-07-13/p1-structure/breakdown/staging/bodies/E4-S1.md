## 🎯 Story

As a **customer admin** I want **the Autonomy Dial as a server-side domain verdict — `policy` computing every autonomy decision from (dial position per tenant+space) × (Operation risk class) × (producer Trust Tier) × (routing confidence), with all three positions (`recommend`/`auto-apply-low-risk`/`full-auto`) shipping and destructive ops always human-gated below `full-auto`** so that **autonomy is enforced policy, not prompt text, and destructive ops can never slip a gate**.

**Definition of Done:** one named test `TestDialVerdictGatesDestructiveBelowFullAuto` (unit+integration — at `recommend` every apply needs a human accept; at `auto-apply-low-risk` only low-risk ops from above-threshold producers are auto-eligible; a `replace-document`/`delete-document` is human-gated at every position below `full-auto`; the verdict is computed in `policy`, never by a surface). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `recommend` (default), When any Proposal is decided, Then apply needs a human accept. *Assert: no auto-apply at recommend.* [Source: mOzNDhn322 FR-STG10; GOacy9kdJz AD-7]
- [ ] **AC2** — Given `auto-apply-low-risk`, When a low-risk op (add-section/edit-section/add-document) from an above-tier producer arrives, Then it is auto-apply-eligible; destructive ops are not. *Assert: destructive ⇒ human-gated regardless.* [Source: mOzNDhn322 FR-STG10]
- [ ] **AC3** — Given any dial position, When a verdict is needed, Then `policy` computes it from the 4-factor product; surfaces only marshal. *Assert: MCP/CLI/UI never compute the verdict.* [Source: GOacy9kdJz AD-7]
- [ ] **AC4 (audit)** — Given an auto-apply, When it happens, Then a justification line is recorded as an event. *Assert: every auto-apply emits its justification.* [Source: mOzNDhn322 FR-STG10; GOacy9kdJz AD-7]

## 🔨 Tasks

- [ ] RED: `TestDialVerdictGatesDestructiveBelowFullAuto` (AC1–AC3).
- [ ] GREEN: `internal/policy` verdict fn taking a scope key (tenant+space, extensible to doc-type later); risk-class table; justification event (AC1–AC4).

## 🧠 Context

Tier placement: `internal/policy` (domain verdict). Engine analogue: an authorization policy service. Seams: none for the verdict (pure fn); consumes E4-S2 tiers + E3 confidence. Sibling: E4-S3 queue buckets, E5-S1 apply gating.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — `internal/policy` is a doc-only stub — no Autonomy Dial server-side policy verdict.

## 🧪 Testing

Named DoD `TestDialVerdictGatesDestructiveBelowFullAuto` (unit+integration). Tiers: unit (verdict matrix) heavy + integration. CS §5: no external mocks — pure domain; never mock own `policy`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/6/7/11; SE-Arch `8sYi523i4t` (server-side-authority, gate-cannot-be-bypassed); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.2 FR-STG10, §8 SM-C1; Architecture `GOacy9kdJz` AD-7.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: dial/risk DTOs); parent epic **E4**; **E3** (confidence), **E4-S2** (tiers).
- [ ] Intra-service: consumed by E4-S3, E5-S1, E7 sweeps.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
