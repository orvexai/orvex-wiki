## 🎯 Story

As an **Orvex operator** I want **the hard-cut migration of ALL agent wiki-write surfaces — MCP `save_page`/`edit` re-point to emit Proposals, the hidden `studio_library_save`/`studio_librarian_session` passthroughs absorbed or removed, and agent-class CLI wiki writes rejected at the wiki-api ingress with 403 + a migration error naming `orvex-cli staging` — with no dual-write and no legacy shim, sequenced LAST** so that **100% of agent knowledge writes flow through staging (SM-1) and direct agent writes become structurally impossible**.

**Definition of Done:** one named test `TestAgentClassWikiWriteIs403WithMigrationError` (integration — an agent-class credential on any wiki write verb (MCP repoint, CLI `page create/update/patch/edit/upsert/block`) is rejected 403 with a migration error naming `orvex-cli staging`; human credentials and the Librarian apply engine's privileged credential still pass; no silent dual-write path exists). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given MCP `save_page`/`edit`, When called by an agent, Then they re-point to emit Proposals (not a direct wiki write). *Assert: a `save_page` produces a Proposal, not an engine write.* [Source: mOzNDhn322 FR-STG25; GOacy9kdJz AD-12]
- [ ] **AC2** — Given an agent-class CLI wiki write, When attempted, Then wiki-api ingress returns 403 + a migration error naming `orvex-cli staging`. *Assert: 403 with the exact migration message; credential-class enforced, not session heuristics.* [Source: mOzNDhn322 FR-STG25, NFR-STG5]
- [ ] **AC3** — Given human credentials or the Librarian apply engine, When they write, Then they pass. *Assert: human + privileged apply credential succeed.* [Source: mOzNDhn322 FR-STG25; GOacy9kdJz AD-4]
- [ ] **AC4 (negative)** — Given the cut, When it lands, Then there is no silent dual-write and no legacy shim; the hidden passthroughs are absorbed or removed. *Assert: no fallback write path exists.* [Source: mOzNDhn322 FR-STG25; no-fallbacks]

## 🔨 Tasks

- [ ] RED: `TestAgentClassWikiWriteIs403WithMigrationError` (AC1, AC2, AC4).
- [ ] GREEN: MCP hero re-points; wiki-api ingress credential-class 403 + migration error; remove hidden passthroughs (AC1–AC4).
- [ ] Gate: sequence LAST — only after E9 six-surface pass against non-501 `ai`+`knowledge`.

## 🧠 Context

Tier placement: MCP repoint (TS) + wiki-api ingress fencing (infra/config) + this service as the destination. Engine analogue: network-fencing direct write routes to wiki-api. Seams: wiki-api ingress, MCP, CLI. Sibling: E8-S1/S2 surfaces, E9 gate (this is sequenced after it).

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — the hard-cut migration of agent wiki-write surfaces is not implemented — no loud-error redirection of legacy write paths.

## 🧪 Testing

Named DoD `TestAgentClassWikiWriteIs403WithMigrationError` (integration). Tiers: integration (ingress + surface). CS §5: fake wiki-api ingress from goldens; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/4/5/6/11; SE-Arch `8sYi523i4t` (hard-cut, credential-class-enforcement, no-shim); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.5 FR-STG25, §7 NFR-STG5, §8 SM-1; Architecture `GOacy9kdJz` AD-4/AD-12; Addendum `P5R7eCXJA0` A3 (MCP inventory).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: migration-error contract, ingress credential-class spec); parent epic **E8**; ALL of E2–E7 and **E9** (sequenced LAST).
- [ ] Prerequisite (external): non-501 `ai`+`knowledge`; wiki-api ingress fencing live.
- [ ] Cross-service: `orvex-studio-mcp`, `orvex-cli`, `orvex-wiki-api`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
