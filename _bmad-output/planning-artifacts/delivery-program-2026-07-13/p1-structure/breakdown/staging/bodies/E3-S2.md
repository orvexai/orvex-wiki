## 🎯 Story

As a **knowledge manager** I want **find-before-create routing that resolves each Proposal against the wiki's living tree + `orvex-studio-knowledge`'s search/dedup surface — amending an existing node rather than creating a sibling, surfacing duplicates as `duplicate-of`, and routing working-context content to `divert-to-workgraph`** so that **the wiki never accumulates duplicate siblings and non-durable content goes to the right store**.

**Definition of Done:** one named test `TestRoutingAmendsExistingNodeNotSibling` (integration — a Proposal matching an existing concept routes as an amend of that node; a near-duplicate yields `duplicate-of`; a working-context item yields `divert-to-workgraph` carrying the shared sha256 content-hash; `reject` covers Trash). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Proposal matching an existing page, When routed, Then it becomes an amend of that node, not a new sibling. *Assert: route target = existing document id; Disposition ∈ {apply-as-routed, apply-with-merge}.* [Source: mOzNDhn322 FR-STG8]
- [ ] **AC2** — Given a near-duplicate, When routed, Then it surfaces as `duplicate-of`. *Assert: Disposition = duplicate-of with the candidate ref.* [Source: mOzNDhn322 FR-STG8]
- [ ] **AC3** — Given working-context content, When routed, Then `divert-to-workgraph` targets `orvex-studio-workgraph` on accept with two-way cross-links + shared content-hash. *Assert: divert payload carries sha256; never the wiki path.* [Source: mOzNDhn322 FR-STG8; GOacy9kdJz AD-6]
- [ ] **AC4 (negative)** — Given the knowledge search surface unavailable, When routing runs, Then it degrades to an `escalate`/low-confidence flag, never a blind new-sibling create. *Assert: no sibling created on dedup failure.* [Source: no-fallbacks; mOzNDhn322 FR-STG8]

## 🔨 Tasks

- [ ] RED: `TestRoutingAmendsExistingNodeNotSibling` (AC1–AC4).
- [ ] GREEN: `internal/routing` against `knowledgeclient` search/dedup; confidence scoring; divert packaging via `workgraphclient` (AC1–AC4).

## 🧠 Context

Tier placement: `internal/routing` (domain). Engine analogue: the doc-amend find-before-create rule, productized. Seams: `orvex-studio-knowledge` (search/dedup), `orvex-studio-workgraph` (divert). Sibling: E3-S1 triage, E7-S1 duplicate sweep.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — `internal/routing` is a doc-only stub — no find-before-create dedup, duplicate-of, or divert-to-workgraph routing.

## 🧪 Testing

Named DoD `TestRoutingAmendsExistingNodeNotSibling` (integration). Tiers: unit (route decision) + integration. CS §5: fake `knowledgeclient`/`workgraphclient` from goldens; never mock own `routing`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/11; SE-Arch `8sYi523i4t` (find-before-create, no-fallback-on-dedup-fail); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.2 FR-STG8, Glossary (Disposition); Architecture `GOacy9kdJz` AD-6.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: knowledge query contract, workgraph divert contract, shared content-hash def); parent epic **E3**; **E3-S1**.
- [ ] Cross-service (soft-gated): `orvex-studio-knowledge`, `orvex-studio-workgraph`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
