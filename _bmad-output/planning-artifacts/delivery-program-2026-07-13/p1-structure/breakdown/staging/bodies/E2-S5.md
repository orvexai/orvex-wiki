## 🎯 Story

As an **Orvex operator and tenant admin** I want **admission control that enforces per-session and per-tenant in-flight caps with reject-at-the-door semantics returning the frozen 402 `QUOTA_EXCEEDED`, plus queue depth/age alerting and admin-visible backpressure** so that **an agent write burst can never degrade the wiki or other tenants, and an at-cap agent gets a clear stop instead of retry-spamming**.

**Definition of Done:** one named test `TestAdmissionRejectsAtCapWith402` (integration — submits up to the entitlement cap succeed, the next returns the frozen 402 `QUOTA_EXCEEDED` shape (never 429, never destructive), and queue depth/age metrics reflect the state). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tenant at its in-flight cap, When another Proposal is submitted, Then it is rejected at the door with 402 `QUOTA_EXCEEDED`. *Assert: response = frozen 402 contract shape; nothing persisted; never 429.* [Source: mOzNDhn322 FR-STG3; GOacy9kdJz AD-11, ADR-0003]
- [ ] **AC2** — Given caps, When computed, Then they read billing-owned entitlements via `billingclient` (billing is SoR). *Assert: cap value sourced from entitlement, not hardcoded.* [Source: mOzNDhn322 FR-STG3; GOacy9kdJz AD-11]
- [ ] **AC3** — Given queue growth, When depth/age crosses thresholds, Then alerting fires and backpressure is visible to tenant admins. *Assert: metric emitted; admin-readable backpressure event.* [Source: mOzNDhn322 FR-STG3; NFR-STG6]
- [ ] **AC4 (idempotency)** — Given a retried submit with the same `Idempotency-Key`, When at cap, Then it does not double-count. *Assert: dedup before cap decrement.* [Source: GOacy9kdJz AD-11]

## 🔨 Tasks

- [ ] RED: `TestAdmissionRejectsAtCapWith402` (AC1, AC4).
- [ ] GREEN: `internal/quota` admission verdict against `billingclient` entitlements; consume the `lifecycle` in-flight predicate; depth/age metrics (AC1–AC4).

## 🧠 Context

Tier placement: `internal/quota` (domain verdict) — caps never embedded in handlers. Engine analogue: Redis quota chokepoint, but verdict is a domain fn. Seams: billing entitlements (via `billingclient`). Sibling: E2-S2 in-flight predicate, E1-S5 metrics.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — `internal/quota` is a doc-only stub — no in-flight caps, no 402 QUOTA_EXCEEDED, no depth/age alerting.

## 🧪 Testing

Named DoD `TestAdmissionRejectsAtCapWith402` (integration). Tiers: unit (verdict fn) + integration (submit path). CS §5: fake `billingclient` from goldens; never mock own `quota`/`lifecycle`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/11; SE-Arch `8sYi523i4t` (frozen error contract, never-429, admission-control lens); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.1 FR-STG3, §7 NFR-STG2/6, §8 SM-4; Architecture `GOacy9kdJz` AD-11, ADR-0003; Addendum A4 (Wikipedia backlog anchors).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: 402 `QUOTA_EXCEEDED` shape, billing entitlement contract); parent epic **E2**; **E1**, **E2-S2**.
- [ ] Cross-service: `orvex-studio-billing` (SoR).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
