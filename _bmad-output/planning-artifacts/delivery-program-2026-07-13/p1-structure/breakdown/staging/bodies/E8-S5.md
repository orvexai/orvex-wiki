## 🎯 Story

As an **Orvex operator** I want **the Curator supersession executed verbatim — Proposal proven a strict superset of Card Contract v1 (outside-proposed vs inside-resolved field-authority transferred verbatim), the shipped Curator golden tests passing against a documented Card→Proposal field mapping, and `classifyOnSave` becoming a capture on-ramp that forwards to staging submit with its local classify path removed at cutover via a loud migration error** so that **there is one classification path (not two) and no silent Card-v1 semantic break**.

**Definition of Done:** one named test `TestCuratorGoldensPassAgainstCardToProposalMapping` (integration — the shipped Curator goldens (idempotency, cap-keyed-on-verified-principal, degrade reasons `sibling-unreachable`/`cap-exhausted`, no-direct-LLM) pass against the Card→Proposal mapping; `classifyOnSave` forwards to staging submit; the local classify path is removed with a loud migration error; any field that cannot map verbatim forces an explicit v2 decision, never a silent break). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given Card Contract v1, When mapped, Then Proposal is a strict superset and the field-authority split transfers verbatim. *Assert: every Card field maps; unmappable fields raise an explicit v2 decision.* [Source: mOzNDhn322 §9; GOacy9kdJz AD-12]
- [ ] **AC2** — Given the shipped Curator goldens, When run against the mapping, Then they pass (idempotency, cap-keyed-on-verified-principal, degrade reasons, no-direct-LLM). *Assert: golden suite green.* [Source: mOzNDhn322 §9; GOacy9kdJz AD-12]
- [ ] **AC3** — Given `classifyOnSave`, When invoked post-supersession, Then it forwards to staging submit and its local classify path is removed with a loud migration error. *Assert: one classify path; loud error on the old path.* [Source: mOzNDhn322 §9; GOacy9kdJz AD-12]
- [ ] **AC4 (negative)** — Given a field that cannot map verbatim, When encountered, Then it forces an explicit v2 supersession-ADR decision — never a silent break. *Assert: unmappable field halts with a decision requirement.* [Source: mOzNDhn322 §9]

## 🔨 Tasks

- [ ] RED: `TestCuratorGoldensPassAgainstCardToProposalMapping` (AC1, AC2, AC4).
- [ ] GREEN: produce the Card→Proposal mapping table; port Curator goldens; `classifyOnSave` on-ramp forwarding + loud removal (AC1–AC4).

## 🧠 Context

Tier placement: mapping + goldens live at the supersession seam; `orvex-studio-api` Curator becomes a staging client. Engine analogue: a verbatim contract superset migration. Seams: `orvex-studio-api` Curator, staging submit. Sibling: E2-S1 submit, E2-S3 provenance (cap-keyed golden), E8-S4 hard cut.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — curator supersession (Card v1 → Proposal verbatim mapping + goldens) is not implemented.

## 🧪 Testing

Named DoD `TestCuratorGoldensPassAgainstCardToProposalMapping` (integration). Tiers: unit (mapping) + integration (goldens). CS §5: reuse shipped Curator goldens as real fixtures; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/5/6/9 (supersession-ADR)/11; SE-Arch `8sYi523i4t` (verbatim-superset, one-path, no-silent-break); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §9, §11 OQ5; Architecture `GOacy9kdJz` AD-12; evidence digest §3 (Card v1→Proposal superset).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: Card→Proposal mapping + supersession ADR — blocked behind the Decision-Records registry); parent epic **E8**; **E2-S1**, **E2-S3**.
- [ ] Cross-service: `orvex-studio-api` (Curator).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
