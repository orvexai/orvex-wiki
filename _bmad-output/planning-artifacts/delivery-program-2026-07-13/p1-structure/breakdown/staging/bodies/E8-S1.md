## 🎯 Story

As a **fleet agent and reviewer** I want **new `staging_*` tools on the Studio MCP server — `staging_propose`, `staging_changeset_submit`, `staging_changeset_status`, `staging_list_mine`, `staging_withdraw`, and reviewer-scoped `staging_review_queue`, `staging_decide` (bulk), `staging_apply` — as a hidden `staging` category with per-agent credential auth** so that **agents propose and reviewers decide through one bidirectional MCP surface, never a shared service credential**.

**Definition of Done:** one named test `TestMcpStagingProposeUsesPerAgentCredentialAndRoutesToSubmit` (integration — `staging_propose` authenticates with per-agent credentials and forwards to the staging submit API; reviewer-scoped tools require reviewer scope; the tools register in the hidden `staging` category before `installToolVisibility`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the MCP server, When it lists tools, Then the `staging_*` set exists in a hidden `staging` category. *Assert: tools present; hidden category via `list_tools`; registered before `installToolVisibility`.* [Source: mOzNDhn322 FR-STG23; GOacy9kdJz Naming convention]
- [ ] **AC2** — Given `staging_propose`, When called, Then it uses per-agent credentials and forwards to the submit API. *Assert: never a shared service credential; call reaches submit.* [Source: mOzNDhn322 FR-STG23]
- [ ] **AC3** — Given reviewer-scoped tools (`staging_review_queue`/`decide`/`apply`), When called without reviewer scope, Then they are denied. *Assert: 403 for non-reviewer.* [Source: mOzNDhn322 FR-STG23; GOacy9kdJz AD-8]
- [ ] **AC4 (bidirectional)** — Given the same server, When it grounds reads and accepts proposals, Then both work. *Assert: read + propose on one server.* [Source: mOzNDhn322 FR-STG23]

## 🔨 Tasks

- [ ] RED: `TestMcpStagingProposeUsesPerAgentCredentialAndRoutesToSubmit` (AC1–AC3).
- [ ] GREEN: `staging-tools.ts` in the MCP repo; register hidden category; wire per-agent auth; call the staging API (AC1–AC4).

## 🧠 Context

Tier placement: TS MCP server (`orvex-studio-mcp`), not the Go satellite — protocol translation only. Engine analogue: MCP tool front-door. Seams: staging API (Go), identity per-agent creds. Sibling: E2/E4 APIs, E8-S4 hard cut re-points heroes.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — MCP staging section lives outside this repo (the MCP server is a separate service); nothing here. No staging_* tools present in this tree.

## 🧪 Testing

Named DoD `TestMcpStagingProposeUsesPerAgentCredentialAndRoutesToSubmit` (integration). Tiers: unit (tool schema) + integration (routing). CS §5: fake the staging API from contract goldens; never mock own MCP modules.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/4/5/6/11 + React/TS front rules (MCP is TS); SE-Arch `8sYi523i4t` (per-agent-cred, thin-protocol-gateway); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.5 FR-STG23; Architecture `GOacy9kdJz` (Naming, AD-8); Addendum `P5R7eCXJA0` A3 (MCP inventory).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: `staging_*` tool grammar); parent epic **E8**; **E2**, **E4**.
- [ ] Cross-service: `orvex-studio-mcp`, `orvex-studio-identity`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
