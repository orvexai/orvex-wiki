## 🎯 Story

As an **Orvex operator / auditor** I want **two-sided immutable provenance on every Proposal — the producing agent identity AND the triggering end-user conversation/session — keyed on the verified principal, surviving into applied wiki history and audit events** so that **every knowledge write is attributable and no body-supplied id can spoof authorship**.

**Definition of Done:** one named test `TestProvenanceKeyedOnVerifiedPrincipalIsImmutable` (integration — provenance records the token-verified agent identity and the server-derived session ref; a body-supplied principal/session id is ignored; an attempt to mutate provenance after creation is rejected). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a submit, When provenance is written, Then it carries agent identity + triggering session, both from the verified token. *Assert: stored producer id = token principal; session ref = server-derived, not body.* [Source: mOzNDhn322 FR-STG5; GOacy9kdJz AD-8]
- [ ] **AC2** — Given an applied Proposal, When it lands in the wiki, Then provenance survives into wiki history + audit events. *Assert: the apply receipt + audit event carry the same producer/session.* [Source: mOzNDhn322 FR-STG5; NFR-STG4]
- [ ] **AC3 (negative)** — Given a body-supplied producer id ≠ token, When submitted, Then the token wins. *Assert: stored producer = token identity.* [Source: GOacy9kdJz AD-8 (cap-keyed-on-principal golden)]
- [ ] **AC4 (immutability)** — Given an existing Proposal, When a provenance update is attempted, Then it is rejected. *Assert: no UPDATE path exists; column is write-once.* [Source: mOzNDhn322 FR-STG5]

## 🔨 Tasks

- [ ] RED: `TestProvenanceKeyedOnVerifiedPrincipalIsImmutable` (AC1, AC3, AC4).
- [ ] GREEN: provenance capture from verified claims at submit; write-once columns; propagate into audit/apply events (AC1–AC4).

## 🧠 Context

Tier placement: `internal/intake` + `internal/lifecycle` (immutable columns). Engine analogue: audit metadata, but keyed on verified identity. Seams: auth (E1-S3), audit events (E1-S5). Sibling: E2-S1 submit, E4-S2 trust tiers (key on same principal).

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — provenance/attribution is not implemented — no provenance table, no two-sided immutable record keyed on verified principal.

## 🧪 Testing

Named DoD `TestProvenanceKeyedOnVerifiedPrincipalIsImmutable` (integration). Tiers: unit + integration. CS §5: real store + verified-claim fixture; no own-package mocks. Reuses the Curator cap-keyed-on-principal golden.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/11; SE-Arch `8sYi523i4t` (no spoofable ids, immutable audit); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.1 FR-STG5, §7 NFR-STG4/5; Architecture `GOacy9kdJz` AD-8.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: provenance shape); parent epic **E2**; **E1-S3/S4**.
- [ ] Intra-service: consumed by E4-S2 (trust), E9-S2 (audit).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
