## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **staging platform engineer** I want **the Postgres store baseline — RLS fail-closed policies shipped in migration `0001`, UUIDv7 PKs, a tenant-keyed schema, and a testcontainers harness** so that **tenant isolation is enforced at the database from day one (not a follow-up) and every repository is tested against a real Postgres**.

**Definition of Done:** one named test `TestRLSFailsClosedCrossTenant` (integration via testcontainers — a query under tenant A's `app.tenant` GUC cannot read tenant B's rows, and a query with no GUC set returns zero rows / errors, never a full-table read). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given migration `0001`, When applied, Then RLS policies exist on every tenant-keyed table with `app.tenant` GUC enforcement. *Assert: `pg_policies` lists a fail-closed policy per table.* [Source: GOacy9kdJz P5+ADR-0014, AD-8]
- [ ] **AC2** — Given tenant A's GUC, When reading tenant B's rows, Then zero rows return. *Assert: cross-tenant SELECT count = 0.* [Source: GOacy9kdJz AD-8; P8 isolation]
- [ ] **AC3 (negative)** — Given no GUC set, When any tenant-keyed query runs, Then it fails closed. *Assert: query returns 0 rows or errors — never all tenants' data.* [Source: GOacy9kdJz P5 RLS fail-closed]
- [ ] **AC4** — Given a new table, When added, Then it carries a UUIDv7 PK and a `tenant` column, no `cell_id` column. *Assert: cell-lint schema checks pass.* [Source: GOacy9kdJz ADR-0011/cell-lint]

## 🔨 Tasks

- [ ] RED: `TestRLSFailsClosedCrossTenant` against an empty schema (AC2, AC3).
- [ ] GREEN: forward-only `0001_*.sql` with RLS policies, UUIDv7 PKs, tenant column; advisory-lock applier (AC1, AC4).
- [ ] Build the testcontainers harness inside `internal/store/postgres` only.

## 🧠 Context

Tier placement: `internal/store/postgres` (only driver import). Engine analogue: TypeORM entities, but here raw pgx + RLS. Seams: none (own store). Sibling dependency: none; every domain repo builds on this.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** present — fully implemented AND tested. `migrations/0001_baseline.sql` ships RLS ENABLED+FORCED, fail-closed on `current_setting('app.tenant')`, with uuidv7() PKs on `proposal`+`outbox`; `internal/store/postgres/postgres.go` is the advisory-locked embedded-migration applier; `postgres_test.go` `TestBaselineRLSFailsClosed` proves fail-closed + per-tenant isolation against a REAL testcontainers Postgres 18 as a non-superuser role, and CI asserts it ran (not skipped).

## 🧪 Testing

Named DoD `TestRLSFailsClosedCrossTenant` (integration/testcontainers). Tiers: integration only (store). CS §5: real Postgres via testcontainers; no store mocks; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/5/6/10/11; SE-Arch `8sYi523i4t` (RLS fail-closed, tenant-isolation lens); cell-lint `JGAUQRsw2g` (UUIDv7, tenant-keyed, no `cell_id`).

## 🔗 References

- Architecture `GOacy9kdJz` (P5/ADR-0014, AD-8, ADR-0011); PRD `mOzNDhn322` §7 NFR-STG5, NFR-STG3.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG for row shapes); **E1-S1**, **E1-S3** (GUC source); parent epic **E1**.
- [ ] Intra-service: blocks every domain package's repository.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
