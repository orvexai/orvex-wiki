## 🎯 Story

As a **new tenant** I want **the Librarian to ship with a conservative cold-start posture — a baseline Prompt Pack, dial = `recommend`, empty Trust Tiers (so nothing is auto-apply-eligible), and generic starter exemplars that embed no other tenant's content** so that **day 1 is recommendation-only, safe, and free of any cross-tenant contamination**.

**Definition of Done:** one named test `TestColdStartIsRecommendationOnlyAndContentFree` (integration — a freshly-provisioned tenant has dial=`recommend`, empty tiers meaning zero auto-apply-eligible Proposals, a baseline pack, and starter exemplars that contain no other tenant's content). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a new tenant, When provisioned, Then dial=`recommend` and Trust Tiers are empty ⇒ nothing auto-apply-eligible. *Assert: no Proposal is auto-eligible on day 1.* [Source: mOzNDhn322 FR-STG27]
- [ ] **AC2** — Given cold-start exemplars, When seeded, Then they embed no other tenant's content (content-free by construction). *Assert: exemplars contain no tenant-scoped strings from any other tenant.* [Source: mOzNDhn322 FR-STG27; GOacy9kdJz AD-14]
- [ ] **AC3** — Given onboarding, When shown, Then the recommendation-only posture is stated as such. *Assert: onboarding copy declares recommend-only day 1.* [Source: mOzNDhn322 FR-STG27]
- [ ] **AC4 (negative)** — Given any future cross-tenant prior, When added, Then it must be content-free by construction. *Assert: a prior carrying tenant content is rejected.* [Source: mOzNDhn322 FR-STG27]

## 🔨 Tasks

- [ ] RED: `TestColdStartIsRecommendationOnlyAndContentFree` (AC1, AC2, AC4).
- [ ] GREEN: tenant-provision hook seeding baseline pack + `recommend` dial + empty tiers + generic exemplars (AC1–AC4).

## 🧠 Context

Tier placement: `internal/learning` + `internal/policy` (default dial). Engine analogue: default-config seeding on tenant create. Seams: identity tenant-provision event. Sibling: E4-S1 dial, E4-S2 tiers, E6-S4 marketplace.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — cold-start defaults are not implemented — no baseline pack, dial recommendation, or content-free exemplars.

## 🧪 Testing

Named DoD `TestColdStartIsRecommendationOnlyAndContentFree` (integration). Tiers: unit + integration. CS §5: real store; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/6/11; SE-Arch `8sYi523i4t` (safe-defaults, no-cross-tenant-contamination); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.3 FR-STG27; Architecture `GOacy9kdJz` AD-14.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: tenant-provision event, baseline pack shape); parent epic **E6**; **E4-S1/S2**.
- [ ] Cross-service: `orvex-studio-identity` (tenant lifecycle).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
