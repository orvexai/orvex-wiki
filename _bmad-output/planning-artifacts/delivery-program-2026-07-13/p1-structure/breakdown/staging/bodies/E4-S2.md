## 🎯 Story

As a **customer admin** I want **per-producer Trust Tiers computed from historical acceptance, recomputed continuously, gating auto-apply eligibility and throttling repeat-rejected producers at admission — visible to admins** so that **autonomy is earned per producer and a persistently-rejected agent is slowed at the door**.

**Definition of Done:** one named test `TestTrustTierGatesAutoApplyAndThrottlesRejected` (integration — a producer's acceptance history computes a tier; below-threshold producers are never auto-apply-eligible; a sustained-rejection producer is throttled at admission; the tier is snapshotted at admission with no mid-session drift). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a producer's Feedback history, When the tier recomputes, Then it reflects acceptance rate and is admin-visible. *Assert: tier value changes with feedback; exposed on an admin read.* [Source: mOzNDhn322 FR-STG11]
- [ ] **AC2** — Given a below-threshold producer, When the dial permits auto-apply, Then that producer's Proposals are still not auto-eligible. *Assert: tier gate blocks auto-apply.* [Source: mOzNDhn322 FR-STG11; GOacy9kdJz AD-7]
- [ ] **AC3** — Given a sustained-rejection producer, When it submits, Then admission throttles it. *Assert: intake pace reduced for that producer.* [Source: mOzNDhn322 FR-STG11; GOacy9kdJz AD-7]
- [ ] **AC4 (consistency)** — Given a session, When policy/quota read the tier, Then they snapshot it at admission — no mid-session drift. *Assert: tier constant within a session even if recomputed.* [Source: GOacy9kdJz AD-14]

## 🔨 Tasks

- [ ] RED: `TestTrustTierGatesAutoApplyAndThrottlesRejected` (AC2, AC3, AC4).
- [ ] GREEN: `internal/learning` Trust Tier as a single versioned entity keyed on verified principal; recompute on feedback; snapshot at admission (AC1–AC4).

## 🧠 Context

Tier placement: `internal/learning` (owns the tier entity), consumed by `internal/policy` + `internal/quota`. Engine analogue: a reputation score (Wikipedia autopromotion / SO submitter gate). Seams: none external. Sibling: E2-S3 provenance (keying), E4-S1 dial, E6-S1 feedback.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — trust tiers are not implemented — no per-producer acceptance history or auto-apply gating.

## 🧪 Testing

Named DoD `TestTrustTierGatesAutoApplyAndThrottlesRejected` (integration). Tiers: unit (tier calc) + integration (gate+throttle). CS §5: real store; never mock own `learning`/`policy`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/6/7/11; SE-Arch `8sYi523i4t` (earned-trust, no-mid-session-drift); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.2 FR-STG11, §8 SM-C1; Architecture `GOacy9kdJz` AD-7/AD-14.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: tier DTO); parent epic **E4**; **E2-S3**, **E6-S1** (feedback signal).
- [ ] Intra-service: consumed by E4-S1, E2-S5.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
