## 🎯 Story

As a **CI pipeline and human power user** I want **an `orvex-cli staging` namespace with parity verbs (`propose`, `changeset submit/status/list`, `review`, `decide`, `apply`)** so that **staging is fully drivable from the CLI at parity with MCP for automation and power-user review**.

**Definition of Done:** one named test `TestCliStagingVerbsReachStagingApiAtParity` (integration — each `orvex-cli staging <verb>` reaches the corresponding staging API operation with parity to the MCP surface, authenticated as the invoking principal). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the CLI, When `orvex-cli staging propose …` runs, Then it submits a Proposal via the staging API. *Assert: call reaches submit; receipt printed.* [Source: mOzNDhn322 FR-STG24]
- [ ] **AC2** — Given the review verbs (`review`, `decide`, `apply`), When run by a reviewer principal, Then they operate the queue at parity with MCP. *Assert: same operations available as `staging_*` MCP tools.* [Source: mOzNDhn322 FR-STG24/FR-STG23]
- [ ] **AC3** — Given `changeset submit/status/list`, When run, Then they map to the ChangeSet lifecycle API. *Assert: status reflects the state machine.* [Source: mOzNDhn322 FR-STG24, FR-STG2]
- [ ] **AC4 (negative)** — Given a non-reviewer principal, When `decide`/`apply` runs, Then it is denied. *Assert: 403.* [Source: GOacy9kdJz AD-8]

## 🔨 Tasks

- [ ] RED: `TestCliStagingVerbsReachStagingApiAtParity` (AC1, AC2, AC4).
- [ ] GREEN: `orvex-cli staging` command tree; codegen'd client from the contracts tag; auth as invoking principal (AC1–AC4).

## 🧠 Context

Tier placement: `orvex-cli` repo (Go CLI). Engine analogue: a service CLI namespace. Seams: staging API. Sibling: E8-S1 MCP parity, E8-S4 hard cut (CLI wiki writes 403).

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — CLI staging section lives in orvex-cli (separate repo); nothing here.

## 🧪 Testing

Named DoD `TestCliStagingVerbsReachStagingApiAtParity` (integration). Tiers: unit (arg parsing) + integration. CS §5: fake the staging API from goldens; never mock own CLI packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/4/5/6/11; SE-Arch `8sYi523i4t` (surface-parity, principal-auth); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.5 FR-STG24; Architecture `GOacy9kdJz` (Naming convention).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: CLI client codegen source); parent epic **E8**; **E2**, **E4**.
- [ ] Cross-service: `orvex-cli`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
