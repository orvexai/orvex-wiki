## 🎯 Story

As a **staging platform engineer** I want **the `orvex-studio-staging` Go satellite skeleton in the six-tier shape with its thin `cmd` binaries, env-only config, and health/readiness probes** so that **every later domain package has a compiling, deployable substrate that echoes its cell identity**.

**Definition of Done:** one named test `TestHealthzEchoesCellIdentity` (integration layer — boots `cmd/api`, GETs `/readyz`, asserts the response body carries `CELL_ID` and `CLUSTER_NAME` from env, and that a missing required env var fails startup loudly, never a silent default). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the repo, When it builds, Then `cmd/{api,triage,relay,sweep}` are thin mains and all domain logic lives under `internal/<context>/`. *Assert: `go build ./...` succeeds and no `cmd/*` file imports `jackc/pgx`.* [Source: GOacy9kdJz AD-1, Structural Seed]
- [ ] **AC2** — Given env vars set, When `cmd/api` boots, Then `/healthz` and `/readyz` return 200 with `CELL_ID`+`CLUSTER_NAME` in the body. *Assert: response JSON has both non-empty fields matching env.* [Source: GOacy9kdJz Observability convention]
- [ ] **AC3** — Given a required env var missing, When the binary starts, Then it exits non-zero with a named error. *Assert: process exit code ≠ 0 and stderr names the missing var — no baked default.* [Source: no-fallbacks-hard-cuts; GOacy9kdJz State & cross-cutting]
- [ ] **AC4 (negative)** — Given a driver import attempted from a `cmd` main, When `go vet`/cell-lint runs, Then it fails. *Assert: cell-lint tier-discipline check reports the violation.* [Source: JGAUQRsw2g cell-lint]

## 🔨 Tasks

- [ ] RED: write `TestHealthzEchoesCellIdentity` against a not-yet-existing `cmd/api` (AC2, AC3).
- [ ] GREEN: scaffold six-tier tree, `internal/config.Load()` (env-only), thin `cmd/api`, health handlers (AC1–AC3).
- [ ] Wire cell-lint reusable workflow + tier-discipline vet (AC4).

## 🧠 Context

Expected tier placement: `cmd/*` = thin binaries; `internal/config`, `internal/server`. Engine analogue: a NestJS bootstrap module, but this is a Go satellite (P1 — never imports `@docmost/*`). Seams crossed: none yet (foundation). Sibling dependencies: none; this unblocks all.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** partial — six-tier tree, four thin binaries (`cmd/{api,triage,relay,sweep}/main.go`), env-only `internal/config/config.go` and the `/healthz`+`/readyz` cell-split (`internal/server/server.go` `Health`/`Ready` echo CELL_ID+CLUSTER_NAME) all exist and compile. Missing: the named DoD test `TestHealthzEchoesCellIdentity` is absent (only the store RLS test is in-tree; health/config handlers untested), and AC3 loud-fail-on-missing-env is NOT met — `config.Load` uses silent dev-convenience defaults (`getenv`/bare `os.Getenv`), never fatals.

## 🧪 Testing

Named DoD test `TestHealthzEchoesCellIdentity` (integration). Tiers: unit (config parse) + integration (boot). CS §5 mocking: mock nothing own; env is real. Never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3 (tier discipline)/4/6/7/11; SE-Arch `8sYi523i4t` lenses (fail-closed config, no silent defaults); cell-lint `JGAUQRsw2g` 1–14.

## 🔗 References

- PRD `mOzNDhn322` §9 (service shape); Architecture `GOacy9kdJz` (AD-1, Structural Seed, Consistency Conventions).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (Definition Pack — contract TAG is the dispatch gate); parent epic **E1**.
- [ ] Intra-service: first story; blocks E1-S2..S5 and all downstream.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
