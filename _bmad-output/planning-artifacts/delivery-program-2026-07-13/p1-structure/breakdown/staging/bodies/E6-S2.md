## 🎯 Story

As a **customer admin** I want **the Librarian to measurably improve per tenant from feedback — drawing few-shot routing/classification exemplars from recent Feedback Events, recalibrating Trust Tiers, and proposing revisions to its own prompt pack (never self-applied) — validated by a held-out exemplars-on/off harness, with admin-visible transparency (acceptance rate over time, per-category error breakdown, pack-revision changelog)** so that **autonomy is earned on evidence and every behavior change is attributable**.

**Definition of Done:** one named test `TestExemplarsOnOffHarnessMeasuresLearningEffect` (integration — a held-out labeled set shows exemplars-on vs off routing/classification accuracy with Trust-Tier composition held constant; a Librarian-proposed pack revision activates only via admin approval; the transparency read reports acceptance-over-time, error breakdown, and a changelog including rejected proposed revisions). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given recent Feedback Events, When triage runs, Then routing/classification exemplars are few-shot-injected and each injection is logged for attribution. *Assert: injection logged; exemplars tenant-scoped.* [Source: mOzNDhn322 FR-STG16; GOacy9kdJz AD-14]
- [ ] **AC2** — Given a proposed pack revision (manual, self-proposed, or marketplace), When it arrives, Then it activates only through admin approval — never self-applied. *Assert: no revision auto-activates.* [Source: mOzNDhn322 FR-STG16; GOacy9kdJz AD-14]
- [ ] **AC3** — Given the held-out harness, When run, Then it compares exemplars-on vs off on a rolling labeled set with tier composition held constant. *Assert: harness reports both arms; not the raw SM-2 trend.* [Source: mOzNDhn322 FR-STG16]
- [ ] **AC4 (transparency)** — Given the admin view, When read, Then it shows acceptance rate over time, per-category error breakdown, and a pack-revision changelog with measured effect (including never-accepted revisions with their rejection). *Assert: changelog includes rejected proposals.* [Source: mOzNDhn322 FR-STG17]

## 🔨 Tasks

- [ ] RED: `TestExemplarsOnOffHarnessMeasuresLearningEffect` (AC1, AC2, AC3).
- [ ] GREEN: exemplar selection + injection logging; tier recalibration hook; proposed-revision path (admin-gated); held-out harness; transparency read model (AC1–AC4).

## 🧠 Context

Tier placement: `internal/learning` (domain) + admin read API. Engine analogue: few-shot retrieval + an offline eval harness. Seams: `orvex-studio-ai` (uses exemplars). Sibling: E6-S1 feedback, E4-S2 tiers, E6-S4 marketplace revisions.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — learning application/transparency is not implemented — no exemplars, tiers, held-out harness, or changelog.

## 🧪 Testing

Named DoD `TestExemplarsOnOffHarnessMeasuresLearningEffect` (integration). Tiers: unit (harness metric) + integration. CS §5: fake `aiclient`; real store; never mock own `learning`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/11; SE-Arch `8sYi523i4t` (attributable-learning, no-self-apply, held-out-validity); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.3 FR-STG16/FR-STG17, §8 SM-2; Architecture `GOacy9kdJz` AD-14.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: exemplar/revision/transparency DTOs); parent epic **E6**; **E6-S1**, **E4-S2**.
- [ ] Cross-service: `orvex-studio-ai`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
