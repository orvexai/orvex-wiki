## 🎯 Story

As a **knowledge manager** I want **a beautification sweep that identifies applied pages failing the FR-STG14 mechanical done-bar (plus broken embeds / render errors) and proposes beautification diffs into the Review Queue — never auto below the dial threshold** so that **the wiki's existing content is progressively brought up to the beautification bar under human control (UJ-4)**.

**Definition of Done:** one named test `TestBeautifySweepProposesDiffsNeverAutoBelowDial` (integration — the sweep flags pages failing the mechanical done-bar and broken embeds, proposes beautification diffs into the Review Queue, and at `recommend`/`auto-apply-low-risk` never auto-applies a beautify below the threshold). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given applied pages, When swept, Then those failing the FR-STG14 done-bar (or with broken embeds/render errors) are flagged with proposed beautification diffs. *Assert: each flagged page carries a diff.* [Source: mOzNDhn322 FR-STG21]
- [ ] **AC2** — Given the dial, When a beautify would apply, Then it never auto-applies below the threshold. *Assert: at recommend, no auto beautify.* [Source: mOzNDhn322 FR-STG21; GOacy9kdJz AD-7/AD-13]
- [ ] **AC3** — Given an accepted beautify diff, When applied, Then it reuses the beautify-at-triage artifact path (immutable, applied-by-hash). *Assert: apply writes the artifact by hash, no re-author.* [Source: mOzNDhn322 FR-STG14/FR-STG21; GOacy9kdJz AD-13]
- [ ] **AC4 (scope)** — Given default scope, When sweeping, Then write-proposals target agent-authored pages; human pages flag-only unless opted in. *Assert: no beautify write-proposal on human pages by default.* [Source: mOzNDhn322 §4.4]

## 🔨 Tasks

- [ ] RED: `TestBeautifySweepProposesDiffsNeverAutoBelowDial` (AC1, AC2, AC4).
- [ ] GREEN: `internal/sweep` done-bar scanner + broken-embed detection; propose diffs via `internal/beautify` artifact path (AC1–AC4).

## 🧠 Context

Tier placement: `internal/sweep` + `internal/beautify` (reuses AD-13 artifact). Engine analogue: a lint-and-fix pass. Seams: `orvex-studio-ai` (re-author), wiki read. Sibling: E5-S2 beautify, E7-S1 executor, E4-S3 queue.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — beautification sweep is not implemented — no mechanical-bar flagging or proposed diffs.

## 🧪 Testing

Named DoD `TestBeautifySweepProposesDiffsNeverAutoBelowDial` (integration). Tiers: unit (done-bar scan) + integration. CS §5: fake `aiclient`/read clients; never mock own `sweep`/`beautify`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/5/6/11; SE-Arch `8sYi523i4t` (never-auto-below-dial, reuse-artifact); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.4 FR-STG21, §4.2 FR-STG14; Architecture `GOacy9kdJz` AD-13.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: done-bar spec, artifact shape); parent epic **E7**; **E5-S2**, **E7-S1**.
- [ ] Cross-service: `orvex-studio-ai`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
