## 🎯 Story

As a **knowledge manager** I want **the apply engine to execute accepted Proposals through the `orvex-wiki-api` chokepoint with CAS `ifVersion`, per-page block ops, per-item receipts, a per-(tenant,page) lease, snapshot-before-mutate for destructive ops / inverse-op rollback for additive ops, and a state-gate that pauses on partial failure with explicit applied-vs-pending members — driven by idempotent `/internal/*` step-APIs called from `orvex-studio-workflows`** so that **~100 documents apply safely and resumably, and a mid-apply failure never silently half-mutates the wiki**.

**Definition of Done:** one named test `TestApplyPausesOnPartialFailureWithExplicitState` (integration — an accepted ChangeSet applies via a faked wiki-api with per-item receipts; an induced mid-apply failure pauses naming applied vs pending members and forces resume/abort; a replay of a step-API is a no-op; a `delete-document` snapshots before mutating). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given accepted Proposals, When applied, Then writes go only through `wikiapiclient` with CAS `ifVersion` and per-item `ApplyReceipt`. *Assert: no engine-direct write; receipt per member.* [Source: mOzNDhn322 FR-STG13; GOacy9kdJz AD-4]
- [ ] **AC2** — Given a partial failure mid-apply, When it occurs, Then the ChangeSet pauses with explicit applied-vs-pending state; a human resumes or aborts. *Assert: state=paused; never silent partial success; ChangeSet cannot flip to applied with an unresolved member.* [Source: mOzNDhn322 FR-STG13; GOacy9kdJz AD-2/AD-4]
- [ ] **AC3** — Given a `/internal/*` step-API retry/replay, When re-invoked with the same `Idempotency-Key`, Then it is a no-op. *Assert: no double write; key ledger dedups.* [Source: mOzNDhn322 NFR-STG3; GOacy9kdJz AD-5]
- [ ] **AC4 (rollback)** — Given a destructive op, When applied, Then it snapshots before mutate (S3, pointer in Postgres); additive ops roll back via inverse Proposals. *Assert: snapshot exists pre-mutate; restore is full-fidelity.* [Source: mOzNDhn322 FR-STG13; GOacy9kdJz AD-4/AD-10]

## 🔨 Tasks

- [ ] RED: `TestApplyPausesOnPartialFailureWithExplicitState` (AC1, AC2, AC3).
- [ ] GREEN: `internal/apply` sequencing + lease + receipts; `/internal/*` idempotent step-APIs; snapshot/inverse rollback (AC1–AC4).

## 🧠 Context

Tier placement: `internal/apply` (domain) + `/internal/*` step-APIs; durable flow lives in `orvex-studio-workflows` (no own Temporal worker — D-WF-1). Engine analogue: a saga executor. Seams: wiki-api writes, workflows step-API. Sibling: E3-S3 conflict, E5-S2 beautify artifact, E2-S4 snapshots.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — `internal/apply` is a doc-only stub; `apply` and `internalApplyStep` routes return typed 501. No wiki-api CAS writes, receipts, pause/rollback, or step-APIs.

## 🧪 Testing

Named DoD `TestApplyPausesOnPartialFailureWithExplicitState` (integration). Tiers: unit (sequencing/lease) + integration. CS §5: fake `wikiapiclient` + workflows caller from goldens; never mock own `apply`/`lifecycle`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/2/3/5/6/7/11; SE-Arch `8sYi523i4t` (no-silent-partial, idempotent-step-API, snapshot-restore); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.2 FR-STG13, §7 NFR-STG3; Architecture `GOacy9kdJz` AD-2/AD-4/AD-5/AD-10; Addendum `P5R7eCXJA0` A3 (apply mechanics, throttle).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: `ApplyReceipt`, wiki-api write facade, workflows step-API); parent epic **E5**; **E3-S3**, **E5-S2**, **E2-S4**.
- [ ] Prerequisite (external): `orvex-wiki-api` write facade past 501; `orvex-studio-workflows` durable flow.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
