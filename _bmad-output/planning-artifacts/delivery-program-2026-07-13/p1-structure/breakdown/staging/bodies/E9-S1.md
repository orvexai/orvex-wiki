## 🎯 Story

As the **release gate owner** I want **the 100-Proposal single-session ChangeSet load benchmark run against non-501 `orvex-studio-ai` + `orvex-studio-knowledge` — measuring submit p95, submit-to-triaged p95, human bulk-clear interactions, and apply latency, with zero partial-silent failures — so its measured envelope is published as the NFR-STG1 contract** so that **the SLOs are fixed by observed reality, not provisional guesses, and "Done" is an observed pass**.

**Definition of Done:** one named test `TestHundredProposalBenchmarkMeetsPublishedEnvelope` (e2e — a 100-Proposal ChangeSet submits, triages, is bulk-cleared, and applies against real deps; measured submit p95, triage p95, interaction count, and apply latency are recorded and asserted against the benchmark-published envelope with zero partial-silent failures). *Final H1–H17 elaboration + exact contract tag/versions (incl. the final numeric SLOs) are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a 100-Proposal ChangeSet, When benchmarked, Then submit-to-triaged p95, bulk-clear interactions (≤10 common case), and apply latency are measured against real `ai`+`knowledge`. *Assert: all three metrics recorded.* [Source: mOzNDhn322 NFR-STG1, §8 SM-3]
- [ ] **AC2** — Given the run, When it completes, Then there are zero partial-silent failures. *Assert: any partial failure surfaced as an explicit pause, never silent.* [Source: mOzNDhn322 NFR-STG1; GOacy9kdJz AD-4]
- [ ] **AC3** — Given measured values, When the run publishes, Then its envelope becomes the NFR-STG1 contract (provisional targets replaced). *Assert: published numbers recorded as the contract; prior provisional values marked superseded.* [Source: mOzNDhn322 NFR-STG1]
- [ ] **AC4 (gate)** — Given deps still at 501, When the benchmark is attempted, Then it cannot bind (release gate not passable). *Assert: gate blocks until deps are non-501.* [Source: mOzNDhn322 NFR-STG1, FR-STG25]

## 🔨 Tasks

- [ ] RED: `TestHundredProposalBenchmarkMeetsPublishedEnvelope` scaffold + metric capture (AC1, AC2).
- [ ] GREEN: `test/e2e` benchmark harness against real deps; envelope publication (AC1–AC4).

## 🧠 Context

Tier placement: `test/e2e` (benchmark) + real deps. Engine analogue: a load/acceptance suite. Seams: all sibling services (real). Sibling: E2–E5 (the pipeline under test), E9-S3 (six-surface).

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — the 100-Proposal load benchmark / SLO release gate does not exist — no benchmark harness in-tree.

## 🧪 Testing

Named DoD `TestHundredProposalBenchmarkMeetsPublishedEnvelope` (e2e). Tiers: e2e against real `ai`+`knowledge`+wiki-api. CS §5: no mocks at this tier — real dependencies; the whole point is reality.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/6/7/11; SE-Arch `8sYi523i4t` (observed-pass, fake-done-antibody, benchmark-as-contract); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §7 NFR-STG1, §8 SM-3; Architecture `GOacy9kdJz` (Capability map, benchmark gate); brief.md Delivery Doctrine.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG + pack certification pins final SLO numbers); parent epic **E9**; ALL of E2–E7.
- [ ] Prerequisite (external): non-501 `ai`+`knowledge`; wiki-api write facade; engine `wiki.*` outbox.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
