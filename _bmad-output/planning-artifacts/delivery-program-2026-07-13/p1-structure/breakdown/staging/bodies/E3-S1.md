## 🎯 Story

As a **knowledge manager** I want **the Librarian to produce, for every Proposal in a ChangeSet, a Disposition + target route + routing-confidence score + rendered diff + one-line justification within SLO — orchestrated by the `cmd/triage` worker with all LLM calls delegated to `orvex-studio-ai`** so that **I review decisions, not raw dumps, and the staging service never grows its own model access**.

**Definition of Done:** one named test `TestTriageProducesDispositionWithConfidenceAndDiff` (integration — a submitted ChangeSet triaged against a faked `ai` yields, per Proposal, a Disposition from the frozen enum, a routing-confidence float in [0,1], a rendered diff, and a justification line; LLM egress happens only via `aiclient`). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a ChangeSet, When triaged, Then each Proposal gets a Disposition ∈ {apply-as-routed, apply-with-merge, duplicate-of, conflict, reject, divert-to-workgraph, escalate} + route + confidence + diff + justification. *Assert: all five fields present per Proposal.* [Source: mOzNDhn322 FR-STG7, Glossary Disposition]
- [ ] **AC2** — Given routing confidence, When produced, Then it is a float in [0,1] distinct from the producer Trust Tier. *Assert: confidence ∈ [0,1]; not derived from tier.* [Source: mOzNDhn322 FR-STG7; GOacy9kdJz AD-7]
- [ ] **AC3** — Given LLM needs, When triage runs, Then all calls go through `aiclient` under a per-tenant rate budget. *Assert: no direct provider SDK import; budget key present.* [Source: mOzNDhn322 FR-STG7; GOacy9kdJz AD-6]
- [ ] **AC4 (error-path)** — Given the ai service unavailable, When triage runs, Then the ChangeSet stays reviewable with an `escalate`/retry state, never a silent pass. *Assert: no Proposal silently gets `apply-as-routed` on ai failure.* [Source: no-fallbacks; GOacy9kdJz AD-6]

## 🔨 Tasks

- [ ] RED: `TestTriageProducesDispositionWithConfidenceAndDiff` (AC1, AC2, AC4).
- [ ] GREEN: `internal/triage` orchestration + `cmd/triage` worker; `aiclient` calls; Disposition/diff assembly (AC1–AC4).

## 🧠 Context

Tier placement: `internal/triage` (domain) + `cmd/triage` (worker). Engine analogue: an async classification pipeline. Seams: `orvex-studio-ai` (classify/route/justify). Sibling: E3-S2 routing, E3-S3 conflict, E5-S2 beautify (also at triage).

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — `internal/triage` is a doc-only stub and `cmd/triage` is a health-only skeleton (drain loop TODO). No disposition production.

## 🧪 Testing

Named DoD `TestTriageProducesDispositionWithConfidenceAndDiff` (integration). Tiers: unit (Disposition assembly) + integration (worker). CS §5: fake `aiclient` from contract goldens; never mock own `triage`/`routing`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/7/11; SE-Arch `8sYi523i4t` (single LLM egress, no-fallback failure lens); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.2 FR-STG7; Architecture `GOacy9kdJz` AD-6/AD-7.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: Disposition enum, ai classify contract, rate-budget key); parent epic **E3**; **E2**.
- [ ] Cross-service (soft-gated for real runs): `orvex-studio-ai` past 501.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
