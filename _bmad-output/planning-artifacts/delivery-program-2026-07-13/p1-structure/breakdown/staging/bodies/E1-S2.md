## 🎯 Story

As a **staging platform engineer** I want **contracts codegen wired from the pinned `orvex-studio-contracts` tag into `gen/`, plus the AGPL-import denylist and drift gate** so that **every seam DTO the service compiles against is generated from the frozen contract, and an AGPL leak or an out-of-date contract fails CI**.

**Definition of Done:** one named test `TestContractsDriftGateFailsOnStaleGen` (CI/tooling layer — regenerates `gen/` from the pinned tag, asserts the working tree is clean, and that a hand-edited `gen/` file or a forbidden `@docmost/*`/AGPL import is rejected). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the pinned contracts tag, When codegen runs, Then `gen/` is produced and committed. *Assert: `make gen` yields a clean `git diff` on a fresh checkout.* [Source: GOacy9kdJz P3+ADR-0008, CI/CD convention]
- [ ] **AC2** — Given a stale `gen/`, When the drift gate runs in CI, Then it fails. *Assert: CI job exits non-zero when `gen/` differs from regenerated output.* [Source: GOacy9kdJz AD-9 drift gates]
- [ ] **AC3 (negative)** — Given an AGPL/`@docmost/*` import added, When the denylist gate runs, Then it fails. *Assert: import-guard reports the banned path.* [Source: GOacy9kdJz P1/P10; AD-9 AGPL-import denylist]
- [ ] **AC4** — Given a reshaping contract change, When it lands, Then the ADR + human-ratify lane is required (additive stays automated). *Assert: gate distinguishes additive vs reshaping per ADR-0008.* [Source: GOacy9kdJz P3+ADR-0008]

## 🔨 Tasks

- [ ] RED: `TestContractsDriftGateFailsOnStaleGen` with a deliberately stale `gen/` fixture (AC2).
- [ ] GREEN: codegen target from the pinned tag; commit `gen/` (AC1).
- [ ] Add AGPL-import denylist + drift gate to the cell-lint reusable workflow (AC3, AC4).

## 🧠 Context

Tier placement: `gen/` (generated) + CI tooling. Engine analogue: OpenAPI client generation. Seams crossed: the contracts seam (read-only consumer). Sibling dependency: `orvex-studio-contracts` pinned tag; `orvex-studio-lib` typed clients.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** partial — the AGPL import-denylist IS present and enforced (`.golangci.yml` depguard `agpl-import-guard` denies `github.com/docmost` across `$all`). Missing: contracts CODEGEN is not wired — `gen/*.go` shapes are hand-authored (gen/doc.go: "Until the generator lands they are hand-authored"), and there is NO contracts drift gate (the only drift check, scripts/check-context-freshness.sh, guards the Coding-Standards copy, not the contract).

## 🧪 Testing

Named DoD `TestContractsDriftGateFailsOnStaleGen` (tooling/CI). Tiers: tooling. CS §5: no mocks — real generator, real fixtures. Never edit `gen/` by hand.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/5/9 (contract change authority)/10/11; SE-Arch `8sYi523i4t` (contract-first, AGPL boundary); cell-lint `JGAUQRsw2g`.

## 🔗 References

- Architecture `GOacy9kdJz` (P3/ADR-0008, AD-9, CI/CD convention); PRD `mOzNDhn322` §9 (contracts dependency).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (the contract TAG is the codegen source and dispatch gate); **E1-S1**; parent epic **E1**.
- [ ] Cross-service: `orvex-studio-contracts`, `orvex-studio-lib`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
