## 🎯 Story

As a **knowledge manager** I want **to schedule an accepted ChangeSet (or accepted subset) to apply at a future time — timezone-aware, cancellable until apply-start, with conflict detection re-run at fire time** so that **I can publish a batch on my schedule and a scheduled apply against moved pages pauses exactly like an immediate one**.

**Definition of Done:** one named test `TestScheduledApplyReRunsConflictAtFireInstant` (integration — a scheduled ChangeSet stores the UTC instant computed at creation plus the origin IANA tzid, fires at the instant, re-runs FR-STG9 conflict detection, and pauses on a moved base; cancellation before apply-start succeeds, after it does not). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a scheduled apply, When created, Then it stores the UTC instant + origin IANA tzid; firing uses the instant. *Assert: stored instant is UTC; tzid retained for display.* [Source: mOzNDhn322 FR-STG28; GOacy9kdJz AD-2]
- [ ] **AC2** — Given a scheduled apply at fire time, When it runs, Then conflict detection (FR-STG9) re-runs; a moved base pauses it. *Assert: moved page ⇒ pause, same as immediate.* [Source: mOzNDhn322 FR-STG28]
- [ ] **AC3** — Given a schedule, When cancelled before apply-start, Then it cancels. *Assert: state returns to accepted; no apply.* [Source: mOzNDhn322 FR-STG28]
- [ ] **AC4 (negative)** — Given a schedule past apply-start, When cancellation is attempted, Then it is rejected. *Assert: cancel fails once applying began.* [Source: mOzNDhn322 FR-STG28]

## 🔨 Tasks

- [ ] RED: `TestScheduledApplyReRunsConflictAtFireInstant` (AC1, AC2, AC4).
- [ ] GREEN: schedule state in `lifecycle`; Temporal schedule in `orvex-studio-workflows` firing the apply step-API; conflict re-run + cancel window (AC1–AC4).

## 🧠 Context

Tier placement: `internal/lifecycle` (schedule state) + `orvex-studio-workflows` (the timer). Engine analogue: Sanity Content Release scheduling. Seams: workflows step-API, wiki-api conflict re-check. Sibling: E5-S1 apply, E3-S3 conflict.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — scheduled publishing is not implemented — `internalPublishScheduled` returns typed 501; no UTC-instant+tzid, cancel, or conflict re-run.

## 🧪 Testing

Named DoD `TestScheduledApplyReRunsConflictAtFireInstant` (integration). Tiers: unit (instant/tzid) + integration (fire+conflict). CS §5: fake workflows caller + wiki-api; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/5/6/7/11; SE-Arch `8sYi523i4t` (tz-correctness, re-check-at-fire); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.6 FR-STG28; Architecture `GOacy9kdJz` AD-2/AD-5.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: schedule DTO, workflows schedule API); parent epic **E5**; **E5-S1**, **E3-S3**.
- [ ] Cross-service: `orvex-studio-workflows`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
