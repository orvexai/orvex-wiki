## 🎯 Story

As a **staging platform engineer** I want **the transactional outbox writer, the `cmd/relay` relay, a Knative-Trigger spine consumer, and OTel via `pkg/obs` with a PII deny-list** so that **every state change publishes a CloudEvent atomically with its DB write, and traces cross the outbox seam without ever leaking content**.

**Definition of Done:** one named test `TestOutboxPublishesAtomicallyWithStateWrite` (integration — a domain transition writes its row and an outbox row in one transaction; `cmd/relay` delivers it to `studio-spine`; a rolled-back transition publishes nothing; spans/labels contain no Proposal content or end-user text). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a state transition, When it commits, Then its outbox event commits in the same transaction. *Assert: a forced rollback leaves zero outbox rows; a commit leaves exactly one.* [Source: GOacy9kdJz P2+D-S13, AD-9]
- [ ] **AC2** — Given outbox rows, When `cmd/relay` runs, Then they publish to `studio-spine` as ADR-0007-enveloped CloudEvents. *Assert: envelope has `[specversion,id,source,type,orvexcell,orvextenant]`, `partitionkey`=tenant.* [Source: GOacy9kdJz ADR-0007/0010]
- [ ] **AC3** — Given a consumed event, When traced, Then trace context rides as DT extension attrs and the consumer LINKs. *Assert: consumer span links, not childOf.* [Source: GOacy9kdJz ADR-0016]
- [ ] **AC4 (negative)** — Given Proposal content or end-user text, When emitting spans/logs/metrics, Then it never appears. *Assert: PII deny-list scrub verified on a payload with known secret strings.* [Source: GOacy9kdJz ADR-0016, Observability convention]

## 🔨 Tasks

- [ ] RED: `TestOutboxPublishesAtomicallyWithStateWrite` (AC1, AC4).
- [ ] GREEN: outbox table + writer in the same tx; `cmd/relay`; Knative-Trigger consumer wiring; `pkg/obs` init with PII deny-list (AC1–AC4).

## 🧠 Context

Tier placement: `internal/event` (outbox + consumers), `cmd/relay`, `internal/obs`. Engine analogue: an outbox pattern over Kafka. Seams: `studio-spine` (Kafka). Sibling dependency: `orvex-studio-lib/pkg/events`, `pkg/obs`.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** partial — the transactional `outbox` table (event_type/payload/published_at + unpublished index) is in the baseline migration and `cmd/relay` boots a poll-loop skeleton with a health split. Missing: the drain/publish itself is a TODO (relay logs "publish TODO"), `internal/event` is a doc-only stub, and there is NO spine consumer and NO OTel instrumentation.

## 🧪 Testing

Named DoD `TestOutboxPublishesAtomicallyWithStateWrite` (integration). Tiers: integration (Postgres + a fake broker). CS §5: fake the broker at the boundary; never mock `pkg/events` internals.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/2/3/6/7/11; SE-Arch `8sYi523i4t` (event-spine, PII deny-list, trace-across-outbox); cell-lint `JGAUQRsw2g` (topic naming, partitions:1).

## 🔗 References

- Architecture `GOacy9kdJz` (P2/D-S13, ADR-0007/0010/0016, AD-9); PRD `mOzNDhn322` §7 NFR-STG4/NFR-STG6.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (event envelope contract + `studio.staging.*` subdomain ADR); **E1-S1**, **E1-S4**; parent epic **E1**.
- [ ] Intra-service: every state-machine story (E2-S2, E5-S1) emits through this.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
