## 🎯 Story

As a **knowledge manager** I want **hard conflict detection that re-checks each Proposal's base-version against the live page through the authoritative content path (wiki-api/ydoc-backed, never the ≤45s-stale content column), re-resolves section anchors against stable block IDs after every write, and yields a `conflict` Disposition for any moved base — never a silent three-way prose merge** so that **a human edit is never silently overwritten and an anchor is never guessed**.

**Definition of Done:** one named test `TestMovedBaseYieldsConflictNeverMerge` (integration — a Proposal whose base-version moved since submit yields a `conflict`; a lost section anchor after a preceding write yields a `conflict`; no code path performs a three-way prose merge). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a page whose version moved since submit, When conflict is re-checked, Then the Proposal gets `conflict` requiring human resolution. *Assert: Disposition = conflict; re-check read the ydoc-backed path, not the stale content column.* [Source: mOzNDhn322 FR-STG9; GOacy9kdJz AD-3]
- [ ] **AC2** — Given multiple section Proposals on one page, When applied in sequence, Then each subsequent anchor re-resolves against stable block IDs; a lost anchor is a `conflict`. *Assert: no anchor guessed; lost anchor → conflict.* [Source: mOzNDhn322 FR-STG9; GOacy9kdJz AD-3]
- [ ] **AC3 (negative)** — Given two conflicting Proposals, When resolved, Then no automatic three-way prose merge occurs. *Assert: no merge codepath; human picks rebase-and-re-review or reject.* [Source: mOzNDhn322 FR-STG9, §5]
- [ ] **AC4** — Given base-version stamps, When compared, Then only the wiki-api version token (not timestamps) is the CAS input. *Assert: timestamp fields never drive the decision.* [Source: GOacy9kdJz AD-3]

## 🔨 Tasks

- [ ] RED: `TestMovedBaseYieldsConflictNeverMerge` (AC1, AC2, AC3).
- [ ] GREEN: authoritative re-read via `wikiapiclient`; anchor re-resolution against block IDs; conflict-keying `(document_id, section_anchor)` (AC1–AC4).

## 🧠 Context

Tier placement: `internal/routing`/`internal/triage` (conflict check) feeding `internal/apply`. Engine analogue: CAS `ifVersion`, but read through the ydoc-backed authoritative path. Seams: wiki-api authoritative read. Sibling: E2-S1 base-version stamp, E5-S1 apply sequencing.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — hard conflict detection is not implemented — no authoritative read, anchor re-resolution, or no-auto-merge guard.

## 🧪 Testing

Named DoD `TestMovedBaseYieldsConflictNeverMerge` (integration). Tiers: unit (conflict decision) + integration (authoritative read). CS §5: fake `wikiapiclient` with a moved-version fixture; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/11; SE-Arch `8sYi523i4t` (no-silent-merge, stale-read hazard lens); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.2 FR-STG9, §5; Architecture `GOacy9kdJz` AD-3; Addendum `P5R7eCXJA0` A3 (≤45s read-after-write).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: wiki-api authoritative read + version token); parent epic **E3**; **E2-S1**.
- [ ] Intra-service: consumed by E5-S1 apply.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
