## 🎯 Story

As a **knowledge manager** I want **the `cmd/sweep` executor plus a corpus-scale duplicate & merge sweep that detects near-duplicate pages and recommends whole-doc supersede-merge (to archive, with two-sided supersession links — P5 semantics) into the same Review Queue under the same dial** so that **the wiki de-duplicates over time (SM-5) without ever silently mutating a page**.

**Definition of Done:** one named test `TestDuplicateSweepRecommendsSupersedeMergeUnderDial` (integration — the sweep finds near-duplicates via `knowledgeclient`, emits `duplicate-of`/supersede-merge recommendations into the Review Queue, and at `recommend` nothing is applied without a human accept; supersession is whole-doc with two-sided links). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the corpus, When the sweep runs, Then near-duplicates are detected and a supersede-merge (whole-doc, archive, two-sided links) is recommended. *Assert: recommendation carries winner + loser + both-sided links.* [Source: mOzNDhn322 FR-STG18]
- [ ] **AC2** — Given recommendations, When produced, Then they enter the same Review Queue governed by the same dial. *Assert: at `recommend`, nothing applied without human accept.* [Source: mOzNDhn322 FR-STG18, §4.4; GOacy9kdJz AD-7]
- [ ] **AC3** — Given sweep scope, When default, Then write-proposals target agent-authored pages; human-authored pages get flag-only unless opted in. *Assert: no write-proposal against a human-authored page by default.* [Source: mOzNDhn322 §4.4; GOacy9kdJz AD-7]
- [ ] **AC4 (budget)** — Given sweep recommendations, When queued, Then they carry a separate daily interaction budget from intake ChangeSets. *Assert: budgets are distinct.* [Source: mOzNDhn322 NFR-STG1; GOacy9kdJz AD-7]

## 🔨 Tasks

- [ ] RED: `TestDuplicateSweepRecommendsSupersedeMergeUnderDial` (AC1, AC2, AC3).
- [ ] GREEN: `internal/sweep` duplicate detection via `knowledgeclient`; supersede-merge recommendation; `cmd/sweep` executor dispatched by the central workflows schedule (AC1–AC4).

## 🧠 Context

Tier placement: `internal/sweep` (domain) + `cmd/sweep` executor; schedule lives in `orvex-studio-workflows`. Engine analogue: doc-consolidate. Seams: `orvex-studio-knowledge` (dedup), Review Queue. Sibling: E3-S2 routing dedup, E4-S3 queue, E5-S1 apply.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — `internal/sweep` is a doc-only stub, `cmd/sweep` is a health-only skeleton, and `internalSweepDispatch` returns typed 501. No sweep executor or duplicate/merge sweep.

## 🧪 Testing

Named DoD `TestDuplicateSweepRecommendsSupersedeMergeUnderDial` (integration). Tiers: unit (recommendation) + integration. CS §5: fake `knowledgeclient`; never mock own `sweep`/`policy`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/5/6/11; SE-Arch `8sYi523i4t` (recommend-under-dial, no-silent-mutate); cell-lint `JGAUQRsw2g`; doc-consolidate P5 semantics.

## 🔗 References

- PRD `mOzNDhn322` §4.4 FR-STG18, §8 SM-5; Architecture `GOacy9kdJz` AD-6/AD-7.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: knowledge dedup, sweep schedule step-API); parent epic **E7**; **E4**, **E5**.
- [ ] Cross-service: `orvex-studio-knowledge`, `orvex-studio-workflows`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
