## 🎯 Story

As a **knowledge manager** I want **the Librarian to beautify Proposal content at triage — re-authoring to the wiki's full ProseMirror capability before review, so the rendered diff I approve IS the final content — stored as an immutable sha256-addressed artifact that apply writes verbatim by hash** so that **review-what-you-apply holds, and beautify LLM latency stays out of the apply SLO**.

**Definition of Done:** one named test `TestBeautifyArtifactIsImmutableAndAppliedByHash` (integration — triage re-authors content to registered PM nodes meeting the mechanical done-bar, stores an immutable sha256 artifact; apply writes exactly that artifact by hash and never re-authors; section ops beautify only their section). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Proposal at triage, When beautified, Then the rendered diff the reviewer approves is the final content, stored as an immutable sha256 artifact. *Assert: apply writes the artifact by hash; no re-author at apply.* [Source: mOzNDhn322 FR-STG14; GOacy9kdJz AD-13]
- [ ] **AC2** — Given the mechanical done-bar, When beautified, Then: TL;DR/lead on doc-level ops; only schema-registered nodes (zero stripped-node warnings); contiguous headings; enumerable content as structured nodes. *Assert: done-bar checks pass.* [Source: mOzNDhn322 FR-STG14]
- [ ] **AC3** — Given a section-scoped Operation, When beautified, Then only the affected section is re-authored (whole-page re-author only for add/replace-document); the section's block anchor ID is preserved verbatim. *Assert: no re-anchoring from our own beautify.* [Source: mOzNDhn322 FR-STG14; GOacy9kdJz AD-3/AD-13]
- [ ] **AC4 (verify)** — Given applied output, When verified, Then equality is full-text + effective-marks per section (engine coalesces same-mark runs). *Assert: verification uses effective-marks, not run-by-run.* [Source: GOacy9kdJz AD-13; docmost-content-json-coalesces]

## 🔨 Tasks

- [ ] RED: `TestBeautifyArtifactIsImmutableAndAppliedByHash` (AC1, AC3, AC4).
- [ ] GREEN: `internal/beautify` re-author via `aiclient`; done-bar checks; sha256 artifact store; anchor-preserving section scope (AC1–AC4).

## 🧠 Context

Tier placement: `internal/beautify` (domain), runs at triage. Engine analogue: PM-JSON authoring only registered nodes. Seams: `orvex-studio-ai` (re-author), S3 artifact store. Sibling: E3-S1 triage, E5-S1 apply (writes the artifact), E7-S4 beautify sweep.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — `internal/beautify` is a doc-only stub — no triage-time re-author, mechanical done-bar, or sha256 artifact.

## 🧪 Testing

Named DoD `TestBeautifyArtifactIsImmutableAndAppliedByHash` (integration). Tiers: unit (done-bar) + integration (artifact round-trip). CS §5: fake `aiclient`; real hashing; never mock own `beautify`/`apply`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/5/6/7/11; SE-Arch `8sYi523i4t` (approved-diff=applied-bytes, no-silent-strip); cell-lint `JGAUQRsw2g`; docmost-diagram/PM authoring conventions.

## 🔗 References

- PRD `mOzNDhn322` §4.2 FR-STG14; Architecture `GOacy9kdJz` AD-13; Addendum `P5R7eCXJA0` A3 (registered-nodes, coalescing).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: authoring-conventions canon slug, artifact shape); parent epic **E5**; **E3-S1**.
- [ ] Intra-service: consumed by E5-S1 apply.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
