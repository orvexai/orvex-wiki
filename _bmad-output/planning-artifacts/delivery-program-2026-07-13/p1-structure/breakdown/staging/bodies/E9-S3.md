## 🎯 Story

As the **release gate owner** I want **the six-surface fresh-tenant E2E protocol (api / mcp / cli / ai / rag / knowledge-sync) run against real data and human-verified, plus the P8 isolation probes (cross-tenant, intra-tenant restricted-content, count-oracle) in CI and post-deploy** so that **staging's security/tenancy (NFR-STG5) and observability (NFR-STG6) are proven by an observed pass, never a reported one — the fake-done antibody**.

**Definition of Done:** one named test `TestSixSurfaceFreshTenantE2EAndIsolationProbes` (e2e — a fresh tenant with a real identity-minted token drives propose→triage→review→apply across api/mcp/cli, with ai classification, rag routing, and knowledge-sync reindex all exercised; the P8 isolation probes pass in CI and post-deploy; observability metrics/events are emitted with no PII). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a fresh tenant + real token, When the six surfaces are driven, Then propose (mcp/cli) → triage (ai) → route (rag/knowledge) → apply → reindex (knowledge-sync) all pass on real data, human-verified. *Assert: each surface exercised; pass reproduced, not reported.* [Source: mOzNDhn322 §7; brief.md six-surface protocol]
- [ ] **AC2** — Given the P8 isolation probes, When run in CI and post-deploy, Then cross-tenant, intra-tenant restricted-content, and count-oracle probes pass. *Assert: all three probes green in both environments.* [Source: mOzNDhn322 NFR-STG5; GOacy9kdJz AD-8/P8]
- [ ] **AC3** — Given observability, When exercised, Then queue depth/age, acceptance rates, apply latencies, and auto-apply counts are metrics; domain events are CloudEvents; no PII in spans/logs/labels. *Assert: metrics present; PII deny-list holds.* [Source: mOzNDhn322 NFR-STG6; GOacy9kdJz ADR-0016]
- [ ] **AC4 (fake-done antibody)** — Given the gate, When claimed Done, Then it is an observed, reproduced pass. *Assert: gate cannot be marked green from a reported-only result.* [Source: brief.md Delivery Doctrine; current-state-map §4]

## 🔨 Tasks

- [ ] RED: `TestSixSurfaceFreshTenantE2EAndIsolationProbes` scaffold (AC1, AC2).
- [ ] GREEN: e2e harness driving all six surfaces on a fresh tenant; wire P8 probes into CI + post-deploy; observability assertions (AC1–AC4).

## 🧠 Context

Tier placement: `test/e2e` + CI/post-deploy probes. Engine analogue: an acceptance protocol on the dev cell. Seams: every sibling (real). Sibling: E9-S1 benchmark, E8 surfaces, E1-S5 obs. This gate precedes E8-S4 hard cut.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — no six-surface E2E and no fresh-tenant real-data pass exist; the only isolation probe in-tree is the DB-level store RLS test (E1-S4), not the P8 cross-surface suite.

## 🧪 Testing

Named DoD `TestSixSurfaceFreshTenantE2EAndIsolationProbes` (e2e). Tiers: e2e + CI probes. CS §5: no mocks — real services, real data; the point is reality.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/6/7/11; SE-Arch `8sYi523i4t` (observed-pass, isolation-probes, PII-deny); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §7 NFR-STG5/NFR-STG6; Architecture `GOacy9kdJz` AD-8/ADR-0016; brief.md Delivery Doctrine; current-state-map §4 (fake-done).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG); parent epic **E9**; ALL of E1–E8 (except E8-S4, which this gate precedes).
- [ ] Prerequisite (external): non-501 `ai`+`knowledge`; wiki-api write facade; engine `wiki.*` outbox.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
