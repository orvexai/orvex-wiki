## 🎯 Story

As a **knowledge manager** I want **a Guru-style staleness & verification sweep — verifier + next-review-due stamps on applied pages, a re-verification queue when due or when drift signals fire — plus the retrieval trust-gating contract so agents ground only on above-threshold content (enforcement living inside `orvex-studio-knowledge` as a v1 dependency deliverable)** so that **stale knowledge is re-reviewed and agents never ground on untrustworthy content**.

**Definition of Done:** one named test `TestStalenessQueueEnqueuesOnDueAndDrift` (integration — a page past its next-review-due, or with a drift signal, is enqueued for re-verification into the Review Queue; the retrieval trust-gating contract is exercised against a faked knowledge enforcer so above-threshold content is groundable and below-threshold is not). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given applied pages, When they carry verifier + next-review-due stamps, Then a page past due enters the re-verification queue. *Assert: due page enqueued.* [Source: mOzNDhn322 FR-STG19]
- [ ] **AC2** — Given a drift signal, When it fires, Then the affected page is queued for re-review even before its due date. *Assert: drift ⇒ enqueue.* [Source: mOzNDhn322 FR-STG19]
- [ ] **AC3** — Given retrieval trust-gating, When an agent grounds, Then only above-threshold content is retrievable (enforcement in knowledge; the gating contract is honored). *Assert: below-threshold content excluded from grounding.* [Source: mOzNDhn322 FR-STG19; GOacy9kdJz AD-6]
- [ ] **AC4 (contract)** — Given the gating contract, When authored, Then it lands in `orvex-studio-contracts`. *Assert: the trust-gating shape is a contract artifact, not staging-local prose.* [Source: mOzNDhn322 FR-STG19; GOacy9kdJz AD-6]

## 🔨 Tasks

- [ ] RED: `TestStalenessQueueEnqueuesOnDueAndDrift` (AC1, AC2, AC3).
- [ ] GREEN: `internal/sweep` staleness detection + verifier stamps; re-verification enqueue; trust-gating contract call to `knowledgeclient` (AC1–AC4).

## 🧠 Context

Tier placement: `internal/sweep` (domain); enforcement is knowledge's. Engine analogue: Guru freshness/verification. Seams: `orvex-studio-knowledge` (trust-gating enforcer). Sibling: E7-S1 sweep executor, E7-S5 groundability flag.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — staleness & verification queue + retrieval trust-gating contract are not implemented.

## 🧪 Testing

Named DoD `TestStalenessQueueEnqueuesOnDueAndDrift` (integration). Tiers: unit (due/drift) + integration. CS §5: fake `knowledgeclient` enforcer from goldens; never mock own `sweep`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/5/6/9/11; SE-Arch `8sYi523i4t` (freshness-gate, cross-service-contract); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.4 FR-STG19, §11 OQ2; Architecture `GOacy9kdJz` AD-6; evidence digest §6 (trust-gating dep).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: trust-gating contract); parent epic **E7**; **E7-S1**.
- [ ] Cross-service: `orvex-studio-knowledge` (v1 dependency deliverable — enforcement).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
