## 🎯 Feature area

The 100-Proposal single-session ChangeSet is the benchmarked contract — "no vendor in the field publishes this; we do" (PRD §7). This epic proves the cross-cutting NFRs end-to-end and is the **release gate**: the load benchmark that fixes the provisional SLOs (submit p95, triage ≤5 min, apply ≤15 min, ≤10 interactions) once run against non-501 `ai`+`knowledge` (NFR-STG1); isolation + auditability (apply-rate throttling, per-tenant fairness, zero engine schema coupling, immutable audit events — NFR-STG2/NFR-STG4); and the six-surface fresh-tenant E2E protocol (api/mcp/cli/ai/rag/knowledge-sync) with the P8 isolation probes (NFR-STG5/NFR-STG6).

Why it exists: the delivery doctrine says "Done" is an observed pass, never a reported one (the fake-done antibody) — and `TestM5KnowledgeE2E` was fake-done'd RED. This epic is the human-verified, real-data acceptance floor for staging; its measured envelope becomes the published NFR-STG1 contract. [Source: mOzNDhn322 §7/§8; brief.md Delivery Doctrine; current-state-map §4]

## 📋 Stories

- [ ] E9-S1 — 100-Proposal load benchmark: submit/triage/apply SLOs measured against real deps, envelope published as the contract — NFR-STG1
- [ ] E9-S2 — Isolation & auditability: apply-rate throttle, per-tenant fairness, zero engine coupling, immutable audit on the spine — NFR-STG2, NFR-STG4
- [ ] E9-S3 — Six-surface E2E + P8 isolation probes: fresh-tenant real-data pass, cross-tenant/count-oracle probes in CI + post-deploy — NFR-STG5, NFR-STG6

## 🔩 FR / NFR ids owned

NFR-STG1, NFR-STG2, NFR-STG4, NFR-STG5, NFR-STG6; validates SM-1/SM-2/SM-3/SM-4 in aggregate.

## ✅ Definition of Done

- [ ] All 3 stories Done (reviewer ≠ implementer).
- [ ] Family-E2E share green: the six-surface protocol passes on a fresh tenant with real data, human-verified and reproduced (not reported); the benchmark run publishes numeric SLOs; isolation probes are green in CI and post-deploy.
- [ ] SDD lines evidenced: no fake-done — the gate is an observed pass; audit history is immutable and content-free on erasure.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG + the pack certification that pins final SLO numbers); ALL of E1–E8.
- [ ] Prerequisite (external): non-501 `orvex-studio-ai` + `orvex-studio-knowledge`; engine outbox emitting `wiki.*`; `orvex-wiki-api` write facade live.
- [ ] Gates: FR-STG25 hard cut (the LAST release step) may only proceed after this epic is green.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
