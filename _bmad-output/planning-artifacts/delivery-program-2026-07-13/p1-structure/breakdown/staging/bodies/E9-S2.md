## 🎯 Story

As an **Orvex operator** I want **isolation and auditability proven — apply-rate throttling toward the engine, per-tenant fairness, zero engine schema coupling (own Postgres, contracts-only integration), and every recommendation/decision/auto-apply-justification/apply-receipt as an immutable audit event on the spine** so that **staging never degrades the wiki or other tenants and the decision history is a complete, immutable audit trail**.

**Definition of Done:** one named test `TestApplyThrottleFairnessAndImmutableAudit` (integration — apply-rate throttling toward the engine is per-tenant and config-owned; per-tenant fairness holds under a competing-tenant load; staging has zero engine-schema coupling; every decision/auto-apply/receipt is an immutable audit event on the spine). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given apply toward the engine, When it runs, Then it self-throttles per-tenant (config-owned, never hardcoded). *Assert: throttle rate sourced from config; engine not overrun.* [Source: mOzNDhn322 NFR-STG2; GOacy9kdJz AD-4/AD-11; Addendum A3]
- [ ] **AC2** — Given two tenants applying concurrently, When throttled, Then per-tenant fairness holds. *Assert: neither tenant starves the other.* [Source: mOzNDhn322 NFR-STG2]
- [ ] **AC3** — Given staging's store, When integrated, Then there is zero engine schema coupling (own Postgres, contracts-only). *Assert: no cross-DB read/FK to the engine; integration via contracts API only.* [Source: mOzNDhn322 NFR-STG2; GOacy9kdJz AD-15]
- [ ] **AC4 (audit)** — Given every recommendation/decision/auto-apply/receipt, When produced, Then it is an immutable audit event on the spine; erasure leaves content-free tombstones. *Assert: audit is append-only; erase preserves immutability via tombstone.* [Source: mOzNDhn322 NFR-STG4; GOacy9kdJz AD-9/AD-10]

## 🔨 Tasks

- [ ] RED: `TestApplyThrottleFairnessAndImmutableAudit` (AC1, AC2, AC4).
- [ ] GREEN: config-owned per-tenant apply throttle; fairness scheduling; audit-event emission on every decision/receipt (AC1–AC4).

## 🧠 Context

Tier placement: `internal/apply` (throttle) + `internal/event` (audit). Engine analogue: rate-limiter + audit log. Seams: wiki-api (throttled writes), spine (audit). Sibling: E5-S1 apply, E1-S5 outbox, E2-S5 quota.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** partial — the per-tenant ISOLATION floor is present and proven (RLS fail-closed baseline + `TestBaselineRLSFailsClosed`). Missing: the apply-rate throttle, per-tenant fairness scheduler, and immutable audit journal named by this story are all ABSENT.

## 🧪 Testing

Named DoD `TestApplyThrottleFairnessAndImmutableAudit` (integration). Tiers: unit (throttle/fairness) + integration (audit). CS §5: fake wiki-api + broker from goldens; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/2/3/6/7/11; SE-Arch `8sYi523i4t` (isolation, per-tenant-fairness, immutable-audit); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §7 NFR-STG2/NFR-STG4; Architecture `GOacy9kdJz` AD-4/AD-9/AD-10/AD-15; Addendum `P5R7eCXJA0` A3.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: audit event shapes); parent epic **E9**; **E5-S1**, **E1-S5**.
- [ ] Cross-service: `orvex-wiki-api`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
