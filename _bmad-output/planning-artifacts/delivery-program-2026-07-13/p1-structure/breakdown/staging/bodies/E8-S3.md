## 🎯 Story

As the **console and other consumers** I want **every staging state change to emit a typed `studio.staging.<resource>.<past-tense>` CloudEvent from the transactional outbox (queue/decision/audit telemetry), with the event catalog + subdomain authored in `orvex-studio-contracts` — while wiki reindexing rides the ENGINE's `wiki.*` events, so staging events never trigger a second reindex** so that **operators see queue metrics and audit trails without staging ever becoming a reindex source of truth**.

**Definition of Done:** one named test `TestStagingEventsAreTelemetryNotReindexTrigger` (integration — a staging state change emits a `studio.staging.*` CloudEvent with the ADR-0007 envelope carrying queue/decision/audit telemetry; no staging event triggers a wiki reindex; reindex rides `wiki.*` only). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a state change, When emitted, Then it is a `studio.staging.<resource>.<past-tense>` CloudEvent (ADR-0007 envelope) from the outbox. *Assert: type + envelope required set present; from outbox, not ad-hoc.* [Source: mOzNDhn322 FR-STG26; GOacy9kdJz AD-9]
- [ ] **AC2** — Given the events, When consumed, Then they carry queue/decision/audit telemetry and console consumes queue metrics. *Assert: telemetry fields present; console consumer receives them.* [Source: mOzNDhn322 FR-STG26; NFR-STG6]
- [ ] **AC3** — Given an applied write, When reindex happens, Then it rides the engine's `wiki.*` events — staging events never trigger a second reindex. *Assert: no reindex from a staging event.* [Source: mOzNDhn322 FR-STG26; GOacy9kdJz AD-9]
- [ ] **AC4 (contract)** — Given the new subdomain, When added, Then event types + payload schemas + topic-domain addition are authored in `orvex-studio-contracts` (additive lane; subdomain via ADR). *Assert: catalog entry exists; drift gate green.* [Source: mOzNDhn322 FR-STG26; GOacy9kdJz AD-9]

## 🔨 Tasks

- [ ] RED: `TestStagingEventsAreTelemetryNotReindexTrigger` (AC1, AC3).
- [ ] GREEN: emit typed events from `internal/event` outbox per state change; author the catalog + subdomain in contracts; console consumer wiring (AC1–AC4).

## 🧠 Context

Tier placement: `internal/event` (outbox) + contracts. Engine analogue: domain events as CloudEvents. Seams: `studio-spine`, contracts catalog, console consumer. Sibling: E1-S5 outbox, E2-S2 lifecycle emits, E5-S1 apply emits.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — the `outbox` table can carry `studio.staging.<resource>.<past-tense>` types, but NO events are ever emitted (relay drain is TODO) and there is no CloudEvents catalog; `internal/event` is a doc-only stub.

## 🧪 Testing

Named DoD `TestStagingEventsAreTelemetryNotReindexTrigger` (integration). Tiers: unit (event schema) + integration (emit + consume). CS §5: fake broker + consumer from goldens; never mock own `event`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/2/6/9/11; SE-Arch `8sYi523i4t` (telemetry-not-truth, single-reindex-source); cell-lint `JGAUQRsw2g` (topic naming).

## 🔗 References

- PRD `mOzNDhn322` §4.5 FR-STG26; Architecture `GOacy9kdJz` AD-9, ADR-0007/0010.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: `studio.staging.*` catalog + subdomain ADR); parent epic **E8**; **E1-S5**.
- [ ] Prerequisite (external): engine outbox emitting `wiki.*` (reindex trigger).
- [ ] Cross-service: `orvex-studio-console`, `orvex-studio-contracts`.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
