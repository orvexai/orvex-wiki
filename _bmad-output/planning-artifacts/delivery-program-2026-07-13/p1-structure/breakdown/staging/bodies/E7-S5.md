## 🎯 Story

As an **Orvex operator** I want **an AI-groundability flag + review-state gate — pages can be human-visible but not AI-groundable/editable (decoupled from human ACLs), stored in wiki-api page-meta and mutated only through wiki-api, with knowledge reading it from the wiki side — and content auto-applied under the dial is not agent-groundable until a human review flips the flag** so that **the AI-writes→AI-grounds→AI-writes amplification loop is broken by construction (SM-C3 = zero)**.

**Definition of Done:** one named test `TestAutoAppliedUnreviewedIsNotGroundableUntilReview` (integration — a page marked not-groundable is excluded from agent grounding; content auto-applied-but-unreviewed is not agent-groundable; a post-hoc human review flips the flag to groundable; the flag lives in wiki-api page-meta and is mutated only through wiki-api). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a page marked human-visible-but-not-groundable, When an agent grounds, Then it is excluded (decoupled from human ACLs). *Assert: not-groundable page absent from grounding results.* [Source: mOzNDhn322 FR-STG22]
- [ ] **AC2** — Given content auto-applied under the dial without human review, When an agent grounds, Then it is not groundable until reviewed. *Assert: SM-C3 grounding-from-unreviewed count = 0.* [Source: mOzNDhn322 FR-STG22, §8 SM-C3]
- [ ] **AC3** — Given a post-hoc human review, When completed, Then the flag flips to groundable. *Assert: review flips the flag; content becomes groundable.* [Source: mOzNDhn322 FR-STG22]
- [ ] **AC4 (store)** — Given the flag, When mutated, Then it lives in wiki-api page-meta, is mutated only through wiki-api, and knowledge reads it from the wiki side — never from staging. *Assert: staging never stores the flag; wiki-api is the mutation path.* [Source: GOacy9kdJz AD-7]

## 🔨 Tasks

- [ ] RED: `TestAutoAppliedUnreviewedIsNotGroundableUntilReview` (AC1, AC2, AC3).
- [ ] GREEN: page-meta flag writes via `wikiapiclient`; auto-apply sets not-groundable; review-flip path; knowledge reads flag from wiki side (AC1–AC4).

## 🧠 Context

Tier placement: `internal/apply`/`internal/policy` set the flag via wiki-api; enforcement is knowledge's. Engine analogue: SharePoint groundability decoupled from ACL. Seams: wiki-api page-meta, knowledge retrieval. Sibling: E5-S1 apply, E7-S2 trust-gating, E4-S1 dial.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — AI-groundability flag + review-state gate are not implemented — no page-meta write via wiki-api.

## 🧪 Testing

Named DoD `TestAutoAppliedUnreviewedIsNotGroundableUntilReview` (integration). Tiers: unit (flag transitions) + integration. CS §5: fake `wikiapiclient`/`knowledgeclient` from goldens; never mock own packages.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/5/6/11; SE-Arch `8sYi523i4t` (break-amplification-loop, single-mutation-path); cell-lint `JGAUQRsw2g`.

## 🔗 References

- PRD `mOzNDhn322` §4.4 FR-STG22, §8 SM-C3; Architecture `GOacy9kdJz` AD-7.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: page-meta groundability flag, knowledge read contract); parent epic **E7**; **E5-S1**, **E7-S2**.
- [ ] Cross-service: `orvex-wiki-api` (page-meta store), `orvex-studio-knowledge` (enforcer).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
