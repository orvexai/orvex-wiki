## 🎯 Story

As a **customer admin** I want **a v1-minimal Librarian Prompt-Pack marketplace on the existing Studio marketplace seam — publish, install, and rate versioned packs that are content-free of tenant data by construction, where an install lands as a proposed pack revision routed through the admin-approval path (never silently active)** so that **customers can share Librarian judgment safely without leaking data or auto-activating unreviewed policy**.

**Definition of Done:** one named test `TestMarketplaceInstallLandsAsProposedRevision` (integration — a published pack is versioned and content-free; an install creates a proposed revision requiring admin approval, never silently active; rate is recorded; no revenue-share path exists in v1). *Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2094); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a pack, When published, Then it is versioned and content-free of tenant data by construction. *Assert: publish rejects a pack carrying tenant content; version assigned.* [Source: mOzNDhn322 FR-STG29; GOacy9kdJz AD-14]
- [ ] **AC2** — Given an install, When performed, Then it lands as a proposed pack revision through the admin-approval path — never silently active. *Assert: installed pack is inactive until admin approves.* [Source: mOzNDhn322 FR-STG29]
- [ ] **AC3** — Given the v1-minimal scope, When exercised, Then publish/install/rate work and no revenue-share exists. *Assert: only the three verbs; no payout path.* [Source: mOzNDhn322 FR-STG29]
- [ ] **AC4 (negative)** — Given a pack with embedded tenant content, When published, Then it is rejected. *Assert: content-free guard blocks it.* [Source: mOzNDhn322 FR-STG27/FR-STG29]

## 🔨 Tasks

- [ ] RED: `TestMarketplaceInstallLandsAsProposedRevision` (AC2, AC4).
- [ ] GREEN: pack publish/install/rate over the `studio_marketplace_*` seam; content-free guard; install→proposed-revision via the FR-STG16 admin path (AC1–AC4).

## 🧠 Context

Tier placement: `internal/learning` (packs) + marketplace seam integration. Engine analogue: the POC GitHub-import→vet→claim pipeline, reused minimally. Seams: Studio marketplace (`studio_marketplace_*`). Sibling: E6-S2 revisions, E6-S3 cold-start content-free rule.

**🧾 Code audit (origin/dev @ 0dc7780, 2026-07-14):** absent — prompt-pack marketplace is not implemented — no publish/install/rate or admin-approval flow.

## 🧪 Testing

Named DoD `TestMarketplaceInstallLandsAsProposedRevision` (integration). Tiers: unit (content-free guard) + integration. CS §5: fake the marketplace seam from goldens; never mock own `learning`.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/5/6/11; SE-Arch `8sYi523i4t` (no-silent-activation, content-free-by-construction); cell-lint `JGAUQRsw2g`. Scope-risk flagged (FR-STG29 maturity note) — not re-litigated.

## 🔗 References

- PRD `mOzNDhn322` §4.6 FR-STG29, §12; Architecture `GOacy9kdJz` AD-14, Deferred (marketplace mechanics).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2094** (contract TAG: marketplace seam + pack DTO); parent epic **E6**; **E6-S2**.
- [ ] Cross-service: Studio marketplace surface.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
