## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As an **operator during a total IdP outage**, I want an IdP-independent offline-sealed break-glass admin credential, scoped to the **console admin plane only**, so that a Clerk/Keycloak outage or lockout cannot leave the platform with no locus of control.

**Definition of Done:** `TestBreakGlassIdpIndependent` (integration layer) — through the restricted admin surface, the offline-sealed credential verifies on a path that requires neither Clerk nor Keycloak, is usable only from the console plane, and every use is heavily audited; the wiki and Studio products have NO break-glass.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a total IdP outage, When the operator presents the offline-sealed credential, Then it verifies without any Clerk/Keycloak call. *assert: break-glass verify path makes 0 IdP calls* [Source: FR-I12]
- [ ] **AC2** — Given break-glass, When used, Then it is usable only from the restricted console admin surface. *assert: break-glass on wiki/Studio surface → refused* [Source: FR-I12]
- [ ] **AC3** — Given any break-glass use, When it occurs, Then it is heavily audited + alerted out-of-band. *assert: use emits an audit record + out-of-band alert* [Source: FR-I12, FR-I18]
- [ ] **AC4** (negative) — Given the wiki/Studio products, When break-glass is attempted, Then there is none (identity is their sole interactive login). *assert: no break-glass route on wiki/Studio* [Source: FR-I12]
- [ ] **AC5** (error-path) — Given a forged/replayed offline token, When presented, Then it is rejected. *assert: invalid offline-signed token → denied* [Source: FR-I12]

## 🔨 Tasks

- [ ] RED: test IdP-independent verify, console-only scope, audit+alert, forgery rejection (AC1–3,5).
- [ ] GREEN: offline-sealed credential verify path (hardware-key / operator-held offline-signed token, OQ-I5) on the restricted admin surface; audit+alert hook (AC1–4).
- [ ] RED→GREEN: write + rehearse the total-IdP-outage runbook (AC1).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — offline-sealed ed25519 break-glass credential mint+verify + sealtool CLI; fail-closed verify (expired/bad-sig/wrong-aud distinguished for logging only).

Tier: transport (restricted admin surface) → break-glass verify service (offline-sealed, no IdP dep). Scope: **console admin plane only** (reconciliation 2026-07-05 evening). Seam: hardware key / offline signer, audit stream. OQ-I5 pins the credential form. Sibling: E4-S6 (admin surfaces), E8-S4 (audit).

## 🧪 Testing

`TestBreakGlassIdpIndependent` (integration). Tiers: integration. CS §5: exercise the verify path real; no IdP mock needed (the point is IdP-independence).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (SPOF removal, heavily-audited recovery). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F4 (FR-I12); Architecture `dQUjrSXhdp` A-BREAKGLASS; OQ-I5.

## 🔗 Dependencies

Blocked by: **ENG-2101** + **OQ-I5** (credential form — pinned by the Definition Pack); parent epic **E4**. Intra-service: E4-S6 (admin plane), E8-S4 (audit).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
