## 🎯 Story

As a **Teams / regulated tenant**, I want BYOK (customer-managed encryption keys) via KMS, per-tenant, enforced at the store layer for Memory + wiki content, so that my data is encrypted under keys I control and the corpus isolation resolves to the stronger wall.

**Definition of Done:** `TestByokStoreLayerEnforced` (integration layer) — through the store layer, a Teams/regulated tenant's Memory + wiki content encrypt under a customer-managed KMS key (per-tenant), and a request without the tenant's key cannot decrypt (isolation resolves to the stronger wall for regulated tenants).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Teams/regulated tenant with BYOK enabled, When content is stored, Then it encrypts under the tenant's customer-managed KMS key. *assert: ciphertext keyed to the tenant's KMS key ref* [Source: FR-S1]
- [ ] **AC2** — Given BYOK, When enforced, Then it applies at the store layer (not just at the app), per-tenant. *assert: store-layer envelope encryption; key ref per tenant* [Source: FR-S1]
- [ ] **AC3** (isolation) — Given regulated tenants, When the isolation grade is applied, Then it resolves to the stronger wall (the corpus wall itself is knowledge-owned; identity supplies the tenant key + BYOK enforcement). *assert: regulated tenant → stronger-wall grade; identity supplies key + BYOK* [Source: FR-S4]
- [ ] **AC4** (negative) — Given a request without the tenant's key access, When decrypting, Then it fails (no cross-tenant plaintext). *assert: missing key → decrypt denied* [Source: FR-S1, FR-S4]
- [ ] **AC5** — Given key rotation, When a customer rotates their KMS key, Then new writes use the new key and old data remains decryptable. *assert: rotation → forward writes new key, back-compat reads* [Source: FR-S1]

## 🔨 Tasks

- [ ] RED: test per-tenant KMS encryption, store-layer enforcement, missing-key decrypt denial (AC1,2,4).
- [ ] GREEN: BYOK key management via `identity`/KMS, per-tenant key refs; store-layer envelope encryption; rotation support (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** absent — no BYOK / KMS customer-managed-key path (store-layer or otherwise).

Tier: BYOK/KMS service → key domain → KMS (vendor) + store layer. Identity owns the F6 security slice; the **corpus isolation grade (FR-S4)** itself is knowledge-owned — identity's share is the tenant key + BYOK wall. Seam: KMS (vendor), knowledge (corpus wall), Turbopuffer BYOC-per-cell posture. Sibling: E10-S3 (BYOC), E3-S1 (tenant key).

## 🧪 Testing

`TestByokStoreLayerEnforced` (integration). Tiers: integration. CS §5: mock the KMS provider at the vendor boundary; exercise the store-layer encryption real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (customer-managed keys, store-layer enforcement), data-residency. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` "Memory gap-closure — requirements" (FR-S1, FR-S4); Architecture `dQUjrSXhdp` AD-8 (BYOK via identity/KMS).

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: BYOK key-ref contract); parent epic **E10**. Intra-service: E3-S1 (tenant key); cross-service knowledge (FR-S4 corpus wall).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
