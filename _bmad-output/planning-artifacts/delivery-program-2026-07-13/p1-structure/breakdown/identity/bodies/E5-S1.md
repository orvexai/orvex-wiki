## 🎯 Story

As **orvex-studio-workflows**, I want identity to serve the studio-side `POST /api/clerk/provision|deprovision {scope,id,mode}` step API, idempotent and authenticated by **workload identity (mTLS/SPIFFE) scoped to workflows + NetworkPolicy**, so that the destructive lifecycle path has no shared-bearer blast radius.

**Definition of Done:** `TestDestructivePathWorkloadIdentity` (integration layer) — through `/api/clerk/*`, a workflows caller with a valid SPIFFE identity succeeds idempotently; a caller without that workload identity is refused by NetworkPolicy/mTLS; responses are uniform (no 404 existence oracle).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a workflows caller with valid mTLS/SPIFFE, When `POST /api/clerk/provision {scope,id,email?}` runs, Then the studio-side org/user record is created idempotently. *assert: repeat call → same result, one record* [Source: FR-I14]
- [ ] **AC2** — Given `{scope,id,mode}` for deprovision, When `mode=disable`, Then it soft-disables (reversible). *assert: `disable` sets status disabled, reversible* [Source: FR-I14]
- [ ] **AC3** (negative) — Given a non-workflows caller, When calling `/api/clerk/*`, Then NetworkPolicy/mTLS refuses it. *assert: non-SPIFFE caller → connection refused/denied* [Source: FR-I14, NFR-I5]
- [ ] **AC4** — Given the studio mirror shape, When called, Then it is `{scope,id,mode}` (NOT the engine's `{clerkOrgId,...}` shape) — identity does not front engine provisioning. *assert: identity accepts studio shape only* [Source: FR-I14]
- [ ] **AC5** (negative) — Given a request for a non-existent record, When called, Then the response is uniform (constant idempotent 200, no 404 existence oracle). *assert: unknown id → same uniform response* [Source: FR-I14]

## 🔨 Tasks

- [ ] RED: test idempotent provision, disable soft-disable, non-workflows refusal, uniform response (AC1–5).
- [ ] GREEN: `/api/clerk/provision|deprovision` step API (studio shape) + mTLS/SPIFFE + NetworkPolicy restricting to workflows (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — provision/deprovision REST handlers exist (POST /api/clerk/provision + /deprovision, provisionEnginePrincipal/provisionAIWorkspace); MISSING workload identity (mTLS/SPIFFE) + NetworkPolicy — referenced only as upstream/deployment concerns, no NetworkPolicy manifest in deploy/.

Tier: transport (`/api/clerk/*` step API) → lifecycle service → tenant/record domain → Postgres. `cmd/lifecycled` Knative Service. Seam: workflows (sole caller, mTLS/SPIFFE), NetworkPolicy (infra). Cross-service: engine keeps its OWN `/api/clerk/*` controllers — the workflow drives both in parallel; identity does not front the engine. Sibling: E5-S2 (two-phase delete), E8-S3 (Idempotency-Key).

## 🧪 Testing

`TestDestructivePathWorkloadIdentity` (integration). Tiers: integration + a NetworkPolicy/mTLS enforcement check. CS §5: real Postgres; the SPIFFE identity is exercised, not mocked away.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (no shared static bearer, workload identity on destructive path), zero-trust. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F5 (FR-I14), §5 (NFR-I5); Architecture `dQUjrSXhdp` A-LIFECYCLE.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: provision/deprovision step-API shapes); parent epic **E5**. Intra-service: E5-S2, E8-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
