## 🎯 Story

As a **privacy-conscious user**, I want the personal↔employer firewall + per-use consent enforced end-to-end via an **identity-owned, single-target, single-use, short-TTL delivery token** (AD-8 — consent binds at the mint boundary, not the untrusted client compose), so that a private memory is never re-composed into a different target without a fresh check.

**Definition of Done:** `TestFirewallConsentEndToEnd` (integration layer) — through the delivery-token mint, a private-memory delivery fetch mints a token scoped `{memory, target, user}`; the client cannot compose without presenting one; every per-use action re-mints; and a token for ChatGPT cannot deliver to Gemini.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a private-memory delivery, When fetched, Then identity mints a single-target single-use short-TTL delivery token scoped `{memory, target, user}`. *assert: token scope == `{memory,target,user}`, single-use, short-TTL* [Source: AD-8, FR-S5]
- [ ] **AC2** (the DoD proof) — Given a token minted for target=ChatGPT, When the client tries to deliver to Gemini, Then it fails (re-mint required per target). *assert: ChatGPT token → Gemini delivery denied* [Source: AD-8]
- [ ] **AC3** — Given the untrusted client compose, When it composes, Then it cannot proceed without presenting a valid delivery token. *assert: compose without token → denied; client compose is not itself a trust boundary* [Source: AD-8, FR-S5]
- [ ] **AC4** — Given the firewall + consent, When enforced, Then it holds end-to-end across capture, storage, retrieval, delivery, and sync-out. *assert: consent enforced at each stage; private-by-default flip respected* [Source: FR-S5]
- [ ] **AC5** — Given AD-6 (client-compose ToS-clean port), When the extension acts, Then identity supplies the gating token; the extension behaviour (compose-box autofill, per-use, input-field-only) is api/ui-owned. *assert: identity supplies the token; AD-6 extension invariant is cross-service, CWS 2026-08-01 re-check owned by api/ui (OQ8)* [Source: AD-6]

## 🔨 Tasks

- [ ] RED: test single-target single-use token, ChatGPT-token-cannot-deliver-to-Gemini, compose-requires-token (AC1–3).
- [ ] GREEN: identity-owned delivery-token mint scoped `{memory,target,user}` (single-use, short-TTL, re-mint per use); consent-record store; end-to-end firewall enforcement (AC1–4); cross-service note for AD-6 (AC5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** absent — no personal<->employer firewall, per-use consent, or AD-8 mint-boundary delivery token.

Tier: delivery-token mint service → consent domain → Postgres (consent records). AD-8 keystone: the trust boundary is the retrieval/mint step, not the untrusted client compose. FR-S6's "injected/retrieved memory treated as untrusted, human-confirm gate, provenance/sensitivity per memory" is primarily staging/knowledge-owned; identity's share is the consent/provenance binding at the mint boundary. AD-6 extension behaviour is api/ui-owned (identity supplies the gating token; the CWS 2026-08-01 policy re-check is the api/ui F1 viability spike, OQ8). Seam: knowledge/staging (memory pipeline), api/ui (extension). Sibling: E10-S1 (BYOK), E2 (mint pattern).

## 🧪 Testing

`TestFirewallConsentEndToEnd` (integration). Tiers: integration. CS §5: exercise the delivery-token mint + consent enforcement real; the client compose + memory pipeline are cross-service boundaries stubbed at their edges.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (consent at the mint boundary, private-memory firewall). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` "Memory gap-closure" (FR-S5, FR-S6, owned slice AD-8); Architecture `dQUjrSXhdp` AD-8, AD-6.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: delivery-token shape `{memory,target,user}`, consent record schema); parent epic **E10**. Cross-service: knowledge/staging (FR-S6 pipeline), api/ui (AD-6 extension, OQ8).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
