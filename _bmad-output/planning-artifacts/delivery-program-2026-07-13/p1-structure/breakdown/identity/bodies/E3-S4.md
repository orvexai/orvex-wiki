## 🎯 Story

As a **satellite**, I want identity to map the IdP's coarse role (Clerk `org_role` admin/member) to the workspace role enum (`workspace_admin`/`workspace_member`), so that the principal carries coarse roles while fine-grained CASL stays in the engine.

**Definition of Done:** `TestRoleMapping` (unit/integration layer) — through the resolver, Clerk `org_role=admin` maps to `workspace_admin` and `member` to `workspace_member`, using the role enum owned by orvex-studio-contracts; identity supplies coarse roles only (no fine-grained authz decision).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Clerk `org_role=admin`, When resolved, Then `roles` includes `workspace_admin`. *assert: `resolve(admin).roles ∋ workspace_admin`* [Source: FR-I10]
- [ ] **AC2** — Given `org_role=member`, When resolved, Then `roles` includes `workspace_member`. *assert: `resolve(member).roles ∋ workspace_member`* [Source: FR-I10]
- [ ] **AC3** — Given the role enum, When mapping, Then values come from the contracts-owned enum (not a local literal). *assert: mapping references the generated enum* [Source: FR-I10]
- [ ] **AC4** (negative) — Given a request for a fine-grained permission decision, When asked, Then identity does not evaluate it (CASL stays in-engine). *assert: identity returns coarse roles only; no permission-eval endpoint* [Source: FR-I10, non-goal]
- [ ] **AC5** — Given a Keycloak realm role, When resolved, Then it maps to the same workspace enum via the resolver. *assert: `resolve(kcRole)` normalizes to workspace enum* [Source: FR-I10]

## 🔨 Tasks

- [ ] RED: test admin/member mapping + Keycloak-role normalization + enum sourced from contracts (AC1–3,5).
- [ ] GREEN: role-mapping in the resolver output; consume the contracts role enum; assert no permission-eval surface (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** absent — no Clerk org_role -> workspace role enum mapping anywhere in the tree.

Tier: resolver/domain (role normalization) — part of the principal produced in E1-S1. Non-goal boundary: identity supplies coarse roles; CASL/ACL fine-grained decisions stay in the engine (FR-13). Seam: contracts (role enum). Sibling: E1-S1 (principal), E3-S1 (tenant).

## 🧪 Testing

`TestRoleMapping` (unit + integration). Tiers: unit (mapping) + integration (resolver). CS §5: exercise the resolver real; the role enum is a generated artifact, not mocked.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (least-privilege coarse role boundary). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F3 (FR-I10), §3 non-goals; Architecture `dQUjrSXhdp` A-IDP.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: role enum owned by contracts); parent epic **E3**. Intra-service: E1-S1 (principal).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
