## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the **Orvex CLI**, I want a stable `GET /v1/whoami` that verifies a token and returns the caller principal, so that `orvex auth login` validates against it and `orvex auth whoami|status` reads it — replacing the engine's `POST /api/users/me`.

**Definition of Done:** `TestWhoamiPrincipal` (integration layer) — through `GET /v1/whoami`, a valid token returns the IdP-agnostic principal `{idp, subject, tenant, org_or_realm, roles, token_scope}` on the same FR-I1/FR-I2 verification path; an invalid/expired token is rejected.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a valid token, When `GET /v1/whoami` is called, Then it returns the IdP-agnostic principal. *assert: response == `{idp, subject, tenant, org_or_realm, roles, token_scope}`* [Source: FR-I20]
- [ ] **AC2** — Given the CLI `login` probe, When it validates, Then `whoami` confirms the caller/tenant/role. *assert: `orvex auth login` succeeds against `/v1/whoami`* [Source: FR-I20]
- [ ] **AC3** — Given verification, When `whoami` runs, Then it uses the same local-JWKS + introspection path (not a bespoke verifier). *assert: whoami delegates to lib/auth E1 path* [Source: FR-I20, FR-I2]
- [ ] **AC4** (negative) — Given an invalid or expired token, When `whoami` is called, Then it is rejected. *assert: bad token → 401* [Source: FR-I20]
- [ ] **AC5** — Given a personal user-keyed tenant, When `whoami` returns, Then `org_or_realm` is empty. *assert: personal → `org_or_realm == ""`* [Source: FR-I20, D-S17]

## 🔨 Tasks

- [ ] RED: test whoami returns the principal + rejects bad tokens + personal empty org (AC1,4,5).
- [ ] GREEN: `GET /v1/whoami` handler delegating to the E1 verify path; return the principal shape (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — GET /v1/whoami mounted behind requireSession + cellPinGuard, echoing the verified principal (opaque-bearer path covered).

Tier: transport (`/v1/whoami`) → E1 verifier. Replaces engine `POST /api/users/me` (`LookupCallerRole`) and the `login` probe of `POST /api/workspace/info`. Seam: Orvex CLI (consumer, docmostcli space). Sibling: E1 (verify), E7-S2 (login mint).

## 🧪 Testing

`TestWhoamiPrincipal` (integration). Tiers: integration. CS §5: exercise the handler + lib/auth real; mock the JWKS edge only.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (uniform verify path). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F7 (FR-I20a); Architecture `dQUjrSXhdp` §3.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: whoami principal shape, FR-A16); parent epic **E7**. Intra-service: E1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
