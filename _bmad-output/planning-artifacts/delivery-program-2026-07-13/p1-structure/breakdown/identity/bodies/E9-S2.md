## 🎯 Story

As the **platform**, I want a per-verified-principal live-tenant cap, progressive-trust tiers, and staged provisioning that gates *when* hosting provisions and the AI trial unlocks, so that "one login → thousands of tenants for free hosting" yields no free resource until trust clears.

**Definition of Done:** `TestPerPrincipalTenantCap` (integration layer) — through the registry provisioning transaction, a per-principal live-tenant cap holds (personal path only), progressive-trust tiers gate provisioning + AI-trial unlock, and staged provisioning (`pending → cooling → live`) holds the farmed grants until trust clears.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a principal at the live-tenant cap, When it creates another personal tenant, Then it is refused (soft, raise-able via step-up). *assert: `COUNT(live personal tenants WHERE owner=principal) >= cap` → refused before cell alloc* [Source: Anti-Sybil Layer 0]
- [ ] **AC2** — Given org tenants, When counting, Then they are exempt (naturally capped by paid seats). *assert: org tenants not counted against the personal cap* [Source: Anti-Sybil Layer 0]
- [ ] **AC3** — Given progressive-trust tiers, When a tenant is email-verified, Then it gets the basic tier; clean velocity/risk or step-up unlocks the full free tier. *assert: tier gates provisioning + AI-trial unlock* [Source: Anti-Sybil Layer 0]
- [ ] **AC4** — Given staged provisioning, When a risky signup is created, Then hosting provisioning + AI-trial activation are HELD (`pending → cooling → live`) until trust clears. *assert: risky signup → held grants; AI trial stays instant-but-metered* [Source: Anti-Sybil Layer 0/3]
- [ ] **AC5** (negative) — Given a legitimate multi-workspace power user, When they hit the soft cap, Then step-up raises it (not a hard wall). *assert: step-up → cap raised, funnel preserved* [Source: Anti-Sybil Layer 0]

## 🔨 Tasks

- [ ] RED: test cap-at-provision, org-exempt, tier-gating, staged-hold (AC1–4).
- [ ] GREEN: per-principal cap check in the provisioning txn; `trust_tier` on the tenant record; `provisioning_state` machine (pending→cooling→live) gating cell alloc (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** absent — no per-principal live-tenant cap, progressive-trust tiers, or staged provisioning.

Tier: provisioning service → trust/tier domain → Postgres (provisioning_state, trust_tier) + the E6 registry write (the enforcement point). Personal path only. Seam: E3-S2 (JIT), E6-S2 (cell alloc), E9-S4 (risk score sets the tier). Caps/tier thresholds are TBD — pinned by ENG-2101.

## 🧪 Testing

`TestPerPrincipalTenantCap` (integration). Tiers: integration. CS §5: real Postgres + registry; exercise the provisioning txn real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (blast-radius cap, ROI-drain), Cost governance. cell-lint JGAUQRsw2g.

## 🔗 References

Anti-Sybil `CYHyoUd7J1` §3 Layer 0; PRD `cnhla0qRRF` §4 F3 (FR-I9); Brief §Scope.

## 🔗 Dependencies

Blocked by: **ENG-2101** (caps/tiers = quota values, PO-decided); parent epic **E9**. Intra-service: E9-S1 (principal), E3-S2, E6-S2, E9-S4 (risk sets tier).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
