## 🎯 Story

As `orvex doctor`, I want identity to expose a `GET /healthz` reachability alias and an interim engine-api-key passthrough, so that the CLI can probe identity's reachability and the CLI→engine transition (WS-6/WS-8) does not break before the engine accepts identity-minted tokens.

**Definition of Done:** `TestDoctorProbeAndInterimKey` (integration layer) — through `GET /healthz`, `orvex doctor` gets a reachability signal (an alias that MUST NOT gate readiness), and an interim engine api-key still verifies through identity's path during the WS-6/WS-8 transition (FR-A16).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `orvex doctor`, When it probes `GET /healthz`, Then it receives a reachability signal echoing cell + cluster. *assert: `/healthz` → 200 `{status, cell, cluster}`* [Source: FR-I20]
- [ ] **AC2** — Given `/healthz`, When wired, Then it is a CLI-doctor alias ONLY and does NOT gate readiness. *assert: `/healthz` not used as the readinessProbe (that is `/readyz`, E8-S2)* [Source: FR-I20, FR-I18]
- [ ] **AC3** — Given the interim transition, When the CLI passes an engine api-key, Then identity's verify path does not break it. *assert: interim engine api-key still verifies during WS-6/WS-8* [Source: FR-I20]
- [ ] **AC4** — Given the engine begins accepting identity-minted tokens, When cutover completes, Then the interim passthrough is retired cleanly. *assert: post-cutover, identity-minted tokens are the path; interim removable* [Source: FR-I20]
- [ ] **AC5** (negative) — Given a dead dependency, When `/healthz` is probed, Then it still returns reachable (it is not a dependency check — that is `/readyz`). *assert: `/healthz` stays 200 on a dead dep; `/readyz` goes NOT-Ready* [Source: FR-I20, FR-I18]

## 🔨 Tasks

- [ ] RED: test `/healthz` reachability alias + interim key passthrough (AC1,3).
- [ ] GREEN: `GET /healthz` alias (echoes cell+cluster, never gates readiness); interim engine-api-key passthrough in the verify path (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — GET /healthz (cell-contract echo) exists and the engine client carries an INTERNAL_API_BEARER_TOKEN; MISSING the /healthz doctor probe alias and a documented interim engine api-key passthrough surface.

Tier: transport (`/healthz` alias) + verify path (interim key). Note the split with E8-S2: `/healthz` = CLI-doctor alias; `/readyz` = the real dependency gate. Seam: Orvex CLI (`doctor`), engine (interim key transition). Sibling: E8-S2 (livez/readyz), E1 (verify).

## 🧪 Testing

`TestDoctorProbeAndInterimKey` (integration). Tiers: integration. CS §5: exercise the real handler; the engine api-key check exercised on identity's path.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Operational Excellence (healthz vs readyz not conflated). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F7 (FR-I20c/d); Architecture `dQUjrSXhdp` §3, §6.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E7**. Intra-service: E8-S2 (readyz is the real gate), E1 (verify path).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
