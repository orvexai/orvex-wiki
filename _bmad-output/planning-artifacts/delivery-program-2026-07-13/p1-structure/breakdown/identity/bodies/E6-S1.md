## 🎯 Story

As the **global control plane**, I want the org→cell registry split into **two schema-isolated stores** — a PII-free routing core and an in-jurisdiction sensitive shard, in a dedicated global CNPG where identity is the sole writer — so that the routing core is cross-jurisdiction replicable while sensitive data stays in-jurisdiction.

**Definition of Done:** `TestRoutingCorePiiFree` (integration layer) — through the registry, the routing core holds only `{tenant_id uuidv7, cell_id, residency_pin, cell_epoch, status}` (no PII) and is replicable, while `stripe_customer`/human `hostname_claims`/contacts live only in the in-jurisdiction sensitive shard; identity is the sole writer of both.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the routing core, When inspected, Then it contains only PII-free routing columns. *assert: routing-core schema == `{tenant_id, cell_id, residency_pin, cell_epoch, status}`, no PII* [Source: FR-I19]
- [ ] **AC2** — Given sensitive data, When stored, Then `stripe_customer`/`hostname_claims`/contacts land only in the in-jurisdiction sensitive shard. *assert: sensitive columns absent from routing core* [Source: FR-I19, NFR-I6]
- [ ] **AC3** — Given the two stores, When written, Then identity is the sole writer of both (in a dedicated global CNPG). *assert: no other service has write grant* [Source: FR-I19]
- [ ] **AC4** — Given the routing core, When replicated cross-jurisdiction, Then replication is legally-plausible (PII-free). *assert: routing core replicable; sensitive shard read in-jurisdiction only* [Source: FR-I19, NFR-I6]
- [ ] **AC5** — Given `cell_catalog`, When inspected, Then rows carry region/jurisdiction/cluster_ref/gateway_origin/status/capacity. *assert: cell_catalog schema present* [Source: FR-I19]

## 🔨 Tasks

- [ ] RED: test routing-core PII-free schema + sensitive-shard separation + sole-writer (AC1–3,5).
- [ ] GREEN: dedicated global CNPG; routing-core + sensitive-shard schemas + cell_catalog; write grants to identity only (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — PII-free routing core is a real schema-confined store (RegistryCore over registry_tenant_cells/registry_hostnames) with isolation tests; MISSING the sensitive in-jurisdiction shard as a separate schema-isolated store (stripe_customer/contacts not implemented).

Tier: registry service → routing-core + sensitive-shard domains → global CNPG (**deployed once, globally** — the platform's only global component). This lands SE-Arch F5. Seam: legal-sign-off on the PII-free classification is OPEN (cell-contract §7) — flag, do not assume. Sibling: E6-S2 (assignment writes here), E6-S3 (KV mirror of the routing core).

## 🧪 Testing

`TestRoutingCorePiiFree` (integration). Tiers: integration (real global CNPG testcontainer). CS §5: real store; no own-package mocks.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security/data-residency (F5 two-store split), Cost governance (routing-only, not entitlement). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F3 (FR-I19), §5 (NFR-I6); Architecture `dQUjrSXhdp` A-CELL-REGISTRY, Datastore; Audit `YuY9XWpKPS` F5.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: routing-core schema); parent epic **E6**. A-CELL-REGISTRY ADR is a family-space human action (flagged only). Legal-sign-off OPEN.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
