## 🎯 Story

As the **platform**, I want native engine email/password login **removed fully — hosted AND standalone (D-S3)** — while preserving the surviving auth endpoints, so that interactive login is always via identity and there is no native-login attack surface or fallback.

**Definition of Done:** `TestNativeLoginRemoved` (integration layer, cross-service with the engine) — the engine has no password login / change-password / forgot / reset / `/setup` endpoints, while `collab-token`, `verify-token`, `logout`, and the FR-15 exchange-consume survive; the removal PR is gated behind identity mint GA (WS-8 before WS-6).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the engine, When probed, Then password login/change/forgot/reset + `/setup` return 404/removed. *assert: each removed route → 404* [Source: FR-I12, D-S3]
- [ ] **AC2** — Given the surviving endpoints, When probed, Then `collab-token`, `verify-token`, `logout`, FR-15 consume still work. *assert: each survivor → functional* [Source: FR-I12]
- [ ] **AC3** — Given a standalone deployment, When it needs interactive login, Then it bundles identity (no native engine path). *assert: standalone image bundles identity; no engine password path present* [Source: FR-I12, D-S3]
- [ ] **AC4** (sequencing) — Given the removal, When merged, Then it merges only after identity's Clerk-exchange + FR-15 mint are GA + login-availability soak-tested (WS-8 before WS-6). *assert: removal gated on the WS-8 soak evidence* [Source: FR-I12, D-S3]
- [ ] **AC5** (negative) — Given no fallback issuer exists, When identity is unavailable at cutover, Then the gate holds removal (no half-removed login). *assert: removal blocked until soak-green* [Source: FR-I12]

## 🔨 Tasks

- [ ] RED: cross-service test asserting removed routes 404 and survivors functional (AC1,2).
- [ ] GREEN: delete password/change/forgot/reset/`/setup` in the engine; keep survivors; standalone bundles identity (AC1–3).
- [ ] RED→GREEN: wire the WS-8-before-WS-6 gate on the removal PR (AC4,5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — identity-side native-login lockout is real (ErrSSORequired + isFederated + PUT enforce-sso admin route); the actual removal of the engine's password login/change/forgot/reset + /setup endpoints lives in the engine repo, not here.

Tier: cross-service engine change (orvexwiki, NestJS auth controllers) coordinated from identity. This is a cross-service contract change → A-SESSION ADR (E8-S5), cross-linked to the engine space. Seam: engine. Sibling: E4-S1 (exchange mint must be GA first), E4-S3 (break-glass, not a native login), OQ-I4 (standalone bootstrap).

## 🧪 Testing

`TestNativeLoginRemoved` (integration, cross-service). Tiers: integration + E2E on dev. CS §5: exercise the real engine build; no own-package mocks.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (attack-surface removal), Reliability (no fallback issuer — sequencing gate). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F4 (FR-I12), §7 rollout, OQ-I4; Architecture `dQUjrSXhdp` A-SESSION; D-S3, D-IDENT-4.

## 🔗 Dependencies

Blocked by: **ENG-2101** + cross-space `orvexwiki`; parent epic **E4**. Intra-service: hard-ordered AFTER E4-S1 GA (WS-8 before WS-6).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
