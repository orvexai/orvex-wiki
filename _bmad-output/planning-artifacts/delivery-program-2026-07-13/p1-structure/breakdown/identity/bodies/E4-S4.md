## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **Keycloak-backed tenant**, I want the fork's OIDC-RP controllers relocated into identity as the first-class Keycloak path — severing every `@docmost/*` import — so that Keycloak/MyIDP is a peer IdP that terminates at the engine session via the same FR-15 bridge.

**Definition of Done:** `TestKeycloakOidcRelocation` (integration layer) — through identity's `/auth/oidc/*` + `/sso/oidc/*`, authorize/callback/provider-CRUD/avatar-refresh/SSO-bridge flows work for a Keycloak tenant, contain **no `@docmost/*` imports**, and route to the engine session only via `POST /v1/exchange` (never `setAuthCookie` on the engine origin).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a Keycloak tenant, When the OIDC authorize/callback runs, Then the user is resolved and an exchange token minted. *assert: callback → `/v1/exchange` → exchange token* [Source: FR-I13]
- [ ] **AC2** — Given the relocated code, When built, Then it contains zero `@docmost/*` imports (AGPL sever, D-P9). *assert: static check: no `@docmost/*` import in the identity module* [Source: FR-I13]
- [ ] **AC3** — Given the OIDC controller runs on identity's origin, When it completes, Then it cannot `setAuthCookie` on the engine origin — it routes through FR-I11. *assert: no engine-origin cookie set; session via exchange only* [Source: FR-I13, FR-I11]
- [ ] **AC4** — Given provider CRUD + avatar refresh + SSO bridge, When exercised, Then each works for a Keycloak tenant. *assert: provider CRUD/avatar/SSO-bridge functional* [Source: FR-I13]
- [ ] **AC5** — Given IdP is selected per tenant, When resolving, Then the `IdentityResolver` picks `KeycloakOidcResolver` from the registry. *assert: registry-keyed resolver selection* [Source: FR-I13, A-IDP]

## 🔨 Tasks

- [ ] RED: test OIDC authorize/callback→exchange; no `@docmost/*`; per-tenant resolver selection (AC1,2,5).
- [ ] GREEN: relocate `orvex/oidc/*` into identity, sever imports, wire to `/v1/exchange`; provider CRUD/avatar/SSO-bridge (AC1–4).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — full Keycloak OIDC-RP relocated as native Go (authorize/callback/provider CRUD/mirror/discovery-cache/PKCE); no @docmost imports (Go repo).

Tier: transport (`/auth/oidc/*`, `/sso/oidc/*`) → oidc service → resolver (`KeycloakOidcResolver`). AGPL basis D-P9: Orvex-authored code moves to the closed service, reaches the engine only across the network boundary; relies on PB-1 counsel review. Seam: Keycloak/MyIDP (vendor), engine (via exchange). Sibling: E4-S1 (exchange), E4-S5 (SCIM), E3-S1 (registry).

## 🧪 Testing

`TestKeycloakOidcRelocation` (integration + a build-time import-guard check). Tiers: integration + static analysis. CS §5: mock the Keycloak OIDC provider at the vendor boundary; exercise identity's OIDC flow real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (per-tenant IdP), and the AGPL import-sever (D-P9). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F4 (FR-I13); Architecture `dQUjrSXhdp` A-IDP, A-SESSION; D-P9; OQ-I3/OQ-I6.

## 🔗 Dependencies

Blocked by: **ENG-2101** + **OQ-I6** (Keycloak tenant unit) + PB-1 counsel (AGPL); parent epic **E4**. Intra-service: E4-S1, E3-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
