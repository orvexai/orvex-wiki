## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the **platform**, I want JIT provisioning on first authenticated presence to be **gated and rate-limited** (verified email / per-source cap / orgs-per-actor cap), so that a controlled BYO-IdP or repeated sign-ups cannot mass-provision tenants (resource + Clerk-billing DoS).

**Definition of Done:** `TestJITRateLimited` (integration layer) — through the JIT path, first authenticated presence provisions the studio-side records once; a verified email is required; per-source and orgs-per-actor caps reject excess; an org-switch re-exchanges to the switched org's tenant.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a first authenticated presence, When JIT runs, Then studio-side records are provisioned idempotently (once). *assert: repeated JIT for same principal → single tenant record* [Source: FR-I9]
- [ ] **AC2** — Given an unverified email, When JIT runs, Then provisioning is gated. *assert: unverified email → no tenant provisioned* [Source: FR-I9]
- [ ] **AC3** (negative) — Given repeated sign-ups from one source beyond the cap, When JIT runs, Then excess is rejected. *assert: N+1th within window → rate-limited* [Source: FR-I9]
- [ ] **AC4** (negative) — Given a controlled BYO-IdP mass-provision attempt, When JIT runs, Then the orgs-per-actor cap blocks it. *assert: actor beyond orgs-per-actor cap → rejected* [Source: FR-I9]
- [ ] **AC5** — Given an org-switch, When re-authenticating, Then it re-exchanges to the switched org's tenant. *assert: post-switch principal.tenant == orgB tenant* [Source: FR-I9]

## 🔨 Tasks

- [ ] RED: test idempotent provision, verified-email gate, per-source + orgs-per-actor caps (AC1–4).
- [ ] GREEN: JIT service with rate buckets (Redis) + verified-email precondition; the workflow (not identity) drives engine workspace creation in parallel (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — OIDC JIT provisioning through OidcUserProvisioner + fixed-window ThrottleLimiters (ported 30/60-per-window limits) + Clerk JIT with concurrent-race idempotency.

Tier: JIT handler → provisioning service → tenant domain (E3-S1) + Redis rate buckets. Note: identity provisions the **studio-side** records; the workflow drives engine workspace/user creation in parallel (FR-I14). Seam: workflows (parallel driver), Redis. Sibling: E9 anti-Sybil hardens this same enforcement point further.

## 🧪 Testing

`TestJITRateLimited` (integration). Tiers: integration (Postgres + Redis). CS §5: real store + buckets; mock the IdP membership edge only.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (mass-provision DoS), Cost governance (Clerk-billing DoS). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F3 (FR-I9); Architecture `dQUjrSXhdp` A-JIT.

## 🔗 Dependencies

Blocked by: **ENG-2101** (caps are quota values — TBD, pinned by the Definition Pack); parent epic **E3**. Intra-service: E3-S1 (registry); hardened by E9.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
