## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the **wiki engine**, I want identity to verify the Clerk (or Keycloak) identity and mint a short-lived **exchange token** that I consume to create a session, so that the direct IdP verification leaves the engine and I keep only the irreducible consume→mint.

**Definition of Done:** `TestExchangeToSessionMint` (integration layer) — through `POST /v1/exchange`, identity verifies the IdP and returns a short-lived exchange token; the engine consumes it (`verifyJwt(otp,'exchange')`) and mints a session; identity never sets the engine's cookie.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a valid Clerk session, When `POST /v1/exchange` is called, Then identity verifies Clerk and returns a short-lived exchange token. *assert: `exchange(clerkSession)` → single-use short-TTL token* [Source: FR-I11]
- [ ] **AC2** — Given the exchange token, When the engine consumes it, Then it mints a session and sets the cookie on the engine origin. *assert: engine `createSessionAndToken` succeeds; cookie set on engine origin only* [Source: FR-I11]
- [ ] **AC3** — Given a Keycloak tenant, When exchanging, Then the same bridge mints an exchange token. *assert: `exchange(kcSession)` → exchange token via same path* [Source: FR-I11, FR-I13]
- [ ] **AC4** (negative) — Given identity, When exchanging, Then it never sets the engine's session cookie itself (cross-origin). *assert: identity response sets no engine cookie* [Source: FR-I11]
- [ ] **AC5** (error-path) — Given an expired/replayed exchange token, When the engine consumes it, Then it is rejected (single-use). *assert: 2nd consume → rejected* [Source: FR-I11]

## 🔨 Tasks

- [ ] RED: test verify→exchange-token→engine consume→session; single-use replay rejected (AC1,2,5).
- [ ] GREEN: `POST /v1/exchange` handler → session service (IdP verify via E1 + short-TTL exchange-token mint) (AC1–4).
- [ ] RED→GREEN: Keycloak path through the same bridge (AC3).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — POST /v1/exchange verifies the Clerk session (clerk.Resolver.Verify) then mints a short-lived opaque exchange token via the same token.Authority; engine consumes it (never sets the cookie itself).

Tier: transport (`/v1/exchange`) → session service → E1 resolver. Cross-service: the engine (orvexwiki, NestJS session service) keeps `token.service.generateExchangeToken` + `createSessionAndToken`; only the direct Clerk verify moves here. Seam: engine (consumes exchange token over the network). Sibling: E1 (verify), E4-S4 (Keycloak).

## 🧪 Testing

`TestExchangeToSessionMint` (integration, identity + a stub engine consume). Tiers: integration. CS §5: mock the engine consume endpoint at the network boundary; exercise identity's verify+mint real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (single-use exchange token, cross-origin cookie boundary). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F4 (FR-I11); Architecture `dQUjrSXhdp` A-SESSION.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: exchange-token shape) + cross-space `orvexwiki` engine consume seam; parent epic **E4**. Intra-service: E1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
