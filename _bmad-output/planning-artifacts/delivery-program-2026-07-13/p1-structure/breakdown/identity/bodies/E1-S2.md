## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As **any satellite**, I want to embed the `orvex-studio-lib/auth` verifier that checks signatures locally against cached JWKS with **no network hop** on the happy path, so that verification is uniform, fast, and deny-by-default everywhere.

**Definition of Done:** `TestLibAuthLocalVerify` (package/conformance layer) — through `lib/auth`'s public verify API, a valid token verifies from cached JWKS with zero network calls, an invalid signature is denied, and an absent/unparseable scope claim yields no access (deny-by-default).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given cached JWKS, When a valid token is verified via `lib/auth`, Then it succeeds with no network hop. *assert: verify passes AND the JWKS HTTP client records 0 calls during verify* [Source: FR-I2]
- [ ] **AC2** — Given a token with a bad signature, When verified, Then it is denied. *assert: `Verify(badSig)` returns error, principal nil* [Source: FR-I2]
- [ ] **AC3** — Given a token with an absent or unparseable scope claim, When verified, Then access is denied (deny-by-default). *assert: `Verify(noScope)` → `ErrDenied`* [Source: FR-I6]
- [ ] **AC4** — Given the same verifier embedded in two different satellites, When both verify the same token, Then results are identical. *assert: conformance test: `Verify` output byte-identical across two importers* [Source: FR-I2, FR-I6]
- [ ] **AC5** (negative) — Given a satellite that tries to reimplement verification, When the conformance test runs, Then it fails unless it delegates to `lib/auth`. *assert: conformance harness rejects a divergent verifier* [Source: FR-I6]

## 🔨 Tasks

- [ ] RED: `lib/auth` package test — local verify with 0 network calls, bad-sig denied, no-scope denied (AC1–3).
- [ ] GREEN: implement `Verify(token) (Principal, error)` embedding the E1-S1 resolver + JWKS cache; deny-by-default scope gate stub (AC1–3).
- [ ] RED→GREEN: conformance harness proving identical output across importers (AC4–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — verifier.go delegates the full crypto pipeline to orvex-studio-lib/pkg/auth MultiIssuerVerifier; deny-by-default Middleware + RequireScope.

Tier: this is the **shared library ceiling** (`orvex-studio-lib/auth`) that every Go satellite imports; the TS bridge (api/mcp/ui) consumes generated types — the Go↔TS mechanism is a program P0 (bridge), not resolved here. Seam: none on the happy path (the point is no hop). Sibling: E1-S1 resolver, E2-S3 (full scope derivation lands the real deny logic).

## 🧪 Testing

`TestLibAuthLocalVerify` (package + conformance). Tiers: unit + conformance. CS §5: mock the JWKS endpoint only (vendor); **never mock own packages** — `lib/auth` IS the unit under test, exercise it real; the conformance test proves satellites do not fork it.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §5 mock boundary, §6 tiering, §7 shared-lib). SE-Arch 8sYi523i4t: Security (deny-by-default ceiling — the weakest satellite must not be the ceiling). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F1 (FR-I2), F2 (FR-I6); Architecture `dQUjrSXhdp` A-VERIFY, A-TOKEN (lib/auth enforcement).

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: token-scope schema); parent epic **E1**. Intra-service: after E1-S1; feeds E2-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
