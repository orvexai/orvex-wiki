## 🎯 Story

As the **platform**, I want table-stakes edge controls on every personal signup — verified-email precondition, disposable-email block with a privacy-relay allowlist, Turnstile, IP/ASN reputation with a datacenter hard-reject, and per-IP/subnet/ASN velocity limits — so that the cheap 80% of abuse is stopped at ~$0 with near-zero legit friction, consistently across BOTH IdP paths.

**Definition of Done:** `TestEdgeControlsBothIdPs` (integration layer) — through the signup edge, a verified email is a hard precondition for tenant creation, raw disposable domains are blocked while Apple/Mozilla/SimpleLogin relays are allowlisted, Turnstile is verified server-side before the registry write, datacenter/hosting-ASN signups are hard-rejected, and per-IP/subnet/ASN velocity limits apply — all across Clerk and Keycloak.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given tenant creation, When email is unverified, Then it is blocked (verified email as a hard precondition, not mere IdP user existence). *assert: unverified email → no tenant* [Source: Anti-Sybil Layer 1]
- [ ] **AC2** — Given a raw disposable/throwaway domain, When signing up, Then it is blocked; Apple Hide-My-Email / Firefox Relay / SimpleLogin are ALLOWLISTED. *assert: throwaway blocked; privacy relays permitted* [Source: Anti-Sybil Layer 1]
- [ ] **AC3** — Given a signup, When it reaches the registry write, Then a Turnstile token is verified server-side first. *assert: missing/invalid Turnstile token → rejected before write* [Source: Anti-Sybil Layer 1]
- [ ] **AC4** — Given a datacenter/hosting-ASN or known-malicious IP, When signing up, Then it is hard-rejected; VPN/Tor is detect-and-challenge (not 403). *assert: datacenter-ASN → hard-reject; VPN → challenge* [Source: Anti-Sybil Layer 1]
- [ ] **AC5** — Given per-IP/subnet/ASN velocity, When exceeded, Then it is limited (CGNAT-aware, never hard-fail on rate alone); an ASN-spike raises an early tripwire. *assert: velocity limited; CGNAT not own-goaled; ASN-spike alert* [Source: Anti-Sybil Layer 1]

## 🔨 Tasks

- [ ] RED: test verified-email precondition, disposable-block+allowlist, Turnstile server-verify, datacenter reject, velocity (AC1–5).
- [ ] GREEN: edge control chain at both IdP paths (Clerk toggles / Keycloak SPI or central API check); Turnstile server verify before the registry write; IP/ASN feed (EU-region); Redis velocity buckets (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** absent — no table-stakes edge controls — no Turnstile, disposable-email block, IP/ASN, or velocity checks.

Tier: gateway/edge middleware ahead of BOTH IdPs → risk/trust service. The durable enforcement point is identity's tenant-creation/registry write (native Clerk controls don't exist in Keycloak). Seam: Turnstile (vendor, GDPR-clean), IP/ASN feed (EU-region, vendor), Redis. Sibling: E9-S4 (fuses these into a score), E6-S2 (registry write).

## 🧪 Testing

`TestEdgeControlsBothIdPs` (integration). Tiers: integration. CS §5: mock Turnstile + IP/ASN feed at the vendor boundary; exercise the control chain real for both IdP paths.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (defense-in-depth edge), GDPR proportionality (Turnstile cleanest, EU-region IP feed). cell-lint JGAUQRsw2g.

## 🔗 References

Anti-Sybil `CYHyoUd7J1` §3 Layer 1; PRD `cnhla0qRRF` §4 F3 (FR-I9), §5 (NFR-I5).

## 🔗 Dependencies

Blocked by: **ENG-2101** (vendor choices + thresholds pinned at pack certification); parent epic **E9**. Intra-service: feeds E9-S4; E6-S2 registry write.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
