## 🎯 Feature area — Polymorphic tenant spine & JIT provisioning

Every principal maps to a tenant via its IdP's tenant unit. The model is **polymorphic (D-S17 — REPLACES orgs-from-the-start)**: a personal user carries **NO Clerk org** (tenant = Clerk user id, `org_or_realm` empty, full workspace + entitlements); a Teams tenant is a Clerk organization; the Keycloak analogue is a realm or group-within-realm (OQ-I6). The registry is authoritative, keyed on the polymorphic `{user|org}` principal with **opaque, non-derivable** IDs (no `sub`/email encoding, no enumeration). JIT provisioning is **gated + rate-limited**, and the **personal→Teams upgrade pass** (mint org at upgrade, re-key the tenant in place, carry data + entitlements) is a named identity+billing joint workstream with an explicit access transition (new members do not retroactively see pre-upgrade private content).

Why it exists: PRD §4 F3 + Goals G3; Architecture A-DATA / A-JIT; decisions D-IDENT-1, D-S17. Registry is **routing/tenant-key only — not an entitlement authority** (billing is SoR, D-P3).

## 📋 Stories

- [ ] E3-S1 — [identity] Tenant registry, idp-tagged polymorphic user|org mapping, opaque IDs (FR-I8, NFR-I4, NFR-I7)
- [ ] E3-S2 — [identity] Gated, rate-limited JIT provisioning (FR-I9)
- [ ] E3-S3 — [identity] Personal→Teams upgrade pass + access-transition isolation (FR-I8, FR-I9)
- [ ] E3-S4 — [identity] Coarse role mapping Clerk org_role → workspace role enum (FR-I10)

## 🧩 FR ids owned

FR-I8, FR-I9, FR-I10, NFR-I4 (isolation), NFR-I7 (no free-org / billing-SoR boundary).

## ✅ Definition of Done (epic)

- [ ] All four stories Done.
- [ ] Family-E2E share green: a fresh personal sign-in provisions a **user-keyed tenant with no Clerk org** and full workspace; a Teams upgrade re-keys the tenant in place carrying data + entitlements; a new team member cannot read pre-upgrade private content; a token for org A never resolves to org B's tenant — all human-verified on dev.
- [ ] SDD lines evidenced: `TestPolymorphicTenantMapping`, `TestUpgradeAccessTransition`, `TestJITRateLimited` green on merged code.

## 🧠 Context

Transport (provision/JIT handlers) → tenant service → registry domain (polymorphic key) → Postgres (RLS by tenant, opaque IDs). Seams: Clerk/Keycloak membership (vendor), billing (supplies tenant key it keys entitlements on), E6 registry (cell assignment happens at tenant creation). Sibling: billing joint workstream for the upgrade pass.

## 🔗 Dependencies

- **Blocked by: ENG-2101** (contract TAG: tenant-mapping shape + role enum owned by contracts) and **OQ-I6** (Keycloak tenant unit) for the Keycloak path.
- Intra-service: E6-S2 (cell assignment fires at tenant creation).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
