## 🎯 Story

As **any satellite**, I want a `POST /v1/introspect` endpoint returning liveness + current scope + revocation, Redis-cached with a short TTL, so that revocation and scope changes propagate within the TTL without per-request load on identity.

**Definition of Done:** `TestIntrospectRevocationTTL` (integration layer) — through `POST /v1/introspect`, a live token returns active + current scope; after revocation, introspection returns inactive within the cache TTL (target 30–60 s); p95 stays under NFR-I1 (< 30 ms).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a live token, When introspected, Then it returns `active=true` + current scope + exp. *assert: `introspect(live).active == true && .scope == grant.scope`* [Source: FR-I2]
- [ ] **AC2** — Given a repeated introspection within TTL, When called, Then it is served from Redis without recomputing. *assert: 2nd call within TTL hits cache (0 store reads)* [Source: FR-I2, NFR-I1]
- [ ] **AC3** — Given a token revoked at T, When introspected after T, Then it returns `active=false` within the TTL. *assert: `introspect(revoked)` at T+TTL → `active=false`* [Source: FR-I2, NFR-I3]
- [ ] **AC4** — Given load, When measuring, Then p95 < 30 ms. *assert: bench p95 < 30ms* [Source: NFR-I1]
- [ ] **AC5** (error-path) — Given Redis is down, When introspecting, Then a fresh authoritative read is served (never a stale-cache lie for high-priv). *assert: on cache-miss+Redis-down, authoritative store read or explicit error, never `active=true` for a revoked token* [Source: NFR-I3]

## 🔨 Tasks

- [ ] RED: integration test — live returns active+scope; revoked returns inactive within TTL (AC1,3).
- [ ] GREEN: `POST /v1/introspect` handler → introspection service → Redis cache (TTL 30–60 s) over the grant store (AC1–3).
- [ ] RED→GREEN: bench p95; Redis-down fallback to authoritative read (AC4,5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — POST /v1/introspect + dual-path Introspector (Clerk session OR opaque grant) present with tests; MISSING the Redis-cached introspection cadence/TTL — cache.go ships only the RevocationDenyList as real Redis, the introspection cache is still stdlib scaffold.

Tier: transport (`/v1/introspect`) → introspection service → grant/revocation domain → Redis cache + Postgres grants. Seam: Redis (infra), the E5 push-revocation broadcast complements this. Sibling: E2-S4 writes revocations this reads; E1-S4 enforces the hard staleness bound on top.

## 🧪 Testing

`TestIntrospectRevocationTTL` (integration). Tiers: integration + bench. CS §5: real Redis (testcontainer) and real Postgres — do NOT mock own store; only vendor edges (none here) are mockable.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Reliability (revocation correctness), Performance/freshness. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F1 (FR-I2), §5 (NFR-I1/I3); Architecture `dQUjrSXhdp` A-VERIFY.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: introspect request/response shape); parent epic **E1**. Intra-service: E1-S1/S2; complemented by E1-S4, E5-S3 (push-revocation).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
