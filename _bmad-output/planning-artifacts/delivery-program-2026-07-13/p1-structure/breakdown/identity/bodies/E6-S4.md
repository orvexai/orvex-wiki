## 🎯 Story

As the **platform**, I want a typed tenant-move step API (quiesce → export → import → activate) with a `TenantMoveManifest` and `TENANT_MOVE.md` coverage-checked against declared datastores, so that a deliberate cross-region tenant move is safe and the non-retrofittable cell-contract rule 10 is honored from day 1.

**Definition of Done:** `TestTenantMoveManifestCoverage` (integration layer) — through the tenant-move step API, a move quiesces/exports/imports/activates a tenant, the `TenantMoveManifest` covers every declared datastore, and the routing-core update is the FINAL commit step (cell-contract rule 10).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a tenant move, When it runs, Then it executes quiesce → export → import → activate in order. *assert: phased state machine transitions in order* [Source: FR-I19]
- [ ] **AC2** — Given the `TenantMoveManifest`, When validated, Then it covers every datastore declared for the tenant (no store left behind). *assert: manifest coverage == declared datastores; a missing store fails the check* [Source: FR-I19]
- [ ] **AC3** — Given the move, When it commits, Then the routing-core update is the FINAL step (rule 10). *assert: routing core updated last; a mid-move failure does not leave a dangling route* [Source: FR-I19]
- [ ] **AC4** — Given `TENANT_MOVE.md`, When present, Then it is coverage-checked against the declared datastores in CI. *assert: CI check ties `TENANT_MOVE.md` to datastore declarations* [Source: FR-I19]
- [ ] **AC5** (error-path) — Given a move that fails mid-phase, When it aborts, Then the tenant stays consistently in its source cell (no split-brain). *assert: abort → source-cell-consistent, no dual routing* [Source: FR-I19]

## 🔨 Tasks

- [ ] RED: test phased move, manifest coverage, routing-core-last, abort safety (AC1–3,5).
- [ ] GREEN: typed tenant-move step API + `TenantMoveManifest` + `TENANT_MOVE.md`; CI coverage check; routing-core update as final commit (AC1–4).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — registry.Move (the activate step) + move_consumer + a Kafka tenant-move trigger + an ORG_MOVE.md store inventory exist; MISSING the full typed quiesce/export/import step API + TenantMoveManifest (token_grants/org_workspaces are noted uncovered; the file is ORG_MOVE.md, not TENANT_MOVE.md).

Tier: transport (tenant-move step API) → move orchestration (workflows drives; identity exposes idempotent steps) → routing core (final commit). Rule 10 is the **non-retrofittable** cell-contract part (the 501 stub is 5 lines any day — but the manifest/coverage must be designed in). Seam: workflows (orchestrator), all tenant datastores. Sibling: E6-S1/S2/S3.

## 🧪 Testing

`TestTenantMoveManifestCoverage` (integration + a CI coverage check). Tiers: integration + CI static check. CS §5: real registry; the datastore export/import stubbed at declared boundaries.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §10 ops, cell-contract rule 10). SE-Arch 8sYi523i4t: Reliability (safe move, no split-brain), Operational Excellence. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F3 (FR-I19); Architecture `dQUjrSXhdp` A-CELL-REGISTRY, §6 (rule 10); Audit `YuY9XWpKPS` F9.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E6**. Intra-service: E6-S1/S2/S3; orchestration by workflows.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
