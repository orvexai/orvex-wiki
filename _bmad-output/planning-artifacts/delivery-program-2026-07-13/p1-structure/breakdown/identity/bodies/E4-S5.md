## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As a **Keycloak-backed enterprise tenant**, I want SCIM 2.0 (Users/Groups + SCIM tokens) relocated from the fork into identity, so that directory-driven provisioning works for Keycloak tenants through the closed identity service.

**Definition of Done:** `TestScimRelocation` (integration layer) — through `/scim/v2/*`, SCIM Users and Groups CRUD + SCIM token management operate for a Keycloak tenant, contain no `@docmost/*` imports, and SCIM-JIT terminates at the engine session via the FR-15 exchange bridge.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a SCIM client, When it creates/updates/deletes a User, Then the SCIM 2.0 Users endpoint applies it. *assert: SCIM Users CRUD conforms to RFC 7644* [Source: FR-I13]
- [ ] **AC2** — Given SCIM Groups, When managed, Then Group CRUD + membership apply. *assert: SCIM Groups CRUD functional* [Source: FR-I13]
- [ ] **AC3** — Given SCIM tokens, When managed, Then token issue/rotate/revoke works and is scoped to the tenant. *assert: SCIM token scoped; cross-tenant use refused* [Source: FR-I13, NFR-I4]
- [ ] **AC4** — Given a SCIM-provisioned user's first login, When JIT runs, Then it routes through the FR-15 exchange bridge (not an engine cookie). *assert: SCIM-JIT → `/v1/exchange`* [Source: FR-I13, A-SESSION]
- [ ] **AC5** — Given the relocated code, When built, Then no `@docmost/*` imports remain. *assert: import-guard passes* [Source: FR-I13, D-P9]

## 🔨 Tasks

- [ ] RED: test SCIM Users/Groups CRUD + token scoping + import-guard (AC1–3,5).
- [ ] GREEN: relocate `orvex/scim/*` into identity, sever imports; SCIM token store; SCIM-JIT via exchange (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — SCIM 2.0 Users/Groups CRUD + membership PATCH + SCIM bearer-token mgmt behind a timing-safe HMAC guard; NOTE the stores are in-memory reference impls (MemoryUserStore/etc), a Postgres-backed impl is deferred.

Tier: transport (`/scim/v2/*`) → scim service → domain → Postgres (SCIM state, SCIM tokens). AGPL sever (D-P9). Seam: enterprise IdP SCIM clients (vendor), engine (via exchange for JIT). Sibling: E4-S1 (exchange), E4-S4 (OIDC), E3-S2 (JIT).

## 🧪 Testing

`TestScimRelocation` (integration + import-guard). Tiers: integration + static analysis. CS §5: exercise SCIM handlers real; mock the external SCIM client where needed at the boundary.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (tenant-scoped SCIM tokens), AGPL sever. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F4 (FR-I13); Architecture `dQUjrSXhdp` A-SESSION; D-P9; OQ-I6.

## 🔗 Dependencies

Blocked by: **ENG-2101** + PB-1 counsel; parent epic **E4**. Intra-service: E4-S1, E4-S4, E3-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
