## 🎯 Story

As the **operator**, I want `/livez` (always-200) split from `/readyz` (real Postgres/Redis/JWKS round-trip), with `CELL_ID` required in prod (fail-boot), a distinct `CLUSTER_NAME`, and both echoed by the health endpoints, so that a dead-dependency pod reads NOT-Ready and identity is the reference cell-contract recipe.

**Definition of Done:** `TestReadyzDependencyRoundtrip` (integration layer) — through the health endpoints, `/livez` returns 200 without touching dependencies, `/readyz` round-trips Postgres/Redis/JWKS and reads NOT-Ready when any is dead, boot fails when `CELL_ID` is unset in prod, and `/livez`+`/readyz`+`/healthz` echo both `cell` and `cluster`.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given `/livez`, When probed, Then it returns 200 with no dependency touch. *assert: `/livez` → 200; 0 store calls* [Source: FR-I18, F4]
- [ ] **AC2** — Given `/readyz`, When a dependency is dead, Then it reads NOT-Ready and the pod is pulled. *assert: dead Postgres/Redis/JWKS → `/readyz` 503* [Source: FR-I18, NFR-I2, F4]
- [ ] **AC3** — Given prod, When `CELL_ID` is unset, Then boot fails (not resolve to `""`). *assert: unset CELL_ID in prod → fail-boot* [Source: FR-I18, F3, cell-contract rule 4]
- [ ] **AC4** — Given `CLUSTER_NAME`, When configured, Then it is a distinct parameter and all three health endpoints echo `cell` + `cluster`. *assert: `/livez`,`/readyz`,`/healthz` echo both* [Source: FR-I18, F3]
- [ ] **AC5** (negative) — Given `/healthz`, When wired, Then it is the CLI-doctor alias and MUST NOT be the readiness gate. *assert: readinessProbe → `/readyz`, not `/healthz`* [Source: FR-I18, F4]

## 🔨 Tasks

- [ ] RED: test livez-no-dep, readyz-dep-roundtrip-503, CELL_ID fail-boot, cell+cluster echo (AC1–4).
- [ ] GREEN: split `/livez`/`/readyz`; config loader requires CELL_ID in prod + distinct CLUSTER_NAME; echo both; point readinessProbe at `/readyz` (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — /healthz echoes CELL_ID + CLUSTER_NAME; MISSING the livez/readyz split (both probes hit /healthz) and CELL_ID is not strictly required (defaults to the "solo" sentinel).

Tier: transport (health endpoints) + config loader. Identity is the cell-claim minter/registry owner → **the reference recipe every satellite copies** (the miss propagates if not fixed here first). CELL_ID stays opaque/non-parsed (AWS-AZ grammar, rule 13). Seam: k8s probes. Sibling: E7-S3 (`/healthz` alias), E8-S1 (deps).

## 🧪 Testing

`TestReadyzDependencyRoundtrip` (integration + config-boot test). Tiers: integration. CS §5: real deps for the round-trip; config-boot exercised real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §10, cell-contract rule 4/13). SE-Arch 8sYi523i4t: Operational Excellence (dead-dependency-reads-green fix). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F6 (FR-I18), §5 (NFR-I2); Architecture `dQUjrSXhdp` §3/§6; Audit `YuY9XWpKPS` F3/F4.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E8**. Intra-service: E8-S1 (deps to round-trip), E7-S3 (healthz alias).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
