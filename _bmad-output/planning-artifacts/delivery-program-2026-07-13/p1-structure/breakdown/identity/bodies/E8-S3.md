## 🎯 Story

As the **cell-contract owner**, I want cell-guard/421 middleware on every route and `Idempotency-Key` on all internal step-APIs and webhook forwards, so that a mis-routed request never serves and 421/Stripe/Temporal retries replay safely.

**Definition of Done:** `TestIdempotencyReplayNoop` (integration layer) — through the middleware stack, a request whose cell does not match 421s (never serves), and a replayed `Idempotency-Key` on an internal step-API is a no-op (no duplicate side effect).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a request whose cell does not match, When it hits any route, Then the cell-guard returns 421 (never-serve). *assert: mismatched cell → 421, no handler executed* [Source: NFR-I5, cell-contract rule 3]
- [ ] **AC2** — Given an internal step-API call with an `Idempotency-Key`, When replayed, Then it is a no-op returning the original result. *assert: replay → same result, one side effect* [Source: NFR-I5, cell-contract rule 11]
- [ ] **AC3** — Given a 421-retry / Stripe-retry / Temporal-retry, When it replays a keyed request, Then no duplicate effect occurs. *assert: retried keyed request → idempotent* [Source: NFR-I5, rule 11]
- [ ] **AC4** — Given a webhook forward, When it carries an `Idempotency-Key`, Then duplicate deliveries are deduped. *assert: duplicate webhook → deduped* [Source: NFR-I5]
- [ ] **AC5** (negative) — Given an internal step-API call missing an `Idempotency-Key`, When required, Then it is rejected. *assert: missing key on required route → 4xx* [Source: NFR-I5]

## 🔨 Tasks

- [ ] RED: test cell-guard 421, idempotency replay no-op, missing-key rejection (AC1,2,5).
- [ ] GREEN: cell-guard/421 middleware on all routes; `Idempotency-Key` middleware + dedup store on `/internal/*` + webhook forwards (AC1–4).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — cellPinGuard 421 (StatusMisdirectedRequest) self-heal middleware present with an integration test; MISSING an HTTP Idempotency-Key on the internal step APIs (registry has a move-id ledger, not the request header).

Tier: transport middleware (applies across E1–E7, esp. E5 destructive step-APIs). SE-Arch F9 day-1 machinery. Seam: Redis/Postgres dedup store. Sibling: E5-S1/S2 (internal step-APIs), E8-S2 (cell config).

## 🧪 Testing

`TestIdempotencyReplayNoop` (integration). Tiers: integration. CS §5: real dedup store; exercise middleware real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (cell-contract rules 3/11). SE-Arch 8sYi523i4t: Reliability (idempotent replay), Security (never-serve on cell mismatch). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §5 (NFR-I5); Architecture `dQUjrSXhdp` §6 (F9 machinery); Audit `YuY9XWpKPS` F9.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E8**. Intra-service: E8-S2 (cell config), applied to E5 step-APIs.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
