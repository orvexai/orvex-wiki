## 🎯 Story

As a **data-residency / air-gap customer**, I want a self-host / BYOC option, so that Memory + wiki data can run in my own cloud/jurisdiction, with identity bundled (per D-S3 standalone deployments bundle identity).

**Definition of Done:** `TestByocSelfHostBundlesIdentity` (integration/structural layer) — through the BYOC packaging, a self-host deployment runs with identity bundled (interactive login always via identity, never a native engine path), aligning with the Turbopuffer BYOC-per-cell posture and honoring residency.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a BYOC/self-host deployment, When packaged, Then it bundles identity (interactive login via identity, no engine native path, D-S3). *assert: BYOC image bundles identity; no engine password path* [Source: FR-S3, D-S3]
- [ ] **AC2** — Given residency/air-gap needs, When deployed, Then data runs in the customer's cloud/jurisdiction. *assert: data plane in-customer-cloud; residency honored* [Source: FR-S3, NFR-I6]
- [ ] **AC3** — Given the BYOC-per-cell posture, When aligned, Then it matches the Turbopuffer BYOC-per-cell design (assumption). *assert: BYOC packaging aligns per-cell* [Source: FR-S3]
- [ ] **AC4** — Given first-run admin bootstrap on a self-host BYO-IdP, When it starts, Then it uses the gated SCIM/OIDC-RP seed through the bundled identity (never `/setup`, OQ-I4). *assert: bootstrap via bundled identity seed, not engine `/setup`* [Source: FR-S3, OQ-I4]
- [ ] **AC5** (negative) — Given a BYOC deployment missing bundled identity, When it boots, Then it fails (no fallback native login). *assert: no bundled identity → fail-boot, no native login fallback* [Source: FR-S3, D-S3]

## 🔨 Tasks

- [ ] RED: structural test — BYOC packaging bundles identity; no engine native path; bootstrap via identity seed (AC1,4,5).
- [ ] GREEN: BYOC/self-host packaging bundling identity; per-cell residency alignment; gated SCIM/OIDC-RP first-admin seed (AC1–4).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — residency primitives exist (residency_pin + residencyCells steer cell selection at Assign); MISSING the self-host / BYOC data-residency option itself.

Tier: packaging/deploy (BYOC manifest) + the bundled-identity bootstrap (ties to E4-S2/S4/S5, OQ-I4). Seam: customer cloud, Turbopuffer BYOC-per-cell. Sibling: E4-S2 (native login removed), E4-S4/S5 (OIDC/SCIM seed), E10-S1 (BYOK).

## 🧪 Testing

`TestByocSelfHostBundlesIdentity` (integration/structural). Tiers: structural + integration. CS §5: exercise the bootstrap path real; the customer cloud is the deploy target boundary.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: data-residency, Security (bundled identity, no native fallback). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` "Memory gap-closure — requirements" (FR-S3), §4 F4 OQ-I4; Architecture `dQUjrSXhdp` A-SESSION; D-S3.

## 🔗 Dependencies

Blocked by: **ENG-2101** + **OQ-I4** (standalone bootstrap); parent epic **E10**. Intra-service: E4-S2/S4/S5, E10-S1.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
