## 🎯 Story

As the **platform**, I want a server-side signup risk score that fuses device fingerprint + IP/ASN reputation + velocity into an **ALLOW / STEP-UP / DENY** decision, and fires heavy step-up (phone line-type→OTP, optional card+3DS) only on the flagged tail, so that friction lands on suspicion — never on the 100k happy path.

**Definition of Done:** `TestRiskScoreStepUp` (integration layer) — through the signup risk service, signals fuse into a score at signup and again at tenant-create → ALLOW/STEP-UP/DENY; a high-risk score HOLDS the AI-trial + cell provisioning behind a passed step-up; DENY is reserved for near-certain automation; the happy path adds zero friction.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given signup signals (device FP + IP/ASN + velocity), When fused, Then a score yields ALLOW / STEP-UP / DENY at signup and again at tenant-create. *assert: score → one of the three verdicts at both points* [Source: Anti-Sybil Layer 2]
- [ ] **AC2** — Given a STEP-UP verdict, When triggered, Then the AI-trial unlock + cell provisioning are HELD until a challenge passes (phone line-type pre-check → risk-conditional SMS OTP). *assert: STEP-UP → grants held; phone step-up gates unlock* [Source: Anti-Sybil Layer 2/3]
- [ ] **AC3** — Given a device fingerprint, When computed, Then the trial + tenant cap bind to the `visitorId` (a risk signal, never a hard cap). *assert: FP bound to trial+cap as a soft signal* [Source: Anti-Sybil Layer 2]
- [ ] **AC4** (negative) — Given a clean happy-path signup, When scored, Then it is ALLOW with zero added friction. *assert: clean signup → ALLOW, no challenge* [Source: Anti-Sybil Layer 2 (protect the funnel)]
- [ ] **AC5** — Given DENY, When issued, Then it is reserved for near-certain automation; an Art. 22 human-appeal path exists. *assert: DENY only at near-certain; appeal path present* [Source: Anti-Sybil Layer 2/3]

## 🔨 Tasks

- [ ] RED: test score→verdict at signup+tenant-create; STEP-UP holds grants; happy-path zero-friction (AC1,2,4).
- [ ] GREEN: risk-fusion service (device FP + IP/ASN + velocity, optionally a vendor consortium signal) → ALLOW/STEP-UP/DENY; phone line-type→OTP step-up; optional card+3DS+fingerprint-dedup for high-risk only; Art. 22 appeal (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** absent — no signup risk-score fusion / ALLOW-STEP-UP-DENY logic or risk-triggered step-up.

Tier: risk service → decisioning domain → step-up handlers. Branches into E9-S2's `provisioning_state`; the verdict *informs*, the principal ledger + caps *enforce*. Seam: device-FP vendor (Fingerprint/Stytch), IP/ASN feed, phone/card step-up (vendor). Sibling: E9-S1 (ledger), E9-S2 (staged provisioning/tiers), E9-S3 (edge signals).

## 🧪 Testing

`TestRiskScoreStepUp` (integration). Tiers: integration. CS §5: mock the FP/phone/card vendors at the boundary; exercise the fusion + decisioning real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (graduated response), GDPR (Art. 22 appeal, minimization; SMS-pumping guard via line-type pre-check). cell-lint JGAUQRsw2g.

## 🔗 References

Anti-Sybil `CYHyoUd7J1` §3 Layers 2/3; PRD `cnhla0qRRF` §4 F3 (FR-I9).

## 🔗 Dependencies

Blocked by: **ENG-2101** (vendor + threshold + step-up policy pinned at pack certification); parent epic **E9**. Intra-service: E9-S1/S2/S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
