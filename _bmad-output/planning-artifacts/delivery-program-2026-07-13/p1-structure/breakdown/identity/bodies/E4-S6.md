## 🎯 Story

As an **admin**, I want the fork's admin surfaces (Clerk settings/dashboard, OIDC provider CRUD, SCIM token management) relocated into identity behind Clerk-admin-org authz, so that identity configuration is managed in one closed service.

**Definition of Done:** `TestAdminSurfaceAuthz` (integration layer) — through identity's admin routes, Clerk/OIDC/SCIM config CRUD is reachable only behind admin-org authz (plus the offline break-glass path), and a non-admin caller is refused.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an admin-org member, When accessing admin config, Then Clerk settings / OIDC provider CRUD / SCIM token management are available. *assert: admin → 200 on admin routes* [Source: FR-I17]
- [ ] **AC2** (negative) — Given a non-admin caller, When accessing admin config, Then it is refused. *assert: non-admin → 403* [Source: FR-I17]
- [ ] **AC3** — Given OIDC provider CRUD, When an admin edits a provider, Then it persists and takes effect for the tenant. *assert: provider update reflected in resolver* [Source: FR-I17]
- [ ] **AC4** — Given admin config is an auth-flow-adjacent change, When accessed, Then it requires fresh (no-grace) introspection (high-priv op). *assert: admin route requires fresh introspect* [Source: FR-I17, FR-I3]
- [ ] **AC5** — Given the break-glass path, When admin-org authz is unavailable (IdP outage), Then admin config is reachable via break-glass only. *assert: break-glass admin access works; no other bypass* [Source: FR-I17, FR-I12]

## 🔨 Tasks

- [ ] RED: test admin-only access + non-admin refusal + fresh-introspect gate (AC1,2,4).
- [ ] GREEN: relocate admin config routes behind admin-org authz; OIDC/SCIM config CRUD; wire break-glass fallback (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — admin routes relocated (setEnforceSSO/clerkConfig/oidcProviders) behind tenant-scoped authz + restricted-token ceiling; MISSING a dedicated admin-org authz gate (today it is self-tenant scoping only).

Tier: transport (admin routes) → admin/config service → Postgres (OIDC/SCIM config). Admin config is a high-priv op → fresh introspection (E1-S4). Seam: Clerk admin org (vendor), break-glass (E4-S3). Sibling: E4-S3, E4-S4/S5.

## 🧪 Testing

`TestAdminSurfaceAuthz` (integration). Tiers: integration. CS §5: exercise admin handlers + authz real; mock the IdP admin-org membership edge.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (admin-org authz + fresh-introspect for high-priv). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F6 (FR-I17); Architecture `dQUjrSXhdp` §3 admin.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E4**. Intra-service: E4-S3 (break-glass), E4-S4/S5 (OIDC/SCIM config), E1-S4 (fresh introspect).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
