## 🎯 Story

As the **security + operations owner**, I want OTel + Prometheus per the house pattern and an audit record for **every token mint/revoke and provision/deprovision** to the family audit stream, so that identity's security-load-bearing actions are observable and forensically traceable.

**Definition of Done:** `TestAuditEveryPrivilegedAction` (integration layer) — through the audit path, every token mint, revoke, provision, and deprovision emits an audit record to the family audit stream, and OTel traces + Prometheus metrics are exported to the LGTM stack consumed by orvex-studio-console.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a token mint, When it commits, Then an audit record is emitted to the family audit stream. *assert: mint → audit record present* [Source: FR-I18]
- [ ] **AC2** — Given a revoke / provision / deprovision, When it commits, Then each emits an audit record. *assert: each privileged op → audit record* [Source: FR-I18]
- [ ] **AC3** — Given the service, When it runs, Then OTel traces + Prometheus metrics export to the LGTM stack consumed by console. *assert: traces/metrics reach LGTM* [Source: FR-I18]
- [ ] **AC4** — Given a break-glass use, When it occurs, Then it is audited + out-of-band alerted (ties to E4-S3). *assert: break-glass → audit + alert* [Source: FR-I18, FR-I12]
- [ ] **AC5** (negative) — Given a privileged op that fails, When it errors, Then the attempt is still audited (no silent privileged action). *assert: failed mint/deprovision → audit record of the attempt* [Source: FR-I18]

## 🔨 Tasks

- [ ] RED: test audit emission on mint/revoke/provision/deprovision incl. failures (AC1,2,5).
- [ ] GREEN: audit hook on every privileged op → family audit stream; OTel/Prometheus per house pattern → LGTM/console (AC1–4).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — audit stream is real (EventJournal + outbox events on mint/revoke/cell-assign); MISSING OTel/Prometheus — no /metrics endpoint (observability.yaml admits this) and no otel instrumentation in code.

Tier: cross-cutting telemetry/audit (wraps E2 mint/revoke, E5 provision/deprovision, E4-S3 break-glass). Observability feeds orvex-studio-console (D-S6), not Grafana directly. Seam: Kafka audit stream, LGTM/console. Sibling: E2, E5, E4-S3.

## 🧪 Testing

`TestAuditEveryPrivilegedAction` (integration). Tiers: integration. CS §5: real audit emission; the LGTM collector stubbed at its ingest boundary.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §10 ops). SE-Arch 8sYi523i4t: Security (audit every privileged action), Operational Excellence. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F6 (FR-I18); Architecture `dQUjrSXhdp` §4/§5.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E8**. Intra-service: E2 (mint/revoke), E5 (provision/deprovision), E4-S3 (break-glass).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
