## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the **token authority**, I want grantable scope **derived server-side at mint from authoritative sources only** — never from client-supplied scope or the caller's own claims — enforced deny-by-default inside `lib/auth`, so that the weakest satellite is not the security ceiling.

**Definition of Done:** `TestScopeDerivedServerSide` (integration layer) — through the mint + verify path, scope is computed from IdP org/role membership + an engine/knowledge membership call; a client-requested scope exceeding the minting user's is rejected; a scope-exceeding request at verify time is denied by the shared `lib/auth` verifier.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a mint request, When scope is computed, Then it is derived from IdP org/role + a membership call, ignoring client-supplied scope. *assert: mint ignores `req.scope`; result scope == server-derived* [Source: FR-I6]
- [ ] **AC2** — Given a user with role member, When minting, Then the token's exp/role cannot exceed the minting user's. *assert: `minted.role <= user.role && minted.exp <= user.exp`* [Source: FR-I6]
- [ ] **AC3** (negative) — Given a request for a space the user is not a member of, When minting, Then that space-scope is not granted. *assert: non-member space omitted from scope* [Source: FR-I6]
- [ ] **AC4** — Given a scope-exceeding request at verify time, When verified by `lib/auth`, Then it is rejected by the shared verifier (not per-satellite). *assert: `Verify(op requiring scope not in token)` → `ErrDenied` in lib/auth* [Source: FR-I6]
- [ ] **AC5** — Given scope is re-checked at introspection, When scope changes upstream, Then introspection reflects it within the TTL. *assert: reduced membership → introspect scope shrinks within TTL* [Source: FR-I6]

## 🔨 Tasks

- [ ] RED: test server-side derivation ignores client scope; exp/role ceiling; non-member space excluded (AC1–3).
- [ ] GREEN: scope-derivation service pulling IdP membership + engine/knowledge membership; land the real deny-by-default gate in `lib/auth` (AC1–4).
- [ ] RED→GREEN: introspection re-check of scope (AC5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — scope derived server-side (derivePlatformScope + intersectWithTokenScope, never client input) and enforced via RequireScope (lib/auth, deny-by-default).

Tier: mint service → scope-derivation domain → membership sources (engine/knowledge). The enforcement lives in `lib/auth` (E1-S2 hook). Seam: engine/knowledge membership call, IdP org/role (vendor). Sibling: E2-S1 (mint), E1-S2 (verifier).

## 🧪 Testing

`TestScopeDerivedServerSide` (integration). Tiers: integration + conformance (lib/auth deny). CS §5: mock engine/knowledge membership + IdP at the network boundary; exercise scope-derivation + lib/auth real (never mock own packages).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (deny-by-default ceiling, least-privilege). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F2 (FR-I6); Architecture `dQUjrSXhdp` A-TOKEN.

## 🔗 Dependencies

Blocked by: **ENG-2101** (contract TAG: token-scope schema + role enum); parent epic **E2**. Intra-service: E1-S2 (lib/auth), E2-S1 (mint), needs engine/knowledge membership seam.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
