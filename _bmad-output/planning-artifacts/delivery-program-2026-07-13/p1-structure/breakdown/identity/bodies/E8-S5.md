## 🎯 Story

As the **architecture governance owner**, I want the mandatory-ADR-trigger decisions **A-TOKEN/A-VERIFY** and **A-SESSION** authored as service-space ADR pages, so that the auth-flow + cross-service contract changes are governed per CS §9 / SE-Arch triggers rather than living only as embedded prose.

**Definition of Done:** `TestAdrPagesExist` (doc/structural check) — the A-TOKEN/A-VERIFY and A-SESSION ADRs exist at status draft in orvexstudioidentity (with the A-SESSION ADR cross-linked to the engine space `orvexwiki`), and each meets the CS §9 three-part test; A-CELL-REGISTRY remains a flagged family-space human action, NOT authored here.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the A-TOKEN/A-VERIFY decision, When filed, Then a service-space ADR exists documenting the shared `lib/auth` verifier + token wire claims/scoping (deny-by-default ceiling). *assert: ADR page present, status draft* [Source: FR-I18, SE-Arch §7]
- [ ] **AC2** — Given the A-SESSION decision, When filed, Then a service-space ADR exists documenting the removal of engine native-login endpoints, cross-linked to `orvexwiki`. *assert: ADR present + cross-link* [Source: FR-I18, SE-Arch §7]
- [ ] **AC3** — Given each ADR, When reviewed, Then it satisfies the CS §9 three-part test (costly-to-reverse ∧ reasonable-engineer-differs ∧ constrains-future). *assert: each ADR states the three-part rationale* [Source: FR-I18]
- [ ] **AC4** — Given A-CELL-REGISTRY, When considered, Then it is flagged as a family-space (orvexstudioarch) human action and NOT authored from this service space. *assert: A-CELL-REGISTRY flagged, not written here* [Source: SE-Arch §7]
- [ ] **AC5** — Given a contract-affecting note, When A-TOKEN/A-VERIFY lands, Then a contract note is added in orvex-studio-contracts. *assert: contract note referenced* [Source: FR-I18]

## 🔨 Tasks

- [ ] RED: structural check — ADR pages exist at draft with the required cross-links (AC1,2,4).
- [ ] GREEN: author A-TOKEN/A-VERIFY + A-SESSION ADRs (status draft) via the wiki write path (doc-amend); add the contracts note; flag A-CELL-REGISTRY for the family space (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — A-TOKEN/A-VERIFY/A-SESSION are captured in the service architecture doc under _bmad-output/planning-artifacts/architecture; MISSING dedicated standalone ADR files authored per SE-Arch §7.

Tier: documentation/governance (not code) — but a required part of the surface (SE-Arch §7 "authoring the ADR pages is the follow-up action"). Seam: orvexstudioidentity (ADR home), orvexwiki (A-SESSION cross-link), orvexstudioarch (A-CELL-REGISTRY, human action), orvex-studio-contracts (note). Sibling: E2 (A-TOKEN), E4-S2 (A-SESSION), E5-S4 (interim-exception ADR if kept).

## 🧪 Testing

`TestAdrPagesExist` (structural/doc check). Tiers: doc/structural. CS §5: n/a (doc work) — verify via wiki page presence + cross-links.

## 📏 Guidance

CS 6aMAzsYeQb §§0/9/11 (ADR triggers + honesty) + §§3/4/5/6/7/10. SE-Arch 8sYi523i4t §7 mandatory triggers. cell-lint JGAUQRsw2g.

## 🔗 References

Architecture `dQUjrSXhdp` §7 (ADR triggers); Audit `YuY9XWpKPS` F6; PRD `cnhla0qRRF` §4 F6.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E8**. Intra-service: reflects E2 (A-TOKEN) + E4-S2 (A-SESSION) decisions; A-CELL-REGISTRY is a family-space human action.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
