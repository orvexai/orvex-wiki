## 🎯 Story

As the **data owner**, I want persistence to be **Postgres (RLS + JSONB append tables) + Redis — Mongo struck (D-S12)** — with identity's own lifecycle-event journal as a range-partitioned Postgres append table (DROP-PARTITION retention), so that the data model matches the ratified doctrine.

**Definition of Done:** `TestPersistenceSplit` (integration layer) — through the data tier, tenant/token/SCIM/OIDC state lives in Postgres with RLS by tenant, the lifecycle-event journal is a range-partitioned append table with DROP-PARTITION retention, Redis holds JWKS/introspection/session/rate/deny-list, and there is NO Mongo anywhere.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the schema, When inspected, Then tenant registry, users, `idp_principal→tenant` mapping, token grants/scopes/revocations, SCIM state, OIDC config live in Postgres with RLS by tenant. *assert: RLS enabled; cross-tenant read blocked* [Source: FR-I16, NFR-I4]
- [ ] **AC2** — Given the lifecycle-event journal, When written, Then it is a range-partitioned Postgres append table with DROP-PARTITION retention. *assert: append table range-partitioned; retention = DROP PARTITION* [Source: FR-I16, D-S12]
- [ ] **AC3** — Given Redis, When used, Then it holds only JWKS/introspection/session-nonce/rate-buckets/push-revocation-deny-list. *assert: Redis keyspace == those caches* [Source: FR-I16]
- [ ] **AC4** (negative) — Given the deploy, When inspected, Then there is no Mongo claim/dependency anywhere. *assert: no Mongo in claims/go.mod/config* [Source: FR-I16, D-S12]
- [ ] **AC5** — Given residency, When data is placed, Then per-cell datastores respect residency (the global registry bits are separate, E6). *assert: per-cell Postgres/Redis; global registry is E6* [Source: NFR-I6]

## 🔨 Tasks

- [ ] RED: test RLS isolation, partitioned journal, Redis keyspace, no-Mongo (AC1–4).
- [ ] GREEN: Postgres schema (RLS + JSONB) + range-partitioned journal + DROP-PARTITION job; Redis caches; Crossplane claims (no Mongo) (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — JSONB append journal (EventJournal) + real Redis (RevocationDenyList) + tenant-scoped isolation asserted by grants/entitlements integration tests; MISSING DB-level Postgres RLS policies — isolation is application-tier "RLS-by-tenant" query scoping, no CREATE POLICY / ENABLE ROW LEVEL SECURITY.

Tier: infra/store tier for E1–E7. Mongo struck (D-S12). Seam: Postgres + Redis (per-cell), Crossplane claims. Sibling: every E1–E7 store operation; E6 owns the separate global registry.

## 🧪 Testing

`TestPersistenceSplit` (integration). Tiers: integration (real Postgres + Redis testcontainers). CS §5: real stores; no own-package mocks.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §5 data, §10 ops). SE-Arch 8sYi523i4t: data model matches ratified doctrine (Mongo struck), tenant isolation. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F6 (FR-I16), §5 (NFR-I6); Architecture `dQUjrSXhdp` A-DATA; D-S12.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E8**. Underpins E1–E7; distinct from E6 global registry.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
