## 🎯 Story

As the **edge geo-router**, I want the routing core mirrored to Workers KV and served through an edge-cached read path, so that I can resolve an authenticated org to its home cell without making identity's global bits a routing SPOF.

**Definition of Done:** `TestEdgeRoutingNoSPOF` (integration layer) — through the KV mirror + edge read path, the geo-router resolves an org to its home cell from cache/KV even with the registry write-store offline; per-cell identity replicas read the routing core via the same cached/KV path (not a hop to the global writer).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a routing-core write, When it commits, Then it mirrors to Workers KV. *assert: KV reflects the new `tenant→cell` binding* [Source: FR-I19]
- [ ] **AC2** — Given an authenticated orvex.ai request, When the edge geo-router resolves it, Then it routes to the org's home cell's studio-api. *assert: request lands in the correct cell* [Source: FR-I19]
- [ ] **AC3** (resilience) — Given the registry write-store is offline, When the geo-router resolves, Then it still routes from the KV/edge cache. *assert: routing survives a registry outage on cache* [Source: FR-I19, NFR-I2]
- [ ] **AC4** — Given per-cell identity replicas, When they read the routing core, Then they use the same cached/KV path satellites use (no SPOF hop). *assert: replica reads via KV/cache, not the global writer directly* [Source: FR-I19, NFR-I2]
- [ ] **AC5** — Given `stripe_customer→cell`, When webhook routing runs, Then KV also mirrors it so webhook routing survives a registry outage. *assert: KV holds `stripe_customer→cell`* [Source: FR-I19]

## 🔨 Tasks

- [ ] RED: test KV mirror on write, edge resolution, resilience with write-store offline (AC1–3).
- [ ] GREEN: KV mirror publisher on routing-core writes; edge read path for the geo-router + per-cell replicas; `stripe_customer→cell` mirror (AC1–5).
- [ ] NOTE: cache TTL vs push-invalidation is **OQ-I7** — pinned by the Definition Pack; gates the second cell.

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — the routing-core Resolve read path + a 2nd-cell routes-only replication prep (internal/replicate DryRun) exist; MISSING the Workers KV mirror + Cloudflare edge geo-router read path (no Cloudflare/KV code).

Tier: registry service → KV mirror publisher (infra) → Workers KV → edge geo-router (consumer). This is the routing hot path — must not be a SPOF (OQ-I7 gates cell #2). Seam: Workers KV, edge geo-router (global anycast). Sibling: E6-S1 (routing core), E6-S2 (assignment).

## 🧪 Testing

`TestEdgeRoutingNoSPOF` (integration). Tiers: integration (KV emulator + registry). CS §5: real KV mirror path; the edge geo-router is a consumer stubbed at its read boundary.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Reliability (no routing SPOF), Performance/freshness (OQ-I7). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F3 (FR-I19), §5 (NFR-I2); Architecture `dQUjrSXhdp` A-CELL-REGISTRY, §5; OQ-I7.

## 🔗 Dependencies

Blocked by: **ENG-2101** + **OQ-I7** (read-path scheme — pinned by the Definition Pack); parent epic **E6**. Intra-service: E6-S1, E6-S2.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
