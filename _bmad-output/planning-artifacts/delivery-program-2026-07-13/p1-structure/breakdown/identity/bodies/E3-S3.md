## 🎯 Story

As a **personal user upgrading to Teams**, I want the personal→Teams upgrade pass to mint a Clerk org and **re-key my tenant in place** carrying data + entitlements, with an explicit access transition, so that upgrading is seamless and new members never retroactively see my pre-upgrade private content.

**Definition of Done:** `TestUpgradeAccessTransition` (integration layer) — through the upgrade pass, a user-keyed tenant is re-keyed to an org-tenant in place (data + entitlements preserved, seats begin counting), and a new team member cannot read pre-upgrade private content.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a personal user-keyed tenant, When the upgrade pass runs, Then a Clerk org is minted and the tenant re-keyed in place. *assert: same `tenant_id`, `principal_kind` user→org, `org_or_realm` now the org* [Source: FR-I8, D-S17]
- [ ] **AC2** — Given the re-key, When complete, Then data + entitlements are carried and seats begin counting. *assert: pre-upgrade pages/entitlements present post-upgrade; seat count > 0* [Source: FR-I8]
- [ ] **AC3** (isolation, the DoD proof) — Given a new team member joins post-upgrade, When they read, Then pre-upgrade private content is not retroactively visible. *assert: member read of pre-upgrade-private → denied* [Source: FR-I8]
- [ ] **AC4** — Given the upgrade is a joint identity+billing workstream, When it runs, Then it is a named pass (not a silent background migration). *assert: upgrade emits an explicit `org.converted` lifecycle event; billing is invoked* [Source: FR-I8, FR-I15]
- [ ] **AC5** (error-path) — Given the upgrade fails midway, When it aborts, Then the tenant remains consistently personal (no half-migrated state). *assert: rollback leaves `principal_kind=user`, no orphan org* [Source: FR-I8]

## 🔨 Tasks

- [ ] RED: test re-key in place, data/entitlement carry, access transition, rollback (AC1–3,5).
- [ ] GREEN: upgrade-pass step API (org mint via Clerk + in-place re-key) emitting `org.converted`; access-transition boundary on pre-upgrade-private content (AC1–4).
- [ ] RED→GREEN: transactional rollback on partial failure (AC5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — polymorphic personal/org tenancy + ReactivateMembers + an org-convert journal event are referenced; MISSING the actual Personal->Teams upgrade pass (org mint + tenant re-key) and access-transition isolation — org-switch re-exchange is not honored.

Tier: upgrade step API → tenant service → registry domain + Clerk org mint. This is a **named identity+billing joint workstream** — billing carries entitlements, identity re-keys. Seam: Clerk (org mint, vendor), billing (entitlement carry), event spine (`org.converted`). Sibling: E3-S1 (registry), E5-S3 (event).

## 🧪 Testing

`TestUpgradeAccessTransition` (integration, with the isolation test explicitly). Tiers: integration. CS §5: real Postgres; mock Clerk org-mint + billing at the vendor/service boundary.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (personal↔org access transition), Reliability (atomic re-key). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F3 (FR-I8), Goals G3; Architecture `dQUjrSXhdp` A-DATA, A-JIT; D-S17.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E3**. Intra-service: E3-S1 (registry); cross-service billing joint workstream; E5-S3 (event).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
