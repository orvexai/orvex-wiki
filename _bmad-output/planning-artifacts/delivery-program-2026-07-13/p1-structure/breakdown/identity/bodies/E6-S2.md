## 🎯 Story

**Audit: already implemented** (evidence in §4) — this story is VERIFY + harden: prove the behaviour via the named DoD test + adversarial review; do not rebuild.

As the **registry**, I want to assign every tenant a home cell at creation by geo-proximity + residency choice and reserve globally-unique `tenant_id`/hostname, so that a tenant's data co-locates in one cell for its lifetime and two cells can never both mint the same name.

**Definition of Done:** `TestGlobalHostnameUniqueness` (integration layer) — through cell assignment, a new tenant is bound to a geo-appropriate cell respecting residency, and `tenant_id` + hostname/slug reservations are enforced GLOBALLY unique (two cells cannot both mint "acme").
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given tenant creation, When assigning a cell, Then it is chosen by geo-proximity + the tenant's residency choice. *assert: assignment == nearest cell honoring residency_pin* [Source: FR-I19]
- [ ] **AC2** — Given a residency choice, When it conflicts with proximity, Then residency wins. *assert: residency-pinned tenant lands in-jurisdiction even if not nearest* [Source: FR-I19, NFR-I6]
- [ ] **AC3** (negative) — Given "acme" already reserved in cell A, When cell B tries to mint it, Then it is rejected (global uniqueness). *assert: 2nd mint of same hostname/slug → conflict* [Source: FR-I19]
- [ ] **AC4** — Given a tenant, When bound, Then the cell binding holds for the tenant's lifetime (a cross-region change is a deliberate move, not drift). *assert: binding stable; change only via the tenant-move step API* [Source: FR-I19]
- [ ] **AC5** — Given single-cell defaults, When only one cell exists, Then everything defaults to the first cell and multi-region is a config addition (not a migration). *assert: single-cell default works; adding a cell is config* [Source: FR-I19, §7 rollout]

## 🔨 Tasks

- [ ] RED: test geo+residency assignment, residency-wins, global hostname/tenant_id uniqueness (AC1–3).
- [ ] GREEN: cell-assignment service (geo-proximity + residency); global uniqueness constraint on tenant_id + hostname/slug; single-cell default (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** present — Assign picks the home cell at creation by geo-proximity + residency pin and ReserveHostname enforces global hostname/slug uniqueness; idempotent re-assign.

Tier: registry service → assignment domain → routing core (E6-S1). Fires at tenant creation (E3). Free-tier writes stay off the hot path (deterministic in-jurisdiction cell computed at the edge; row lands lazily; hostname claims created only when a wiki is provisioned). Seam: edge (deterministic cell compute). Sibling: E3-S1/S2 (tenant creation), E6-S1 (store), E6-S4 (move).

## 🧪 Testing

`TestGlobalHostnameUniqueness` (integration). Tiers: integration. CS §5: real global CNPG; exercise assignment real.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: data-residency, Reliability (stable binding). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F3 (FR-I19), §7; Architecture `dQUjrSXhdp` A-CELL-REGISTRY; D-S5.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E6**. Intra-service: E6-S1 (store), E3-S1 (tenant); feeds E6-S3.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
