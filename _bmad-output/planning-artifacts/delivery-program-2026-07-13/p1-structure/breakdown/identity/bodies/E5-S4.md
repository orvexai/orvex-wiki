## 🎯 Story

As the **architecture owner**, I want the scaffold's CloudEvent-consuming `cmd/deprovisioner` retired in favor of the REST step-API, so that orchestration lives solely in orvex-studio-workflows (D-WF-1, "no double ownership") and the F2 defect is resolved.

**Definition of Done:** `TestDeprovisionerOwnershipReconciled` (integration/structural layer) — the CloudEvent-consuming `cmd/deprovisioner` no longer ingests `wiki.user.deprovision.requested` and orchestrates; identity is a stateless REST callee (`POST /api/clerk/deprovision {scope,id,mode}`) driven by workflows, with the Kafka Trigger removed (or an ADR filed for an explicit interim exception).
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the deprovision flow, When it runs, Then identity is invoked as a REST step-API by workflows (not via a self-consumed CloudEvent). *assert: deprovision arrives via `/api/clerk/deprovision`, not a Trigger* [Source: FR-I15, F2]
- [ ] **AC2** — Given the scaffold worker, When reconciled, Then `cmd/deprovisioner`'s CloudEvent consume + engine-call orchestration is removed. *assert: no `wiki.user.deprovision.requested` consumer in identity* [Source: FR-I15, F2]
- [ ] **AC3** — Given the Kafka Trigger `kafka-trigger-deprovision.yaml`, When reconciled, Then it is removed (or an ADR documents an interim exception). *assert: Trigger gone OR ADR present* [Source: F2]
- [ ] **AC4** — Given D-WF-1, When verified, Then identity runs no orchestration/dedup/timer for deprovision (all in workflows). *assert: no Temporal/orchestration code in identity for lifecycle* [Source: FR-I15, D-WF-1]
- [ ] **AC5** (negative) — Given a stray CloudEvent, When it arrives at identity, Then identity does not act as an orchestrator on it. *assert: no double-ownership path re-appears* [Source: F2]

## 🔨 Tasks

- [ ] RED: structural test — no `wiki.user.deprovision.requested` consumer; deprovision arrives via REST (AC1,2).
- [ ] GREEN: remove `cmd/deprovisioner` CloudEvent consume + Trigger; keep the REST step-API path (E5-S1) (AC1–4).
- [ ] RED→GREEN: if an interim exception is kept, file the justifying ADR (AC3).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — the reconciler CloudEvent worker + orphan-sweep are implemented (real Tenancy-backed SpineReconciler); MISSING the F2 resolution itself — retiring the CloudEvent worker in favour of a REST-callee-only path is still flagged an OPEN decision in-code.

Tier: removal of a scaffold worker + Trigger; the REST step-API (E5-S1) is the sanctioned path. This resolves SE-Arch F2 (the Clerk-POC worker in the wrong service). Seam: workflows (the sole orchestrator). Sibling: E5-S1 (REST callee), E8-S5 (ADR if interim exception).

## 🧪 Testing

`TestDeprovisionerOwnershipReconciled` (integration + structural). Tiers: integration + static/structural. CS §5: exercise the REST path real; the absence check is structural.

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11 (esp. §9 ADR if exception, §11 honesty). SE-Arch 8sYi523i4t: seam-ownership (no double ownership), event-design. cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F5 (FR-I15), D-WF-1; Architecture `dQUjrSXhdp` A-LIFECYCLE, §8 (F2); Audit `YuY9XWpKPS` F2.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E5**. Intra-service: E5-S1 (REST callee must exist first); E8-S5 (ADR if interim).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
