## 🎯 Story

As a **user on either front end**, I want to mint an MCP/agent token via `POST /v1/tokens` with my Clerk user session, where the endpoint treats **both front ends as untrusted input**, so that a confused-deputy cannot mint on my session.

**Definition of Done:** `TestTokenMintUntrustedFrontends` (integration layer) — through `POST /v1/tokens`, a request without CSRF/origin binding + first-party proof-of-user is rejected; a valid request from either the Studio or wiki front end succeeds; a cross-origin/forged-origin request is refused.
*Final H1–H17 elaboration + exact contract tag/versions are pinned at pack certification (ENG-2101); this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a valid Studio front-end session with CSRF+origin proof, When minting, Then it succeeds. *assert: `POST /v1/tokens` from Studio origin → 200 + token* [Source: FR-I5]
- [ ] **AC2** — Given a valid wiki front-end session, When minting, Then it succeeds (both front ends supported). *assert: `POST /v1/tokens` from wiki origin → 200* [Source: FR-I5, D-IDENT-5]
- [ ] **AC3** (negative) — Given a request missing CSRF/origin binding, When minting, Then it is rejected. *assert: no-CSRF → 403* [Source: FR-I5]
- [ ] **AC4** (negative) — Given a forged/cross-origin request on a victim's session (confused deputy), When minting, Then it is refused. *assert: mismatched Origin/Referer → 403; no token minted on victim session* [Source: FR-I5]
- [ ] **AC5** — Given the wiki front end (the exposed AGPL surface), When minting, Then first-party proof-of-user is required beyond the session cookie. *assert: session-cookie-only without proof → 403* [Source: FR-I5]

## 🔨 Tasks

- [ ] RED: test both origins succeed; no-CSRF and forged-origin rejected (AC1–4).
- [ ] GREEN: `POST /v1/tokens` with CSRF/origin binding + first-party proof-of-user; delegate mint to E2-S1 (AC1–5).

## 🧠 Context

**🧾 Code audit (origin/dev @ cf0c6e6, 2026-07-14):** partial — POST /v1/tokens is gated by requireSession (bearer) and mints via token.Authority; MISSING explicit CSRF/origin binding + first-party proof-of-user for the browser front ends.

Tier: transport middleware (CSRF/origin/first-party proof) → mint service (E2-S1). Seam: both front ends (untrusted callers). The wiki front end is the exposed AGPL surface — hardest threat. Sibling: E2-S1 (mint), E2-S3 (scope derivation).

## 🧪 Testing

`TestTokenMintUntrustedFrontends` (integration). Tiers: integration (HTTP). CS §5: exercise the real handler + middleware; mock only the IdP issuance edge (via E2-S1).

## 📏 Guidance

CS 6aMAzsYeQb §§0/3/4/5/6/7/10/11. SE-Arch 8sYi523i4t: Security (confused-deputy / CSRF on the exposed surface). cell-lint JGAUQRsw2g.

## 🔗 References

PRD `cnhla0qRRF` §4 F2 (FR-I5); Architecture `dQUjrSXhdp` A-TOKEN.

## 🔗 Dependencies

Blocked by: **ENG-2101**; parent epic **E2**. Intra-service: E2-S1 (mint), E2-S3 (scope).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
