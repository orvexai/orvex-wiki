## 🎯 Story

As a **ChatGPT user** I want **the extension to place a Memory-enriched composed prompt into the ChatGPT web-UI compose box without copy/paste** so that **I get an enriched prompt in the assistant with no memory API, exactly the biggest-hole case the PRD names (FR-D1, FR-D6)**.

**Definition of Done:** one named test `TestChatGPTAdapterInjectsEnrichedPrompt` (integration against a ChatGPT-shaped DOM fixture — the adapter resolves the compose surface via versioned selector fixtures and injects the enriched prompt; it never auto-submits; on selector miss it yields to the E5 fallback). *Exact selector fixtures + composed-prompt payload are pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the ChatGPT web UI, When delivery runs, Then the enriched prompt appears in the compose box, ready for the user to send. *Assert: compose value == enriched prompt; send not fired.* [Source: g9vWbSYplh FR-D1 (ChatGPT web UI, no copy/paste)]
- [ ] **AC2** — Given ChatGPT has no memory API, When delivery is chosen, Then the extension is the reach path (not sync-out); sync-out for API-bearing vendors is cross-referenced, not implemented here. *Assert: no vendor-memory-API call in this adapter.* [Source: g9vWbSYplh FR-D6; knowledge **ENG-2696** owns sync-out — cross-ref only]
- [ ] **AC3** — Given the ChatGPT compose selector cannot be resolved, When injection runs, Then the adapter returns `INJECTION_TARGET_MISSING` and hands off to fallback. *Assert: miss → typed miss, no partial write.* [Source: g9vWbSYplh FR-D7, FR-D5]
- [ ] **AC4 (negative)** — Given injection, When it completes, Then nothing beyond the compose field is read or modified. *Assert: only compose node mutated; no page-text read.* [Source: g9vWbSYplh FR-D5]

## 🔨 Tasks

- [ ] RED: `TestChatGPTAdapterInjectsEnrichedPrompt` (AC1, AC3, AC4).
- [ ] GREEN: `src/adapters/chatgpt` implementing the E3-S1 port, versioned selector fixtures, miss→fallback handoff (AC1–AC4).

## 🧠 Context

Tier placement: `src/adapters/chatgpt`. This is F1's flagship case — ChatGPT has no memory API, so the extension *is* the delivery. Outbound sync-out for vendors that do expose an API is knowledge's ENG-2696; this adapter does not duplicate it. Sibling: E3-S1 framework, E5-S2 canary watches these fixtures.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No ChatGPT adapter exists.

*Mechanism per the ENG-2689 verdict; on DEGRADE/NO-GO this story is re-scoped loudly, never silently.*

## 🧪 Testing

Named DoD `TestChatGPTAdapterInjectsEnrichedPrompt` (integration, ChatGPT-shaped fixture). Tiers: unit (selector resolution) + integration (injection). Live-UI coverage is E6-S2. CS §5: real DOM fixture, never a mocked adapter.

## 📏 Guidance

CS `6aMAzsYeQb` §§4/5/6/7; SE-Arch `8sYi523i4t` (ToS-safe own-surface, no auto-submit, fail-typed).

## 🔗 References

- PRD `g9vWbSYplh` FR-D1, FR-D6, FR-D5, FR-D7; F1 Gate (viability). Cross-ref knowledge **ENG-2696** (vendor-API sync-out) — do not duplicate.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — selectors + payload); parent epic **E3**; needs E3-S1.
- [ ] Blocks: E5-S2 (canary), E6-S2 (live E2E).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any ToS/breakage gap.
