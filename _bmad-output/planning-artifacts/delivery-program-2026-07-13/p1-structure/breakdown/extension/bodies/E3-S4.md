## 🎯 Story

As a **product owner** I want **an explicit Claude + Grok delivery posture — the beachhead set is covered honestly, with the correct path per vendor and honest "not yet supported" where injection isn't ready** so that **the beachhead promise (ChatGPT/Claude/Gemini/Grok) is met without over-claiming, and the Claude-native-memory sync path is pointed at its real owner (FR-D3, FR-D6)**.

**Definition of Done:** one named test `TestClaudeGrokPostureIsHonestAndRoutesSyncOut` (integration — for Claude and Grok the extension reports the correct posture: injection-adapter where implemented, else an honest "not yet supported"; and Claude's native-memory sync-out path is a documented cross-reference to knowledge ENG-2696, NOT re-implemented in the extension). *Exact posture matrix is pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the beachhead set, When posture is queried, Then ChatGPT + Gemini are injection-covered (E3-S2/S3) and Claude + Grok each report their true posture. *Assert: posture matrix has no "connected" for an unimplemented adapter.* [Source: g9vWbSYplh FR-D3 (beachhead coverage)]
- [ ] **AC2** — Given a not-yet-supported beachhead path, When shown, Then messaging is honest and non-actionable, never a fake-connect. *Assert: unsupported → "not yet supported," no connect CTA that no-ops.* [Source: g9vWbSYplh FR-D3]
- [ ] **AC3** — Given Claude has a native memory API, When sync-out is relevant, Then the extension defers to knowledge **ENG-2696** and makes no vendor-memory-API write itself. *Assert: no Claude memory-API call in the extension; posture links ENG-2696.* [Source: g9vWbSYplh FR-D6; ENG-2696 cross-ref]
- [ ] **AC4 (negative)** — Given Grok with no ready adapter, When delivery is attempted, Then it degrades to copy/paste (E5-S1), not silent failure. *Assert: unsupported delivery → fallback path.* [Source: g9vWbSYplh FR-D5, FR-D7]

## 🔨 Tasks

- [ ] RED: `TestClaudeGrokPostureIsHonestAndRoutesSyncOut` (AC1–AC4).
- [ ] GREEN: `src/registry/posture` (per-assistant posture matrix), ENG-2696 cross-ref wiring, fallback routing for unimplemented adapters (AC1–AC4).

## 🧠 Context

Tier placement: `src/registry/posture`. The boundary story: it draws the honest line between what the extension does (in-page injection, FR-D1) and what belongs to knowledge (vendor-API sync-out, ENG-2696) per FR-D6. It prevents the extension from re-implementing sync-out. Sibling: E2-S3 status surface renders this posture; E5-S1 fallback.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No posture matrix exists.

*Mechanism per the ENG-2689 verdict; on DEGRADE/NO-GO this story is re-scoped loudly, never silently.*

## 🧪 Testing

Named DoD `TestClaudeGrokPostureIsHonestAndRoutesSyncOut` (integration). Tiers: unit (posture derivation) + integration (fallback routing, no-API-call assertion). CS §5: assert absence of any vendor-memory-API call.

## 📏 Guidance

CS `6aMAzsYeQb` §§4/6/7; SE-Arch `8sYi523i4t` (honest scope, no duplication of a sibling service's ownership, no fake-success).

## 🔗 References

- PRD `g9vWbSYplh` FR-D3, FR-D6, FR-D5; knowledge **ENG-2696** (vendor-API sync-out, the real owner — cross-ref only); AD-5a vs AD-5b (extension writes, MCP is not this).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — posture matrix); parent epic **E3**; needs E3-S1, E2-S3.
- [ ] Blocks: E2-S3 status render, E5-S1 fallback wiring.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any scope/duplication gap.
