## 🎯 Feature area

The extension only counts as shipped when a non-technical user can install it from a store, it updates itself, it passes provider-UI E2E under CI, its popup meets the consumer-grade design bar (a11y + dual theme), and the FR-O4 connect target is verified by real usability testing. This epic owns store distribution (Chrome Web Store + Firefox AMO review constraints, versioning, auto-update) and the quality gates that certify the whole surface "looks good AND works."

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No store listing, release pipeline, E2E harness, or usability evidence exist. This epic enforces the family "delivered = looks good AND works, not gates-green" standard at the client edge. [Source: g9vWbSYplh FR-D1, FR-D2, FR-D7, FR-O4; AD-5a]

## 📋 Stories

- [ ] E6-S1 — Chrome Web Store + Firefox AMO packaging, review constraints, versioning & auto-update
- [ ] E6-S2 — E2E harness against provider web UIs: delivery + breakage-detection under CI
- [ ] E6-S3 — Popup a11y + dual-theme quality bar (consumer-grade)
- [ ] E6-S4 — FR-O4 first-run usability test: connect ≤3 taps ≤2 min, no config, verified

## 🔩 FR / NFR ids owned

AD-5a (the shippable client port), FR-D1 + FR-D7 (proven end-to-end against real provider UIs), FR-D2 + FR-O4 (usability-verified connect), consumer-grade a11y/dual-theme (NFR-5 accessibility).

## ✅ Definition of Done

- [ ] All 4 stories Done (reviewer ≠ implementer each).
- [ ] Signed packages pass Chrome Web Store + AMO review-constraint lint; auto-update channel validated; CI E2E proves delivery into a live-shaped provider UI and a forced breakage triggers fallback+alert; popup passes axe + both themes; a moderated first-run test records connect ≤3 taps / ≤2 min with zero config for a non-technical participant.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack). Mechanism per the **ENG-2689** verdict — packaging/E2E assume the extension mechanism; on DEGRADE/NO-GO these are re-scoped loudly, never silently. Builds on **E1**–**E5**.
- [ ] Blocks: production release / GA.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never "closes") → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any store-policy or accessibility gap.
