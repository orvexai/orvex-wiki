## 🎯 Story

As a **Gemini user** I want **the extension to place a Memory-enriched composed prompt into the Gemini web-UI compose box without copy/paste** so that **the second named beachhead web-UI target (FR-D1) is delivered on the same framework as ChatGPT**.

**Definition of Done:** one named test `TestGeminiAdapterInjectsEnrichedPrompt` (integration against a Gemini-shaped DOM fixture — the adapter resolves the compose surface via versioned selector fixtures and injects the enriched prompt; never auto-submits; selector miss yields to E5 fallback). *Exact selector fixtures + payload are pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the Gemini web UI, When delivery runs, Then the enriched prompt appears in the compose box ready to send. *Assert: compose value == enriched prompt; send not fired.* [Source: g9vWbSYplh FR-D1 (Gemini web UI, no copy/paste)]
- [ ] **AC2** — Given the Gemini compose selector cannot be resolved, When injection runs, Then the adapter returns `INJECTION_TARGET_MISSING` and hands off to fallback. *Assert: miss → typed miss, no partial write.* [Source: g9vWbSYplh FR-D7, FR-D5]
- [ ] **AC3** — Given injection, When it completes, Then nothing beyond the compose field is read or modified. *Assert: only compose node mutated.* [Source: g9vWbSYplh FR-D5]
- [ ] **AC4 (parity)** — Given the same composed prompt, When delivered to Gemini vs ChatGPT, Then both use the identical E3-S1 port and payload (no bespoke divergence). *Assert: both call the shared `InjectionPort`.* [Source: g9vWbSYplh FR-D1; AD-5a]

## 🔨 Tasks

- [ ] RED: `TestGeminiAdapterInjectsEnrichedPrompt` (AC1–AC3).
- [ ] GREEN: `src/adapters/gemini` implementing the E3-S1 port, versioned selector fixtures, miss→fallback handoff (AC1–AC4).

## 🧠 Context

Tier placement: `src/adapters/gemini`. Second of the two FR-D1 web-UI targets; proves the framework generalizes beyond ChatGPT. Sibling: E3-S1 framework, E5-S2 canary watches these fixtures, E6-S2 live E2E.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No Gemini adapter exists.

*Mechanism per the ENG-2689 verdict; on DEGRADE/NO-GO this story is re-scoped loudly, never silently.*

## 🧪 Testing

Named DoD `TestGeminiAdapterInjectsEnrichedPrompt` (integration, Gemini-shaped fixture). Tiers: unit (selector resolution) + integration (injection). Live-UI coverage is E6-S2. CS §5: real DOM fixture.

## 📏 Guidance

CS `6aMAzsYeQb` §§4/5/6/7; SE-Arch `8sYi523i4t` (ToS-safe own-surface, no auto-submit, shared port not bespoke).

## 🔗 References

- PRD `g9vWbSYplh` FR-D1, FR-D5, FR-D7; AD-5a.

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — selectors + payload); parent epic **E3**; needs E3-S1.
- [ ] Blocks: E5-S2 (canary), E6-S2 (live E2E).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any ToS/breakage gap.
