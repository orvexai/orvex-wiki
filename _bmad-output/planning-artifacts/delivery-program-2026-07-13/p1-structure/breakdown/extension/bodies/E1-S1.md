## 🎯 Story

As a **build engineer** I want **a Manifest V3 extension scaffold — service worker, popup shell, env-only config, and a bundler build wired into CI — that loads unpacked in Chrome and Firefox** so that **the write/compose client port (AD-5a) physically exists as the substrate every delivery, consent, and breakage story builds on**.

**Definition of Done:** one named test `TestExtensionLoadsAndServiceWorkerBoots` (integration — a headless-browser load of the built unpacked extension: the MV3 service worker registers, the popup document renders, the self-check reports healthy, zero console errors). *Exact manifest keys, bundler, and CI matrix are pinned at definition-pack certification (ENG-2690); this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the built artifact, When loaded unpacked in Chrome (MV3), Then the service worker registers and the popup renders with no console errors. *Assert: worker `activated`; popup root mounts.* [Source: g9vWbSYplh §7 (new browser-extension component); AD-5a]
- [ ] **AC2** — Given the same source, When built for Firefox, Then it loads under AMO-compatible MV3 with no manifest errors. *Assert: `web-ext lint` clean.* [Source: g9vWbSYplh FR-D3 (beachhead breadth implies cross-browser)]
- [ ] **AC3** — Given configuration, When the worker boots, Then all config is read from env/build-time injection only — no baked endpoints, no hardcoded secrets. *Assert: a grep gate finds zero literal URLs/keys.* [Source: AD-5a; family no-baked-defaults doctrine]
- [ ] **AC4 (error-path)** — Given a missing required config value, When the worker boots, Then it fails loud with a typed startup error, never a silent fallback. *Assert: self-check reports unhealthy with the missing key named.* [Source: g9vWbSYplh NFR-4]

## 🔨 Tasks

- [ ] RED: `TestExtensionLoadsAndServiceWorkerBoots` (AC1, AC4).
- [ ] GREEN: `manifest.json` (MV3), `src/worker/`, `src/popup/`, `src/config` (env-only loader), bundler config, `web-ext`/Chrome CI matrix (AC1–AC4).

## 🧠 Context

Tier placement: `src/worker` (background), `src/popup` (UI shell), `src/config` (env loader). This is the greenfield floor — no prior code. Sibling: E1-S2 (client codegen) and E1-S3 (permissions) land alongside.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No manifest, worker, popup, or build exist.

*Mechanism per the ENG-2689 verdict; on DEGRADE/NO-GO this story is re-scoped loudly, never silently* (a bookmarklet/native-helper verdict changes the substrate wholesale).

## 🧪 Testing

Named DoD `TestExtensionLoadsAndServiceWorkerBoots` (integration, headless browser). Tiers: unit (config loader) + integration (load). CS §5: drive the real built artifact, never a mocked worker.

## 📏 Guidance

CS `6aMAzsYeQb` §§0/3/4/6/7; SE-Arch `8sYi523i4t` (no-fallbacks on missing config, fail-loud startup).

## 🔗 References

- PRD `g9vWbSYplh` §7 (new browser-extension component), AD-5a (write/compose client port); Brief `brief-orvex-studio-2026-07-13/brief.md` (cross-AI delivery).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — manifest/bundler/CI contract); parent epic **E1**.
- [ ] Blocks: all of E1-S2, E1-S3, and every later epic.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE.
