## 🎯 Story

As a **client developer** I want **a generated TypeScript contracts client (from the pinned contracts tag) plus an AGPL-import/license denylist and a drift gate in CI** so that **the extension talks to Orvex only through the frozen contract, and can never link the AGPL engine into a distributed client bundle**.

**Definition of Done:** one named test `TestGeneratedClientMatchesTagAndBlocksAGPL` (integration — the checked-in generated client is byte-identical to a fresh codegen from the pinned tag; a planted AGPL/`@orvex/engine` import fails the license gate; a contract-drift shim goes RED when the tag advances). *Exact contract tag + codegen toolchain are pinned at ENG-2690; this story is dispatch-blocked until that tag exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the pinned contracts tag, When codegen runs, Then the generated TS client is emitted into `gen/` and is byte-identical to the checked-in copy. *Assert: `git diff --exit-code gen/`.* [Source: AD-5a; g9vWbSYplh §7 (contracts seam)]
- [ ] **AC2** — Given the bundle dependency graph, When the license gate runs, Then any AGPL-licensed or engine-internal import fails the build. *Assert: planted `@orvex/engine` import → non-zero exit with the offending path named.* [Source: family AGPL-guard doctrine; CLAUDE.md engine=AGPL]
- [ ] **AC3** — Given the tag advances without regeneration, When CI runs, Then the drift gate is RED. *Assert: stale `gen/` → gate fails.* [Source: g9vWbSYplh §7 (orvex-studio-contracts memory schema + MCP contract)]
- [ ] **AC4 (negative)** — Given a hand-edited generated file, When the codegen check runs, Then it fails (no manual edits to `gen/`). *Assert: diff non-empty → fail.* [Source: AD-5a]

## 🔨 Tasks

- [ ] RED: `TestGeneratedClientMatchesTagAndBlocksAGPL` (AC1–AC3).
- [ ] GREEN: codegen wiring in `gen/`, `scripts/codegen`, license-denylist CI step, drift shim (AC1–AC4).

## 🧠 Context

Tier placement: `gen/` (generated, never hand-edited) + `scripts/codegen` + CI. Seam: `orvex-studio-contracts` (the memory schema + MCP/delivery contract). Sibling: E1-S1 scaffold hosts it; E2 auth + E3 adapters consume the client.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No `gen/`, codegen, or license gate exist.

## 🧪 Testing

Named DoD `TestGeneratedClientMatchesTagAndBlocksAGPL` (integration). Tiers: unit (denylist matcher) + integration (codegen determinism). CS §5: run real codegen against a fixture tag; never mock the generator.

## 📏 Guidance

CS `6aMAzsYeQb` §§2/4/6/7; SE-Arch `8sYi523i4t` (no legacy shims, contract-first). AGPL-guard is a hard gate, not a warning.

## 🔗 References

- PRD `g9vWbSYplh` §7 (contracts seam, new extension component), AD-5a; CLAUDE.md (engine is AGPL, satellites/clients must not link it).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — the contract TAG is the dispatch gate); parent epic **E1**; needs E1-S1.
- [ ] Blocks: E2 (auth client), E3 (adapter payload types).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any contracts/license gap.
