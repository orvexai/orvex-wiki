## 🎯 Story

As a **security owner** I want **the delivery boundary to capture nothing beyond the compose surface and exfiltrate nothing off-device except the intended enriched prompt** so that **the extension, running on third-party pages, can never become a data-leak vector (FR-D5, security first-class)**.

**Definition of Done:** one named test `TestDeliveryBoundaryCapturesNothingBeyondCompose` (integration — during a full delivery the only DOM read is the sanctioned compose accessor, the only network egress is the Orvex API call for the composed prompt, and no provider page content, token, or telemetry-with-PII leaves the device). *Exact egress allowlist + telemetry redaction rules are pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given an active delivery, When DOM access is traced, Then the only page read is via the E1-S3 compose accessor — no full-page text, no other fields. *Assert: DOM-read trace ⊆ compose selectors.* [Source: g9vWbSYplh FR-D5 (no scrape); FR-S6 (untrusted page)]
- [ ] **AC2** — Given network activity, When egress is traced, Then the only outbound host is the Orvex API origin; provider page content is never posted anywhere. *Assert: egress hosts ⊆ {Orvex API}.* [Source: g9vWbSYplh FR-D5; NFR-2]
- [ ] **AC3** — Given telemetry/breakage signals (E5), When emitted, Then payloads are PII-redacted and carry no memory content or page text. *Assert: telemetry scan finds no memory/page strings.* [Source: g9vWbSYplh FR-D7 (alert without leaking); NFR-6]
- [ ] **AC4 (negative)** — Given a content script attempts an off-allowlist fetch/`sendMessage` with page data, When CI static-scans, Then it fails. *Assert: planted exfil call → RED.* [Source: family data-minimization doctrine]

## 🔨 Tasks

- [ ] RED: `TestDeliveryBoundaryCapturesNothingBeyondCompose` (AC1–AC4).
- [ ] GREEN: egress allowlist enforcement, telemetry redactor, DOM-read audit trace, static exfil-scan CI gate (AC1–AC4).

## 🧠 Context

Tier placement: `src/content/dom` (read audit) + `src/obs/redact` (telemetry) + CI static scan. This is the runtime complement to E1-S3's manifest-level constraints: E1-S3 limits what *can* be touched; E4-S3 proves at runtime nothing *is* exfiltrated. Sibling: E5-S2/S3 telemetry it redacts, E2-S2 token hygiene.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No egress allowlist, redactor, or exfil scan exist.

## 🧪 Testing

Named DoD `TestDeliveryBoundaryCapturesNothingBeyondCompose` (integration + static scan). Tiers: unit (redactor) + integration (DOM/egress trace). CS §5: trace a real delivery in a headless page; never mock the network layer.

## 📏 Guidance

CS `6aMAzsYeQb` §§4/6/7; SE-Arch `8sYi523i4t` (data minimization, egress allowlist, PII-safe telemetry, treat page as untrusted).

## 🔗 References

- PRD `g9vWbSYplh` FR-D5, FR-D7, FR-S6, NFR-2/NFR-6; the ui AI-privacy surface is **ENG-2697** (cross-ref only — that surface, not this boundary).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — egress allowlist + redaction rules); parent epic **E4**; needs E1-S3, E2-S2.
- [ ] Blocks: E5 telemetry rides this redactor; E6 store-review depends on a clean egress posture.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer, security lens) → TICK → DONE (orchestrator-only) → ESCALATE on any exfiltration finding.
