## 🎯 Story

As a **non-technical user** I want **the extension popup to meet a consumer-grade accessibility bar and render correctly in both light and dark themes** so that **the connect/status/consent surfaces are usable by everyone and look good as well as work (FR-D2, the family "looks good AND works" bar)**.

**Definition of Done:** one named test `TestPopupPassesA11yAndDualTheme` (integration — the popup passes automated accessibility checks (axe: roles, labels, focus order, contrast) and renders without visual breakage in both light and dark themes across the connect, status, and consent surfaces). *Exact design tokens/theme contract are pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given the popup surfaces (connect, status, consent), When axe runs, Then there are zero critical/serious violations (labels, roles, focus order, contrast). *Assert: axe critical+serious == 0.* [Source: g9vWbSYplh NFR-5 (non-technical accessibility); FR-D2]
- [ ] **AC2** — Given light and dark themes, When each surface renders, Then contrast and legibility hold with no clipped/invisible content. *Assert: both themes pass contrast; snapshot diff within tolerance.* [Source: g9vWbSYplh NFR-5; family dual-theme bar]
- [ ] **AC3** — Given keyboard-only navigation, When the user tabs through, Then every actionable control is reachable and operable. *Assert: full keyboard traversal; visible focus ring.* [Source: g9vWbSYplh NFR-5]
- [ ] **AC4 (negative)** — Given a control ships without an accessible name or with failing contrast, When CI runs, Then the a11y gate fails. *Assert: planted violation → RED.* [Source: family accessibility gate]

## 🔨 Tasks

- [ ] RED: `TestPopupPassesA11yAndDualTheme` (AC1–AC4).
- [ ] GREEN: theme tokens + `prefers-color-scheme` handling, ARIA/labels/focus management, axe CI gate (AC1–AC4).

## 🧠 Context

Tier placement: `src/popup/*` styling + a11y layer. This applies the consumer-grade "looks good AND works" bar to the E2-S1 connect, E2-S3 status, and E4-S1 consent surfaces — the visible face of the whole component. Sibling: E2-S3 status, E4-S1 consent, E6-S4 usability.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No popup styling, theming, or a11y exist.

## 🧪 Testing

Named DoD `TestPopupPassesA11yAndDualTheme` (integration). Tiers: unit (theme token resolution) + integration (axe + snapshot both themes). CS §5: run axe against the real rendered popup.

## 📏 Guidance

CS `6aMAzsYeQb` §§3/6/7; SE-Arch `8sYi523i4t` (accessibility as a gate, dual-theme parity, consumer-grade polish).

## 🔗 References

- PRD `g9vWbSYplh` NFR-5 (non-technical accessibility), FR-D2; Brief (consumer product first, friendliness a hard requirement).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — design tokens/theme contract); parent epic **E6**; needs E2-S1, E2-S3, E4-S1.
- [ ] Blocks: E6-S4 (usability test runs on the accessible popup).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any accessibility gap.
