## 🎯 Story

As a **security owner** I want **the delegated token issued with the minimum scope, held in secure extension storage, and refreshable/revocable — never leaked to a page or logged** so that **a compromised provider tab can never lift the user's Orvex credential, and consent/firewall enforcement (FR-D4) has a trustworthy principal to key on**.

**Definition of Done:** one named test `TestTokenIsScopedStoredSecurelyAndRevocable` (integration — the minted token carries only the delivery scope; it lives in extension-private storage never reachable from a content script/page context; revoke clears it and invalidates it server-side; it never appears in logs or the injected payload). *Exact scope set + refresh contract are pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a minted token, When inspected, Then it carries only the delivery/compose scope and no read-all or admin scope. *Assert: scope claim ⊆ allowlist.* [Source: g9vWbSYplh FR-D4 (principled least authority)]
- [ ] **AC2** — Given a provider page context, When it tries to read the token, Then it cannot — storage is background-worker-private, never exposed to content scripts. *Assert: content-script read returns undefined.* [Source: g9vWbSYplh FR-D5 (no secret leakage to third-party page)]
- [ ] **AC3** — Given the user revokes, When revoke runs, Then local storage is cleared and the server-side principal is invalidated. *Assert: subsequent delivery call → 401.* [Source: g9vWbSYplh FR-D4]
- [ ] **AC4 (negative)** — Given any log or injected payload, When scanned, Then the token never appears. *Assert: log/payload grep for the token = 0 hits.* [Source: g9vWbSYplh FR-D5, FR-S6]

## 🔨 Tasks

- [ ] RED: `TestTokenIsScopedStoredSecurelyAndRevocable` (AC1–AC4).
- [ ] GREEN: `src/auth/store` (worker-private secure storage), scope validation, refresh + revoke, log-redaction guard (AC1–AC4).

## 🧠 Context

Tier placement: `src/auth/store` (background-only) + `src/auth/scope`. The token never crosses into content-script/page context — the E1-S3 permission wall plus this storage boundary are the two halves of "no secret leakage." Sibling: E2-S1 mints it; E4 keys consent/firewall on the principal it carries.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No token storage, scope check, or revoke exist.

## 🧪 Testing

Named DoD `TestTokenIsScopedStoredSecurelyAndRevocable` (integration). Tiers: unit (scope validator, redactor) + integration (storage isolation, revoke). CS §5: real storage API in a headless extension context.

## 📏 Guidance

CS `6aMAzsYeQb` §§4/6/7; SE-Arch `8sYi523i4t` (least authority, secret hygiene, no token in logs/payloads).

## 🔗 References

- PRD `g9vWbSYplh` FR-D4, FR-D5, FR-S6; identity/BYOK seam (`orvex-studio-identity`, §7).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — scope + refresh/revoke contract); parent epic **E2**; needs E2-S1, E1-S3.
- [ ] Blocks: E4 (consent/firewall principal), all E3 adapters.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer, security lens) → TICK → DONE (orchestrator-only) → ESCALATE on any leak finding.
