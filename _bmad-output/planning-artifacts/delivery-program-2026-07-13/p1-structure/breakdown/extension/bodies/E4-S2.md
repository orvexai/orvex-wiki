## 🎯 Story

As a **user with separate personal and employer contexts** I want **the personal↔employer firewall enforced at the moment of delivery, and injection only ever into the assistant session I am actually using** so that **employer memory never crosses into a personal chat (or vice versa), and delivery stays ToS-safe by touching only my own compose surface (FR-D4, FR-D5)**.

**Definition of Done:** one named test `TestFirewallHoldsAtDeliveryAndOwnSurfaceOnly` (integration — a delivery carrying employer-scoped memory into a personal-scoped session is blocked; injection targets only the active user session's own compose surface; no cross-scope leak and no write to any non-owned surface). *Exact scope model + session-binding are pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given employer-scoped memory and a personal session, When delivery runs, Then it is blocked by the firewall. *Assert: cross-scope delivery → `FIREWALL_BLOCKED`; nothing injected.* [Source: g9vWbSYplh FR-D4 (honor the personal↔employer firewall)]
- [ ] **AC2** — Given a scope-matched delivery, When injection runs, Then it writes only into the user's own active session compose surface. *Assert: target == active-tab compose; no cross-tab/other-user write.* [Source: g9vWbSYplh FR-D5 (own session/compose surface)]
- [ ] **AC3** — Given delivery, When it completes, Then it never scrapes or reads provider content to make its scope decision — scope comes from Orvex's own memory metadata. *Assert: no page-content read in the firewall path.* [Source: g9vWbSYplh FR-D5]
- [ ] **AC4 (negative)** — Given an ambiguous/unknown scope, When evaluated, Then the firewall fails closed (blocks), never fails open. *Assert: unknown scope → blocked.* [Source: g9vWbSYplh FR-D4; FR-S5 (end-to-end firewall)]

## 🔨 Tasks

- [ ] RED: `TestFirewallHoldsAtDeliveryAndOwnSurfaceOnly` (AC1–AC4).
- [ ] GREEN: `src/consent/firewall` scope check + session binding, fail-closed default (AC1–AC4).

## 🧠 Context

Tier placement: `src/consent/firewall`, adjacent to the E4-S1 consent gate. This is the delivery-edge instance of the family personal↔employer invariant (FR-S5 end-to-end). Scope is read from Orvex memory metadata, never inferred from the provider page. Sibling: E4-S1 consent, E2-S2 principal, E3-S1 port.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No firewall check exists.

## 🧪 Testing

Named DoD `TestFirewallHoldsAtDeliveryAndOwnSurfaceOnly` (integration). Tiers: unit (scope decision, fail-closed) + integration (target binding). CS §5: real decision over fixture scope metadata.

## 📏 Guidance

CS `6aMAzsYeQb` §§4/6/7; SE-Arch `8sYi523i4t` (fail-closed firewall, own-surface only, no page-content scraping).

## 🔗 References

- PRD `g9vWbSYplh` FR-D4, FR-D5, FR-S5 (firewall end-to-end); Brief (personal↔org firewall).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — scope model + session binding); parent epic **E4**; needs E2-S2, E3-S1, E4-S1.
- [ ] Blocks: E3 adapters route through both consent (E4-S1) and firewall (E4-S2).

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer, privacy lens) → TICK → DONE (orchestrator-only) → ESCALATE on any fail-open path.
