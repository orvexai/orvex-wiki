## 🎯 Story

As a **QA owner** I want **an end-to-end harness that drives the extension against live-shaped provider web UIs — proving delivery lands and proving forced breakage triggers fallback + alert — under CI** so that **FR-D1 delivery and FR-D7 breakage-detection are certified against real provider surfaces, not just fixtures**.

**Definition of Done:** one named test `TestE2EDeliversAndDetectsBreakageAgainstProviderUIs` (E2E — a browser-driven run injects an enriched prompt into ChatGPT + Gemini compose surfaces and asserts it lands; a run with a deliberately stale selector asserts the canary trips, the copy/paste fallback engages, and a team alert fires). *Exact provider-UI test targets + CI cadence are pinned at ENG-2690; this story is dispatch-blocked until that contract exists.*

## ✅ Acceptance Criteria

- [ ] **AC1** — Given a driven ChatGPT + Gemini session, When delivery runs, Then the enriched prompt lands in each compose surface. *Assert: compose value == enriched prompt on both.* [Source: g9vWbSYplh FR-D1]
- [ ] **AC2** — Given a deliberately stale selector, When the E2E run delivers, Then the canary trips, fallback engages, and an alert fires. *Assert: broken→fallback+alert observed end-to-end.* [Source: g9vWbSYplh FR-D7]
- [ ] **AC3** — Given the E2E suite, When a provider UI drifts in reality, Then the suite fails in CI (it is the early-warning tripwire). *Assert: real drift → RED run.* [Source: g9vWbSYplh FR-D7 (durable delivery)]
- [ ] **AC4 (no-autosubmit)** — Given any E2E delivery, When it completes, Then no message is auto-sent to the provider — the harness asserts user-initiated send only. *Assert: no submit fired by the extension.* [Source: g9vWbSYplh FR-D5; NFR-2]

## 🔨 Tasks

- [ ] RED: `TestE2EDeliversAndDetectsBreakageAgainstProviderUIs` (AC1, AC2, AC4).
- [ ] GREEN: `e2e/` harness (browser-driver), provider-UI targets, stale-selector scenario, CI wiring + cadence (AC1–AC4).

## 🧠 Context

Tier placement: `e2e/`. This is the certifying gate the family "prove it end-to-end, not gates-green" standard demands for delivery — the live-UI complement to the fixture tests in E3/E5. It doubles as the early-warning tripwire when providers drift in production. Sibling: E3 adapters, E5 canary/fallback/alert.

**🧾 Code audit (2026-07-14):** absent — repo is a pre-definition scaffold (README only). No E2E harness exists.

*Mechanism per the ENG-2689 verdict; on DEGRADE/NO-GO this story is re-scoped loudly, never silently* (what "drive the extension against provider UIs" means depends on the chosen mechanism).

## 🧪 Testing

Named DoD `TestE2EDeliversAndDetectsBreakageAgainstProviderUIs` (E2E, browser-driven). Tiers: E2E only (the fixture tiers live in E3/E5). CS §5: drive the real built extension in a real browser; no mocks in the E2E lane.

## 📏 Guidance

CS `6aMAzsYeQb` §§5/6/7; SE-Arch `8sYi523i4t` (real end-to-end proof, tripwire coverage, no auto-submit).

## 🔗 References

- PRD `g9vWbSYplh` FR-D1, FR-D7, FR-D5, NFR-2; Phase-1 done-definition (works via every surface, proven E2E).

## 🔗 Dependencies

- [ ] Blocked by: **ENG-2690** (Definition Pack — provider targets + cadence); parent epic **E6**; needs E3-S2/S3, E5-S1/S2/S3.
- [ ] Blocks: GA sign-off.

## 📡 Protocol

CLAIM → PLAN → PROGRESS → COMMITS ("Part of ENG-NNN", never closes) → HANDOFF → REVIEW (reviewer ≠ implementer) → TICK → DONE (orchestrator-only) → ESCALATE on any flakiness/unprovable-delivery gap.
