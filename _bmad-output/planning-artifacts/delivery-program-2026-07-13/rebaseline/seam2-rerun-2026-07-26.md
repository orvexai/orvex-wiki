# Seam 2 re-run — 2026-07-26 (ENG-2053 closing evidence, partial)

Context: DEFECT-1 (cross-cutting.md) found the 402 QUOTA_EXCEEDED chokepoint
inert on the dev cell — `ORVEX_MODULES_ENABLED` off cluster-wide,
`ORVEX_BILLING_API_URL` unwired. The fix legs landed in 110694c0 (dev overlay
flip), 725090bd (base-wide cutover) and 6dcb72ca (billing URL wiring), but no
Seam 2 re-run artifact was ever recorded (`program-status-2026-07-14.md`:
"402 unproven-live"). This is that re-run, executed with
`scripts/verify/seam2-quota-402.sh` (added alongside this artifact).

**Result: the DEFECT-1 root cause is fixed live.** The engine on
`dev-docmost.eu-central-1.myidp.cloud` mounts 29 `/api/orvex/*` routes — the
module tree only registers when `ORVEX_MODULES_ENABLED === 'true'` on the
running pod, so the flag demonstrably took effect (contrast the original run:
`POST /api/orvex/session/exchange` 404'd because NO orvex route existed).

**Still open before the ticket is fully closed:**
- **Leg 3 (pod env):** needs `pods/exec` RBAC in `docmost-dev` (crew identity
  is namespace-scoped). Operator command:
  `kubectl exec -n docmost-dev deploy/docmost -- env | grep -Ei 'ORVEX_MODULES_ENABLED|ORVEX_BILLING_API_URL'`
- **Leg 4 (live 402):** needs an authenticated session on a tenant whose page
  cap is reached/reachable:
  `SEAM2_COOKIE_JAR=cookies.txt SEAM2_SPACE_ID=<uuid> scripts/verify/seam2-quota-402.sh`
  A 402 with the frozen `{error:'QUOTA_EXCEEDED',resource,limit}` body closes
  ENG-2053 for good.
- Note: the deployed build (`6bdfa5b2`, sourceRepo orvexai/docmost) predates
  the ADR-0049 session-exchange controller now on `dev`, so
  `/api/orvex/session/exchange` still 404s on this cell for staleness reasons,
  not flag reasons.

## Run log (verbatim)

```
# Seam 2 re-run — quota 402 chokepoint (ENG-2053)
date: 2026-07-26T13:44:23Z   target: https://dev-docmost.eu-central-1.myidp.cloud

### LEG 1 — engine health
GET /api/health -> {"status":"ok","info":{"database":{"status":"up"},"redis":{"status":"up"}},"error":{},"details":{"database":{"status":"up"},"redis":{"status":"up"}}}
LEG1: PASS

### LEG 2 — orvex module tree mounted (ORVEX_MODULES_ENABLED live)
live /api/orvex/* routes: 29
  /api/orvex/ai/ask
  /api/orvex/ai/related
  /api/orvex/audit
  /api/orvex/audit/self
  /api/orvex/content-handle/{handleId}
  /api/orvex/drift
  /api/orvex/events
  /api/orvex/events/connections
  /api/orvex/events/connections/{id}
  /api/orvex/events/head
  /api/orvex/events/log
  /api/orvex/events/settings
  /api/orvex/fetch
  /api/orvex/llms-full.txt
  /api/orvex/llms.txt
  /api/orvex/page-visuals/changelog
  /api/orvex/page-visuals/freshness
  /api/orvex/page-visuals/subpage-cards
  /api/orvex/pages/confirm-token
  /api/orvex/pages/duplicate-check
  /api/orvex/pages/duplicates
  /api/orvex/pages/ratify-token
  /api/orvex/pages/status
  /api/orvex/pages/supersede
  /api/orvex/pages/unsupersede
  /api/orvex/pages/verify
  /api/orvex/pages/{pageId}/page.md
  /api/orvex/settings/ratify-gate
  /api/orvex/spec-gate/check
deployed build: {"sha":"6bdfa5b2ebb62b9d151adbb6eb0ddf5135ece1f4","sourceRepo":"https://github.com/orvexai/docmost","integrations":["oidc","mcp","ai","linear"]}
LEG2: PASS

### LEG 3 — pod env carries the two DEFECT-1 keys (namespace docmost-dev)
LEG3: SKIPPED (no pods/exec RBAC in docmost-dev for this identity — run as an operator with cell access)

### LEG 4 — authenticated over-cap write returns the frozen 402 body
LEG4: SKIPPED (set SEAM2_COOKIE_JAR + SEAM2_SPACE_ID for a CAPPED tenant to run the live 402 probe)

== SEAM 2 RE-RUN: no failing leg ==
```
