---
topic: Orvex Studio — Agent Staging Area (safe agent write-buffer + librarian routing to wiki)
updated: 2026-07-10T04:44
---

- (decision) Dedicated PRD, NOT folded into main Studio PRD; page lands in orvexstudioarch space as DRAFT via doc-amend (Daniel: 'studioarchitecture space')
- (decision) Fast path elected — Daniel brain-dumped and delegated ('handle it properly' with research subagents); consolidated gap questions before drafting
- (decision) Staging area sits OUTSIDE the wiki store so agent writes never pollute wiki history; agents never write the wiki directly (Daniel)
- (decision) Staging ops: add documents, add sections, edit, replace, delete; the librarian consumes staging and decides routing into the wiki (Daniel)
- (decision) Librarian is a dedicated component with a dedicated prompt, tweakable per customer (Daniel)
- (decision) Feature surfaces as new sections in BOTH the Studio MCP server and the Studio CLI (Daniel)
- (event) Scale audit mandated: librarian interface + portal suspected not to scale (100-doc single-chat case); prosemirror beautification can be librarian's post-pass (Daniel's lean)
- (decision) Daniel mid-run: use ULTRACODE subagents to research similar platforms for staging area + especially cross-AI memory, and discuss findings with him before drafting
- (event) Research fan-out launched: wiki canon map + librarian/portal scale audit (opus agents) + ultracode prior-art workflow (sonnet profiles, opus synthesis)
- (event) Session limit killed the 4 named agents + workflow critics/synthesis mid-run; account switched and recovered: wiki-canon-map.md landed pre-crash; 4 scale-audit sub-fork reports extracted from transcripts; 10 staging platform profiles recovered from stray paths (workflow args arrived JSON-stringified so t.dir interpolated as 'undefined')
- (decision) Find-before-create verdict (wiki-canon): NO dedicated staging-area PRD exists, but 'Curator / Card Contract v1 / engineGate()' in orvex-studio-api + OPS space already implement a staging-like feature — Daniel must decide supersede/absorb vs new-peer-service before parent/title are fixed
- (event) Scale audit complete: librarian TODAY is a prompt (doc-librarian skill chain -> docmost-cli), not a service; per-customer prompts aspirational (prompt-studio near-skeleton); engineGate() ABSENT from code despite canon claim; Curator classify path 501; MCP save_page/edit write docmost DIRECTLY (wiki-api facade 501); 100-doc scenario confirmed broken: ~200 round-trips, unbounded history rows, no cross-page atomicity, 45s read-after-write staleness, human-gated one-question cadence
- (decision) Evidence seed: staging store sits OUTSIDE the wiki (drafts are full-cost pages rows w/ history) but REUSES wiki contracts as routing target (status lifecycle, human-only ratify, format:json/DfM writes, per-page block batch) — final call with Daniel
- (event) Prior-art landscape complete (14 platforms incl. 4 fills: Veeva Vault DCC, Atlassian Rovo, Notion Custom Agents, M365/SharePoint Knowledge Agent). Headline: big vendors govern direct writes rather than stage; only Sanity stages; nobody ships an AI reviewer that merges = white space; evidence argues against Dolt storage and unbenchmarked 100-doc atomicity
- (decision) Daniel: librarian default = RECOMMENDATIONS only, not autonomous; full-auto is an opt-in mode (per-customer autonomy dial); librarian must CONSTANTLY LEARN from human feedback on its recommendations; librarian also owns ongoing wiki maintenance/tidiness as a standing duty (not just intake routing)
- (decision) Daniel: staging store is NOT docmost ('would prefer not') — dedicated Postgres store outside the wiki; wiki contracts (status lifecycle, ratify gate, content-json/DfM writes) are the routing TARGET only
- (decision) Daniel: 'all the rest will go with your recommendation' — ownership = NEW platform services (studio-api/ai become clients; Curator/Card-Contract + Memory specs superseded w/ ADR); staging ChangeSet container model, state-gate atomicity, trust-tiered autopromotion + admission control, section-level granularity (novel-engineering budget), MCP-first bidirectional surface, two-sided provenance; tidy duties v1 = beautify + dedup + staleness/verification + structure/link hygiene, all recommendation-mode under the autonomy dial
- (change) Rubric review (strong, 0 crit/high, 3 med/7 low) resolved: beautification got a mechanical done-bar + conventions-slug assumption; FR-STG16 learning wired to SM-2 w/ tier-drift control; divert-to-memory Disposition wired to sibling Memory service (OPS Wiki-Memory-Trash shelves); queue buckets mapped to Disposition enum; SLO marked provisional-until-benchmark (benchmark = the gate); FR-STG25 credential-level framing; UJ-4 given Marta; addendum pointer added. Deferred to polish: prior-art shorthand trimming in FR bodies
- (change) Adversarial review (REVISE: 1 crit/6 high/7 med/4 low) fully resolved: C1 — enforcement chokepoint NAMED (orvex-wiki-api = only write ingress, agent creds 403; wiki-api promotion past 501 = prerequisite epic; engine fencing = infra not fork edit) + hard cut extended to CLI path & hidden tools + sequencing invariant (cut is LAST, gated on non-501 deps); H2/H3 — beautification moved to TRIAGE (review-what-you-apply), section-scoped, block-ID anchors, re-anchor sequencing; H1 — SLO gated on dep maturity + concurrency/rate-budget stated; H5 — Card v1 transfers VERBATIM (Proposal = strict superset, goldens must pass; Curator classifyOnSave becomes on-ramp, local path removed); H6 — routing-confidence score + mandatory one-question lane excluded from bulk-accept; M1 — UJ-2 rewritten to coherent recommend-dial state; M2 — partial-apply honesty + snapshot-before-mutate for destructive rollback; M3 — learning validated by held-out A/B, FR-STG17 scoped to manual edits v1; M4 — review-state gate on AI-groundability + SM-C3; M5 — memory-service + object-storage deps declared; M6 — sweeps scoped to agent-authored pages + separate maintenance budget; M7 — FR-STG27 cold-start defaults; L1 — ADR-registry blockage named as OQ7; L2 — SM-1 = 100% w/ leak alarm; L4 — authoritative-path reads + re-render-on-version-change
- (event) Finalize: input reconciliation vs Daniel's brain-dump complete — every spoken requirement traces to an FR (5 ops, no-direct-writes, tweakable librarian prompt, MCP+CLI sections, 100-doc scale, beautiful prosemirror docs, staging outside docmost, recommend-first + learning + tidy). Beautify timing note: Daniel said 'librarian can make them beautiful afterwards' — realized as beautify-at-triage for proposals (post-agent, pre-wiki) + FR-STG21 sweep for existing pages
- (decision) Memlog audit conducted as summary-for-skim (Daniel in delegate mode tonight) rather than line-by-line walk; full trail remains in this file
- (decision) Wiki handoff performed inline per external_handoffs contract (explicit --space orvexstudioarch) rather than invoking skill:doc-amend, because doc-amend binds to this project's own manual space while these PRDs are family canon per Daniel's explicit placement instruction
- (event) Handoff receipts: PRD page MyS79tK0k7 (draft, doc_type prd, top-level orvexstudioarch, sibling of Doc Governance) + addendum child MbK4S3xef1; pre-probe empty; server dup-guard passed; pending Daniel's doc-ratify
- (event) PRD finalized
- (decision) Daniel: EVERYTHING in v1, nothing deferred — full-auto dial ships v1 (default stays recommend, SM-C1 governs growth); Librarian prompt-pack self-revisions v1; full maintenance sweeps; scheduled ChangeSet publishing (new FR-STG28); prompt-pack marketplace v1-minimal (new FR-STG29, risk-flagged); knowledge-side retrieval trust-gating enforcement = v1 dependency deliverable; public surface = MCP+CLI+pinned OpenAPI. 6.2 now records the no-deferral decision; validation gates stay as sequencing
- (event) v1-everything revision republished to wiki: MyS79tK0k7 content replaced (outcome=updated, 2026-07-10T00:54Z); 29 FRs; staging addendum unchanged
- (change) PRD amended per workgraph rename (applied to wiki MyS79tK0k7): divert-to-memory Disposition renamed divert-to-workgraph (glossary, FR-STG8, FR-STG12 flagged bucket); §9 dependency orvex-studio-memory → orvex-studio-workgraph. FR-STG26 reindex-trigger refinement (wiki.* not staging.*) remains spine-flagged, NOT yet folded into the PRD
- (change) FR-STG26 refined per architecture AD-9 (Daniel: 'fold it in', 2026-07-10): wiki reindexing rides the ENGINE's wiki.* events post-apply (single mutation source of truth); staging events = queue/decision/audit telemetry, never a second reindex trigger; engine outbox named a gating prerequisite alongside FR-STG25's non-501 deps; §9 knowledge + engine-contracts dependency lines updated. Republished to MyS79tK0k7
