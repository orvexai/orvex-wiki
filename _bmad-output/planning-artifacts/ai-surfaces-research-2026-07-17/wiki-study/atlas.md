# Cross-Wiki Canon Atlas — AI-Surfaces Redo (orvex-wiki-api / orvex-studio-mcp / orvex-cli)

Synthesis of 19 per-space study digests over ~368 pages of the local read-only wiki mirror (`/home/daniel/repos/orvex-studio/.cache/docs/`, synced 2026-07-17, treated as current). This atlas is the find-before-create + binding reference for re-baselining the three AI-facing surfaces. It does **not** re-table every page — the exhaustive per-page tables live in the 19 digests under `/tmp/claude-1000/-home-daniel-repos-orvex-wiki/bdff3111-9ca4-4cf6-9661-f1749df35294/scratchpad/wiki-study/` (one file per space). Sections 1–5 below are the atlas proper.

**Authority order** (from `orvexstudioarch/GmKOk6xz0J` + `MYRICqtrEj`): family canon (`orvexstudioarch`) → the owning service's own space → `orvexwiki`. Within a service space: the deployed artifact wins over canon, canon wins over README/ticket. Draft ≠ canonical; frontmatter `status` is unreliable on Wave-3/4 packs (many say `canonical` in frontmatter, `DRAFT / never self-ratify` in body — trust the body).

**The four known 2026-07-16/17 live-state anchors** used throughout as the "current reality" yardstick: (a) MCP live-green on dev = the **namespaced hero-13** catalog, first 3-repo E2E 6/6 GREEN 2026-07-17T12:48Z; (b) wiki-api `/v1` verb grammar is **live-serving** (only ~3 endpoints honest-501); (c) streaming is folded into existing verbs (R21/ADR-0002), not new tools; (d) `orvex-studio-audit` (#18) PRD/architecture/ADR-0037 ratified 2026-07-16.

---

## 1. CORPUS MAP — role, load-bearing pages, what each space settles

Nineteen spaces. For each: role, the load-bearing pages (slug — title — status), and one paragraph on what it settles for the redo. "LB pages" = the pages a redo author must read; the remaining pages in each space are inventoried in that space's digest.

### 1.1 `orvexstudioarch` — Family architecture canon (122 pages) [digest: arch-1/2/3.md]
**Role:** the constitution, the ADR registry, the cell contract, and the per-service canon pages. Highest authority.
**LB pages:** `CxjFpIVUZY` Orvex Studio — Architecture & Principles (canonical); `6aMAzsYeQb` Coding Standards (canonical); `8sYi523i4t` SE Architect — Review Agent (canonical); `9VUHxAcoXw` Issue Authoring Prompt (canonical); `32Huug8U4B` Decision Records / ADR registry (canonical); `MYRICqtrEj` Decision Records D-P/D-S ledger (frontmatter archived, functions as live top-canon); `JGAUQRsw2g` Multi-Region Cells + 14-rule cell contract (canonical); `pTeVSp9P5f` Systems Architecture (canonical); `z5K777kNkD` Event-Driven Architecture (canonical); `yXUWpQpRjx` Phase-1 Definition Factory prompt + live-repo survey (draft); `WON2z73Gie` orvex-studio-mcp page (draft, 2026-07-17 correction banner); `dCNYFnoLNF` orvex-wiki-api page (draft); `B66dINHNvs` orvex-cli page (draft). ADRs binding the redo: `7E0Hbb9it2` ADR-0003 402 (canonical), `8kTZK91vHd` ADR-0028 402-enrollment (canonical), `JLMuwgc9fV` ADR-0008 change-authority (canonical), `QbEBPuKcGR` ADR-0035 Go↔TS bridge (canonical), `9KcwAG5SRg` ADR-0020 MCP cell-discovery routing (canonical), `F7tTjMNVAr` ADR-0013 MCP raw-Kafka-consumer + knowledge-direct clarification (canonical), `WZWmazrlS0` ADR-0029 MCP edge-verifier cutover (canonical), `hSJ5tgNZMm` ADR-0021 wiki-api caller-token pass-through (draft/pending), `pYkl7HpOgr` ADR-0007 CloudEvent envelope (canonical), `qZFmNWGY47` ADR-0011 tenant→cell registry (canonical), `yOyavcWHEz` ADR-0030 cell claim on Principal (canonical), `K5rumy9a8c`/`HylAAMRbPO` ADR-0001/0012 polymorphic tenancy (canonical), `pbNe8DJtvx` ADR-0038 MCP streaming (draft), `QnouxhOHK5` ADR-0032 in-fork /mcp decommission (canonical), `Z6WrJjZDAO` ADR-0037 audit founding (canonical), `yNFx3YyNap` ADR-0033 work-claim arbiter (canonical), `12aDkq4iOd` ADR-0034 credential lanes (canonical), `7Okcxv4i3Y` ADR-0040 claim-lease (draft, Part A PO-ruled), `RJkFGHoht4` ADR-0036 Clerk lifecycle (draft).
**Settles:** the whole rule surface — the 13 principles, the frozen 402 vocabulary, the 14-rule cell contract, codegen discipline, the domain-pure routing law, the ADR change-lane model, tenancy, the envelope, and MCP's per-verb routing lanes (wiki→wiki-api, ask→ai, search/RAG→knowledge-direct). **Caveat:** its own MCP roster rows in `CxjFpIVUZY` are self-flagged wrong (see §3), and its build-state framing systematically understates reality (per `yXUWpQpRjx`).

### 1.2 `orvexwikiapi` — orvex-wiki-api service (9 pages) [digest: orvexwikiapi.md]
**Role:** the Go strangler-facade terminating the agent contract in front of the engine + knowledge. **A direct redo target.**
**LB pages:** `pG9s9OjqZA` Architecture (canonical); `8jAiCBifDW` PRD (**superseded**, still the substantive FR-A1..A24 SoR); `yB3U1Ui0Zc` Architecture Audit (canonical); Wave-3 pack `5NTAkEn8KZ` Build Prompt / `fJogMGcbBb` Contract Summary / `RPduIa4x9Y` PRD-delta / `XS7DetLXAH` SDD / `fwHouFgtIh` Test Plan (frontmatter canonical, body draft).
**Settles:** verb grammar `search/get/save/edit/list` over `{resource_type, locator}`; **no `ask` verb ever** (D-A12); one atomic engine `apply-ops` write per batch; CAS `ifVersion`→409; receipt `{url,id,version,persisted:true}`; doc-governance (drift + spec-gate) re-homed here from the engine (D-S8); caller-token pass-through, no elevated credential; AGPL clean-room (`pkg/dfm`, no `@docmost/*` import). **MR-A1 (the blocker):** the contracts `wiki-api.yaml` is a flat 8-op draft grammar that structurally diverges from the deployed RESTful `/v1/{resource}/{locator}` server — the tag exists but does not describe the served surface. **MR-A6:** MCP "vendors-but-bypasses" wiki-api per the 2026-07-15 snapshot.

### 1.3 `orvexstudiomcp` — orvex-studio-mcp gateway (19 pages) [digest: orvexstudiomcp.md]
**Role:** the stateless TS agent front-door. **A direct redo target — and the one already re-baselined 2026-07-17.**
**LB pages:** `ZGjLctEnGH` **WDS Re-baseline 2026-07-17 — THE ANCHOR** (draft field, ratified en bloc live); `k1sWjtJq3x` PRD post-split repoint (canonical, ratified, carries the re-baseline front-section); `BE0jTNoweS` Architecture (canonical, body pre-re-baseline/stale); `O6o61ov2e7` Live State & Ops Runbook (draft, current ground truth); `ok9tiXDCjB` ADR-0002 progress-notification streaming/R21 (draft); `lR3kpc0Ra2` ADR-0001 /v1 client regen + ask→ai (draft); Wave-3 stale-baseline pack `ppYIXBL4Cp`/`koIvfpnSbX`/`8nngxUpOXY`/`tQcQ01U2iy`/`RcvZDU2Sds`. Historical/Era-1-2 (superseded): `2hPabxB7Fh`, `986V0taYy5`, `lRERyG7Zpj`, `CIemV9m0eb`, `CjaHVTg8Id`, `6e2jqBdknZ`, `J8nvlh1ctw`, `BZldxVAmdR`.
**Settles:** the current surface = namespaced **hero-13** (`whoami`, `list_tools`, `wiki_get`, `wiki_save` [merged upsert of save_page+edit], `knowledge_search`, `ai_ask`, `memory_recall`, `staging_propose`, `workgraph_prime/ready/claim/save/handoff`); writes **ON by default** + hard `READ_ONLY` ceiling + per-tenant Tenant Governance Profile (R-WDS-3); MCP is the **main route** to staging + workgraph (R-WDS-2); three-plus upstream lanes with knowledge **direct** for search/RAG; token pass-through / no minting; marketplace/skill tools **kept separate** (reverses the Decision-4 fold, R-SEAM-3); streaming as a requirement on verbs, not new tools.

### 1.4 `orvexcli` — orvex-cli (9 pages) [digest: orvexcli.md]
**Role:** the one Go/cobra platform CLI, namespace-first. **A direct redo target.**
**LB pages:** `pf10XC2Qjz` Architecture (canonical, build-state stale); `R4AOVBLST7` PRD (**superseded**); `EJ9WgVAuls` Architecture Audit (canonical); `PACF13d3I3` Audit Findings & Rebirth Plan (draft, 474-line evidence base); `UQFNh4QEmw` PRD-delta (canonical); `lhqTzMTPCj` SDD (canonical); `6NVIjKeiWs` Build Prompt (draft); `9AdZkNTlyj` Contract Summary (draft); `8GNuGKq8wn` Test Plan (canonical).
**Settles:** from-scratch rewrite (D-S21 amended, docmost-cli = behavioral reference only, no history seed); namespaces `wiki/search/ai/auth/admin` (+ `workgraph`, `staging` reserved); domain-pure routing (wiki→wiki-api, search→knowledge, ask→ai, auth→identity, engine never contacted); contracts-tag codegen; in-process `pkg/dfm`; frozen 0-9 exit codes + ~19/58-code errorCode vocabulary; SSE cache-sync wire contract. **Live defect D-CLI1:** local `go test` false-green via ambient `DOCMOST_API_TOKEN`; CI RED since 2026-07-13 (unverified past 2026-07-15). Open: MR-CLI1 host form, MR-CLI2 Go codegen bridge (ADR-0035 is TS-only — no Go stubs), MR-CLI4 SSE cursor + content_pm parity.

### 1.5 `orvexstudiocontracts` — the pinned seam repo (9 pages) [digest: orvexstudiocontracts.md]
**Role:** the Apache-2.0 non-running contract set (OpenAPI/CloudEvents/SSE/error-vocab/fixtures) all three surfaces codegen from.
**LB pages:** `o2waDNw3ix` Architecture (canonical, stale "~90% unbuilt"); `jwF4VLHfNs` PRD (**superseded** frontmatter, still the FR-C1..C24 SoR every newer page cites); `OI7v2rfZEG` Contract Summary (canonical/draft); `w25dZASIbG` PRD-delta (canonical/draft); `Zy0pKYt4bo` Build Prompt; `fagdfAvx1B` SDD; `nV1HysTsZZ` Test Plan; `nngOgO0CGO` Audit; `OCyvakxVTP` Go-TS bridge proof (draft, "demonstrated not done").
**Settles:** `wiki-api.yaml` = `search/get/save/edit/list`, **no `ask`** (D-ASK); `ai.yaml` owns the entire cited-ask/RAG loop; codegen source is always a local contracts checkout, never the served AGPL descriptor; write receipt `{url,id,version,persisted}`; `ifVersion` = monotonic version/etag not wall-clock; the detailed SSE wire contract (FR-C10); ADR-0008 two-layer change authority (automated additive lane vs ADR+human-ratify breaking lane). **Blockers:** only 7/38 SEAMS rows pinned; `wiki-api.yaml`/`ai.yaml` still `x-status:draft`; no v0.1.4 tag; MR-1 (unnamed human ADR-lane ratifier, OQ-C1) blocks any breaking change; MR-2/3/4 (Free-tier AI shape + spend-cap spellings/currencies) block the ai.yaml GA cutover.

### 1.6 `orvexstudioknowledge` — search/RAG/SSE backbone (11 pages) [digest: orvexstudioknowledge.md]
**Role:** the single multi-corpus search/RAG/SSE/export/CLI-sync engine — MCP's direct search lane and the CLI's freshness source.
**LB pages:** `azRwTCZMqw` Architecture (canonical, self-contradicts on isolation grade); `dCbFzRQGDr` PRD (canonical); `0mFImku9Gp` R-9 Search-Stack Consolidation (draft); `oqrEtMg46M` PRD-delta (canonical); plus `eFq4WnWOGC` SDD, `XZiMOK2l4N` Test Plan, `3z7lJ1t9N9` Build Prompt, `6Wb2fYVQI2` Contract Summary, `NdH4benMGe` Audit, `dcOUbpzE8L` Addendum.
**Settles:** knowledge is THE one search service family-wide (Decision 4/D-S14, ranking lives here permanently); the **A1 universal ACL choke point** (engine FR-13 ∩ token-scope, never cached) binds every content-bearing egress including MCP tools and CLI verbs; single Postgres (RLS + JSONB projection) + Turbopuffer + S3 + Redis, Mongo struck; the SSE/cache-sync wire contract (FR-K15/K15a/K20) is load-bearing for the CLI. **Unresolved MR-K1:** memory-corpus isolation grade contradicts itself on the canonical page (attribute-scoped-default vs namespace-wall). MR-K6 cursor + MR-K7 content_pm parity gate cutover.

### 1.7 `orvexstudioai` — the AI brain (8 pages) [digest: orvexstudioai.md]
**Role:** owner of the full cited-ask RAG loop; MCP's third upstream (ask/chat/inline/generate call ai directly).
**LB pages:** `pbKI3BpQmY` PRD (canonical); `Cb0XBkNezd` Architecture (canonical, stale on free-tier doctrine); `oaD5XUHqfb` Audit (canonical); Wave-3 `gmvqNoNNax`/`n1elHysgrl`/`0zSYFVo5OW`/`E6N8BWHqR7`/`y6eOtSn7Ox` (frontmatter canonical, body draft).
**Settles:** wiki-api's `ask` verb is **removed** — agents ask ai; the K5 cited-answer shape `{answer, citations[], confidence, unanswered, gapNote, followups}` is ai-owned and frozen-by-convention; every AI write (incl. ai's own inline/draft) goes through wiki-api's block chokepoint with the caller's token; LiteLLM is the sole model gateway; retrieval always carries the caller's delegated principal into knowledge. **Open:** MR-AI1 (deployed contract at `/v1/*` 4 ops vs mandated `/api/ai/*` ~20 ops) blocks all 50 stories; free-tier doctrine flip (D-AI12, model-class allowlist) is ruled but deployed code still runs the retired 10-lifetime-action gate.

### 1.8 `orvexstudiostaging` — Agent Staging Area + Librarian (8 pages) [digest: orvexstudiostaging.md]
**Role:** the future sole agent write-path into the wiki (agents `propose`, Librarian triages/applies).
**LB pages:** `mOzNDhn322` PRD (canonical); `P5R7eCXJA0` PRD Addendum (canonical); `GOacy9kdJz` Architecture (canonical, AD-1..AD-14); Wave-2 `Iy1ePNnJOM`/`YfpVs4uJtG`/`0CTtbzRvjM`/`zJWnHp1WHq`/`huRZk58726` (draft).
**Settles:** the eventual **hard cut** of ALL agent wiki-write surfaces (MCP `save_page`/`edit`, CLI wiki writes → 403 at wiki-api ingress for agent-class creds), sequenced LAST and **not yet executed** (staging's apply path is still a `doc.go` stub); `orvex-wiki-api` named the ONLY write ingress (NFR-STG5); the `staging_*` MCP tool names and `orvex-cli staging` namespace; DfM/PM-JSON-only payloads; Idempotency-Key on all writes; 402 not 429. R-WDS-2 makes MCP the main route to staging.

### 1.9 `orvexstudioworkgraph` — cross-agent coordination "beads" store (40 pages) [digest: orvexstudioworkgraph.md]
**Role:** cluster-internal MCP+CLI-first coordination satellite; architecturally isolated from wiki-api but a precedent source.
**LB pages:** `bnbRUdCm6R` Architecture (canonical); `Kgp6JT3IOR` PRD (canonical); `2KrwsvY5zL` Addendum (canonical); `IhfwJqlNpc` PRD-delta (canonical); `gRHhBOQKaM` Interface Architecture / AX index (draft) + 9 Interface Contract pages + 8 DS-Pn pattern pages + 5 UX Scenario pages (all draft, all 2026-07-17); `nJjVVot6j5` SDD, `us7THM542L` Test Plan, `YlKGzO3eUe` Build Prompt, `8ukmD2OPPr` Contract Summary (canonical).
**Settles:** precedent only for the redo — the frozen `Error/QuotaExceeded/Page/Banner` envelope family (ADR-0003), the anti-footgun doctrine ("247 issues destroyed by obeying error text"), the token-budget/A-N1 abandonment guard (lazy schema discovery + `brief=true` + result compaction), the `orvex-cli workgraph` namespace-first + frozen 0-9 exit-code extension, and the `studio.memory.*`/`studio.workgraph.*` naming-reservation collision boundary. `workgraph_*` verbs are hero-tier on the MCP now (R-WDS-2). **This space's most fully-worked "AX interface architecture" pass is the closest existing template for a cross-surface AI research/design artifact.**

### 1.10 `orvexstudioaudit` — Centralized Compliance Audit Service #18 (4 pages) [digest: orvexstudioaudit.md]
**Role:** the family-wide WORM/tamper-evident audit sink. Same-day-fresh (ratified 2026-07-16).
**LB pages:** `vhC5XXCYkC` PRD (canonical, 38 FR/10 NFR); `iWvaOcrzRD` Architecture Spine (canonical, AD-1..AD-16); `zPssXupzLZ` Research (canonical, 545 lines); `bEZ31uqofJ` Addendum (draft); companion `orvexstudioarch/Z6WrJjZDAO` ADR-0037 (canonical).
**Settles:** 7 named producers (engine, wiki-api, cli, console, identity, ai, billing) — **MCP is not named** (silent gap the redo must close); events-only integration via outbox→relay→spine (never point-to-point); wiki-api's `/v1/audit` re-points from engine to the shared service; CLI dual-write stays unchanged; all reads via `/v1/audit/*` under a dedicated audit-reader scope, every read itself audited; never block on audit (0ms latency); 7-year retention; `audit.<category>.<verb>` taxonomy governed by contracts/ADR-0008 gated on ADR-0010/MR-C3.

### 1.11 `orvexstudioidentity` — auth spine + tenant→cell registry (10 pages) [digest: orvexstudioidentity.md]
**Role:** dual-IdP verify/mint authority, CLI's auth home, owner of the global registry. Furthest-along satellite.
**LB pages:** `dQUjrSXhdp` Architecture (canonical); `cnhla0qRRF` PRD (**superseded**); `ntuw6s4E5P` PRD-delta (canonical); `mZdESLTkvP` SDD (draft); `KxfO1u4PYU` Support-Access spec (draft, PO ruling R24); `CYHyoUd7J1` Anti-Sybil (draft); `k3J1v8ovfG` Contract Summary; `2CEeUYe7LL` Build Prompt; `QJ4phlmRcl` Test Plan.
**Settles:** every satellite verifies via `orvex-studio-lib/auth` (deny-by-default, scope derived server-side only); MCP/agent tokens frozen-at-mint, no client-supplied scope; CLI seam is identity-owned (`GET /v1/whoami`, device-auth mint, doctor); polymorphic tenancy (personal = user-keyed, no Clerk org); high-privilege ops need fresh no-grace introspection. **Gaps:** contract is a 10-op draft skeleton vs 47 real routes (MR-I2); `POST /v1/registry/move` served unauthenticated (MR-I8); MR-I1 Clerk-deprovision no-op resolved-by-ADR-0036 but not built; `/readyz`/`/livez` don't exist.

### 1.12 `orvexstudiolib` — shared Go library (8 pages) [digest: orvexstudiolib.md]
**Role:** the mandatory Go substrate (`pkg/auth`, `pkg/dfm`, `pkg/events`, typed clients) wiki-api/CLI import.
**LB pages:** `0NPvDR3C0m` Architecture (canonical); `JxnqaaYx3j` Architecture Audit (canonical); `fG2jKeRGiu` PRD (**superseded**); `5GaAv3nwu1` PRD-delta (draft); `M5H7Ny9wRs` Build Prompt, `S14cg8tNbb` Contract Summary, `jBCeoGljBJ` SDD, `AIelgWK5mH` Test Plan.
**Settles:** `pkg/auth` = the family security ceiling (identity imports it too), with `authtest` V1-V12 conformance (currently unverified in ANY consumer — the redo could be the first to close it); `pkg/dfm` = clean-room DfM↔PM serializer shared by wiki-api + CLI in-process (no engine `/convert` calls); F-QUOTA 402 typed terminal/non-retryable via `engineclient`; `pkg/security.IsPrivateIp` must be called before any user-URL dial; ADR-0035 is **TS-only** — lib's Go clients are and stay hand-authored. **Live defect FR-L31:** `pkg/obs.Setup` silently succeeds while doing nothing (false green). MR-1 contracts-pin authority blocks correctly testing sibling clients.

### 1.13 `orvexstudiobilling` — entitlement/quota SoR (8 pages) [digest: orvexstudiobilling.md]
**Role:** the plan→entitlement→cap system-of-record; never on any hot path.
**LB pages:** `Blcvui4UIn` PRD (canonical); `o2ZqSrZjE6` Architecture (canonical); `LnLqFdVJgT` PRD-delta (canonical); `4zOSYmRSLX` Contract Summary; `LLjofewAZs` Build Prompt; `D2U0IUQ6p0` SDD; `GgOHp6sd0x` Test Plan; `ukKiIMWYRg` Audit.
**Settles:** the ONE frozen family op `GET /v1/entitlements/{principal_type}/{principal_id}` (`getEntitlement`, x-status:active) — every AI surface reading entitlements consumes this exact shape via generated client; the 402 `QUOTA_EXCEEDED` frame authored once, propagated verbatim; billing DECIDES/SERVES/PUSHES but never ENFORCES — wiki quota enforcement is the **engine's** write chokepoints (incl. collab/Yjs), **not wiki-api** (content is creatable bypassing wiki-api); act-as-user keys on the acting delegated principal's tenant; locked Free/£7 quota tables. Only 1 of 7 ops frozen; 14 served paths uncontracted.

### 1.14 `orvexstudioapi` — Studio product BFF (8 pages) [digest: orvexstudioapi.md]
**Role:** the TS/Hono product BFF; owns Studio domain (skills/Memory/marketplace); Memory system-of-record.
**LB pages:** `ekTh7nDQqo` Architecture (canonical, 2026-07-17 D7/D11/D12/D13 fold-in); `85qj2wwU2L` PRD (**superseded** by the WDS FR-reconciliation register); `HjtdumU48C` PRD-delta (canonical); `2i7tNkqgvX` Build Prompt; `RwWnI1bWxS` Contract Summary; `o5FIxdJn5t` SDD; `DsfM4wZxDy` Test Plan; `TpxkDsKTkC` Audit.
**Settles (binding on MCP write-verb design):** **D11 two machine-write lanes** into Memory/wiki — a **direct-write** lane (provenance-stamped, `unreviewed`-flagged, deterministic fail-closed screen, per-user globally configurable, may be the only Free-tier machine-write option) AND the curated Librarian confirm lane; the confirm gate is the sole *curated* path, no longer the sole path. **D12 tri-axis denial:** 402 QUOTA_EXCEEDED (period) / 403 ENTITLEMENT_REQUIRED (tier) / 429 + Retry-After (burst) — distinct axes, "reads never 429" is a quota-axis promise only. **D7 host doctrine:** region-neutral `api.studio.orvex.<env>`. "Memory" is a live 3-way naming collision (api/workgraph/knowledge, MR-2). DocmostPort re-point to wiki-api is still open (OQ-SA6). **Live defect D-API2:** event spine 0% wired (Outbox/Relay throw); D-API3 isolation e2e RED.

### 1.15 `orvexstudioworkflows` — single Temporal control plane (9 pages) [digest: orvexstudioworkflows.md]
**Role:** the family's one home for all durable/multi-step/retryable/timer-bearing work.
**LB pages:** `JiIyp0RLLJ` Architecture (canonical); `4IF3xjIdAs` PRD (**superseded**); `DxXIJWyzWb` PRD-delta (canonical); `HdRj1oIXYJ` Contract Summary; `D8DsxAX2JE` Build Prompt; `T4WvwYI9GE` SDD; `ra8InY9IF1` Test Plan; `7LGGFR5tGE` Audit.
**Settles:** D-WF-1 — no saga/retry-loop/timer logic anywhere in MCP/wiki-api/CLI; delegate to workflows via a step-API + spine CloudEvent; **no inbound start-workflow HTTP API** (A-W10, enqueue = CE-on-spine only); step-API contract discipline (idempotent, cursor-chunked, complete-in-response, typed retry class); Idempotency-Key mandatory on `/internal/*`. MR-W-DEPROV resolved by ADR-0036 (workflows owns full Clerk lifecycle); MR-W-INGEST open. Live P0 loss window (10-attempt retry cap on provision/deprovision legs).

### 1.16 `orvexstudioconsole` — admin/observability BFF (11 pages) [digest: orvexstudioconsole.md]
**Role:** the human operator console + LGTM observability BFF; a consumer of the AI surfaces' SLIs, never an agent target.
**LB pages:** `kldiZu93EC` PRD (canonical); `L1NVnmRgpy` Architecture (canonical, stale on audit-trigger + workflows-proxy); `1YS71L9neD` Contract Summary; `EJ5L3wIepg` PRD-delta; `U0M9YqGPdb` SDD; `AwD2Fn7rIt` Test Plan; `OkqxIlMGlD` Build Prompt; `Ii9q9kBNms`/`TYWAtnvliD` PostHog UBA (draft).
**Settles:** console does NOT serve agents/scripts (agents use satellites' own contracts-authored APIs — no console-authored contract for the redo to target); the "no elevated credential on a mutation path" rule (FR-CT16/D-CT6, cites wiki-api FR-A16); the obs attribute vocabulary (service/tenant/cell/trace_id/workflow_id) any new MCP tool / wiki-api endpoint must conform to for console's health board; troubleshooting UI = console over LGTM, not Grafana; message-tracing-across-outbox is the named family-wide gap console surfaces.

### 1.17 `orvexstudioui` — Studio SPA (9 pages) [digest: orvexstudioui.md]
**Role:** thin BFF-only React SPA; not an AI surface but a source of layering + draft-vs-committed patterns.
**LB pages:** `DmJsnB5Z9Y` Architecture (canonical, "scaffold" framing stale); `UwsOKqter2` Amendment (draft); `xsRMrju3D1` PRD (**superseded**); `57IqCbTRAW` PRD-delta; `aUBuYRrCZM` SDD; `m5KMQAVKUi` Test Plan; `41XQMj2CV9` Build Prompt; `cxLMFgGAYw` Contract Summary; `1dFHMUtcKS` Audit.
**Settles:** A-SHELL one-upstream discipline (independent confirmation of the no-layering-shortcuts rule); draft-vs-committed as an AI-authorship invariant ("AI proposes, human accepts" surfaced as a proposal/applied distinction the API/MCP response contract must carry, not client heuristics); the cell-bearing session token shape (not raw Clerk JWT); the Studio Decision Records ADR registry is still TBD (a shared blocker for any redo ADR).

### 1.18 `orvexstudio` — umbrella / product / memory gap-closure (26 pages) [digest: orvexstudio.md]
**Role:** the product vision + the cross-service Memory gap-closure PRD/architecture (the load-bearing cluster for MCP's memory role).
**LB pages:** `g9vWbSYplh` PRD Memory Gap-Closure (canonical); `iiCcKhGptV` Architecture Spine Memory Gap-Closure (canonical, AD-4 stale vs D11); `vBvVDFklZo` Fold-in Map (canonical); `rgBOQh31p3` Product Brief Orvex Studio (canonical); `3z78laG6dB` Addendum Product Brief incl. D11 (canonical); `fr7YaPq8Tl` Product Brief The Librarian (canonical); `CSqjqciAX9` Vision (canonical, stale wedge); `XqxC4ayK4v` Family Map (canonical); `FC23qWA8n3` Memory Feature Gap (canonical).
**Settles:** MCP's memory role is **read-only retrieval under I-4** (ACL ∩ delegated principal) — MCP is "not a compose adapter" (AD-5b); the compose/delivery path is a separate new unowned browser-extension (F1/AD-5a); the six-surface acceptance protocol (api/mcp/cli/ai/rag/knowledge-sync) is the Phase-1-done definition and requires CLI to round-trip through wiki-api `/v1` (byte-faithful DfM); all cross-service surfaces pinned in contracts before build (AD-11). **Contains a live AD-4-vs-D11 contradiction** and the 19-vs-73 MCP tool-count ambiguity (see §3).

### 1.19 (companion notes)
The full ADR set and the engine internals live in `orvexstudioarch` (§1.1) and `orvexwiki` (the engine, 40 pages — digest orvexwiki.md). `orvexwiki` load-bearing pages for the redo: `pVDJS0woHl` PRD-delta (current), `twQ3BBzpTE` Architecture, `ILXUWrSgvh`/`w91FbgAvCy`/`gcecRSYLsK`/`ajywjRNC8a` Wave-3 pack, `W613jcEl3k` Free-at-Scale quota table, ADR-0001..0005 (`5uf23v6wIW`/`wpphVwQ3aO`/`Nr3WIs0Zpt`/`MXKW6srwyD`/`sABdrZoQdp`). It settles: engine exposes exactly one write primitive `apply-ops`; F-QUOTA is engine-resident and non-bypassable (both REST and collab paths); `wiki.*` events (not `studio.wiki.*`); the AGPL DfM import boundary; the 402 payload `{quota,state,limit,usage,percent,graceEndsAt?,resetsAt?,plan,upgradeUrl,message}` explicitly stated to also be the MCP tool-error payload shape.

---

## 2. BINDING REGISTER — every commitment that constrains the MCP/API/CLI redo

Each binding: **[status]** essence — source(s). "canonical" = ratified page/ADR; "draft" = mature-but-unratified; "ruled" = PO decision recorded but not yet a ratified ADR/page.

### 2.1 Layering & routing
- **B1 [canonical] Domain-pure routing is absolute.** All wiki-shaped programmatic access (page CRUD, history/diff/version, import/export, attachments, comments, drift, audit) routes through `orvex-wiki-api`; the engine raw API is internal-only; the React client is the ONLY platform-wide engine-direct exception. — `MYRICqtrEj` D-S16, `orvexwiki/twQ3BBzpTE` A-APIBOUNDARY, `orvexwikiapi/pG9s9OjqZA`, `orvexcli/pf10XC2Qjz`.
- **B2 [canonical] Per-verb upstream lanes for MCP (and by analogy CLI).** wiki verbs → wiki-api; ask/chat/inline/generate → ai (D-M7/D-A12); search/related/RAG/neighborhood → **knowledge DIRECT** (D-M8 / PO ruling 9 / ENG-1403). The wiki-api-for-search intermediary is explicitly retired. — `orvexstudioarch/F7tTjMNVAr` (2026-07-17 clarification), `orvexstudiomcp/k1sWjtJq3x` FR-M7, `orvexstudioai/pbKI3BpQmY`. **Note the still-live canon bug: `CxjFpIVUZY` prints the WRONG (via-wiki-api+ai) split — see §3.**
- **B3 [canonical] MCP is the main route to staging + workgraph, not wiki-only.** — `orvexstudiomcp/ZGjLctEnGH` R-WDS-2.
- **B4 [canonical] No workflow/saga/retry/timer logic in any AI surface.** Delegate to workflows via a step-API + a spine CloudEvent; there is no inbound start-workflow HTTP API (enqueue = CE-on-spine only). — `orvexstudioworkflows/4IF3xjIdAs` D-WF-1 / A-W10.
- **B5 [canonical] wiki-api owns only `search/get/save/edit/list`; no `ask` verb ever.** The cited-ask/RAG loop is ai's alone (D-ASK); routing ask-shaped intent through wiki-api is an ADR-trigger violation. — `orvexstudiocontracts/jwF4VLHfNs` D-ASK, `orvexwikiapi/pG9s9OjqZA` A-VERBS, `orvexstudioai/pbKI3BpQmY`.

### 2.2 Verb grammar & write semantics
- **B6 [canonical, ruled] Verb namespacing + merged upsert.** MCP tools are `namespace_verb`; `save_page`+`edit` are merged into one `wiki_save(id?)` CAS upsert. Any wiki-api-facing redo confirms its grammar matches the merged-upsert shape. — `orvexstudiomcp/ZGjLctEnGH` R-WDS-1 / R-SEAM.
- **B7 [canonical] One atomic engine write primitive.** Atomic multi-op batches map to exactly ONE engine `apply-ops` call; never decompose a batch into N calls. — `orvexwiki/EPsdD7uK8e`, `orvexwikiapi/pG9s9OjqZA`.
- **B8 [canonical] CAS everywhere.** Every write is `ifVersion`-guarded → `409 VERSION_MISMATCH`; `ifVersion` is a monotonic version/etag, never a wall-clock timestamp; receipt is always `{url,id,version,persisted:true}`, never a re-rendered body or legacy `{block_id,status}`. — `orvexstudiocontracts/OI7v2rfZEG`, `orvexwikiapi/pG9s9OjqZA`, `orvexstudiomcp/k1sWjtJq3x`.
- **B9 [canonical] DfM-only across the agent boundary.** Raw ProseMirror JSON never crosses the MCP/CLI boundary; opaque nodes (diagrams/embeds/mentions) travel as typed handles; DfM↔PM conversion is server-side (`pkg/dfm` in-process for wiki-api + CLI), no engine `/convert` calls. — `orvexstudiomcp/k1sWjtJq3x`, `orvexstudiolib/0NPvDR3C0m`, `orvexwiki/twQ3BBzpTE` A-SEAMS.
- **B10 [ruled, unbuilt] Two machine-write lanes (D11).** Machines get BOTH a direct-write lane (provenance-stamped, `unreviewed`-flagged, deterministic fail-closed screen, per-user globally configurable, may be Free-tier's only machine-write) AND the curated Librarian confirm lane. The confirm gate is the sole *curated* path, not the sole path. Any MCP/CLI write-verb design must represent both, not a propose-only model. — `orvexstudioapi/ekTh7nDQqo` D11 (ENG-2848), `orvexstudio/3z78laG6dB` D11. **Not yet reconciled into the Memory Architecture Spine's AD-4 — see §3.**

### 2.3 Quotas, entitlements, errors
- **B11 [canonical] Frozen 402 `QUOTA_EXCEEDED`, never 429, never destructive.** Reads/exports/deletes always succeed; verdict is domain logic; the payload `{quota,state,limit,usage,percent,graceEndsAt?,resetsAt?,plan,upgradeUrl,message}` is also the MCP tool-error shape; propagated verbatim through wiki-api/CLI/MCP, never cached or partial-written-around. — `orvexstudioarch/7E0Hbb9it2` ADR-0003, `orvexwiki/W613jcEl3k`, `orvexstudiobilling/Blcvui4UIn`.
- **B12 [ruled] Tri-axis denial vocabulary (D12).** 402 QUOTA_EXCEEDED (period quota) / 403 ENTITLEMENT_REQUIRED (tier lock) / 429 + Retry-After (burst/abuse, distinct per-principal AND per-credential token bucket). "Reads never 429" is a quota-axis promise only; burst protection may 429 any caller incl. MCP tools. — `orvexstudioapi/ekTh7nDQqo` D12 (ENG-2849).
- **B13 [canonical] Enforcement locus is fixed.** Wiki quota enforcement is the **engine's** write chokepoints (page create, attachment, collab/Yjs) — **wiki-api CANNOT be the enforcement point** (content is creatable bypassing wiki-api). AI spend enforcement is ai's LiteLLM `max_budget`. Billing DECIDES/SERVES/PUSHES only, never enforces. — `orvexstudiobilling/Blcvui4UIn` A-CAP, `orvexwiki/W613jcEl3k`.
- **B14 [canonical/active] The one frozen family op.** `GET /v1/entitlements/{principal_type}/{principal_id}` (`getEntitlement`, x-status:active) is consumed via generated client by anything reading entitlements/quota; act-as-user keys on the **acting delegated principal's tenant**, never the service's own identity or the token minter. — `orvexstudiobilling/4zOSYmRSLX`, `Blcvui4UIn` FR-B6.
- **B15 [canonical] New-write-verb quota enrollment pattern.** New write surfaces enroll into the frozen 402 vocabulary via the ADR D5 lane (staging/workgraph did so). — `orvexstudioarch/8kTZK91vHd` ADR-0028.

### 2.4 Tenancy & auth
- **B16 [canonical] Polymorphic {user|org} tenancy.** Personal users = user-keyed tenant with NO Clerk org; only Teams are Clerk orgs. Assuming org-only is a CI-probeable bug class. Two-segment `{principal_type}/{principal_id}` addressing is canonical. — `orvexstudioarch/K5rumy9a8c`/`HylAAMRbPO` ADR-0001/0012, D-S17 everywhere.
- **B17 [canonical] One auth verifier + token pass-through.** Every satellite verifies via `orvex-studio-lib/pkg/auth` (deny-by-default, scope derived server-side only, never client-supplied); MCP/CLI forward the caller's identity-minted bearer verbatim and never mint or hold a service credential; MCP never calls `POST /v1/tokens`. — `orvexstudioidentity/dQUjrSXhdp` A-TOKEN, `orvexstudiolib/0NPvDR3C0m`, `orvexstudioarch/WZWmazrlS0` ADR-0029, `orvexstudiomcp/k1sWjtJq3x` FR-M11/M12.
- **B18 [canonical/draft] wiki-api caller-token pass-through, no elevated credential.** wiki-api forwards the caller's verified token byte-for-byte to engine/knowledge; reuses `pkg/auth.RequireScope`; `IDENTITY_VERIFY_ENABLED=false` is pass-through (upstream still enforces), not unauthenticated. — `orvexstudioarch/hSJ5tgNZMm` ADR-0021 (**draft/pending-ratify**).
- **B19 [canonical] MCP edge verifier = IdentityJwtVerifier; rebind on `tenant:subject`.** Multi-issuer JWKS + deny-list + TTL introspection; retires SessionManager/ReplayStore/HS256; frozen Principal has no `sid`. — `orvexstudioarch/WZWmazrlS0` ADR-0029.
- **B20 [canonical] Cell claim is carrier-not-owner.** `cell`/`cell_epoch` live only in short-TTL tokens (optional fields on the Principal per ADR-0030); long-lived bearer and **scoped MCP tokens carry NO cell claim** — routed by stable tenant/org claim via KV. — `orvexstudioarch/qZFmNWGY47` ADR-0011, `yOyavcWHEz` ADR-0030, `JGAUQRsw2g`.
- **B21 [canonical] CLI auth is identity-owned.** `GET /v1/whoami`, device-auth token mint (`orvex auth login`), doctor probe — CLI targets identity directly, not the engine. — `orvexstudioidentity/dQUjrSXhdp` FR-I20.
- **B22 [canonical] High-privilege ops need fresh no-grace introspection.** — `orvexstudioidentity/dQUjrSXhdp` FR-I3c.

### 2.5 Cell contract (binds any per-cell-deployed surface)
- **B23 [canonical] The 14-rule cell-lint CI gate binds wiki-api/MCP/CLI-backing services.** Cluster-local Service DNS for peer calls (rule 1), cell-guard middleware → typed 421 `CELL_MISMATCH`/`CELL_UNRESOLVED` (rule 3, reference impl `ODz3dEqZRJ` ADR-0019), CELL_ID≠CLUSTER_NAME echoed on `/healthz` (rule 4), `orvexcell` envelope stamped + fail-closed on consume (rule 6), `Idempotency-Key` mandatory on all `/internal/*` and webhook forwards (rule 11), no host literals (rule 9), tenant-move step-API (rule 10, non-retrofittable), NO-KEDA (rule 13). — `orvexstudioarch/JGAUQRsw2g`, `orvexstudiolib/0NPvDR3C0m` (A-CELL apparatus). **CLI note:** CLI is a client not a cell workload — R11 Idempotency-Key is currently NOT MET (`orvexcli/lhqTzMTPCj`).
- **B24 [canonical] Discover-once-pin-421 routing for MCP's flat host.** Front-door redirect only, never per-request proxying; pin invalidated ONLY by a real 421; SOLO_CELL short-circuit; fail-closed on ambiguous/missing cell. — `orvexstudioarch/9KcwAG5SRg` ADR-0020.

### 2.6 Events, spine, observability
- **B25 [canonical] Outbox → relay → Kafka `studio-spine` is the only event mechanism.** No Redis→Kafka bridge (a repeatedly-cited phantom); ID-only/no-PII payloads; at-least-once, dedup on CloudEvent `id`; consumers fail closed on `orvexcell` mismatch. — `orvexstudioarch/z5K777kNkD`, `orvexwiki/5uf23v6wIW` ADR-0001.
- **B26 [canonical] CloudEvent envelope + taxonomy.** Required `[specversion,id,source,type,orvexcell,orvextenant]`, `partitionkey`=tenant, `version` inside `data`; type taxonomy `studio.<svc>.<resource>.<past-tense>` (ADR-0010); engine emits `wiki.*` (not `studio.wiki.*`). New event types authored in contracts first (additive lane). — `orvexstudioarch/pYkl7HpOgr` ADR-0007, `orvexwiki/ILXUWrSgvh`.
- **B27 [canonical] MCP revocation consumption = raw in-process Kafka consumer, not a Knative Trigger.** An ADR-sanctioned exception (always-on service). — `orvexstudioarch/F7tTjMNVAr` ADR-0013.
- **B28 [canonical] Obs three-way split + console vocabulary.** metrics→Mimir (OTLP), ops logs→Loki, domain/audit events→CloudEvents on the spine (never Loki); OTel attrs conform to obs/CONVENTIONS.md (service/tenant/cell/trace_id/workflow_id); troubleshooting UI = console over LGTM, not Grafana. Known family gap: message-tracing-across-outbox. — `orvexstudioarch/6aMAzsYeQb` CS §10, `orvexstudioconsole/L1NVnmRgpy`.

### 2.7 Streaming
- **B29 [canonical/draft, ruled] Streaming is a requirement ON existing verbs, not new tools.** ask-class streams via MCP SDK `notifications/progress` (request-scoped, caller-addressed), never a new transport/SSE fan-out; edit-class gets local stage-progress. The ask-class relay is built-but-dormant (buffered-only) pending ai `/v1/ask` streaming. Cancellation error codes `ABORTED_UNCERTAIN_STATE`/`VERIFICATION_UNCONFIRMED`. — `orvexstudiomcp/ok9tiXDCjB` ADR-0002 (R21), `orvexstudioarch/pbNe8DJtvx` ADR-0038 (**draft; its "two-tool-only" framing is narrower than the R21 fold — see §3/§4**).
- **B30 [canonical] The SSE wire contract binds any CLI/MCP streaming that rides the gateway.** `/events/head` bootstrap, monotonic+epoch cursor v1/v2, legacy UNPREFIXED frame names, thin `content_updated{pageIds[]}`, `permission.changed` added→full resync, 1 MiB/512-queue, <45s heartbeat, `applied_events` dedup, 7-day resume window, frame visibility = key-type ∩ token scope. — `orvexstudiocontracts/OI7v2rfZEG` FR-C10, `orvexstudioknowledge/dCbFzRQGDr` FR-K15a, `orvexcli/PACF13d3I3`.

### 2.8 Audit
- **B31 [canonical] Events-only audit integration; MCP producer status is an open gap.** Never point-to-point to `orvex-studio-audit`; wiki-api's `/v1/audit` re-points from engine to the shared service; CLI dual-write unchanged; all reads via `/v1/audit/*` under audit-reader scope, every read itself audited; never block on audit (0ms). **MCP is not among the 7 named producers** — the redo must explicitly state whether MCP inherits coverage transitively via wiki-api's audited `/v1` or becomes an 8th producer; MCP-layer denials that never reach wiki-api have no audit coverage today. — `orvexstudioaudit/vhC5XXCYkC`, `iWvaOcrzRD` AD-1/8/12/13, `orvexstudioarch/Z6WrJjZDAO` ADR-0037.
- **B32 [canonical] Deny/failure/consent/export actions require active emission**, not passive projection from success-path domain events. Any AI-surface verb whose failure path has no domain mutation needs an explicit audit-emission path. — `orvexstudioaudit/iWvaOcrzRD` AD-8.

### 2.9 Contracts, codegen, governance
- **B33 [canonical] Contracts-tag codegen only, never the served descriptor.** Source is always a local contracts checkout (`ORVEX_STUDIO_CONTRACTS_DIR`); generating from the AGPL served descriptor is forbidden; a GA cutover must pin an `x-status: pinned` artifact at a released tag; mcp is the one proven end-to-end pattern (generalize it). — `orvexstudioarch/QbEBPuKcGR` ADR-0035, `orvexstudiocontracts/w25dZASIbG` FR-C25/26.
- **B34 [canonical] ADR-0035 is TS-only; the CLI's Go codegen bridge is UNDECIDED (MR-CLI2).** Any redo claiming Go client codegen must NOT cite ADR-0035 — Go clients are hand-authored. — `orvexstudiolib/S14cg8tNbb`, `orvexcli/9AdZkNTlyj`.
- **B35 [canonical] ADR-0008 two-layer change authority.** Additive/mechanical contract edits → automated CI lane; breaking/seam-reshaping (rename/remove field, changed required set, altered envelope, retired endpoint, coordinated multi-repo cutover) → mandatory ADR + human doc-ratify; ambiguity fails safe to the ADR lane. **The named human ADR-lane ratifier is still unnamed (MR-1/OQ-C1) — breaking changes are currently BLOCKED.** — `orvexstudioarch/JLMuwgc9fV` ADR-0008, `orvexstudiocontracts/w25dZASIbG`.
- **B36 [canonical] AGPL boundary is absolute and CI-guarded.** No closed repo (wiki-api/MCP/CLI) imports `@docmost/*` in-process; DfM is the one clean-room Go package (`pkg/dfm`). — `orvexwikiapi/pG9s9OjqZA` NFR-A7, `orvexstudiocontracts/jwF4VLHfNs`.
- **B37 [canonical] The engine's in-fork /mcp is retired at REST-gap parity; the standalone gateway is the ONE agent door (G4).** No in-engine MCP surface may be re-added. — `orvexstudioarch/QnouxhOHK5` ADR-0032.
- **B38 [canonical] RATIFY/CONFIRM tokens are server-minted and transport-only.** MCP (and CLI) never promote draft→canonical on any path — they only transport human-minted tokens. — `orvexstudiomcp/k1sWjtJq3x` D-A8, `orvexwikiapi/pG9s9OjqZA`.
- **B39 [canonical] Frozen error/exit-code vocabulary shared cross-namespace.** ~58 errorCodes + CLI 0-9 exit-code buckets, extend-never-renumber; `LINEAR_NOT_CONNECTED` removed (D-S11, Linear absent family-wide). — `orvexstudiocontracts/OI7v2rfZEG` FR-C23, `orvexcli/R4AOVBLST7`.
- **B40 [canonical] Writes ON by default + hard READ_ONLY ceiling + Tenant Governance Profile.** Per-tenant policy object (memory-write mode staged|direct, sharing grants console-only|agent-enabled, publish path ratify-required|scoped-key), all defaulting to human/Librarian-gated, sitting inside the R-WDS-3 floor. — `orvexstudiomcp/ZGjLctEnGH` R-WDS-3.

### 2.10 Memory (binds MCP memory verbs specifically)
- **B41 [canonical] MCP memory role = read-only retrieval under I-4; MCP is "not a compose adapter".** Retrieval always carries the caller's delegated principal into knowledge's ACL ∩ token-scope; the compose/delivery/write path is a separate new unowned browser-extension component. — `orvexstudio/iiCcKhGptV` AD-5b, `vBvVDFklZo`. (Reconcile against R-SEAM-2's `memory_propose` write-leg and D11 direct-write — see §3.)
- **B42 [canonical] "Memory" is a live 3-way naming collision.** api `/v1/memory` (product SoR) vs workgraph coordination vs knowledge retrieval-corpus. MCP tool names using "memory" must disambiguate; `studio.memory.*`/`studio_memory_*` are reserved. — `orvexstudioapi/HjtdumU48C` MR-2, `orvexstudioworkgraph/bnbRUdCm6R` (ADR-0010/0025 reservation).

### 2.11 The staging hard-cut (a sequencing constraint)
- **B43 [canonical, unexecuted] Agent wiki-writes will be hard-cut to route through staging (FR-STG25).** MCP `save_page`/`edit` and CLI wiki writes → 403 at wiki-api ingress for agent-class creds → redirected to `staging_*` / `orvex-cli staging`. Sequenced LAST, gated on staging's apply path (still a stub) + non-501 ai/knowledge. The redo must design write verbs so this cut is a clean swap-in, not a re-architecture, and must NOT assume it has landed. — `orvexstudiostaging/mOzNDhn322` FR-STG25 / AD-4 / AD-12.

### 2.12 Process / house style
- **B44 [canonical] Definition-Factory artifact shape + anti-fake-done.** PRD-delta + frozen tagged contract + Contract Summary + SDD + Build Prompt + Test Plan; mechanical totality check; reviewer≠author, never self-ratify; ArgoCD-Healthy/CI-green/closed-ticket are NOT evidence; measure against the deployed artifact; surface a contest to an explicit MR list, decide nothing an agent may not decide. Linear stories already exist (query before authoring). — `orvexstudioarch/9VUHxAcoXw`, `8sYi523i4t`, `yXUWpQpRjx`; instantiated in every service space's Wave-3 pack.

---

## 3. CONTRADICTIONS AND STALENESS

### 3.A Intra-corpus contradictions (spaces/pages disagree with each other)

- **C1 — MCP↔knowledge routing: family canon root vs the authoritative ruling.** `orvexstudioarch/CxjFpIVUZY` (constitution) roster rows print "mcp routes knowledge via wiki-api + ai (not directly)"; `F7tTjMNVAr` (ADR-0013 2026-07-17 clarification), `orvexstudiomcp/ZGjLctEnGH` "Register item 3", and `k1sWjtJq3x` FR-M7/D-M8 all state knowledge is **DIRECT** for search/related/RAG (PO ruling 9 / ENG-1403, Done). **Evidence favors DIRECT.** `CxjFpIVUZY`'s rows are a known-wrong OD-7 hygiene artifact self-flagged by its own banner and by ZGjLctEnGH; a corrective ticket is owed but unfiled. **The redo must use the direct split and flag the canon fix.**
- **C2 — Memory mutation path: AD-4 (sole confirm gate) vs D11 (direct-write lane).** `orvexstudio/iiCcKhGptV` AD-4 (2026-07-14): "the confirm gate is the sole mutation path into Memory." `orvexstudio/3z78laG6dB` D11 + `orvexstudioapi/ekTh7nDQqo` D11 (ruled 2026-07-17): a Free-tier machine DIRECT-WRITE lane bypasses the confirm gate; confirm gate is the sole *curated* path only. **Evidence favors D11** (later, ruled, folded into api's binding FR register). The Memory Architecture Spine has not been amended. Also touches `orvexstudiomcp/ZGjLctEnGH` R-SEAM-2 (`memory_propose` with staged|direct modes) — consistent with D11, in tension with AD-4/AD-5b's read-only framing.
- **C3 — MCP memory role: read-only retrieval (AD-5b) vs a write-leg (R-SEAM-2).** `orvexstudio/iiCcKhGptV`/`vBvVDFklZo` say MCP is retrieval-only, "not a compose adapter." `orvexstudiomcp/ZGjLctEnGH` R-SEAM-2 puts a memory write-leg (`memory_propose`, staged|direct, tenant-selectable) IN the MCP surface. **Evidence favors the MCP re-baseline** (2026-07-17, ratified en bloc, later than the memory spine) — but the two must be explicitly reconciled: the redo should state that MCP's memory write-leg is the `staging`/`direct` proposal path, not the browser-extension compose port, and update the Fold-in Map.
- **C4 — MCP tool-count target: 19-verb "amazing MCP" vs 73-tool fork parity.** `orvexstudio/rgBOQh31p3` and `3z78laG6dB` six-surface protocol cite "mcp at ~19/73 tools" / "tool parity → 73"; `orvexwiki/qJojHbWJni` WS-16 frames 73 as destination; `orvexstudiomcp/ZGjLctEnGH` (2026-07-17, ratified) settles the live surface as the namespaced **hero-13** ceiling (retire-one-to-add-one) with a long tail disclosed on demand — NOT 73. **Evidence favors hero-13.** The 73-tool figure survives only as the in-fork `packages/orvex-mcp` REST-gap-ledger decommission target (`QnouxhOHK5` ADR-0032), which is a *deletion* gate, not a tool-count destination. The 73-tool acceptance criterion in the umbrella six-surface protocol is itself stale and needs formal supersession.
- **C5 — knowledge memory-corpus isolation grade: OPEN vs DECIDED, on the same page.** `orvexstudioknowledge/azRwTCZMqw` §5 says the grade is OPEN (attribute-scoped `owner_user` default); the same page's memory fold-in section states AD-7 as DECIDED (Turbopuffer namespace wall); `dCbFzRQGDr` PRD repeats the decided framing; `0mFImku9Gp` R-9 (meant to settle it) is still draft; the Wave-3 pack correctly flags it as unresolved MR-K1. **Unresolved** — the redo must not settle it by picking whichever page it read last.
- **C6 — Memory system-of-record: api (AD-1) vs ai (Cb0XBkNezd).** `orvexstudio/iiCcKhGptV` AD-1: Memory SoR is `orvex-studio-api /v1/memory`; the canonical `orvexstudioai/Cb0XBkNezd` still claims ai holds "memory state /`/memories`." **Evidence favors api** (AD-1 explicitly supersedes; ai's PRD-delta says "the arch page is STALE, do not obey it here") — but the ai architecture page is unamended.
- **C7 — console Data store: canon "Postgres, Redis" vs Redis-only.** `orvexstudioarch/CxjFpIVUZY` lists console Data = Postgres+Redis; `orvexstudioconsole/L1NVnmRgpy` (A-STATE) and the deployed artifact are Redis-only. **Evidence favors Redis-only** (arch + deployed manifest). Canon-row fix owed.
- **C8 — cert-manager CA for the wiki wildcard: Origin CA vs Let's Encrypt.** `orvexstudioarch/PCbBbVfhtM` (cert-manager) says Cloudflare Origin CA; `WWngFAdCYk` ADR-0002 + `JGAUQRsw2g` say Let's Encrypt. **Unresolved** in the corpus; low relevance to the AI-surfaces redo but flagged.
- **C9 — trial shape & free-tier AI: three live spellings.** Card-required 7-day trial (`orvexstudio` older drafts, base PRDs) vs no-card free-month (`orvexstudioui/57IqCbTRAW` FR-UI-D7) vs 7-day card-required corrected-canon (`orvexstudio/rgBOQh31p3`, `orvexstudioapi/ekTh7nDQqo` "trial = 7 days"). Free-tier AI: 10-lifetime-action counter (deployed code `orvexstudiobilling/gen/catalog.go`, `orvexstudioai/internal/budget`) vs model-class allowlist (`orvexstudioai/pbKI3BpQmY` D-AI12, ruled 2026-07-13). Contracts flags these as MR-2/3/4 (`orvexstudiocontracts/w25dZASIbG`). **Evidence favors: model-class allowlist for AI; trial shape genuinely contested (a billing-pack must-resolve).** Relevant to the redo only where MCP/CLI surface entitlement/quota — consume billing's served values, do not hardcode.
- **C10 — MCP as an audit producer: unaddressed by omission.** `orvexstudioaudit/vhC5XXCYkC` names exactly 7 producers, MCP absent; no page rules MCP in or out. **A silent gap, not a contradiction** — but this is the exact OQ-A13/OQ-CLI3 pattern ADR-0037 was created to catch; the redo must close it explicitly.

### 3.B Corpus-vs-live-state staleness (pages behind the known 2026-07-16/17 reality)

- **S1 — MCP framing across most spaces is pre-re-baseline.** Every Wave-3 MCP page (`ppYIXBL4Cp`, `koIvfpnSbX`, `8nngxUpOXY`, `tQcQ01U2iy`, `RcvZDU2Sds`) plus the pre-re-baseline bodies of `BE0jTNoweS`/`k1sWjtJq3x` describe ~19 flat un-namespaced tools, `WRITES_ENABLED=false` default, old Studio-issuer auth, and "G5 has never run." **Superseded** by `ZGjLctEnGH` + `O6o61ov2e7`: namespaced hero-13, writes-ON, Clerk-live, knowledge-direct, `kpi-golden-tape` required CI gate, 417/417 hermetic tests, first 3-repo E2E 6/6 GREEN 2026-07-17. Host `dev-mcp.studio.orvex.ai` is DEAD; current door `mcp.orvex.dev`.
- **S2 — wiki-api "Phase-0 stub / 501" framing is stale.** `orvexstudioarch/dCNYFnoLNF`, `orvexstudiomcp/koIvfpnSbX`, and multiple pages describe wiki-api's `/v1` as unbuilt/501-upstream. The 2026-07-15 wiki-api Wave-3 pack (`5NTAkEn8KZ` Build Prompt) already measures the full `/v1` verb grammar LIVE (only ~3 endpoints honest-501), and the task's live-state anchor says `/v1` cutover is done. **But the contract still diverges (MR-A1) — the served RESTful grammar ≠ the flat draft `wiki-api.yaml`.** The redo must verify the current contracts tag and either resolve MR-A1 or own it.
- **S3 — "engine emits zero events / spine idle" is stale.** `orvexstudioarch/i5Kp2WMTah`, `omPa9ZJKSS`, `z5K777kNkD`, `pTeVSp9P5f` and most engine pages say the outbox→Kafka path is not live. Contradicted by `orvexwiki/pVDJS0woHl` (prod flipped `ORVEX_MODULES_ENABLED=true` 2026-07-14, outbox→Kafka proven on prod), the 2026-07-17 3-repo E2E (live Kafka→indexer exercised by `knowledge_search`), and `yXUWpQpRjx`'s survey. **Evidence favors: the engine's own outbox IS live on prod/dev; the family-wide spine is still partial (studio-api's own outbox throws, `orvexstudioapi/HjtdumU48C` D-API2).**
- **S4 — "prod runs vanilla Docmost, modules off" is stale since 2026-07-14** (PR #113 / `725090bd`). Repeated across nearly every 2026-07-05-era `orvexwiki` page. Corrected by `orvexwiki/pVDJS0woHl`/`gcecRSYLsK`.
- **S5 — build-state "scaffold/~90% unbuilt/193-LOC" claims across canon are systematically understated.** `orvexstudiocontracts/o2waDNw3ix` (~90% unbuilt), `orvexstudioai/oaD5XUHqfb` (193-LOC scaffold), `orvexstudioknowledge` README (scaffold), `orvexcli/pf10XC2Qjz` (2-file scaffold), `orvexstudioapi` README, `orvexstudiobilling` README, `orvexstudioidentity/server.go` doc-comment, `orvexstudiolib` (52-byte stub) — ALL falsified by their own 2026-07-15 Wave-3 measurement pages against deployed artifacts. **The `yXUWpQpRjx` core finding — canon self-reports understate real build state — is the master staleness rule; verify every "scaffold/draft/unbuilt" claim against live code.**
- **S6 — ADR-0038's "two-tool-only named exception" framing is narrower than R21.** `orvexstudioarch/pbNe8DJtvx` (ADR-0038) frames streaming as a scoped two-tool exception; per R21 (2026-07-16) it is folded into the hero-13 verb set as a cross-cutting requirement. The mechanism (progress-notification relay) is still correct; the "two-tool-only" framing is stale.
- **S7 — audit-service reachability + producer wiring is decided-not-built.** ADR-0037/PRD are canonical (matches the live-state anchor), but wiki-api's `/v1/audit` re-point, CLI's re-point, console's producer migration, and identity's re-point are all "authorized-but-unscheduled"; wiki-api's `/v1/audit` was 401-in-prod pending `pkg/auth`. Verify whether `pkg/auth` has landed and `/v1/audit` is reachable (FR30 gate).
- **S8 — Wave-3/4 pack frontmatter says `canonical`, body says `DRAFT / never self-ratify`.** Affects the Wave-3 packs in `orvexwikiapi`, `orvexstudioai`, `orvexstudioapi`, `orvexstudioui`, `orvexstudioworkflows`, `orvexcli` and others. **Trust the body: treat these as unratified drafts.**
- **S9 — CLI CI-RED / D-CLI1 false-green is unverified past 2026-07-15.** `orvexcli` measures CI RED since 2026-07-13; no page confirms status at 2026-07-17. Do not assume fixed or still-red — re-check live.
- **S10 — contracts tag state (v0.1.3, 7/38 rows pinned, no v0.1.4) is dated 2026-07-15.** Given the live-state anchors (wiki-api /v1 done, MCP live-green), the contracts repo may have advanced; the space itself insists on `git tag -l` verification over any wiki/ticket claim. **Verify the current tag before relying on any "draft/unpinned" claim** for `wiki-api.yaml`, `ai.yaml`, knowledge query/SSE/projection ops, or workgraph/staging schemas (the latter two exist only at unreleased dev HEAD, not any tag).

---

## 4. SUPERSESSION CHAINS relevant to the redo

**Chain A — MCP baseline → the 2026-07-17 anchor (the most important chain).**
Era-1 founding intent (`orvexstudiomcp/2hPabxB7Fh` Docmost Server Requirements, `986V0taYy5` Brainstorm Canon, `6e2jqBdknZ` Thin Build Plan, `J8nvlh1ctw` Technical Research — Docmost-scoped-key auth, resource-agnostic `ask/search/get/save/edit/list` over `{resource_type,locator}` incl. Linear)
→ Era-2 (`CjaHVTg8Id` Studio↔MCP resolve-based auth, `lRERyG7Zpj` act-as-user runbook, `CIemV9m0eb` three-repo runbook with dead host — all now HISTORICAL)
→ post-split canon (`k1sWjtJq3x` PRD post-split repoint: 3 upstreams, ask→ai, identity-minted tokens presented directly, Linear stripped)
→ Wave-3 pack (`8nngxUpOXY` PRD-delta / `tQcQ01U2iy` SDD / `ppYIXBL4Cp` Build Prompt / `koIvfpnSbX` Contract Summary / `RcvZDU2Sds` Test Plan — the ~19-flat-tool, writes-off, G5-never-run snapshot)
→ **`ZGjLctEnGH` WDS Re-baseline 2026-07-17 (THE ANCHOR, ratified en bloc)** — R-WDS-1/2/3 + R-SEAM-1..10, namespaced hero-13, writes-ON + READ_ONLY ceiling + Tenant Governance Profile, MCP as main route to staging+workgraph, marketplace un-folded, memory write-leg in, knowledge direct
→ **`O6o61ov2e7` Live State & Ops Runbook** (current operational ground truth: dev @ `mcp.orvex.dev`, 3-repo E2E 6/6 green, prod owner-held). The re-baseline front-sections in `k1sWjtJq3x` and the (still-stale) body of `BE0jTNoweS` are amended-in-place by ZGjLctEnGH; the Wave-3 pack is superseded wholesale.

**Chain B — wiki-api PRD.** `orvexwikiapi/8jAiCBifDW` PRD (status **superseded**, but still the substantive FR-A1..A24 SoR) → reconciled by the Wave-3 pack, esp. `RPduIa4x9Y` PRD-delta (RATIFIES FR-WA-D1..D6, does not renumber). No newer full PRD exists — the redo locates the successor as the PRD-delta layer, not a fresh page.

**Chain C — CLI PRD.** `orvexcli/R4AOVBLST7` PRD (**superseded**) → `UQFNh4QEmw` PRD-delta + `lhqTzMTPCj` SDD + `PACF13d3I3` Rebirth Plan + `6NVIjKeiWs` Build Prompt (current layer). Architecture `pf10XC2Qjz` is canonical but build-state-stale.

**Chain D — free-tier AI doctrine.** 10-lifetime-action trial (base PRDs, still in deployed `gen/catalog.go` + ai `internal/budget`) → **D-AI12 model-class allowlist** (`orvexstudioai/pbKI3BpQmY`, ruled 2026-07-13; code not yet swapped).

**Chain E — trial shape.** no-card → card-required D-S23 → no-card free-month (`orvexstudioui/57IqCbTRAW`) → 7-day card-required corrected canon (`orvexstudio/rgBOQh31p3`, `orvexstudioapi/ekTh7nDQqo`). Contested; billing-pack must-resolve.

**Chain F — Memory mutation path.** AD-4 "confirm gate is sole path" (`orvexstudio/iiCcKhGptV`, 2026-07-14) → **D11 direct-write + curated lanes** (`orvexstudio/3z78laG6dB` + `orvexstudioapi/ekTh7nDQqo`, ruled 2026-07-17). Memory Architecture Spine unamended.

**Chain G — audit sink.** Six uncoordinated per-service sinks + `C3EDqJjJjq` archived research → **ADR-0037 + orvex-studio-audit PRD/architecture** (`orvexstudioaudit/vhC5XXCYkC`/`iWvaOcrzRD`/`zPssXupzLZ`, ratified 2026-07-16); retention 6y→7y within the same doc-set; consumer = raw in-process Kafka (ADR-0037 self-corrects "Knative Trigger"→raw consumer).

**Chain H — deprovision/Clerk lifecycle ownership.** Two-sided unowned gap → **ADR-0036** (`orvexstudioarch/RJkFGHoht4`, draft/PO-ruled 2026-07-15: workflows owns full lifecycle; identity lands the real callee). Resolved-by-decision, not yet built (`orvexstudioidentity/mZdESLTkvP`, `orvexstudioworkflows/DxXIJWyzWb`).

**Chain I — workgraph claim-lease.** advisory/observability framing → **QE-6 / ADR-0040 authoritative grant** (`orvexstudioworkgraph/X4tcCCtrNl`, `orvexstudioarch/7Okcxv4i3Y`, PO-ruled 2026-07-17 Part A; distinct `CLAIM_CONFLICT` 409). Relevant if the redo's `workgraph_*` MCP verbs surface claim conflicts.

**Chain J — engine deployment posture.** "prod vanilla Docmost, modules off, zero events" → **modules-ON since 2026-07-14** (`orvexwiki/pVDJS0woHl`, PR #113).

---

## 5. LANDING MAP — find-before-create targets for the redo artifacts

Seven artifacts: three PRDs, three architectures, one cross-surface research. The house pattern (per the MCP re-baseline precedent) is **amend-in-place with a dated re-baseline front-section**, never a fresh dated sibling, and always land at `status: draft` pending human ratify. Targets below give the exact page to amend; if a target is `status: superseded`, amend the current successor layer (PRD-delta) and add the re-baseline front-section there, mirroring how `ZGjLctEnGH`'s rulings were folded into `k1sWjtJq3x`.

| Redo artifact | AMEND this page (find-before-create target) | Space · slug · status | Notes |
|---|---|---|---|
| **wiki-api PRD** | PRD `8jAiCBifDW` (superseded) — but land the re-baseline on the current successor layer, the PRD-delta `RPduIa4x9Y` | `orvexwikiapi` · `8jAiCBifDW`/`RPduIa4x9Y` · superseded / canonical | Do NOT mint a new PRD page. Re-baseline front-section carries: resolve MR-A1 (flat draft grammar vs served RESTful), the verb grammar B5, no-ask B5, write invariants B7/B8/B9, staging-cut readiness B43, audit re-point B31, tri-axis denial B12. Reconcile the still-superseded `8jAiCBifDW` status by pointing it at the successor. |
| **wiki-api architecture** | Architecture `pG9s9OjqZA` (canonical) | `orvexwikiapi` · `pG9s9OjqZA` · canonical | Amend in place; fold the SE-Arch "review tightening" addendum's open decisions (OD-1..OD-8), correct the phantom "Redis→Kafka bridge" recap lines, pin host form (MR-A3), and state the `/v1` cutover-done reality (S2). |
| **MCP PRD** | PRD `k1sWjtJq3x` (canonical, already carries the 2026-07-17 re-baseline front-section) | `orvexstudiomcp` · `k1sWjtJq3x` · canonical/ratified | This is the freshest of the three — the redo largely *ratifies/extends* rather than rewrites. Extend the existing re-baseline front-section; do not spawn a new PRD. Cross-reference `ZGjLctEnGH` (the ruling anchor) and `O6o61ov2e7` (ops ground truth). |
| **MCP architecture** | Architecture `BE0jTNoweS` (canonical, body pre-re-baseline/stale) | `orvexstudiomcp` · `BE0jTNoweS` · canonical | The single highest-value amend: walk the body section-by-section against `ZGjLctEnGH` (its top banner already flags §3 and the OD entries superseded). Land hero-13, three-plus lanes, knowledge-direct, writes-on, Tenant Governance Profile. |
| **CLI PRD** | PRD `R4AOVBLST7` (superseded) — land the re-baseline on the successor layer, PRD-delta `UQFNh4QEmw` | `orvexcli` · `R4AOVBLST7`/`UQFNh4QEmw` · superseded / canonical | Amend the PRD-delta; correct the D-CLI1 CI-red status (S9), reserve `staging`/`workgraph` namespaces, resolve/own MR-CLI1 (host) + MR-CLI2 (Go codegen — NOT ADR-0035) + MR-CLI4 (SSE cursor + content_pm). |
| **CLI architecture** | Architecture `pf10XC2Qjz` (canonical, build-state stale) | `orvexcli` · `pf10XC2Qjz` · canonical | Amend the stale "2-file scaffold" build-state to the measured 17,913-LOC reality (S5); fold in ADR-0037 audit-sink note and the Rebirth Plan's 42-row repoint matrix reference. |
| **Cross-surface research** | **New page (genuinely new — no existing home)** | recommend `orvexstudioarch` (family canon) OR a new "AI Surfaces" section under the umbrella | No cross-surface AI-experience research doc exists. The closest existing template is workgraph's AX Interface Architecture (`orvexstudioworkgraph/gRHhBOQKaM` + its DS-Pn pattern pages) — reuse the *format* (doors/transports table, cross-surface conventions, error-envelope + anti-footgun + token-budget patterns), but the content (a unified verb/routing/write-lane/streaming/audit model across wiki-api+MCP+CLI) is new. Land at `status: draft`. The stale `orvexstudiomcp/J8nvlh1ctw` (Technical Research) is Era-1 historical and should be cited as background, not amended. |

**Landing-map cautions (do not create duplicates):**
- The `orvexstudioarch` per-service canon pages (`dCNYFnoLNF` wiki-api, `WON2z73Gie` mcp, `B66dINHNvs` cli) are *summary* pages that also need their stale framing corrected (esp. `WON2z73Gie`'s already-bannered body and `CxjFpIVUZY`'s wrong MCP routing rows, C1). These are canon-hygiene amends, distinct from the service-space redo artifacts above — coordinate so the two do not diverge.
- Contracts changes the redo requires (`wiki-api.yaml` MR-A1 grammar reshape, promoting `ai.yaml`/`wiki-api.yaml` draft→pinned, a v0.1.4 tag) land in `orvexstudiocontracts` and are **breaking → ADR lane**, currently BLOCKED on the unnamed ADR-lane ratifier (B35/MR-1). Flag, do not self-ratify.
- Any new ADR the redo needs (auth-flow change, new external dep, MCP-as-audit-producer C10/B31) must be filed against the registry `orvexstudioarch/32Huug8U4B` with a live-rescanned next-free number (the registry warns against trusting a hardcoded "next free" — three real 2026-07-10 collisions).
- New Linear stories: the redo surfaces already have filed story sets (wiki-api ENG-2511..2543; mcp ENG-2449..2475+2707; cli ENG-2544..2578). Query before authoring (B44).
