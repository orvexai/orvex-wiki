---
scope: orvex-studio-staging — Agent Staging Area + Librarian (PRD MyS79tK0k7 + addendum MbK4S3xef1, 29 FRs, v1-everything)
purpose: build substrate: consistency contract for downstream epics/stories
altitude: feature→epics (new Go platform service)
updated: 2026-07-10T04:44
---

- (constraint) [ADOPTED] Family CS binds (carried copy project-context.md, cs-source 6aMAzsYeQb): six-tier Go service shape, Postgres-only (D-S12, no Mongo), services NEVER share a DB, every cross-service hop over orvex-studio-contracts seam (OpenAPI or CloudEvent on studio-spine), secrets env-only, no-fallbacks/hard-cuts doctrine
- (decision) [ADOPTED] Staging store = own dedicated Postgres OUTSIDE the wiki (Daniel: 'would prefer not' docmost); binds all FR-STG storage; prevents wiki history/tree pollution and engine schema coupling (NFR-STG2)
- (decision) [ADOPTED] 5 Operations with conflict keying: sub-document ops keyed (document_id, section_anchor) on stable block IDs (UniqueID ext), doc-level ops whole-doc-keyed; binds FR-STG9/13/14; prevents heading-text anchoring and whole-page rewrites riding section edits
- (decision) [ADOPTED] Autonomy Dial recommend|auto-apply-low-risk|full-auto is SERVER-ENFORCED policy per tenant+space, destructive ops human-gated below full-auto; binds FR-STG10/12; prevents prompt-text-as-policy divergence
- (decision) [ADOPTED] orvex-wiki-api = ONLY write ingress to wiki; agent-class creds 403 on write verbs; engine write routes network-fenced (infra, not fork edit); wiki-api past-501 promotion = prerequisite epic; binds FR-STG25/NFR-STG5; prevents bypass paths surviving the hard cut
- (decision) [ADOPTED] Hard cut of ALL agent wiki-write surfaces is the LAST release step, gated on staging E2E in prod against non-501 orvex-studio-ai + orvex-studio-knowledge; binds FR-STG25 sequencing; prevents dead knowledge-write capability
- (decision) [ADOPTED] Beautify at TRIAGE (review-what-you-apply): reviewed diff IS final content; apply = mechanical engine writes only; section-scoped ops beautify only their section; binds FR-STG13/14, NFR-STG1; prevents LLM latency in apply SLO and conflict-key collapse
- (decision) [ADOPTED] Never silent three-way prose merge; conflicts (moved base at conflict key) always human-resolved; binds FR-STG9; prevents silent content corruption
- (decision) [ADOPTED] ChangeSet atomicity = workflow-state gate (Veeva MDCC pattern), NOT cross-store transaction; paused mid-apply = explicit partial state naming applied vs pending + human resume/abort; binds FR-STG13; prevents pretend-ACID across the staging→wiki seam
- (decision) [ADOPTED] LLM access ONLY via orvex-studio-ai (staging owns orchestration, never model access); search/dedup candidates + reindex via orvex-studio-knowledge; binds FR-STG7/8/18/19; prevents a second model-access path
- (decision) [ADOPTED] Durable apply + sweeps ride orvex-studio-workflows (central Temporal) [PRD ASSUMPTION NFR-STG3 — confirm topology at draft]; binds FR-STG13/28, maintenance sweeps
- (decision) [ADOPTED] Proposal payloads pointer-state: bodies in object storage, pointers in queue rows; binds FR-STG6; prevents pinned-compute queue bloat
- (decision) [ADOPTED] Surfaces = new staging sections on the EXISTING Studio MCP server + orvex-cli; per-agent credentials, never shared service creds; binds FR-STG23/24
- (decision) [ADOPTED] Events = studio.staging.* CloudEvents subdomain on studio-spine (new subdomain via ADR); knowledge consumes apply events for reindex; console consumes queue metrics; binds FR-STG26, NFR-STG6
- (decision) [ADOPTED] Supersession: Card Contract v1 field-authority split transfers VERBATIM (Proposal = strict superset); Curator classifyOnSave becomes staging on-ramp, local classify path removed loud; shipped Curator golden tests must pass against Card→Proposal mapping; binds §9
- (event) ADR registry found UNBLOCKED at architecture time: ADR-0001..0016 filed in orvexstudioarch (PRD OQ7 stale) — supersession ADR + studio.staging.* subdomain ADR + service-name decision now unblockable
- (question) Librarian orchestration: module inside orvex-studio-staging vs own deployable (PRD OQ1) — to fix as AD
- (question) Snapshot retention window for pre-mutate snapshots (PRD OQ3)
- (question) Dial granularity per-space vs per-doc-type (PRD OQ4)
- (event) As-built Curator audit: classifyOnSave is skills-path auto-tagging only (ENG-1527 AC1–AC9; FR-SA numbering absent from code), cap-gated on billing feature curator-classify (fails CLOSED), 3s deadline degrade-to-unclassified, AI egress = POST {ai}/v1/curator/classify; golden fixture is the contracts-committed shape (AI sibling classify route unshipped)
- (direction) Supersession AD binds the on-ramp at the classify seam: goldens to preserve = idempotency, cap-keyed-on-verified-principal, degrade reasons (sibling-unreachable|cap-exhausted), no-direct-LLM grep gate; Card→Proposal field mapping golden-verified at migration
- (decision) Librarian deployable (resolves PRD OQ1): a MODULE inside orvex-studio-staging (bounded-context packages: triage, routing, beautify, policy, learning, sweep) — no second deployable; family adds services reluctantly; triage parallelism is a worker-binary/scaling concern (P7), not a service boundary
- (decision) Reindex ownership: knowledge reindexes wiki content from the ENGINE's wiki.* events after apply (single mutation source of truth); studio.staging.* events carry queue/decision/audit telemetry and are NEVER a second reindex trigger — prevents dual-trigger divergence
- (decision) As-built conventions RATIFIED — same set as memory workspace entry (go1.26/toolchain go1.26.5, lib v0.3.0 real-auth/real-events/stub-obs, pgxpool 5.10, bounded-context domain packages, confined store, NNNN idempotent migrations, no satellite Temporal worker per D-WF-1 + step-APIs, stdlib mux, lib typed clients as intent — staging adds aiclient/stagingclient to lib as seam owner)
- (decision) MCP integration (as-built pattern): new staging-tools.ts family file on the existing TS server (@modelcontextprotocol/sdk ^1.29, zod v4), STAGING_CATEGORY_TOOLS + hidden category revealed via list_tools, registered before installToolVisibility; per-tool 5-step shape (claims→resource-check→write-gate→session wrapper→okContent/errorContent); session-resolve response extended with a staging key (resourceType overload) — a studio-api/control-plane contracts change
- (constraint) Contracts as-built: zero staging footprint anywhere; needs openapi/staging.yaml (flat per-seam-server file), catalog.yaml event entries + per-type schemas, topics.domains extension, gates/agpl-import denylist.yaml + gates/drift/gates.yaml runs_in enrollment, org ruleset wiring (admin), errors vocabulary surfaces addition for 402; staging is NOT a P9 content source (no sources/staging.yaml) — wiki reindex rides wiki.*
- (event) Daniel rulings on memory's shared questions received (vector→Turbopuffer-via-knowledge; studio.memory.* transfer) — staging's divert-to-memory contract unaffected; wiki-worthy/divert seams ride each service's published OpenAPI via lib typed clients
- (event) Sibling renamed orvex-studio-workgraph (Daniel 2026-07-10); memory = user-managed memory product, unchanged. divert-to-memory Disposition surfaces as divert-to-workgraph (PRD enum id stable pending amendment); seam rides workgraph's published OpenAPI via lib workgraphclient
- (change) PRD reconcile (6 gaps + 4 notes, all resolved): ratify-gate/no-self-promotion clause added to AD-4; FR-STG26 reindex refinement (wiki.* not staging.*) kept but explicitly flagged for PRD amendment in AD-9; separate maintenance interaction budget + agent-authored-only sweep-write scope + human-settable groundability flag + repeat-rejected producer throttle added to AD-7; SM-C2 acceptance-weighted volume bound in AD-11; GDPR erasure cascade w/ content-free tombstones added to AD-10; partially-accepted state added to lifecycle diagram; submit/triage SLOs named in Deferred
- (change) Canon reconcile (4 findings, resolved): 402 surfaces enrollment moved to ADR lane in AD-9; P8 isolation-probe trio homed in AD-8; P9 inherited row corrected (content sources only — staging is not one); CI/CD conventions row added (Tekton→Harbor exclusive, self-hosted runners, OIDC lib token)
- (change) Adversarial gate (14 holes + receipt note, ALL closed): routing-confidence pinned float[0,1]/1=certain/sole producer routing (H1-CRIT); Idempotency-Key = f(run_id,step,target_id), ledger primary + CAS defense (H2-CRIT); beautified output = immutable sha256-addressed artifact, apply-by-hash (H3); groundability flag lives in wiki-api page-meta, sole mutation via wiki-api, knowledge reads wiki-side (H4); base_version = wiki-api version token verbatim, timestamps display-only (H5); Disposition records producing pack_version, Feedback inherits (H6); Trust Tier = single learning-owned versioned entity, snapshot-at-admission (H7); per-(tenant,page) apply lease serializes cross-ChangeSet applies (H8); divert target = workgraph via workgraphclient, PRD rename flagged (H9); schema validation once at intake (H10); beautify preserves target anchor ID (H11); public write verbs take Idempotency-Key (H12); lifecycle exports the single in-flight predicate (H13); scheduled applies store UTC instant + origin tzid (H14); SubmitReceipt/ApplyReceipt split (H15)
- (change) Rubric gate (3 med/6 low, all resolved; 0 crit/high): FR-STG22 flag routed through the AD-6 gating contract (store home already fixed by adversary H4) [M1]; engine wiki.* outbox named a gating prerequisite in AD-9 [M2]; ingress posture decided — cluster-internal, Studio-plane /api/* proxy, ASSUMPTION pending M12 CLI wiring [M3]; ChangeSet groups on server-derived session ref [L1]; AD-4 snapshot wording reconciled with S3 pointer-state [L2]; divert seam invariant strengthened [L3]; NFR map row added [L4]; mermaid labels quoted [L5]; cmd/sweep vs workflows schedules clarified [L6]
- (event) spine finalized
- (event) Sibling rename amendments applied to staging PRD (divert-to-workgraph); AD-6 note updated; spine wiki page DBA3v1gglh republished; FR-STG26 refinement still pending PO ratify via spine
- (event) FR-STG26 amendment folded into the PRD (Daniel-instructed); AD-9 note updated from 'flagged' to 'amended'; spine page DBA3v1gglh republished
