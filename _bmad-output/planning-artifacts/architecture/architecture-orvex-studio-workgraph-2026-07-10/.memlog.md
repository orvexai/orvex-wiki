---
scope: orvex-studio-memory — Cross-Agent Memory Service (PRD HTExyRFHhs + addendum PkXBRU4GkC, 27 FRs, v1-everything)
purpose: build substrate: consistency contract for downstream epics/stories
altitude: feature→epics (new Go platform service)
updated: 2026-07-10T04:40
---

- (constraint) [ADOPTED] Family CS binds (carried copy project-context.md, cs-source 6aMAzsYeQb): six-tier Go service shape, Postgres-only (D-S12), services NEVER share a DB, cross-service only via orvex-studio-contracts seam, secrets env-only, no-fallbacks/hard-cuts doctrine
- (decision) [ADOPTED] Store = own Postgres + pgvector with bi-temporal edge model (event-time + ingest-time); Dolt DROPPED (addendum A1: single-writer, no shard, MySQL-wire vs D-S12, beads production evidence); cell-conflict keying survives as schema design only; binds all FR-MEM storage
- (decision) [ADOPTED] Coordination writes are DIRECT + ungated (latency is the product); async consolidation via orvex-studio-ai scoped to Remembered Facts + CLOSED items ONLY — live coordination state untouchable by LLM loop; binds FR-MEM1-7/11; prevents Librarian-gating the coordination surface and LLM mutation of live state
- (decision) [ADOPTED] ZERO LLM calls on any hot path (committed invariant NFR-MEM1); fast-path tier: exact-key recall + pinned-only prime-compact ≤100ms p95 index-bound; binds FR-MEM9/10; prevents Zep-style LLM-in-retrieval latency
- (decision) [ADOPTED] Server-issued per-tenant short IDs (sequence + short encoding, unique-indexed); beads hash-ladder/content-derived IDs DROPPED (centralized store needs no decentralized-merge machinery); binds FR-MEM1
- (decision) [ADOPTED] Concurrency = all-on-main + row-level transactions + atomic CAS claim; never per-agent branches (beads' own reversal); claim stays server-side CAS even in degraded mode; binds FR-MEM4, NFR-MEM6
- (decision) [ADOPTED] MCP-first on the EXISTING Studio MCP server (memory_* section) + orvex-cli parity; prompt packs replace hooks (chat platforms have none) with server-side reinforcement (MCP instructions, tool-description micro-prompts, stale-claim nudges); pre-GA adherence spike = GA gate; binds FR-MEM14/15/18/19
- (decision) [ADOPTED] Supersedes BUILT /v1/memory + tenant-partitioned memory table in orvex-studio-api: backfill via studio-api's own API/export surface (NEVER cross-service DB read), verify counts/checksums, client swap, route removal with loud migration errors + one-window deprecation stubs; 3-state privacy enum (open/private/shared-private) carried as first-class attribute, base for FR-MEM23 grants; binds FR-MEM21/22
- (decision) [ADOPTED] studio.memory.* CloudEvent subdomain already RESERVED by ADR-0010; every mutation emits on studio-spine; console reads via knowledge projections, not direct store reads; binds FR-MEM17, NFR-MEM5
- (decision) [ADOPTED] Hard cascading delete with erasure-wins precedence over restore/audit (content-free tombstone only); no TTL-floor soft-expiry; binds FR-MEM13, NFR-MEM5/7
- (decision) [ADOPTED] Consolidation/compaction LLM path MUST run under contractual no-train, EU-resident, zero-retention provider config enforced in orvex-studio-ai + named in contracts seam (NFR-MEM7 dependency clause)
- (question) Deployable topology: user phrasing presupposes two services (orvex-studio-staging / orvex-studio-memory) — separate services day-1 vs shared deployable (PRD OQ1) to fix as AD with rationale
- (question) Bi-temporal edge model depth v1: full as-of queries vs invalidation timestamps only (PRD OQ3)
- (question) Interop adapter mapping table entity/relation/observation ↔ item/fact/edge (PRD OQ6) — seed at architecture, golden at build
- (question) Adherence telemetry sharing Librarian feedback-learning machinery: one learning loop, two prompt packs? (PRD OQ7) — cross-spine consistency call
- (event) As-built audit of supersession target (orvex-studio-api, TS/Hono): /v1/memory persists PRIVACY METADATA ONLY — columns id/tenant_id/owner_id/privacy/shared_with/created_at; NO content/embedding/namespace/agent-id; verbs POST create + GET list-by-tenant only; privacy plain TEXT app-enforced 3-state open|private|shared-private; interim trusted-header auth (x-orvex-principal-id, no identity.verify), WHERE-clause tenancy, no RLS
- (direction) FR-MEM21 backfill = privacy-grant stubs, not content (content store is greenfield); counts/checksums verify applies to metadata rows; 3-state enum carries as first-class attribute (PO-confirmed) and seeds FR-MEM23 grants
- (constraint) Do NOT copy the interim trusted-header seam: auth pattern to inherit is the skills route's identity.verify (attested principal as owner+entitlement subject, anti-IDOR)
- (decision) Daniel ruling (2026-07-10, vector store): 'really depends on the use case - if turbopuffer will function better let's go for it'. Evaluation verdict: Turbopuffer via the P9 source-adapter route WINS — memory registers as a knowledge content source; knowledge indexes items/notes/facts from studio.memory.* rich events into Turbopuffer (namespace-per-tenant, structural isolation, BYOC/EU); semantic recall = memory→knowledge-query with acl_primitive live-intersect; fusion stays in memory's ranking package over Postgres legs (keyword/graph/recency/exact). NO pgvector — honors ADR-0014/P5 (no second live vector store), 'knowledge = the one multi-corpus search service' (locked family decision). Fresh-window covered by Postgres legs; projection lag honest; PRD retrieval spike gates the SLOs and is the revisit trigger
- (decision) Daniel ruling (2026-07-10, event namespace): TRANSFER studio.memory.* — supersession ADR amends ADR-0010's producer binding orvex-studio-api → orvex-studio-memory; knowledge consumer role + FR-MEM17 projections continue; ADR lane per ADR-0008
- (constraint) Contracts as-built: events/catalog.yaml topics.domains=[wiki,billing,identity,ai,clerk,tenant] — no studio domain materialized yet; studio.memory.* types + topic-domain extension + openapi/memory.yaml + gates runs_in enrollment are all NEW contracts work (additive lane except the producer amendment)
- (decision) As-built conventions RATIFIED for the new service (evidence: family-conventions audits): go 1.26.0/toolchain go1.26.5; lib v0.3.0 (pkg/auth verifier REAL incl. cell-guard/421 + Middleware; pkg/events envelope+BrokerPublisher real; pkg/obs a no-op stub — OTel landing in lib is prerequisite work); pgx v5.10.0 pgxpool native (NOT database/sql); domain = bounded-context packages under internal/ (no internal/domain dir); store confined to internal/store/postgres; multiple thin binaries under cmd/; migrations = idempotent forward-only NNNN_*.sql + advisory-lock applier (no framework); Kafka via lib pkg/events (segmentio transitive — franz-go NOT the family client); NO Temporal SDK in satellites (D-WF-1: central workflows calls idempotent /internal/* step-APIs, lib pkg/temporalutil/stepapi exists); stdlib ServeMux Go 1.22 method-patterns; /healthz+/readyz echo CELL_ID+CLUSTER_NAME; lib typed clients = documented intent (adopt + extend lib with missing clients), hand-rolled internal/clients is the legacy majority pattern
- (decision) Deployable topology (resolves PRD OQ1): TWO separate services/repos day-1 — memory is a latency-critical CAS kernel, staging is an LLM pipeline; different scaling shapes, stores, release cadence; user request presupposes both names
- (decision) Bi-temporal depth v1 (resolves PRD OQ3): event_time + ingest_time + invalidated_at columns (invalidation timestamps); full as-of query engine DEFERRED
- (decision) Claude memory-tool pin (FR-MEM27, resolves 'pin at architecture time'): tool type memory_20250818, name 'memory', GA no beta header, commands view/create/str_replace/insert/delete/rename rooted /memories; interop KG shape pin (FR-MEM16): @modelcontextprotocol/server-memory 2026.7.4 (entities/relations/observations verbs); MCP spec 2025-11-25 (2026-07-28 is RC — do not pin); adapters are protocol translators onto the same core verbs, never direct store access
- (decision) RENAME (Daniel 2026-07-10): service = orvex-studio-workgraph — 'memory' stays exactly what it was: the user-managed memory product (/v1/memory FormSpec, studio.memory.*, ADR-0010 untouched). The agent service must not reuse the word. The earlier studio.memory.* transfer ruling is VOID; workgraph mints studio.workgraph.* as a NEW additive subdomain (automated lane). Tools workgraph_*, CLI 'orvex-cli workgraph', space orvexstudioworkgraph. Naming guard vs orvex-studio-workflows: always the full word workgraph
- (decision) FormSpec SPLIT (Daniel 2026-07-10): the built /v1/memory user-memory store STAYS product-side — workgraph holds AGENT state only, GREENFIELD (no row migration). FR-MEM21 narrows to design inheritance: 3-state privacy enum adopted as the grants design base only. FR-MEM22 rescoped: studio_memory_get/save remain user-memory tools, descriptions re-pointed to workgraph_* for agent state. PRD HTExyRFHhs needs correct-course amendment (title, FR-MEM21/22, §9) — flagged, not silently rewritten
- (change) PRD reconcile (10 gaps, all resolved): NFR-MEM2 scale envelope + measured degradation curve + compute-isolation + burst-safe checkpoints added to AD-4 and load-test gate to Deferred; 3 anomaly detectors named in AD-7; beads-ADR-0002 agent-safety conventions row added (no weaponizable errors, read-looking writes, honest degradation); consolidation decision-trail + tier-2 + plan-tier retention bounds in AD-5; pack ritual content verbatim in AD-11; prime-compact content contract in AD-13; AD-1 self-contradiction fixed (studio_memory_* remain product-side user-memory surfaces, only descriptions gain the workgraph_* pointer); §5 non-goals fenced + MIT NOTICE in AD-1; ChatGPT-bridge external constraint in AD-10; ERD notes/comments
- (change) Canon reconcile (4 findings, resolved): 402 surfaces ADR-lane enrollment homed in AD-13; ADR-0014 D3 bytes=0 + P8 probe set homed in AD-3; studio_memory_* self-contradiction already fixed in AD-1; CI/CD conventions row added
- (change) Adversarial gate (15 holes + 4 minors, ALL closed): sync-verb schemas join the contracts tag, adapters codegen from it (H1-TOP); canonical item model pinned — status {open,in-progress,closed}, blocked/deferred/claimed orthogonal, ready predicate fixed (H2-TOP); handoff state derived from (status,assignee,resolution), claim-CAS advances it atomically (H3); single recomputeBlocked(affectedSet) primitive, all triggers funnel in (H4); reaper revert = conditional CAS + reaper_grace>heartbeat_interval, one lease-timing config (H5); system principal class in authz — scoped capability, not bypass (H6); rich-event-inline indexing authoritative, acl_primitive shaped fail-closed, purge shaped (H7); grants precedence — enum is floor, grants only widen (H8); step-API bodies pinned + janitor owns loop, workflows triggers (H9); detector input signals in event schema (H10); batch emits N per-resource events, ids derived from (Idempotency-Key,resource_id) (H11); event_time caller-supplied server-clamped, ingest_time drives recency/staleness (H12); one banner envelope {kind,scope,detail}, degraded=store-down only (H13); fact keys (tenant,scope,key) + session→agent→tenant resolution (H14); short-id grammar wg-+base32-crockford pinned [ASSUMPTION] (H15); ready excludes open handoffs; compact top item = priority/age; claim_next best-effort; wiki-worthy hash defined once in stagingclient contract
- (change) Rubric gate PASS-WITH-FIXES (0 crit/0 high/2 med/9 low, all resolved): AD-7 producer-lane reconciliation (P9 registration; drift-gate ambiguity fail-safes to ADR lane, settled at contracts authoring) [M1]; knowledge-past-501 + platform→knowledge query-client contract named as dependency gates in Deferred [M2 — verifier half stale: real MultiIssuerVerifier landed in lib v0.3.1]; migration runner named (embedded-FS applier + advisory lock) [L1]; cluster-internal no-public-host [L2]; workload-shape Deferred tightened to cmd/reaper only [L3]; tenant-move outbox/reprojection ordering named [L4]; feedback/adherence envelope authored once in contracts [L5]; per-tenant rate limits enforced in-service at admission [L7]; adapter→store import-lint [L8]; template instantiation cycle-rejected transactional [L9]
- (event) spine finalized
- (event) PRD amendments applied + republished (HTExyRFHhs retitled 'PRD: Workgraph', addendum retitled, staging PRD divert renames); spine AD-1 flag-note updated; spine wiki page uJAiMMsrJB republished
