---
scope: Orvex Studio Extension — cross-AI delivery browser-extension component (family's 17th component; the AD-5a client compose port). Component/feature-altitude spine under the initiative spine iiCcKhGptV.
purpose: build-substrate for ENG-2690 Definition Pack Task 2 (architecture); pins the invariants the extension's build stories ENG-2711..2730 must share
altitude: feature (component under initiative iiCcKhGptV; keeps the B1-B6 build milestones coherent)
updated: 2026-07-15T07:39
---

- (direction) Fast path, component/feature altitude under initiative spine iiCcKhGptV. Draft produced via opus-plan/sonnet-write/opus-adversarial-review/sonnet-fix; distilled into ARCHITECTURE-SPINE.md. Deliverable = ENG-2690 Definition Pack Task 2.
- (constraint) [inherited iiCcKhGptV I-1] Cross-service interaction ONLY via orvex-studio-contracts (OpenAPI/CloudEvent on studio-spine); no shared DB; Postgres-only.
- (constraint) [inherited iiCcKhGptV I-4] Retrieval is a knowledge call with the caller's delegated principal (ACL intersect token-scope at knowledge's chokepoint).
- (constraint) [inherited iiCcKhGptV AD-5a] composeInto(target,text)->{ok|needs-manual-paste}; user-triggered write-only single-session compose-box insertion. Mechanism resolved by spike V6hlDjecfh = Chrome MV3 extension.
- (constraint) [inherited iiCcKhGptV AD-6] ToS-clean invariant: own authenticated session; explicit per-use action; writes input field only (no Output scrape); no auto-send. Compose-box autofill distinct from prompt-injection tooling (CWS restricts 2026-08-01).
- (constraint) [inherited iiCcKhGptV AD-8] Consent + personal<->employer firewall enforce at the identity MINT boundary via single-target/single-use/short-TTL delivery token, re-minted per use. Client compose is NEVER a trust boundary.
- (constraint) [inherited iiCcKhGptV AD-11] Card schema + AD-5a port + MCP pinned in orvex-studio-contracts; memory.* CloudEvents; delivery reads are read-your-writes for the confirming user.
- (constraint) [adopted V6hlDjecfh] Per-assistant tiers verbatim: ChatGPT GO, Gemini GO, Claude DEGRADE, Grok DEGRADE, 0 NO-GO. Four caveats carried unlaundered: counsel-before-injection; live-DOM prototype; Google ToS 2026-07-30; CWS 2026-08-01. Claude/Grok FR-D1 'without copy/paste' withdrawn.
- (constraint) [adopted IgOjzk034v] Fork supermemory (MIT WXT/MV3 scaffold) + pattern-donate mem0 (MIT); build-fresh the canary, AD-6 review pause, GO/DEGRADE split, copy/paste degrade UI, Orvex backend wiring.
- (decision) AD-EXT-1: Delivery is a provider-adapter framework behind one registry. Binds: how a provider plugs in. Prevents: 4 divergent hand-rolled paths. Rule: one DeliveryAdapter realizes AD-5a; per-provider entrypoints/content/<provider>.ts; registry maps providerId->{tier,hostMatch,selectorPackRef,writeStrategy}; adapters carry no tier/consent logic; per-file breakage isolation.
- (decision) AD-EXT-2: Tier (GO/DEGRADE) is data, never code (FR-PA3). Rule: tier is a field in versioned/signed remote JSON provider config, same channel as selector packs; a tier flip is a config push within the FR-BC7 SLA, not a store update; absent/invalid/stale config fail-safes to DEGRADE; the closed-shadow-DOM verdict feeds this channel.
- (decision) AD-EXT-3: The extension is a consent-UX surface + token PRESENTER; enforcement server-side at mint (AD-8; reconciles FR-CF1..CF4/CF7). Client shows memories, captures per-use consent, requests mint scoped {memory,target,user}, presents token, cannot compose without one, re-mints per action/target. Firewall breach refused at mint, never warned client-side. FR-CF7 = pre-mint fail-closed target-identity guard.
- (decision) AD-EXT-4: Every delivery is a single canary transaction; closed-shadow-DOM is a categorical prototype-time go/no-go feeding AD-EXT-2. Rule: resolve->verify-writable->insert(native-setter+InputEvent)->read-back ~250ms->branch to {NOTFOUND,NOTWRITABLE,AMBIGUOUS,VERIFY}; abort before visible write. Closed shadow root => DEGRADE via config, not a selector fix.
- (decision) AD-EXT-5: MV3 zero-trust: isolated-world content scripts; least-privilege host_permissions (no <all_urls>); zero non-Orvex egress (strip supermemory/mem0 phone-home, bundle network audit); no remote code (logic ships as store update; packs are signed data); minimal read scope; reproducible build; CWS autofill-not-injector positioning.
- (decision) AD-EXT-6: Orvex auth + live scope binding is independent and fail-closed. Rule: session token in extension storage, refresh before expiry, cleared on sign-out/uninstall; every delivery binds an explicit live personal-vs-employer scope resolved from the live Orvex session (never the provider tab, never defaulted); expired/absent session at compose = fail closed + re-auth prompt.
- (decision) AD-EXT-7: Delivery reads are read-your-writes for the confirming user (AD-11). Rule: bundle retrieved via knowledge under delegated principal, but the confirming user's just-confirmed cards read the api SoR until projection catches up; precompute (OQ-E4) stays behind this RYW invariant.
- (decision) AD-EXT-8: The contracts seam is authored here; the git-TAG is Wave-1-gated. Rule: three shapes land in orvex-studio-contracts (composeInto port; consent-delivery-token gate; canary heartbeat), consumed via generated TS client + AGPL denylist + drift gate. git-TAG is ENG-2690 AC5 dispatch gate, BLOCKED on Wave-1 ENG-2037/ENG-2091 (both Todo); shape authored, tag never claimed to exist.
- (version) WXT 0.20.27 (extension framework, wxt.dev)
- (version) TypeScript 7.0.2 (npm latest dist-tag)
- (version) React 19.2.7 (popup/UI shell only, presentational)
- (version) @types/chrome 0.2.2 (MV3 platform typings)
- (version) Vitest 4.1.10 (unit/adapter tests)
- (version) Generated TS client from orvex-studio-contracts — version is Wave-1-gated (tag not yet cut; ENG-2037/ENG-2091).
- (assumption) Numeric targets PO-to-confirm: NFR-1 click-to-visible p95 <=400ms; clipboard auto-clear ~60s; FR-BC7 remediation SLA <=24h canary-trip-to-fix. Grok v1 = grok.com only.
- (question) Store-distribution posture (OPEN, PO): Chrome-first; Firefox AMO + Edge phased. Distinguish MV3 review LATENCY (bounded by signed data channel) from CWS REJECTION/TAKEDOWN (existential; fallback = self-host/enterprise sideload then AMO).
- (question) Open-source posture of the extension (OPEN, PO) — candidate trust lever FR-TS5; does not block architecture.
- (question) OQ-E1 (knowledge): does Anthropic native memory/import become a superior DEGRADE route for Claude vs copy/paste? MUST NOT be conflated with FR-D1 injection.
- (question) OQ-E2: Google ToS revision effective 2026-07-30 re-diff before Gemini ships (closed-shadow-DOM gate is categorical regardless).
- (question) Hard build preconditions gating build dispatch: (1) legal counsel on automated-means + CWS facilitation; (2) store-policy viability; (3) live-DOM prototype incl. closed-shadow-DOM IN/OUT per GO provider; (4) Wave-1 contract tag.
- (event) spine drafted + decisions logged; entering Finalize (lint + reviewer gate)
- (event) spine finalized: reviewer gate FAIL->fix->PASS (7 divergence pairs closed, AD-EXT-9 added); lint clean; status:final
