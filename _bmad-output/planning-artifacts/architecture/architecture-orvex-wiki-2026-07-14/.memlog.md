---
scope: Orvex Memory gap-closure — cross-feature invariants over PRD g9vWbSYplh (F0–F8)
purpose: build substrate to drive fold-in into service PRDs (ai/knowledge/mcp/api/identity) + epics
altitude: initiative→features (memory gap-closure)
updated: 2026-07-14T18:46
---

- (constraint) INHERITED family paradigm (ADOPTED): Go six-tier services + AGPL NestJS engine; cross-service only via orvex-studio-contracts (OpenAPI or CloudEvent on Kafka studio-spine); services never share a DB; Postgres-only (no Mongo, D-S12).
- (constraint) INHERITED three-store model (ADOPTED, rgBOQh31p3): Memory (curated facts, api /v1/memory) + wiki (kept pages) + beads (volatile narration, workgraph/ADR-0025); RAG/Turbopuffer is a mechanism not a store.
- (constraint) INHERITED Librarian confirm ritual (ADOPTED, fr7YaPq8Tl): propose-and-confirm, trust dial, edits/deletes/sensitive always human-gated.
- (direction) Fast path; spine routes to wiki as draft architecture page after finalize (doctrine, though skill has no auto-handoff).
- (question) CANON CONFLICT (OQ5, P1 ruling): Product Brief rgBOQh31p3 says product Memory owned by orvex-studio-api /v1/memory; canonical Architecture: orvex-studio-ai (Cb0XBkNezd) says ai's Postgres holds 'memory state' + serves /memories. Two owners of 'Memory'. MUST resolve — surface to Daniel (may require amending ai arch).
- (decision) AD-MEM-OWNER (proposed, resolves OQ5): SPLIT the three 'memories'. (1) product Memory (curated facts, lifecycle, firewall) = orvex-studio-api SoR /v1/memory; (2) ai owns EXTRACTION compute + its ephemeral chat-recall (FR-AI11) RENAMED to avoid collision; (3) knowledge owns the memory RETRIEVAL projection (source_type=memory corpus). Prevents two writers of a Memory card.
- (decision) AD-ISOLATION (resolves OQ2): memory corpus = per-user/tenant Turbopuffer NAMESPACE wall (the stronger grade), not attribute-scoped filtering — regulated beachhead forces it; Turbopuffer supports millions of namespaces (Cursor 80M+). Prevents cross-tenant memory leakage.
- (decision) AD-CAPTURE: capture/extraction is ASYNC off the write path; proposals land in the staging review queue (orvex-studio-staging, the Librarian/ChangeSets); NOTHING becomes a Memory card without passing the human-confirm gate (also the anti-poisoning invariant FR-S6/X6). Prevents un-curated writes to canon.
- (decision) AD-DELIVERY (F1): fix a pluggable DELIVERY-ADAPTER PORT (channels: browser-extension, MCP, native-memory-sync, copy-paste) with one common contract; the mechanism binding (extension) is deferred to the F1 viability spike. Prevents each channel re-inventing the delivery contract.
- (decision) AD-LIFECYCLE (F4): memory carries bi-temporal validity (observed_at, valid_from/until) + a state model (active/superseded/archived); Reconcile INVALIDATES the older assertion (never deletes), reversible. Prevents divergent lifecycle/conflict handling.
- (decision) AD-SECURITY (F6): BYOK via orvex-studio-identity/KMS seam, per-tenant keys, enforced at the store layer; firewall + per-use consent threaded end-to-end (capture→store→retrieve→deliver→sync). Prevents inconsistent enforcement points.
- (decision) AD-CONTRACTS: memory card schema, the delivery-adapter port, and MCP additions are pinned in orvex-studio-contracts (OpenAPI + CloudEvent catalog); cross-service memory events ride studio-spine. Prevents drift across independently-built services.
- (decision) AD-DELIVERY refined (F1, web-verified): paradigm = user-triggered, WRITE-ONLY, single-session compose-box insertion (product composes prompt+memory, inserts into the user's own input field on explicit action; never auto-submits, never parses model Output). Seam = narrow DeliveryAdapter: composeInto(target,text)->{ok|needs-manual-paste}, core is mechanism-agnostic. ToS-clean invariant (enforced at seam): user's authenticated session only; explicit per-use action; input-field write only, no output scraping; no auto-Send without separate opt-in — legally autofill, not a bot. Comparables MemoryPlugin/mem0/Supermemory all use MV3 content-script this way.
- (version) MV3 content-script CSP (2026): no remote code / no server hot-patch of broken selectors without a Chrome Web Store resubmission; vendor UI changes are a leading cause of breakage → FR-D7 breakage-detection + fast point-release cadence + resilient hooks (role=textbox/aria) is an operational invariant, not optional.
- (constraint) Capture ToS boundary: output-parsing capture (MemoryPlugin marker-line) is the risky path; Orvex capture stays explicit user 'save this' + the Librarian confirm — reinforces AD-CAPTURE.
- (event) Reviewer gate: tech-currency=CURRENT (Turbopuffer ns + MV3 confirmed; added CWS 2026-08-01 AI-chatbot-injector policy + Limited-Use re-check to F1 gate/AD-6). Adversary=HOLES-FOUND(6), all closed: AD-8 consent-at-mint token, AD-5 split into 3 delivery surfaces (MCP not an adapter), AD-11 per-card ordering+read-your-writes, AD-7 namespace-key=owner_scope, AD-4 all-mutations-through-gate + atomic supersede, AD-3 valid_from=world-time. OQ3 now gates delivery surface (c).
- (event) spine finalized
- (event) Spine routed to wiki as DRAFT architecture page iiCcKhGptV (child of PRD g9vWbSYplh); 2 mermaid diagrams landed as codeBlocks under ## Diagrams; verify render OK. NOT ratified — blocked on the AD-1 Memory-ownership conflict ruling (needs Daniel). Next: fold-in (amend ai arch per AD-1) → update phases.
- (decision) Daniel RULED (2026-07-14): ADOPT AD-1 — api owns Memory SoR /v1/memory; ai=extraction+renamed chat-recall; knowledge=projection. Fold-in MUST amend canonical Architecture: orvex-studio-ai (Cb0XBkNezd). Proceed to fold-in + update phases.
- (event) FOLD-IN COMPLETE: AD-1 boundary appended to ai arch Cb0XBkNezd (force-self-ratify); owned-slice sections appended to 5 service PRDs (ai pbKI3BpQmY, knowledge dCbFzRQGDr, api 85qj2wwU2L, identity cnhla0qRRF, mcp k1sWjtJq3x); Memory Gap-Closure epic set appended to Delivery Program 5eFdxN3edd; Fold-in Map page vBvVDFklZo created (draft). Next: bmad-create-epics-and-stories over the map.
- (event) NO-LOSS MERGE done (Daniel-directed): all 43 FRs + 12 ADs sliced by owner and merged VERBATIM into the 5 service PRDs (FRs) + 5 architectures (ADs); coverage-checked 43/43 + 12/12, zero dropped. Delivery-client FRs + F0 UX → fold-in map (new service home). Phase placement appended to Delivery Program 5eFdxN3edd: Phase 1 definition (Service Definition Packs) → Phase 2 later build wave → 2.5 acceptance; F1 spike early Phase 1; nothing in Phase 0 (done). Umbrella PRD/spine tagged cross-cutting view; services authoritative.
