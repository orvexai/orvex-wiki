
## Memory gap-closure — owned slice (fold-in)

Per PRD `g9vWbSYplh` + spine `iiCcKhGptV` (map `vBvVDFklZo`), orvex-studio-identity owns: **F6 BYOK** (FR-S1–3 — customer-managed keys via KMS, a SOC 2/HIPAA path, BYOC), the **personal↔employer firewall + consent** (FR-S5), and **AD-8** — consent binds at the **mint boundary**: a single-target, single-use, short-TTL **delivery token** scoped `{memory,target,user}` that the delivery client must present to compose. The untrusted client compose is never itself a trust boundary.
