
## Memory gap-closure — owned slice (fold-in)

Per PRD `g9vWbSYplh` + spine `iiCcKhGptV` (map `vBvVDFklZo`), orvex-studio-knowledge owns: the **memory retrieval projection** (AD-1c, read-only), the **F5 retrieval eval harness** (FR-E1–4 — recall@10 ≥ 0.80, regression gate, raw wiki kept searchable), **AD-7** per-user Turbopuffer namespace keyed by `owner_scope`, and **AD-11** projection consistency (`memory.*` partitioned by card-id, monotonic version, read-your-writes for the confirming user).
