
## Memory gap-closure — owned slice (fold-in)

Per PRD `g9vWbSYplh` + spine `iiCcKhGptV` (map `vBvVDFklZo`), orvex-studio-ai owns: **F2 proposal/extraction quality** (FR-X1–6 — a measurable precision/recall bar, the beads→staging capture edge, async off the write path), **F5 proposal-quality eval**, and **F8 token-cost** (FR-C1–3 — token-budgeted injection, ≥50% reduction). It owns extraction *compute* + **chat-recall** (FR-AI11); the product Memory SoR is api (AD-1). Budget via AD-10 (a separate nested extraction budget).
