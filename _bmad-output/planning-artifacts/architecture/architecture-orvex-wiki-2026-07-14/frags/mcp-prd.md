
## Memory gap-closure — owned slice (fold-in)

Per PRD `g9vWbSYplh` + spine `iiCcKhGptV` (map `vBvVDFklZo`), orvex-studio-mcp exposes the **MCP memory retrieval surface** (AD-5b) — memory read as a `knowledge` tool call under the caller's delegated principal (I-4), **not** a write/compose adapter. The client compose port (browser extension, AD-5a) and stateful sync (AD-5c) are separate surfaces; the DeliveryAdapter contract is pinned in orvex-studio-contracts. **New component:** the F1 browser-extension delivery client has no service home yet — it needs a new repo (gated on the F1 viability spike, OQ8).
