# actionlint config — declares the org's self-hosted runner labels
# (CS §13 substrate; the family runs CI on the self-hosted `runners` label,
# with dind-* variants for docker-needing jobs).
self-hosted-runner:
  labels:
    - runners
    - dind-runners
    - dind-runners-large
